function @main() -> i32 [role=entry, binding=strong, keep_alias=yes] {
  slot $empty : obj<1x1>
  slot $one : obj<1x1>

  block ^entry:
    %t1 = addr $empty
    %t2 = addr $one
    %t3 = addr $empty
    %t4 = addr $one
    return i32 0
}
