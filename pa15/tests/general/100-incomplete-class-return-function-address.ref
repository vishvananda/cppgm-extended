declare function @make_result() -> void [binding=strong, object=_Z11make_resultv]

global @stored : ptr [binding=strong, object=_Z6stored] = zero

function @__cppgm_init() -> void [role=init, binding=internal] {
  block ^entry:
    %t1 = addr @make_result
    store ptr %t1, @stored
    return void
}
