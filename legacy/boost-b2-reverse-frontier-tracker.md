# Boost B2 Reverse Frontier Tracker

Base commit: `8da321ead64daa6d51e13f7d4f3251a19f28e1f8`

Worktree: `/Users/vishvananda/cppgm-boost-b2-frontier-reverse`

Branch: `codex/boost-b2-frontier-reverse-20260512`

Lane rule: start from the bottom of
`docs/boost-b2-suite-status-20260511.md`, skip passing suites, and move
downward. Do not duplicate the forward lane's active Fusion/libc++ char_traits
work.

| Status | Suite / target | Failure signature | Reducer | Earliest PA / regression | Validation | Commit | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| fixed | `libs/xpressive/test//test2` | Boost.Optional `boost/none_t.hpp` stopped at `#error "Boost.Optional requires some C++11 features..."` because Boost.Config saw `BOOST_NO_CXX11_EXPLICIT_CONVERSION_OPERATORS`; `cppgm++` already accepted explicit conversion operators, but `__has_feature(cxx_explicit_conversions)` / `__has_extension(cxx_explicit_conversions)` returned false | `/tmp/cppgm-boost-reducers-20260511/has_feature_explicit_conversions_nostl.cpp` | PA33 / `pa33/tests/preproc/400-has-feature-cxx11-core.t` | `make -C dev -j12 cppgm++`; reducer passed with clang and `dev/cppgm++`; `ACTIVE_TEST_REPORT_PAS='pa33' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `253 / 253`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` advanced to separate Boost.Proto `typedef Expr::proto_tag type` member-collection failure in `boost/proto/traits.hpp:241`; perf check against `/tmp/cppgm-perf-baseline-reverse-8da321ea-20260512.json` passed: instructions `+0.08%`, RSS `+1.72%`, footprint `+0.06%` | same commit | Temporary reverse B2 toolset version uses `gcc-cppgmreverse`; B2 rejected the hyphenated handoff version `gcc-cppgm-reverse` as an unknown gcc subfeature before any target ran |
| fixed | `libs/xpressive/test//test2` | Boost.Proto partial specialization `template<typename Cases> struct switch_<Cases> : transform<switch_<Cases> >` forced the primary default argument `tag_of<_>()`, instantiating `tag_of<_>` and failing on `typedef typename Expr::proto_tag type` while only collecting the partial specialization | `/tmp/cppgm-boost-reducers-20260511/partial_specialization_default_function_type_no_eager.cpp` | PA21 / `pa21/tests/general/400-partial-specialization-default-function-type-no-eager.t` | `make -C dev -j12 cppgm++`; reducer passed with clang and `dev/cppgm++`; `ACTIVE_TEST_REPORT_PAS='pa21' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `141 / 141`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` advanced to separate `using proto_extends::operator=` lookup failure in `boost/xpressive/regex_primitives.hpp:539`; perf check against `/tmp/cppgm-perf-baseline-reverse-8da321ea-20260512.json` passed: instructions `-0.27%`, RSS `-0.80%`, footprint `+0.03%` | same commit | The fix preserves source text for defaulted type arguments during trailing default normalization before falling back to semantic type parsing, avoiding premature instantiation of function-type default return types |
| fixed | `libs/xpressive/test//test2` | Boost.Xpressive `mark_tag` expands `BOOST_PROTO_EXTENDS_USING_ASSIGN_NON_DEPENDENT(mark_tag)` to `typedef mark_tag::proto_extends proto_extends; using proto_extends::operator=;`; during reference-member collection `proto_extends` was still a deferred current-class alias, so the using-declaration could not find the inherited Proto assignment operator templates | `/tmp/cppgm-boost-reducers-20260511/using_inherited_alias_operator_template_nostl.cpp` | PA18 / `pa18/tests/spec/100-using-inherited-alias-operator-template.t` | `make -C dev -j12 cppgm++`; reducer passed with clang and `dev/cppgm++`; `ACTIVE_TEST_REPORT_PAS='pa18' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `174 / 174`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` advanced past `using proto_extends::operator=`; direct compile with the same Boost arguments now reports separate duplicate class definition for `fill_t` in `boost/xpressive/detail/utility/sequence_stack.hpp:25`; perf check against `/tmp/cppgm-perf-baseline-reverse-8da321ea-20260512.json` passed: instructions `-0.23%`, RSS `-1.77%`, footprint `+0.05%` | same commit | The reverse B2 wrapper still hides this next diagnostic in the target log; running `dev/cppgm++` directly with the same arguments shows the duplicate-class frontier |
| fixed | `libs/xpressive/test//test2` | Boost.Xpressive `BOOST_INLINE_CONSTEXPR struct fill_t {} fill = {};` in `boost/xpressive/detail/utility/sequence_stack.hpp` was included through a nonprimary source; the native compile static-storage probe collected the embedded namespace-scope class before normal declaration collection, then the real declaration path diagnosed a duplicate `fill_t` definition | `/tmp/cppgm-boost-reducers-20260511/nonprimary_embedded_class_inline_var.cpp` | PA33 / `pa33/tests/compile/700-nonprimary-embedded-class-inline-var.t` | `make -C dev -j12 cppgm++`; reducer passed with clang and `dev/cppgm++`; `ACTIVE_TEST_REPORT_PAS='pa33' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `254 / 254`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` advanced to separate semantic fallback for overloaded `operator|` at `libs/xpressive/test/./test2.hpp:21:44`; perf check against `/tmp/cppgm-perf-baseline-reverse-8da321ea-20260512.json` passed: instructions `+0.26%`, RSS `-1.19%`, footprint `+0.01%` | same commit | The fix disables embedded-type collection in the nonprimary static-storage declaration probe so the probe remains read-only with respect to class collection |
| fixed | `libs/xpressive/test//test2` | Boost.Proto `operator|` return type instantiates `deduce_domain2` / `common_domain2`; the default non-type template argument `sizeof(proto::detail::default_test((domain_<D0>*)0, (domain_<D1>*)0))` parsed its call expression with synthetic token ordering, so namespace function visibility filtering dropped the prior `default_test` overloads and the operator template was removed from overload resolution | `/tmp/cppgm-boost-reducers-20260511/default_nttp_qualified_function_lookup.cpp` | PA21 / `pa21/tests/general/410-default-nontype-qualified-function-lookup.t` | `make -C dev -j12 cppgm++`; reducer passed with clang and `dev/cppgm++`; Boost-header `operator|` probe passed with `dev/cppgm++`; `ACTIVE_TEST_REPORT_PAS='pa21' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `142 / 142`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` advanced to separate semantic fallback for overloaded `operator>>` at `libs/xpressive/test/./test2.hpp:15:1`; perf check against `/tmp/cppgm-perf-baseline-reverse-8da321ea-20260512.json` passed: instructions `-0.04%`, RSS `-1.06%`, footprint `-0.01%` | same commit | The fix makes namespace-function source-order filtering honor `parser_trace::current_order_use_location()` while evaluating default template arguments, preserving declaration-time lookup without trusting copied expression token indices |
| fixed | `libs/xpressive/test//test2` | Boost.Proto `operator>>` viability used `lazy_matches<result_of::make_expr<tag::shift_right, basic_default_domain, Left, Right>, regex_domain::proto_grammar>`; `regex_domain` captured `address_of<any>` while `address_of<T>` was only forward-declared, so later matching could not resolve `typename Grammar::proto_grammar` from the completed primary template and removed the operator template from overload resolution | `/tmp/cppgm-boost-reducers-20260511/forward_template_reference_member_alias_nostl.cpp` | PA21 / `pa21/tests/general/411-forward-template-reference-member-alias.t` | `make -C dev -j12 cppgm++`; no-STL reducer passed with clang and `dev/cppgm++`; Boost-header Proto domain grammar probe passed; direct xpressive shift probe advanced to `unsupported enumerator value`; `ACTIVE_TEST_REPORT_PAS='pa21' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `143 / 143`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` log `/tmp/boost-reverse-frontier-20260512/libs__xpressive__test__test2_after_forward_template_reference_alias.log` no longer contains the old `operator>>` / `lazy_matches` signature and stops at `unsupported enumerator value` in `boost/xpressive/basic_regex.hpp:117:52`; perf check against `/tmp/cppgm-perf-baseline-reverse-8da321ea-20260512.json` passed: instructions `-0.20%`, RSS `-0.83%`, footprint `-0.06%` | same commit | The fix refreshes forward class-template selections before collecting reference members, so reference instantiations based on a forward declaration can see member typedefs from the later primary definition |
| fixed | `libs/xpressive/test//test2` | Boost.Xpressive static grammar reached `boost/xpressive/basic_regex.hpp:117:52` and diagnosed `invalid regex` because Proto matching selected incomplete/default-truncated partial-specialization shapes while evaluating recursive `switch_` grammars; the compiler had to treat template-template arity mismatch as deduction failure, expand defaulted class-template arguments for partial matching, compare using-declared template names by resolved entity, and avoid the dependent forward-primary shortcut when partial specializations exist | `/tmp/cppgm-boost-reducers-20260511/template_template_arity_incomplete_partial_nostl.cpp`; `/tmp/cppgm-boost-reducers-20260511/forward_primary_partial_switch_value_nostl.cpp` | PA21 / `pa21/tests/general/412-template-template-arity-incomplete-partial.t`; PA21 / `pa21/tests/general/413-forward-primary-partial-switch-value.t` | `make -C dev -j12 cppgm++`; no-STL reducers passed with clang and `dev/cppgm++`; Xpressive grammar probes passed with `dev/cppgm++`; `ACTIVE_TEST_REPORT_PAS='pa21' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `145 / 145`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` log `/tmp/boost-reverse-frontier-20260512/libs__xpressive__test__test2_after_effective_arg_index.log` no longer contains the old `basic_regex.hpp:117` invalid-regex diagnostic and advances to `ERROR: unknown function make_adaptor<matchable_ex<BidiIter>>` at `boost/xpressive/detail/static/compile.hpp:44`; perf check against `/tmp/cppgm-perf-baseline-reverse-8da321ea-20260512.json` passed: instructions `-0.14%`, RSS `-2.55%`, footprint `-0.08%` | same commit | The focused B2 target also has an unrelated Boost.Test `framework.o` failure in the same run; the Xpressive object advanced to the `make_adaptor` lookup frontier |
| fixed | `libs/xpressive/test//test2` | Boost.Xpressive `compile.hpp:44` failed `make_adaptor<matchable_ex<BidiIter>>`; the path depended on Boost.Proto `child_c<Expr const &, 1>` partial ordering, `as_matcher::impl` current-class lookup finding `impl::data` through an in-progress base, qualified `this->Matcher::match(...)` preserving preselected member-template candidates, and libc++/Clang-style `__builtin_popcountg` support | `/tmp/cppgm-boost-reducers-20260511/child_c_constref_min.cpp`; `/tmp/cppgm-boost-reducers-20260511/dependent_nested_impl_result_type_probe.cpp`; `/tmp/cppgm-boost-reducers-20260511/static_member_template_dependent_qualified_no_forward.cpp`; `/tmp/cppgm-boost-reducers-20260511/forward_class_static_member_match_nostl.cpp`; `/tmp/cppgm-boost-reducers-20260511/builtin_popcountg_nostl.cpp` | PA21 / `pa21/tests/general/414-partial-specialization-reference-cv-nontype.t`; PA21 / `pa21/tests/general/415-reference-member-lookup-in-progress-base-typedef.t`; PA21 / `pa21/tests/general/416-qualified-preselected-member-template-call.t`; PA33 / `pa33/tests/preproc/400-has-builtin-popcountg.t`; PA33 / `pa33/tests/compile/710-builtin-popcountg.t` | `make -C dev -j12 cppgm++`; promoted reducers passed with clang and `dev/cppgm++`; direct Xpressive `make_adaptor` probe passed with `dev/cppgm++`; `ACTIVE_TEST_REPORT_PAS='pa21 pa33' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `404 / 404`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` log `/tmp/boost-reverse-frontier-20260512/libs__xpressive__test__test2_after_make_adaptor_static_match_popcountg_final.log` no longer contains the old `make_adaptor`, `assert_bos_matcher::match`, or `__builtin_popcountg` diagnostics and advances to a diagnostic-free `test2.o` failure; direct replay of the same compile exits `SIGSEGV` (`139`) with output in `/tmp/boost-reverse-frontier-20260512/test2_direct_after_make_adaptor_static_match_popcountg_final.out`; perf check against `/tmp/cppgm-perf-baseline-reverse-8da321ea-20260512.json` passed: instructions `-0.67%`, RSS `-2.34%`, footprint `+0.00%` | same commit | The containing Boost.Test and Regex dependencies built in the focused target; the next frontier is the full `libs/xpressive/test/test2.cpp` compiler segfault after the smaller Xpressive static probe succeeds |
| fixed | `libs/xpressive/test//test2` | Remaining `make_adaptor<matchable_ex<BidiIter>>` argument analysis failed while instantiating Boost.Proto `if_`: the boolean non-type argument `static_cast<bool>(remove_reference<condition>::type::value)` treated `remove_reference<condition>` as a failed boolean trait and stopped before evaluating the intermediate qualifier's `::type::value` | `/tmp/cppgm-boost-reducers-20260511/intermediate_type_transform_value_nttp_nostl.cpp` | PA21 / `pa21/tests/general/417-intermediate-type-transform-value-nontype.t` | `make -C dev -j12 cppgm++`; reducer passed with clang and `dev/cppgm++`; direct Boost.Proto `if_` probe passed; minimal Xpressive `!as_xpr('+')` probe passed; `ACTIVE_TEST_REPORT_PAS='pa21' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `149 / 149`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` log `/tmp/boost-reverse-frontier-20260512/libs__xpressive__test__test2_after_intermediate_type_transform.log` no longer contains the `make_adaptor` or failed `remove_reference<condition>::type::value` signature and advances to `ERROR: invalid member initializer` in `boost::xpressive::grammar_detail::as_marker::impl::operator()`; direct full `test2.cpp` compile still exits `SIGSEGV` (`139`) with empty output in `/tmp/boost-reverse-frontier-20260512/test2_direct_after_intermediate_type_transform.out`; perf check passed: instructions `-0.45%`, RSS `+1.01%`, footprint `+0.03%` | same commit | This finishes the semantic failure that was hidden by the direct full-file crash path; Boost.Test `framework.o` remains an unrelated focused-target prerequisite failure in the same B2 run |
| fixed | `libs/xpressive/test//test2` | Boost.Xpressive `as_marker::impl::operator()` failed `result_type that = {{begin}, {proto::right(expr), {end}}};` with `ERROR: invalid member initializer`; aggregate appertainment greedily brace-elided `proto::right(expr)` plus the following marker into the nested Proto `bitwise_or` aggregate instead of first accepting the direct copy-initialization of that aggregate field, and the named-return-slot LowIR path then treated trivial subobject constructor actions as whole-object initialization | `/tmp/cppgm-boost-reducers-20260511/nested_aggregate_reference_member_copy.cpp`; `/tmp/cppgm-boost-reducers-20260511/xpressive_marker_optional_alt_probe.cpp` | PA15 / `pa15/tests/general/201-aggregate-appertainment-direct-copy-reference-members.t` | `make -C dev -j12 cppgm++`; no-STL reducer passed with clang and `dev/cppgm++` native execution (`exit=0`); Boost-header marker probe passed with clang and `dev/cppgm++`; `make -C pa15 check TEST=tests/general/201-aggregate-appertainment-direct-copy-reference-members.t` passed; `ACTIVE_TEST_REPORT_PAS='pa15' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `163 / 163`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed; focused Boost `libs/xpressive/test//test2` log `/tmp/boost-reverse-frontier-20260512/libs__xpressive__test__test2_after_aggregate_appertainment.log` no longer contains the old `invalid member initializer` signature and advances to ambiguous `regex_search` overloads in `boost/xpressive/regex_algorithms.hpp:503` and `:533`; perf check passed: instructions `-0.27%`, RSS `-0.74%`, footprint `-0.10%` | same commit | Boost.Test `decorator.o` and `framework.o` still fail as unrelated focused-target prerequisites in the same B2 run; the next Xpressive object frontier is ordinary overload resolution for `regex_search(this->str_, what, this->rex_)` in `libs/xpressive/test/test.hpp:101` |
| fixed | `libs/xpressive/test//test2` | Boost.Xpressive `regex_search(this->str_, what, this->rex_)` reported ambiguous range overloads because `BidiRange&` and `BidiRange const&` both instantiated to `std::string const&` through an alias-template parameter path; ordinary function-template partial ordering had no remaining distinction, but the original source reference pattern for a const lvalue must prefer the `const&` overload | `/tmp/cppgm-boost-reducers-20260511/cv_lvalue_ref_alias_template_param_cv_fallback_nostl.cpp`; `/tmp/cppgm-boost-reducers-20260511/xpressive_regex_search_const_string_probe.cpp` | PA21 / `pa21/tests/general/418-function-template-reference-cv-alias-partial-order.t` | `make -C dev -j12 cppgm++`; no-STL reducer passed with `dev/cppgm++` native execution; PA21 focused regression passed; `ACTIVE_TEST_REPORT_PAS='pa21' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `150 / 150`; `CPPGM_SKIP_DEV_REBUILD=1 CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=12 make test-strict` passed; Boost-header probe and direct full `libs/xpressive/test/test2.cpp` replay no longer contain the old ambiguous `regex_search` signature and advance to `ERROR: base class must be complete: conditional<bool(T::value),T,disjunction<U...>>::type` in Boost.Optional/Boost.TypeTraits; focused Boost `libs/xpressive/test//test2` log `/tmp/boost-reverse-frontier-20260512/libs__xpressive__test__test2_after_ref_cv_alias.log` fails at `test2.o` without the old ambiguity; perf check passed: instructions `-0.03%`, RSS `-0.34%`, footprint `-0.03%` | this commit | The forward lane's nearby `c54a72b7` fixes a different ref-cv surface in function-template redeclaration matching; this reverse fix is overload ranking after both candidates survive |
| fixed | `libs/xpressive/test//test2` | Boost.Optional/TypeTraits `disjunction` produced an incomplete base for `conditional<bool(T::value),T,disjunction<U...>>::type` because the concrete `T::value` came from a substituted `||` expression whose operands still looked syntactically dependent through member-type syntax | `/tmp/cppgm-boost-reducers-20260511/namespaced_disjunction_conditional_or_identity_short.cpp`; `/tmp/cppgm-boost-reducers-20260511/boost_optional_disjunction_base_probe.cpp` | PA21 / `pa21/tests/general/419-bool-or-dependent-member-type-conditional-base.t` | `make -C dev -j12 cppgm++`; shrunken no-STL reducer and Boost-header Optional probe passed with `dev/cppgm++`; PA21 focused regression passed; `ACTIVE_TEST_REPORT_PAS='pa21' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `151 / 151`; `CPPGM_SKIP_DEV_REBUILD=1 CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=12 make test-strict` passed; focused Boost `libs/xpressive/test//test2` log `/tmp/boost-reverse-frontier-20260512/libs__xpressive__test__test2_after_bool_or_dependency.log` no longer contains the old Optional base-completion diagnostic and `test2.o` advances to the existing diagnostic-free direct compile `SIGSEGV` (`139`); perf check passed: instructions `-1.14%`, RSS `+1.92%`, footprint `-0.08%` | this commit | The promoted reducer was shrunk from the larger Boost.Optional scaffold to a no-STL trait/disjunction shape; attempts with only one dependent-looking `||` operand passed, so the kept reducer preserves the failing two-operand path |
| fixed | `libs/xpressive/test//test2` | Diagnostic-free `SIGSEGV` while mangling Xpressive global regex/Proto symbols; `TemplateParameterInfo::owned_syntax` was shared by shallow copies, so later spelling updates could reset the shared owned AST and leave earlier raw syntax pointers dangling. The observed corrupted node was Boost.Range `range_size<C, bool B = ...>`'s non-type decl-specifier syntax | `/tmp/cppgm-boost-reducers-20260511/xpressive_range_global_char_ptr_regex.cpp` | External Boost-header reducer; no no-STL PA regression isolated | `make -C dev -j12 cppgm++`; reducer passed with clang, crashed with the pre-fix compiler (`SIGSEGV` 139), and with `dev/cppgm++` no longer segfaults, advancing to `std::__1::__count_bool` alias-declaration; `ACTIVE_TEST_REPORT_PAS='pa21' CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-report` passed `151 / 151`; `CPPGM_SKIP_DEV_REBUILD=1 CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=12 make test-strict` passed; focused Boost `libs/xpressive/test//test2` log `/tmp/boost-reverse-frontier-20260512/libs__xpressive__test__test2_after_template_parameter_deepcopy.log` advances to `unsupported alias-declaration` in libc++ `__algorithm/count.h:49`; perf check passed: instructions `-0.69%`, RSS `+0.37%`, footprint `+0.55%` | this commit | The reducer was shrunk from the full `test2.cpp`/14-line `std::string` regex probe to a 5-line Boost-header case using `basic_regex<char const *>`; the fix deep-copies owned template-parameter syntax on copy and preserves efficient moves |
