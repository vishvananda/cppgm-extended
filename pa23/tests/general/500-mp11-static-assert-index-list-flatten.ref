function @main() -> i32 [role=entry, binding=strong, keep_alias=yes] {
  slot $c : obj<1x1>
  slot $indices : obj<1x1>

  block ^entry:
    %t1 = addr $c
    %t2 = addr $indices
    %t3 = addr $c
    %t4 = addr $indices
    return i32 0
}
