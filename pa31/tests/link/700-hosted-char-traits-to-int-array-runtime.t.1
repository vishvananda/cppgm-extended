#include <cstdio>
#include <string>

int main()
{
  char data[4] = {0, 1, 2, 3};
  int total = 0;
  for(int i = 0; i != 4; ++i) {
    total = total * 10 + std::char_traits<char>::to_int_type(data[i]);
  }
  std::printf("%d\n", total);
  return total == 123 ? 0 : 1;
}
