template<class T> using decay_alias = __decay(T);

template<class T>
decay_alias<T> decay_return(T *value)
{
  return *value;
}

int main()
{
  int value = 7;
  return decay_return(&value) == 7 ? 0 : 1;
}
