# Boost B2 Frontier Tracker - 2026-05-19

This tracker starts from `origin/fix-lowir-external-lifecycle-perf` and the
rebased Boost frontier branch.

## Baseline

- base branch: `fix-lowir-external-lifecycle-perf`
- base commit: `529454c61befdf931373f6dc32b81983ec0d7390`
- rebased frontier head at tracker start:
  `8c9e3ce62fc95e6b8b88ebbe854f05fadae3627c`
- safety branch:
  `backup/boost-frontier-before-lowir-lifecycle-rebase-20260519-072414`
- perf baseline:
  `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json`
- rebased stack perf check: passed, instructions `-14.88%`, RSS `-2.77%`,
  footprint `-5.72%`
- rebased-head perf baseline: recorded at branch head and used as the baseline
  for subsequent Boost frontier fixes
  `6735bc6cf22ab9de04de945a25cf6583235acb92`, compiler-equivalent to the
  rebased frontier head; median instructions `312,198,629,210`, RSS `1.10 GiB`,
  footprint `856.84 MiB`
- active perf baseline: `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json`
  recorded at `c4b544837865a4464f268511ff23e03e0ff38a37` after accepting the
  current rebased frontier head as the new baseline; median instructions
  `312,426,043,980`, RSS `1.08 GiB`, footprint `857.18 MiB`

Local Boost wrapper state:

- `/Users/vishvananda/boost_1_91_0/cppgm-b2-toolchain.sh` points at this
  worktree's `dev/cppgm++`.
- The wrapper routes C and assembly compile/preprocess actions to host clang,
  C++ compile/preprocess actions to `cppgm++`, and link actions to host clang++.

## Sequential Process

1. Run Boost suites in the order listed in
   `docs/boost-b2-suite-status-20260511.md`.
2. Use one suite at a time with B2 compile parallelism, for example:

   ```sh
   cd /Users/vishvananda/boost_1_91_0
   JOBS=12 ./run-cppgm-b2.sh -a libs/accumulators/test
   ```

3. If the suite passes, mark it in this tracker and continue to the next suite.
4. If it fails, identify the first real compiler frontier, reduce it to the
   smallest useful source, and prefer a non-STL reducer when possible.
5. Place the regression in the earliest owning PA and first correct cluster.
   Use `ROADMAP.md`, PA READMEs, the reducer's essential assertion, and:

   ```sh
   python3 scripts/audit_pa_feature_placement.py --pa paNN \
     --markdown-out /tmp/boost-frontier-placement-paNN.md \
     --json-out /tmp/boost-frontier-placement-paNN.json \
     --fail-on-early
   ```

   Include every touched PA. Course tests for PA1-PA9 belong under
   `cppgm.tests/course/pa*` unless the reducer is a hosted C++ extension, in
   which case use the hosted compatibility PAs.
6. Before implementation changes, confirm the reducer fails on the current
   branch and record the pre-fix diagnostic here.
7. Fix the compiler in `dev/`, validate the focused regression, the owner PA
   report, strict LowIR compare when relevant, and the Boost target.
8. Run the perf gate against the baseline above before committing any compiler
   fix.
9. Commit each coherent fix with the regression, implementation, and tracker
   update before moving to the next unrelated frontier.

## Suite Cursor

| # | Suite | Status | Notes |
|---|---|---|---|
| 1 | `libs/accumulators/test` | pass | `JOBS=12`, 9s, log `/tmp/boost-frontier-seq-20260519-073659-j12-6735bc6cf/libs__accumulators__test.log`. |
| 2 | `libs/algorithm/test` | pass | `JOBS=12`, direct run, updated 218 targets after integral NTTP value-binding conversion fix. Expected negative tests still fail as expected under B2. |
| 3 | `libs/align/test` | pass | `JOBS=12`, direct run, updated 60 targets after Apple hosted compiler macro import. |
| 4 | `libs/any/test` | pass | `JOBS=12`, direct run after cv-correct rvalue-reference binding fix updates 164 targets. The previous runtime failures pass, and the negative compile tests including `unique_const_rvalue_construction_failed.cpp` now fail as expected under B2. |
| 5 | `libs/array/test` | pass | `JOBS=12`, direct run after braced array reference argument lowering fix; `to_array_test` and full suite pass. |
| 6 | `libs/asio/test` | frontier | Initial suite run exposed 211 `unsupported out-of-class conversion operator` failures through Boost.System. After fixing qualified conversion-operator binding, inherited member class-template lookup, function-type tail pack expansion, recursive supportable-property member-template owner binding, constructor SFINAE trait evaluation, the hidden-friend `query_free` probe, explicit function-pointer NTTP calls, trailing-pack target deduction for function-template addresses, pointer/reference explicit type-argument resolution, class-reference `static_cast` through a converting constructor, host EH declaration reuse, null nothrow-new initialization, virtual dispatch through a dereferenced member pointer, ambiguous member-pointer NTTP SFINAE, member-template trailing-return implicit object context, constexpr static function-table construction, hidden-friend SFINAE lexical scope protection, dependent-return hidden-friend specialization scoping, in-progress reference class instantiation reuse, indirect class call-result materialization, logical-not builtin fallback after rejected overloads, class array-new construction for trivially destructible elements, Darwin `__builtin_bzero` host-libcall compatibility, function-template parameter-clause scope fallback for later parameter types that depend on earlier parameter names, and non-type pack static-data initializer binding, `libs/asio/test//any_completion_executor`, `libs/asio/test//any_completion_handler`, and `libs/asio/test//any_completion_handler_select` pass; `libs/asio/test//basic_datagram_socket` now advances to a `use_service` member-expression frontier in `boost/asio/impl/io_context.hpp`. |

## Fix Ledger

| Status | Frontier seed | Root cause | Owner PA | Regression | Pre-fix result | Validation | Fix commit |
|---|---|---|---|---|---|---|---|
| committed | `libs/algorithm/test` dependency build via Boost.Test/Boost.Regex; libc++ `std::__1::vector` iterator conversion needed `__wrap_iter<T*>` to `__wrap_iter<T const*>` | Structural alias-template expansion handled a dependent alias before preserving an outer `TK_CV`, so `const remove_reference_t<T> &` with `T=item &` collapsed to `item &` during defaulted SFINAE checks. | `pa22:500` | `pa22/tests/general/500-const-reference-alias-default-sfinae.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/alias_const_lvalue_ref_default_arg.cpp` failed with explicit type argument fallback / SFINAE rejection; STL reducer `/tmp/cppgm-boost-reducers-20260519/vector_const_iterator.cpp` failed to compile `vector::erase(v.begin())` / `insert(end(), begin(), end())`. | Reducers pass; focused LowIR ref matches; placement audit OK; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `407/407`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/algorithm/test` advances to subrange failures; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.14%` instructions). | `091da7fe6` |
| committed | `libs/algorithm/test` subrange targets; libc++ `std::default_random_engine` instantiates `__lce_ta<__a, __c, __m, _Mp>` from `linear_congruential_engine<unsigned int, ...>` | Non-type template argument resolution required exact integral type equality for named value bindings. A bound `unsigned int` constant could not be reused as an `unsigned long long` NTTP even when representable, so libc++'s `__lce_ta` helper failed when a value binding such as `__c` was carried semantically rather than as a syntax expression. | `pa19:100` | `pa19/tests/general/100-nontype-integral-value-binding-conversion.t` | Pre-fix compiler rejected `/tmp/cppgm-boost-reducers-20260519/nontype_integral_conversion_value_binding.cpp` with `static_assert unevaluated`; Boost `partition_subrange_test.o` and `sort_subrange_test.o` failed while instantiating `__lce_ta<__a,__c,__m,_Mp>`. | Reducer passes; placement audit OK for `pa19:100`; `ACTIVE_TEST_REPORT_PAS='pa19' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `100/100`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa19` passes with `compared=92`; direct subrange compiles pass; `JOBS=12 ./run-cppgm-b2.sh -a libs/algorithm/test` passes, updating 218 targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.53%` instructions). | `e0b2dac73` |
| committed | `libs/align/test` targets including `aligned_alloc_test.o`, `aligned_delete_test.o`, and aligned allocator tests include Apple `TargetConditionals.h` through hosted headers. | Host predefined macro import filtered out `__APPLE_CC__`, so Apple `TargetConditionals.h` saw `__GNUC__` without the Apple compiler-identity macro it requires and took its `unknown compiler` error branch. | `pa34:500` | `pa34/tests/compile/500-host-apple-target-conditionals-macro.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/apple_target_conditionals.cpp` failed with `#error TargetConditionals.h: unknown compiler`; `libs/align/test` had four compile failures with the same diagnostic. | Reducer passes; new test passes via `make -C pa34 check TEST=tests/compile/500-host-apple-target-conditionals-macro.t`; PA34 placement audit row for the new test is owner-reached, with unrelated existing PA34 cluster warnings still present; `ACTIVE_TEST_REPORT_PAS='pa34' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `294/294`; `JOBS=12 ./run-cppgm-b2.sh -a libs/align/test` passes, updating 60 targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.37%` instructions). | `03a278f97` |
| committed | `libs/any/test` `basic_tests<Any>::copy_counter::count` in `libs/any/test/basic_test.hpp` | The template declaration collector only recognized out-of-class static member definitions when the immediate qualifier was a template-id and resolved the owner path without the template pattern scope. Instantiation replay and output also assumed direct static member keys and source-template classes, so nested non-template class static storage inside a class template was not replayed or emitted. | `pa21:400` | `pa21/tests/general/400-template-nested-static-member-out-of-class-definition.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/nested_template_static_member_definition.cpp` failed with `failed type template argument resolution: T [scope n::<here>] ... lookup_type [n::outer<T>]`; after the collection path was corrected, the same reducer linked with an undefined `n::outer<int>::inner::value` until nested static definition replay/output was added. | Reducer compile/link/run passes; direct static-member control passes; placement audit OK for `pa21:400`; focused LowIR ref matches; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `175/175`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa21` passes with `compared=164`; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to separate `any_cast` compile failures and `unique_any` runtime failures; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.31%` instructions). | `91c14c0a2` |
| committed | `libs/any/test` `boost::any_cast<std::string>(returning_string1())`; Boost `any` has both `any(const T&)` and a forwarding `any(T&&, enable_if... = 0, enable_if... = 0)` constructor | Constructor-template overload selection compared instantiated defaulted `enable_if` parameters when applying the forwarding-reference tie-breaker. The selected candidate lists had different lengths, so the existing lvalue-reference-over-forwarding-reference preference did not run and `any(const T&)` tied with `any(T&&, ...)` for a `const T&` argument. | `pa22:300` | `pa22/tests/general/300-constructor-template-defaulted-forwarding-lvalue-order.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/dual_constructor_template_const_lvalue.cpp` failed with `ambiguous constructor [class box]`; Boost probe `/tmp/cppgm-boost-reducers-20260519/boost_any_construct_const_string_ref.cpp` failed the same way before the fix. | Non-STL reducer passes; selected-control reducer returns the `const T&` constructor path under clang; Boost `any` construct and `any_cast` probes pass; placement audit OK for `pa22`; focused LowIR ref matches; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `408/408`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes with `compared=381`; direct `libs/any/test/any_test.cpp` advances to separate nested placeholder member-initializer failure; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.49%` instructions). | `999535cc4` |
| committed | `libs/any/test` `boost::any::placeholder::clone()` in `boost/any.hpp`; nested `boost::any::placeholder` derives from `boost::anys::detail::placeholder`, which has the same unqualified class name. | Recording a direct base class blindly rebound the base injected-class name into the derived member scope. For a same-name nested derived class, that overwrote the derived injected-class-name, so `placeholder * clone() const` was stored as returning `boost::anys::detail::placeholder *` instead of `boost::any::placeholder *`. | `pa15:200` | `pa15/tests/general/200-nested-injected-class-name-hides-base-name.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/nested_class_name_shadows_base_in_return.cpp` failed with `invalid member initializer`; `CPPGM_TRACE=lifetime.init` showed `target_type=pointer to struct any::placeholder init_type=pointer to struct lib::placeholder`. | Reducer passes; focused PA15 check passes; placement audit OK for `pa15`; `ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `129/129`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes PA18/19/21/22 with no LowIR failures; direct `libs/any/test/any_test.cpp` compiles; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to 15 later failed targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-dee7966f4-20260519.json` passes (`+0.26%` instructions, within tolerance). | `fd3a1803d` |
| committed | `libs/any/test` MPL-if `boost::any_cast<double>(a)` and `boost::anys::any_cast<double>(a)` paths. | Scalar by-value returns of reference-typed expressions emitted the reference address directly. `static_cast<T&>(*result)` lowered to `return f64 %ptr`, leaving native lowering to treat a pointer temp as a floating result and fail with `unknown storage %t7`; integer variants compiled but returned pointer bits. | `pa14:200` | `pa14/tests/general/200-scalar-reference-static-cast-return.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/return_static_cast_ref_double_notemplate.cpp` failed with `ERROR: unknown storage %t1 in @read_value`; Boost `any_test_mplif.cpp` failed with `ERROR: unknown storage %t7 in @boost__any_cast__ov8`. | Reducer LowIR now loads `f64` through the reference address before return; focused PA14 check passes; placement audit OK for `pa14`; `ACTIVE_TEST_REPORT_PAS='pa14' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `51/51`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes with `compared=381`; direct `any_test_mplif.cpp` and `basic_any_test_mplif.cpp` compiles pass; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to 13 failed targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-dee7966f4-20260519.json` passes (`+0.09%` instructions). | this commit |
| committed | `libs/any/test` no-RTTI `boost::typeindex::detail::after_substrig<ArrayLength - detail::skip().size_at_begin>` path. | The constexpr evaluator treated `T{...}` as a call with a braced argument before attempting typed initialization for class aggregates. A constexpr function returning an aggregate with `return skip_info{1};` therefore could not feed `skip().size_at_begin` into an integral constant expression or explicit non-type template argument. | `pa20:400` | `pa20/tests/general/400-constexpr-function-aggregate-member-nttp.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/constexpr_function_aggregate_member.cpp` failed with `unsupported constexpr variable initializer`; reducer `/tmp/cppgm-boost-reducers-20260519/nontype_param_member_constexpr_template_arg.cpp` failed resolving `after<N-skip().size_at_begin>`; Boost no-RTTI `any_test.cpp` failed in the `ArrayLength`/`after_substrig` family. | Reducers pass; direct Boost no-RTTI `any_test.cpp` compile passes; placement audit OK for `pa20`; focused PA20 check passes; `ACTIVE_TEST_REPORT_PAS='pa20' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `69/69`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to 7 failed targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-8df3b529-20260519.json` passes (`-0.48%` instructions, RSS `+2.99%`). | `eec0e35a8` |
| committed | `libs/any/test` `boost::anys::basic_any<8, 8>::small_manager` destroying a `const ValueType *` in the `Copy` path. | Explicit destructor member calls used ordinary implicit-object conversion, so `const T *` could not invoke `~T()` even though C++ permits destructor calls on const objects. | `pa15:300` | `pa15/tests/general/300-const-pointer-explicit-destructor-call.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/const_pointer_destructor_call_notemplate.cpp` failed with `no viable overload for member call ~value`; Boost `basic_any_test.cpp` failed in `small_manager` with the same implicit object conversion rejection. | Non-STL and nested-template reducers pass; direct Boost `basic_any_test.cpp` compiles with and without RTTI; placement audit OK for `pa15`; focused PA15 check passes; `ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `130/130`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to 6 failed targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-8df3b529-20260519.json` passes (`-0.03%` instructions). | `f310f7c67` |
| committed | `libs/any/test/unique_any/emplace.cpp` initializer-list construction and member `emplace<non_movable_t>({ ... }, unique_ptr<int>)` | Function-template candidate deduction analyzed braced-init-list arguments target-aware against still-dependent parameters. That let a plain `Args&&...` candidate deduce from a braced-init-list and also let `std::initializer_list<U>` materialize as `std::initializer_list<U>` before `U` was deduced, leaving stale dependent argument metadata in the selected call. | `pa27:200` | `pa27/tests/general/200-member-template-initializer-list-deduction.t` | Original Boost compile failed by selecting the forwarding-pack path and then rejecting `non_movable_t`; after rejecting braced lists for non-`initializer_list` patterns, reducer `/tmp/cppgm-boost-reducers-20260519/initlist_member_template_emplace_min.cpp` failed with `unsupported expression in PA14 LowIR lowering kind=initializer-list-object type=class std::initializer_list<U>`. | Reducer passes; direct Boost `unique_any/emplace.cpp` compiles; placement audit OK for `pa27:200`; focused PA27 check and report pass (`39/39`); PA22 report passes (`408/408`); `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`-0.19%` instructions). | `080ebc54b` |
| committed | `libs/any/test` runtime failures in `any_test`, `basic_any_test`, `unique_any/unique_base`, and `unique_any/unique_from_any`; `boost::typeindex::stl_type_index::equal` compared a null `typeid(int *).name()` | Host RTTI lowering emitted pointer typeinfo objects as scalar zero placeholders. That made the typeinfo address stable enough for `typeid(T*) == typeid(T*)` but left the ABI `std::type_info` layout invalid, so `name()` returned null and Boost.TypeIndex crashed in `strcmp`. | `pa27:100` | `pa27/tests/general/100-typeid-pointer-rtti-global.t`; updated `pa27/tests/general/100-typeid-template-pointer-no-eager-complete.ref` to require structured pointer RTTI | Reducers `/tmp/cppgm-boost-reducers-20260519/typeid_pointer_name_null.cpp` and `/tmp/cppgm-boost-reducers-20260519/typeid_pointer_name_compare.cpp` returned failure/segfault before the fix; `lldb` showed the Boost runtime failures stopped in `_platform_strcmp` from `boost::typeindex::stl_type_index::equal`. | Pointer RTTI reducers pass; direct Boost `unique_any/base.cpp` and `unique_any/from_any.cpp` compile/link/run; placement audit OK for `pa27`; focused PA27 checks pass; `ACTIVE_TEST_REPORT_PAS='pa22 pa27' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `449/449`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances past the previous runtime failures and reports `failed updating 0 target`; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`-0.42%` instructions). | `79dce277f` |
| committed | `libs/any/test/unique_any/emplace.cpp` direct `unique_any b(in_place_type_t<int>{})` constructed a holder for the tag and later threw `boost::bad_any_cast` | Function-template partial ordering compared transformed parameter lists against full constructor parameter lists after default arguments were appended. The forwarding constructor's defaulted SFINAE parameter and the in-place constructor's empty trailing pack were both treated as participating parameters, so the less-specific forwarding constructor beat the in-place overload. | `pa22:300` | `pa22/tests/spec/300-constructor-default-pack-partial-ordering.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/in_place_default_parameter_order.cpp` compiled but returned `1` before the fix; direct Boost `unique_any/emplace.cpp` aborted with `boost::bad_any_cast`. | Reducer and clang control pass; direct Boost `unique_any/emplace.cpp` and no-RTTI variant compile/link/run; placement audit OK for `pa22:300`; focused PA22 check passes; `ACTIVE_TEST_REPORT_PAS='pa22 pa27' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `449/449`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to accepted-invalid `unique_const_rvalue_construction_failed.cpp`; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`-0.42%` instructions). | `79dce277f` |
| committed | `libs/any/test/unique_any/const_rvalue_construction_failed.cpp` was accepted, so the B2 compile-fail target was skipped instead of failing as expected. | Rvalue-reference standard conversion used a non-reference conversion fallback after a same-type cv mismatch. That let `const T&&` bind to `T&&`, so Boost selected the move constructor instead of the forwarding constructor whose static assertion rejects self construction from a const rvalue. | `pa12:100` | `pa12/tests/general/100-bad-rvalue-reference-binding-top-level-const-source.t` | Non-STL reducers `/tmp/cppgm-boost-reducers-20260519/const_int_rvalue_binds_nonconst_rvalue_ref.cpp` and `/tmp/cppgm-boost-reducers-20260519/const_rvalue_constructor_static_assert.cpp` were accepted or chose the move path before the fix; direct Boost compile of `unique_any/const_rvalue_construction_failed.cpp` was accepted. | Clang rejects the scalar reducer; focused PA12 check passes; PA12 placement review shows no detected later-feature findings for the new test, with PA12/ROADMAP owning standard conversions and reference binding; `ACTIVE_TEST_REPORT_PAS='pa12' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `123/123`; direct Boost compile-fail target fails with the expected static assertion; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` passes, updating 164 targets; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`+0.02%` instructions). | `b0ab8f34a` |
| committed | `libs/array/test` `boost::get_c_array()` in `array6.cpp` and `array_c_array_test.cpp`; Boost returns `T (&)[N]` from a `boost::array<T, N>::elems` member. | Function-template result-type patterns kept only top-level pointer operators. For a nested result declarator like `T (&get_c_array(...))[N]`, instantiation initially substituted the right result type, then reparsed the stored pattern as plain `T`, so calls returned the element type instead of an lvalue reference to the member array. | `pa18:100` | `pa18/tests/spec/100-function-template-member-array-reference-return.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/array_member_reference_return.cpp` failed with `invalid initializer [target lvalue-reference to array of 5 bool] [source bool]`; Boost `array6.o` and `array_c_array_test.o` failed with equivalent array-reference initializer diagnostics. | Reducer compiles and runs; `CPPGM_TRACE=template.resolve` shows the reparsed result stays `lvalue-reference to array of 5 bool`; focused PA18 check passes; placement audit marks the new test as `pa18:100` with no review flag; `ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `152/152`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/array/test` advances to the separate `array7.run` and `to_array_test.o` failures; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`+0.19%` instructions, RSS `-0.88%`, footprint `-0.47%`). | `8f02c8036` |
| committed | `libs/array/test/array7.cpp` runtime address checks for `boost::array<std::string, N>` and `std::array<std::string, N>`; `&*begin()` differed from the reference returned by `get<0>()`. | Class-valued built-in comma expressions were semantically lvalues, but `emit_lvalue_address` materialized every special class comma node before reaching the comma lvalue-address lowering. Reference initialization from `(0, object)` therefore copied the right operand into a temporary and bound the reference to the copy. | `pa15:200` | `pa15/tests/general/200-comma-class-lvalue-reference-init.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/comma_lvalue_reference_init_address.cpp` returned `1` under cppgm while clang returned `0`; Boost reducer `/tmp/cppgm-boost-reducers-20260519/boost_array_string_address.cpp` returned `1`; `array7.run` reported four address-check failures for `std::string`. | Non-STL reducer now returns `0` under cppgm and clang; LowIR stores `addr $object` directly into the reference slot with no `tmpobj`/`copyobj`; Boost reducer returns `0`; placement audit marks the new PA15 test owner-reached with no review flag; focused PA15 check passes; `ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `306/306`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/array/test//array7` passes; direct `libs/array/test` run advances to only `to_array_test.o`; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`+0.08%` instructions, RSS `+1.82%`, footprint `-0.36%`). | `5363a0a11` |
| committed | `libs/array/test/to_array_test.cpp`; `boost::to_array({1, 2, 3})` selects array-reference overloads. | Target-aware analysis represented braced array temporaries as lvalues even when the target was an array reference, so overload resolution preferred const lvalue-reference binding and LowIR had no call-argument materialization path for braced array reference temporaries. The same conversion path also needed to preserve array/function lvalue decay into pointer temporaries for rvalue-reference pointer parameters. | `pa26:100` | `pa26/tests/general/100-braced-array-reference-argument.t`; `pa26/tests/general/100-bad-braced-array-nonconst-lvalue-ref-argument.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/braced_array_ref_call_notemplate.cpp` failed with `unsupported expression in PA14 LowIR lowering kind=braced-init-list type=lvalue-reference to array of 3 const int`; overload reducer `/tmp/cppgm-boost-reducers-20260519/braced_array_overload_selection.cpp` needed to select the `int (&&)[3]` overload; non-const lvalue-reference negative reducer had to remain rejected. | Reducers pass with expected statuses; existing `pa26/tests/general/200-defaulted-member-alias-constructor.t` still passes; placement audit marks both new PA26 cluster-100 tests OK; focused PA26 checks pass; `ACTIVE_TEST_REPORT_PAS='pa26' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `96/96`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/array/test//to_array_test` passes; `JOBS=12 ./run-cppgm-b2.sh libs/array/test` passes; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`+0.29%` instructions, RSS `-5.40%`, footprint `-0.05%`). | `d5804e2b2` |
| committed | `libs/asio/test` dependency build through Boost.Chrono/Boost.System; `boost::system::error_category::operator std::error_category const& () const` is defined out-of-class in `boost/system/detail/error_category_impl.hpp`. | Conversion-operator semantic parsing and binding used raw `::` text splitting. For an out-of-class definition such as `error_category::operator std::error_category const&`, that split confused the class qualifier with the qualified conversion target, so the declaration was rejected or failed to bind. The AST already preserved the correct qualified-name structure. | `pa16:400` | `pa16/tests/general/400-out-of-class-qualified-conversion-operator-result.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/out_of_class_conversion_operator_qualified_ref_global.cpp` failed with `unsupported out-of-class conversion operator`; earlier controls showed `operator int` already passed while qualified reference targets failed. Boost `libs/asio/test` logged 211 equivalent failures. | Reducer passes under cppgm and clang; placement audit marks the new PA16 cluster-400 test owner-reached; focused PA16 check passes; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `125/125`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost.Chrono `process_cpu_clocks.cpp` compile passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances past the conversion-operator failure to the next Asio frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`+0.39%` instructions, RSS `+0.47%`, footprint `-0.01%`). | `c583b325b` |
| committed | `libs/asio/test//any_completion_executor`; `boost::asio::execution::any_executor` declares `const prop_fns<any_executor>* prop_fns_`, where `prop_fns` is a class template inherited from `detail::any_executor_base`. | Unqualified class-template lookup in class scopes only checked the lexical scope chain. It did not search base-class member template declarations, although type lookup already searches inherited member types. Instantiating the member declaration therefore left `prop_fns<any_executor>` unresolved. | `pa18:200` | `pa18/tests/general/200-inherited-class-template-id-member-pointer.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/const_inherited_template_id_member_pointer.cpp` failed with `unsupported class member decl-specifier-seq`; a no-pack reducer `/tmp/cppgm-boost-reducers-20260519/const_inherited_template_id_member_pointer_nopack.cpp` captured the same missing lookup without the PA18 pack cluster. Boost target failed at `boost/asio/execution/any_executor.hpp:1835`. | Reducers pass under cppgm and clang; inherited alias-template companion reducer also passes; placement audit marks the new PA18 cluster-200 test owner-reached; focused PA18 check passes; `ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `153/153`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances past the `prop_fns` declaration to a separate `constraint_t` alias-template parse frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`-0.35%` instructions, RSS `+0.35%`, footprint `+0.03%`). | `abb11e6f0` |
| committed | `libs/asio/test//any_completion_executor`; `boost::asio::execution::detail::supportable_properties<I, void(Head, Tail...)>` recursively checks `void(Tail...)` while building `prefer_only`. | Function type substitution treated a parameter pack in `void(Tail...)` like a C varargs marker instead of expanding it into ordinary function parameters. That left recursive specializations with `void(second, ...)`/stale dependent text instead of `void(second)` and `void(second, third)`. Witness source-use discovery also tried to resolve qualified member template uses through the instantiated pack spelling after the semantic type was already known. | `pa21:400` | `pa21/tests/general/400-function-type-tail-pack-recursive-specialization.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/pack_function_type_tail_expansion.cpp` failed to compile the recursive function-type pack case; Asio reducer `/tmp/cppgm-boost-reducers-20260519/asio_supportable_prefer_pack.cpp` failed with `explicit type template argument resolution reached text fallback [arg void(Tail...)]`; Boost target failed in `constraint_t<false, int>`. | Reducers pass; focused PA21 check passes; placement audit OK for `pa21:400`; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `176/176`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances to the next `allocator<void>` member declaration frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`-0.36%` instructions, RSS `-0.43%`, footprint `+0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `boost::asio::execution_context::allocator<void>` declares `allocator_impl_base *impl_` inside an explicit specialization of a member class template. | Explicit class specializations recorded the namespace/global declaration scope as their binding scope. For `template<> class outer::allocator<void>`, class completion therefore lost the enclosing `outer` member scope, so unqualified lookup inside the specialized member class body could not find sibling nested types such as `outer::base`. | `pa21:300` | `pa21/tests/general/300-member-class-explicit-specialization-owner-lookup.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/nested_private_owner_type_in_member_class_specialization_use.cpp` failed with `unsupported class member decl-specifier-seq` for `base *specialized`; Boost target failed at `boost/asio/execution_context.hpp:461` for `allocator_impl_base *impl_`. | Reducer passes; focused PA21 check passes; placement audit OK for `pa21:300`; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `177/177`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances to the next `std::__1::atomic<long> == 0` overload frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`-0.02%` instructions, RSS `+0.57%`, footprint `+0.01%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; Asio's scheduler compares `boost::asio::detail::atomic_count` (`std::__1::atomic<long>`) with `0`. | User-defined conversion selection treated `operator T() const` and `operator T() const volatile` as indistinguishable when both conversion results had the same second standard conversion. The tie-breaker did not compare the conversion functions' implicit object parameter qualification, so builtin comparison fallback rejected the class operand instead of choosing the less-qualified `const` conversion for a non-volatile object. | `pa16:400` | `pa16/tests/general/400-cv-conversion-operator-builtin-comparison.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/dual_cv_direct_conversion_operator_compare_zero.cpp` failed with `unsupported comparison operands`; STL reducer `/tmp/cppgm-boost-reducers-20260519/atomic_compare_zero_min.cpp` failed with `operator-overload-to-builtin`; Boost target failed at `boost/asio/detail/impl/scheduler.ipp:205` for `outstanding_work_ == 0`. | Reducers pass, including inherited-template and `<atomic>` shapes; focused PA16 check passes; placement audit OK for `pa16:400`; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `126/126`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances to the next `any_completion_executor == nullptr_t` comparison frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`-0.32%` instructions, RSS `-0.44%`, footprint `-0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `boost::asio::execution::any_executor` declares hidden friend `operator==` templates whose `enable_if` return type compares the deduced `AnyExecutor` with the current specialization name `any_executor`. | Hidden friend function templates declared inside class templates were instantiated using the namespace/entity scope only. After candidate deduction selected `AnyExecutor = any_executor<int>`, non-type evaluation of `is_same<AnyExecutor, any_executor>::value` still resolved `any_executor` without the selected class-template specialization context, so the viable hidden friend was dropped and comparison fell back to builtin operators. | `pa22:300` | `pa22/tests/general/300-hidden-friend-current-specialization-enable-if.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/hidden_friend_current_specialization_enable_if_compare.cpp` reproduced the reduced shape; larger Boost-shaped reducer `/tmp/cppgm-boost-reducers-20260519/hidden_friend_current_specialization_nullptr_compare.cpp` failed with `operator-overload-to-builtin`; trace showed `candidate-instantiation-failed ... failed non-type template argument evaluation: is_same<AnyExecutor,any_executor>::value`. | Reducers pass; focused PA22 check passes; placement audit OK for `pa22:300`; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `410/410`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances to the next constructor frontier from `thread_pool::basic_executor_type` to `any_completion_executor`; new clean-head baseline recorded at `/tmp/cppgm-perf-baseline-boost-frontier-current-0a3d50de-20260519.json`; perf check passes (`+0.24%` instructions, RSS `+1.54%`, footprint `-0.03%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; Asio recursively evaluates `supportable_properties<I, void(Head, Tail...)>::is_valid_target<T>::value` while checking whether `thread_pool::basic_executor_type` can construct `any_completion_executor`. | Class template instantiation owner remapping matched only the owner class template name. When a nested member class template was resolved under the concrete leaf specialization `supportable_properties<0, void(prefer_only<prop_a>)>`, instantiation remapped its owner to the enclosing recursive `supportable_properties<0, void(prefer_only<prop_a>, prefer_only<prop_b>)>` because both owners share the same primary template name. Function-type argument parsing also needed to preserve recursive `void(Head)`/`void(Tail...)` template-id arguments without falling back to text. | `pa21:400` | `pa21/tests/general/400-recursive-member-template-concrete-owner.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/supportable_properties_recursive_leaf_trait_expression.cpp` failed with `failed non-type template argument evaluation` for the recursive `is_valid_target<T>::value` expression; direct Boost `any_completion_executor.cpp` failed with no viable constructor for `thread_pool::basic_executor_type`. | Reducers pass; new PA21 regression passes; placement audit marks the regression owner-reached at `pa21:400`; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `178/178`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to a separate `fat_executor` constructor frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-b65474fca-20260519.json` passes (`+0.77%` instructions, RSS `+0.13%`, footprint `+2.12%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `fat_executor` construction triggers defaulted constructor SFINAE through imported `std::is_base_of`, recursive `conditional<...>::type::value`, and `supportable_properties<...>::template is_valid_target<T>::value`. | Constructor template parameter-clause replay used the restricted signature parser, so defaulted SFINAE parameters could not resolve the needed class-template references. Type-trait arguments sometimes arrived as structured id-expression syntax instead of type-id syntax, concrete member-template `::value` lookup missed the concrete owner specialization, and type-pack expansion needed to preserve typed cv/ref/function/member-value patterns instead of falling back to text. | `pa22:500`, `pa34:700` | `pa22/tests/general/500-constructor-sfinae-member-template-value.t`; `pa34/tests/compile/700-hosted-imported-base-of-constructor-sfinae.t` | Reducers `/tmp/cppgm-boost-reducers-20260519/constructor_template_function_type_trait_constraint.cpp`, `/tmp/cppgm-boost-reducers-20260519/defaulted_constraint_constructor_imported_std_base_of.cpp`, and `/tmp/cppgm-boost-reducers-20260519/fat_executor_trait_probe.cpp` failed through the `fat_executor` constructor constraint; direct Boost `any_completion_executor.cpp` stopped at that constructor frontier. | Reducers pass; placement audits mark the new PA22 and PA34 tests owner-reached in the intended clusters; `ACTIVE_TEST_REPORT_PAS='pa22 pa34' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `706/706`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to ambiguous `boost::asio::query` overloads at line 409; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-b65474fca-20260519.json` passes (`+0.72%` instructions, RSS `-0.74%`, footprint `+2.16%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `query_free_trait<T, P, void_t<decltype(query(declval<T>(), declval<P>()))>>` probes Asio hidden-friend `query` before a later `api::query` CPO object is declared. | Leaf dependent `decltype`/`noexcept` call probing did not perform definition-point ADL, allowed a later ordinary CPO object to supply the result, did not keep hidden-friend candidates needing a user-defined conversion, and class-template alias/noexcept constant handling did not use template-aware semantic expression evaluation for this probe. | `pa22:500` | `pa22/tests/spec/500-hidden-friend-query-free-decltype-noexcept.t` | Non-STL reducer failed to preserve the hidden-friend result type; direct Boost `any_completion_executor.cpp` stopped at ambiguous `boost::asio::query` overloads. | Focused reducer passes; placement audit OK for `pa22:500`; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `412/412`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to `unknown function boost::asio::detail::run_test<&any_completion_executor_construction_test>`; shared baseline `/tmp/cppgm-perf-baseline-boost-frontier-current-b65474fca-20260519.json` still fails without this WIP (`+1.33%` instructions), so the slice was gated against isolated pre-slice baseline `/tmp/cppgm-perf-baseline-pre-query-free-9f725e-20260519.json` and passes (`-0.04%` instructions, RSS `+1.40%`, footprint `+0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `BOOST_ASIO_TEST_CASE(any_completion_executor_construction_test)` expands to `boost::asio::detail::run_test<&any_completion_executor_construction_test>(...)`. | Explicit function-template argument resolution only recognized bare function names for function-pointer NTTPs. A structured address-of expression such as `&sample` was sent through integral constant evaluation, failed, and dropped the only `run_test` function-template candidate before overload resolution. | `pa22:400` | `pa22/tests/spec/400-function-template-nontype-function-pointer-call.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/run_test_function_pointer_nttp.cpp` and the checked-in regression failed with `unknown function api::detail::run_test<&sample>`; trace showed `explicit-arg-resolution-failed` after `resolve-template-arg kind=non-type text=&sample`. | Regression passes; placement audit OK for `pa22:400`; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `413/413`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to `unknown id-expression service_registry::create<Service,execution_context>`; perf check against `/tmp/cppgm-perf-baseline-pre-run-test-6bdcd76ac-20260520.json` passes after keeping the new function-address path behind the existing named-value fast path (`-0.01%` instructions, RSS `-1.18%`, footprint `+0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `factory_type factory = &service_registry::create<Service, execution_context>` in `boost/asio/detail/impl/service_registry.hpp`. | Function-template target-type deduction compared the whole function type before recognizing a trailing function parameter pack. With explicit `Service, Owner` arguments and an empty `Args...`, the generic matcher saw the pattern as having one more parameter than the target function pointer and dropped the only viable static member function-template specialization. | `pa22:400` | `pa22/tests/general/400-member-function-template-address-explicit-pack.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/member_function_template_address_explicit_args.cpp` and the checked-in regression failed with `unknown id-expression service_registry::create<Service,context>`; direct Boost failed at `service_registry::create<Service,execution_context>`. | Reducer passes; placement audit run for PA22 with existing backend-owner notes only; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `414/414`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to `explicit-type-argument-text-fallback [arg int *]` in libc++ tuple machinery; perf check against `/tmp/cppgm-perf-baseline-pre-member-template-target-pack-f20672781-20260520.json` passes (`+0.08%` instructions, RSS `-0.13%`, footprint `+0.22%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; libc++ tuple construction resolves `std::__1::_Not<std::__1::tuple<int *>::_IsThisTuple<int *&>>` while checking a pointer argument constructor. | Explicit type-argument resolution could strip reference and cv wrappers from type-id text, but could not build a semantic pointer type for concrete explicit type arguments such as `int *` before applying an outer reference. Nested member template-ids that used `outer<int *>::inner<int *&>` therefore reached the hard explicit-type-argument fallback instead of a `TypePtr`. | `pa22:500` | `pa22/tests/general/500-nested-member-template-pointer-type-arg.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/type_pack_alias_and_fallback.cpp` failed with `explicit-type-argument-text-fallback [arg int *] [scope not_<outer<int *>::inner<int * &>>]`; STL probe `/tmp/cppgm-boost-reducers-20260519/libcpp_tuple_int_ptr_ctor.cpp` failed compiling `std::tuple<int *> t(p)`; direct Boost stopped in libc++ tuple machinery. | Reducer and libc++ tuple probe pass; placement audit OK for PA22; focused PA22 check passes; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `415/415`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to unsupported `static_cast` in `any_executor::prefer`; perf check against `/tmp/cppgm-perf-baseline-pre-explicit-pointer-type-arg-b51342e2d-20260520.json` passes (`-0.59%` instructions, RSS `-4.60%`, footprint `-0.24%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `any_executor::prefer` passes `&static_cast<const prefer_only<tracked_t<0>>&>(p)` where `p` is the inner `tracked_t<0>` property. | `static_cast` routed complete non-reference class targets through constructor/direct-initialization, but reference targets to complete class types stayed on the ordinary cast path. That missed the allowed temporary construction and const-reference binding for `static_cast<const Wrapper&>(source)` when `Wrapper` has a converting constructor. | `pa16:400` | `pa16/tests/general/400-static-cast-class-reference-temporary.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/static_cast_const_ref_converting_ctor.cpp` failed with `unsupported cast expression [op static_cast] [target lvalue-reference to const struct wrapper<tracked>] [operand struct tracked]`; direct Boost failed at `any_executor::prefer`. | Reducer passes; placement audit OK for PA16 after reducing to a non-template value-semantics case; focused PA16 check passes; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `127/127`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to `duplicate LowIR function role eh_allocate_exception`; perf check against `/tmp/cppgm-perf-baseline-pre-static-cast-class-ref-e5363cac2-20260520.json` passes (`-0.00%` instructions, RSS `-0.61%`, footprint `-0.06%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; the TU declares libc++/host `extern "C" __cxa_allocate_exception` and also uses C++ `throw`. | Runtime helper lowering always synthesized `@__external_runtime____cxa_allocate_exception` for generated throws even when semantic collection had already recorded the direct host C ABI function symbol. The resulting LowIR contained two declarations with the `eh_allocate_exception` role and failed object generation. | `pa33:100` | `pa33/tests/general/100-host-eh-runtime-declaration-reuse.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/host_eh_raw_cxa_and_throw.cpp` failed with `duplicate LowIR function role eh_allocate_exception`; LowIR showed both direct `@__cxa_allocate_exception` and internal external-runtime alias declarations. | Reducer passes and LowIR now has a single `eh_allocate_exception` role on `@__cxa_allocate_exception`; focused PA33 check passes; placement audit OK for PA33; `ACTIVE_TEST_REPORT_PAS='pa33' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `80/80`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` compiles and links, then advances to a separate runtime segfault after `any_completion_executor_construction_test passed`; perf check against `/tmp/cppgm-perf-baseline-pre-host-eh-raw-cxa-e2659bc5f-20260520.json` passes (`-0.06%` instructions, RSS `+0.93%`, footprint `+0.04%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `any_completion_executor_nothrow_construction_test` forces `operator new(std::nothrow)` to return null before constructing `fat_executor`. | New-expression lowering treated every allocation result as non-null before running scalar value initialization or class constructors. For a known-nothrow allocation function taking `std::nothrow_t`, C++ requires null allocation to skip initialization and return the null pointer; cppgm instead called the constructor with `this == 0`. | `pa16:300` | `pa16/tests/general/300-nothrow-new-null-skips-constructor.t` | Non-STL reducers `/tmp/cppgm-boost-reducers-20260519/nothrow_new_null_skips_constructor.cpp` and `/tmp/cppgm-boost-reducers-20260519/nothrow_new_null_constructor_side_effect.cpp` failed by segfaulting or running the constructor after null allocation; LLDB showed the Boost crash in `shared_target_executor::impl_base::impl_base()` with `this == 0`. | Reducers and clang controls pass; focused PA16 check passes; placement audit OK for PA16; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `128/128`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` passes; perf check against `/tmp/cppgm-perf-baseline-pre-nothrow-new-null-8738f09e0-20260520.json` passes (`+0.21%` instructions, RSS `-1.03%`, footprint `+0.01%`). | this commit |
| committed | `libs/asio/test//any_completion_handler`; `boost::asio::cancellation_slot::clear` calls `(*handler)->destroy()` through a member `base **handler`. | Virtual-dispatch callee construction peeled through every unary `*` while finding the dispatch object class. For an implicit object argument shaped like `(*handler)`, that skipped from the actual `base *` receiver back to the member storage `base **`, leaving no class for the vtable view lookup. | `pa17:300` | `pa17/tests/general/300-virtual-call-dereferenced-member-pointer.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/virtual_call_through_dereferenced_member_pointer.cpp` failed with `missing virtual dispatch object class for base::destroy [function slot::clear]`; clang accepted the reducer. | Reducer and clang control pass; focused PA17 check passes; placement audit OK for PA17; `ACTIVE_TEST_REPORT_PAS='pa17' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `22/22`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler` advances to `unknown function boost::asio::bind_allocator`; perf check against `/tmp/cppgm-perf-baseline-pre-virtual-dispatch-deref-member-30655253f-20260520.json` passes (`-0.02%` instructions, RSS `+2.39%`, footprint `+0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_handler`; nested `bind_allocator(bind_cancellation_slot(bind_executor(...)))` checks Asio's `is_executor` trait, which probes `&executor_memfns_derived<T>::context` where `executor_memfns_derived<T>` inherits both `T` and a fallback base. | Ambiguous member lookup while substituting a non-type template argument escaped as a hard `logic_error` during instantiated function parameter-clause refresh and function-template deduction-pattern preparation. That invalid immediate context should drop the candidate by SFINAE, allowing the fallback probe overload to win. | `pa28:300` | `pa28/tests/spec/300-ambiguous-member-nontype-arg-sfinae.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/member_ambiguous_sfinae_call.cpp` failed with `ambiguous member lookup [root derived<has_f>] [candidates has_f; fallback]`; Boost-shaped reducer `/tmp/cppgm-boost-reducers-20260519/any_completion_handler_nested_bind_allocator.cpp` failed before reaching the fn-table frontier. | Reducers pass or advance to the next frontier; placement audit marks the regression owner-reached at `pa28:300`; `ACTIVE_TEST_REPORT_PAS='pa28' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `42/42`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler` advances to `callee expression is not callable` for `any_completion_handler<void()>::operator()`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.22%` instructions). | this commit |
| committed | `libs/asio/test//any_completion_handler`; `std::move(h1)()` invokes `boost::asio::any_completion_handler<void()>::operator()`, whose trailing return type is `decltype(fn_table_->call(impl_, ...))`. | Member-template trailing return types were reparsed without a member-function context. Unqualified non-static data members in `decltype(...)` therefore could not resolve through the implicit object during function-template result substitution; the shared trailing-return parser also lacked this context for non-dependent member-template result types. | `pa18:300` | `pa18/tests/spec/300-member-template-trailing-return-implicit-field.t` | Reducers `/tmp/cppgm-boost-reducers-20260519/member_template_trailing_member_call_nontemplate_class.cpp` and `/tmp/cppgm-boost-reducers-20260519/member_template_trailing_member_call.cpp` failed with `callee expression is not callable`; `/tmp/cppgm-boost-reducers-20260519/member_template_trailing_member_call_simple.cpp` failed during class collection with `unsupported function template decl-specifier-seq`. | Reducers pass; placement audit marks the member-template feature owner-reached at `pa18:300`; `ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `154/154`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler` advances to `unsupported constexpr class member initializer`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.50%` instructions). | this commit |
| committed | `libs/asio/test//any_completion_handler`; `any_completion_handler_fn_table_instance<Handler, Signatures...>::value` builds a static constexpr function table from `&...::template impl<Handler>` entries. | Constant evaluation did not evaluate address-of function-template ids before the generic unary path, and constexpr class construction selected constructors from raw AST arguments even when pack-expanded function addresses needed concrete value types. Target-aware constructor recovery also needed to preserve expanded argument nodes for LowIR, and function-id target lookup had to treat substitution failure as candidate dropping. | `pa20:300` | `pa20/tests/general/300-constexpr-static-fn-template-address-pack.t` | Pre-fix checked-in regression and reducer `/tmp/cppgm-boost-reducers-20260519/constexpr_fnptr_pack_ctor_min.cpp` failed with `unsupported constexpr class member initializer`; larger reducers `constexpr_pack_base_fn_template_single.cpp` and `constexpr_static_table_function_pointers.cpp` captured the recursive Boost table shape. | Reducers pass; pre-fix disposable worktree confirms the checked-in regression fails on `25a0e40f1`; placement audit marks the new PA20 cluster-300 test owner-reached, with only existing PA20 backend-owner notes elsewhere; `ACTIVE_TEST_REPORT_PAS='pa20' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `70/70`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler` advances to `boost::asio::any_io_executor` no viable constructor in `associated_immediate_executor::get`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.09%` instructions). | this commit |
| committed | `libs/asio/test//any_completion_handler`; `associated_immediate_executor::get` constructs `any_io_executor any_candidate(std::nothrow, candidate)` after checking `can_query<executor_type, context_as_t<execution_context&>>::value`. | Hidden friend function-template instantiation overlaid the caller use-scope before protecting the enclosing class template's lexical parameter names. A probe whose caller also had a template parameter named `T` shadowed `context_as_t<T>`'s own `T`, so Asio evaluated the hidden-friend constraint against the executor type instead of the requested context reference. | `pa22:300` | `pa22/tests/general/300-hidden-friend-sfinae-use-scope-shadowing.t` | The checked-in regression failed in a disposable pre-fix worktree at `3c972663c` with `static_assert false: query_free<executor,context_as_t<int&>>::value`; the Boost reducer `/tmp/cppgm-boost-reducers-20260519/asio_any_io_executor_thread_pool.cpp` failed because the `any_io_executor` constructor SFINAE-dropped the candidate. | Regression and Boost reducer pass; placement audit marks the PA22 cluster-300 test owner-reached; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `416/416`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler` advances to `operator==` fallback for `boost::asio::any_io_executor`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.01%` instructions). | this commit |
| committed | `libs/asio/test//any_completion_handler`; `boost::asio::execution::any_executor` hidden friend `operator==` has a dependent `enable_if` return type that names the current `any_executor<...>` specialization, and `has_equal<any_completion_executor>` can be checked before converting from `any_io_executor`. | Pending hidden friend function templates were merged across class-template specializations using only the namespace/entity key and template parameter list. A dependent return type whose collapsed type-id names the injected class specialization was then instantiated through the first specialization's pattern scope, so ADL for `any_completion_executor` reused the `any_executor<io_property>` friend and dropped the viable candidate. | `pa22:300` | `pa22/tests/general/300-hidden-friend-dependent-return-specialization-scope.t` | The checked-in regression failed in a disposable pre-fix worktree at `4bdf6c083` with `operator-overload-to-builtin` for `any_completion_executor == any_completion_executor`; Boost-shaped reducer `/tmp/cppgm-boost-reducers-20260519/asio_any_completion_handler_immediate_executor.cpp` failed at `boost/asio/traits/equality_comparable.hpp:57` before the fix. | Regression passes; placement audit marks the PA22 cluster-300 test owner-reached with no later-owner violation after removing `__is_base_of`; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `417/417`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; Boost-shaped reducer and focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler` advance to ambiguous `can_prefer` member lookup; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.07%` instructions, RSS `-0.40%`, footprint `+0.11%`). | this commit |
| committed | `libs/asio/test//any_completion_handler`; converting `boost::asio::any_io_executor` to `boost::asio::any_completion_executor` recursively checks `supportable_properties<...>::is_valid_target<any_executor<...>>`, including `can_prefer<any_executor<io_properties...>, prefer_only<outstanding_work::untracked_t>>`. | A reference-only class instantiation could be promoted into the full-instantiation map while its own reference-member collection was still active. The reentrant full population recorded a transient `integral_constant<bool,false>` base for `can_prefer`, then the outer reference pass recorded the final `integral_constant<bool,true>` base on the same class, causing ambiguous `::value` lookup. | `pa22` | External Boost-header reducer `/tmp/cppgm-boost-reducers-20260519/asio_completion_executor_min.cpp`; larger Boost-shaped reducer `/tmp/cppgm-boost-reducers-20260519/asio_any_completion_handler_immediate_executor.cpp`. No checked-in non-STL regression was added because synthetic reconstructions did not reproduce the stale reference/full-instantiation interleave on the old compiler. | Disposable pre-fix compiler at `da2333a44` rejects `asio_completion_executor_min.cpp` with ambiguous `can_prefer<...>` member lookup; current compiler accepts it. The larger reducer advances from the same ambiguity to `named type not supported in procedural LowIR`. | Boost-header reducer passes; larger reducer advances to the LowIR named-type frontier; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler` advances to the same LowIR named-type frontier; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `417/417`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.21%` instructions, RSS `-0.16%`, footprint `+0.12%`). | `91d20e897` |
| committed | `libs/asio/test//any_completion_handler`; `executor_binder` constructs its stored executor from `static_cast<Executor&&>(other.get_executor())`, where `get_executor()` returns `thread_pool::basic_executor_type` by value through an indirect class-result boundary. | LowIR lowering used the converted call-expression node type to decide whether a call needed class-result materialization. After the surrounding `static_cast` converted the call node to `basic_executor_type&&`, lowering missed that the callable still returned a class object by value and tried to treat the named class type as a procedural scalar. | `pa16:300` | `pa16/tests/general/300-indirect-call-result-rvalue-reference-materialization.t` | Disposable pre-fix compiler at `56340bd9` rejects the PA16 regression with `named type not supported in procedural LowIR [type struct executor]`; Boost reducers `/tmp/cppgm-boost-reducers-20260519/asio_any_completion_handler_bind_executor_only.cpp` and `/tmp/cppgm-boost-reducers-20260519/asio_any_completion_handler_immediate_executor.cpp` failed with the same named-type frontier for `thread_pool::basic_executor_type`. | Regression passes; placement audit marks the PA16 cluster-300 test owner-reached; Boost bind-executor reducers pass; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `129/129`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.20%` instructions, RSS `-0.03%`, footprint `+0.13%`). | `1ff97c0dc` |
| committed | `libs/asio/test//any_completion_handler_select`; `socket_ops::inet_ntop` tests `!ec` for `boost::system::error_code` while `boost::asio::operator!(cancellation_type)` is visible in the enclosing namespace. | Unary overload lookup treated a rejected non-member `operator!` candidate for a class operand as a hard failure. The built-in logical-not path remained viable through `error_code::explicit operator bool()`, but cppgm never fell through to the contextual bool conversion. | `pa16:400` | `pa16/tests/general/400-logical-not-after-rejected-operator-overload.t` | Disposable pre-fix compiler at `9e0d479bf` rejects `/tmp/cppgm-boost-reducers-20260519/asio_error_code_not_min.cpp` with `no viable overload for call operator!`; clang accepts the reducer. | Reducer and regression pass; placement audit marks the PA16 cluster-400 test owner-reached; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `130/130`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler_select` advances to `class array new-expression unsupported`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.25%` instructions, RSS `+0.44%`, footprint `+0.09%`). | `24f3231ff` |
| committed | `libs/asio/test//any_completion_handler_select`; `hash_map<int, reactor_op_queue<int>::mapped_type>::rehash` allocates `new bucket_type[num_buckets]` for a nested bucket class containing libc++ list iterators. | Array `new` rejected all class element types even when the element type was destructible without per-element cleanup. LowIR also had no loop to run default constructors for dynamically sized class arrays. | `pa16:400` | `pa16/tests/general/400-trivial-class-array-new.t` | Disposable pre-fix compiler at `8928944e5` rejects `/tmp/cppgm-boost-reducers-20260519/trivial_class_array_new.cpp` with `class array new-expression unsupported`; current compiler accepts and runs it. | Reducer and regression pass; placement audit marks the PA16 cluster-400 test owner-reached; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `131/131`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler_select` advances to missing `__builtin_bzero`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.14%` instructions, RSS `+1.67%`, footprint `+0.13%`). | `7dde85553` |
| committed | `libs/asio/test//any_completion_handler_select`; Darwin `FD_ZERO` in `boost/asio/detail/posix_fd_set_adapter.hpp` expands to `__builtin_bzero(&fd_set_, sizeof(fd_set_))`. | The hosted builtin registry and host-libcall symbol policy covered the memcpy/memmove/memcmp/memchr family but omitted Clang/Darwin `__builtin_bzero`, so semantic lookup failed before lowering. | `pa34:600` | `pa34/tests/compile/600-builtin-bzero.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/builtin_bzero.cpp` failed with `unknown function __builtin_bzero`; Boost stopped while compiling `any_completion_handler_select`. | Reducer passes; focused PA34 check passes; placement audit marks PA34 cluster-600 as owner-reached; `ACTIVE_TEST_REPORT_PAS='pa34' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `296/296`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_handler_select` passes, updating 23 targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.35%` instructions, RSS `-1.79%`, footprint `+0.14%`). | `779710e08` |
| committed | `libs/asio/test//basic_datagram_socket`; `boost/asio/detail/is_buffer_sequence.hpp` declares `char (&buffer_sequence_begin_helper(T* t, enable_if_t<!is_same<decltype(boost::asio::buffer_sequence_begin(*t)), void>::value>*))[2];`. | The template declaration collector parsed namespace function-template declarations through the variable-declaration path first. When the later parameter type contained a `decltype` expression using an earlier parameter name, that path had no transient function parameter scope, so the collector rejected the declarator before reaching function-template collection. | `pa18:100` | `pa18/tests/general/100-function-template-parameter-decltype-ref-array.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/function_template_parameter_decltype_ref_array.cpp` failed with `unsupported template declarator [declarator ]`; the raw `char (&probe(T*))[2]` form compiled, confirming the missing surface was the later parameter type's dependency on the earlier parameter name. | Reducer passes; focused PA18 check passes; placement audit reviewed PA18 template-function ownership; `ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `155/155`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//basic_datagram_socket` advances past `is_buffer_sequence.hpp` to a separate variable-template pack definition frontier in `boost/asio/buffer.hpp`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.52%` instructions, RSS `+1.72%`, footprint `+0.31%`). | `1fcee59be` |
| committed | `libs/asio/test//basic_datagram_socket`; `boost/asio/buffer.hpp` defines `template<unsigned char...Bytes> const unsigned char bin_literal<bytes<Bytes...>>::data[sizeof...(Bytes)] = { Bytes... };`. | Template scopes tracked type parameter packs but not non-type value parameter-pack names, so declarator array-bound dependency checks did not recognize `sizeof...(Bytes)`. After parsing, out-of-class static member initializer replay for a class-template partial specialization rebound the definition's `Bytes...` against the primary argument list (`bytes<4,7>`) instead of using the already-deduced partial-specialization member scope, producing zero-filled static data. | `pa21:400` | `pa21/tests/general/400-static-data-nttp-pack-sizeof-bound.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/static_data_nttp_pack.cpp` first failed with `unsupported template declarator [declarator ]` on the `sizeof...(Bytes)` array bound; after the parse fix, it linked but returned `1` because the static data emitted `{0, 0}` instead of `{65, 66}`. | Reducer compile/link/run passes; focused PA21 check passes; placement audit OK for PA21; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `179/179`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//basic_datagram_socket` advances past `buffer.hpp` to `member expression is not a data member [function boost::asio::use_service]`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.51%` instructions, RSS `+2.80%`, footprint `+0.50%`). | `663a71c1a` |
| committed | `libs/asio/test//basic_datagram_socket`; `boost::asio::use_service<detail::io_context_impl>(io_context&)` is an explicit specialization among overloads for `execution_context&`/`io_context&`. | Explicit function-template specializations with explicit template arguments accepted the first same-name function template whose arguments resolved, without verifying that the instantiated function type matched the specialization declaration. The `io_context&` specialization was therefore attached to the `execution_context&` overload and its body was checked with the wrong parameter binding. | `pa19:200` | `pa19/tests/general/200-explicit-function-specialization-overload-parameter-match.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/function_template_explicit_specialization_overload_base_use.cpp` failed with `member expression is not a data member`; tracing showed both specialization parameters rebound as the base context type. | Reducer passes against the patched compiler and fails against the saved pre-fix compiler; clang accepts the reducer; placement audit marks PA19 cluster 200 as owner-reached; `ACTIVE_TEST_REPORT_PAS='pa19' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `101/101`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//basic_datagram_socket` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.42%` instructions, RSS `+2.91%`, footprint `+0.47%`). | `pending` |
| committed | `libs/asio/test//append`; `boost::asio::async_initiate` returns an `executor_binder_completion_handler_async_result<...>` whose member `get()` has trailing return type `decltype(target_.get())`. | The shared semantic trailing-return parser created only a parameter scope. In a class member, that left no synthetic member-function context or `this` binding, so `decltype(target_.get())` in an instantiated class member body was parsed as if it were outside a member function. | `pa18:200` | `pa18/tests/general/200-function-template-result-member-trailing-return.t` | Disposable pre-fix compiler at `099ef3d14` rejects the checked-in regression with `unsupported member function definition`; reduced control `/tmp/cppgm-boost-reducers-20260519/function_template_result_class_member_trailing_return_min.cpp` and original Boost reducer `/tmp/cppgm-boost-reducers-20260519/asio_async_initiate_append_bind_executor_template.cpp` failed before the fix. | Regression and reducers pass; placement audit for PA18 has no finding for the new test; `ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `156/156`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//append` advances past this frontier to `handler_type_requirements`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.37%` instructions, RSS `-0.87%`, footprint `+0.49%`). | this commit |
| committed | `libs/asio/test//append`; handler type requirement checks and `append`/`async_initiate` bind paths instantiate `target_` accessors, inherited `init_wrapper` constructors, member-pointer SFINAE probes, and disabled async-initiate overloads with dependent trailing returns. | Several independent semantic surfaces blocked the same Boost source: lvalue ref-qualified const members must accept const prvalue implicit objects; dependent-base inherited constructor templates must be registered as derived constructor templates with semantic base initializers and forwarded rvalue parameters; member-function-pointer NTTP lookup must use semantic member lookup and treat ambiguity as SFINAE; disabled function-template overloads that retain a dependent result type after non-dependent substitution must be SFINAE-dropped; and function-template instantiation scopes must check the current owner's member scope before inherited nested class-template lookup. | `pa16:200`, `pa21:400`, `pa22:300`, `pa22:500`, existing `pa28:300` | `pa16/tests/spec/200-const-ref-qualified-temporary-call.t`; `pa21/tests/general/400-nested-class-template-current-owner-lookup.t`; `pa22/tests/general/300-inherited-constructor-template-forwarding.t`; `pa22/tests/general/500-async-initiate-dependent-return-sfinae.t`; existing `pa28/tests/spec/300-ambiguous-member-nontype-arg-sfinae.t` | Focused reducers in `scratch/` reproduced the non-STL surfaces before the fix: const ref-qualified prvalue call, nested current-owner class-template lookup, inherited constructor-template forwarding, async-initiate dependent-return SFINAE, and the existing PA28 ambiguous member-pointer NTTP SFINAE reducer. A broad `template_arguments_are_dependent` value-syntax marker fixed the Boost source but regressed perf by about `+3.1%` instructions, so it was removed in favor of the narrower semantic fixes. | Focused checks for the new PA16/PA21/PA22 regressions and existing PA28 regression pass; `ACTIVE_TEST_REPORT_PAS='pa16 pa21 pa22 pa28' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `773/773`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct `libs/asio/test/append.cpp` compile through `cppgm-b2-toolchain.sh` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.31%` instructions, RSS `+0.32%`, footprint `+0.44%`). | `ff52acc7a` |
| committed | `libs/asio/test//bind_allocator`; nested `bind_allocator(std::allocator<void>(), std::bind(...))` through `steady_timer::async_wait` requires an inherited `async_result<allocator_binder<...>>::init_wrapper<outer>::init_wrapper(outer&&)` constructor definition after the inherited constructor was already cached. | The function-template cache-hit path only materialized cached template definitions when the source declaration had an ordinary function body. Inherited constructor templates are represented by a semantic `ctor_initializer` from the `using` declaration rather than `effective_template_body`, so a declaration-only cached binding stayed bodyless when output later required the constructor. | `pa22:300` | External Boost target only. Non-STL and hosted standalone reductions in `scratch/asio_bind_allocator_*_reduce.cpp` reproduced the nested async-initiate shape but did not fail on the pre-fix compiler because their concrete source-owner lookup materialized the constructor before the cache-hit path. | Pre-fix `libs/asio/test//bind_allocator` failed at link with undefined `boost::asio::async_result<allocator_binder<std::__1::__bind<...>, std::__1::allocator<void>>, void(error_code)>::init_wrapper<...>::init_wrapper(...)`; trace showed `has-definition=no source-template=yes` for the required constructor. | Focused Boost target `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//bind_allocator` passes; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false make test-report` passes `419/419`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.03%` instructions, RSS `+1.76%`, footprint `+0.43%`). | `d1225e494` |
| committed | `libs/asio/test//any_io_executor`; `fat_executor` must satisfy `static_require<fat_executor, blocking.never_t>` through the constexpr expression `Property::value() == traits::static_query<T, Property>::value()`. | Constexpr return and typed-initializer evaluation only applied builtin casts for plain expressions targeting class types, so `return token()`/`constexpr property p = property::required()` missed the converting constructor path. Constexpr overloaded-operator evaluation also passed raw operand values into `Evaluator::call`, whose parameter conversion path only handles builtin casts, so the selected `operator==(property, property)` could not receive user-defined conversions for its operands. | `pa20:400` | `pa20/tests/general/400-constexpr-return-operator-conversion.t` | Pre-fix `libs/asio/test//any_io_executor` rejected `boost::asio::any_io_executor ex6 = fat_executor(1);` with `no viable constructor [class boost::asio::any_io_executor]`; tracing narrowed the false trait to `static_require<fat_executor, blocking.never_t>::is_valid`. The reduced PA20 test failed before the constexpr direct-initialization fallback with `static_assert false: converted_value() == 1`. | Reducer passes; placement audit marks the PA20 cluster-400 test owner-reached; `ACTIVE_TEST_REPORT_PAS='pa20' ORDERED=false make test-report-nobuild` passes `71/71`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_io_executor` passes; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-nobuild` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.43%` instructions, RSS `-1.21%`, footprint `+0.64%`). | `ee7970939` |
| committed | `libs/asio/test//buffer`; Boost.Asio buffer literal tests use binary user-defined integer literals such as `0b00000000_buf` in C++11 mode. | Integer literal classification only recognized decimal/octal/hex. `0b..._buf` was consumed as a pp-number, but `classify_int` stopped after the leading `0` and treated `b..._buf` as an invalid user-defined suffix. | `pa34:400` | `pa34/tests/preproc/400-host-binary-integer-ud-literal.t` | Pre-fix preprocessing of `libs/asio/test/buffer.cpp` failed with `Invalid token in sequence: 0b00000000_buf`; the full compile later surfaced this as `expected declaration in namespace buffer_literals near KW_VOID:void` at `buffer.cpp:703`. | Focused PA34 preproc check passes; placement audit exits 0 for PA34; `ACTIVE_TEST_REPORT_PAS='pa34' ORDERED=false make test-report-nobuild` passes `297/297`; `libs/asio/test//buffer` advances past the parse error to the separate overload ambiguity at `buffer.cpp:216`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa22' STRICT_SUBTEST_JOBS=1 make test-strict-nobuild` passes after an initial parallel strict timeout; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.29%` instructions, RSS `+0.21%`, footprint `+0.54%`). | `71b21d7eb` |
| committed | `libs/asio/test//buffer`; `boost::asio::buffer(std::array<const char, N>&)` must prefer the `std::array<const PodType, N>&` overload over the generic `std::array<PodType, N>&` overload. | Function-template partial ordering tied when competing class-template arguments differed only by nested top-level cv (`holder<T>` versus `holder<const T>`). The ordinary transformed-parameter acceptance path treated both directions as viable, so overload selection could not choose the more specialized const-argument template. | `pa22:200` | `pa22/tests/general/200-function-template-partial-order-class-template-cv.t` | Reduced test and Boost `buffer.cpp:216` failed pre-fix with an ambiguous overload for `buffer(array<char const, N>&)`; clang selects the const-argument overload. | Regression passes; clang control returns 0; placement audit marks `template.function_partial_ordering` owner-reached at `pa22:200`; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report-nobuild` passes `420/420`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa22' STRICT_SUBTEST_JOBS=1 make test-strict-nobuild` passes; focused `libs/asio/test//buffer` advances to the separate `""_buf` user-defined string literal frontier at `buffer.cpp:709`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.03%` instructions, RSS `-0.16%`, footprint `+0.44%`). | `abc57ac93` |
| committed | `libs/asio/test//buffer`; Boost.Asio buffer literal tests initialize `const_buffer` with `""_buf` and `"hello"_buf` after `using namespace boost::asio::buffer_literals`. | Semantic literal analysis rejected suffixed string literals before overload lookup, and it did not have the active expression scope needed to resolve a literal operator through ordinary unqualified lookup and using-directives. | `pa15:300` | `pa15/tests/general/300-user-defined-string-literal-operator.t` | Reduced `scratch/udl_string_min.cpp` failed with `unsupported literal: "hello"_buf`; focused Boost `buffer.cpp:709` failed while initializing `boost::asio::const_buffer b1 = ""_buf`. | Reducer compiles/runs; clang control for the PA15 regression returns 0; focused PA15 check passes; placement audit accepts `pa15:300` after correcting the conversion-operator detector false positive for literal operators; `ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `132/132`; focused `libs/asio/test//buffer` advances to the separate integer literal-template frontier at `0x00_buf`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.23%` instructions, RSS `-1.77%`, footprint `+0.78%`). | `pending` |
| committed | `libs/asio/test//buffer`; Boost.Asio uses `template <char... Chars> operator ""_buf()` for integer buffer literals such as `0x00_buf`. | Semantic integer literal analysis rejected all user-defined integer suffixes instead of forming the standard literal-operator-template call with one explicit `char` non-type template argument for each source character before the suffix. | `pa19:100` | `pa19/tests/general/100-user-defined-integer-literal-template.t` | Reduced `scratch/udl_integer_template_min.cpp` failed with `user-defined literals unsupported`; focused Boost `buffer.cpp:716` failed while initializing `boost::asio::const_buffer b3 = 0x00_buf`. | Reducer compiles/runs; clang control for the PA19 regression returns 0; placement audit marks PA19 cluster 100 owner-reached; focused PA19 check passes; `ACTIVE_TEST_REPORT_PAS='pa19' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `102/102`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa19' STRICT_SUBTEST_JOBS=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `libs/asio/test//buffer` advances to separate unary `+` array-decay failure in the literal operator body; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.24%` instructions, RSS `+0.40%`, footprint `+0.45%`). | `pending` |
| committed | `libs/asio/test//buffer`; Boost.Asio literal operator body returns `const_buffer(+detail::literal<Chars...>::data, detail::literal<Chars...>::size)`. | Unary `+` analysis applied the usual value-conversion type query, which correctly decayed array lvalues to pointer types, but then rejected non-arithmetic operands. C++ permits unary `+` on pointers after array-to-pointer/function-to-pointer conversion, and LowIR already treats unary plus as a no-op on the resulting value. | `pa14:100` | `pa14/tests/general/100-unary-plus-array-decay.t` | Reduced `scratch/unary_plus_array_decay_min.cpp` failed with `ERROR: unsupported unary arithmetic operand`; focused Boost failed in `boost/asio/buffer.hpp:2880` while compiling `+detail::literal<Chars...>::data`. | Reducer compiles/runs; clang control accepts the reducer; focused PA14 check passes; placement audit marks the new test owner-reached in PA14 cluster 100; `ACTIVE_TEST_REPORT_PAS='pa14' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `52/52`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa18 pa19 pa21 pa22' STRICT_SUBTEST_JOBS=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `libs/asio/test//buffer` advances to a separate ambiguous partial class-specialization frontier in recursive literal parsing; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.16%` instructions, RSS `-7.01%`, footprint `+0.47%`). | `pending` |
| committed | `libs/asio/test//buffer`; binary buffer literals instantiate `bin_literal<bytes<>, '0','0','0','0','0','0','0','0'>`, which matches both the fixed-eight-bit partial and the catchall trailing-pack partial. | Partial-specialization ordering transformed direct pack-expansion arguments using rendered `arg_texts`, so `Chars...` could be missed when the syntax was stored as a direct NTTP pack expansion. The ordering fallback also counted pack-specificity from text alone, leaving the fixed-eight partial and catchall partial tied. | `pa21:400` | `pa21/tests/general/400-nontype-pack-fixed-tail-partial-specialization-ordering.t` | Non-STL reducer `scratch/partial_pack_literal_ambiguous_min.cpp` failed with `ERROR: ambiguous partial class specialization`; focused Boost failed in `boost/asio/buffer.hpp` while instantiating recursive `bin_literal` through `detail::literal<Chars...>::data`. | Reducer compiles/runs; clang control returns 0; focused PA21 check passes; placement audit exits 0 for PA21; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `181/181`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa18 pa19 pa21 pa22' STRICT_SUBTEST_JOBS=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `libs/asio/test//buffer` now compiles/links and advances to runtime failures in buffer-sequence traits and literal byte values; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.32%` instructions, RSS `-5.91%`, footprint `+0.47%`). | `pending` |
| committed | `libs/asio/test//buffer`; `buffer_literals::test` compares generated literal storage for escapes such as `"\xab"` against one-byte buffers. | Parsed quote literals only preserved decoded Unicode scalar contents. Ordinary string literal storage, bounds, constexpr arrays, and LowIR globals later re-encoded those contents as UTF-8, so a numeric escape such as `\xab` became bytes `c2 ab` and `sizeof("\xab")` became `3` instead of a single code unit plus terminator. | `pa14:100` | `pa14/tests/general/100-string-hex-escape-code-unit.t` | Minimal reducers `scratch/hex_escape_string_value_min.cpp` and `scratch/hex_escape_string_memcmp_min.cpp` returned failure with the pre-fix compiler; focused Boost `buffer_literals::test` failed the `memcmp(..., "\xab", ...)` checks. | Reducers compile/run; focused PA14 check passes; placement audit marks the PA14 cluster-100 owner reached; `ACTIVE_TEST_REPORT_PAS='pa14' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `53/53`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa18 pa19 pa21 pa22' STRICT_SUBTEST_JOBS=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `libs/asio/test//buffer` now reports `buffer_literals::test passed` and advances to the separate buffer-sequence trait runtime failures; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.64%` instructions, RSS `+2.98%`, footprint `+0.59%`). | `pending` |
| committed | `libs/asio/test//buffer`; Boost.Asio buffer-sequence traits probe `buffer_sequence_element_type_helper(T *t, enable_if_t<is_convertible<decltype(*buffer_sequence_begin(*t)), Buffer>::value> *)`. | Function-template instantiation reparsed each parameter as an isolated one-parameter clause. Later parameter types therefore could not see earlier function parameter names such as `t` while resolving dependent alias-template SFINAE arguments. | `pa22:500` | `pa22/tests/general/500-function-template-parameter-alias-sees-previous-param.t` | Minimal reducer `scratch/enable_if_decltype_parameter_min.cpp` failed pre-fix with `unknown function helper<value>` after the candidate instantiation could not resolve `decltype(*t)` inside `enable_if_t`; larger reducer `scratch/buffer_sequence_trait_min.cpp` reproduced the Asio trait shape. | Reducers compile; clang control accepts the regression; focused PA22 check passes; placement audit exits 0 for PA22; `ACTIVE_TEST_REPORT_PAS='pa22' CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `421/421`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa18 pa19 pa21 pa22' CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `libs/asio/test//buffer` advances from twelve buffer-sequence failures to nine remaining begin/end and vector trait failures; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.33%` instructions, RSS `-0.46%`, footprint `+0.67%`). | `18d219cfb` |
| committed | `libs/asio/test//buffer`; Asio's `buffer_sequence_begin_helper(T *t, enable_if_t<!is_same<decltype(boost::asio::buffer_sequence_begin(*t)), void>::value> *)` must see the qualified namespace function templates declared before the helper. | Stored dependent alias-template argument syntax kept reparsed/local AST token ranges for nested `decltype` calls. During later SFINAE evaluation, namespace function-template visibility checked those local token ranges against declaration order and hid the viable `buffer_sequence_begin` overloads. The fix preserves optional owner template-id syntax only on dependent qualified-member types that need it, rebases cloned argument AST tokens from the recorded source-token anchor, and carries that anchor through nested `decltype` reparsing without recursively resolving already-structured dependent owners. | `pa22:500` | `pa22/tests/general/500-dependent-alias-decltype-qualified-function-source-tokens.t` | Non-STL reducer `scratch/alias_enable_if_qualified_function_decltype_min.cpp` and Boost-header probe `scratch/asio_buffer_sequence_detail_probe.cpp` reproduced the false SFINAE drop before the token-anchor fix. A first implementation stored full owner syntax on every `Type`, which passed functionally but failed the footprint perf gate; the committed version stores it optionally. | Reducer and Boost-header probe compile; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//buffer` passes; placement audit marks the new PA22 cluster-500 regression owner-reached; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 TEST_REPORT_ASSIGNMENT_JOBS=1 CPPGM_SKIP_DEV_REBUILD=1 make test-report` passes `422/422`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa18 pa19 pa21 pa22' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.61%` instructions, RSS `+0.04%`, footprint `+1.24%`). | `9da752f5c` |
| committed | `libs/asio/test//buffered_read_stream`; `buffered_read_stream<socket>::async_write_some` forwards through a trailing-return `decltype(next_layer_.async_write_some(...))` while its class template also defines out-of-class static data and uses `conditional_t`-style alias selection. | Member-template trailing-return replay bound the member template's own parameters but not the current class-template owner arguments, so source-owner replay lost the concrete `Stream` binding needed to resolve `next_layer_type::async_write_some`. The alias expander also left known `conditional_t`/`__conditional_t` aliases structurally dependent after their bool selector was non-dependent. | `pa22:500` | `pa22/tests/general/500-member-template-conditional-alias-trailing-return.t` | Non-STL reducer `scratch/source_owner_member_trailing_return_decltype11.cpp` failed before the owner-scope binding with `unknown member function ... async_write_some`; the Boost target failed at the same `async_write_some` frontier. | Reducer compiles; placement audit marks the PA22 cluster-500 regression owner-reached; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false make test-report` passes `423/423`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes with `compared=382 failures=0 skipped=41`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//buffered_read_stream` advances past `async_write_some` to a separate `async_read_some` overload ambiguity; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.34%` instructions, RSS `-0.80%`, footprint `+0.88%`). | `b330d4c7e` |
| committed | `libs/asio/test//buffered_read_stream`; after earlier `async_read_some(..., &read_handler)` calls, the later `async_read_some(..., handle_read_eof)` call must deduce the forwarding-reference handler from a function lvalue. | Implicit member function-template specializations were registered in the ordinary member function lookup set. A later call therefore saw the cached pointer-handler specialization as a normal overload alongside the freshly deduced function-lvalue specialization, and overload ranking tied them. | `pa22:300` | `pa22/tests/general/300-member-template-implicit-instantiation-not-overload.t` | Non-STL reducer `scratch/asio_async_read_some_cached_handler_min.cpp` and the Boost target failed with an ambiguous member call between `Handler = pointer to function` and `Handler = function&`. | Reducer compiles; placement audit marks the PA22 cluster-300 regression owner-reached; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false make test-report` passes `424/424`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes with `compared=383 failures=0 skipped=41`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//buffered_read_stream` advances past the ambiguity to a separate `any_io_executor` constructor frontier; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.09%` instructions, RSS `-0.15%`, footprint `+0.86%`). | `1e52ea2f0` |
| committed | `libs/asio/test//buffered_read_stream`; `boost::asio::any_io_executor executor(context.get_executor())` checks `supportable_properties<..., prefer_only<outstanding_work::untracked_t>>::is_valid_target<io_context::executor_type>` through `traits::prefer_free<T, Property, void_t<decltype(prefer(...))>>`, and the following `static_query` path checks `traits::query_free<T, Property, void_t<decltype(query(...))>>`. | Unqualified value lookup for a dependent call treated a later ordinary CPO object as visible during template instantiation. When substituted source locations collapsed to `:1:1`, the source-order check could not reject the later value, so the probe called the CPO object recursively instead of leaving ordinary lookup empty and letting ADL find the hidden friend. The fix filters non-class value callees by declaration source location, compares source-location ids before token-order fallback, and threads dependent type-expression order-use locations through substituted `decltype(...)` evaluation. | `pa22:300` | `pa22/tests/general/300-dependent-adl-hidden-friend-before-later-value.t` | Minimal non-STL reducer `/tmp/two_phase_min_extern.cpp` fails on the pre-fix compiler with `static_assert false: traits::prefer_free<associated::arg,associated::property>::value`; Boost probes `scratch/asio_supportable_work_untracked_probe.cpp`, `scratch/asio_any_io_executor_from_io_context_executor.cpp`, and `scratch/asio_static_query_probe.cpp` failed in the same later-CPO visibility path. | Regression passes; clang control accepts the reducer; placement audit marks `sfinae` and `template.detector_idiom` owner-reached at PA22 cluster 300; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false make test-report` passes `425/425`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes with `compared=384 failures=0 skipped=41`; focused Boost target first advanced to `target_fns_table<Executor>` and, after source-id/order-use tightening, advances to `unsupported cast target type` for `initiate_dispatch_with_executor<immediate_ex_type>(immediate_ex)`; perf checks against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` pass (`-0.45%` and `-0.47%` instructions). | `c047d6e93` |
| committed | `libs/asio/test//buffered_read_stream`; Asio dispatches through `(boost::asio::detail::initiate_dispatch_with_executor<immediate_ex_type>(immediate_ex))(...)`. | The parser treated the outer parentheses as a C-style cast because the inner qualified template-id plus initializer was accepted as a `type-id` with a function-style abstract-declarator. When another postfix call followed the closing parenthesis, this should instead parse as a parenthesized functional-construction expression used as the callee. | `pa22:400` | `pa22/tests/general/400-parenthesized-qualified-template-functional-call.t` | Non-STL reducer `scratch/function_style_qualified_template_cast_min.cpp` fails on the pre-fix compiler with `unsupported cast target type [target_ast {type-id ... abstract-declarator parameter-clause ...}]`; Boost-header probe `scratch/asio_initiate_dispatch_cast_probe.cpp` reproduces the same Asio shape and is accepted by clang. | Regression passes and fails on the pre-fix compiler; placement audit has no finding for the new PA22 cluster-400 test; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `426/426`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa22' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes with `compared=384 failures=0 skipped=42`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//buffered_read_stream` advances to a separate `lazy_concrete_handler` constructor access/viability frontier; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.41%` instructions, RSS `-1.96%`, footprint `+0.88%`). | `6fee9f551` |
| committed | `libs/asio/test//buffered_read_stream`; `executor_function` builds a `detail::binder2<lazy_concrete_handler<...>>` and passes it by value through the handler path. | By-address class by-value parameters were copied inside the callee from the incoming storage pointer. That lost xvalue move semantics, forced unused template copy-constructor bodies to be emitted or instantiated, and made move-only handler wrappers appear non-viable. A stale call-semantic ABI hook also required class by-value copy constructors eagerly. | `pa16:200`, `pa18:100` | `pa16/tests/general/200-xvalue-pass-by-value-uses-move-ctor.t`; `pa18/tests/general/100-template-move-only-byvalue-argument-does-not-copy.t`; updated PA16 value-transfer LowIR refs | The PA16 reducer compiled with the pre-fix compiler but returned `1` because the parameter used the copy constructor. The PA18 reducer failed pre-fix with `no viable constructor` while instantiating `box<move_only>::box(const box&)`; Boost failed similarly in `lazy_concrete_handler`. | Reducers compile/run; parent compiler reproduces both failures; placement audits mark PA16 value ABI and PA18 template/value features owner-reached; `ACTIVE_TEST_REPORT_PAS='pa16 pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `290/290`; `ACTIVE_TEST_REPORT_PAS='pa22' ... make test-report-nobuild` passes `426/426`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa18 pa22' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//buffered_read_stream` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.59%` instructions, RSS `-0.28%`, footprint `+0.86%`). | `1e1e60b72` |
| committed | `libs/asio/test`; `basic_waitable_timer::async_wait` instantiates Asio's `detail::non_const_lvalue<WaitHandler> handler2(handler);` wrapper before forwarding `handler2.value` into `deadline_timer_service::async_wait`. | Block-scope function-style local initialization was accepted as a local function declaration before checking whether the parenthesized argument names a value in the current ordinary lookup scope. When a value parameter hid an outer type with the same name, the initialized local was recorded as a function, so later member access such as `handler2.value` made member-template argument analysis drop the viable candidate. | `pa15:200` | `pa15/tests/general/200-local-class-direct-init-parameter-hides-type.t` | Reduced Asio shape `scratch/asio_non_const_lvalue_reduce.cpp` failed pre-fix with `handler2` typed as `function of (...) returning non_const_lvalue<...>` and the nested `async_wait` candidate rejected during argument analysis. | Non-STL reducer and Boost `system_timer.async_wait(handler)` probe compile; placement audit marks the PA15 cluster-200 regression owner-reached; `ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `133/133`; `ACTIVE_TEST_REPORT_PAS='pa18 pa22' ... make test-report-nobuild` passes `583/583`; focused Asio cluster advances to the next direct `basic_waitable_timer::async_wait(asio::append/as_tuple/...)` adaptor frontier; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.53%` instructions, RSS `-0.24%`, footprint `+0.93%`). | `7bc97af3c` |
| committed | `libs/asio/test`; `basic_waitable_timer::async_wait` with `append`, `append_select`, `as_tuple`, and `consign` completion-token adaptors enters nested `async_result<executor_binder<...>>::initiate` / `async_initiate` trailing-result substitution. | Trailing-result reparse for member function templates always rebound the owner class template's primary parameters into the result parse scope. For a selected partial specialization such as `async_result<binder<T>, Signature>`, that shadowed the partial specialization's `T` with the primary argument `binder<handler>`, causing recursive `async_initiate` substitution and candidate dropping. | `pa22:500` | `pa22/tests/general/500-partial-member-template-trailing-result-scope.t` | Minimal non-STL reducer `/tmp/declval_nested_partial_min2.cpp` failed with `unknown function async_initiate<binder<handler>,void(int)>`; focused Boost `append`/`as_tuple` adaptors failed with unknown `basic_waitable_timer::async_wait` candidates. | Reducer compiles; clang control accepts the checked-in regression; placement audit marks PA22 cluster-500 owner-reached with only existing backend-owner/semantic-owner notes; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `427/427`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa22' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes with `compared=384 failures=0 skipped=43`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//append libs/asio/test//append_select libs/asio/test//as_tuple libs/asio/test//cancel_after libs/asio/test//cancel_at libs/asio/test//consign` now passes `append`, `append_select`, `as_tuple`, and `consign`, and advances to a separate `completion_payload<void(boost::system::error_code)>` ambiguous partial-specialization frontier in `cancel_after`/`cancel_at`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.57%` instructions, RSS `+0.66%`, footprint `+0.87%`). | `5fb3fe6cb` |
| committed | `libs/asio/test//cancel_after`; `libs/asio/test//cancel_at`; `completion_message<void(boost::system::error_code)>` inherits the matching completion payload for a one-argument signature. | Partial-specialization ordering treated `message<R(Arg0)>` and catchall `message<R(Args...)>` as mutually more specialized after transformed function-type deduction. The tie missed semantic function parameter-pack shape, so the fixed one-argument partial was rejected as ambiguous instead of preferred over the trailing function-parameter pack fallback. | `pa21:400` | `pa21/tests/general/400-function-type-partial-specialization-fixed-arity-over-pack.t` | Non-STL reducer `scratch/completion_message_ambig_min.cpp` and Boost `completion_payload<void(boost::system::error_code)>` failed with `ERROR: ambiguous partial class specialization`; trace showed both `R(Arg0)` and `R(Args...)` matched with score 1 and each considered more specialized than the other. | Reducer compiles; clang accepts the checked-in regression; placement audit marks PA21 cluster-400 owner-reached; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `182/182`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa21' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes with `compared=164 failures=0 skipped=18`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//cancel_after libs/asio/test//cancel_at` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.55%` instructions, RSS `-0.81%`, footprint `+0.89%`). | `96997ccbf` |
| committed | `libs/asio/test//composed_select`; `post(ctx, token)` instantiates `initiate_post_with_executor<Executor>::operator()` through a dependent trailing `decltype(...)` source owner before the body uses the concrete executor owner. | Member function-template instantiation rebound function template parameters but did not overlay the active class-template owner's concrete arguments and member typedefs when the declaring source owner was the same class template with dependent arguments. Defaulted SFINAE parameters therefore kept the dependent `executor_type` alias and left all four Asio `operator()` overloads viable. | `pa22:500` | `pa22/tests/general/500-member-template-dependent-owner-defaulted-sfinae.t` | Non-STL reducer `scratch/defaulted_enable_if_member_operator_min.cpp` failed pre-fix with an ambiguous call among the four defaulted-SFINAE member `operator()` templates; focused Boost `composed_select` failed in `boost/asio/detail/initiate_post.hpp` at `initiate_post_with_executor<Executor>::operator()`. | Reducer compiles/runs; clang accepts the checked-in regression; placement audit marks PA22 cluster-500 owner-reached; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `428/428`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa22' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes with `compared=384 failures=0 skipped=44`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//composed_select` advances from compile ambiguity to a separate undefined `composed_op<impl_cancellable<...>>` constructor link frontier; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.58%` instructions, RSS `-1.11%`, footprint `+0.88%`). | `f059f2581` |
| committed | `libs/asio/test//composed_select`; after the previous SFINAE fix, the target linked against an undefined inherited constructor template for `boost::asio::detail::composed_op<impl_cancellable<...>, composed_work<void()>, ...>::composed_op<I,W,H>(I&&,W&&,H&&)`. | Instantiating an inherited constructor template reused the source synthetic base initializer after selecting a class-template partial specialization. The initializer still carried source template-parameter types (`I`, `W`, `H`), so LowIR declared a dependent base constructor symbol instead of emitting the concrete base-entry constructor for the instantiated base. | `pa22:300` | `pa22/tests/general/300-partial-specialization-inherited-constructor-template.t`; updated `pa22/tests/general/300-inherited-constructor-template-forwarding.ref` for the corrected base-entry call. | Non-STL reducer `scratch/inherited_constructor_partial_template_min.cpp` reproduced the link failure before the fix; semantic/LowIR tracing showed the base call using `rvalue-reference to typename I/W/H` before the fix and concrete `impl/work/handler` parameter types after it. | Reducer compiles/runs; placement audit marks the PA22 cluster-300 regression owner-reached; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `429/429`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa22' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes with `compared=384 failures=0 skipped=45`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//composed_select` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.32%` instructions, RSS `-1.24%`, footprint `+0.88%`). | `446c6dfe3` |
| committed | `libs/asio/test//disposition`; `libs/asio/test//disposition_select`; `boost::system::error_code ec` compares with `boost::asio::no_error`, whose hidden friend `operator==(Disposition, no_error_t)` is constrained by `is_disposition<Disposition>`. | `__is_nothrow_assignable(T&, T&&)` used by the hosted disposition trait treated viable implicit/defaulted assignment operators as potentially throwing unless they had explicit noexcept metadata. `boost::system::error_code` therefore failed `is_nothrow_move_assignable`, making `is_disposition<error_code>` false and SFINAE-dropping the `no_error_t` comparison operator. | `pa33:700` | `pa33/tests/compile/700-builtin-nothrow-assignable-implicit-assignment.t` | Non-STL reducer `scratch/nothrow_assignable_template_operator_reduce.cpp` failed before the fix on `__is_nothrow_assignable(error_code&, error_code&&)` while clang accepted it; Boost probes `scratch/boost_error_code_nothrow_traits_probe.cpp` and `scratch/asio_no_error_trait_probe.cpp` isolated the false trait and dropped comparison. | Reducers and Boost probes compile; placement audit marks the PA33 cluster-700 regression owner-reached; `ACTIVE_TEST_REPORT_PAS='pa33' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `298/298`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//disposition libs/asio/test//disposition_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.50%` instructions, RSS `-1.53%`, footprint `+0.91%`). | `820f4f3ba` |
| committed | `libs/asio/test//connect`; `libs/asio/test//connect_select`; constructing the bound argument tuple for a function-pointer connect condition evaluates `_Not<tuple<..., FnPtr>::_IsThisTuple<..., FnRef>>::value`. | Structured bool evaluation tried the text-only member-template value shortcut before using carried qualifier type syntax. Re-parsing the owner `tuple<int, bool (*)(...)>` from text reached the explicit-type-argument fallback even though the AST already carried semantic qualifier type syntax for the owner/member chain. | `pa21:400` | `pa21/tests/general/400-structured-bool-member-template-function-pointer-owner.t` | Non-STL reducer `scratch/explicit_function_pointer_member_value_reduce.cpp` captured `not_<tuple<int, condition_ptr>::is_this_tuple<int, condition_ref>>::value`; the pre-fix path failed with `explicit type template argument resolution reached text fallback [arg bool (*)(...)]`. | Reducer compiles under cppgm and clang; patched-Clang witness ref generated; placement audit marks PA21 cluster-400 owner-reached; focused PA21 check passes; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `183/183`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa21' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes with `compared=165 failures=0 skipped=18`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//connect libs/asio/test//connect_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.46%` instructions, RSS `-1.48%`, footprint `+0.91%`). | `f769f57ce` |
| committed | `libs/asio/test//connect_pipe`; `libs/asio/test//connect_pipe_select`; the Asio read operation forwards `buffers_.total_consumed()` into libc++ `std::__bind::operator()` where the forwarding pack parameter is lowered as `ptr [pass=reference]`. | Native LowIR register allocation allowed a scalar call-result temp to stay register-only even when a later call argument needed a stable storage address for `pass=reference`/by-address passing. Function layout now records address-taken temps from storage-address contexts and keeps only those temps in frame storage, preserving the normal scalar register path. | `pa23:600` | `pa23/tests/strict/600-call-pass-mode-register-temp-address-materialization.t` | Saved Boost LowIR `/tmp/connect_pipe.lowir` failed in native lowering with `register temp does not have a stable address %t50` in the instantiated `read_op::operator()`; the focused LowIR reducer failed pre-fix when `%v = call i64 @make_value()` was passed to `copy_ref(..., %v)` with `%src : ptr [pass=reference]`. | Regression passes; existing PA23 address-materialization control still passes; placement audit for PA23 exits 0 with expected LowIR semantic-owner notes; `ACTIVE_TEST_REPORT_PAS='pa23' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_APP_ARGS= CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `102/102`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//connect_pipe libs/asio/test//connect_pipe_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.44%` instructions, RSS `-1.88%`, footprint `+0.89%`). | `6e2d7c82c` |
| committed | `libs/asio/test//any_completion_executor`; `libs/asio/test//any_completion_executor_select`; Asio checks `supportable_properties<0, props>::is_valid_target<fat_executor>::value` through recursive function-type partial specializations. | Template-id type resolution could instantiate the qualified owner template-id, but it did not then resolve a concrete member class template-id whose owner was itself a template-id qualifier such as `supportable_properties<I, void(Head)>::template is_valid_target<T>`. The structured bool evaluator therefore saw the left side of the recursive `&&` as dependent and rejected the valid target executor. | `pa21:400` | `pa21/tests/spec/400-qualified-member-template-id-bool-constant.t` | Non-STL reducer `supportable_properties_nested_prop_min.cpp` failed before the fix with `static_assert unevaluated`; focused Boost probes failed `is_valid_target_executor<fat_executor, props>::value`, and the B2 targets failed with `ERROR: no viable constructor [class boost::asio::any_completion_executor] [arg_count 1]`. | Regression and clang syntax control pass; placement audit marks the PA21 cluster-400 regression owner-reached; focused LowIR output matches; `ACTIVE_TEST_REPORT_PAS='pa21' TEST_REPORT_ASSIGNMENT_JOBS=1 TEST_REPORT_SUBTEST_JOBS=8 ORDERED=false make test-report-nobuild` passes `184/184`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa21` passes with `compared=165 failures=0 skipped=19`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor libs/asio/test//any_completion_executor_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.07%` instructions, RSS `+1.83%`, footprint `+0.85%`). | `17b455b44` |
| committed | `libs/asio/test//coroutine`; `libs/asio/test//coroutine_select`; Asio's stackless coroutine macro expands `reenter(coro)` to `switch (coroutine_ref _coro_value = coro)`. | Switch condition declarations were checked and lowered using the declared variable's class type instead of the condition value after contextual switch conversion. A class with `operator int()` was therefore rejected semantically or, after semantic conversion, still emitted the class object as the LowIR switch selector. | `pa16:400` | `pa16/tests/general/400-switch-condition-declaration-conversion-operator.t` | Minimal reducer `switch (ref state = value)` with `ref::operator int() const` failed before the fix with `ERROR: invalid switch condition type [function f]`; after only the semantic conversion, LowIR failed with `switch requires integer selector`. | Regression compiles/runs and clang accepts the reducer; explicit conversion control remains rejected like clang; placement audit marks PA16 cluster-400 owner-reached; `ACTIVE_TEST_REPORT_PAS='pa14 pa16' TEST_REPORT_ASSIGNMENT_JOBS=2 TEST_REPORT_SUBTEST_JOBS=8 ORDERED=false make test-report-nobuild` passes `187/187`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=8 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//coroutine libs/asio/test//coroutine_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.07%` instructions, RSS `+1.94%`, footprint `+0.84%`). | `59f44ad04` |
| committed | `libs/asio/test//placeholders`; `libs/asio/test//placeholders_select`; libc++ exposes `std::placeholders::_1` through `static constexpr auto& error = std::placeholders::_1` in `boost/asio/placeholders.hpp`. | Namespace and out-of-class `constexpr` variables always required a folded scalar `constexpr_value`. Reference variables already preserved their constant initializer, but `constexpr T&` cannot fold to a scalar value; the semantic gate rejected a valid reference constant initializer before output/lowering could treat it as reference storage. | `pa20:300` | `pa20/tests/general/300-constexpr-object-reference-binding.t` | Non-STL reducer `namespace target { static constexpr int &alias = source::value; }` failed before the fix with `ERROR: unsupported constexpr variable initializer`; the original Boost targets failed at `boost/asio/placeholders.hpp:63:49`. | Regression passes; placement audit marks the PA20 cluster-300 constexpr-object regression owner-reached; `ACTIVE_TEST_REPORT_PAS='pa20' CPPGM_SKIP_DEV_REBUILD=1 ORDERED=false make test-report` passes `72/72`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//placeholders libs/asio/test//placeholders_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.11%` instructions, RSS `+1.71%`, footprint `+0.84%`). | `26879cb47` |
| committed | `libs/asio/test//ip_tcp`; `libs/asio/test//ip_tcp_select`; `basic_socket_iostream::operator=(basic_socket_iostream&&)` calls the protected libc++ `std::basic_iostream<char>::operator=(std::basic_iostream<char>&&)`. | Ordinary class member declarations registered from an `init_declarator` did not inherit `_LIBCPP_HIDE_FROM_ABI` / `exclude_from_explicit_instantiation` metadata from the containing class `simple_declaration`, and stored out-of-class member definitions for class templates did not carry the declaration's exclusion bit. The extern-template suppression path therefore emitted only a declaration for an inline hidden out-of-class member definition that must be emitted locally. | `pa34:600` | `pa34/tests/link/600-hosted-basic-iostream-move-assign-link-smoke.t` | Hosted reducer `/tmp/cppgm-boost-reducers-20260521/basic_iostream_move_assign.cpp` linked under clang but failed pre-fix with undefined `std::__1::basic_iostream<char, std::__1::char_traits<char>>::operator=(...)`; LowIR contained only a weak declaration for the operator. | Reducer now links and LowIR emits the weak local operator definition; placement audit marks the PA34 hosted smoke owner-reached; focused Boost run for `libs/asio/test//ip_tcp` and `libs/asio/test//ip_tcp_select` passes; `ACTIVE_TEST_REPORT_PAS='pa34' CPPGM_SKIP_DEV_REBUILD=1 ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `123/123`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.21%` instructions, RSS `-1.07%`, footprint `+0.94%`). | `10a43d1ec` |
| committed | `libs/asio/test//io_context`; `libs/asio/test//io_context_select`; `boost::asio::io_context ioc4{test_context_service_maker{}}` constructs a braced derived service-maker temporary whose base has a protected default constructor. | Constexpr/default-constructor replay initialized implicit base and member subobjects using the caller scope instead of the constructor's own scope. A synthesized derived default constructor therefore attempted to default-construct a protected base subobject from `main`, causing a false inaccessible-constructor diagnostic. | `pa15:200` | `pa15/tests/general/200-default-constructor-protected-base-subobject-scope.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260521/braced_derived_service_maker.cpp` is accepted by clang but failed pre-fix with `ERROR: no viable constructor [class noncopyable] ... [context constexpr construction]`; Boost `io_context.o` and `io_context_select.o` failed at the same `noncopyable` default-construction path. | Reducer and PA15 regression pass; placement audit with `--fail-on-early` accepts the new PA15 cluster-200 regression; `ACTIVE_TEST_REPORT_PAS='pa15' CPPGM_SKIP_DEV_REBUILD=1 ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `134/134`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict` passes; focused Boost `io_context` and `io_context_select` now compile/link and the service test passes, advancing to a separate runtime failure in `io_context_test`; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.22%` instructions, RSS `-0.89%`, footprint `+0.94%`). | `8e8746118` |
| committed | `libs/asio/test//deadline_timer`; `libs/asio/test//deadline_timer_select`; `deadline_timer_cancel_test` constructs a local array of timer objects and schedules waits on the elements. | Local declarations only requested class lifetime actions when the declared type itself was a class. Default-initialized arrays of class objects therefore emitted raw storage without element constructors/destructors, leaving Asio timer executors uninitialized. | `pa15:200` | `pa15/tests/general/200-local-default-class-array-lifecycle.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260521/local_default_class_array_ctor_count.cpp` returned `1` before the fix because no element constructors ran; Boost `deadline_timer_cancel_test` segfaulted/bad-executor when `async_wait` used unconstructed timer array elements. | Reducer passes; PA15 regression passes; placement audit with `--fail-on-early` accepts the PA15 cluster-200 regression; `ACTIVE_TEST_REPORT_PAS='pa15' CPPGM_SKIP_DEV_REBUILD=1 ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `135/135`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict` reports only the existing PA19 direct-text drift; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//deadline_timer libs/asio/test//deadline_timer_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.02%` instructions, RSS `-0.61%`, footprint `+0.85%`). | `f5e6fca40` |
| committed | `libs/asio/test//io_context`; `libs/asio/test//io_context_select`; nested `dispatch(*ioc, handler)` inside `io_context::run()` depends on Asio's `call_stack<scheduler, thread_info_base>::top_` template static object constructing its pthread key. | Required template static member output skipped default class lifetime actions for class-type static data members with no initializer. The instantiated `static tss_ptr<context> top_` storage stayed zero-initialized, so `pthread_setspecific`/`pthread_getspecific` used an unconstructed key and `scheduler::can_dispatch()` stayed false inside `run()`. | `pa18:100` | `pa18/tests/general/100-class-template-static-member-class-lifetime.t`; updated `pa18/tests/general/300-namespace-function-template-hides-outer-callable-object.ref` to drop now-unneeded no-op static construction. | Non-STL reducer `/tmp/cppgm-boost-reducers-20260521/template_static_object_constructor.cpp` returned `1` before the fix; `/tmp/cppgm-boost-reducers-20260521/template_static_pthread_holder.cpp` reproduced the pthread-key shape. Boost reducer `/tmp/cppgm-boost-reducers-20260521/io_context_nested_dispatch_direct.cpp` showed `running_in_this_thread()` false and aborted before the fix. | Reducers pass; placement audit with `--fail-on-early` accepts the PA18 cluster-100 regression; `ACTIVE_TEST_REPORT_PAS='pa18 pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `342/342`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict` reports only the existing PA19 direct-text drift; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//io_context libs/asio/test//io_context_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.26%` instructions, RSS `-0.03%`, footprint `+0.85%`). | `2c07f19e3` |
| committed | `libs/asio/test//streambuf`; `boost::asio::basic_streambuf` initializes `pend` with `(std::min<std::size_t>)(max_size_, buffer_delta)`. | Parenthesized type-id/expression disambiguation treated a qualified template-id from an included namespace as a C-style cast target unless the full template-id was already known to be a value. The lazy header body then reached call analysis with `std::min<std::size_t>` as an ordinary unresolved id-expression instead of a structured function-template call. | `pa18:100` | `pa18/tests/general/100-lazy-header-parenthesized-qualified-function-template-id-call.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260521/lazy_parenthesized_template_id_call.cpp` and the checked-in PA18 regression fail on the baseline compiler with `unknown function LazyTemplateIdLib::template_id_min<T>`; Boost reducer `/tmp/cppgm-boost-reducers-20260521/asio_streambuf_min.cpp` failed with `unknown function std::min<std::size_t>`. | Reducers pass; the baseline compiler from `/Users/vishvananda/cppgm/dev/cppgm++` still fails the checked-in regression; placement audit marks PA18 cluster-100 owner-reached; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false make test-report` passes `159/159`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make -C pa18 test-strict CPPGM_SKIP_DEV_REBUILD=1` passes with `compared=134 failures=0 skipped=25`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test/streambuf` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`+0.19%` instructions, RSS `-0.01%`, footprint `+0.90%`). | `34e2699d3` |
| committed | `libs/asio/test//inline_or_executor`; `libs/asio/test//inline_or_executor_select`; constructing `boost::asio::inline_or_executor<boost::asio::executor>` from an `io_context` executor links the type-erased executor implementation after scheduler work-count comparisons. | Binary operator resolution returned the first viable overloaded operator before ranking it against viable builtin candidates. `std::__1::atomic<long> == 0` could therefore select Asio's unrelated `operator==(executor, executor)` via two user-defined conversions, causing bogus `executor::impl<int>` and `executor::impl<std::atomic<long>>` output. After the overload fix exposed the real link path, inline polymorphic constructors still did not trigger the required vtable output pass, leaving the legitimate `executor::impl<io_context::executor_type>` vtable undefined. | `pa16:400`, `pa17:400` | `pa16/tests/general/400-builtin-comparison-prefers-fewer-user-conversions.t`; `pa17/tests/general/400-inline-polymorphic-constructor-vtable.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260521/asio_inline_or_executor_link.cpp` failed pre-fix with missing vtables for both bogus and legitimate executor implementation specializations; clang emitted only the legitimate `io_context::executor_type` implementation. | Reducer compiles, links, and runs; placement audit marks the PA16 conversion-operator comparison and PA17 polymorphic vtable regressions owner-reached; `ACTIVE_TEST_REPORT_PAS='pa15 pa16 pa17' ORDERED=false make test-report` passes `293/293`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 ACTIVE_TEST_REPORT_PAS='pa15 pa16 pa17' ORDERED=false make test-report` passes `293/293`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//inline_or_executor libs/asio/test//inline_or_executor_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.34%` instructions, RSS `-0.32%`, footprint `+0.86%`). | `4342bc0e4` |
| committed | `libs/asio/test//read`; `libs/asio/test//read_select`; `libs/asio/test//read_at`; `libs/asio/test//read_at_select`; `consuming_buffers<...>::prepare` tests `(result.count) < result.max_buffers`. | Member access to a class-scope enumerator through an object, such as `result.max_buffers`, was routed to the static-member variable path because enumerators are stored as `VK_VARIABLE` bindings. That path tried to emit/load a storage-backed id-expression for an anonymous enum constant instead of producing the enumerator's prvalue constant. | `pa15:100` | `pa15/tests/general/100-object-member-enumerator-constant.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260521/member_enum_object_access_min.cpp` failed pre-fix with `unknown id-expression max_buffers`; clang accepted it. The Boost read/read_at targets failed at the same `prepared_buffers::max_buffers` expression. | Reducer compiles and runs; placement audit marks the PA15 class/member regression owner-reached; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false CPPGM_SKIP_DEV_REBUILD=1 make test-report` passes `136/136`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//read libs/asio/test//read_select libs/asio/test//read_at libs/asio/test//read_at_select` passes all four targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.26%` instructions, RSS `-1.47%`, footprint `+0.90%`). | `b9f616fd3` |
| committed | `libs/asio/test//strand`; `libs/asio/test//strand_select`; `strand_executor_service::do_execute` constructs `invoker<Executor>(impl, ex)` where `invoker` is an out-of-class partial specialization of a nested class template. | The enclosing class reference-member collection reset preserved nested class templates only when their primary definition lived outside the owner. A nested class-template primary declared inside the class but carrying out-of-class explicit/partial specializations was cleared and recreated during full collection, so instantiating `service::wrapper<T, void>` saw only the primary forward declaration and synthesized implicit special members. | `pa21:400` | `pa21/tests/general/400-nested-member-partial-specialization-survives-reference-reset.t` | Reducers `/tmp/cppgm-boost-reducers-20260521/nested_member_partial_specialization_ctor.cpp` and `/tmp/cppgm-boost-reducers-20260521/nested_member_partial_specialization_ctor_pa21.cpp` failed pre-fix with no viable constructor for the nested partial-specialization type; clang accepted both. The Boost targets failed at `ex.execute(invoker<Executor>(impl, ex))` after dropping the `invoker` constructor. | Reducers compile and run; placement audit reports only earlier-owned support features for the PA21 cluster-400 regression; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false CPPGM_SKIP_DEV_REBUILD=1 make test-report` passes `185/185`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_PAS='pa21' STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes with `compared=165 failures=0 skipped=20`; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//strand libs/asio/test//strand_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.26%` instructions, RSS `-1.04%`, footprint `+0.87%`). | `6df9e3ccd` |
| committed | `libs/asio/test//system_timer`; `libs/asio/test//system_timer_select`; libc++ `std::chrono::duration_cast` evaluates `__duration_cast<...>()(__fd).count()` while constructing a timer duration. | Constant-call evaluation resolved the constexpr `operator()` on a temporary functor, but for non-member-expression callees it used the current `this` object as the method implicit object. In a converting `duration` constructor this re-entered the same constructor path until stack overflow instead of evaluating the temporary `__duration_cast` object as the implicit object. | `pa20:400` | `pa20/tests/general/400-constexpr-temporary-functor-member-call.t` | Reducer `/tmp/cppgm-boost-reducers-20260521/chrono_timer_one_static_int.cpp` segfaulted before the fix; Boost `system_timer.o` and `system_timer_select.o` failed with compiler exit 139. Live `CPPGM_TRACE=consteval` showed repeated duration construction after the callable temporary path. | Non-STL PA20 regression passes; chrono reducers and the Boost.Asio include reducer pass; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//system_timer libs/asio/test//system_timer_select` passes both targets; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 CPPGM_SKIP_DEV_REBUILD=1 make test-strict` reports only the pre-existing PA19 direct-text drift while PA18/21/22 compare clean; `ACTIVE_TEST_REPORT_PAS='pa20' CPPGM_SKIP_DEV_REBUILD=1 make test-report` still reports the pre-existing `300-function-local-static-array-guard.t` LowIR drift, verified with this code change stashed; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.40%` instructions, RSS `-1.08%`, footprint `+0.85%`). | ce0595ee7 |
| committed | `libs/asio/test/execution/{blocking,blocking_adaptation,inline_exception_handling,mapping,outstanding_work,relationship}`; Asio `BOOST_ASIO_TEST_CASE(test_can_query<...>)` calls `boost::asio::detail::run_test<&test_can_query<...>>`. | Explicit non-type template argument resolution handled ordinary function names but not structured function-template-id operands. `&test_can_query<executor,property,true>` instantiated the specialization during lookup, then failed to bind it as the function-pointer NTTP value for `void (*)()`, so the qualified `run_test` call was dropped as an unknown function. | `pa22:400` | `pa22/tests/spec/400-function-template-nontype-function-pointer-specialization-call.t` | Reducer `/tmp/cppgm-boost-reducers-20260521/qualified_function_template_nttp_call.cpp` failed before the fix with `ERROR: unknown function ns::detail::run_test<&test_can_query<executor,property,true>>`; `CPPGM_TRACE=template.resolve` showed `explicit-arg-resolution-failed` after structured function-template-id lookup found the specialization. | Reducer passes; PA22 regression and report pass (`430/430`); strict LowIR compare reports only the known pre-existing PA19 direct-text drift while PA18/21/22 compare clean; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.03%` instructions, RSS `+0.18%`, footprint `+0.90%`); focused six-target Asio execution rerun advances past the original NTTP diagnostic but was stopped after six parallel compiles spent about 6m in later template-output analysis without producing objects. | 8c4012566 |
| committed | `libs/asio/test/execution//prefer_only`; `any_executor<blocking, prefer_only<blocking.possibly>, prefer_only<blocking.never>>` constructs from `either_blocking_executor`, whose executor trait depends on `std::is_nothrow_copy_constructible`. | `__is_nothrow_constructible(T, const T&)` analyzed implicit copy constructors like default constructors. For a class with a member whose default constructor may throw, cppgm default-initialized the member while computing the copy constructor exception specification, so Asio considered `either_blocking_executor` not copy-constructible enough to be a target executor. | `pa33:600` | `pa33/tests/compile/600-builtin-nothrow-copy-constructible-implicit-member.t` | Boost trait probe `scratch/asio_prefer_only_trait_probe.cpp` isolated `boost::asio::is_nothrow_copy_constructible<either_blocking_executor>::value` as false. The checked-in regression fails on the baseline compiler from `/Users/vishvananda/cppgm-extended/dev/cppgm++` with the copied-holder static assert while clang accepts it. | Regression and clang control pass; placement audit marks the new PA33 cluster-600 builtin-trait test owner-reached, with only existing older PA33 cluster-early warnings elsewhere; `ACTIVE_TEST_REPORT_PAS='pa33' CPPGM_SKIP_DEV_REBUILD=1 ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `299/299`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 CPPGM_SKIP_DEV_REBUILD=1 STRICT_SUBTEST_JOBS=12 make test-strict` reports only the known pre-existing PA19 direct-text drift while PA18/21/22 compare clean; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test/execution//prefer_only` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-0.23%` instructions, RSS `-0.42%`, footprint `+0.90%`). | `4ea549989` |
| committed | Strict LowIR compare fallout from the local default class-array lifecycle fix; `pa19/tests/general/300-template-instantiation-use-location-explicit-specialization.t` default-initializes `Local a[1]`. | The compiler now correctly emits an element default-constructor call for the local class array, but the PA19 LowIR reference still reflected the older missing-constructor output. | `pa19` | Reference update only: `pa19/tests/general/300-template-instantiation-use-location-explicit-specialization.ref`. | `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa19` failed with a direct LowIR diff showing the missing default-constructor call and constructor definition. The same failure reproduced with the pending Asio cache patch stashed, so it was not caused by that patch. | Updated only the LowIR `.ref`; witness output already matched. `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa19` passes (`102/102`, `compared=92 failures=0 skipped=10`); full `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes after the cache patch is restored. | `608c71e1b` |
| committed | `libs/asio/test/execution/{blocking,blocking_adaptation,inline_exception_handling,mapping,outstanding_work,relationship}` after the function-template NTTP fix; the six targets advanced past the original diagnostic but spent minutes in repeated Asio execution-property template analysis. | Template-resolution caches keyed otherwise-equivalent template-bound scopes and function-template deduction arguments by transient scope/type identity. Asio repeatedly rebuilds structurally identical execution CPO predicate contexts, so the compiler missed stable cache hits and re-expanded dependent aliases and function-template deductions across equivalent scopes. | perf/cache | No behavioral regression test; this is a compile-time frontier for existing Asio execution tests. | Prefix reducers such as `/tmp/asio_blocking_64.cpp`, `/tmp/asio_blocking_256.cpp`, and `/tmp/asio_blocking_512.cpp` showed very high `resolve_template_arguments` and function-template deduction traffic around `is_applicable_property`, `can_query`, and related Asio predicates. `_512` remained a long compile but improved from timing out at 240s to about 238s, while smaller prefixes improved substantially. | Added stable template-bound scope cache identity, structural type fingerprints for function-template deduction cache arguments, and stable alias-expansion caching outside witness source capture. `make -C dev cppgm++` passes; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes with instructions `-12.24%`, RSS `-2.85%`, footprint `-1.53%`. | `123e4e2fc` |
| committed | `libs/asio/test/execution//blocking_adaptation`; `libs/asio/test/execution//outstanding_work`; `libs/asio/test/execution//relationship`; Asio execution property adapters define hidden friend `require`/property conversion helpers inside class templates and return a flipped class-template specialization from the friend body. | Inline non-member friend definitions inside class templates were output using the namespace declaration scope when `FunctionBinding::declaration_scope` was present. The instantiated hidden friend had the correct function type, but body analysis resolved class-template parameters from namespace scope, so `return ex_fq_fr<Other, Current>()` was checked as `ex_fq_fr<Other, Other>` and rejected. | `pa18:200` | `pa18/tests/general/200-class-template-hidden-friend-body-lexical-scope.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260521/friend_flip_return.cpp` failed pre-fix with `invalid return expression [target struct pair_box<second, first>] [expr_type struct pair_box<second, second>] [function flip]`; clang accepted it. | Reducer and clang control compile; placement audit marks PA18 cluster-200 owner-reached; focused PA18 check passes; `ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 TEST_REPORT_ASSIGNMENT_JOBS=1 CPPGM_SKIP_DEV_REBUILD=1 make test-report-nobuild` passes `160/160`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test/execution//blocking_adaptation libs/asio/test/execution//outstanding_work libs/asio/test/execution//relationship` passes all three targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-12.21%` instructions, RSS `-2.39%`, footprint `-1.53%`). | `eed7133bf` |
| committed | `libs/asio/test/execution//blocking`; after the hidden-friend and template-bound cache fixes, the full Boost target still spent minutes repeatedly selecting the same concrete class-template partial specializations for Asio execution-property traits. | Class-template partial specialization selection was recomputed for non-dependent argument lists even when the same template-id key had already selected an explicit specialization, primary template, or winning partial specialization at the same specialization epoch. Asio reuses those exact concrete trait instantiations heavily while evaluating execution CPO predicates. | perf/cache | No behavioral regression test; this is a compile-time frontier for existing Asio execution tests. | Subset reducers in `/tmp/cppgm-asio-blocking-subsets/` isolated the repeated selection cost: `cq_prefer_head30.cpp` improved from about `109.5s` to `23.4s`, and `cq_prefer_head8.cpp` improved from about `31.5s` to `9.8s`; `cq_can_prefer.cpp` stayed about flat. | Cached non-dependent class specialization selections on `ClassTemplateDecl`, gated off during witness source-use capture and template tracing. `make -C dev -j12 cppgm++` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test/execution//blocking` passes in `334.88s`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-14.23%` instructions, RSS `-1.90%`, footprint `+0.10%`). | `3ea4d050c` |
| committed | `libs/asio/test//spawn`; `libs/asio/test//spawn_select`; lazy materialization of `boost/asio/impl/spawn.hpp` reaches `function(&spawned_thread);` inside `spawn_entry_point`. | Statement-ambiguity parsing treated an identifier as a declaration starter whenever any visible type name matched it, even when a nearer block-scope value of the same name existed. In the lazy header-body path the outer `Function function(...)` parameter type hint survived, so the local callable `function` call was parsed as a local declaration and rejected as an unsupported local decl-specifier. | `pa10:300` | `pa10/tests/spec/300-declaration-statement-ambiguity.t` | AST reducer `/tmp/cppgm-boost-reducers-20260521/spawn_local_value_hides_type_call.cpp` parsed `function(&spawned_thread);` as a `simple-declaration` before the fix and as an `expression-statement` after it; full Boost trace showed `can-start-decl-specifier-seq token=TT_IDENTIFIER:function next=OP_LPAREN:(` at the failing line. | PA10 regression and ref pass; placement audit no longer flags the added PA10 row after using a typedef type hint; `ACTIVE_TEST_REPORT_PAS='pa10' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-report` passes `128/128`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//spawn libs/asio/test//spawn_select` advances past the parser failure to a separate `std::runtime_error` RTTI link symbol frontier; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-14.11%` instructions, RSS `-2.20%`, footprint `+0.17%`). | `3c8a22d43` |
| committed | `libs/asio/test//spawn`; `libs/asio/test//spawn_select`; libc++ `std::__make_exception_ptr_explicit<std::runtime_error>` passes `&typeid(_Ep)` to `__cxa_init_primary_exception`. | Static `typeid(type-id)` lowering kept only the result type (`const std::type_info`) on the semantic node, so the host ABI path could not recover the operand type and emitted cppgm's internal `@__rtti_class_std__runtime_error` symbol. The fix records the typeid operand type semantically and uses it to select the host `_ZTISt13runtime_error` alias when host runtime support is active. | `pa32:200` | `pa32/tests/general/200-host-make-exception-ptr-runtime-error-rtti.t` | Non-STL hosted reducer `/tmp/cppgm-boost-reducers-20260521/make_exception_ptr_runtime_error_min.cpp` failed before the fix with undefined `__cppgm_405f5f727474695f636c6173735f7374645f5f72756e74696d655f6572726f72` from `std::__make_exception_ptr_explicit<std::runtime_error>`. | Reducer and clang control run; generated LowIR now uses `@__external_rtti__class_std__runtime_error` in the `__cxa_init_primary_exception` call; PA32 single test and full PA32 report pass (`81/81`); placement audit has no finding for the PA32 regression; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 STRICT_SUBTEST_JOBS=12 CPPGM_SKIP_DEV_REBUILD=1 make test-strict-nobuild` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//spawn libs/asio/test//spawn_select` passes both targets; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-14.16%` instructions, RSS `-0.13%`, footprint `+0.20%`). | `8a93e3e14` |
| committed | `libs/asio/test/experimental//awaitable_operators`; `libs/asio/test/experimental//awaitable_operators_select`; Boost.Asio's experimental awaitable operators parse `co_await`, `co_yield`, and `co_return` spellings inside uninstantiated template bodies while the frontier build still uses `-std=c++11`. | The lexer classifies C++20 coroutine spellings as identifiers in the C++11 mode used by the Boost run, but the parser had no contextual handling for them. `co_await expr` was parsed as an id-expression followed by an unconsumed operand, so the containing function template body failed and the later template declaration was blamed. | `pa33:700` | `pa33/tests/compile/700-hosted-coroutine-contextual-operators-compile.t` | Reducer `/tmp/cppgm-boost-reducers-20260521/asio_awaitable_operator_coroutine_contextual.cpp` failed before the fix with `expected declaration after template-parameter-clause near TT_IDENTIFIER:awaitable`; the old PA10 unsupported-coroutine test still fails semantically after the fix. | Reducer compiles; PA33 regression passes and verifies the contextual spellings do not steal already-bound ordinary identifiers; placement audit reports no early-placement issue for the new `pa33:700` test; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test/experimental//awaitable_operators libs/asio/test/experimental//awaitable_operators_select` advances to the next `constraint_t<!is_constructible<T>::value>* = 0` function-template parameter frontier; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-14.03%` instructions, RSS `-1.62%`, footprint `+0.16%`). | `9cfe12201` |
| committed | `libs/asio/test/experimental//basic_channel`; `channel_service<Mutex>::implementation_type<Traits, R()>` is an out-of-class partial specialization of a member class template whose owner is still a dependent class-template-id. | Qualified class partial-specialization scope resolution required a concrete class scope and recorded the partial only on the dependent reference owner. Concrete `channel_service<int>::implementation_type` instantiations therefore did not inherit the pending member-template partial, and the specialization pattern also needed the owner template arguments bound before replay. | `pa21:400` | `pa21/tests/general/400-dependent-owner-member-class-template-partial-specialization.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260521/asio_basic_channel_dependent_owner_member_partial.cpp` failed before the fix with `unknown qualified class partial specialization scope`; Boost failed in `boost/asio/experimental/detail/channel_service.hpp` at the same dependent-owner member partial specialization. | Reducer compiles; focused PA21 regression passes; placement audit marks the new test OK at `pa21:400`; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `186/186`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; focused `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test/experimental//basic_channel` passes; perf check against `/tmp/cppgm-perf-baseline-pre-bind-allocator-27aff887b-20260520.json` passes (`-13.71%` instructions, RSS `-1.69%`, footprint `+0.83%`). | `6178e3dda` |
