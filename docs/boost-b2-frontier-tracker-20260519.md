# Boost B2 Frontier Tracker - 2026-05-19

This tracker starts from `origin/fix-lowir-external-lifecycle-perf` and the
rebased Boost frontier branch.

## Baseline

- base branch: `fix-lowir-external-lifecycle-perf`
- base commit: `529454c61befdf931373f6dc32b81983ec0d7390`
- rebased frontier head at tracker start:
  `8c9e3ce62fc95e6b8b88ebbe854f05fadae3627c`
- safety branch:
  `backup/boost-frontier-before-lowir-lifecycle-rebase-20260519-072414`
- perf baseline:
  `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json`
- rebased stack perf check: passed, instructions `-14.88%`, RSS `-2.77%`,
  footprint `-5.72%`
- rebased-head perf baseline: recorded at branch head and used as the baseline
  for subsequent Boost frontier fixes
  `6735bc6cf22ab9de04de945a25cf6583235acb92`, compiler-equivalent to the
  rebased frontier head; median instructions `312,198,629,210`, RSS `1.10 GiB`,
  footprint `856.84 MiB`
- active perf baseline: `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json`
  recorded at `c4b544837865a4464f268511ff23e03e0ff38a37` after accepting the
  current rebased frontier head as the new baseline; median instructions
  `312,426,043,980`, RSS `1.08 GiB`, footprint `857.18 MiB`

Local Boost wrapper state:

- `/Users/vishvananda/boost_1_91_0/cppgm-b2-toolchain.sh` points at this
  worktree's `dev/cppgm++`.
- The wrapper routes C and assembly compile/preprocess actions to host clang,
  C++ compile/preprocess actions to `cppgm++`, and link actions to host clang++.

## Sequential Process

1. Run Boost suites in the order listed in
   `docs/boost-b2-suite-status-20260511.md`.
2. Use one suite at a time with B2 compile parallelism, for example:

   ```sh
   cd /Users/vishvananda/boost_1_91_0
   JOBS=12 ./run-cppgm-b2.sh -a libs/accumulators/test
   ```

3. If the suite passes, mark it in this tracker and continue to the next suite.
4. If it fails, identify the first real compiler frontier, reduce it to the
   smallest useful source, and prefer a non-STL reducer when possible.
5. Place the regression in the earliest owning PA and first correct cluster.
   Use `ROADMAP.md`, PA READMEs, the reducer's essential assertion, and:

   ```sh
   python3 scripts/audit_pa_feature_placement.py --pa paNN \
     --markdown-out /tmp/boost-frontier-placement-paNN.md \
     --json-out /tmp/boost-frontier-placement-paNN.json \
     --fail-on-early
   ```

   Include every touched PA. Course tests for PA1-PA9 belong under
   `cppgm.tests/course/pa*` unless the reducer is a hosted C++ extension, in
   which case use the hosted compatibility PAs.
6. Before implementation changes, confirm the reducer fails on the current
   branch and record the pre-fix diagnostic here.
7. Fix the compiler in `dev/`, validate the focused regression, the owner PA
   report, strict LowIR compare when relevant, and the Boost target.
8. Run the perf gate against the baseline above before committing any compiler
   fix.
9. Commit each coherent fix with the regression, implementation, and tracker
   update before moving to the next unrelated frontier.

## Suite Cursor

| # | Suite | Status | Notes |
|---|---|---|---|
| 1 | `libs/accumulators/test` | pass | `JOBS=12`, 9s, log `/tmp/boost-frontier-seq-20260519-073659-j12-6735bc6cf/libs__accumulators__test.log`. |
| 2 | `libs/algorithm/test` | pass | `JOBS=12`, direct run, updated 218 targets after integral NTTP value-binding conversion fix. Expected negative tests still fail as expected under B2. |
| 3 | `libs/align/test` | pass | `JOBS=12`, direct run, updated 60 targets after Apple hosted compiler macro import. |
| 4 | `libs/any/test` | pass | `JOBS=12`, direct run after cv-correct rvalue-reference binding fix updates 164 targets. The previous runtime failures pass, and the negative compile tests including `unique_const_rvalue_construction_failed.cpp` now fail as expected under B2. |
| 5 | `libs/array/test` | pass | `JOBS=12`, direct run after braced array reference argument lowering fix; `to_array_test` and full suite pass. |
| 6 | `libs/asio/test` | frontier | Initial suite run exposed 211 `unsupported out-of-class conversion operator` failures through Boost.System. After fixing qualified conversion-operator binding, inherited member class-template lookup, function-type tail pack expansion, recursive supportable-property member-template owner binding, constructor SFINAE trait evaluation, the hidden-friend `query_free` probe, explicit function-pointer NTTP calls, and trailing-pack target deduction for function-template addresses, `libs/asio/test//any_completion_executor` advances to a libc++ tuple explicit type argument fallback in `std::__1::_Not<std::__1::tuple<int *>::_IsThisTuple<int *>>`. |

## Fix Ledger

| Status | Frontier seed | Root cause | Owner PA | Regression | Pre-fix result | Validation | Fix commit |
|---|---|---|---|---|---|---|---|
| committed | `libs/algorithm/test` dependency build via Boost.Test/Boost.Regex; libc++ `std::__1::vector` iterator conversion needed `__wrap_iter<T*>` to `__wrap_iter<T const*>` | Structural alias-template expansion handled a dependent alias before preserving an outer `TK_CV`, so `const remove_reference_t<T> &` with `T=item &` collapsed to `item &` during defaulted SFINAE checks. | `pa22:500` | `pa22/tests/general/500-const-reference-alias-default-sfinae.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/alias_const_lvalue_ref_default_arg.cpp` failed with explicit type argument fallback / SFINAE rejection; STL reducer `/tmp/cppgm-boost-reducers-20260519/vector_const_iterator.cpp` failed to compile `vector::erase(v.begin())` / `insert(end(), begin(), end())`. | Reducers pass; focused LowIR ref matches; placement audit OK; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `407/407`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/algorithm/test` advances to subrange failures; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.14%` instructions). | `091da7fe6` |
| committed | `libs/algorithm/test` subrange targets; libc++ `std::default_random_engine` instantiates `__lce_ta<__a, __c, __m, _Mp>` from `linear_congruential_engine<unsigned int, ...>` | Non-type template argument resolution required exact integral type equality for named value bindings. A bound `unsigned int` constant could not be reused as an `unsigned long long` NTTP even when representable, so libc++'s `__lce_ta` helper failed when a value binding such as `__c` was carried semantically rather than as a syntax expression. | `pa19:100` | `pa19/tests/general/100-nontype-integral-value-binding-conversion.t` | Pre-fix compiler rejected `/tmp/cppgm-boost-reducers-20260519/nontype_integral_conversion_value_binding.cpp` with `static_assert unevaluated`; Boost `partition_subrange_test.o` and `sort_subrange_test.o` failed while instantiating `__lce_ta<__a,__c,__m,_Mp>`. | Reducer passes; placement audit OK for `pa19:100`; `ACTIVE_TEST_REPORT_PAS='pa19' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `100/100`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa19` passes with `compared=92`; direct subrange compiles pass; `JOBS=12 ./run-cppgm-b2.sh -a libs/algorithm/test` passes, updating 218 targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.53%` instructions). | `e0b2dac73` |
| committed | `libs/align/test` targets including `aligned_alloc_test.o`, `aligned_delete_test.o`, and aligned allocator tests include Apple `TargetConditionals.h` through hosted headers. | Host predefined macro import filtered out `__APPLE_CC__`, so Apple `TargetConditionals.h` saw `__GNUC__` without the Apple compiler-identity macro it requires and took its `unknown compiler` error branch. | `pa34:500` | `pa34/tests/compile/500-host-apple-target-conditionals-macro.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/apple_target_conditionals.cpp` failed with `#error TargetConditionals.h: unknown compiler`; `libs/align/test` had four compile failures with the same diagnostic. | Reducer passes; new test passes via `make -C pa34 check TEST=tests/compile/500-host-apple-target-conditionals-macro.t`; PA34 placement audit row for the new test is owner-reached, with unrelated existing PA34 cluster warnings still present; `ACTIVE_TEST_REPORT_PAS='pa34' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `294/294`; `JOBS=12 ./run-cppgm-b2.sh -a libs/align/test` passes, updating 60 targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.37%` instructions). | `03a278f97` |
| committed | `libs/any/test` `basic_tests<Any>::copy_counter::count` in `libs/any/test/basic_test.hpp` | The template declaration collector only recognized out-of-class static member definitions when the immediate qualifier was a template-id and resolved the owner path without the template pattern scope. Instantiation replay and output also assumed direct static member keys and source-template classes, so nested non-template class static storage inside a class template was not replayed or emitted. | `pa21:400` | `pa21/tests/general/400-template-nested-static-member-out-of-class-definition.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/nested_template_static_member_definition.cpp` failed with `failed type template argument resolution: T [scope n::<here>] ... lookup_type [n::outer<T>]`; after the collection path was corrected, the same reducer linked with an undefined `n::outer<int>::inner::value` until nested static definition replay/output was added. | Reducer compile/link/run passes; direct static-member control passes; placement audit OK for `pa21:400`; focused LowIR ref matches; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `175/175`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa21` passes with `compared=164`; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to separate `any_cast` compile failures and `unique_any` runtime failures; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.31%` instructions). | `91c14c0a2` |
| committed | `libs/any/test` `boost::any_cast<std::string>(returning_string1())`; Boost `any` has both `any(const T&)` and a forwarding `any(T&&, enable_if... = 0, enable_if... = 0)` constructor | Constructor-template overload selection compared instantiated defaulted `enable_if` parameters when applying the forwarding-reference tie-breaker. The selected candidate lists had different lengths, so the existing lvalue-reference-over-forwarding-reference preference did not run and `any(const T&)` tied with `any(T&&, ...)` for a `const T&` argument. | `pa22:300` | `pa22/tests/general/300-constructor-template-defaulted-forwarding-lvalue-order.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/dual_constructor_template_const_lvalue.cpp` failed with `ambiguous constructor [class box]`; Boost probe `/tmp/cppgm-boost-reducers-20260519/boost_any_construct_const_string_ref.cpp` failed the same way before the fix. | Non-STL reducer passes; selected-control reducer returns the `const T&` constructor path under clang; Boost `any` construct and `any_cast` probes pass; placement audit OK for `pa22`; focused LowIR ref matches; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `408/408`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes with `compared=381`; direct `libs/any/test/any_test.cpp` advances to separate nested placeholder member-initializer failure; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-rebased-8c9e3ce62-20260519.json` passes (`-0.49%` instructions). | `999535cc4` |
| committed | `libs/any/test` `boost::any::placeholder::clone()` in `boost/any.hpp`; nested `boost::any::placeholder` derives from `boost::anys::detail::placeholder`, which has the same unqualified class name. | Recording a direct base class blindly rebound the base injected-class name into the derived member scope. For a same-name nested derived class, that overwrote the derived injected-class-name, so `placeholder * clone() const` was stored as returning `boost::anys::detail::placeholder *` instead of `boost::any::placeholder *`. | `pa15:200` | `pa15/tests/general/200-nested-injected-class-name-hides-base-name.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/nested_class_name_shadows_base_in_return.cpp` failed with `invalid member initializer`; `CPPGM_TRACE=lifetime.init` showed `target_type=pointer to struct any::placeholder init_type=pointer to struct lib::placeholder`. | Reducer passes; focused PA15 check passes; placement audit OK for `pa15`; `ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `129/129`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes PA18/19/21/22 with no LowIR failures; direct `libs/any/test/any_test.cpp` compiles; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to 15 later failed targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-dee7966f4-20260519.json` passes (`+0.26%` instructions, within tolerance). | `fd3a1803d` |
| committed | `libs/any/test` MPL-if `boost::any_cast<double>(a)` and `boost::anys::any_cast<double>(a)` paths. | Scalar by-value returns of reference-typed expressions emitted the reference address directly. `static_cast<T&>(*result)` lowered to `return f64 %ptr`, leaving native lowering to treat a pointer temp as a floating result and fail with `unknown storage %t7`; integer variants compiled but returned pointer bits. | `pa14:200` | `pa14/tests/general/200-scalar-reference-static-cast-return.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/return_static_cast_ref_double_notemplate.cpp` failed with `ERROR: unknown storage %t1 in @read_value`; Boost `any_test_mplif.cpp` failed with `ERROR: unknown storage %t7 in @boost__any_cast__ov8`. | Reducer LowIR now loads `f64` through the reference address before return; focused PA14 check passes; placement audit OK for `pa14`; `ACTIVE_TEST_REPORT_PAS='pa14' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `51/51`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict-pa22` passes with `compared=381`; direct `any_test_mplif.cpp` and `basic_any_test_mplif.cpp` compiles pass; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to 13 failed targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-dee7966f4-20260519.json` passes (`+0.09%` instructions). | this commit |
| committed | `libs/any/test` no-RTTI `boost::typeindex::detail::after_substrig<ArrayLength - detail::skip().size_at_begin>` path. | The constexpr evaluator treated `T{...}` as a call with a braced argument before attempting typed initialization for class aggregates. A constexpr function returning an aggregate with `return skip_info{1};` therefore could not feed `skip().size_at_begin` into an integral constant expression or explicit non-type template argument. | `pa20:400` | `pa20/tests/general/400-constexpr-function-aggregate-member-nttp.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/constexpr_function_aggregate_member.cpp` failed with `unsupported constexpr variable initializer`; reducer `/tmp/cppgm-boost-reducers-20260519/nontype_param_member_constexpr_template_arg.cpp` failed resolving `after<N-skip().size_at_begin>`; Boost no-RTTI `any_test.cpp` failed in the `ArrayLength`/`after_substrig` family. | Reducers pass; direct Boost no-RTTI `any_test.cpp` compile passes; placement audit OK for `pa20`; focused PA20 check passes; `ACTIVE_TEST_REPORT_PAS='pa20' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `69/69`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to 7 failed targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-8df3b529-20260519.json` passes (`-0.48%` instructions, RSS `+2.99%`). | `eec0e35a8` |
| committed | `libs/any/test` `boost::anys::basic_any<8, 8>::small_manager` destroying a `const ValueType *` in the `Copy` path. | Explicit destructor member calls used ordinary implicit-object conversion, so `const T *` could not invoke `~T()` even though C++ permits destructor calls on const objects. | `pa15:300` | `pa15/tests/general/300-const-pointer-explicit-destructor-call.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/const_pointer_destructor_call_notemplate.cpp` failed with `no viable overload for member call ~value`; Boost `basic_any_test.cpp` failed in `small_manager` with the same implicit object conversion rejection. | Non-STL and nested-template reducers pass; direct Boost `basic_any_test.cpp` compiles with and without RTTI; placement audit OK for `pa15`; focused PA15 check passes; `ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `130/130`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to 6 failed targets; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-8df3b529-20260519.json` passes (`-0.03%` instructions). | `f310f7c67` |
| committed | `libs/any/test/unique_any/emplace.cpp` initializer-list construction and member `emplace<non_movable_t>({ ... }, unique_ptr<int>)` | Function-template candidate deduction analyzed braced-init-list arguments target-aware against still-dependent parameters. That let a plain `Args&&...` candidate deduce from a braced-init-list and also let `std::initializer_list<U>` materialize as `std::initializer_list<U>` before `U` was deduced, leaving stale dependent argument metadata in the selected call. | `pa27:200` | `pa27/tests/general/200-member-template-initializer-list-deduction.t` | Original Boost compile failed by selecting the forwarding-pack path and then rejecting `non_movable_t`; after rejecting braced lists for non-`initializer_list` patterns, reducer `/tmp/cppgm-boost-reducers-20260519/initlist_member_template_emplace_min.cpp` failed with `unsupported expression in PA14 LowIR lowering kind=initializer-list-object type=class std::initializer_list<U>`. | Reducer passes; direct Boost `unique_any/emplace.cpp` compiles; placement audit OK for `pa27:200`; focused PA27 check and report pass (`39/39`); PA22 report passes (`408/408`); `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`-0.19%` instructions). | `080ebc54b` |
| committed | `libs/any/test` runtime failures in `any_test`, `basic_any_test`, `unique_any/unique_base`, and `unique_any/unique_from_any`; `boost::typeindex::stl_type_index::equal` compared a null `typeid(int *).name()` | Host RTTI lowering emitted pointer typeinfo objects as scalar zero placeholders. That made the typeinfo address stable enough for `typeid(T*) == typeid(T*)` but left the ABI `std::type_info` layout invalid, so `name()` returned null and Boost.TypeIndex crashed in `strcmp`. | `pa27:100` | `pa27/tests/general/100-typeid-pointer-rtti-global.t`; updated `pa27/tests/general/100-typeid-template-pointer-no-eager-complete.ref` to require structured pointer RTTI | Reducers `/tmp/cppgm-boost-reducers-20260519/typeid_pointer_name_null.cpp` and `/tmp/cppgm-boost-reducers-20260519/typeid_pointer_name_compare.cpp` returned failure/segfault before the fix; `lldb` showed the Boost runtime failures stopped in `_platform_strcmp` from `boost::typeindex::stl_type_index::equal`. | Pointer RTTI reducers pass; direct Boost `unique_any/base.cpp` and `unique_any/from_any.cpp` compile/link/run; placement audit OK for `pa27`; focused PA27 checks pass; `ACTIVE_TEST_REPORT_PAS='pa22 pa27' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `449/449`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances past the previous runtime failures and reports `failed updating 0 target`; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`-0.42%` instructions). | `79dce277f` |
| committed | `libs/any/test/unique_any/emplace.cpp` direct `unique_any b(in_place_type_t<int>{})` constructed a holder for the tag and later threw `boost::bad_any_cast` | Function-template partial ordering compared transformed parameter lists against full constructor parameter lists after default arguments were appended. The forwarding constructor's defaulted SFINAE parameter and the in-place constructor's empty trailing pack were both treated as participating parameters, so the less-specific forwarding constructor beat the in-place overload. | `pa22:300` | `pa22/tests/spec/300-constructor-default-pack-partial-ordering.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/in_place_default_parameter_order.cpp` compiled but returned `1` before the fix; direct Boost `unique_any/emplace.cpp` aborted with `boost::bad_any_cast`. | Reducer and clang control pass; direct Boost `unique_any/emplace.cpp` and no-RTTI variant compile/link/run; placement audit OK for `pa22:300`; focused PA22 check passes; `ACTIVE_TEST_REPORT_PAS='pa22 pa27' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `449/449`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` advances to accepted-invalid `unique_const_rvalue_construction_failed.cpp`; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`-0.42%` instructions). | `79dce277f` |
| committed | `libs/any/test/unique_any/const_rvalue_construction_failed.cpp` was accepted, so the B2 compile-fail target was skipped instead of failing as expected. | Rvalue-reference standard conversion used a non-reference conversion fallback after a same-type cv mismatch. That let `const T&&` bind to `T&&`, so Boost selected the move constructor instead of the forwarding constructor whose static assertion rejects self construction from a const rvalue. | `pa12:100` | `pa12/tests/general/100-bad-rvalue-reference-binding-top-level-const-source.t` | Non-STL reducers `/tmp/cppgm-boost-reducers-20260519/const_int_rvalue_binds_nonconst_rvalue_ref.cpp` and `/tmp/cppgm-boost-reducers-20260519/const_rvalue_constructor_static_assert.cpp` were accepted or chose the move path before the fix; direct Boost compile of `unique_any/const_rvalue_construction_failed.cpp` was accepted. | Clang rejects the scalar reducer; focused PA12 check passes; PA12 placement review shows no detected later-feature findings for the new test, with PA12/ROADMAP owning standard conversions and reference binding; `ACTIVE_TEST_REPORT_PAS='pa12' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `123/123`; direct Boost compile-fail target fails with the expected static assertion; `JOBS=12 ./run-cppgm-b2.sh -a libs/any/test` passes, updating 164 targets; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`+0.02%` instructions). | `b0ab8f34a` |
| committed | `libs/array/test` `boost::get_c_array()` in `array6.cpp` and `array_c_array_test.cpp`; Boost returns `T (&)[N]` from a `boost::array<T, N>::elems` member. | Function-template result-type patterns kept only top-level pointer operators. For a nested result declarator like `T (&get_c_array(...))[N]`, instantiation initially substituted the right result type, then reparsed the stored pattern as plain `T`, so calls returned the element type instead of an lvalue reference to the member array. | `pa18:100` | `pa18/tests/spec/100-function-template-member-array-reference-return.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/array_member_reference_return.cpp` failed with `invalid initializer [target lvalue-reference to array of 5 bool] [source bool]`; Boost `array6.o` and `array_c_array_test.o` failed with equivalent array-reference initializer diagnostics. | Reducer compiles and runs; `CPPGM_TRACE=template.resolve` shows the reparsed result stays `lvalue-reference to array of 5 bool`; focused PA18 check passes; placement audit marks the new test as `pa18:100` with no review flag; `ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `152/152`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/array/test` advances to the separate `array7.run` and `to_array_test.o` failures; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`+0.19%` instructions, RSS `-0.88%`, footprint `-0.47%`). | `8f02c8036` |
| committed | `libs/array/test/array7.cpp` runtime address checks for `boost::array<std::string, N>` and `std::array<std::string, N>`; `&*begin()` differed from the reference returned by `get<0>()`. | Class-valued built-in comma expressions were semantically lvalues, but `emit_lvalue_address` materialized every special class comma node before reaching the comma lvalue-address lowering. Reference initialization from `(0, object)` therefore copied the right operand into a temporary and bound the reference to the copy. | `pa15:200` | `pa15/tests/general/200-comma-class-lvalue-reference-init.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/comma_lvalue_reference_init_address.cpp` returned `1` under cppgm while clang returned `0`; Boost reducer `/tmp/cppgm-boost-reducers-20260519/boost_array_string_address.cpp` returned `1`; `array7.run` reported four address-check failures for `std::string`. | Non-STL reducer now returns `0` under cppgm and clang; LowIR stores `addr $object` directly into the reference slot with no `tmpobj`/`copyobj`; Boost reducer returns `0`; placement audit marks the new PA15 test owner-reached with no review flag; focused PA15 check passes; `ACTIVE_TEST_REPORT_PAS='pa15' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `306/306`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/array/test//array7` passes; direct `libs/array/test` run advances to only `to_array_test.o`; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-2fcb9d5f-20260519.json` passes (`+0.08%` instructions, RSS `+1.82%`, footprint `-0.36%`). | `5363a0a11` |
| committed | `libs/array/test/to_array_test.cpp`; `boost::to_array({1, 2, 3})` selects array-reference overloads. | Target-aware analysis represented braced array temporaries as lvalues even when the target was an array reference, so overload resolution preferred const lvalue-reference binding and LowIR had no call-argument materialization path for braced array reference temporaries. The same conversion path also needed to preserve array/function lvalue decay into pointer temporaries for rvalue-reference pointer parameters. | `pa26:100` | `pa26/tests/general/100-braced-array-reference-argument.t`; `pa26/tests/general/100-bad-braced-array-nonconst-lvalue-ref-argument.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/braced_array_ref_call_notemplate.cpp` failed with `unsupported expression in PA14 LowIR lowering kind=braced-init-list type=lvalue-reference to array of 3 const int`; overload reducer `/tmp/cppgm-boost-reducers-20260519/braced_array_overload_selection.cpp` needed to select the `int (&&)[3]` overload; non-const lvalue-reference negative reducer had to remain rejected. | Reducers pass with expected statuses; existing `pa26/tests/general/200-defaulted-member-alias-constructor.t` still passes; placement audit marks both new PA26 cluster-100 tests OK; focused PA26 checks pass; `ACTIVE_TEST_REPORT_PAS='pa26' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `96/96`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/array/test//to_array_test` passes; `JOBS=12 ./run-cppgm-b2.sh libs/array/test` passes; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`+0.29%` instructions, RSS `-5.40%`, footprint `-0.05%`). | `d5804e2b2` |
| committed | `libs/asio/test` dependency build through Boost.Chrono/Boost.System; `boost::system::error_category::operator std::error_category const& () const` is defined out-of-class in `boost/system/detail/error_category_impl.hpp`. | Conversion-operator semantic parsing and binding used raw `::` text splitting. For an out-of-class definition such as `error_category::operator std::error_category const&`, that split confused the class qualifier with the qualified conversion target, so the declaration was rejected or failed to bind. The AST already preserved the correct qualified-name structure. | `pa16:400` | `pa16/tests/general/400-out-of-class-qualified-conversion-operator-result.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/out_of_class_conversion_operator_qualified_ref_global.cpp` failed with `unsupported out-of-class conversion operator`; earlier controls showed `operator int` already passed while qualified reference targets failed. Boost `libs/asio/test` logged 211 equivalent failures. | Reducer passes under cppgm and clang; placement audit marks the new PA16 cluster-400 test owner-reached; focused PA16 check passes; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `125/125`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost.Chrono `process_cpu_clocks.cpp` compile passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances past the conversion-operator failure to the next Asio frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`+0.39%` instructions, RSS `+0.47%`, footprint `-0.01%`). | `c583b325b` |
| committed | `libs/asio/test//any_completion_executor`; `boost::asio::execution::any_executor` declares `const prop_fns<any_executor>* prop_fns_`, where `prop_fns` is a class template inherited from `detail::any_executor_base`. | Unqualified class-template lookup in class scopes only checked the lexical scope chain. It did not search base-class member template declarations, although type lookup already searches inherited member types. Instantiating the member declaration therefore left `prop_fns<any_executor>` unresolved. | `pa18:200` | `pa18/tests/general/200-inherited-class-template-id-member-pointer.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/const_inherited_template_id_member_pointer.cpp` failed with `unsupported class member decl-specifier-seq`; a no-pack reducer `/tmp/cppgm-boost-reducers-20260519/const_inherited_template_id_member_pointer_nopack.cpp` captured the same missing lookup without the PA18 pack cluster. Boost target failed at `boost/asio/execution/any_executor.hpp:1835`. | Reducers pass under cppgm and clang; inherited alias-template companion reducer also passes; placement audit marks the new PA18 cluster-200 test owner-reached; focused PA18 check passes; `ACTIVE_TEST_REPORT_PAS='pa18' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `153/153`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances past the `prop_fns` declaration to a separate `constraint_t` alias-template parse frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`-0.35%` instructions, RSS `+0.35%`, footprint `+0.03%`). | `abb11e6f0` |
| committed | `libs/asio/test//any_completion_executor`; `boost::asio::execution::detail::supportable_properties<I, void(Head, Tail...)>` recursively checks `void(Tail...)` while building `prefer_only`. | Function type substitution treated a parameter pack in `void(Tail...)` like a C varargs marker instead of expanding it into ordinary function parameters. That left recursive specializations with `void(second, ...)`/stale dependent text instead of `void(second)` and `void(second, third)`. Witness source-use discovery also tried to resolve qualified member template uses through the instantiated pack spelling after the semantic type was already known. | `pa21:400` | `pa21/tests/general/400-function-type-tail-pack-recursive-specialization.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/pack_function_type_tail_expansion.cpp` failed to compile the recursive function-type pack case; Asio reducer `/tmp/cppgm-boost-reducers-20260519/asio_supportable_prefer_pack.cpp` failed with `explicit type template argument resolution reached text fallback [arg void(Tail...)]`; Boost target failed in `constraint_t<false, int>`. | Reducers pass; focused PA21 check passes; placement audit OK for `pa21:400`; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `176/176`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances to the next `allocator<void>` member declaration frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`-0.36%` instructions, RSS `-0.43%`, footprint `+0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `boost::asio::execution_context::allocator<void>` declares `allocator_impl_base *impl_` inside an explicit specialization of a member class template. | Explicit class specializations recorded the namespace/global declaration scope as their binding scope. For `template<> class outer::allocator<void>`, class completion therefore lost the enclosing `outer` member scope, so unqualified lookup inside the specialized member class body could not find sibling nested types such as `outer::base`. | `pa21:300` | `pa21/tests/general/300-member-class-explicit-specialization-owner-lookup.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/nested_private_owner_type_in_member_class_specialization_use.cpp` failed with `unsupported class member decl-specifier-seq` for `base *specialized`; Boost target failed at `boost/asio/execution_context.hpp:461` for `allocator_impl_base *impl_`. | Reducer passes; focused PA21 check passes; placement audit OK for `pa21:300`; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `177/177`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances to the next `std::__1::atomic<long> == 0` overload frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`-0.02%` instructions, RSS `+0.57%`, footprint `+0.01%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; Asio's scheduler compares `boost::asio::detail::atomic_count` (`std::__1::atomic<long>`) with `0`. | User-defined conversion selection treated `operator T() const` and `operator T() const volatile` as indistinguishable when both conversion results had the same second standard conversion. The tie-breaker did not compare the conversion functions' implicit object parameter qualification, so builtin comparison fallback rejected the class operand instead of choosing the less-qualified `const` conversion for a non-volatile object. | `pa16:400` | `pa16/tests/general/400-cv-conversion-operator-builtin-comparison.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/dual_cv_direct_conversion_operator_compare_zero.cpp` failed with `unsupported comparison operands`; STL reducer `/tmp/cppgm-boost-reducers-20260519/atomic_compare_zero_min.cpp` failed with `operator-overload-to-builtin`; Boost target failed at `boost/asio/detail/impl/scheduler.ipp:205` for `outstanding_work_ == 0`. | Reducers pass, including inherited-template and `<atomic>` shapes; focused PA16 check passes; placement audit OK for `pa16:400`; `ACTIVE_TEST_REPORT_PAS='pa16' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `126/126`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances to the next `any_completion_executor == nullptr_t` comparison frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-c4b544837-20260519.json` passes (`-0.32%` instructions, RSS `-0.44%`, footprint `-0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `boost::asio::execution::any_executor` declares hidden friend `operator==` templates whose `enable_if` return type compares the deduced `AnyExecutor` with the current specialization name `any_executor`. | Hidden friend function templates declared inside class templates were instantiated using the namespace/entity scope only. After candidate deduction selected `AnyExecutor = any_executor<int>`, non-type evaluation of `is_same<AnyExecutor, any_executor>::value` still resolved `any_executor` without the selected class-template specialization context, so the viable hidden friend was dropped and comparison fell back to builtin operators. | `pa22:300` | `pa22/tests/general/300-hidden-friend-current-specialization-enable-if.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/hidden_friend_current_specialization_enable_if_compare.cpp` reproduced the reduced shape; larger Boost-shaped reducer `/tmp/cppgm-boost-reducers-20260519/hidden_friend_current_specialization_nullptr_compare.cpp` failed with `operator-overload-to-builtin`; trace showed `candidate-instantiation-failed ... failed non-type template argument evaluation: is_same<AnyExecutor,any_executor>::value`. | Reducers pass; focused PA22 check passes; placement audit OK for `pa22:300`; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `410/410`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; `JOBS=12 ./run-cppgm-b2.sh -a libs/asio/test//any_completion_executor` advances to the next constructor frontier from `thread_pool::basic_executor_type` to `any_completion_executor`; new clean-head baseline recorded at `/tmp/cppgm-perf-baseline-boost-frontier-current-0a3d50de-20260519.json`; perf check passes (`+0.24%` instructions, RSS `+1.54%`, footprint `-0.03%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; Asio recursively evaluates `supportable_properties<I, void(Head, Tail...)>::is_valid_target<T>::value` while checking whether `thread_pool::basic_executor_type` can construct `any_completion_executor`. | Class template instantiation owner remapping matched only the owner class template name. When a nested member class template was resolved under the concrete leaf specialization `supportable_properties<0, void(prefer_only<prop_a>)>`, instantiation remapped its owner to the enclosing recursive `supportable_properties<0, void(prefer_only<prop_a>, prefer_only<prop_b>)>` because both owners share the same primary template name. Function-type argument parsing also needed to preserve recursive `void(Head)`/`void(Tail...)` template-id arguments without falling back to text. | `pa21:400` | `pa21/tests/general/400-recursive-member-template-concrete-owner.t` | Reducer `/tmp/cppgm-boost-reducers-20260519/supportable_properties_recursive_leaf_trait_expression.cpp` failed with `failed non-type template argument evaluation` for the recursive `is_valid_target<T>::value` expression; direct Boost `any_completion_executor.cpp` failed with no viable constructor for `thread_pool::basic_executor_type`. | Reducers pass; new PA21 regression passes; placement audit marks the regression owner-reached at `pa21:400`; `ACTIVE_TEST_REPORT_PAS='pa21' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `178/178`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to a separate `fat_executor` constructor frontier; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-b65474fca-20260519.json` passes (`+0.77%` instructions, RSS `+0.13%`, footprint `+2.12%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `fat_executor` construction triggers defaulted constructor SFINAE through imported `std::is_base_of`, recursive `conditional<...>::type::value`, and `supportable_properties<...>::template is_valid_target<T>::value`. | Constructor template parameter-clause replay used the restricted signature parser, so defaulted SFINAE parameters could not resolve the needed class-template references. Type-trait arguments sometimes arrived as structured id-expression syntax instead of type-id syntax, concrete member-template `::value` lookup missed the concrete owner specialization, and type-pack expansion needed to preserve typed cv/ref/function/member-value patterns instead of falling back to text. | `pa22:500`, `pa34:700` | `pa22/tests/general/500-constructor-sfinae-member-template-value.t`; `pa34/tests/compile/700-hosted-imported-base-of-constructor-sfinae.t` | Reducers `/tmp/cppgm-boost-reducers-20260519/constructor_template_function_type_trait_constraint.cpp`, `/tmp/cppgm-boost-reducers-20260519/defaulted_constraint_constructor_imported_std_base_of.cpp`, and `/tmp/cppgm-boost-reducers-20260519/fat_executor_trait_probe.cpp` failed through the `fat_executor` constructor constraint; direct Boost `any_completion_executor.cpp` stopped at that constructor frontier. | Reducers pass; placement audits mark the new PA22 and PA34 tests owner-reached in the intended clusters; `ACTIVE_TEST_REPORT_PAS='pa22 pa34' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `706/706`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to ambiguous `boost::asio::query` overloads at line 409; perf check against `/tmp/cppgm-perf-baseline-boost-frontier-current-b65474fca-20260519.json` passes (`+0.72%` instructions, RSS `-0.74%`, footprint `+2.16%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `query_free_trait<T, P, void_t<decltype(query(declval<T>(), declval<P>()))>>` probes Asio hidden-friend `query` before a later `api::query` CPO object is declared. | Leaf dependent `decltype`/`noexcept` call probing did not perform definition-point ADL, allowed a later ordinary CPO object to supply the result, did not keep hidden-friend candidates needing a user-defined conversion, and class-template alias/noexcept constant handling did not use template-aware semantic expression evaluation for this probe. | `pa22:500` | `pa22/tests/spec/500-hidden-friend-query-free-decltype-noexcept.t` | Non-STL reducer failed to preserve the hidden-friend result type; direct Boost `any_completion_executor.cpp` stopped at ambiguous `boost::asio::query` overloads. | Focused reducer passes; placement audit OK for `pa22:500`; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `412/412`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to `unknown function boost::asio::detail::run_test<&any_completion_executor_construction_test>`; shared baseline `/tmp/cppgm-perf-baseline-boost-frontier-current-b65474fca-20260519.json` still fails without this WIP (`+1.33%` instructions), so the slice was gated against isolated pre-slice baseline `/tmp/cppgm-perf-baseline-pre-query-free-9f725e-20260519.json` and passes (`-0.04%` instructions, RSS `+1.40%`, footprint `+0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `BOOST_ASIO_TEST_CASE(any_completion_executor_construction_test)` expands to `boost::asio::detail::run_test<&any_completion_executor_construction_test>(...)`. | Explicit function-template argument resolution only recognized bare function names for function-pointer NTTPs. A structured address-of expression such as `&sample` was sent through integral constant evaluation, failed, and dropped the only `run_test` function-template candidate before overload resolution. | `pa22:400` | `pa22/tests/spec/400-function-template-nontype-function-pointer-call.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/run_test_function_pointer_nttp.cpp` and the checked-in regression failed with `unknown function api::detail::run_test<&sample>`; trace showed `explicit-arg-resolution-failed` after `resolve-template-arg kind=non-type text=&sample`. | Regression passes; placement audit OK for `pa22:400`; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `413/413`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to `unknown id-expression service_registry::create<Service,execution_context>`; perf check against `/tmp/cppgm-perf-baseline-pre-run-test-6bdcd76ac-20260520.json` passes after keeping the new function-address path behind the existing named-value fast path (`-0.01%` instructions, RSS `-1.18%`, footprint `+0.02%`). | this commit |
| committed | `libs/asio/test//any_completion_executor`; `factory_type factory = &service_registry::create<Service, execution_context>` in `boost/asio/detail/impl/service_registry.hpp`. | Function-template target-type deduction compared the whole function type before recognizing a trailing function parameter pack. With explicit `Service, Owner` arguments and an empty `Args...`, the generic matcher saw the pattern as having one more parameter than the target function pointer and dropped the only viable static member function-template specialization. | `pa22:400` | `pa22/tests/general/400-member-function-template-address-explicit-pack.t` | Non-STL reducer `/tmp/cppgm-boost-reducers-20260519/member_function_template_address_explicit_args.cpp` and the checked-in regression failed with `unknown id-expression service_registry::create<Service,context>`; direct Boost failed at `service_registry::create<Service,execution_context>`. | Reducer passes; placement audit run for PA22 with existing backend-owner notes only; `ACTIVE_TEST_REPORT_PAS='pa22' ORDERED=false TEST_REPORT_SUBTEST_JOBS=12 make test-report` passes `414/414`; `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passes; direct Boost `any_completion_executor.cpp` advances to `explicit-type-argument-text-fallback [arg int *]` in libc++ tuple machinery; perf check against `/tmp/cppgm-perf-baseline-pre-member-template-target-pack-f20672781-20260520.json` passes (`+0.08%` instructions, RSS `-0.13%`, footprint `+0.22%`). | this commit |
