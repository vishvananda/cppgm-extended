# Boost B2 Frontier V2 Tracker

This is the live tracker for `docs/boost-b2-frontier-v2-plan.md`. It starts from
zero credited Boost suites. V1 pass/fail state is historical only.

## Start State

- started: `2026-07-13`
- compiler baseline: `db9879223b4249a9e5516de0205e76fa158d5549`
- base branch: `main`
- frontier branch: `boost-frontier-v2`
- Boost tree: `/Users/vishvananda/boost_1_91_0`
- Boost release: `1.91.0`
- suite inventory: `docs/boost-b2-suite-status-20260511.md`
- suite count: `147`
- completed suites: `71 / 147`
- current cursor: `#72 libs/multi_array/test`
- active compiler frontiers: MultiArray's initial forced C++11 graph is pending

## Baseline Gates

| Gate | Status | Evidence |
|---|---|---|
| Exact compiler start commit | pass | `main`, `origin/main`, and the merged PR head were verified at `db9879223` before this branch was created. |
| PR Tests workflow | pass | GitHub Actions run `29298098410` completed successfully with all 21 checks green across GCC/libstdc++ and Clang/libc++ on Ubuntu 24.04 and 26.04. |
| PR-triggered inception | pass | Run `29299551532` completed `Compare cppgm++ inception` successfully in `56m53s`. |
| Warning-clean `dev/cppgm++` build | pass | `make -C dev -j8 cppgm++` relinked the compiler at `2acedcec7` without warnings; production compiler sources are unchanged from `db9879223`. |
| Full direct-LowIR report | pass | `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 ORDERED=false make test-report` passed `3818 / 3819`; the only miss was the known load-sensitive PA9 generated-program timeout. Its immediate isolated direct-LowIR rerun passed `11 / 11`, and the timeout was accepted as load noise. |
| Strict direct-LowIR report | pass | `CPPGM_LOWIR_DIRECT_TEXT_COMPARE=1 make test-strict` passed all configured PA18, PA19, PA21, PA22, and PA23 comparisons. |
| Strict text-reparse audit | pass | `audit_text_reparse.py --strict --list-sites` reported all 23 categories at zero; all 14 audit unit tests passed. |
| PA placement audit | pass | PR CI placement audit passed. |
| Fixed V2 performance baseline | pass | Three runs were recorded from the detached worktree `/tmp/cppgm-boost-v2-baseline-db9879223` at exact commit `db9879223`; medians are recorded below. |

## Environment

Fill this before the first suite run. Keep these values stable for comparable
suite evidence and performance measurements.

| Item | Value |
|---|---|
| host OS/version | macOS 26.3 (25D125), Darwin 25.3.0 x86_64 |
| CPU | Intel Core i7-9750H, 6 physical / 12 logical cores, 16 GiB memory |
| host C compiler | `/usr/local/opt/llvm/bin/clang`, Homebrew clang 22.1.0 |
| host C++ compiler | `/usr/local/opt/llvm/bin/clang++`, Homebrew clang 22.1.0 |
| C++ standard library | system libc++ (`/usr/lib/libc++.1.dylib`, current version 2000.67.0; headers `_LIBCPP_VERSION=220100`) |
| B2 wrapper | `/Users/vishvananda/boost_1_91_0/run-cppgm-b2.sh` |
| compiler under test | `/Users/vishvananda/cppgm-extended/dev/cppgm++` |
| default suite jobs | `2`; memory sampling shows ordinary MP11 units release between translation units and do not accumulate RSS |
| default suite timeout | `14400s` for large suites; shorter only for focused diagnostics |

## Fixed Performance Baseline

| Commit | Baseline file | Instructions | Max RSS | Peak footprint | Wall time | Status |
|---|---|---:|---:|---:|---:|---|
| `db9879223` | `/tmp/cppgm-boost-frontier-v2-db9879223-baseline.json` | 271,651,249,439 | 1,293,901,824 B | 1,024,110,592 B | 60.43s | recorded, immutable; legacy live-project-header epoch through suite 21 |

## Performance Workload Epochs

The original gate froze `semantic_overload.cpp` but selected live `dev/src`
headers. That made its source stable while allowing the project-header workload
to drift. Those results remain valid within their original epoch, including the
Config candidate/parent comparison, but must not be compared with the corrected
frozen-header epoch.

| Epoch | Compiler baseline | Project workload | Report | Instructions | Max RSS | Peak footprint | Status |
|---|---|---|---|---:|---:|---:|---|
| legacy live headers | `db9879223` | frozen source plus live `dev/src` headers | `/tmp/cppgm-boost-frontier-v2-db9879223-baseline.json` | 271,651,249,439 | 1,293,901,824 B | 1,024,110,592 B | historical through suite 21; immutable within its epoch |
| frozen project headers | `9764b3835` | frozen source plus the 51-file closure under `benchmarks/self_compile/stable/include` | `/tmp/cppgm-boost-frontier-v2-frozen-header-epoch-9764b3835.json` | 263,559,800,479 | 1,292,648,448 B | 1,009,885,184 B | current immutable epoch baseline; candidate-only record, no parent rerun |

## Performance Ledger

Every production compiler commit gets a row against the immutable baseline for
its workload epoch. Add a rolling delta only when it helps isolate the
incremental cost. Never compare metrics across epochs; the gate rejects command
differences other than the output path.

| Commit | Frontier | Fixed instruction delta | Max RSS delta | Footprint delta | Rolling delta | Report | Decision |
|---|---|---:|---:|---:|---:|---|---|
| `(qualified-friend fix)` | Qualified friend class template-id initial parse | +0.17% | +1.66% | +0.00% | n/a | `/tmp/cppgm-boost-frontier-v2-qualified-friend-perf.json` | pass; instruction and memory gates remain within tolerance |
| `(typed-qualified-call fix)` | Substituted qualified static member-template call | -0.08% | +1.15% | -0.01% | n/a | `/tmp/cppgm-boost-frontier-v2-typed-qualified-call-perf.json` | pass; below hotspot threshold |
| `(typed-va-list ABI fix)` | Host `__builtin_va_list` function symbols | -0.11% | +0.22% | -0.03% | n/a | `/tmp/cppgm-boost-frontier-v2-va-list-typed-abi-perf.json` | pass; below hotspot threshold |
| `(typed-member-pointer fix)` | Qualified current-specialization member-function address | -0.10% | -0.81% | +0.01% | n/a | `/tmp/cppgm-boost-frontier-v2-typed-member-pointer-perf.json` | pass; below hotspot threshold |
| `(typed bound-template fix)` | Nested member alias template-template call result | -0.13% | -0.09% | +0.02% | n/a | `/tmp/cppgm-boost-frontier-v2-bound-template-identity-perf.json` | pass; below hotspot threshold |
| `(parenthesized noexcept member fix)` | Parenthesized class member name with a `noexcept` definition | -0.13% | -0.31% | +0.02% | n/a | `/tmp/cppgm-boost-frontier-v2-parenthesized-member-noexcept-perf.json` | pass; below hotspot threshold |
| `(unused default lazy fix)` | Explicit alias argument must not instantiate an unused default | -0.27% | -0.26% | -0.06% | n/a | `/tmp/cppgm-boost-frontier-v2-unused-default-lazy-perf.json` | pass; all cumulative metrics improve |
| `(non-deduced partial pattern fix)` | Reused non-deduced partial-specialization pattern after earlier deductions | +0.75% | -0.56% | +0.04% | eager typed substitution was +0.94%; resolver-first fast path recovered 0.19 percentage points | `/tmp/cppgm-boost-frontier-v2-nondeduced-partial-pattern-fastpath-perf.json` | pass; hard gates pass; hotspot counters and 1 ms sample show no pathological path or cache-masked correctness |
| `(duplicate-base static lookup fix)` | Inherited non-object declaration reached through duplicate base subobjects | +0.74% | +1.05% | +0.06% | -0.01 percentage points instructions from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-duplicate-base-static-member-perf.json` | pass; all fixed cumulative gates remain within tolerance |
| `(typed base-trait traversal fix)` | Transitive base trait across distinct specializations of the same class template | +0.73% | -0.06% | +0.10% | -0.01 percentage points instructions from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-base-of-specialization-cycle-key-perf.json` | pass; changed evaluator is absent from the 1 ms sample and semantic text-resolution counters remain zero |
| `(external vbase layout runtime fix)` | Imported virtual-base pointer adjustment for classes without locally observed virtual functions | +0.74% | +0.50% | +0.10% | +0.01 percentage points instructions from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-external-vbase-layout-runtime-final-perf.json` | pass; below hotspot threshold and all fixed cumulative gates remain within tolerance |
| `(extern-template out-of-class member fix)` | Suppressed out-of-class member ownership under an explicit class-instantiation declaration | +0.88% | -0.05% | +0.06% | +0.14 percentage points instructions from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-extern-template-out-of-class-member-perf-repeat.json` | pass; repeated without code changes after the first near-limit +0.95% result; incremental movement remains below the hotspot threshold |
| `(dependent alignof NTTP fix)` | Concrete class layout for a substituted `alignof(T)` non-type template argument | +0.68% | +1.59% | +0.06% | -0.20 percentage points instructions from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-align-dependent-nttp-perf.json` | pass; all cumulative hard gates pass and instructions improve from the preceding frontier |
| `(declarator-owner implicit typename fix)` | Nested member-template type in an out-of-class static data definition | +0.80% | +0.25% | +0.15% | +0.12 percentage points instructions from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-owner-implicit-typename-perf.json` | pass; all cumulative instruction and memory gates remain within tolerance |
| `(qualified rvalue-reference parser fix)` | Namespace-qualified template-id followed by an `&&` abstract declarator | +0.81% | +0.99% | +0.10% | +0.01 percentage points instructions from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-qualified-rref-parser-perf.json` | pass; initial parsing retains the structured type-id and all cumulative hard gates remain within tolerance |
| `7c7e7cfc9` | Constexpr flat aggregate initialization and array member access | +0.86% | +0.50% | +0.10% | the initial implementation was optimized before acceptance to avoid duplicate semantic work | `/tmp/cppgm-boost-frontier-v2-array-constexpr-final-perf.json` | pass; fixed cumulative gates remain within tolerance after the normal-path optimization |
| `43bd0cc6b` | Constexpr array subobject pointer identity | +0.65% | +0.23% | +0.10% | -0.21 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-array-pointer-final-perf.json` | pass; cumulative instructions and memory improve from the preceding frontier |
| `(array cv reference binding fix)` | Top-level cv comparison for array reference binding | +0.73% | +0.26% | +0.08% | +0.09 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-array-cv-reference-perf.json` | pass; counters are stable and sampling contains no frame in the changed helper |
| `(direct function-type pack fix)` | Retained direct function-type pack substitution in alias SFINAE | +0.79% | -0.49% | +0.07% | +0.06 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-bd4dcb2fd-direct-function-pack-final-perf.json` | pass; node visits, query requests, and cache counts match the preceding checkpoint, and the changed annotator appears once in the 10-second sample |
| `(qualified nested allocator rebind alias fix)` | Substituted qualified nested rebind alias through a detector-selected partial specialization | +0.85% | -0.53% | +0.06% | +0.06 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-allocator-rebind-perf.json` | pass; structured resolution is limited to substituted class aliases containing a qualified template-id, and all six relevant cache-disabled modes prove the underlying algorithm |
| `(enclosing NTTP substitution fix)` | Concrete enclosing class non-type binding in a nested member-template result | +0.90% | +1.27% | +0.10% | +0.05 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-enclosing-nttp-perf-optimized-r3.json` | pass; three-run median after a near-threshold single run, with stable hotspot counters, zero text-resolution metrics, and no separate changed-helper frame in the 1 ms sample |
| `(retained decltype sidecar fix)` | Concrete reevaluation of a detector partial-specialization pattern retained below template-argument sidecars | +0.96% | +0.78% | +0.11% | +0.06 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-bind-allocator-final-perf.json` | pass; broad intermediate scans were removed, semantic hotspot counts are stable, and the changed structured type-syntax resolver contributes only nine top-stack samples in ten seconds |
| `(typed numeric UDL selection fix)` | Raw numeric literal-operator template coexisting with an ordinary string literal operator | +0.92% | -0.54% | +0.10% | -0.04 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-buffer-literal-perf-r2.json` | pass on a no-code-change repeat after the first near-threshold +1.01% median; the benchmark does not exercise user-defined literals, hotspot query counts are unchanged, and neither changed helper appears in the sample |
| `(structured template-id type-probe fix)` | Retained explicit function template-id containing a dependent member-template type in a trailing return | +0.89% | +0.29% | +0.15% | -0.03 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-ip-tcp-structured-anchor-perf.json` | pass; the cold exact-anchor mismatch branch consumes retained syntax, performs no parse or reconstruction, and cumulative instructions improve from the preceding frontier |
| `(typed this member-result scope fix)` | Member-template trailing return containing `this` inside a functional cast and explicit template-id call | +0.79% | +0.36% | +0.14% | -0.10 instruction percentage points from the preceding frontier; the initial broad member-result hook broke the self-compile benchmark and was narrowed before acceptance | `/tmp/cppgm-boost-frontier-v2-posix-member-result-scope-perf.json` | pass; the sidecar-aware AST scan runs only for a still-dependent non-static member result and the fixed benchmark compiles successfully |
| `(full-expression temporary emission fix)` | Nested member-template class prvalue whose retained type predates completed layout | +0.78% | +0.22% | +0.03% | -0.007% instructions, -0.14% RSS, and -0.11% footprint from the preceding checkpoint | `/tmp/cppgm-boost-frontier-v2-strand-stale-layout-perf.json` | pass; the added typed `ClassInfo` lookup is confined to layout-free named prvalues and adds no measurable aggregate cost |
| `(conversion default SFINAE and ABI fix)` | Concrete defaulted SFINAE arguments and conversion-function-template symbols | +0.77% | +0.78% | +0.20% | -0.01 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-assign-default-sfinae-perf.json` | pass; substituted retained AST is resolved only after it becomes structurally concrete, all cache-disabled outputs are identical, and no text-reparse or rendered-symbol path is added |
| `(nested structured template-id fix)` | Namespace-qualified lookup of a nested template-id retained inside another template argument | +0.82% | +1.15% | +0.14% | +0.05 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-nested-template-id-anchor-perf.json` | pass; the cold exact-anchor mismatch path prefilters by typed identifier, all cache-disabled outputs are identical, and the incremental movement is below the hotspot threshold |
| `(Atomic typed lowering fixes)` | Structured GNU asm atomic operands plus scalar/class correctness exposed by Boost.Atomic | +0.91% | +0.19% | +0.32% | +0.09 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-atomic-final-perf.json` | pass; all fixed gates remain within tolerance and the incremental movement is below the hotspot threshold |
| `(dependent owner-pack ABI fix)` | Out-of-class constructor definition whose owner pack is renamed from the primary declaration | +0.86% | -0.55% | +0.33% | +0.00 instruction percentage points and +0.00 footprint percentage points from the preceding Beast frontier | `/tmp/cppgm-boost-frontier-v2-beast-dependent-owner-pack-perf.json` | pass; the fixed cumulative gates pass and the unchanged instruction result is below the hotspot threshold |
| `(nested definition owner-pack binding fix)` | Concrete `sizeof...` in an out-of-class nested definition whose owner pack is renamed | +0.80% | -0.30% | +0.28% | -0.06 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-beast-nested-owner-pack-binding-perf.json` | pass; all cumulative metrics remain within tolerance and improve from the preceding frontier |
| `(nested trailing-return deferral fix)` | Dependent trailing-return alias on an out-of-class nested member definition | +0.83% | +1.04% | +0.32% | +0.03 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-beast-nested-trailing-return-defer-perf.json` | pass; all cumulative metrics remain within tolerance and the incremental movement is below the hotspot threshold |
| `(in-class dependent trailing-return SFINAE fix)` | Defaulted member-template `decltype` probe over inherited protected members | +0.78% | +0.06% | +0.35% | -0.05 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-beast-declval-trailing-return-perf.json` | pass; three-run median improves from the preceding checkpoint and remains below the hotspot threshold |
| `(dependent-base implicit assignment using fix)` | Implicit assignment operator imported from a concrete dependent base | +0.73% | +1.16% | +0.36% | -0.05 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-beast-implicit-assignment-perf.json` | pass; all fixed cumulative gates remain within tolerance and instructions improve from the preceding checkpoint |
| `(dependent-call point-of-definition lookup fix)` | Later ordinary CPO object excluded from an earlier dependent expression-SFINAE probe | +0.68% | -0.88% | +0.26% | -0.05 instruction percentage points from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-late-cpo-visibility-perf.json` | pass; all fixed cumulative gates pass and instructions and memory improve from the preceding checkpoint |
| `(out-of-class owner parameter scope fix)` | Renamed owner parameter in an ordinary out-of-class member body | +0.67% | +0.31% | +0.24% | +0.07% instructions, +1.19% RSS, and flat footprint from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-ooc-owner-param-scope-perf.json` | pass; all fixed and rolling gates remain within tolerance |
| `(hidden-friend visibility fix)` | Namespace and associated-class visibility for non-template hidden friends | -0.79% | -1.27% | +0.22% | -1.44% instructions, -1.58% RSS, and -0.02% footprint from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-hidden-friend-perf.json` | pass; removing unrelated hidden friends before conversion probing improves the normal self-compile path |
| `(qualified member sizeof constexpr fix)` | Reference-to-full class transition while evaluating a retained qualified member `sizeof` initializer | -0.83% | -0.97% | +0.07% | -0.05% instructions, +0.30% RSS, and -0.15% footprint from the preceding frontier | `/tmp/cppgm-boost-frontier-v2-chacha-perf.json` | pass; binding reacquisition is cold around recursive evaluation and the AST-only recovery adds no normal-path cost |
| `(qualified type before constexpr parser fix)` | Named qualified type followed by a later decl-specifier | -0.82% | -0.42% | +0.10% | +0.01% instructions, +0.56% RSS, and +0.03% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-qualified-constexpr-parser-perf.json` | pass; one keyword predicate aligns the declaration-start prefilter with the initial parser and remains below the hotspot threshold |
| `(implicit-object cv leaf-call fix)` | Const callable object in a dependent `decltype` expression-SFINAE probe | -0.80% | -1.12% | +0.06% | +0.02% instructions, -0.71% RSS, and -0.04% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-invocable-implicit-object-cv-perf.json` | pass; four typed cv checks remain confined to the existing leaf call resolver and all cumulative gates pass |
| `(nested member-template definition ownership fix)` | Out-of-class nested class/member definitions under an explicitly instantiated outer class | -0.95% | -0.99% | +0.07% | -0.15% instructions, +0.13% RSS, and +0.01% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-nested-owner-definitions-perf.json` | pass; the AST-node index is restricted to member class templates, and the rejected broader output-node assignment was removed before acceptance |
| `(nested definition parameter alias fix)` | Renamed non-type parameter on an out-of-class nested member class-template definition | -0.84% | -0.70% | +0.07% | +0.113% instructions, +0.293% RSS, and -0.004% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-member-definition-param-alias-perf.json` | pass; the existing structured redeclaration merge runs only when a retained definition replaces its forward declaration, and all nine cache-disabled modes are byte-identical |
| `(ADL reference-shape fix)` | Associated namespaces contributed by a concrete class-template specialization's bases before deduction | -0.69% | -0.40% | +0.13% | +0.149% instructions, +0.302% RSS, and +0.066% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-adl-reference-shape-perf.json` | pass; reference collection is demand-driven for the concrete associated class, all nine cache-disabled modes are byte-identical, and rolling instructions remain below the hotspot threshold |
| `(qualified decltype type parser fix)` | Qualified nested type whose nested-name-specifier is a `decltype` expression | -0.76% | -0.49% | +0.10% | -0.073% instructions, -0.092% RSS, and -0.033% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-decltype-qualified-name-perf.json` | pass; both declaration and type-id forms stay in the initial structured parser, all nine cache-disabled modes are byte-identical, and every rolling metric improves |
| `(qualified decltype template argument and lexical static_assert fix)` | Qualified `decltype` template arguments plus local declarations used by early member-body `static_assert` validation | -0.50% | +0.10% | +0.20% | +0.267% instructions, +0.600% RSS, and +0.093% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-qualified-decltype-static-assert-perf.json` | pass; parsing is initial and structured, normal statement semantics replace the assertion-only shortcut, cached and uncached body output are identical, and rolling movement remains below the hotspot threshold |
| `(typed overloaded member-template cast fix)` | `static_cast` target selection for an overloaded member-template address passed to another function template | -0.53% | +0.84% | +0.18% | -0.030% instructions, +0.740% RSS, and -0.016% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-member-template-cast-perf.json` | pass; the existing typed target-aware selector is entered only for a member-function-pointer cast over an address expression, all cache-disabled outputs are identical, and no parse or lookup scan is added to ordinary casts |
| `(pointer-like builtin invoke fix)` | Member pointer invocation through a class with `operator*`, including a dependent `decltype(__builtin_invoke(...))` result | -0.57% | +0.21% | +0.17% | -0.043% instructions, -0.628% RSS, and -0.008% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-pointer-like-invoke-perf.json` | pass; the typed path is confined to member pointers whose object is neither a direct owner nor a raw pointer, cache-disabled outputs are identical, and sampling shows no new repeated lookup path |
| `(aggregate functional value-init fix)` | Empty same-type functional cast used to value-initialize a trivial aggregate | -0.50% | -0.02% | +0.17% | +0.068% instructions, -0.228% RSS, and +0.002% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-value-init-zero-perf.json` | pass; the typed initializer predicate is cold, redundant whole-object static zero actions are removed, and rolling movement is below the hotspot threshold |
| `(typed local-class RTTI owner fix)` | Dependent qualified result type in a function-template local-class RTTI symbol | -0.47% | -0.61% | +0.12% | +0.03 instruction percentage points, -0.59% RSS, and -0.05% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-local-rtti-owner-perf.json` | pass; the existing typed ABI IR is checked only before accepting a dependent owner, and all cumulative and rolling gates remain within tolerance |
| `(precollected operator ADL fix)` | Synthetic unary, binary, and compound-assignment calls whose candidate scopes already contain ordinary and ADL candidates | -4.22% | -1.80% | -1.02% | -3.75 instruction percentage points, -1.19% RSS, and -1.14% footprint from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-detect-ssl-adl-perf.json` | pass; stable self-compile counters show ADL collections `6307 -> 4337`, type visits `12413 -> 8473`, and template-deduction cache checks `95245 -> 58938`; a 10-second 1 ms sample has 46 pre-fix samples in the redundant ADL append and none after the fix |
| `(captureless lambda default-parameter fix)` | Captureless lambda whose call operator has a default parameter | -4.22% | -1.37% | -1.05% | flat instructions, +0.43 RSS percentage points, and -0.03 footprint percentage points from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-file-lambda-default-perf.json` | pass; the existing synthetic-function fast path gains one scan of the already parsed default-argument vector and ordinary captureless lambdas remain on that path |
| `(post-layout member static-assert validation fix)` | Inherited member-template calls in a non-template method whose body contains `static_assert` | -4.05% | +0.26% | -0.96% | +0.17 instruction percentage points, +1.63 RSS percentage points, and +0.09 footprint percentage points from the preceding measurement | `/tmp/cppgm-boost-frontier-v2-postlayout-static-assert-perf.json` | pass; validation now iterates the existing binding declaration order after finalized layout, normal and cache-disabled outputs are byte-identical, and the incremental instruction movement remains below the hotspot threshold |
| `(out-of-class copy-assignment head fix)` | Same-type assignment definition adjacent to a matching member-template overload | -4.03% | +0.34% | -0.95% | +0.026% instructions, +0.083% RSS, and +0.011% footprint from the preceding checkpoint | `/tmp/cppgm-boost-frontier-v2-ooc-copyassign-template-head-perf.json` | pass; the one-condition typed template-head partition change is below the hotspot threshold |
| `(dependent constructor ABI expression fix)` | Concrete class-owner NTTPs, namespace constants, and function packs in a dependent constructor symbol | -3.95% | -0.11% | -0.93% | +0.080% instructions, -0.445% RSS, and +0.020% footprint from the preceding checkpoint | `/tmp/cppgm-boost-frontier-v2-span-dependent-abi-final-perf.json` | pass; rolling instructions are below the hotspot threshold, so no sampling escalation is required |
| `a9f116ad6` | Static-string source ownership, expression identity, list ranking, and definition attachment | -3.04% | -1.17% | -0.83% | +0.95% instructions, -1.07% RSS, and +0.10% footprint against the preceding extracted checkpoint | `/tmp/cppgm-boost-frontier-v2-static-string-final-perf.json` | pass; typed result comparison is off the unique-match path and targeted sampling found no dominant changed helper |
| `(structured lambda capture fix)` | Implicit `this` capture for an unqualified explicit member-template call | -3.01% | -0.61% | -0.82% | +0.029% instructions, +0.574% RSS, and +0.013% footprint from `a9f116ad6` | `/tmp/cppgm-boost-frontier-v2-basic-parser-lambda-capture-perf.json` | pass; the initial-AST capture scan adds only a structured template-id identifier lookup and rolling instructions remain below the hotspot threshold |
| `(fold-expression alias result fix)` | Concrete variadic fold result used by an alias-template function result | -3.03% | -0.86% | -0.82% | -0.025% instructions, -0.254% RSS, and -0.004% footprint from the structured lambda checkpoint | `/tmp/cppgm-boost-frontier-v2-fold-alias-perf.json` | pass; only bound fold nodes enter the existing structured constexpr reducer, unchanged `decltype` operands are not copied, and all rolling metrics improve |
| `(file-body constructor fixes)` | Explicit constructor-template recursion during implicit conversion plus out-of-class nested constructor-template definition replay | -3.19% | -0.62% | -1.04% | -0.166% instructions, +0.247% RSS, and -0.218% footprint from the fold-expression checkpoint | `/tmp/cppgm-boost-frontier-v2-file-body-nested-member-template-perf.json` | pass; the invalid explicit template is rejected before deduction, nested declaration identity stays typed through shared AST nodes, and all fixed and rolling hardware-work gates pass |
| `(defaulted default constructor fix)` | Implicit deletion of an explicitly defaulted default constructor and single-type `__is_constructible` | -3.25% | -0.68% | -0.99% | -0.06 instruction percentage points, -0.06 RSS percentage points, and +0.05 footprint percentage points from the file-body checkpoint | `/tmp/cppgm-boost-frontier-v2-message-defaulted-ctor-perf-final.json` | pass; refresh is confined to the typed constructor bucket, all cache-disabled outputs are identical, and fixed and rolling hardware-work gates pass |
| `(template type-argument token-anchor fix)` | Declaration-order lookup inside a retained namespace-alias-qualified `decltype` type argument | -3.21% | -0.40% | -0.99% | +0.04 instruction percentage points, +0.28 RSS percentage points, and flat footprint from the message checkpoint | `/tmp/cppgm-boost-frontier-v2-type-argument-token-anchor-perf.json` | pass; the stored initial-parse token anchor is threaded only through structured type-id/`decltype` resolution, all cache-disabled modes are byte-identical, and rolling movement is below the hotspot threshold |
| `(direct nested-member completion fix)` | Final member-type lookup in a concrete out-of-class nested class definition | -3.27% | -0.63% | -0.96% | -0.06 instruction percentage points, -0.23 RSS percentage points, and +0.03 footprint percentage points from the token-anchor checkpoint | `/tmp/cppgm-boost-frontier-v2-direct-nested-member-completion-perf.json` | pass; class completion is requested only after the structured qualifier walk reaches a concrete class, every cache-disabled mode is byte-identical, and all rolling movement is below the hotspot threshold |
| `(member-template overload-head definition fix)` | Out-of-class member-template definition beside a shorter overload head on a non-template class | -3.31% | -1.53% | -0.96% | -0.045% instructions, -0.913% RSS, and +0.004% footprint from the direct nested-member checkpoint | `/tmp/cppgm-boost-frontier-v2-member-template-overload-head-perf.json` | pass; the typed suffix fallback is now restricted to a real owner template prefix, all cache-disabled modes are byte-identical, and instructions and RSS improve |
| `(namespace anonymous-class array fix)` | Namespace-scope anonymous class definition whose adjacent object declarator is an array | -3.36% | -0.24% | -0.99% | -0.052% instructions, +1.31% RSS, and -0.030% footprint from the member-template checkpoint | `/tmp/cppgm-boost-frontier-v2-anonymous-class-array-perf.json` | pass; both semantic frontends consume the retained class/declarator AST, instructions and footprint improve, and fixed memory remains below baseline |
| `(initial member-call disambiguation fix)` | Member function/template hiding an outer type in a call statement | -3.27% | -0.82% | -0.91% | +0.099% instructions, -0.582% RSS, and +0.084% footprint from the anonymous-class array checkpoint | `/tmp/cppgm-boost-frontier-v2-member-call-initial-parser-perf.json` | pass; the original lazy-body parse consumes the retained class member value/template-value snapshot, with no semantic retry, source scan, text parse, or added normal-path semantic work |
| `(function-pointer noexcept fix)` | Exception specification on a pointer-to-function object declarator | -3.25% | -0.93% | -0.94% | +0.021% instructions, -0.107% RSS, and -0.031% footprint from the initial member-call checkpoint | `/tmp/cppgm-boost-frontier-v2-noexcept-function-pointer-perf.json` | pass; the typed declarator builder consumes the retained function-qualifier only after a structured function suffix, with no parsing, cache, symbol, or normal-path semantic expansion |
| `(nested-owner member-template definition fix)` | Out-of-class member-template definition on a nested class defined outside its class-template owner | -3.36% | -0.95% | -0.94% | -0.114% instructions, -0.022% RSS, and -0.006% footprint from the function-pointer checkpoint | `/tmp/cppgm-boost-frontier-v2-nested-owner-member-template-perf.json` | pass; the ordinary resolver reuses the existing typed nested-owner preparation path and every fixed and rolling metric improves |
| `(inline nested-owner template-prefix fix)` | Out-of-class constructor and ordinary member-template definitions on a class nested directly in a class template | -2.81% | -0.43% | -0.86% | +0.040% instructions, +0.754% RSS, and +0.030% footprint from the using-directive checkpoint | `/tmp/cppgm-boost-frontier-v2-inline-nested-owner-template-prefix-perf.json` | pass; the typed enclosing-owner check runs only when the direct class has no source template, all cache-disabled outputs are byte-identical, and rolling instructions remain below the hotspot threshold |
| `(typed pack-expanded argument syntax fix)` | Recursive conditional-alias pack expansion carrying an exact semantic type into later class-template materialization | -2.81% | -0.66% | -0.97% | -0.006% instructions, -0.230% RSS, and -0.112% footprint from the inline nested-owner checkpoint | `/tmp/cppgm-boost-frontier-v2-typed-pack-syntax-perf.json` | pass; all fixed and rolling metrics improve, and normal, every individual cache-disabled mode, and all-disabled mode emit byte-identical LowIR |
| `(dependent qualified-member placeholder cache fix)` | Partial-specialization placeholder pattern containing a dependent qualified member | -2.66% | -1.32% | -0.99% | +0.06% instructions, -0.80% RSS, and +0.06% footprint from the typed pack-syntax checkpoint | `/tmp/cppgm-boost-frontier-v2-qualified-member-cache-fixed-perf.log`, `/tmp/cppgm-boost-frontier-v2-qualified-member-cache-rolling-perf.log` | pass; context-sensitive dependent qualified-member patterns bypass only the persistent cross-match placeholder cache, and normal, witness/cache-off, and trace/selection-cache-off LowIR are byte-identical |
| `(nested cv partial identity fix)` | Nested cv-pointer deduction and ordering in a class partial-specialization template-id | -3.46% | -1.46% | -1.83% | aggregate from the preserved pre-Bimap typed-pack checkpoint: -0.39% instructions, -2.28% RSS, and -1.00% footprint | `/tmp/cppgm-boost-frontier-v2-nested-cv-fixed-perf.log`, `/tmp/cppgm-boost-frontier-v2-nested-cv-rolling-perf.log` | pass; the matcher uses typed template-parameter identity, and normal, all-cache-disabled, witness/cache-off, and trace/selection-cache-off LowIR are byte-identical |
| `(qualified declval fallback fix)` | Qualified `declval` template-id dependency in a trailing-return operator probe | -3.52% | -1.09% | -1.83% | aggregate from the preserved pre-Bimap typed-pack checkpoint: -0.53% instructions, -1.54% RSS, and -0.87% footprint | `/tmp/cppgm-boost-frontier-v2-qualified-declval-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-qualified-declval-rolling-perf.json` | pass; the final fallback consumes the already retained structured dependency signal, every cache-disabled mode is byte-identical, and both fixed and rolling hardware-work metrics improve |
| `(typed overload-candidate refresh fix)` | Member overload candidates invalidated by implicit-object class completion | -3.06% | -1.09% | -1.86% | aggregate from the preserved pre-Bimap typed-pack checkpoint: -0.25% instructions, -0.89% RSS, and -0.87% footprint | `/tmp/cppgm-boost-frontier-v2-bimap-candidate-refresh-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-bimap-candidate-refresh-rolling-perf.json` | pass; the +0.28 instruction percentage-point movement from the preceding aggregate exceeds the documentation threshold but remains below the hotspot trigger; snapshot and liveness work is confined to member candidates on incomplete classes whose reference members have been collected |
| `(visible value shadows function-id fix)` | Function parameter shadowing a namespace function during target-aware function-pointer resolution | -3.33% | -0.96% | -1.75% | -0.282% instructions, +0.127% RSS, and +0.119% footprint from the typed overload-candidate refresh checkpoint | `/tmp/cppgm-boost-frontier-v2-bind-function-shadow-fixed-perf.json` | pass; target-aware overload-set resolution now defers to the already retained visible value binding, fixed hardware work improves, and all rolling increases remain below the early-warning threshold |
| `(concrete member-alias identity fix)` | Concrete class argument retained through a member typedef beside a same-spelled template parameter | -3.39% | -1.22% | -1.85% | -0.03% instructions, +0.81% RSS, and +0.18% footprint from the function-shadow checkpoint | `/tmp/cppgm-boost-frontier-v2-apply-rv-typed-identity-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-apply-rv-typed-identity-rolling-perf-valid.json` | pass; the legacy spelling fallback is now skipped for already concrete typed identities, fixed hardware work improves, and all rolling deltas remain within tolerance |
| `(unnamed local-class ABI fix)` | Function-local unnamed class constructor symbols in a class-template member | -3.09% | -0.27% | -1.83% | +0.321% instructions, +1.092% RSS, and +0.011% footprint from the concrete member-alias checkpoint | `/tmp/cppgm-boost-frontier-v2-bloom-unnamed-local-fixed-perf-optimized.json`, `/tmp/cppgm-boost-frontier-v2-bloom-unnamed-local-rolling-perf-optimized.json` | pass; the +0.30 instruction percentage-point movement is documented above the early-warning threshold but remains below the hotspot trigger; retained local-entity metadata and ABI work are confined to actual named or unnamed function-local class owners |
| `(typed pack-element result fix)` | Variadic function-template result containing two local-lambda rvalue references | -3.24% | -1.01% | -1.84% | +0.30% instructions, +0.85% RSS, and +0.01% footprint from the pre-Bloom Bind checkpoint across the concrete-alias, unnamed-local ABI, and typed pack-element fixes | `/tmp/cppgm-boost-frontier-v2-bloom-pack-result-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-bloom-pack-result-rolling-from-bind-perf.json` | pass; the fixed cumulative result improves every primary metric, while the broader three-fix rolling movement is recorded above the +0.25% early-warning threshold and remains below the +0.50% hotspot trigger |
| `(typed local member-pointer owner fix)` | Pack-expanded class alias producing a member pointer owned by a function-local class | -3.23% | -1.87% | -1.83% | +0.11% instructions, -0.08% RSS, and +0.02% footprint from the pre-Bloom Bind checkpoint | `/tmp/cppgm-boost-frontier-v2-callable-local-member-owner-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-callable-local-member-owner-rolling-from-bind-perf.json` | pass; member-pointer substitution and declarator analysis share the retained semantic owner, all twelve cache configurations are byte-identical, and every fixed and rolling gate remains within tolerance |
| `(qualified member-address ref fix)` | `decltype(&T::operator())` used to classify cv/ref-qualified function objects | -3.26% | -1.06% | -1.76% | -0.00% instructions, +0.87% RSS, and -0.01% footprint from the typed local member-pointer-owner checkpoint | `/tmp/cppgm-boost-frontier-v2-callable-member-address-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-callable-member-address-rolling-perf.json` | pass; the leaf resolver consumes retained/ordinary typed owner lookup and reconstructs the function ref-qualifier from `FunctionBinding`, with all twelve cache configurations byte-identical |
| `(structured member-pointer argument fix)` | Nested alias substitution of a dependent data type inside `T C::*` | -3.28% | -1.65% | -1.83% | -0.02% instructions, +0.55% RSS, and +0.00% footprint from the qualified member-address checkpoint | `/tmp/cppgm-boost-frontier-v2-callable-member-pointer-argument-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-callable-member-pointer-argument-rolling-perf.json` | pass; structured member-pointer declarators remain on recursive typed substitution, all twelve cache configurations are byte-identical, and every fixed and rolling gate remains within tolerance |
| `(captureless lambda closure identity fix)` | Direct captureless-lambda operand in `auto` deduction plus its standard function-pointer conversion | -3.79% | -1.13% | -1.86% | -0.48% instructions, -0.50% RSS, and +0.02% footprint from the structured member-pointer-argument checkpoint | `/tmp/cppgm-boost-frontier-v2-captureless-lambda-identity-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-captureless-lambda-identity-rolling-perf.json` | pass; `auto` retains the existing typed closure representation, function-pointer materialization is lazy and typed, all twelve cache configurations are byte-identical, and every fixed and rolling gate passes |
| `(constexpr class-to-scalar built-in fix)` | Built-in comparison whose class operand has an inherited constexpr conversion to the other scalar operand type | -3.72% | -0.98% | -1.81% | +0.09% instructions, -0.60% RSS, and +0.00% footprint from `9290640fa` | `/tmp/cppgm-boost-frontier-v2-invocable-conversion-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-invocable-conversion-rolling-perf.json` | pass; the constexpr fallback reuses typed target conversion before retrying the original built-in operator, all twelve cache configurations are byte-identical, and every fixed and rolling gate passes |
| `(member-pointer ref-qualifier fix)` | C++11 `.*`/`->*` object-category viability for ref-qualified member-function pointers | -3.63% | -0.51% | -1.85% | -0.06% instructions, +1.38% RSS, and +0.04% footprint from `3433e1bcc` | `/tmp/cppgm-boost-frontier-v2-pmf-ref-qualifier-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-pmf-ref-qualifier-rolling-perf.json` | pass; the normal call analyzer and structured invocation helper enforce the same typed member-pointer rule, all twelve cache configurations are byte-identical, and every fixed and rolling gate passes |
| `(typed base-pack element fix)` | Namespace-qualified direct type-pack elements used as class bases | -3.62% | -0.39% | -1.77% | -0.00% instructions, -0.08% RSS, and +0.01% footprint from exact `e816d6560` | `/tmp/cppgm-boost-frontier-v2-charconv-base-pack-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-charconv-base-pack-rolling-perf.json` | pass; direct base-pack expansion carries each bound exact `TypePtr`, all twelve cache configurations are byte-identical, and every fixed and rolling gate passes |
| `(concrete structured qualifier constexpr fix)` | Concrete `decltype`-qualified static constexpr data-member value use | -3.78% | -1.61% | -1.85% | -0.03% instructions, +0.50% RSS, and +0.05% footprint from exact `149cff9a3` | `/tmp/cppgm-boost-frontier-v2-charconv-static-constexpr-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-charconv-static-constexpr-rolling-perf.json` | pass; constant folding is enabled only when the retained structured qualifier resolves to an exact non-dependent `TypePtr`, all twelve cache configurations are byte-identical, and every fixed and rolling gate passes |
| `(defaulted non-type declaring-scope fix)` | Dependent class result whose trailing non-type argument is source-defaulted | -3.80% | -0.99% | -1.83% | -0.11% instructions, +0.19% RSS, and +0.01% footprint from exact `01425122b` | `/tmp/cppgm-boost-frontier-v2-chrono-default-scope-fixed-perf.json`, `/tmp/cppgm-boost-frontier-v2-chrono-default-scope-rolling-perf.json` | pass; only the trailing source-defaulted argument run is regenerated in the class template's declaring scope, all twelve cache configurations are byte-identical, and every fixed and rolling gate passes |
| `(overloaded unary constexpr fallback fix)` | Class-valued unary expression resolved through a constexpr member operator | -3.75% | -1.78% | -1.80% | +0.052% instructions, -0.795% RSS, and +0.026% footprint from the already recorded `069eb2dc8` candidate measurements | `/tmp/cppgm-boost-frontier-v2-chrono-unary-constexpr-fixed-perf.json` | pass; built-in unary evaluation falls through to the existing typed special-expression hook only on failure, the synthetic member call retains a structured `QualifiedName`, all twelve cache configurations are byte-identical, and every fixed and rolling gate passes |
| `(nested catch active-cleanup fix)` | Exception propagation from a nested typed catch through an active enclosing catch | -3.90% | -1.73% | -1.79% | -0.156% instructions, +0.049% RSS, and +0.008% footprint from the already recorded `3c2623d84` candidate measurements | `/tmp/cppgm-boost-frontier-v2-chrono-active-catch-candidate.json` | pass; only the current candidate was measured, while both fixed and rolling comparisons reuse their immutable recorded measurements; nested landing pads expose enclosing typed handlers after cleanup and execute typed scopes plus the active catch cleanup before transferring control; all twelve cache configurations are byte-identical and every fixed and rolling gate passes |
| `(dependent result active-scope fix)` | Out-of-class member result carrying a direct owner type parameter | -3.31% | -0.69% | -1.73% | +0.615% instructions, +1.064% RSS, and +0.069% footprint from the already recorded `33bebba1e` candidate measurements | `/tmp/cppgm-boost-frontier-v2-chrono-dependent-result-scope-candidate.json` | pass; only the current candidate was measured and the rolling comparison reuses the immutable parent medians; active-scope resolution is restricted to metadata carrying a direct type-parameter argument, while compound-only alias and partial-specialization metadata retains the conservative declaration-scope path |
| `(member-data cv/xvalue fix)` | Pointer-to-data-member access through const and rvalue objects | -3.36% | -0.47% | -1.74% | +0.564% instructions, +1.286% RSS, and +0.059% footprint from the already recorded `33bebba1e` candidate measurements | `/tmp/cppgm-boost-frontier-v2-compat-member-data-candidate.json` | pass; only the current candidate was measured. The rolling instruction movement crosses the +0.50% sampling trigger, but the 7,359-sample profile contains neither changed helper and remains dominated by allocation/tokenization and existing template work; semantic hotspot metrics keep both text-resolution counters at zero. |
| `(function-designator constexpr fix)` | Forwarded function designators and indirect constexpr calls | -2.88% | -0.31% | -1.62% | +0.492% instructions, +0.164% RSS, and +0.112% footprint from the already recorded `e1e9f0094` candidate measurements | `/tmp/cppgm-boost-frontier-v2-compat-function-designator-perf.json` | pass; only the current candidate was measured. The rolling instruction movement remains below the +0.50% hotspot trigger and all cumulative gates pass. |
| `(member-pointer constexpr fix)` | Forwarded data and member-function pointers through base and derived objects in constant evaluation | -2.82% | -1.00% | -1.66% | +0.068% instructions, -0.697% RSS, and -0.031% footprint from the immutable recorded `b7efbce95` parent medians | `/tmp/cppgm-boost-frontier-v2-compat-member-pointer-constexpr-final-optimized-candidate.json` | pass; only the current candidate was measured. Rolling deltas are arithmetic against saved parent medians `263821754216` instructions, `1289953280` RSS, and `1007472640` footprint; every cumulative and rolling gate passes below the hotspot trigger. |
| `(implicit-object ref-qualifier rank fix)` | Otherwise tied `const &` and `const &&` member overloads called on a const rvalue | -2.87% | -1.33% | -1.60% | -0.052% instructions, -0.335% RSS, and +0.056% footprint from the immutable recorded `4c3c19f3a` candidate medians | `/tmp/cppgm-boost-frontier-v2-compat-implicit-object-refq-candidate.json` | pass; only the current candidate was measured. Rolling deltas are arithmetic against saved parent medians `263999879466` instructions, `1280966656` RSS, and `1007157248` footprint; every cumulative and rolling gate passes below the hotspot trigger. |
| `(structured member-pointer NTTP deduction fix)` | Member-pointer non-type template arguments retained only their scalar placeholder during function-template deduction; repeated member-address analysis, forwarding-reference binding, and template-template partial matching then lost or misclassified the structured value. Deduction now carries the exact `TemplateArgument`, qualified member-address analysis consumes typed field ownership, named non-type template parameters use the existing typed prvalue hooks, and template-template parameters retain recursively parsed kind/type descriptors checked on every structured deduction path. | -2.84% | -0.17% | -1.42% | +0.027% instructions, +0.229% RSS, and +0.254% footprint from immutable saved `9c12bf798` parent medians | `/tmp/cppgm-boost-frontier-v2-function-ref-nttp-candidate-perf.json` | pass; only the candidate was measured, all fixed and rolling gates pass, and no hotspot threshold is crossed |
| `(Compat final closure)` | Explicit initializer-list casts, global placement new, repeated named local-class ABI entities, shadow-safe resolved default alias types, function-designator trait categories, and cv-aware class transfer | -3.32% | -0.64% | -1.49% | -0.495% instructions, -0.464% RSS, and -0.064% footprint from immutable saved `a61937ef0` parent medians | `/tmp/cppgm-boost-frontier-v2-compat-final-candidate.json` | pass; only the exact final candidate was measured, all fixed and rolling metrics improve, and no parent command was run |
| `(function-valued NTTP semantic validation fix)` | Semantic validation of a selected function-valued non-type template argument and its called function bodies | -3.05% | -1.13% | -1.45% | -0.216% instructions, -0.960% RSS, and -0.024% footprint from immutable saved `768ee5f44` parent medians | `/tmp/cppgm-boost-frontier-v2-concept-check-semantic-validation-candidate.json` | pass; only the candidate was measured, all fixed and rolling metrics improve, and no parent command was run |
| `(constexpr reference storage identity fix)` | Constant-evaluated references passed through a constexpr function while retaining their named object's storage identity | -2.89% | -2.31% | -1.40% | +0.160% instructions, -1.196% RSS, and +0.050% footprint from immutable saved `56afe87c1` parent medians | `/tmp/cppgm-boost-frontier-v2-config-constexpr-reference-candidate.json` | pass; only the candidate was measured, every fixed and rolling gate passes, and no parent command was run |
| `(alias-template non-deduced context fix)` | Function-template parameter whose direct alias target is a dependent qualified member type | -0.12% | -2.11% | -0.16% | n/a; first production candidate in the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-container-alias-perf.log` | pass; only the candidate was measured against immutable frozen-header baseline `9764b3835`, and no parent command was run |
| `(concrete deferred-layout retry fix)` | Concrete recursive node specialization completed structurally before its value type acquired layout | -0.09% | -1.54% | -0.03% | n/a; compared only within the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-container-recursive-layout-perf.json` | pass; only the candidate was measured against immutable frozen-header baseline `9764b3835`, all cumulative metrics improve, and no parent command was run |
| `(concrete namespace-function scope isolation fix)` | Concrete namespace function-template return SFINAE reached from a partial-specialization member template | -0.66% | -3.53% | -2.52% | n/a; compared only within the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-container-scoped-allocator-usage-perf.json` | pass; only the candidate was measured against immutable frozen-header baseline `9764b3835`, all cumulative metrics improve, and no parent command was run |
| `(concrete owner-pack deduction fix)` | Converting constructor inherited from an all-pack primary's partial specialization | -0.56% | -2.22% | -2.52% | n/a; compared only within the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-container-scoped-allocator-adaptor-perf.json` | pass; only the candidate was measured against immutable frozen-header baseline `9764b3835`, all cumulative metrics improve, and no parent command was run |
| `(defaulted template-head coverage fix)` | Partial ordering among direct template-template heads that omit different numbers of canonical default arguments | -0.59% | -3.45% | -2.51% | n/a; compared only within the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-container-flat-set-adaptor-perf.json` | pass; only the candidate was measured against immutable frozen-header baseline `9764b3835`, all cumulative metrics improve, and no parent command was run |
| `(reference initializer-list constructor fix)` | Phase-one list-constructor selection for a `const std::initializer_list<T>&` parameter | -0.49% | -2.94% | -2.56% | n/a; compared only within the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-container-devector-initlist-perf.json` | pass; only the candidate was measured against immutable compiler-and-header baseline `9764b3835`, all cumulative metrics improve, and no parent command was run |
| `(default-owner and initializer-list lifetime fixes)` | Declaring-template rematerialization for defaulted non-type arguments plus backing-array cleanup and lifetime extension | +0.02% | -3.63% | -2.57% | n/a; compared only within the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-container-default-owner-initlist-candidate.json` | pass; only the combined candidate was measured against immutable compiler-and-51-header baseline `9764b3835`, using the checked-in 51-header closure rather than live `dev/src`; no parent command was run |
| `(callable-object ranking and dependent result rebinding fixes)` | Structured `operator()` result selection by typed conversion rank plus declaration-scope rebinding of source-dependent non-type result arguments | +0.10% | -3.07% | -2.49% | n/a; compared only within the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-container-hash-candidate.json` | pass; only the combined candidate was measured against immutable compiler-and-51-header baseline `9764b3835`, using the checked-in 51-header closure rather than live `dev/src`; no parent command was run |
| `(Contract typed semantic and lowering closure)` | Functional-cast lookup, captureless-lambda identity, destructor lifetime/unwind, elision access, member-pointer access, literal control flow, virtual receiver adjustment, i128 call ABI, using-declaration filtering, and local-lambda RTTI | +0.52% | -1.38% | -2.43% | n/a; compared only within the frozen-project-header epoch | exact candidate-only check against `/tmp/cppgm-boost-frontier-v2-frozen-header-epoch-9764b3835.json`; diagnostic sample `/tmp/cppgm-contract-self-compile.sample.txt` | pass; only the candidate was measured against immutable compiler-and-51-header baseline `9764b3835`; the threshold diagnostic found no changed helper in a hotspot, and no parent or live project header was measured |
| `(Core typed constexpr and access closure)` | Address-only constant values and `this` base references, deleted-constructor selection, private-base `static_cast` access, and lazy class-scope `static_assert` evaluation | +0.80% | -1.21% | -2.35% | n/a; compared only within the frozen-project-header epoch | `/tmp/cppgm-boost-frontier-v2-core-candidate.json`; diagnostic sample `/tmp/cppgm-boost-frontier-v2-core.sample.txt` and hotspot log `/tmp/cppgm-boost-frontier-v2-core-hotspot.log` | pass; only the candidate was measured against immutable compiler-and-51-header baseline `9764b3835`, using `benchmarks/self_compile/stable/include`; the threshold diagnostic found no changed helper in a hotspot, and no parent or live project header was measured |
| `(explicit-specialization member-template owner fix)` | Out-of-class member and constructor templates of an explicit class specialization | +0.77% | -1.27% | -2.30% | -0.03 instruction percentage points from the Core checkpoint | `/tmp/cppgm-boost-frontier-v2-coroutine-candidate.json`; diagnostic sample `/tmp/cppgm-boost-frontier-v2-coroutine.sample.txt` and hotspot log `/tmp/cppgm-boost-frontier-v2-coroutine-hotspot.log` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`, using `benchmarks/self_compile/stable/include`. The collector is an existing hot semantic function, but the new explicit-specialization path adds only a typed flag check and empty owner-binding selection, with no loop, lookup, allocation, or cache; fragment requests remain zero. No parent or live project header was measured. |
| `(dependent NTTP declarator preservation fix)` | Dependent non-type parameter type whose declared identifier is already bound in the use scope | +0.82% | -1.53% | -2.31% | +0.05 instruction percentage points from the Coroutine checkpoint | `/tmp/cppgm-boost-frontier-v2-crc-candidate.json`; diagnostic sample `/tmp/cppgm-boost-frontier-v2-crc.sample.txt` and hotspot log `/tmp/cppgm-boost-frontier-v2-crc-hotspot.log` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`, using `benchmarks/self_compile/stable/include`. The existing substitution helper falls from 25 to 22 top-stack samples versus the preceding checkpoint, while the new work is a structured declarator copy and identifier restore with no lookup, parse, cache, or semantic scope allocation. No parent or live project header was measured. |
| `(DLL typed semantic, ABI, and object closure)` | Linkage/attributes, namespace and member lookup, packs, initialization/conversion, host ABI, mangling, builtin invocation, and predefined function text | +0.96% | +0.96% | +0.39% | +0.14 instruction percentage points from the CRC checkpoint | `/tmp/cppgm-boost-frontier-v2-dll-candidate-scoped-lookup-final.json`; sample `/tmp/cppgm-boost-frontier-v2-dll.sample.txt` and hotspot log `/tmp/cppgm-boost-frontier-v2-dll-hotspot.log` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The checked-in epoch manifest verifies the exact frozen source and 51-header closure before every run. Namespace visibility metadata is lazy, source-point filtering is prefiltered, large memory-class arguments avoid duplicate triviality work, and using-directive lookup is limited to the structured qualified-type consumer. No parent or live project header was measured. |
| `(Dynamic Bitset deferred member-definition replay fix)` | Nested conversion operators, out-of-class defaulted special members, and repeated injected-class owner qualifiers | +0.96% | +0.11% | +0.45% | +0.001% instructions, -0.848% RSS, and +0.065% footprint from the recorded DLL candidate medians | `/tmp/cppgm-boost-frontier-v2-dynamic-bitset-candidate-guarded.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The first implementation attempted a second qualified-owner lookup after every failed multi-qualifier lookup and was rejected at +12.86% instructions, +8.02% RSS, and +9.04% footprint. The accepted typed-name guard limits that lookup to repeated injected-class forms. No parent or live project header was measured. |
| `(Exception typed semantic and lazy conversion closure)` | Concrete-owner result SFINAE, dependent alias detection, unsigned enum lowering, derived/base pointer equality, elaborated template-argument display, and selected-only conversion-template body/symbol materialization | -1.44% | +1.76% | +2.19% | n/a; one final three-run candidate measurement for the complete Exception closure | `/tmp/cppgm-boost-frontier-v2-exception-final-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Filesystem typed lookup and range closure)` | Qualified overload-set arguments, source-point-aware ADL imports, range-for lookup and auto deduction, and parameter-aware return-SFINAE identity | -1.30% | +1.50% | +2.24% | n/a; one final three-run candidate measurement for the complete Filesystem closure | `/tmp/cppgm-boost-frontier-v2-filesystem-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Flyweight overload and constexpr termination fixes)` | Element-wise conversion ranking between initializer-list overloads plus a no-progress recursion guard for typed initializer constant evaluation | -1.46% | +1.39% | +2.18% | n/a; one final three-run candidate measurement for the complete Flyweight closure | `/tmp/cppgm-boost-frontier-v2-flyweight-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Function implicit-static allocation function fix)` | Language-mandated static classification for class-scope allocation and deallocation functions across declarations, definitions, templates, and output lookup | -1.26% | +1.71% | +2.23% | n/a; one final three-run candidate measurement for the complete Function closure | `/tmp/cppgm-boost-frontier-v2-function-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Fusion qualified value-pack and concrete dependency fixes)` | Initial parsing of qualified-id expression pack expansions plus concrete typed SFINAE-result dependency classification | -1.49% | +0.84% | +2.23% | n/a; one final three-run candidate measurement for the complete Fusion closure | `/tmp/cppgm-boost-frontier-v2-fusion-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(LEAF typed semantic and dispatch closure)` | Dependent non-type packs and qualified `sizeof` results, conversion-function deduction/ranking, concrete alias materialization, injected-class lookup, same-type RTTI conversion, inline-namespace pretty-function spelling, and base-view virtual dispatch | -2.22% | +1.27% | +2.44% | n/a; one final three-run candidate measurement for the complete LEAF closure | `/tmp/cppgm-boost-frontier-v2-leaf-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(parenthesized qualified-call parse fix)` | Parenthesized qualified zero-argument call immediately followed by multiplication | -2.00% | +1.20% | +2.35% | n/a; one final three-run candidate measurement for the complete Lexical Cast closure | `/tmp/cppgm-boost-frontier-v2-lexical-cast-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Local Function structured semantic closure)` | Macro-retained namespace-template visibility, repeated local declaration/expression probes, and qualified-return function-pointer declarations | -1.84% | +2.23% | +2.50% | n/a; one final three-run candidate measurement for the complete Local Function closure | `/tmp/cppgm-boost-frontier-v2-local-function-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Locale typed semantic and hosted-facet closure)` | Function-specialization deduction, Unicode scalar encoding, explicit-instantiation replay, range-for conversion, implicit-object ranking, unknown-bound array deduction, and qualified `num_put::do_put` dispatch | -1.78% | +2.51% | +2.54% | n/a; one final three-run candidate measurement for the complete Locale closure | `/tmp/cppgm-boost-frontier-v2-locale-candidate.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Lockfree memory closure and bounded template cache)` | Counted class-array lifecycle lowering, in-place LowIR/object transformations, and a 4096-entry bound on the template-argument backing cache | -5.35% | -0.39% | +2.34% | the uncapped candidate was -5.62% instructions, +2.13% RSS, and +4.01% footprint; retaining the 512-entry fast ring while bounding its backing map recovers memory with only 0.27 instruction percentage points returned | `/tmp/cppgm-boost-frontier-v2-lockfree-memory-candidate-cap4096.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Log allocation lookup closure)` | Class-specific sized deallocation, qualified template-id parsing under a value-name shadow, and authoritative typed qualifier chains for synthetic inherited allocation calls | -5.51% | -0.50% | +2.36% | versus the bounded Lockfree checkpoint, instructions improve 0.16 percentage points and RSS improves 0.11 percentage points while footprint moves only +0.02 percentage points | `/tmp/cppgm-boost-frontier-v2-log-structured-qualifier-final-r3.json` | pass; candidate-only measurement against immutable compiler-and-51-header baseline `9764b3835`. The gate verified the exact frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent or live project header was measured. |
| `(Log AST rare-payload memory recovery)` | Copy-on-write storage for the four rare GNU ext-vector, section, and asm-label strings previously embedded in every `CppAstNode` | -4.78% | -2.59% | -1.99% | versus the pre-compaction Phoenix candidate, instructions improve another 0.20%, RSS falls 2.13%, and footprint falls 4.20% | `/tmp/cppgm-boost-frontier-v2-log-ast-rare-strings-perf.json` | pass; `CppAstNode` shrinks from the allocator's 384-byte size class to a 296-byte object. The three-run candidate-only gate verified the exact frozen source, all 51 headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`. No parent or live project header was measured. |
| `(Log nested template-id CV fallback fix)` | Full structured CV/ref shape validation before recursively deducing an embedded template-id | -4.76% | -2.65% | -2.01% | versus the rare-payload checkpoint, instructions move only +0.02 percentage points while RSS and footprint improve another 0.06 and 0.02 percentage points | `/tmp/cppgm-boost-frontier-v2-log-nested-cv-fallback-perf.json` | pass; the three-run candidate-only gate verified the exact frozen source, all 51 headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`. The fix retains the recovered memory signal and essentially all of the instruction win; no parent or live project header was measured. |
| `(Log compact Type and remaining semantic closure)` | Copy-on-write storage for rare dependent `Type` metadata plus the remaining local-static, global-initialization, aggregate-lowering, vtable-slot, and inline-linkage fixes | -4.47% | -8.42% | -9.46% | versus the nested-CV checkpoint, RSS improves 5.77 percentage points and footprint improves 7.45 percentage points while retaining 4.47% fewer instructions than baseline | `/tmp/cppgm-boost-frontier-v2-inline-prior-declaration-final-perf.json` | pass; `sizeof(Type)` falls from 560 to 280 bytes and recursive dependency walks visit shared type nodes once. The candidate-only gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`; no parent or live header was measured. |
| `(Log multiple-pack transformed-template ordering fix)` | One synthesized scalar placeholder per function-template parameter, including each independent pack, during partial ordering | -4.41% | -8.47% | -9.48% | versus the preceding complete Log checkpoint, movement is negligible: +0.06 instruction percentage points, -0.05 RSS percentage points, and -0.02 footprint percentage points | `/tmp/cppgm-boost-frontier-v2-function-pointer-inner-pack-final-perf.json` | pass; candidate medians are 251,927,356,098 instructions, 1,183,129,600 B RSS, and 914,116,608 B footprint. The gate verified frozen source epoch `9764b3835` and the exact 51-header closure before every run. No parent compiler or live project header was measured. |
| `(Logic overloaded constexpr expression fix)` | Overloaded `&&`/`||` selection plus contextual explicit-bool conversion in constant expressions | -3.93% | -8.34% | -8.94% | versus the final Log checkpoint, +0.48 instruction percentage points, +0.14 RSS percentage points, and +0.54 footprint percentage points; scalar fast paths and a raw type predicate retain nearly all of the instruction win while keeping the recovered memory signal | `/tmp/cppgm-boost-frontier-v2-logic-constexpr-final.json` | pass; candidate medians are 253,211,604,272 instructions, 1,184,903,168 B RSS, and 919,605,248 B footprint. The candidate-only gate verified frozen source epoch `9764b3835` and the exact 51-header closure before every run. No parent compiler or live project header was measured. |
| `(Metaparse structured deferral closure)` | Dependent scalar value initialization, nested member-class-template specialization ownership, and structural deferral of dependent typedef chains | -4.34% | -8.42% | -9.08% | versus the final Logic checkpoint, instructions improve 0.41 percentage points while RSS improves 0.08 and footprint improves 0.14 percentage points; the exponential typedef-chain path falls to ordinary linear work | `/tmp/cppgm-boost-frontier-v2-metaparse-final.json` | pass; candidate medians are 252,127,533,039 instructions, 1,183,866,880 B RSS, and 918,171,648 B footprint. The candidate-only gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent compiler or live project header was measured. |
| `(Move C++11 semantic closure)` | Deleted-copy triviality, non-class member-pointer SFINAE, and retained same-name type/function call ambiguity | -4.23% | -8.30% | -9.04% | versus the final Metaparse checkpoint, movement is small: +0.11 instruction percentage points, +0.12 RSS percentage points, and +0.04 footprint percentage points while preserving the large cumulative wins | `/tmp/cppgm-boost-frontier-v2-move-final.json` | pass; candidate medians are 252,415,291,337 instructions, 1,185,398,784 B RSS, and 918,568,960 B footprint. The candidate-only gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent compiler or live project header was measured. |
| `(MP11 fold alias-template compaction and query memo)` | Exact nested alias-template-id ambiguity compaction, lazy restoration at the actual pack expansion, and short-lived dependent-type resolution memoization | -11.15% | -10.33% | -11.38% | versus the final Move checkpoint, instructions improve 6.92 percentage points while RSS improves 2.03 and footprint improves 2.34 percentage points | `/tmp/cppgm-boost-frontier-v2-mp11-fold-rss-final-perf.json` | pass; candidate medians are 234,183,911,570 instructions, 1,159,094,272 B RSS, and 894,939,136 B footprint. The candidate-only three-run gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent compiler or live project header was measured. |
| `(MP11 class-reference dependency refresh)` | Upgrade a textual class-template cache hit from a dependent placeholder instance to the later concrete template argument before reusing its member alias scope | -11.30% | -10.65% | -11.45% | versus the preceding MP11 fold checkpoint, instructions improve 0.15 percentage points, RSS 0.32, and footprint 0.07 | `/tmp/cppgm-boost-frontier-v2-mp-fold-q-cache-rebind-perf.json` | pass; candidate values are 233,790,295,604 instructions, 1,154,969,600 B RSS, and 894,275,584 B footprint. The candidate-only gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`. No parent compiler or live project header was measured. |
| `(MP11 both-pack fixed-prefix partial order)` | Prefer the otherwise unconstrained template-id trailing-pack pattern with the longer unique fixed type-parameter prefix | -11.27% | -10.64% | -11.36% | versus the class-reference refresh checkpoint, +0.03 percentage points instructions, +0.01 RSS, and +0.09 footprint | `/tmp/cppgm-boost-frontier-v2-mp-reverse-fold-pack-order-perf.json`, `/tmp/cppgm-boost-frontier-v2-mp-reverse-fold-pack-order-perf-repeat.json` | pass twice on the final uninstrumented candidate; the confirmation records 233,864,001,553 instructions, 1,155,149,824 B RSS, and 895,197,184 B footprint. Both runs used frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`; no parent compiler or live project header was measured. |
| `(MP11 template-template pack identity and cache-key ownership)` | Preserve every concrete template-template pack element during structured alias expansion and retain each dependent-type cache input for the bounded lifetime of its pointer-keyed entry | -11.08% | -10.45% | -11.43% | versus the reverse-fold checkpoint, +0.19 percentage points instructions, +0.19 RSS, and -0.07 footprint; all cumulative metrics remain substantially improved | `/tmp/cppgm-boost-frontier-v2-mp-compose-cache-lifetime-perf.json` | pass on three candidate-only runs; medians are 234,354,837,915 instructions, 1,157,537,792 B RSS, and 894,443,520 B footprint. The gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`; each compiler process released before the next run and recorded zero swaps. No parent compiler or live project header was measured. |
| `(MP11 empty-owner and member-alias source-scope fixes)` | Materialize a structured zero-argument class specialization before consulting stale class-info cache state, and expand retained member-alias source arguments in their original use scope | -11.12% | -10.61% | -11.45% | versus the identity-alias checkpoint, instructions move +0.07 percentage points while RSS improves 0.05 and footprint improves 0.08 percentage points | `/tmp/cppgm-boost-frontier-v2-mp-set-scope-fixed-perf.json` | pass; candidate medians are 234,257,336,609 instructions, 1,155,497,984 B RSS, and 894,291,968 B footprint. The three-run candidate-only gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent compiler or live project header was measured. |
| `(MP11 tuple-transform nested expansion and default deduction)` | Expand only packs owned by the current expansion layer, preserve nested pack-expansion AST until its own layer, and apply default non-type function-template arguments only after deduction | -11.12% | -10.49% | -11.41% | versus the preceding set checkpoint, instructions improve by 13,315,349 while RSS moves +1.49 MiB and footprint +0.37 MiB; all three cumulative signals remain substantially improved | `/tmp/cppgm-boost-frontier-v2-mp11-tuple-transform-perf.json` | pass; candidate medians are 234,244,021,260 instructions, 1,157,058,560 B RSS, and 894,676,992 B footprint. The three-run candidate-only gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent compiler or live project header was measured. |
| `(MP11 default member-alias declaration-scope binding)` | Protect declaration-owned template parameter names while overlaying the use scope for defaulted member-alias arguments | -10.91% | -10.47% | -11.42% | versus the tuple-transform checkpoint, instructions move +557,308,654, RSS +249,856 B, and footprint -90,112 B; the small movement retains the substantial cumulative wins | `/tmp/cppgm-boost-frontier-v2-mp11-partial-sum-perf.json` | pass; candidate medians are 234,801,329,914 instructions, 1,157,308,416 B RSS, and 894,586,880 B footprint. The three-run candidate-only gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent compiler or live project header was measured, and all runs recorded zero swaps. |
| `(MPL literal and array-pointer NTTP closure)` | Full-compiler ordinary multicharacter constant evaluation plus qualified static-array decay and retained-symbol lowering for pointer non-type template arguments | -11.16% | -10.72% | -11.37% | versus the final MP11 checkpoint, instructions improve by 663,791,277 and RSS by 3,244,032 B while footprint moves only +479,232 B; the instruction win and recovered RSS are both retained | `/tmp/cppgm-boost-frontier-v2-mpl-candidate.json` | pass; candidate medians are 234,137,538,637 instructions, 1,154,064,384 B RSS, and 895,066,112 B footprint. The candidate-only gate verified frozen source epoch `9764b3835`, frozen source hash `ab00b2e1c3c7463baf9d8e1e7fc754b9cde2c18749568616062011f31e7daba2`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent compiler or live project header was measured; all three fresh processes released before the next and recorded zero swaps. |
| `(MSM initial semantic closure)` | Inherited and dependent member-template lookup, deleting-destructor deallocation overload analysis, qualified-result parenthesized member-pointer declarations, and target-aware member-function-template address resolution | -11.08% | -10.42% | -11.38% | versus the MPL checkpoint, instructions move +208,710,543 while RSS moves +3,911,680 B and footprint improves by 98,304 B; all cumulative signals remain substantially improved | `/tmp/cppgm-msm-initial-closure-perf.json` | pass; three candidate-only runs have medians of 234,346,249,180 instructions, 1,157,976,064 B RSS, and 894,967,808 B footprint. The gate used immutable frozen source epoch `9764b3835` and its checked-in 51-header closure; no parent compiler or live project header was measured. An initially broad parser predicate was rejected after it misclassified libc++'s `std::__destroy_at(...)` expression and failed the workload; the accepted structural scan requires an actual parenthesized member-pointer declarator. |
| `(MSM qualified identity and incomplete-indirect closure)` | Collision-local namespace identity for class-template template arguments plus no eager class completion through pointer/reference cast targets and ABI parameters | -10.70% | -10.65% | -11.58% | versus the initial MSM checkpoint, instructions move +1,005,004,442 while RSS improves by 3,014,656 B and footprint improves by 1,978,368 B; the large cumulative instruction and memory wins remain | `/tmp/cppgm-boost-frontier-v2-msm-identity-incomplete-pointee-perf.json` | pass; candidate medians are 235,351,253,622 instructions, 1,154,961,408 B RSS, and 892,989,440 B footprint. The candidate-only gate verified frozen source epoch `9764b3835` and its exact 51-header closure before every run. All fresh processes released before the next, recorded zero swaps, and used no parent compiler or live project header. |
| `(MSM nested identity and virtual-parameter ABI closure)` | Preserve collision-qualified class-template identity through nested alias results and recover virtual-base parameter ABI metadata from an incomplete class's typed base graph without completing its members | -12.53% | -10.68% | -11.63% | versus the preceding MSM checkpoint, instructions improve by 4,808,435,527 while RSS improves by 339,968 B and footprint improves by 577,536 B; the large instruction win and recovered memory signal are both retained | `/private/tmp/cppgm-boost-frontier-v2-msm-pvb-final-perf.json` | pass; candidate medians are 230,542,818,095 instructions, 1,154,621,440 B RSS, and 892,411,904 B footprint. The candidate-only gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`. No parent compiler or live project header was measured. |
| `(MSM template-state sharing and pack ABI closure)` | Share immutable primary class-template arguments with binding and mangle state, discard disabled audit-only creation strings, preserve empty owner packs, and retain dependent member-pointer declarators in symbols | -12.89% | -11.97% | -12.91% | versus the preceding MSM checkpoint, instructions improve by 949,425,793 while RSS improves by 16,756,736 B and footprint improves by 12,890,112 B; both the instruction win and the recovered memory signal strengthen | `/private/tmp/cppgm-boost-frontier-v2-msm-pack-memory-final-accessor.json` | pass; the final candidate-only run records 229,593,392,302 instructions, 1,137,864,704 B RSS, and 879,521,792 B footprint. The gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`. The originally named `/tmp` baseline had been cleaned, so the check used the preserved byte-equivalent epoch baseline with the same head and medians; no parent compiler or live project header was measured. |
| `(MSM mangle-name allocation recovery)` | Copy only the rooted/qualifier syntax that survives when class-specialization metadata replaces its long internal name with the short source template name | -12.75% | -11.40% | -13.09% | versus the preceding checkpoint, instructions move +357,837,613 and RSS +7,393,280 B while footprint improves by 1,818,624 B; all cumulative signals remain substantially improved | `/private/tmp/cppgm-boost-frontier-v2-msm-mangle-name-final.json` | pass; the exact 18,083-class Euml census found 17,009,208 B of retained allocation for only 177,518 B of live template-name syntax. At the identical class count, releasing that discarded capacity reduced the instrumented process maximum from 1,872,293,888 B to 1,610,145,792 B. The final implementation avoids allocating and copying the discarded name at all. The frozen gate records 229,951,229,915 instructions, 1,145,257,984 B RSS, and 877,703,168 B footprint; it verified epoch `9764b3835` and all 51 frozen headers. |
| `(MSM algorithmic cleanup package)` | Remove eager unused instantiation, compact/share typed template state, avoid repeated rendered-name recovery, and skip witness-only class-lifecycle rendering when no witness session exists | -25.36% | -22.41% | -28.72% | versus the mangle-name checkpoint, instructions improve by 33,233,712,344, RSS by 142,258,176 B, and footprint by 157,884,416 B; the final witness-only gate alone removes another 7,299,286,986 instructions from exact `AnonymousEuml` with byte-identical object output | `/private/tmp/cppgm-boost-frontier-v2-final-witness-gate-perf.json` | pass; three-run medians are 196,717,517,571 instructions, 1,002,999,808 B RSS, and 719,818,752 B footprint. The gate verified frozen source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8` before every run. No parent compiler or live project header was measured, and system swap remained unchanged. |
| `(MSM structured template reconstruction package)` | Preserve qualified/template-id sidecars through direct type substitution, consume already resolved alias arguments without resolving their display text again, structurally deduce class-template arguments in partial function results, and bound dependent-qualified-member scans by exact `Type` identity | -24.30% | -21.11% | -30.09% | versus the preceding recorded medians, instructions move +1.42%, RSS +1.67%, and footprint improves 1.92%; the required sample finds no new structural-deduction helper with at least five self samples, only two samples in the visited-set walk, and 11 of 7,787 samples in template-syntax reconstruction | `/private/tmp/cppgm-boost-frontier-v2-template-template-candidate.json`; diagnostic `/private/tmp/cppgm-template-template.sample.txt` and `/private/tmp/cppgm-template-template-hotspot.log` | pass; three-run medians are 199,516,398,659 instructions, 1,019,727,872 B RSS, and 705,990,656 B footprint. The immutable gate verified exact epoch `9764b3835`, its frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`; no parent compiler or live project header was measured. |
| `(MSM concrete template-argument syntax compaction)` | Drop source AST from retained concrete type and ordinary value arguments after typed mangle state is complete; preserve member-pointer values, dependent arguments, template entities, and all witness-session syntax | -23.87% | -23.06% | -29.47% | versus the reference-field checkpoint, instructions move +0.19 percentage points, RSS improves 1.74 percentage points, and footprint moves +0.81 percentage points | `/private/tmp/cppgm-boost-frontier-v2-concrete-syntax-perf.json` | pass; three-run medians are 200,651,979,093 instructions, 994,582,528 B RSS, and 712,261,632 B footprint. The immutable gate verified epoch `9764b3835`, frozen source hash `ab00b2e1c3c7463baf9d8e1e7fc754b9cde2c18749568616062011f31e7daba2`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`; no parent compiler or live project header was measured. |
| `173ed08ec` | Materialize only the referenced class member/type slice, retain exact local-type identity in reconstructed displays, compact nested semantic identity keys, reuse completed host-ABI facts, bound compiler-internal function names, and remove rendered symbol work and a process-lifetime component-string memo that could not avoid linear work | -26.39% | -28.56% | -35.36% | versus the preceding concrete-syntax checkpoint, instructions improve 3.32%, RSS 7.15%, and footprint 8.35% | `/private/tmp/cppgm-boost-frontier-v2-msm-lazy-reference-clean-head-perf.json` | pass; the clean committed-HEAD three-run medians are 193,997,459,005 instructions, 923,467,776 B RSS, and 652,795,904 B footprint. The gate verified immutable epoch `9764b3835`, frozen source, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`; no parent compiler or live project header was measured. |
| `2c6db27c0` | Preserve the exact integral value of an inherited lazy static member after structured boolean evaluation materializes its qualifier | -26.56% | -28.56% | -35.61% | versus `173ed08ec`, instructions improve by 439,687,392, RSS by 45,056 B, and footprint by 2,535,424 B | `/private/tmp/cppgm-boost-frontier-v2-msm-lazy-integral-final-perf.json` | pass; committed-HEAD three-run medians are 193,557,771,613 instructions, 923,422,720 B RSS, and 650,260,480 B footprint. The gate verified immutable source epoch `9764b3835`, all 51 frozen headers, and closure hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`; no parent compiler or live project header was measured. |

## Suite Cursor

The inventory order comes from `docs/boost-b2-suite-status-20260511.md`. Add a
row when a suite is attempted. Do not prepopulate passes from V1.

| # | Suite | V2 status | Commit | Forced run evidence | Notes |
|---:|---|---|---|---|---|
| 1 | `libs/accumulators/test` | pass | `(external vbase layout runtime fix)` | Exact final forced survey rebuilt the full graph for 1800s with zero failures; all five former persistence failures (`rolling_count`, `rolling_sum`, `rolling_moment`, `rolling_variance`, and `rolling_mean`) rebuilt and passed. Log: `/tmp/boost-frontier-v2-suite-001-external-vbase-layout-runtime-final.log`. The non-forced continuation updated the remaining 12 targets, all four tests passed, and B2 exited successfully. Log: `/tmp/boost-frontier-v2-suite-001-external-vbase-layout-runtime-final-continuation.log`. The two compiler children orphaned by the forced timeout were terminated, then their affected `tail_variate_means` targets were forced cleanly; both rebuilt, passed, and B2 updated all 76 requested targets. Log: `/tmp/boost-v2-acc-external-vbase-layout-runtime-final-tail-focused.log`. | Suite closed on the exact final implementation. The final repository direct-LowIR report passed `3830 / 3830`; log `/tmp/cppgm-boost-frontier-v2-external-vbase-layout-runtime-final-test-report.log`. |
| 2 | `libs/algorithm/test` | pass | `(extern-template out-of-class member fix)` | The exact focused target forced 32 targets, rebuilt the Boost.Test archive, and passed compile, link, and runtime; log `/tmp/boost-v2-algorithm-apply-permutation-extern-template-fix.log`. The exact full forced suite rebuilt 218 targets and exited successfully; log `/tmp/boost-frontier-v2-suite-002-extern-template-fix-full.log`. | Initial forced evidence `/tmp/boost-frontier-v2-suite-002-initial-forced.log` had one real failure: `apply_permutation_test` linked against the otherwise redundant Boost.Test archive and reported 27 duplicate symbols. All positives and deliberate compile-fail targets pass with the fix. Final direct-LowIR report passes `3830 / 3830`; log `/tmp/cppgm-boost-frontier-v2-algorithm-extern-template-final-test-report.log`. |
| 3 | `libs/align/test` | pass | `(dependent alignof NTTP fix)` | The focused `alignment_of_test` forced four targets and passed compile, link, and runtime; log `/tmp/boost-v2-align-alignment-of-after-layout-fix.log`. The exact complete forced suite rebuilt all 60 targets and exited successfully; log `/tmp/boost-frontier-v2-suite-003-alignof-layout-fix-full.log`. | The initial forced survey passed 56 targets and had one causal compile failure plus three downstream skips: libc++ `alignment_of<Struct<bool>>` could not instantiate `integral_constant<size_t, alignof(_Tp)>`; log `/tmp/boost-frontier-v2-suite-003-initial-forced.log`. The final direct-LowIR report passes `3831 / 3831`; log `/tmp/cppgm-boost-frontier-v2-align-final-test-report.log`. |
| 4 | `libs/any/test` | pass | `(qualified rvalue-reference parser fix)` | Forced focused `unique_move` and `no_rtti_unique_move` rebuilt eight targets and both passed compile, link, and runtime; log `/tmp/boost-v2-any-unique-move-parser-fix.log`. The exact complete forced survey updated 164 targets in 54.3s and exited successfully; log `/tmp/boost-frontier-v2-suite-004-qualified-rref-parser-fix/libs__any__test.log`. | Initial forced evidence `/tmp/boost-frontier-v2-suite-004-initial-forced.log` had eight failures from the separately committed missing-`typename` defect plus the later move-cast failure. The first fix cleared all no-RTTI failures; the final parser fix clears both remaining move targets. Final direct-LowIR report passes `3833 / 3833`; log `/tmp/cppgm-boost-v2-suite-004-qualified-rref-full-report.log`. |
| 5 | `libs/array/test` | pass | `(three Array fixes)` | Focused constexpr aggregate access passed in `/tmp/boost-v2-array-init-cx-focused-final.log`; focused pointer-identity targets passed in `/tmp/boost-v2-array-pointer-focused-final.log`; focused `to_array_test` passed in `/tmp/boost-v2-array-to-array-fixed.log`. The exact complete forced survey rebuilt all 146 discovered targets in 56.7s and exited successfully; log `/tmp/boost-frontier-v2-suite-005-array-cv-reference-fix/libs__array__test.log`. | Initial forced evidence `/tmp/boost-frontier-v2-suite-005-initial-forced/libs__array__test.log` had six constexpr failures split between structured aggregate access and subobject pointer identity, plus one independent array cv-reference overload failure. Each cause has an earliest owner reducer and uncached proof. Final direct-LowIR report passes `3836 / 3836`; log `/tmp/cppgm-boost-frontier-v2-array-final-full-report.log`. |
| 6 | `libs/asio/test` | pass | `53a73a395` | The exact eight-job forced survey rebuilt through 210 passing tests before the 1800-second external timeout, with no failures; log `/tmp/boost-frontier-v2-asio-after-strand-full-j8.log`. All eight compiler children left in flight at the timeout (`read`, `read_at`, `socket_base`, and `spawn`, normal/select) were then force-rebuilt; all eight compile, link, and run successfully in `/tmp/boost-frontier-v2-asio-after-strand-interrupted-tail-focused.log`. The non-forced full continuation updated 680 targets, ran 285 passing tests, and exited successfully; log `/tmp/boost-frontier-v2-asio-after-strand-continuation.log`. | Suite closed with no skipped or failed targets. The `strand` failure was not a missing `on_invoker_exit` definition: those destructors were emitted and referenced the missing concrete `basic_executor_type<recycling_allocator<void, default_tag>, 4>::~basic_executor_type`. The final direct-LowIR report passes `3833/3833` with PA9 explicitly omitted. The large tail `execution/mapping` compile peaked near 3 GiB and sampled primarily in LowIR hidden virtual-base argument emission; this is recorded as a separate performance lead, not attributed to the cold temporary-class lookup. |
| 7 | `libs/assert/test` | pass | `53a73a395` | The exact forced survey rebuilt 72 targets; all 18 tests compiled, linked, ran, and passed, and B2 exited successfully. Log: `/tmp/boost-frontier-v2-suite-007-assert-full.log`. | No compiler fix or repository regression was required. |
| 8 | `libs/assign/test` | pass | `(nested structured template-id fix)` | The initial exact forced survey passed the other discovered targets and isolated three compile failures: `array`, `list_of`, and `multi_index_container`; log `/tmp/boost-frontier-v2-suite-008-assign-initial-forced.log`. The conversion-template fix cleared `array` and `list_of`; the final exact forced survey rebuilt 84 targets, ran all 14 tests, and exited successfully, including `multi_index_container`; log `/tmp/boost-frontier-v2-suite-008-assign-final-forced.log`. | Both independent causes are closed. The final repository direct-LowIR report passes `3836/3836` with PA9 explicitly omitted as requested. |
| 9 | `libs/atomic/test` | pass | `(Atomic typed lowering fixes)` | The initial forced survey exposed an incomplete `__is_trivially_copyable` class trait, zero-initialized scoped-enum globals, and unsupported x86 GNU asm atomic operations; log `/tmp/boost-frontier-v2-suite-009-atomic-after-trait-enum.log`. Focused API, reference, IPC, and wait targets passed after the fixes. The final exact forced suite rebuilt all 90 requested targets and passed every positive and expected-failure test; log `/tmp/boost-frontier-v2-suite-009-atomic-final.log`. | GNU asm operand expressions are parsed from the original token range during the initial parse and retained as typed AST/CallSem nodes; lowering classifies only exact supported instruction templates and never reparses operand text. The final direct-LowIR report passes `3840/3840` with PA9 explicitly omitted. |
| 10 | `libs/beast/test` | pass | `(inline nested-owner template-prefix fix)` | The final exact forced survey finds 7384 targets, updates all 447 requested targets, records 103 passing test actions with no failed actions or downstream skips, and exits successfully in 1228.8s; log `/tmp/boost-frontier-v2-suite-010-inline-nested-owner-final-full-forced-r2/libs__beast__test.log`. The previously failing `tcp_stream` and `basic_stream` objects both compile, link, run, and pass in that full run. | All independently observed Beast frontiers are closed on the final compiler. The final direct-LowIR report passes `3893/3893`, including PA37 object roundtrip `7/7`, with PA9 explicitly omitted as requested. |
| 11 | `libs/bimap/test` | pass | `(typed overload-candidate refresh fix)` | The final exact eight-job forced survey finds 6965 targets, updates all 205 requested targets, and records 40 passing test outcomes, including all six deliberate compile failures as failed-as-expected. Xpressive compiles, links, runs, and passes inside that concurrent full graph; log `/tmp/boost-frontier-v2-suite-011-candidate-refresh-final-full-forced.log`. The preceding focused forced Xpressive replay also passes compile, link, and runtime; log `/tmp/boost-frontier-v2-suite-011-candidate-refresh-xpressive-forced.log`. | The earlier no-diagnostic Xpressive miss was a stale `FunctionBinding *` use after class completion, not an external memory-pressure kill. The PA22 liveness reducer deterministically records one refresh attempt and one successful typed reacquisition normally and with all semantic caches disabled; both emit byte-identical LowIR. The final PA9-excluded direct report passes `3898/3898`, including PA37 object roundtrip `7/7`; strict, placement, and all 23 text-reparse categories remain clean. |
| 12 | `libs/bind/test` | pass | `38b19fbd6` + `(concrete member-alias identity fix)` | The initial exact eight-job forced survey finds 1207 targets, updates 291 of 303 requested targets, records 73 passing test outcomes and the deliberate `arg_copy_fail` as failed-as-expected, then exits with four unexpected failures and eight downstream skips; log `/tmp/boost-frontier-v2-suite-012-initial-forced.log`. After the function-shadowing fix, the exact full forced replay updates 295 targets and leaves only the two `apply_rv` compile failures; log `/tmp/boost-frontier-v2-suite-012-function-shadow-full-forced.log`. The final exact forced graph finds 1207 targets, updates all 303 requested targets, records 77 passing test actions including the deliberate compile failure, and exits successfully; log `/tmp/boost-frontier-v2-suite-012-typed-identity-final-full-forced.log`. | The two runtime targets were one latent value-shadowing defect. The two `apply_rv` targets were a second latent baseline defect: a concrete class argument `::F` already carried through `typedef R type` was rebound by spelling to the neighboring template parameter `F`, producing `F (*)()` instead of the callable class result. Both causes have typed owner regressions and uncached proof; the final PA9-excluded direct report passes `3900/3900`. |
| 13 | `libs/bloom/test` | pass | `466dd3c77` + `(typed pack-element result fix)` | The initial exact eight-job forced survey finds 365 targets, requests 38 updates, records one passing action, nine compile failures, and 25 downstream skips; all nine failures report the same inability to construct the implicit constructor symbol for Bloom's local anonymous filter class; log `/tmp/boost-frontier-v2-suite-013-initial-forced.log`. After the typed ABI fix, the exact full forced replay updates 34 targets and records nine passing tests, one independent compile failure, and three downstream skips; log `/tmp/boost-frontier-v2-suite-013-unnamed-local-full-forced.log`. Focused `test_capacity` passes in `/tmp/boost-frontier-v2-suite-013-unnamed-local-focused-capacity.log`. The final focused `test_construction` replay passes in `/tmp/boost-frontier-v2-suite-013-pack-result-focused-construction.log`, and the exact final eight-job forced graph updates all 38 requested targets with every test passing in `/tmp/boost-frontier-v2-suite-013-pack-result-full-forced.log`. | Both independent causes are closed. Unnamed function-local classes use Clang-matching Itanium `Ut_`, `Ut0_`, ... entities. The apparent `std::get<B ? 0 : 1>` failure was downstream: `std::forward_as_tuple(f, g)` deduced both local-lambda arguments, but general `T&&...` result expansion rewrote only retained syntax/text while leaving both semantic argument types as the source `T&&`. Per-element substitution now carries each exact `TypePtr` through class-template materialization. The final PA9-excluded direct report passes `3902/3902`, including PA37 object roundtrip `7/7`; strict, placement, reparse, cache-independence, and fixed/rolling performance gates pass. |
| 14 | `libs/callable_traits/test` | pass | `733e3f182` intake + `0a04194b6` + `6ac60d65e` + `f70bdfadd` + `9290640fa` + `3433e1bcc` + `(member-pointer ref-qualifier fix)` | The initial exact eight-job forced survey finds 1348 targets, updates 994, fails 34 compile actions, and skips 102 downstream targets; log `/tmp/boost-frontier-v2-suite-014-initial-forced.log`. The failures are 17 eager/lazy test pairs. The packed-owner, qualified member-address, and structured member-pointer-argument replays reduce that frontier to four eager/lazy pairs; logs `/tmp/boost-frontier-v2-suite-014-member-pointer-owner-full-forced.log`, `/tmp/boost-frontier-v2-suite-014-member-address-qualifier-full-forced.log`, and `/tmp/boost-frontier-v2-suite-014-apply-member-pointer-full-forced.log`. The lambda-identity replay leaves only eager/lazy `is_invocable` and `is_invocable_r`; log `/tmp/boost-frontier-v2-suite-014-captureless-lambda-identity-full-forced.log`. Focused invocation replays advance through the class-conversion and ref-qualifier causes, with the final eager targets passing in `/tmp/boost-frontier-v2-suite-014-invocable-ref-qualifier-focused-final.log`. The exact final eight-job graph finds 1348 targets, requests and updates all 376 remaining targets, and exits successfully after the shared-rule refactor; log `/tmp/boost-frontier-v2-suite-014-invocable-final-shared-helper-full-forced.log`. | All 17 eager/lazy pairs are closed. The final two causes were generic constexpr class-to-scalar conversion and the C++11 object-category rules for invoking ref-qualified member-function pointers through `.*`/`->*`. The normal semantic call path and the structured invocation helper now consume the same typed function ref-qualifier and object value category through one shared rule. The final PA9-excluded direct report passes `3907/3907`, including PA37 object roundtrip `7/7`; strict, placement, reparse, cache-independence, and fixed/rolling performance gates pass. |
| 15 | `libs/charconv/test` | pass | `149cff9a3` + `(concrete structured qualifier constexpr fix)` | The initial exact eight-job forced survey finds 730 targets, requests 150 updates, updates 39, fails one compile action, and skips 110 downstream targets; `to_chars.o` cannot evaluate inherited `policy_holder::return_has_sign` as a non-type template argument. Log: `/tmp/boost-frontier-v2-suite-015-initial-forced.log`. After preserving exact base-pack element types, the exact forced replay compiles and archives Charconv, updates 105 targets, and advances to 15 link failures with 30 downstream skips. Log: `/tmp/boost-frontier-v2-suite-015-typed-base-pack-full-forced.log`. The focused `to_chars_float` replay then compiles, links, runs, and passes in `/tmp/boost-frontier-v2-suite-015-concrete-qualifier-focused-to-chars-float.log`. The exact final eight-job forced graph updates all 150 requested targets and exits successfully in `/tmp/boost-frontier-v2-suite-015-concrete-qualifier-full-forced.log`. | Both independent causes are closed. Direct `Policies...` base expansion now preserves exact bound type identities. Structured qualified value lookup now folds a static constexpr member only when its retained qualifier resolves to a concrete non-dependent type, eliminating the erroneous undefined symbols for `nearest_to_even::tag`, `uint_with_known_number_of_digits<...>::digits`, and `uconst<...>::value`. The final PA9-excluded direct report passes `3909/3909`, including PA37 `7/7`; strict, placement, reparse, cache-independence, GCC 15, and fixed/rolling performance gates pass. |
| 16 | `libs/chrono/test` | pass | `(defaulted non-type declaring-scope fix)` + `(overloaded unary constexpr fallback fix)` + `(nested catch active-cleanup fix)` + `(dependent result active-scope fix)` | The initial exact forced survey finds 2997 targets, requests 420 updates, updates 304, fails 32 actions, and skips 84 downstream targets; log `/tmp/boost-frontier-v2-suite-016-initial-forced.log`. The first three fixes advance through 30, 25, and 6 failures in `/tmp/boost-frontier-v2-suite-016-after-default-scope-forced.log`, `/tmp/boost-frontier-v2-suite-016-after-unary-constexpr-forced.log`, and `/tmp/boost-frontier-v2-suite-016-after-active-catch-forced.log`. The focused `v1_io_ex1_s` target then passes compile, link, and runtime. The exact final forced graph finds 2997 targets, updates all 420 requested targets, and exits successfully with no failed actions or downstream skips; log `/tmp/boost-frontier-v2-suite-016-dependent-result-scope-final-forced.log`. | All four independent causes are closed. The final link family was not an ABI spelling defect: the out-of-class `ratio_string<Ratio, Char>::prefix()` result retained direct owner parameter `Char` after its metadata resolver discarded the active owner binding scope, so the definition did not attach to the concrete declaration. Direct carried owner parameters now use the active typed scope; compound-only metadata stays conservative. The final PA9-excluded direct report passes `3913/3913`, including PA37 `7/7`; strict, placement, reparse, cache-independence, GCC 15, and fixed/rolling performance gates pass. |
| 17 | `libs/circular_buffer/test` | pass | `7836d6a18` | The exact four-job forced graph finds 1540 targets, updates all 34 requested targets, rebuilds and passes all seven test actions, and exits successfully; log `/tmp/boost-frontier-v2-suite-017-initial-forced.log`. | The current compiler passes `constant_erase_test`, `soft_iterator_invalidation`, both base variants, both space-optimized variants, and `bounded_buffer_comparison` without a repository change. |
| 18 | `libs/compat/test` | pass | `(Compat final closure)` | The initial exact four-job forced graph finds 941 targets, requests 664 updates, updates 614, fails 20 actions, and skips 30 downstream targets. The member-data, function-designator, member-pointer, implicit-object ranking, contained-object virtual-base, structured member-pointer NTTP, initializer-list, global placement-new, local-class ABI, alias identity, function-designator trait, and cv-aware transfer repairs close the graph in sequence. The exact minimal final replay without the discarded consteval guard finds 941 targets, rebuilds all 220 requested targets, and exits successfully; log `/tmp/boost-frontier-v2-suite-018-without-consteval-guard-full-forced.log`. | Every Compat target passes, including `move_only_function_test` and `to_array_rvalue_test`. The PA9-excluded direct report passes `3928/3928`, including PA37 object roundtrip `7/7`; strict, placement, all 23 zero-reparse categories, twelve cache configurations, Clang/GCC warning gates, and candidate-only performance pass. |
| 19 | `libs/compute/test` | pass | `cef8c677f` | The exact forced four-job graph performs its configuration checks, reports both OpenCL variants unavailable, finds the two bookkeeping targets, and exits successfully; log `/tmp/boost-frontier-v2-suite-019-initial-forced.log`. An immediate exact rerun independently records exit status 0. | This matches the inventory and prior-frontier behavior for this host configuration. No compiler or repository change was required. |
| 20 | `libs/concept_check/test` | pass | `(function-valued NTTP semantic validation fix)` | The initial forced graph exposed seven deliberate compile-fail targets that incorrectly succeeded because their selected function-address non-type template arguments did not trigger body validation. The final exact four-job forced graph finds 428 targets, updates all 44 requested targets, passes every positive test, and reports every negative test as failed-as-expected; log `/tmp/boost-frontier-v2-suite-020-final-forced.log`. | The PA21/PA23/PA26 affected report passes `710/710`; all configured strict suites, twelve cache configurations, Clang/GCC warning checks, PA23 placement, all 23 zero-reparse categories, and all 23 audit unit tests pass. The PA9-excluded broad report accounts for all `3929/3929` tests after the sole load-sensitive PA3 timeout passes `19/19` in isolation, including PA37 `7/7`. |
| 21 | `libs/config/test` | pass | `(constexpr reference storage identity fix)` | The initial exact four-job forced graph finds 1391 targets, updates 112, and exposes four compile failures with twelve downstream skips. All four failures share `boost_no_constexpr.ipp`: the address of a named static object passed through a constexpr reference parameter could not initialize a constexpr pointer. The final exact graph rebuilds all 128 requested targets, passes every positive test and deliberate compile failure, and exits successfully; log `/tmp/boost-frontier-v2-suite-021-final-forced.log`. | PA20 passes `79/79`; all configured strict suites, thirteen positive and thirteen negative cache configurations, Clang/GCC warning checks, PA20 placement, all 23 zero-reparse categories, and all 23 audit unit tests pass. The PA9-excluded broad report passes `3931/3931`, including PA37 `7/7`. |
| 22 | `libs/container/test` | pass | `(Container final closure)` | The initial exact forced graph finds 1909 targets, requests 325 updates, updates 293, fails eight compile actions, and skips 24 downstream targets; log `/tmp/boost-frontier-v2-suite-022-initial-forced.log`. The alias, recursive-layout, namespace-scope, owner-pack, default-coverage, and reference-list-constructor fixes successively leave only `devector_test`. Its invalid free disappears after defaulted non-type arguments are rematerialized from their declaring template's substituted prefix. That exposes 41 live `regular_elem` objects from initializer-list backing arrays; the focused target passes after those elements enter the existing cleanup stack with named-list lifetime extension. The exact final four-job graph finds 1909 targets, updates all 325 requested targets, passes `devector_test` and every other test action, and exits successfully; log `/tmp/boost-frontier-v2-suite-022-default-owner-initlist-final-full-forced.log`. | Suite closed with no failed actions or downstream skips. PA22 passes `273/273`, PA25 passes `74/74`, all configured strict suites pass, and the final PA9-excluded direct report passes `3940/3940`, including PA37 `7/7`. |
| 23 | `libs/container_hash/test` | pass | `(callable-object ranking and dependent result rebinding fixes)` | The initial exact four-job forced graph finds 3199 targets, requests 357 updates, and reaches four runtime failures with no compile failures: `hash_deque_test`, `hash_list_test`, `hash_set_test`, and `is_range_test4`; log `/tmp/boost-frontier-v2-suite-023-initial-forced.log`. Typed callable-object candidate ranking closes the three sequence-container hash failures. Typed source-pattern rebinding for dependent non-type result arguments closes `is_range_test4`. The exact final forced graph finds 3199 targets, updates all 341 then-outdated targets, passes every test and example action, and exits successfully; log `/tmp/boost-frontier-v2-suite-023-final-forced.log`. | Suite closed with no failed actions or downstream skips. PA22 passes `275/275`; all configured strict suites, placement/hygiene, all 23 zero-reparse categories, all 14 audit tests, eleven cache-disabled configurations for both reducers, Clang/GCC controls, and GCC 15 production builds pass. The final PA9-excluded direct report passes `3942/3942`, including PA37 `7/7`. |
| 24 | `libs/context/test` | pass | `(no compiler change)` | The exact four-job forced graph performs the host configuration checks, finds the single bookkeeping target, and exits successfully; log `/tmp/boost-frontier-v2-suite-024-initial-forced.log`. | Configuration-only pass on this host. No compiler change, regression update, or performance measurement was required. |
| 25 | `libs/contract/test` | pass | `(Contract typed semantic and lowering closure)` | The initial exact four-job forced graph exposed independent compile, link, and runtime failures; log `/tmp/boost-frontier-v2-suite-025-initial-forced.log`. The exact final forced graph runs 214 tests, updates all 877 requested targets, and exits successfully; log `/tmp/boost-frontier-v2-suite-025-full-final-candidate.log`. After the final cleanup refactor, forced rechecks of `public_function-throwing_body`, `throwing_body_virtual`, `virtual_sparse`, `virtual_access_multi`, and `max_bases` all pass. | Suite closed with no failed action or downstream skip. The affected direct report passes `1365/1365`; all configured strict suites pass; the final PA9-excluded direct report passes `3955/3955`, including PA37 `7/7`. Placement/hygiene, all 23 zero-reparse categories, all 23 audit tests, eleven cache modes across six reducers, host controls, and GCC 15 production builds pass. Candidate-only frozen compiler-and-51-header performance passes at +0.52% instructions, -1.38% RSS, and -2.43% footprint; the threshold sample finds no changed helper in a hotspot. |
| 26 | `libs/conversion/test` | pass | `(no compiler change)` | The exact four-job forced graph finds 364 targets, updates all 18 requested targets, reports `implicit_cast_fail` and `implicit_cast_fail2` as failed-as-expected, passes all three positive compile/link/runtime tests, and exits successfully; log `/tmp/boost-frontier-v2-suite-026-initial-forced.log`. | The historical mixed inventory is clean on the current compiler. No compiler change, regression update, or performance measurement was required. |
| 27 | `libs/convert/test` | pass | `(no compiler change)` | The exact four-job forced graph completes its feature and dependency configuration checks, finds the single bookkeeping target, and exits successfully; log `/tmp/boost-frontier-v2-suite-027-initial-forced.log`. | Configuration-only pass on this host. No compiler change, regression update, or performance measurement was required. |
| 28 | `libs/core/test` | pass | `(Core typed constexpr and access closure)` | The initial exact four-job forced graph exposed independent constexpr address/reference, deleted-constructor, inheritance-access, and deferred class-scope assertion failures. The exact final forced graph finds 6233 targets, updates all 1137 requested targets, reports every deliberate compile failure as failed-as-expected, passes every positive compile/link/runtime action, and exits successfully; log `/tmp/boost-frontier-v2-suite-028-final-forced-r4.log`. | Suite closed with no unexpected failure or downstream skip. The affected PA15, PA16, PA19, and PA20 direct report passes `606/606`; all configured strict suites pass; the PA9-excluded broad report passes `3963/3963`, including PA37 `7/7`. Placement/hygiene, all 23 zero-reparse categories, all 14 audit tests, twelve cache modes, Clang/GCC warning gates, and candidate-only frozen compiler-and-51-header performance pass. |
| 29 | `libs/coroutine/test` | pass | `(explicit-specialization member-template owner fix)` | The initial exact four-job forced graph finds 3227 targets, updates 66, and exposes one compile failure in `test_asymmetric_coroutine`, with three downstream skips; log `/tmp/boost-frontier-v2-suite-029-initial-forced.log`. The exact final forced graph updates all 70 requested targets, passes both asymmetric and symmetric compile/link/runtime actions, and exits successfully; log `/tmp/boost-frontier-v2-suite-029-explicit-owner-full-forced-r1.log`. | Suite closed with no failed action or downstream skip. The PA21/PA22 owner report passes `513/513`; all configured strict suites pass; the PA9-excluded broad report passes `3965/3965`, including PA37 `7/7`. Placement/hygiene, all 23 zero-reparse categories, all 14 audit tests, twelve cache modes across both reducers, Clang/GCC warning gates, and candidate-only frozen compiler-and-51-header performance pass. |
| 30 | `libs/crc/test` | pass | `(dependent NTTP declarator preservation fix)` | The initial exact forced graph finds 752 targets, updates 18 of 26, and exposes two compile failures in `crc_test` and `crc_test2`, with six downstream skips; log `/tmp/boost-frontier-v2-suite-030-initial-forced.log`. The exact final forced graph finds 752 targets, updates all 26 requested targets, passes all six compile/link/runtime actions, and exits successfully; log `/tmp/boost-frontier-v2-suite-030-declarator-id-final-forced.log`. | Suite closed with no failed action or downstream skip. PA19 passes `143/143`; all configured strict suites pass; the PA9-excluded broad report passes `3966/3966`, including PA37 `7/7`. Placement/hygiene, all 23 zero-reparse categories, all 14 audit tests, twelve byte-identical cache modes, Clang/GCC warning gates, and candidate-only frozen compiler-and-51-header performance pass. |
| 31 | `libs/date_time/test` | pass | `(no compiler change)` | The exact four-job forced graph performs the wide-stream configuration check, finds 2323 targets, updates all 272 requested targets, passes 59 positive compile/link/runtime actions, reports all four deliberate special-value compile failures as failed-as-expected, and exits successfully; log `/tmp/boost-frontier-v2-suite-031-initial-forced.log`. | The current compiler passes the full calendar, duration, parsing, facet, serialization, clock, and local-time graph. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 32 | `libs/decimal/test` | pass | `(no compiler change)` | The exact four-job forced graph reports both C++14 `decltype(auto)` configurations unavailable in the C++11 lane, finds the single bookkeeping target, and exits successfully; log `/tmp/boost-frontier-v2-suite-032-initial-forced.log`. | Configuration-only pass on this host and language mode. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 33 | `libs/describe/test` | pass | `(no compiler change)` | The exact four-job forced graph reports both C++14 Describe configurations unavailable in the C++11 lane, finds 856 targets, updates all 182 requested targets, passes 49 positive compile/link/runtime actions, reports all four deliberate C++14-only compile failures as failed-as-expected, and exits successfully; log `/tmp/boost-frontier-v2-suite-033-initial-forced.log`. | The current compiler passes the complete C++11 preprocessor, enum, member, base, overload, union, and pedantic Describe graph. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 34 | `libs/detail/test` | pass | `(no compiler change)` | The exact four-job forced graph passes both scoped-enum configuration checks, finds 544 targets, updates all 48 requested targets, passes all 12 compile/link/runtime actions, and exits successfully; log `/tmp/boost-frontier-v2-suite-034-initial-forced.log`. | The current compiler passes the full Detail utility, numeric, codecvt, allocator, and container-forwarding graph. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 35 | `libs/dll/test` | pass | `(DLL typed semantic, ABI, and object closure)` | The initial exact four-job graph finds 2940 targets, requests 456 updates, updates 328, fails 41 actions, and skips 87 downstream targets; log `/tmp/boost-frontier-v2-suite-035-initial-forced.log`. The final authoritative lane uses `LINK=shared` and `define=BOOST_TRAVISCI_BUILD`: the former avoids contradictory static/dynamic library macros, while the latter serializes an upstream macOS concurrent-`dlopen` race also reproduced with host Clang. The exact final forced graph finds 2925 targets, updates all 192 requested targets, passes 31 test actions, reports both deliberate compile failures as failed-as-expected, and exits successfully; log `/tmp/boost-frontier-v2-suite-035-final-scoped-lookup-forced.log`. | Fifteen independent typed compiler defects are closed with owner regressions spanning PA12, PA14-PA16, PA21-PA22, PA30, PA32, and PA34. The final PA9-excluded report passes `3981/3981`, including PA37 `7/7`; configured strict suites, placement/hygiene, all 23 zero-reparse categories, all 22 audit/perf unit tests, twelve cache configurations, host controls, and isolated GCC 15 production builds pass. Candidate-only frozen performance passes; no parent or live header was measured. |
| 36 | `libs/dynamic_bitset/test` | pass | `(Dynamic Bitset deferred member-definition replay fix)` | The initial exact four-job forced graph finds 3137 targets, requests 130 updates, updates 115, fails five link actions, and skips ten downstream targets; all five links lack deferred nested or injected-owner member definitions. Log `/tmp/boost-frontier-v2-suite-036-initial-forced.log`. The exact final forced graph finds 3137 targets, updates all 90 requested targets, passes nine positive compile/link/runtime actions, reports `test_std_hash_disabled` as failed-as-expected and passed, and exits successfully; log `/tmp/boost-frontier-v2-suite-036-final-forced.log`. | The PA18 owner regression retains and emits a nested defaulted copy constructor, a nested conversion operator, and `Iter<T>::Iter::operator++`. PA18 passes `235/235`; configured strict suites, template hygiene, all 23 zero-reparse categories, all 22 audit/perf unit tests, twelve byte-identical cache modes, Clang/GCC host controls, GCC 15 warning checks, and the PA9-excluded `3982/3982` report pass. Candidate-only frozen performance passes; no parent or live header was measured. |
| 37 | `libs/endian/test` | pass | `(no compiler change)` | The exact four-job forced graph finds 793 targets, updates all 184 requested targets, passes all 47 compile/link/runtime actions, and exits successfully; log `/tmp/boost-frontier-v2-suite-037-initial-forced.log`. | The current compiler passes the complete intrinsic/non-intrinsic Endian graph, including buffer, arithmetic, load/store, conversion, constexpr reverse, packed, data, and convenience tests. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 38 | `libs/exception/test` | pass | `(Exception typed semantic and lazy conversion closure)` | The initial exact four-job forced graph finds 4583 targets, requests 249 updates, updates 232, fails six targets, and skips eleven; the three causal compile failures are Boost.JSON, `boost_json_test`, and `nlohmann_json_test`, while all four deliberate compile failures are handled as failed-as-expected. Log `/tmp/boost-frontier-v2-suite-038-initial-forced.log`. The final exact graph finds the same 4583 targets, updates all 217 requested targets, records 54 passing test actions, handles all four deliberate failures as failed-as-expected, and exits successfully in 782.16s; log `/tmp/boost-frontier-v2-suite-038-final-forced-current.log`. The independently forced nlohmann target also compiles, links, and runs in 678.91s; log `/tmp/boost-frontier-v2-suite-038-nlohmann-final-clang-lane.log`. | Seven earliest-owner reducers cover six coherent typed fixes across PA14, PA22, PA26, and PA34. The owner report passes `760/760`; all configured strict suites pass; the PA9-excluded broad report passes `3989/3989`, including PA37 `7/7`. Placement/hygiene, all 23 zero-reparse categories, all 22 audit/perf unit tests, twelve byte-identical cache modes for every reducer, and an isolated GCC 15 production build pass. Final candidate-only frozen performance passes at -1.44% instructions, +1.76% RSS, and +2.19% footprint; no parent or live header was measured. |
| 39 | `libs/fiber/test` | pass | `(no compiler change)` | The exact four-job forced graph completes architecture, C++11, threading, ICU, atomic, and host capability checks, finds the single bookkeeping target, and exits successfully in 22.91s; log `/tmp/boost-frontier-v2-suite-039-initial-forced.log`. | Configuration-only pass on this host. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 40 | `libs/filesystem/test` | pass | `(Filesystem typed lookup and range closure)` | The initial exact forced graph finds 3521 targets, requests 420 updates, updates 384, fails nine compile actions, and skips 27 downstream targets; log `/tmp/boost-frontier-v2-suite-040-initial-forced.log`. The final exact graph finds the same 3521 targets, updates all 168 requested targets, handles the deliberate `cf_path_nullptr_test` failure as failed-as-expected, and exits successfully; log `/tmp/boost-frontier-v2-suite-040-final-forced.log`. | Six earliest-owner reducers cover qualified overload-set arguments, source-point-aware ADL imports, range-for lookup and copy deduction, and return-SFINAE template identity across PA12, PA15, PA22, and PA24. The PA9-excluded broad report passes `3995/3995`, including PA37 `7/7`; configured strict suites, placement/hygiene, all 23 zero-reparse categories, all 14 audit tests, twelve cache modes per reducer, Clang/GCC controls, and warning-clean production builds pass. Candidate-only frozen performance passes at -1.30% instructions, +1.50% RSS, and +2.24% footprint; no parent or live header was measured. |
| 41 | `libs/flyweight/test` | pass | `(Flyweight overload and constexpr termination fixes)` | The initial exact four-job graph finds 3307 targets, requests 111 updates, fails three compile actions, and skips their downstream targets; log `/tmp/boost-frontier-v2-suite-041-initial-forced.log`. The exact final four-job graph finds the same 3307 targets, updates all 111 requested targets, records 11 passing test actions, and exits successfully in 191s; log `/tmp/boost-frontier-v2-suite-041-final-forced.log`. | Two earliest-owner header-free reducers cover initializer-list element conversion ranking and no-progress recursion during typed initializer constant evaluation. PA20/PA25 pass `161/161`; configured strict suites, placement/hygiene, all 23 zero-reparse categories, all 22 audit/perf unit tests, twelve byte-identical cache modes per reducer, Clang/GCC controls, GCC 15 warning checks, and the PA9-excluded `3997/3997` report pass. Candidate-only frozen performance passes at -1.46% instructions, +1.39% RSS, and +2.18% footprint; no parent or live header was measured. |
| 42 | `libs/foreach/test` | pass | `(no compiler change)` | The exact four-job forced graph finds 875 targets, updates all 98 requested targets, records 25 passing compile/link/runtime actions, and exits successfully in 55s; log `/tmp/boost-frontier-v2-suite-042-initial-forced.log`. | The current compiler passes the complete STL, array, C-string, pair, user-defined, call-once, rvalue, dependent-type, miscellaneous, and noncopyable Foreach graph. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 43 | `libs/format/test` | pass | `(no compiler change)` | The exact four-job forced graph reports ICU unavailable for this lane, finds 1360 targets, updates all 43 requested targets, records nine passing compile/link/runtime actions, and exits successfully in 46s; log `/tmp/boost-frontier-v2-suite-043-initial-forced.log`. | The current compiler passes all ordinary, Windows-header, enum, exception, and wide-string Format tests available in the C++11 configuration. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 44 | `libs/function/test` | pass | `(implicit-static class allocation function fix)` | The initial exact four-job forced graph finds 1350 targets, requests 214 updates, fails `stateless_test` compilation, and skips its three downstream targets; log `/tmp/boost-frontier-v2-suite-044-initial-forced.log`. The exact final forced graph finds the same 1350 targets, updates all 214 requested targets, passes every positive compile/link/runtime action, handles both deliberate compile failures as failed-as-expected, and exits successfully; log `/tmp/boost-frontier-v2-suite-044-final-forced.log`. | Class-scope `operator new`, `operator new[]`, `operator delete`, and `operator delete[]` are now classified as static even when the redundant `static` specifier is omitted. PA16 passes `172/172`; configured strict suites, placement/hygiene, all 23 zero-reparse categories, all 22 audit/perf unit tests, twelve byte-identical cache modes, Clang/GCC controls, GCC 15 warning checks, and the PA9-excluded `3997/3997` report pass. Candidate-only frozen performance passes at -1.26% instructions, +1.71% RSS, and +2.23% footprint; no parent or live header was measured. |
| 45 | `libs/function_types/test` | pass | `(no compiler change)` | The exact four-job forced graph finds 2580 targets, updates all 89 requested targets, records 43 passing test actions, handles all three negative decomposition tests as failed-as-expected, and exits successfully; log `/tmp/boost-frontier-v2-suite-045-initial-forced.log`. | The current compiler passes the full property, decomposition, synthesis, calling-convention, preprocessing-mode, and example graph. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 46 | `libs/functional/test` | pass | `(no compiler change)` | The exact four-job forced graph finds 174 targets, updates all four requested targets, compiles, links, and runs `function_test`, and exits successfully; log `/tmp/boost-frontier-v2-suite-046-initial-forced.log`. | The current compiler passes the complete Functional graph. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 47 | `libs/fusion/test` | pass | `(qualified value-pack and concrete dependency fixes)` | The initial exact graph finds 6952 targets and exposes both a local Boost metadata collision and the causal `fold` compile failure; log `/tmp/boost-frontier-v2-suite-047-initial-forced.log`. After correcting the external fixtures and both compiler defects, the authoritative exact two-job forced graph finds 6963 targets, updates all 844 requested targets, records 220 passing test actions, and exits successfully; log `/tmp/boost-frontier-v2-suite-047-forced-closure-final.log`. | The external Boost 1.91 tree had an obsolete unconditional `make_pair_r-value` compile-fail target alongside the current guarded positive target, plus a malformed local source fixture that also failed under host Clang. The Jamfile now has only the guarded positive target (current SHA-256 `86458665c2c3b574df0321bd0960b205a1c3d0d85fd7f47e70db3d37339c6110`), and `support/make_pair_r-value.cpp` was synchronized from malformed SHA-256 `64a644954dacd1eaabd195238f5c7ba0bc562a3475827111b4aac14bf36ae8c4` to upstream SHA-256 `00e4c6ba0605a7bff778f5aaa0f63b32e321dd98435a060208b3f019ca4fed3f`; its Clang and cppgm controls pass. Two PA22 reducers cover qualified static-value pack expansion and qualified dependent-result SFINAE. PA22 passes `286/286`; configured strict suites, placement/hygiene, all 23 zero-reparse categories, all 22 audit/perf unit tests, twelve byte-identical cache modes per reducer, Clang/GCC controls, warning-clean production units, and the PA9-excluded `3999/3999` report pass. Candidate-only frozen performance passes at -1.49% instructions, +0.84% RSS, and +2.23% footprint; no parent or live header was measured. |
| 48 | `libs/geometry/test` | pass | `(no compiler change)` | The exact four-job forced graph completes its host and C++14 constexpr configuration checks, finds the single bookkeeping target, and exits successfully; log `/tmp/boost-frontier-v2-suite-048-initial-forced.log`. | Configuration-only pass in the C++11 lane because both Geometry C++14 constexpr configurations are unavailable. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 49 | `libs/gil/test` | pass | `(external Jamfile correction; no compiler change)` | The initial exact graph reports `No best alternative` for `core/io/path_spec` because the Boost 1.91 Jamfile declares both compile-only and run actions with the same implicit target name; log `/tmp/boost-frontier-v2-suite-049-initial-forced.log`. After removing the redundant compile-only action, the exact four-job forced graph completes its C++11/C++14 configuration checks, finds the single bookkeeping target, and exits successfully; log `/tmp/boost-frontier-v2-suite-049-corrected-forced.log`. | The duplicate target is present in both the authoritative Boost 1.91 tag and current GIL `develop`, so this is an upstream setup defect rather than local drift or a compiler failure. The retained run action subsumes compilation and is the stronger test. External Jamfile SHA-256 changed from `778b6ec39d56880e48dd0239173828b2cd366dc1de19b284431e584dba06a55e` to `6cd3346ead45b2f9fb4ef6175d680049de6a2f8f6ed4c6b7c0eedea6ce88d97f`. The corrected C++11 lane is configuration-only because C++14 constexpr is unavailable. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 50 | `libs/graph/test` | pass | `(no compiler change)` | The exact four-job forced graph finds 12539 targets and requests 1035 updates. Its 30-minute wrapper deadline expires after 163 explicit passes with no failure, skip, or error marker; log `/tmp/boost-frontier-v2-suite-050-initial-forced.log`. After terminating the four compiler children orphaned by that external deadline, an exact continuation updates the remaining 18 targets, records six further passes, and exits successfully; log `/tmp/boost-frontier-v2-suite-050-continuation.log`. A final exact current-state rerun finds the complete 12547-target closure with nothing left to update and exits successfully; log `/tmp/boost-frontier-v2-suite-050-final-current.log`. | The timeout was only intermediate evidence, not the pass criterion. The successful continuation plus zero-update current-state proof closes the full Graph graph with 169 explicit passing test actions and no failed action or downstream skip. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 51 | `libs/graph_parallel/test` | blocked-external | `(no compiler change)` | The exact four-job forced intake stops during B2 target selection before invoking the compiler: neither `/boost/graph_parallel//boost_graph_parallel` nor `/boost/mpi//boost_mpi` has a build alternative, and the example graph cannot resolve `/python//python`; log `/tmp/boost-frontier-v2-suite-051-initial-forced.log`. | This checkout has no configured MPI toolchain (`mpic++` and `mpirun` are absent), and the suite Jamfiles unconditionally require the Python library target for both tests and examples. This matches the historical non-compiler setup classification. No compiler result can be credited, and no compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 52 | `libs/hana/test` | pass | `(no compiler change)` | The exact four-job forced graph checks C++14 constexpr support, finds the single bookkeeping target, and exits successfully; log `/tmp/boost-frontier-v2-suite-052-initial-forced.log`. | Configuration-only pass in the C++11 lane because both Hana C++14 constexpr configurations are unavailable. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 53 | `libs/hash2/test` | pass | `(no compiler change)` | The exact four-job forced graph finds 2144 targets, updates all 506 requested targets, records 130 passing test actions, handles the deliberate `append_tag_invoke_4` compile failure as failed-as-expected, and exits successfully; log `/tmp/boost-frontier-v2-suite-053-initial-forced.log`. | The current compiler passes the complete Hash2 trait, append, digest, keyed-hash, constexpr, legacy, quality, concept, buffer, and seeded-hash graph. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 54 | `libs/heap/test` | pass | `(no compiler change)` | The exact four-job forced graph completes its compiler and ICU configuration checks, finds 3066 targets, updates all 69 requested targets, passes all eight compile/link/runtime test actions, and exits successfully; log `/tmp/boost-frontier-v2-suite-054-initial-forced.log`. | The current compiler passes the priority queue, mutable, skew, move-only, pairing, Fibonacci, binomial, and d-ary heap graph with no failure, skip, or error marker. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 55 | `libs/iterator/test` | pass | `(no compiler change)` | The exact four-job forced graph completes its ICU configuration check, finds 4492 targets, updates all 175 requested targets, records 46 passing test actions, handles all eight deliberate compile failures as failed-as-expected, and exits successfully; log `/tmp/boost-frontier-v2-suite-055-initial-forced.log`. | All positive zip, archetype, concept, adaptor, transform, indirect, filter, reverse, permutation, function, category, advance, distance, and shared-iterator actions pass. There is no unexpected skip or failed update. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 56 | `libs/lambda/test` | pass | `(no compiler change)` | The exact four-job forced graph finds 1437 targets, updates all 79 requested targets, records 20 passing compile/link/runtime test actions, and exits successfully; log `/tmp/boost-frontier-v2-suite-056-initial-forced.log`. | The current compiler passes the complete algorithm, bind, cast, constructor, control-flow, result-trait, member-pointer, exception, operator, rvalue, result-of, issue, and switch graph with no failure, skip, or error marker. No compiler change, regression update, repository-wide rerun, or performance measurement was required. |
| 57 | `libs/leaf/test` | blocked-external | `(this commit)` | The final exact four-job forced graph finds 3229 targets, requests 482 updates, updates 478, records 122 passing actions, and handles all 22 deliberate failures as failed-as-expected. The sole failed positive update is `nlohmann_json_encoder_test.o`, whose requested external `nlohmann/json.hpp` is absent; its executable, run, and aggregate test are the only three skipped targets. Log: `/tmp/boost-frontier-v2-suite-057-leaf-final-after-validation-forced.log`. | Nine earliest-owner reducers close the compiler-owned dependent-NTTP, qualified-type/SFINAE, conversion ranking, alias materialization, injected-name lookup, RTTI, pretty-function, and virtual-dispatch failures. The repaired `error_code_test`, `print_file_leaf_result`, and `print_file_system_result` compile and link. Configured strict suites, all PA9-excluded direct-LowIR tests (`4008/4008`), placement/hygiene, all 23 zero-reparse categories, audit tests, cache-disabled proofs, and GCC 15 warning checks pass. The suite remains uncreditable as a compiler pass only because the positive nlohmann target has no dependency to compile. |
| 58 | `libs/lexical_cast/test` | pass | `(this commit)` | The initial exact four-job forced graph finds 4096 targets, requests 166 updates, and exposes the same parser failure in `lexical_cast_test.o` and `float_types_test.o`, with six downstream skips; log `/tmp/boost-frontier-v2-suite-058-initial-forced.log`. The final exact graph finds the same 4096 targets, updates all 158 requested targets, records 30 passing test actions, handles both deliberate failures as failed-as-expected, and exits successfully with no skip or failed update; log `/tmp/boost-frontier-v2-suite-058-final-forced.log`. | The PA10 AST reducer covers a parenthesized qualified zero-argument call before multiplication. PA10 passes `137/137`; configured strict suites, the PA9-excluded broad report (`4009/4009`), all 23 zero-reparse categories, 14 audit tests, twelve byte-identical cache modes, Clang/GCC controls, and GCC 15 warning checks pass. The new reducer has no placement finding; PA10's full conservative audit retains its pre-existing 492-item syntax-suite review backlog. Candidate-only frozen performance passes at -2.00% instructions, +1.20% RSS, and +2.35% footprint; no parent or live header was measured. |
| 59 | `libs/local_function/test` | pass | `(this commit)` | The initial exact four-job forced graph finds 1875 targets, requests 314 updates, and fails 32 updates across three compiler-owned semantic fronts; log `/tmp/boost-frontier-v2-suite-059-initial-forced.log`. The final exact graph finds the same 1875 targets, updates all 314 requested targets, records 80 passing test actions, handles all six deliberate failures as expected, and exits successfully with no unexpected failure or skip; log `/tmp/boost-frontier-v2-suite-059-final-forced.log`. | Three structured reducers cover retained macro-token visibility, Boost.LocalFunction's repeated declaration/expression probe, and a qualified-return function-pointer declaration. PA12/PA22/PA24 pass `532/532`; configured strict suites, the PA9-excluded broad report (`4012/4012`), PA37 (`7/7`), placement/hygiene, all 23 zero-reparse categories, 14 audit tests, twelve byte-identical cache modes per reducer, Clang controls, and GCC 15 production-unit checks pass. PA12's full conservative audit retains its pre-existing 149-item backlog, while the new PA12 reducer adds no finding. Candidate-only frozen performance passes at -1.84% instructions, +2.23% RSS, and +2.50% footprint; no parent or live header was measured. |
| 60 | `libs/locale/test` | pass | `(this commit)` | The initial exact four-job graph finds 4618 targets, requests 403 updates, and fails 26 compile updates, led by omitted-template-id explicit specializations and the maximum Unicode scalar; log `/tmp/boost-frontier-v2-suite-060-initial-forced.log`. The second exact graph exposes the remaining overload, array, and runtime frontiers; log `/tmp/boost-frontier-v2-suite-060-second-forced.log`. The final exact graph updates all 173 then-outdated targets, records 28 passing test actions, and exits with no failure or skip; log `/tmp/boost-frontier-v2-suite-060-final-forced.log`. | Seven earliest-owner reducers cover complete-function-type specialization deduction, U+10FFFF encoding, late static-member definition replay, range-for element conversion, implicit-object cv ranking, pack-expanded unknown-bound arrays, and qualified base `num_put::do_put` dispatch. The owner report passes `978/978`; configured strict suites, PA9-excluded `4019/4019`, PA37 `7/7`, placement/hygiene, all 23 zero-reparse categories, 14 audit tests, twelve cache modes for all five semantic reducers, Clang/GCC controls, and warning-clean production units pass. Candidate-only frozen performance passes at -1.78% instructions, +2.51% RSS, and +2.54% footprint; no parent or live header was measured. |
| 61 | `libs/lockfree/test` | skipped-language | `(this commit)` | Boost 1.91 declares `"cxxstd": "14"` in `libs/lockfree/meta/libraries.json`. The exact C++11 graph confirms the suite does not self-gate that requirement: it attempts C++14-only `std::conditional_t` and `std::make_unique` sources and fails 14 compile updates; log `/tmp/boost-frontier-v2-suite-061-lockfree-compact-array-full.log`. | CPPGM's supported source-language lane remains C++11. The later C++14 run was diagnostic only and cannot earn frontier credit. Modern libc++ implementation syntax needed by ordinary C++11 compilations remains hosted-compatibility work, but a library whose declared minimum is C++14 is explicitly skipped rather than broadening compiler scope. The retained memory closure reduces `spsc_queue_stress_test` from about 7.3 GiB to about 635 MiB RSS with zero swap. Focused owners pass `654/654`, PA22/PA26 pass `372/372`, configured strict suites pass, and the PA9-excluded broad report passes `4028/4028`, including PA37 `7/7`; warning, cache-parity, placement, zero-reparse, and audit controls pass. The initially uncapped candidate failed only footprint at +4.01%; bounding the low-yield template-argument backing map while retaining its 512-entry fast ring produces the final candidate-only result of -5.35% instructions, -0.39% RSS, and +2.34% footprint. No parent or live header was measured. |
| 62 | `libs/log/test` | pass | `(this commit)` | The initial exact four-job forced graph finds 26689 targets, requests 549 updates, fails 79, and skips 239; log `/tmp/boost-frontier-v2-suite-062-log-after-qualified-anchor-forced.log`. The final exact serialized forced graph finds the same 26689 targets, updates all 549 requested targets, performs 281 C++ compiles, one C compile, 14 archives, 125 links, and 54 captured runtime actions, records 59 passing test actions plus one deliberate compile failure as failed-as-expected, and exits with no failure or skip; log `/tmp/boost-frontier-v2-suite-062-function-pointer-pack-final-full-forced.log`. It directly passes the five former runtime families, all three duplicate-Asio links, `tutorial_filtering`, `tutorial_attributes`, and `extension_app_launcher`. | The closure adds compact earliest-owner C++11 reducers for local-static specialization ownership, static-reference dynamic initialization, constant nested aggregate initialization, inherited virtual-slot identity, prior-declaration inline linkage, and independent-pack function-template partial ordering. `CppAstNode` is 296 bytes and `Type` is 280 bytes, down from 560. PA22 passes `294/294`; configured strict suites pass PA18 `186/186`, PA19 `115/115`, PA21 `162/162`, PA22 `158/158`, and PA23 `338/338`; the PA9-excluded direct report passes `4047/4047`, including PA37 `7/7`; all 23 zero-reparse categories, 14 audit tests, cache parity, placement, and Clang/GCC warning controls pass. The final immutable compiler-and-51-header gate records -4.41% instructions, -8.47% RSS, and -9.48% footprint. Serial monitoring finds one compiler child throughout, bounded heavy-TU samples up to about 1.74 GiB, full release between units, and 71--72% free system memory; Docker has no running VM workload. |
| 63 | `libs/logic/test` | pass | `(this commit)` | The exact final serialized forced graph finds 236 targets, updates all 30 requested targets, passes `tribool_test`, `tribool_rename_test`, and `tribool_io_test` through compile/link/runtime, reports all six deliberate negative tests as failed-as-expected, and exits with no failure or downstream skip; log `/tmp/boost-frontier-v2-suite-063-logic-final-forced-r3.log`. | A 17-line / 779-byte PA20 reducer covers overloaded logical selection on both scalar/class operand orders and contextual explicit-bool conversion. PA20 passes `86/86`; configured strict suites pass PA18 `186/186`, PA19 `115/115`, PA21 `162/162`, PA22 `158/158`, and PA23 `338/338`; the PA9-excluded direct report passes `4048/4048`, including PA37 `7/7`; all 23 zero-reparse categories, 14 audit tests, twelve byte-identical cache modes, placement/hygiene, and Clang/GCC warning controls pass. Serial monitoring samples the Logic compiler child near 203 MiB RSS with unchanged swap. The final immutable compiler-and-51-header gate records -3.93% instructions, -8.34% RSS, and -8.94% footprint. |
| 64 | `libs/math/test` | skipped-language | `(this commit)` | Boost 1.91 declares `"cxxstd": "14"` in `libs/math/meta/libraries.json`; its build requirements explicitly include C++14 constexpr, `decltype(auto)`, generic lambdas, return-type deduction, and variable templates. | CPPGM's supported source-language lane remains C++11. Per the frontier language policy, no C++11 graph was run and no compiler work is inferred from Math's historical configuration-only result. The cursor advances directly to C++03-declared Metaparse. |
| 65 | `libs/metaparse/test` | pass | `(this commit)` | The initial exact serialized forced graph exposed three compile fronts and one exponential compile-time path. After the structured fixes, the exact full forced graph finds 3623 targets, updates all 241 requested targets, passes every positive action, handles `too_long_string` as failed-as-expected, and exits successfully in 910.13s. A post-refactor forced replay of `all_headers` and `string` also passes in 258.60s at 593,928,192 B maximum RSS with zero swaps. | Three compact C++11 reducers cover dependent `T()` constant initialization at PA20, nested member-class-template explicit-specialization lookup at PA21, and the repeated dependent-reference typedef progression at PA18. PA18 passes `238/238`; PA21 passes its specialization owner; configured strict suites pass PA18 `186/186`, PA19 `115/115`, PA21 `162/162`, PA22 `158/158`, and PA23 `338/338`; the PA9-excluded direct report passes `4051/4051`, including PA37 `7/7`. All 23 zero-reparse categories, 14 audit tests, twelve byte-identical cache modes, and Clang/GCC warning controls pass. The full graph's isolated 1.16 GiB maximum is consistent with the deliberately extreme 128-argument expected-failure unit; ordinary units do not accumulate memory and swap remains unchanged. The final frozen gate records -4.34% instructions, -8.42% RSS, and -9.08% footprint. |
| 66 | `libs/move/test` | pass | `(this commit)` | The initial exact serialized graph finds 882 targets, requests 121 updates, updates 109, fails four targets, and skips eight in 208.28s; its maximum RSS is 534,925,312 B with zero swaps. After the three C++11 repairs, the final exact graph finds the same 882 targets, updates all 121 requested targets, passes every compile/link/runtime action including both benchmarks, and exits successfully in 215.65s. Final maximum RSS is 515,858,432 B with zero swaps. | Three minimal header-free owners cover a deleted-copy class trait (15 lines / 415 bytes), non-class member-pointer substitution (21 lines / 430 bytes), and the same-name type/function call ambiguity used by the benchmark clock helper (19 source lines / 242 bytes). PA26/PA34 pass `408/408`; configured strict suites have zero failures; the PA9-excluded direct report passes `4054/4054`, including PA37 `7/7`. Normal, ten individual cache-off modes, and all-off output are byte-identical for all three reducers. PA26/PA34 placement and hygiene are clean; all 23 text-reparse categories, 28 audit/perf unit tests, Clang 22 and GCC 15 warning builds, and `git diff --check` pass. Serial monitoring shows one compiler child, full per-unit release, and unchanged 208.75 MiB system swap. The final immutable gate retains -4.23% instructions while improving RSS by 8.30% and footprint by 9.04%. |
| 67 | `libs/mp11/test` | pass | `(this commit)` | The final exact two-job forced C++11 graph finds 3857 targets, updates all 922 requested targets, and exits successfully with no failure or skip in 1188.32s; log `/tmp/boost-frontier-v2-suite-067-final-forced.log`. The repaired `mp_partial_sum` target separately compiles, links, runs, and passes in 14.28s at 272,891,904 B maximum RSS with zero swaps; log `/tmp/boost-frontier-v2-suite-067-mp-partial-sum-fixed.log`. | The suite closes all 13 initial failures with compact earliest-owner C++11 regressions. The full graph peaks at 1,634,521,088 B during finite `mp_fold` stress over every size from 0 through 37, then releases fully; two live samples show active structured alias/class-template expansion rather than deadlock or source-text reparsing, and system swap remains unchanged at 551.75 MB. Configured strict suites, all 23 zero-reparse categories, audit tests, cache parity, placement, Clang/GCC warning controls, and the PA9-excluded broad report (`4077/4077`, including PA37 `7/7`) pass. The final immutable compiler/source/51-header gate records -10.91% instructions, -10.47% RSS, and -11.42% footprint. |
| 68 | `libs/mpi/test` | blocked-external | `(no compiler change)` | The exact forced two-job graph finds only one bookkeeping target and exits successfully without creating any compile, link, or runtime action in 3.00s at 39,317,504 B maximum RSS and zero process swaps; log `/tmp/boost-frontier-v2-suite-068-mpi-initial-forced.log`. | Boost.MPI declares C++03, but its Jamfile creates the test suite only when `mpi.configured` is true. This host has neither `mpic++` nor `mpirun`, so the empty successful B2 graph is external-configuration evidence, not a compiler pass. System swap remains unchanged at 551.75 MB. |
| 69 | `libs/mpl/test` | pass | `(this commit)` | The final exact forced two-job C++11 graph finds 2413 targets, updates all 220 requested targets, records 99 passing test actions, and exits with no failure or skip in 332.56s; log `/private/tmp/boost-frontier-v2-suite-069-mpl-full-fixed.log`. | Two minimal, header-free C++11 owners cover ordinary multicharacter constant evaluation and qualified static-array decay to a pointer non-type template argument. PA19/PA22 pass `453/453`; all 959 configured strict comparisons, the PA9-excluded broad report (`4079/4079`, including PA37 `7/7`), all 31 audit/performance unit tests, placement review, cache parity, Clang/GCC controls, and warning-clean Clang/GCC production builds pass. The final graph peaks at 447,705,088 B RSS, essentially unchanged from the one-failure intake, releases fully, and records zero swaps. |
| 70 | `libs/mqtt5/test` | skipped-language | `(this commit)` | Boost 1.91 declares `"cxxstd": "17"` in `libs/mqtt5/meta/libraries.json`. | CPPGM's supported source-language lane remains C++11. Per the frontier language policy, no graph was run and no compiler work is inferred from the historical configuration-only result. The cursor advances directly to C++03-declared MSM. |
| 71 | `libs/msm/test` | pass | `2c6db27c0` | The final exact two-job forced C++11 target `libs/msm/test//msm-unit-tests` finds 4901 targets, updates all 213 requested targets, passes all 37 compile/link/runtime actions, and exits successfully in 2562.09s; log `/private/tmp/boost-msm-cxx11-suite-typed-retry.log`. The separately declared C++17 and C++20 suites were intentionally excluded. | The last three failures shared one lazy integral-comparison defect: initial typed lookup of an inherited static `value` failed before materialization, the boolean fallback completed the qualifier but collapsed its exact integer `3` to `true`, and equality compared `1 == 1`. Typed lookup now retries after that materialization and preserves the exact integral value. The 21-line / 352-byte PA19 reducer is header-free C++11 and uses no `<type_traits>`; its witness comes from patched Clang. A serialized replay of `Serialize`, `SerializeSimpleEuml`, and `SerializeWithHistory` updates all 77 targets and passes in 710.11s at 2,839,158,784 B maximum RSS with zero process swaps. The full two-job graph peaks at 4,380,020,736 B when known multi-GiB units overlap; each TU releases fully, while the overlap moves about 488 MiB of system pages to swap, so future known-heavy replays are serialized while ordinary work remains parallel. The final PA9-excluded direct report passes `4094/4094`, PA37 passes `7/7`, all 960 strict comparisons pass, all 23 reparse categories remain zero, all 31 audit/performance tests pass, all cache-off modes are byte-identical, and Clang 22 C++11 warning controls pass. The committed frozen-source/51-header gate records -26.56% instructions, -28.56% RSS, and -35.61% footprint. |

Allowed statuses are `pending`, `running`, `frontier`, `blocked-external`,
`skipped-language`, and `pass`. A timeout is evidence, not a pass.

## Active Frontier

- suite: `#72 libs/multi_array/test`
- focused target: initial forced graph pending
- last closed suite: `#71 libs/msm/test` (`pass`)
- failure phase: none observed yet
- diagnostic: none yet
- reduced repro: none yet
- owning PA/cluster: to be determined from the first causal failure
- implementation area: to be determined from the initial graph
- performance risk: start with two jobs and monitor compiler children; serialize
  only a target demonstrated to be multi-GiB
- language lane: Boost.MultiArray declares C++03 in
  `libs/multi_array/meta/libraries.json`, so it is supported and must run
- next action: run the exact forced C++11 graph and classify its first failure

## Fix Ledger

Add one row per coherent compiler fix. Keep detailed logs in `/tmp`; keep the
stable command, diagnostic, reducer, validation, and measured deltas here.

| Status | Suite/target | Root cause and typed fix | Owner regression | Pre-fix evidence | Validation | Perf vs fixed baseline | Commit |
|---|---|---|---|---|---|---|---|
| fixed | MSM lazy reference-member materialization and structured specialization identity | Reference-only class completion eagerly instantiated every member and retained a broad semantic graph even when lookup needed one name. It now collects the requested member/type slice and preserves the old eager path for witness generation. Concrete nested types retain compact typed identity, reconstructed local displays keep their discriminator, completed host-ABI facts are reused, internal names are bounded, and symbol construction no longer retains a process-lifetime memo of large component strings that still had to be hashed, compared, and copied. No semantic source text is reparsed and no correctness cache is introduced. | `pa21/tests/general/300-nested-base-prefers-direct-sibling-member-template.t`, 25 nonblank lines / 553 bytes, header-free C++11 with no `<type_traits>` and a 106-byte empty-main LowIR reference | The reducer selects a direct sibling member template through a nested dependent base path. The guarded default-backend `CompositeEuml` scale input is the scale witness: the prior package reached 8.98 GiB physical footprint without output, whereas this package completes it at 1.95 GB RSS. The exact source advances to a later independent qualified-call diagnostic at 3.45 GB RSS. | The reducer passes cppgm++, Clang 22, and GCC 15 under C++11; normal, ten individual cache-off modes, and all-off runs are byte-identical. The final PA9-excluded direct report passes `4092/4092`, PA37 object roundtrip passes `7/7`, all 959 strict comparisons pass, all 23 text-reparse categories stay zero, and all 14 audit tests pass. Five normal LowIR refs change for lazy first-use ordering or removal of an internal overload ordinal; witness refs are unchanged. | -26.39% instructions, -28.56% RSS, and -35.36% footprint; clean committed-HEAD three-run immutable frozen-source/51-header report `/private/tmp/cppgm-boost-frontier-v2-msm-lazy-reference-clean-head-perf.json`. Versus the preceding concrete-syntax checkpoint, instructions improve another 3.32%, RSS 7.15%, and footprint 8.35%. | `173ed08ec` |
| fixed | MSM serialization lazy inherited integral comparison | Typed member-value lookup initially failed because the inherited qualifier had not yet been lazily materialized. The structured boolean fallback materialized it but converted the exact integer `3` to `true`; the surrounding equality then compared `1 == 1` and selected an unchosen serialization branch. After successful structured materialization, equality operands now retry typed member lookup and preserve the exact integral value. No cache, source-text path, eager completion, or language extension is added. | `pa19/tests/general/100-lazy-inherited-integral-member-comparison.t`, 21 lines / 352 bytes, header-free C++11 with no `<type_traits>` and a patched-Clang witness | The saved pre-fix compiler makes `equal_to<level<int>>::value` true; Clang 22 and the fixed compiler make it false. Pre-completing `level<int>::value` also makes the old compiler pass, proving the lazy-order dependency. Exact MSM selected `load_primitive` from the unchosen `mpl::eval_if` branch for three state-machine types. | `Serialize`, `SerializeSimpleEuml`, and `SerializeWithHistory` pass both serialized and in the full graph. All 37 supported MSM actions pass; C++17/20 suites are excluded. PA19 placement is clean; the PA9-excluded direct report passes `4094/4094`, strict passes `960/960`, all 23 reparse categories and 31 audit/performance tests pass, cache modes are byte-identical, and Clang 22 C++11 warning controls pass. | -26.56% instructions, -28.56% RSS, and -35.61% footprint; committed immutable frozen-source/51-header report `/private/tmp/cppgm-boost-frontier-v2-msm-lazy-integral-final-perf.json` | `2c6db27c0` |
| fixed | MSM `CompositeEuml` reference-field `decltype` handoff | Reference-only class collection recorded static data members but omitted ordinary fields. A later class-scope `decltype(x + x)` alias therefore lacked `x`, and ordinary expression analysis could not form an implicit member access without a function `this`. Reference collection now records a typed `ValueBinding` for each field, and a scoped unevaluated-operand mode analyzes its type and value category without inventing an object. Data-member-pointer non-type template arguments complete an incomplete owner and refresh that binding before encoding the layout offset. The fix adds no source text or cache path. | `pa21/tests/general/200-reference-field-decltype-alias.t`, five lines / 193 bytes, header-free C++11 with a 106-byte empty-main LowIR reference and no `<type_traits>` | The saved pre-fix compiler rejects the alias chain. Exact `CompositeEuml` reaches the same reference-only typedef handoff after clearing the guarded transition and `Playing` fronts. The first broad run after adding field bindings collapsed `&pair::x` and `&pair::y` to the same offset and omitted a PA32 owner-sensitive ABI symbol; forcing layout before numeric encoding repairs both cases without reference changes. | The reducer passes cppgm++, Clang 22, and GCC 15 under C++11. PA26/PA32 pass `195/195`; the PA9-excluded direct report passes `4091/4091`, including PA37 `7/7`; all configured strict comparisons pass. Normal, ten individual cache-off modes, and all-off runs produce byte-identical reducer LowIR. PA21 placement and hygiene have zero findings; all 23 reparse categories and 31 audit/performance tests pass; changed production units build warning-clean under Clang 22 and GCC 15. | -24.06% instructions, -21.32% RSS, and -30.28% footprint; candidate-only frozen report `/private/tmp/cppgm-boost-frontier-v2-msm-reference-field-candidate.json`; rolling instructions are flat, RSS improves 0.04%, and footprint improves 2.39%; the gate measured neither a parent nor a live header | `(this commit)` |
| fixed | MSM `CompositeMachine` current injected specialization identity | Current injected class lookup compared only the source spelling and the materialized `ClassInfo::qualified_name`. For a concrete nested specialization whose materialized name contains semantic identity components, neither matched the lexical template-id; a stale unrelated exact-lookup anchor then won. The lookup now also compares the retained typed instantiation identity. No source text is parsed and no speculative lookup or cache change remains. | No new fixture: all compact header-free scratch forms passed before and after, so none causally reduced the Boost failure. The exact `CompositeMachine` TU is retained as the pre/post witness rather than adding a large or false reducer. | Unpatched HEAD rejects the exact TU in 12.19s at 449,990,656 B RSS with the stale `mpl::vector<...>` anchor; the isolated 13-line fix passes that point and remains active through a bounded 20s replay at 651,878,400 B RSS. Two other provisional lookup changes were independently removed without losing this advance. | PA22 direct LowIR passes `306/306`; the PA9-excluded direct report passes `4090/4090`; configured strict comparisons pass PA18 `186`, PA19 `115`, PA21 `162`, PA22 `158`, and PA23 `338` with zero failures; all 23 zero-reparse categories and 31 audit/performance tests pass; Clang 22 and GCC 15 warning builds are clean. | -24.06% instructions, -21.29% RSS, and -28.57% footprint; three-run immutable frozen-source/51-header report `/private/tmp/cppgm-boost-frontier-v2-msm-current-injected-identity.json`; restored embedded baseline hash `c03bfd5c1b63bb022cd119eb0968dace0346052cdc13091665ea22a3a4f54996`; no parent or live header was measured | `(this commit)` |
| fixed | MSM constituent alias/template-template reconstruction and dependent-type DAG walk | Direct type substitution discarded structured qualified/template-id sidecars, concrete template-template arguments reconstructed only display text, and partial function-result deduction did not compare the retained class-template declaration and typed arguments. The typed paths now preserve or reconstruct those structures, alias expansion can consume the caller's already resolved argument vector, and partial deduction compares dependent/concrete class-template metadata. Separately, the dependent-qualified-member predicate treated a shared `Type` graph as a tree; an exact pointer-identity visited set bounds the traversal without a result cache. No semantic text is parsed. | `pa21/tests/general/200-alias-template-template-argument-use-scope.t` and `pa21/tests/general/200-template-template-function-result-reconstruction.t`, both compact header-free C++11 compile-pass cases with ordinary LowIR references | Clang 22 accepts both reducers; the two controlled clean pre-fix compilers reject both. Exact `CompositeEuml` previously failed its first guarded subscript result and now reaches the later `Playing` row. A fresh `CompositeMachine` replay proves that its similar-looking structured-anchor failure remains open, so the smaller alias reducer is recorded as a constituent repair rather than overclaimed as the complete Boost reduction. An attempted owner-first member-pointer lookup made inherited name collisions viable and broke two PA26 controls; it was reverted completely. | PA21 passes `254/254`; PA26 passes `82/82`; configured strict suites pass PA18 `238/238`, PA19 `147/147`, PA21 `254/254`, PA22 `306/306`, and PA23 `409/409`; the full direct-text report passes `4101/4101`, including PA37 `7/7`. PA21 placement/hygiene has zero findings, all 23 reparse categories and 31 audit/perf unit tests pass, and the changed production units build warning-clean under Clang 22 and GCC 15. | -24.30% instructions, -21.11% RSS, and -30.09% footprint; three-run immutable frozen-header report `/private/tmp/cppgm-boost-frontier-v2-template-template-candidate.json`; required diagnostic sample finds no new dominant helper | `(this commit)` |
| fixed | MSM `AnonymousAndGuard` qualified template-template identity | Class-template specialization/reference keys rendered namespace template-template arguments by leaf name. `boost::mpl::end` and `boost::fusion::result_of::end` therefore both became `end`, and Fusion reused MPL's helper. When an occupied legacy key carries a different typed template entity, the class-template path disambiguates it with the entity's structured namespace scope while retaining legacy keys for non-colliding cases. Selected class-template IDs and nested alias results now carry that collision-aware key instead of falling back to a generic argument key or reconstructed display identity. No lookup text is parsed and no success or failure cache is added. | `pa21/tests/spec/200-deduced-template-template-qualified-identity.t`, strengthened to 11 lines / 580 bytes with a nested `outer<F>::type` layer; header-free C++11 with two same-named namespace templates and no `<type_traits>` | The saved pre-fix compiler aliases the second `x` to the first. A controlled removal of only the two nested identity-propagation edits makes the strengthened second assertion fail while the original direct form still passes. In exact MSM, the initial collision produces `boost::fusion::extension::value_at_impl<..., mpl_::int_<4>>` for a four-element vector; that identical invalid entity accounts for 3,494 of 3,771 reset traces. After repair, 19,264 of 19,266 later reference-member events have distinct `ClassInfo` identities and names, with only two revisits and no substitution failures: this is legitimate specialization breadth, not one alias resolving thousands of times, and no cache change is warranted. | The reducer passes cppgm++, Clang 22, and GCC 15 warning-clean under C++11. Normal, all ten cache-disabled modes, and template tracing produce byte-identical output. The exact trace distinguishes legacy `x` from qualified `b::x`, traverses Fusion iterator indices 0 through 3, and selects terminal `for_each_impl<true>`. PA21 passes `252/252`; all configured strict suites, placement, all 23 zero-reparse categories, 20 audit tests, GCC 15 production builds, and PA37 `7/7` pass. | final combined frozen gate: -12.53% instructions, -10.68% RSS, and -11.63% footprint; candidate-only report `/private/tmp/cppgm-boost-frontier-v2-msm-pvb-final-perf.json`; no parent or live header was measured | `(this commit)` |
| fixed | MSM function-template pointer argument with an incomplete pointee | Cast type-id analysis and virtual-base ABI-parameter metadata both completed a class reached only through a pointer/reference. That eagerly instantiated an intentionally ill-formed lazy member although neither pointer conversion nor indirect parameter layout needs the pointee definition. Cast parsing is now reference-only until an actual named class object target needs completion; identical pointer conversion short-circuits before inheritance work; and virtual-base parameter metadata uses an indirect class only when it is already complete. No cache is added. | `pa21/tests/spec/300-function-template-pointer-argument-incomplete-pointee.t`, 5 lines / 228 bytes, header-free C++11 with no `<type_traits>` | The saved pre-fix compiler rejects `call::execute(static_cast<lazy<n> *>(0))` by instantiating `lazy<n>::type`. Clang 22 and GCC 15 accept the class-static function-template form; moving it to a global function changes the language behavior and is not a valid reduction. LLDB shows both eager completion sites in the pre-fix stack. | The reducer passes cppgm++, Clang 22, and GCC 15 warning-clean under C++11. PA21 and all configured strict suites pass. The PA9-excluded direct report passes 4,084 of 4,085 before the sole deterministic PA24 order-only reference is refreshed; PA24 then passes `115/115` direct. All 23 zero-reparse categories, 14 audit tests, placement, cache-disabled parity, GCC 15 production builds, and PA37 `7/7` pass. | included in the same final frozen checkpoint: -10.70% instructions, -10.65% RSS, and -11.58% footprint; every fresh process records zero swaps and releases before the next | `(this commit)` |
| fixed | MSM `AnonymousAndGuard` virtual reference-parameter ABI | The earlier RSS recovery correctly stopped completing every class reached only through an ABI pointer/reference parameter, but a caller translation unit containing only the virtual declaration then lacked `std::basic_ostream`'s hidden virtual-base parameter layout. A new typed base-graph-only collector resolves inheritance without walking members or completing the class, attaches unique virtual-base identities to virtual-call arguments, and lets lowering compute each hidden pointer from the vtable offset. Direct nonvirtual calls retain the existing strict symbol-layout requirement. No cache, source text, or hosted-header special case is added. | `pa27/tests/general/100-virtual-dispatch-incomplete-vbase-parameter.t`, reduced after closure to 6 lines / 120 bytes with a 16-line / 512-byte LowIR reference; minimal header-free C++11 with no `<type_traits>` | LLDB shows `boost::unit_test::results_reporter::make_report` passing only the formatter and stream object while the compiled virtual callee expects a third hidden virtual-base pointer; the missing argument is null at the crash in libc++ stream handling. The pre-fix LowIR contains a two-argument virtual call. The repaired LowIR loads the virtual-base offset from vtable slot `-24`, adjusts the stream object, and passes `%__pvbptr0`. | The minimized PA27 reducer passes its focused direct check and Clang/GCC 15 warnings-as-errors under C++11. Every PA27 reference above 5 KiB was reviewed; their source owners are already compact and the remaining size comes from required VTT, vtable, constructor, destructor, or adjustor output rather than bloated reducers. The exact `AnonymousAndGuard` graph passes after all 69 affected targets rebuild. The final direct compile peaks at 1,463,574,528 B RSS with zero process swaps. | final combined frozen gate: -12.89% instructions, -11.97% RSS, and -12.91% footprint; `/private/tmp/cppgm-boost-frontier-v2-msm-pack-memory-final-accessor.json` | `(this commit)` |
| fixed | MSM retained template-state and pack symbol closure | Each `ClassInfo` retained duplicate primary, binding, and mangle argument vectors even when all three were immutable and identical, and disabled template auditing still stored a creation-context string for every instantiated class. Primary class arguments now have shared ownership, binding and mangle state borrow that storage until a real mutation detaches it, and audit strings are retained only when auditing is enabled. Separately, the structured ABI builder now preserves an explicit empty owner pack and dependent member-pointer declarators; incompatible partial-specialization pack grouping retries the primary parameter grouping rather than dropping the symbol. No result cache or source-text path is added. | `pa32/tests/general/200-empty-owner-pack-symbol-spelling.t.1` and `pa32/tests/general/200-partial-pack-copy-constructor-symbol-spelling.t.1`, each 10 nonblank source lines. Both are header-free C++11 ABI-owner tests without `<type_traits>`; the raw expected symbols match Clang 22. | A 20,000-class retained-state census attributes about 709 MB to genuinely distinct semantic objects while all measured caches total only about 8.75 MB. Exact alias-declaration/canonical-argument probes find no hot repeated key, and 19,264 of 19,266 reference-member events are distinct class identities. The final exact `AnonymousAndGuard` compile saves 79,331,328 B RSS from the preceding replay. Clang confirms the four refreshed PA26 member-pointer symbols use the dependent `MT0_FT...` declarator form rather than premature concrete substitution. | Focused PA32 symbol tests pass `2/2`; PA26 passes `82/82`; the PA21/PA22/PA32 direct report passes `671/671`; all configured strict PA18/19/21/22/23 suites pass; the final PA9-excluded broad report passes `4088/4088`, including PA37 `7/7`. Normal and all ten caches disabled are byte-identical for the empty-pack, partial-pack, and `remove_cvref` reducers. All 23 zero-reparse categories, all 23 audit unit tests, PA32 placement/hygiene, and Clang/GCC 15 C++11 warning controls pass. | -12.89% instructions, -11.97% RSS, and -12.91% footprint; candidate-only frozen compiler/source/51-header report `/private/tmp/cppgm-boost-frontier-v2-msm-pack-memory-final-accessor.json`; no parent compiler or live header was measured | `(this commit)` |
| fixed | MPL ordinary multicharacter literal compatibility | The assignment-level post-token contract deliberately rejects multicharacter literals, but the full C++ compiler used that strict mode unchanged even though ordinary multicharacter literals are conditionally supported C++11 integer literals. Full-source analysis now explicitly enables a compatibility mode that packs the last `sizeof(int)` ordinary narrow code units in host-compatible order and carries the `int` value through semantic and constexpr evaluation. Direct PA2 post-tokenization stays strict by default. No hosted-header, language-version, source-text, or cache path changes. | `pa19/tests/general/100-ordinary-multicharacter-constant-eval.t`, 3 lines / 94 bytes, header-free C++11 with a 106-byte empty-main reference | Exact MPL `string.cpp:66` is rejected at its first `'aaaa'` before the fix. The existing `pa2/tests/200-character-literal.t` requires direct post-tokenization of `'abc'` to remain invalid, establishing the compatibility boundary. Clang 22 and GCC 15 accept the reducer under C++11 with `-Wno-multichar`. | The new PA19 check and existing PA2 strict control pass; the PA19/PA22 direct report passes `453/453`; all 959 configured strict comparisons, all 31 audit/performance unit tests, placement, Clang/GCC controls, and warning-clean production-unit builds pass. Exact MPL `string` then advances to the independent array-pointer argument failure, and the final suite passes all 99 test actions. | measured with the complete MPL closure: -11.16% instructions, -10.72% RSS, and -11.37% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-mpl-candidate.json`; no parent or live header was measured | `(this commit)` |
| fixed | MPL qualified static-array pointer non-type template argument | Value-binding template argument conversion accepted a scalar object for a pointer target but not the required array-to-pointer decay of a qualified static data member. The structured binder now recognizes a non-field array with a cv-compatible element type, preserves the exact `ValueBinding`, and records the pointer parameter type. Expression rematerialization applies the same typed array compatibility so a use such as `*P` retains the static symbol through LowIR. No source text, spelling rule, hosted-header, or cache path is added. | `pa22/tests/general/400-qualified-static-array-pointer-nontype-argument.t`, 5 lines / 273 bytes with a 484-byte LowIR reference; header-free C++11 with no `<type_traits>` | After multicharacter literals were accepted, exact MPL `string.cpp:343` failed to instantiate `greeting<mpl::c_str<...>::value>`; accepting the argument alone then exposed `unknown id-expression sz` in the instantiated body. The reducer includes `*P`, so it covers both the decay and retained-symbol lowering rather than only a typedef. Clang 22 and GCC 15 compile and run it warning-clean as C++11. | The reducer passes normally and in all six relevant individual cache-disabled modes with byte-identical LowIR. The focused exact `string` target compiles, links, runs, and passes in 15.65s at 328,839,168 B RSS with zero swaps. PA22, strict, broad `4079/4079`, all 23 zero-reparse categories, all 31 audit/performance unit tests, placement, warning, and final two-job MPL gates pass. | measured with the complete MPL closure: -11.16% instructions, -10.72% RSS, and -11.37% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-mpl-candidate.json`; no parent or live header was measured | `(this commit)` |
| fixed | MP11 `mp_partial_sum` default member-alias declaration scope | A defaulted member-alias argument combined the alias declaration scope with caller bindings, but the general overlay copied a caller template-template binding named `F` over the alias declaration's outer `F`. Thus `N = F<typename V::type, T>` rebound `F` to the member alias itself and recursively expanded. A shared helper now collects all declaration-owned template-bound names and protects them while applying the existing structured use-scope overlay at all four default-argument paths. It uses retained scopes, bindings, and AST only; no source text is reparsed. | `pa21/tests/general/300-member-alias-default-declaration-scope.t`, 10 lines / 427 bytes including `// VALIDATION`, header-free C++11 with no `<type_traits>` and a 106-byte empty-main reference | The saved pre-fix compiler `/tmp/cppgm-zero-pack-fixed` exceeds an 8-second bound on the reducer; the unfixed exact Boost target remained CPU-active for more than 11 minutes with repeated default-argument/member-alias expansion stacks. Clang 22 and GCC 15 accept the reducer warning-clean as C++11. | Exact `mp_partial_sum` passes compile/link/runtime in 14.28s at 272,891,904 B RSS with zero swaps. The final two-job MP11 graph updates all 922 targets with no failure or skip. The focused PA21 owner, configured strict suites, PA9-excluded broad report (`4077/4077`), normal plus ten individually disabled and all-disabled cache modes, PA21 placement, all 23 zero-reparse categories, audit tests, and Clang/GCC production warning builds pass. The full graph's 1,634,521,088 B maximum belongs to finite `mp_fold` size stress, releases fully, and does not increase swap. | -10.91% instructions, -10.47% RSS, and -11.42% footprint; candidate-only frozen compiler/source/51-header report `/tmp/cppgm-boost-frontier-v2-mp11-partial-sum-perf.json`; no parent compiler or live header was measured | `(this commit)` |
| fixed | MP11 `tuple_transform` and `tuple_transform_2` nested call expansion and defaulted deduction | Outer function-call pack discovery recursively counted packs already consumed by nested pack expansions, incorrectly requiring independent inner and outer packs to have equal lengths; substitution could also rewrite a nested expansion before its own layer. Discovery now skips nested expansion/`sizeof...` nodes and structured substitution preserves nested pack-expansion AST until that expansion runs. Separately, defaulted non-type function-template arguments were bound before deduction, making a defaulted pattern concrete and preventing deduction from the corresponding call argument. Defaults now fill only parameters left undeduced after the ordinary loop. No source text is reparsed. | `pa19/tests/general/200-nested-call-pack-expansion-independent-packs.t`, 5 lines / 293 bytes; `pa19/tests/general/200-nested-call-pack-expansion-same-pack.t`, 4 lines / 213 bytes; and `pa22/tests/general/100-defaulted-nontype-deduction-overrides-default.t`, 4 lines / 139 bytes. All are minimal header-free C++11 owners without `<type_traits>`. | The saved pre-fix compiler rejects the independent-pack reducer as unsupported, reduces the same-pack reducer's inner call to one argument, and instantiates the deduction reducer with its default `B=1` instead of deducing `B=4`. Clang 22 and GCC 15 accept all three warning-clean under C++11. | Exact `tuple_transform` and `tuple_transform_2` compile, link, run, and pass together in 28.24s at 349,736,960 B maximum RSS with zero swaps. Focused PA19/PA22 owners pass; normal, ten individually disabled caches, and all caches disabled produce byte-identical LowIR for all three reducers. Configured strict suites, all 23 text-reparse categories, 14 audit tests, Clang/GCC production warning builds, placement checks, and the PA9-excluded broad report (`4076/4076`, including PA37 `7/7`) pass. | -11.12% instructions, -10.49% RSS, and -11.41% footprint; candidate-only frozen compiler/source/51-header report `/tmp/cppgm-boost-frontier-v2-mp11-tuple-transform-perf.json`; no parent compiler or live header was measured | `(this commit)` |
| fixed | MP11 `mp_set_difference` and `mp_set_intersection` empty-owner/member-alias scope | A structured `X<>` specialization has no mangle arguments, so class lookup could reject its valid empty specialization metadata or return a stale cached class instance before materializing the exact owner. Structured zero-argument specialization metadata is now authoritative when it fully binds the class parameters and is consulted before the cache. Separately, retained source arguments for a dependent member-alias specialization were expanded in the callee alias's instantiation scope; a same-named callee pack could capture the caller member-alias parameter and recursively nest the transformed type. Expansion now uses the original use scope. Both changes consume existing typed metadata and AST; neither reparses source text. | `pa21/tests/general/400-empty-pack-member-alias-owner-specialization.t`, 10 lines, and `pa21/tests/general/400-pack-expanded-member-alias-source-scope.t`, 8 lines. Both are minimal header-free C++11 PA21:400 integration tests with no `<type_traits>`. | A saved pre-fix compiler fails the empty-owner reducer; the compiler after only that fix passes it but fails the source-scope reducer, establishing the independent second cause. The source-scope case needs two pack elements to expose the capture and cannot be reduced to one. Clang 22 and GCC 15 accept both warning-clean under C++11. | Exact `mp_set_difference` and `mp_set_intersection` compile, link, run, and pass together in 12.19s at 247,087,104 B maximum RSS with zero swaps. PA21 passes `247/247`; configured strict PA18/19/21/22/23 suites pass; the PA9-excluded broad report passes `4073/4073`, including PA37 `7/7`. Normal, all ten individually disabled caches, and all caches disabled produce byte-identical LowIR for both reducers. All 23 text-reparse categories and 14 audit tests pass; Clang/GCC production warning builds and the PA21 placement audit pass. The remaining tuple-transform pair still fails independently. | -11.12% instructions, -10.61% RSS, and -11.45% footprint; candidate-only frozen compiler/source/51-header report `/tmp/cppgm-boost-frontier-v2-mp-set-scope-fixed-perf.json`; no parent compiler or live header was measured | `(this commit)` |
| fixed | MP11 `construct_from_tuple`, `tuple_apply`, and `tuple_for_each` identity-alias partial deduction | A class partial-specialization argument such as libc++'s `__enable_if_tuple_size_imp<const T, constraints...>` has an identity-alias result but additional arguments that participate in substitution. The placeholder matcher retained the whole alias as opaque, so it could not deduce `T` from the alias result and selected the incomplete `tuple_size` primary. Deduction now follows the retained alias entity to the exact type argument named by its direct result parameter, then resolves the original retained argument AST in the concrete deduced scope. This both exposes the result pattern and revalidates every ignored SFINAE argument; no source text is parsed or reconstructed. | `pa21/tests/general/400-identity-alias-cv-partial-specialization.t`, 6 lines / 239 bytes, header-free C++11 with a 106-byte empty-main reference and no `<type_traits>` | `X<A<T const, int>>` fails to match `X<int const>` before the fix, leaving the primary incomplete. Exact `construct_from_tuple` fails its first const-tuple call because `std::tuple_size<const tuple<...>>::value` remains unavailable. | Exact `construct_from_tuple`, `tuple_apply`, and `tuple_for_each` compile, link, run, and pass together in 17.28s at 242,778,112 B maximum RSS with zero swaps. The existing PA22 detected-or fallback test confirms that a missing member in an ignored `void_t<Op<T>>` argument still rejects the partial. PA21/PA22 pass `549/549`; configured strict PA18/19/21/22/23 suites pass; the PA9-excluded broad report passes `4071/4071`, including PA37 `7/7`. Normal and all ten cache-disabled reducer outputs are byte-identical; Clang/GCC C++11 controls, all 23 zero-reparse categories, 23 audit tests, and Clang/GCC 15 production warning builds pass. | -11.19% instructions, -10.56% RSS, and -11.37% footprint; candidate-only frozen compiler/source/51-header report `/tmp/cppgm-boost-frontier-v2-mp11-identity-alias-perf.json`; all processes release fully and swap is unchanged | `(this commit)` |
| fixed | MP11 `mp_compose_sf` partial-owner and concrete member-alias binding | Member-alias instantiation rebound only a class template's primary parameters even when the selected owner was a partial specialization, so a same-spelled forwarding parameter could shadow the partial's template argument. The binding path now selects and rebinds the exact retained partial parameters and instantiation arguments. Separately, direct AST substitution replaced an already-bound concrete member-template head with display syntax; aliases with a trailing pack then missed the structural fast path and lost the typed owner. Such replacements now retain the existing bound head and let normal typed lookup recover its declaration and owner. No source text is reparsed. | `pa21/tests/general/400-partial-owner-member-alias-scope-shadow.t`, 11 lines / 385 bytes, and `pa23/tests/general/400-member-alias-template-trailing-pack-forward.t`, 12 lines / 370 bytes. Both are header-free C++11 tests with 106-byte empty-main references. | The first reducer produces `N<N<char>>` only when the selected partial owner wins over the same-spelled forwarding parameter. The second forwards `O<int>::M` through a template-template alias with a trailing pack; replacing the pack with a fixed parameter hid the defect. | Exact `mp_compose_sf` compiles, links, runs, and passes in 5.63s at 76,201,984 B maximum RSS with zero swaps. PA21/PA23 direct-text tests pass `653/653`; strict PA21 passes `244/244` with `162/162` witness comparisons, and strict PA23 passes `409/409` with `338/338` comparisons. Normal and all ten cache-disabled outputs are byte-identical for both reducers. Clang and GCC accept both with C++11 warnings-as-errors; all 23 zero-reparse categories and 23 audit tests pass. | frozen candidate gate pending before the next MP11 closure; the immutable compiler/source/header epoch remains unchanged | `(this commit)` |
| fixed | MP11 `mp_fold` nested alias-template result | Repeated substitution retained both sides of an exact type/expression template-id ambiguity through every nested alias result, and dependent-type queries revisited identical typed work. The general clone now compacts only an exact direct template-id ambiguity, restores its type-id lazily at an actual pack expansion, and memoizes dependent-type resolution only for the lifetime of one outer query using scope instance, binding fingerprint, and exact `Type *`. Witness capture and a cache-disable control retain the uncached path. | `pa22/tests/general/200-nested-alias-template-id-ambiguity.t`, reduced after closure to 16 lines / 706 bytes with a 106-byte LowIR reference; it remains header-free C++11, uses no `<type_traits>`, and retains all ten nesting levels needed to expose the amplification | The pre-compaction metric capture records 4,204,946 resolver calls but only 130 misses and 4,204,815 hits: the work was recursive replay before the existing resolver-cache lookup, not millions of distinct aliases. A current cache-disabled hotspot capture explains the residual graph exactly: the innermost prefix is requested 2048 times, then its parents 1024, 512, and so on because structured type lookup and class-reference/member-alias collection both recursively consume the retained inner prefix. The sampled stack stays in alias substitution and reference-member collection; it is active exponential DAG traversal, not a deadlock. | The minimized reducer passes cppgm++, Clang 22, and GCC 15 and matches its LowIR reference. A fresh normal run needs 44 resolver calls (23 misses, 12 hits), 0.18s, and 14,319,616 B RSS; deliberately disabling the long-standing template-argument cache exposes 6161 calls but still finishes in 0.26s, emits byte-identical LowIR, and records zero swaps. Normal and `CPPGM_DISABLE_DEPENDENT_TYPE_RESOLUTION_CACHE=1` outputs also remain byte-identical. The original PA22/PA23, strict, reparse, audit, warning, and exact `mp_fold` closure evidence remains valid. No new cache change is part of the MPL commit. | -11.15% instructions, -10.33% RSS, and -11.38% footprint; `/tmp/cppgm-boost-frontier-v2-mp11-fold-rss-final-perf.json` | `a667c6844` plus reducer minimization in `(this commit)` |
| fixed | Move deleted-copy triviality | The semantic `__is_trivial` implementation checked trivial default construction and destruction but omitted the required trivially-copyable condition. It now requires the existing typed trivially-copyable predicate, and named-class template-argument evaluation routes `__is_trivial` through the semantic context just like the related POD and standard-layout traits. | `pa34/tests/compile/500-builtin-trivial-deleted-copy.t`, 15 lines / 415 bytes at the hosted builtin-trait owner | Boost.Move `type_traits.cpp` incorrectly accepted a class whose copy constructor and copy assignment are deleted as trivial. The compact reducer checks `__is_trivially_copyable`, `__is_trivial`, and `__is_pod` together. | The focused PA34 owner, full Move graph, all cache modes, owner/strict/broad reports, placement, zero-reparse/audit, and Clang/GCC warning controls pass. | measured with the complete Move closure: -4.23% instructions, -8.30% RSS, -9.04% footprint; `/tmp/cppgm-boost-frontier-v2-move-final.json` | `(this commit)` |
| fixed | Move non-class member-pointer SFINAE | Substitution could form a member-pointer type whose owner had become a concrete non-class, so the classic class-or-union overload probe accepted `void (int::*)()`. Typed member-pointer construction now rejects a definitely non-class owner, including the already-bound function-parameter instantiation path, by ordinary template substitution failure. | `pa26/tests/general/300-member-pointer-nonclass-owner-sfinae.t`, 21 lines / 430 bytes at the remaining object-model/SFINAE owner | Boost.Move `conversion_test.cpp` selected the class/union probe for `int`, which in turn chose the wrong conversion overload. The reducer is the complete causal two-overload probe with one assertion. | The focused PA26 owner, exact conversion target, final Move graph, all cache modes, owner/strict/broad reports, placement, zero-reparse/audit, and warning controls pass. | measured with the complete Move closure | `(this commit)` |
| fixed | Move retained same-name type/function call ambiguity | A block statement such as `clock_info(&info);` was committed as a declaration before the retained ambiguous expression alternative was tried; the nested-declarator recovery also discarded its unary `&`. The statement path now tries the exact retained local-call alternative before declaration preparation and reconstructs the retained unary operator when forming the argument. No source text is reparsed. | `pa34/tests/run/800-same-name-type-function-call-run.t.1`, 19 lines / 242 bytes at source-to-native runtime behavior | Boost.Move's `mach_timebase_info(&info);` benchmark helper became a shadowing declaration, left the timebase data uninitialized, and made `bench_sort` and `bench_merge` segfault in `nsec_clock`. The header-free runtime owner uses the same type/function-name shape. | The reducer, both focused benchmark compile/runs, final Move graph, all cache modes, owner/strict/broad reports, placement, all 23 zero-reparse categories, audit tests, and Clang/GCC warning controls pass. | measured with the complete Move closure | `(this commit)` |
| fixed | Metaparse dependent functional value initialization | The retained dependent `T()` initializer reaches constexpr evaluation as an ordinary `argument_list`, while the typed scalar-initializer paths recognized only the other parenthesized initializer node kinds. Both the semantic and template-leaf evaluators now treat that existing structured node as the same zero- or one-argument initializer form. | `pa20/tests/spec/300-constexpr-dependent-value-initialization.t`, seven core C++11 lines at the full constexpr-evaluation owner | `BOOST_METAPARSE_STRING` and `long_string` could not form the enum value produced through the dependent `string_value` initialization chain. The header-free reducer's `value_init<char>()` assertion failed before the fix. | The reducer, PA20 owner checks, exact Metaparse graph, twelve cache modes, configured strict suites, all 23 zero-reparse categories, audit tests, broad report, and Clang/GCC warning controls pass. | measured with the complete Metaparse closure: -4.34% instructions, -8.42% RSS, -9.08% footprint; `/tmp/cppgm-boost-frontier-v2-metaparse-final.json` | `(this commit)` |
| fixed | Metaparse nested member class-template explicit specialization | Qualified explicit-specialization collection looked up `A<int>::B` through a rendered qualified name and missed the retained class-specialization owner. It now resolves the owner from the specialization AST, searches that typed member scope, and retains the node-aware structured lookup as fallback. No source reconstruction, cache, spelling rule, or Boost-specific path is added. | `pa21/tests/spec/300-nested-member-class-template-explicit-specialization.t`, six core C++11 lines at explicit-specialization ownership | `all_headers` and `string` rejected an explicit specialization nested below a concrete outer class-template specialization. The controlled header-free reducer failed with `unknown class template explicit specialization`. | The reducer, focused PA21 check, exact Metaparse graph, twelve cache modes, configured strict suites, all 23 zero-reparse categories, audit tests, broad report, and Clang/GCC warning controls pass. | measured with the complete Metaparse closure | `(this commit)` |
| fixed | Metaparse dependent reference-typedef chain compile time | Reference collection eagerly parsed every dependent typedef in each class-template specialization. Boost.MPL's repeated iterator aliases then recursively rematerialized the full predecessor chain, producing exponential instruction growth without proportional RSS growth. For a dependent typedef declaration whose retained declarators are all explicitly dependent, the collector now structurally converts each typedef specifier/declarator AST into an abstract `type_id`, binds the existing dependent-alias placeholder, and defers ordinary parsing until instantiation. The transformation removes only `typedef` and the declared identifier; it never reparses text. | `pa18/tests/spec/300-dependent-reference-typedef-chain-out-of-class-array.t`, a 20-line / 534-byte three-step C++11 progression at the earliest dependent class-template owner | A 13-alias causal reducer exceeded 60 seconds and consumed hundreds of billions of instructions at roughly 6.5 MiB RSS. After the fix it completes in 0.02--0.03s; the exact `<boost/mpl/string.hpp>` include completes in 3.20s at about 94 MiB with zero swap. | PA18 passes `238/238`; its new reducer has no early-placement finding. Exact Metaparse passes all 241 updates, the post-refactor `all_headers`/`string` replay passes, all twelve cache modes are byte-identical, configured strict suites and the PA9-excluded `4051/4051` report pass, and reparse/audit plus Clang/GCC warning controls are clean. PA18's full conservative placement audit retains two unrelated pre-existing findings. | -4.34% instructions, -8.42% RSS, -9.08% footprint; `/tmp/cppgm-boost-frontier-v2-metaparse-final.json` | `(this commit)` |
| fixed | Logic overloaded constexpr logical and contextual-bool expressions | The generic constant evaluator applied built-in `&&`/`||` short-circuiting before the semantic overloaded-operator hook, while the overload mapper omitted both logical operator names. It also tested `?:` conditions and casts through raw value truthiness, bypassing contextual explicit `operator bool`. Class-valued evaluated operands now enter ordinary typed overload resolution; a scalar short circuit probes the unevaluated operand's semantic type before committing to the built-in result. Conditions and casts keep scalar fast paths and use the existing typed conversion path only when required. The final predicate walks type wrappers without reference-counted handle copies. No text parse or cache is added. | `pa20/tests/spec/300-constexpr-overloaded-logical-operators.t`, 17 lines / 779 bytes with a 106-byte LowIR reference at PA20's full constexpr-evaluation owner | Boost.Logic's `tribool_test.cpp` could not constant-initialize `array_ors` after `false || tribool(false) || false || indeterminate`; the initial eager semantic probe fixed that expression but made the PA21 operator-overload regression time out, so it was discarded. | Exact Logic graph passes all three positive and six deliberate-negative tests. PA20 `86/86`, all configured strict suites, PA9-excluded `4048/4048`, placement/hygiene, all 23 zero-reparse categories, 14 audit tests, twelve cache modes, and Clang 22/GCC 15 warning controls pass. | -3.93% instructions, -8.34% RSS, -8.94% footprint; `/tmp/cppgm-boost-frontier-v2-logic-constexpr-final.json` | `(this commit)` |
| fixed | Log nested function-template local static | Local-static symbols derived ownership only from the immediate block scope, losing the enclosing instantiated function and allowing specializations to collide. Symbol construction now walks to the typed enclosing function binding and includes its specialization identity. | `pa20/tests/general/300-nested-function-template-local-static-array.t`, a compact two-specialization C++11 owner | `util_stp_filter_parser` reused the wrong local static array across instantiated parser helpers before the fix. | The reducer and `util_stp_filter_parser`'s `12/12` runtime checks pass; final strict, broad, cache, audit, warning, and Log closure gates cover the combined change. | measured with the final Log closure: -4.41% instructions, -8.47% RSS, -9.48% footprint | `(this commit)` |
| fixed | Log class-template static reference initialization | Output analysis stripped reference types before classifying required static-member initialization and consequently failed to emit the dynamic initializer for a class-template static reference. It now preserves reference identity long enough to schedule the typed initializer. | `pa18/tests/general/300-class-template-static-reference-dynamic-initialization.t` | `form_named_scope`, `filt_attr`, and `attr_named_scope` reached uninitialized reference storage before the fix. | The PA18 reducer and all three focused Boost runtime families pass; final strict, broad, cache, audit, and warning gates pass. | measured with the final Log closure: -4.41% instructions, -8.47% RSS, -9.48% footprint | `(this commit)` |
| fixed | Log constant global aggregate and class-array initialization | Global lowering recognized only a narrow array shape and otherwise emitted zero storage plus runtime actions, including cases whose fully typed field and element assignments were constant. The backend now resolves constant aggregate subobject offsets and emits sorted data items, with the existing guarded runtime path retained as fallback; class arrays use the same typed fallback when constant emission is unavailable. | `pa15/tests/general/100-global-aggregate-nested-array-initializer.t`, with existing PA16 and PA24 class-array controls | Log formatter and attribute globals were misinitialized before the fix, producing independent runtime-family failures. | The PA15 reducer and affected PA16/PA24 owners pass; final strict, broad, audit, warning, and Log runtime gates pass. | measured with the final Log closure: -4.41% instructions, -8.47% RSS, -9.48% footprint | `(this commit)` |
| fixed | Log inherited virtual-slot identity | Vtable override matching checked only the immediate primary polymorphic base. An inherited slot owned farther up the actual primary-base chain was treated as unrelated, creating the wrong slot/adjustor identity. The typed lookup now walks the complete primary chain. | `pa27/tests/general/100-multibase-inherited-virtual-slot-identity.t`, 12 source lines and a 7,044-byte ABI reference | The reduced class hierarchy and Log runtime dispatch select the wrong inherited virtual target before the fix. | PA27 passes `37/37`; `form_date_time` passes `6/6`; the PA27 large-reference audit minimized the reducible cases and left only the irreducible VTT/destructor lifecycle owners above 10 KiB. | measured with the final Log closure: -4.41% instructions, -8.47% RSS, -9.48% footprint | `(this commit)` |
| fixed | Log inline prior-declaration linkage | A function defined `inline` after a non-inline prior declaration retained the earlier binding's local inline flag, so object output selected strong rather than weak ODR linkage. The shared typed predicate now consults the definition's member specifiers as well as the binding. | `pa29/tests/general/100-inline-prior-declaration-defined-in-separate-objects.t`, a minimal two-object C++11 link reducer | Three Log/Asio example links reported duplicate definitions before the fix. | PA29 passes `75/75`; the reducer and all three focused Boost links pass; final broad, warning, and audit gates pass. | measured at -4.47% instructions, -8.42% RSS, -9.46% footprint in `/tmp/cppgm-boost-frontier-v2-inline-prior-declaration-final-perf.json` | `(this commit)` |
| fixed | Log Phoenix function-pointer inner-pack partial ordering | Transformed function-template construction flattened placeholder values for two independent parameter packs. The first pack could consume the second pack's synthesized placeholder, making structured candidate transformation fail and abandoning partial ordering. The transformer now presents one scalar synthesized entity per original template parameter, including each pack, to ordinary typed substitution. | `pa22/tests/spec/200-function-pointer-inner-pack-partial-ordering.t`, 16 lines / 392 bytes with a 106-byte LowIR reference | `boost::phoenix::bind(&unary_function, 1)` was ambiguous between the function-pointer pattern and generic callable pattern, preventing `extension_app_launcher` from finding its formatter overload. | PA22 passes `294/294`; strict suites pass PA18 `186/186`, PA19 `115/115`, PA21 `162/162`, PA22 `158/158`, PA23 `338/338`; normal, ten individual cache-off modes, and all-off are byte-identical at SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`; all 23 zero-reparse categories, 14 audit tests, Clang/GCC warning controls, the PA9-excluded `4047/4047` report, and focused Boost compile/link pass. | -4.41% instructions, -8.47% RSS, -9.48% footprint; `/tmp/cppgm-boost-frontier-v2-function-pointer-inner-pack-final-perf.json` | `(this commit)` |
| fixed | Log compiler memory | Four strings used only for GNU ext-vector, section, and asm-label annotations were embedded in every `CppAstNode`; adding the ext-vector annotation pushed the node into the allocator's 384-byte size class. The four fields now share a copy-on-write rare payload, preserving value-copy semantics while leaving ordinary nodes payload-free. No semantic lookup, text, cache, symbol, or ABI behavior changes. | Existing PA34 GNU asm, attribute, and ext-vector tests cover every moved field; no new language regression is required for this representation-only change. | `vmmap` on the 1.36 GB `tutorial_attributes` child reports only 2% fragmentation and 1.2 GB of live allocations. `heap` identifies about 705,000 allocations in the 384-byte `CppAstNode` size class, making the rare field cost material rather than allocator noise. | Warning-clean compiler rebuild; seven focused PA34 GNU asm/attribute/ext-vector checks pass; the full PA34 report passes `325/325`; `git diff --check` passes. Record-layout inspection reports `sizeof(CppAstNode) == 296`. | -4.78% instructions, -2.59% RSS, and -1.99% footprint against immutable frozen epoch `9764b3835`; relative to the pre-compaction Phoenix candidate: -0.20% instructions, -2.13% RSS, and -4.20% footprint. Three-run report `/tmp/cppgm-boost-frontier-v2-log-ast-rare-strings-perf.json`; no parent or live header was measured. | `(this commit)` |
| fixed | Log `tutorial_attributes` retained type graph | The dependency classifier used by ABI mangling recursively revisited shared and cyclic `TypePtr` graphs, while fifteen dependent/member/template fields occupied every `Type` even though ordinary types never use them. A per-query identity visit set now traverses each reachable type once, using 32 inline entries before allocating an overflow set. The rare fields moved into the existing copy-on-write named metadata payload, and the const accessor is inline. `sizeof(Type)` falls from 560 to 280 bytes without changing value-copy behavior, semantic lookup, source parsing, cache keys, symbols, ABI output, or language acceptance. | Existing dependent alias, class-template, template-template, qualified-member, member-owner, and ABI suites exercise every moved field; this representation and graph-traversal change needs no large Boost-derived reducer. | The unreduced formatter scales from a one-field expression at about 992 MB to the full nested formatter exceeding 1.5 GB. The retained-memory census counts 187,555 `Type` objects and about 144 MB in that category before compaction. Sampling the pre-guard compiler places 1,077 of 1,378 samples in the repeated dependency walk; after the guard that path falls to 21 of 535 samples. | Warning-clean compiler and ABI builds; `sizeof(Type) == 280`; PA21/PA22 pass `534/534`; PA30 ABI passes `89/89`; all configured strict suites, the PA9-excluded `4041/4041` report, all 23 zero-reparse categories, all 14 audit tests, and `git diff --check` pass. The original Boost source compiles serially and its executable links. | -4.45% instructions, -8.93% RSS, and -9.56% footprint against immutable frozen epoch `9764b3835`; report `/tmp/cppgm-boost-frontier-v2-log-type-rare-inline-perf.json`; no parent or live header was measured. | `(this commit)` |
| fixed | Log Phoenix logical-not callable result | Concrete class-template instantiations that contained nested member templates inherited lexical placeholder maps, and the class collector treated the entire concrete owner as dependent solely because those maps were nonempty. It therefore skipped full member collection for Phoenix's `call_impl_`, losing the instantiated non-constexpr static `call` member and leaving only the zero-argument `error_expecting_arguments` overloads. Concrete source-template identity now wins over leftover lexical placeholders; genuinely dependent instantiations still defer. Structured class-member declaration and definition replay also expands already-bound type packs before parsing function types and non-trailing function parameter packs. The hot path first requires a nonempty bound-pack map. No source text, cache, spelling rule, or Boost-specific path is added. | Existing `pa23/tests/spec/400-function-type-pack-template-argument.t` now also covers the recursive partial specialization, nested member template, non-trailing function parameter pack, inherited static call, and its function-pointer result in a compact C++11 case; the checked-in LowIR reference remains byte-identical | Exact `filter_parser.cpp` and a reduced Phoenix logical-not call retain only zero-argument fallback overloads before the fix. The isolated failing Boost compile uses 962 MB RSS. Clang 22 and GCC 15 accept the reduced C++11 shape. | The focused PA23 direct comparison passes; exact `filter_parser.cpp` compiles in 38.75s at 1.083 GB RSS with zero swap. Configured strict suites pass, the PA9-excluded broad report passes `4033/4033`, PA23/PA27 have no early-placement violation, all 23 text-reparse categories and 14 audit tests pass, all-cache-disabled output is byte-identical at SHA-256 `fb38fe8e693c3c1eec62fd5eba62e4331cbc2360138370fb58fdbc9bd1e7cf50`, and the three changed units compile warning-clean under Clang 22 and GCC 15. | -4.58% instructions, -0.47% RSS, +2.31% footprint against immutable compiler-and-51-header epoch `9764b3835`; three-run report `/tmp/cppgm-boost-frontier-v2-phoenix-final-3run.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Log inherited class-specific allocation call | `new` analysis found an inherited class-specific `operator new` and synthesized a fully qualified callee, but discarded the already-resolved allocated class and enclosing class-template specialization types. General call lookup then repeatedly reconstructed those displayed template-id qualifiers and failed with `[structured anchor no]`. The synthetic callee now carries an explicitly authoritative `TypePtr` for every class component in its qualifier chain; the general resolver trusts only that marked synthetic fact, preserving ordinary source-AST constant folding. Operator-function calls also skip inapplicable value, functional-cast type, and `char_traits` probes. No text parse, cache, spelling special case, ABI path, or Boost rule is added. | `pa18/tests/general/300-class-template-inherited-operator-new-structured-qualifier.t`, a 17-line nested-class-template case that inherits `operator new`; no witness reference was fabricated because the patched-Clang witness producer is absent | The controlled pre-fix compiler rejects `new box<int>::item` with `failed template-id parse in type lookup: box<int> [structured anchor no]`. The initial Log graph repeats the same failure for `attribute_value_impl<...>`, `severity_level<int>`, and other allocation classes. An initially over-broad semantic-type reuse made PA22 emit unnecessary `integral_constant::value` storage; strict direct comparison caught it, and the final authoritative marker removes that drift. | Clang C++11 and the current compiler accept the reducer; focused PA18 and PA22 controls pass. Normal, all ten individual cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `7f6295e8299d63fc889e81eea232fd03a5812a9eaa749e29688d921f70a3e912`. Configured strict suites, all 23 zero-reparse categories, all 14 audit tests, Clang/GCC 15 warning checks, and the PA9-excluded broad report pass `4033/4033`, including PA37 `7/7`. The new test has no placement or hygiene finding; PA18 retains one unrelated pre-existing placement finding. Exact repaired Log units and focused B2 pass with zero swaps. | measured with the current Log closure: -5.51% instructions, -0.50% RSS, +2.36% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-log-structured-qualifier-final-r3.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Log class-specific sized deallocation | A generated deleting-destructor call always invoked class `operator delete` with one argument. C++11 requires the object size when class lookup finds a sized `(void*, size_t)` deallocation function and no unsized alternative. Typed candidate inspection now selects that form and appends the exact class size. | `pa16/tests/general/300-class-specific-sized-delete.t`, reduced to a virtual destructor and the sole two-parameter class deallocation function | The pre-fix generated destructor rejects the sole candidate as having the wrong argument count; Log's `attribute::impl` declares exactly the sized form. | PA16 passes with the focused Log allocation targets; configured strict, broad, audit, and warning gates recorded with the combined closure pass. | measured with the current Log closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Log qualified template-id component under value-name shadow | During initial qualified type parsing, a visible value with the same spelling as the qualified template component could force the component head into expression interpretation, even though `m::flag<sizeof(T) && true>` is unambiguously a type-template-id. A scoped angle-lookup adapter suppresses value preference only for that qualified component head while leaving its template arguments under ordinary lookup. | `pa10/tests/general/200-qualified-base-template-id-logical-argument-value-shadow.t` plus the instantiated PA19 control `200-qualified-base-template-id-logical-argument-value-shadow-instantiation.t` | The real Karma header and `named_scope_format_parser.cpp` fail before the parser retains the qualified template-id; Clang C++11 and the fixed compiler accept the reduced cases. | PA10/PA19 focused and direct reports pass; `named_scope_format_parser.cpp` compiles; configured strict, broad, zero-reparse, audit, and warning gates recorded with the combined closure pass. | measured with the current Log closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Function `stateless_test` class-specific placement new | Class-scope allocation and deallocation functions are static members by language rule even when their declarations omit `static`. The class collector treated such functions as ordinary methods, prepended `this`, and rejected placement new's two explicit arguments against the resulting three-parameter binding. A shared typed-name classifier now supplies the language-mandated static fact to ordinary, reference, dependent, inline/out-of-line definition, function-template, and semantic-output paths. No source text, cache, ABI spelling, or Boost-specific rule is added. | `pa16/tests/general/300-class-specific-placement-new.t`, the existing header-free class-placement-new reducer, now omits the redundant `static` while retaining its checked-in LowIR unchanged | The initial Function graph reports both `stateless_integer_add::operator new` candidates as methods; the placement overload is rejected because its fabricated implicit object cannot bind. The exact strict direct compile of `stateless_test.cpp` reproduces the diagnostic before the fix and succeeds afterward. | PA16 focused and direct reports pass `172/172`; configured strict suites pass; PA16 placement/hygiene has zero findings; all 23 text-reparse categories and all 22 audit/perf tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `af0e40368c07efefe75e5d310e9f5424a17ca1ccc3561e07c5303392910e61b7`. Clang 22 and GCC 15 accept and run the reducer warning-clean, both changed production units compile warning-clean under GCC 15, the PA9-excluded broad report passes `3997/3997`, and the exact final Function graph passes. | -1.26% instructions, +1.71% RSS, +2.23% footprint against immutable frozen compiler-and-51-header epoch `9764b3835`; report `/tmp/cppgm-boost-frontier-v2-function-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Flyweight `test_multictor` initializer-list assignment | Overload ranking treated two `std::initializer_list<T>` parameters as tied after establishing that both were list-initialization sequences. The candidate record now retains the typed standard-conversion rank of each braced element and compares the two element-rank vectors by dominance, so an exact `int` or `unsigned int` element conversion wins over the competing conversion. Pack-expanded braced elements use the existing structured expansion service. No text, cache, symbol, ABI, or Boost-specific path is added. | `pa25/tests/general/200-initializer-list-element-conversion-ranking.t`, a header-free unevaluated assignment reducer at PA25's initializer-list overload owner | The initial graph reports an ambiguous assignment in `test_multictor`; the reduced `decltype(get_box() = {1})` and unsigned counterpart select no unique result before the fix, while Clang 22 and GCC 15 select the exact element type. | PA25 passes `77/77`; the combined PA20/PA25 report passes `161/161`; configured strict suites, PA25 placement/hygiene, all 23 zero-reparse categories, all 22 audit/perf tests, twelve byte-identical cache modes, Clang/GCC warning controls, GCC 15 production builds, the PA9-excluded `3997/3997` report, focused target, and exact final Flyweight graph pass. | measured with the complete Flyweight closure: -1.46% instructions, +1.39% RSS, +2.18% footprint against immutable frozen compiler-and-51-header epoch `9764b3835`; report `/tmp/cppgm-boost-frontier-v2-flyweight-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Flyweight `test_concurrent_factory` typed-initializer recursion | Speculative constant evaluation of nested rvalue construction could route from class typed-initializer evaluation back into the exact same initializer AST and normalized target type. With no semantic progress or cycle check, the compiler recursed for more than 17,000 frames and crashed. A thread-local RAII guard now rejects only exact active `(CppAstNode, Type)` re-entry as a soft constant-evaluation failure, leaving distinct subobjects and target types unaffected. No cache, text, lookup retry, symbol, ABI, or Boost-specific path is added. | `pa20/tests/general/300-nested-rvalue-construction-constexpr-probe.t`, a header-free two-layer rvalue-construction reducer at the PA20 full constant-evaluation owner | LLDB on the exact Boost source records the cycle `evaluate_class_typed_initializer -> evaluate_typed_initializer_value -> evaluator.eval_initializer` repeating for more than 17,000 frames before stack exhaustion. The initial graph also loses the intermodule holder during the same semantic closure; after the termination fix, its qualified `pointer_traits::to_raw_pointer` trace retains both typed member-template candidates and its focused target passes. | PA20 and combined owner reports pass; configured strict suites, PA20 placement/hygiene, all reparse/audit gates, twelve byte-identical cache modes, Clang/GCC controls, GCC 15 production builds, the `3997/3997` broad report, focused concurrent/intermodule targets, and exact final Flyweight graph pass. | measured with the complete Flyweight closure; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-flyweight-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Filesystem qualified overloaded function arguments | Target-directed function-pointer resolution used the unqualified identifier sidecar even when the expression retained a structured qualified name. Both direct target resolution and function-template argument option collection now perform node-based lookup for genuinely qualified syntax while preserving the established unqualified lookup path. | `pa12/tests/general/200-qualified-overloaded-function-argument.t` and `pa22/tests/spec/300-qualified-overload-set-function-pointer-deduction.t` | Boost.Bind's `convenience_test` rejects a qualified overloaded function-id passed to a typed bind target before the fix. The two header-free reducers separate ordinary target conversion from function-template deduction; Clang 22 and GCC 15 accept both. | Focused owner tests, Clang/GCC controls, twelve cache modes per reducer, configured strict suites, and the PA9-excluded `3995/3995` report pass. The final Filesystem graph exits successfully. | measured with the complete Filesystem closure: -1.30% instructions, +1.50% RSS, +2.24% footprint against immutable frozen compiler-and-51-header epoch `9764b3835`; report `/tmp/cppgm-boost-frontier-v2-filesystem-candidate.json` | `(this commit)` |
| fixed | Filesystem ADL using-declaration source points | Namespace ADL collected imported functions without retaining where a using-declaration introduced each binding, so later imports could affect earlier expressions. Function-binding mutation now records the earliest source token for imported bindings, and ADL includes them only after that point while retaining direct declarations from the collapsed namespace. | `pa15/tests/general/300-adl-using-declaration-source-point.t` | Boost.Filesystem operations overloads using `using v4::equivalent` exposed both missing imported ADL candidates and incorrect visibility timing. The reducer checks a call before and after the using-declaration; Clang 22 and GCC 15 agree with the fixed compiler. | PA15 focused/direct checks, placement, all cache modes, strict/reparse/audit gates, warning-clean production builds, broad report, and final Filesystem replay pass. | measured with the complete Filesystem closure; candidate-only frozen report, no parent or live-header command | `(this commit)` |
| fixed | Filesystem range-for ADL local-name isolation | Synthesized range `begin`/`end` calls reused the active value and variable-template maps, allowing a block-local object named `end` to suppress the ADL-only range customization lookup required by the language. The synthesized ADL scope now removes those local non-function bindings before resolving the hidden call. | `pa24/tests/general/100-range-for-adl-ignores-local-end-object.t` | Boost.Filesystem's `operations_unit_test` declares a local `end` value and then uses a range whose customization functions are found by ADL. The header-free reducer fails before the fix and passes Clang 22, GCC 15, and the fixed compiler. | PA24 focused/direct checks, placement, twelve cache modes, configured strict suites, zero-reparse/audit gates, broad report, and final Filesystem graph pass. | measured with the complete Filesystem closure; candidate-only frozen report, no parent or live-header command | `(this commit)` |
| fixed | Filesystem return-SFINAE parameter identity | Function-template result identity mixed retained spelling and position-insensitive structural comparisons. Left/right overloads whose traits bind to opposite template-parameter positions could therefore be merged or misclassified. Return-SFINAE identity now canonicalizes identifiers by declaration position and compares the typed retained patterns, so equivalent renamings match while swapped semantic roles remain distinct; raw decl-spec wrappers for `enable_if`, `disable_if`, and `void_t` participate in the same path. | `pa22/tests/spec/300-return-sfinae-swapped-template-parameter-identity.t` | Dynamic/static path tests and v3 compatibility paths lose the intended left/right trait overload set before the fix. The header-free reducer uses renamed and swapped template parameters to prove that opposite parameter roles remain distinct; Clang 22 and GCC 15 accept it. | PA22 and PA26 reports pass after three PA26 LowIR refs record corrected overload ordinals only; strict, cache, placement, reparse/audit, host-warning, broad, and final Boost gates pass. | measured with the complete Filesystem closure; candidate-only frozen report, no parent or live-header command | `(this commit)` |
| fixed | Filesystem range-for const-reference begin copy | Hidden range-for iterator variables used the helper expression's reference-preserving type directly instead of applying `auto` deduction. A customization returning `const I&` therefore made the hidden iterator object const and rejected its increment. Hidden `begin` and `end` initialization now use the existing value-conversion type, producing the required iterator copy. | `pa24/tests/general/100-range-for-adl-const-reference-begin-copy.t` | Boost.Foreach's Filesystem dependency exposes a `const` iterator increment failure. The header-free reducer returns a const iterator reference and requires the hidden range-for variable to be a mutable copy; Clang 22 and GCC 15 accept it. | PA24 owner checks, twelve cache modes, strict, placement, zero-reparse/audit, production warning, PA9-excluded `3995/3995`, and exact Filesystem replay pass. | measured with the complete Filesystem closure; candidate-only frozen report, no parent or live-header command | `(this commit)` |
| fixed | Exception concrete-owner member result SFINAE | A member-template instantiation could retain placeholder bindings from its primary class lexical scope after its active owner was already a concrete specialization. The final result-SFINAE gate treated that concrete instantiation as dependent and retained an invalid `enable_if<false>` candidate. Non-dependence now recognizes the concrete active owner while preserving placeholder deferral for genuinely dependent owners. No source text, cache, or recovery lookup is added. | `pa22/tests/spec/300-concrete-owner-member-result-sfinae.t` | The initial Boost.JSON build retains disabled `boost::system::result::value()` overloads and reports an ambiguity. The header-free reducer leaves the same invalid concrete-owner member candidate viable before the fix. | Focused reducer and Boost.JSON checks pass; the PA14/PA22/PA26/PA34 owner report passes `760/760`; configured strict suites and the PA9-excluded `3989/3989` report pass. Placement/hygiene, all 23 zero-reparse categories, all 22 audit/perf tests, twelve cache modes, and GCC 15 production build pass. | measured with the complete Exception closure: -1.44% instructions, +1.76% RSS, +2.19% footprint against immutable frozen compiler-and-51-header epoch `9764b3835`; report `/tmp/cppgm-boost-frontier-v2-exception-final-candidate.json` | `(this commit)` |
| fixed | Exception dependent alias and detection-idiom substitution | Alias canonicalization could retain a dependent source syntax under an apparently concrete semantic type, but the stable expansion key recorded only the semantic dependency bit. Structural expansion also checked only aggregate parameter mention, retained dependent alias syntax was not re-resolved after bindings changed, and template-template alias partials skipped the concrete expression recheck. The alias path now preserves per-parameter dependency, includes structured source dependence in its stable key, re-resolves retained dependent alias types, and requests the typed partial recheck for template-template detector patterns. No semantic text is reparsed. | `pa22/tests/spec/300-dependent-validity-alias-category.t` and `pa22/tests/spec/300-template-template-alias-detector-scope.t` | Boost.JSON and nlohmann detection paths select or discard aliases using stale category/scope facts, producing the initial `tag_invoke` and serialization candidate failures. Both header-free reducers fail before the typed dependency repair. | Both reducers, focused Boost.JSON, focused nlohmann, `760/760` owner report, strict suites, PA9-excluded `3989/3989`, four placement audits, 23 zero-reparse categories, 22 audit/perf tests, twelve cache modes per reducer, and GCC 15 production build pass. | measured with the complete Exception closure: -1.44% instructions, +1.76% RSS, +2.19% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-exception-final-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Exception unsigned scoped-enum LowIR lowering | LowIR mapped every named enum to a signed integer spelling and excluded enums from unsigned scalar classification, even when the typed enum retained an unsigned underlying type. Memory types, scalar types, comparison selection, and integer/float conversion helpers now derive signedness from the exact enum underlying `TypePtr`. | `pa14/tests/general/200-scoped-enum-unsigned-high-bit.t`, with corrected existing PA14/PA24 LowIR references | The controlled reducer lowers the high-bit enumerator through signed operations before the fix, while Clang and GCC preserve the unsigned value. Existing enum LowIR references expose the same signedness mismatch. | PA14 owner tests pass within the `760/760` combined report; strict, broad, placement, reparse, audit, cache, GCC 15, focused Boost, and final forced Exception gates pass. | measured with the complete Exception closure: -1.44% instructions, +1.76% RSS, +2.19% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-exception-final-candidate.json` | `(this commit)` |
| fixed | Exception derived/base pointer equality adjustment | Equality compatibility recognized a unique public class-hierarchy relationship but left both operands at their original pointer types. LowIR therefore compared raw addresses instead of applying the required derived-to-base pointer adjustment, which is observable for a non-primary base. The expression analyzer now performs the existing typed inheritance conversion on the derived operand, preserving the combined pointee cv qualifiers before ordinary comparison lowering. | `pa26/tests/general/100-derived-nonprimary-base-pointer-equality.t`, with corrected PA15/PA17 LowIR references | The header-free multiple-inheritance reducer compares a derived address with its non-primary base address and fails before the adjustment; Clang and GCC return true. Existing covariance/reference controls record the newly explicit adjustment. | PA26 owner tests pass within `760/760`; strict, PA9-excluded `3989/3989`, placement, all 23 zero-reparse categories, all 22 audit/perf tests, twelve cache modes, GCC 15 production build, and final Boost graph pass. | measured with the complete Exception closure: -1.44% instructions, +1.76% RSS, +2.19% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-exception-final-candidate.json` | `(this commit)` |
| fixed | Exception inline elaborated template-argument display | The elaborated-type keyword service returned tokens with an embedded trailing space even though structured renderers already own separator insertion. An inline declaration such as `wrapper<struct inline_tag_, int>` therefore contaminated retained template-argument display and `__PRETTY_FUNCTION__` normalization. The service now returns the exact keyword token without presentation whitespace. | `pa34/tests/run/800-pretty-function-inline-elaborated-template-argument-run.t` | The runtime reducer observes the doubled/retained elaborated spacing before the fix; the nlohmann-heavy Exception target exercises the same template-display path. Clang and the fixed compiler produce the normalized inline-tag spelling expected by the hosted lane. | PA34 runtime and owner report, strict suites, PA9-excluded `3989/3989`, placement/hygiene, all 23 zero-reparse categories, all 22 audit/perf tests, twelve cache modes, and both final nlohmann and full Exception graphs pass. | measured with the complete Exception closure: -1.44% instructions, +1.76% RSS, +2.19% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-exception-final-candidate.json` | `(this commit)` |
| fixed | Exception losing conversion-template body and symbol laziness | User-defined conversion ranking instantiated every conversion-function-template body before knowing which candidate won. Invalid bodies on worse candidates could therefore escape SFINAE, and building full weak Itanium object symbols for all lookup-only candidates made the nlohmann stress target unnecessarily expensive. Candidate discovery now instantiates lookup-only, the selected conversion acquires its definition through the existing strict upgrade path after ranking, and lookup-only weak bindings defer object-symbol construction until that upgrade. Selected bodies and ABI symbols remain mandatory; only losing candidates stay lazy. | `pa22/tests/general/300-worse-conversion-function-template-body-not-instantiated.t` | The reducer fails when a worse conversion template's body is instantiated. The initial nlohmann target cannot complete its conversion/serialization path; the final focused target compiles, links, and runs in 678.91s. | Focused reducer, selected-conversion runtime controls, all twelve cache modes, `760/760` owner report, configured strict suites, PA9-excluded `3989/3989`, placement/reparse/audit gates, isolated GCC 15 production build, focused nlohmann, and final 217-target Exception graph pass. | measured with the complete Exception closure: -1.44% instructions, +1.76% RSS, +2.19% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-exception-final-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Dynamic Bitset deferred nested and injected-owner member definitions | The template collector queried the wrong syntax node for a conversion operator, did not inherit the enclosing class template as the owner of a nested class definition, discarded `= default`/`= delete` when storing special-member definitions, and rejected a repeated injected-class qualifier such as `Iter<T>::Iter`. Stored member-definition facts now retain the exact qualified syntax, output owner, and default/delete state; instantiation replay applies those facts to the concrete binding. Repeated injected-class lookup is guarded by equality of the final two typed qualifier components before resolving the reduced owner. | `pa18/tests/spec/300-nested-out-of-class-member-definition-forms.t` | All five initial Dynamic Bitset failures compile but link with undefined references to `dynamic_bitset<...>::reference` copy construction/conversion and `const_bit_iterator<...>::operator++/--`; log `/tmp/boost-frontier-v2-suite-036-initial-forced.log`. The header-free reducer linked the same three missing forms while Clang and GCC accepted and ran it. | PA18 `235/235`; focused regression; exact final Boost graph; strict suites; all 23 zero-reparse categories; all 22 audit/perf tests; normal, ten individually cache-disabled, and all-disabled LowIR byte-identical at SHA-256 `915ea4a0fa352f880844284def27a63004e1548970fd5d8cc17ca111a3a760ad`; host controls; GCC 15 warning checks; PA9-excluded `3982/3982`. | +0.96% instructions, +0.11% RSS, +0.45% footprint against immutable frozen compiler-and-51-header epoch `9764b3835`; candidate-only report `/tmp/cppgm-boost-frontier-v2-dynamic-bitset-candidate-guarded.json`; rejected unguarded fallback recorded separately at +12.86% instructions | `(this commit)` |
| fixed | DLL C-linkage plugin variables and GNU weak definitions | A definition outside an `extern "C"` block did not inherit a prior declaration's C linkage, and GNU `weak` attributes were parsed without affecting symbol linkage. Variable redeclaration now preserves prior C linkage unless a new internal-linkage declaration overrides it, while the retained attribute fact selects weak object linkage through semantic output and native emission. | `pa32/tests/general/200-extern-c-variable-definition-inherits-prior-linkage.t`, with object inspection for the unmangled weak symbol | The initial graph rejects or mislinks `static_plugin`, `load_all`, and library-info plugin objects; focused evidence is in `/tmp/boost-frontier-v2-suite-035-c-linkage-full-forced.log` and `/tmp/boost-frontier-v2-suite-035-library-info-gnu-weak-fixed.log`. | PA32 owner tests, object inspection, host controls, strict/audit/cache/GCC gates, PA9-excluded `3981/3981`, and the final exact DLL graph pass. | measured with the combined DLL closure: +0.96% instructions, +0.96% RSS, +0.39% footprint against immutable epoch `9764b3835`; candidate-only report `/tmp/cppgm-boost-frontier-v2-dll-candidate-scoped-lookup-final.json` | `(this commit)` |
| fixed | DLL local function-type typedef references | A block-scope typedef naming a function type was parsed as though its declarator introduced a function declaration, so later references could not form the intended function pointer/reference type. Declarator classification now retains the local typedef's function-type identity. | `pa14/tests/general/200-local-function-type-typedef-reference.t` | `tutorial8_static` and related objects fail on the local function typedef before the fix; focused and full evidence is in `/tmp/boost-frontier-v2-suite-035-local-function-typedef-focused.log` and `-full-forced.log`. | PA14 owner, host controls, strict/audit/cache/GCC, broad, and final DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL member template-id after `.`/`->` | Member-expression parsing retained the member identifier but dropped its explicit template-id sidecar when the owner also had a value of the same spelling. The parser and expression path now preserve and resolve the exact member-template syntax before ordinary value fallback. | `pa21/tests/spec/300-member-template-id-not-blocked-by-owner-value-name.t` | `validate_link` and plugin targets report an unknown/invalid member template; focused evidence is `/tmp/boost-frontier-v2-suite-035-validate-link-member-template-fixed.log` and the full replay `/tmp/boost-frontier-v2-suite-035-member-template-full-forced.log`. | PA21 owner, strict/audit/cache/GCC, broad, and exact DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL `sizeof` local value shadowing a type name | The `sizeof(identifier)` path preferred an eagerly known type over the nearer block-scope value with the same spelling. Typed lexical lookup now selects the local value first and applies expression `sizeof`; type-form lookup remains unchanged when no value hides it. | `pa14/tests/general/100-sizeof-local-value-shadows-type-name.t` | `getting_started` fails its size computation before the fix; focused and full evidence is in `/tmp/boost-frontier-v2-suite-035-getting-started-sizeof-fixed.log` and `-sizeof-full-forced.log`. | PA14 owner, host controls, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL string literal to mutable `void *` conversion | Array-to-pointer decay lost pointee constness while ranking conversion to `void *`, incorrectly accepting a string literal as mutable storage. The typed conversion path now preserves element cv through decay and rejects the qualification-removing conversion. | `pa15/tests/general/200-string-literal-does-not-convert-to-mutable-void-pointer.t` | `shared_library_errors` selects an invalid overload before the fix; focused evidence is `/tmp/boost-frontier-v2-suite-035-shared-library-errors-void-cv-fixed.log`. | PA15 owner, Clang/GCC controls, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL delete through pointer-to-const | Delete analysis rejected any cv-qualified pointee even though deleting a pointer to const object is well-formed. The typed delete path now removes top-level cv only for destruction/deallocation target classification while preserving the original pointer value and destructor semantics. | `pa16/tests/general/300-delete-through-pointer-to-const.t` | `shared_library_errors` rejects a valid cleanup path before the fix; focused and graph evidence is `/tmp/boost-frontier-v2-suite-035-shared-library-errors-delete-const-fixed.log` and `-delete-const-full-forced.log`. | PA16 owner, host controls, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL large aggregate by-value host ABI | The x86-64 host ABI classifier treated every aggregate as at most two direct eight-byte chunks, so classes larger than 16 bytes were passed in registers instead of by memory. Typed ABI classification now assigns the memory class before chunk construction, and lowering/object emission use the resulting indirect argument consistently. | `pa32/tests/general/200-host-large-class-by-value-memory-argument.t`, linked against a host-compiled provider | Library-info parsing crosses the ABI boundary with corrupted aggregate arguments before the fix; focused evidence is `/tmp/boost-frontier-v2-suite-035-library-info-memory-class-fixed.log`. | PA32 cross-compiler runtime owner, LowIR reference corrections, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure; the final classifier avoids duplicate large-class triviality work | `(this commit)` |
| fixed | DLL GNU section attribute emission | GNU `section("...")` syntax was accepted but discarded before semantic symbol registration. The parser now stores the exact attribute payload as an AST fact, semantic output carries it through LowIR/MIR, and Mach-O emission assigns the requested section without source-text recovery. | `pa32/tests/general/200-host-gnu-section-attribute.t`, with object-section inspection | `library_info_test` cannot find the plugin metadata section before the fix; evidence is `/tmp/boost-frontier-v2-suite-035-library-info-custom-section.log` and `/tmp/boost-frontier-v2-suite-035-full-after-section.log`. | PA32 inspect owner, object expectation harness, host controls, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL direct-list initialization with deleted move | Direct-list initialization of a same-type prvalue performed an unnecessary move-constructor availability check after constructing the result object directly. The lifetime path now records the typed guaranteed direct construction and does not require the deleted move. | `pa16/tests/general/300-direct-list-init-does-not-require-move.t` | The concurrent-load object rejects a direct-list temporary with deleted move before the fix; evidence is `/tmp/boost-frontier-v2-suite-035-concurrent-direct-list.log`. | PA16 owner, Clang/GCC controls, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL `__builtin_invoke` function-pointer argument conversion | Structured builtin invocation required an exact function-pointer parameter match and skipped ordinary user-defined conversion for a class argument. It now reuses typed call conversion analysis for the selected function pointer before result computation and lowering. | `pa34/tests/run/800-builtin-invoke-user-defined-argument-conversion-run.t` | The concurrent-load wrapper drops the callable path before the fix; focused evidence is `/tmp/boost-frontier-v2-suite-035-concurrent-invoke-conversion.log`. | PA34 hosted runtime owner, Clang control, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL `__PRETTY_FUNCTION__` class-owner template arguments | Predefined function text rendered only function-template arguments, omitting the enclosing class specialization's bound owner arguments. Rendering now appends typed owner bindings first and function bindings second. | `pa34/tests/run/800-pretty-function-owner-template-arguments-run.t` | `cpp_mangling` observes incomplete parser text before the fix; evidence is `/tmp/boost-frontier-v2-suite-035-cpp-mangling-pretty-owner.log`. | PA34 runtime owner, host control, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL qualified namespace lookup at the source point | Eager namespace collection made a later namespace declaration hide an enclosing namespace selected at the earlier use. Namespace bindings now retain their first token in lazy metadata, and qualified simple-type lookup applies source-point filtering only when that metadata proves it necessary. Ordinary qualified namespace type lookup separately follows nominated using-directives, closing libc++ `std::string_view` without broad source-point traversal. | `pa12/tests/general/200-enclosing-namespace-qualified-type-before-later-shadow.t`; the exact Boost `path_source_traits<std::string_view>` specialization is the nominated-namespace integration control | The reduced PA12 case selects the later inner namespace before the fix. An over-broad first implementation passes correctness but raises instructions by 13.02%; an over-narrow prefilter then makes every DLL compile reject `std::string_view`. `/tmp/boost-frontier-v2-suite-035-final-post-opt-forced.log` and `/tmp/boost-frontier-v2-suite-035-final-post-lookup-fix-forced.log` record both boundaries. | PA12 owner, strict/audit/cache/GCC, PA9-excluded `3981/3981`, final DLL graph, and final frozen perf pass. | +0.96% instructions after lazy metadata, prefiltering, and consumer-scoped using-directive lookup; the rejected broad fallback measured +1.03% | `(this commit)` |
| fixed | DLL qualified class type-pack expansion | A pack expansion whose type parameter was reached through a qualified class owner substituted the owner but did not expand the selected member type pack. Structured substitution now carries the qualified member pack binding and emits one exact type per pack element. | `pa22/tests/general/300-qualified-member-type-pack-parameter-expansion.t` | DLL template-method/mangling paths retain an unexpanded qualified pack before the fix; the reduced owner fails while Clang/GCC accept it. | PA22 owner, all twelve cache modes, host controls, strict/audit/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL pack-expanded explicit member-template arguments | Explicit template arguments on a member call retained type-id sidecars for a pack expansion but substituted only the first directly recognized shape. Pack expansion now rewrites the retained type-id AST for every bound element before ordinary typed template argument resolution. | `pa22/tests/general/300-pack-expanded-explicit-member-template-arguments.t` | DLL template-method/import paths fail to instantiate the explicit member-template pack before the fix; the header-free owner isolates the same retained syntax. | PA22 owner, all twelve cache modes, Clang/GCC controls, strict/audit/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | DLL Itanium cv-qualified member symbol order | Typed ABI construction encoded a const-volatile member function as `KV`, while Itanium canonical order is `VK`. Member-function qualifier emission now uses the canonical volatile-then-const sequence consistently for declarations, definitions, references, and RTTI facts. | `pa30/tests/abi/200-member-function-cv-qualifier-order.t` | DLL C++ mangling/link tests disagree with host symbols before the fix; existing PA15, PA16, PA19, and PA25 references expose the same ordering drift. | PA30 ABI owner, host symbol controls, intentional reference updates, strict/audit/cache/GCC, broad, and DLL gates pass. | measured with the combined DLL closure | `(this commit)` |
| fixed | CRC `crc_test` and `crc_test2` dependent non-type parameter types | When a dependent non-type parameter type was resolved in a use scope that already bound that same parameter, structured substitution correctly made earlier parameters such as `Bits` concrete but also rewrote the target declarator's declared identifier, such as `Poly`, to its bound value. The declarator then became structurally invalid. Substitution now masks only the retained declarator's declared-identifier AST node, substitutes all type and expression syntax normally, and restores that exact typed node before parsing the declarator. No source or semantic text is parsed, and no cache, spelling rule, semantic scope, ABI path, or Boost-specific behavior is added. | `pa19/tests/general/100-dependent-nontype-parameter-declarator-use-scope-shadow.t`, a header-free reducer at PA19's dependent non-type parameter owner | `/tmp/boost-frontier-v2-suite-030-crc-test2-trace.log` shows `Bits=32` and successful `uint_t<32>` resolution before explicit argument resolution rejects `TruncPoly`; temporary typed tracing isolated the later failure to the declarator after its declared identifier was captured by the existing `Poly=7` binding. The header-free reducer fails before the fix, while Clang 22 and GCC 15 accept it warning-clean. | PA19 passes `143/143`; all configured strict suites pass; placement/hygiene, all 23 zero-reparse categories, all 14 audit tests, twelve cache configurations with byte-identical LowIR SHA-256 `b7174816a29666b52d65b02c1a7daee28929bce00d2daede8727bf19e4df716a`, Clang/GCC warning checks, and the PA9-excluded `3966/3966` report pass. The exact final CRC graph rebuilds all 26 requested targets and passes every compile/link/runtime action. | +0.82% instructions, -1.53% RSS, and -2.31% footprint against frozen compiler-and-51-header epoch `9764b3835`; candidate-only report `/tmp/cppgm-boost-frontier-v2-crc-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Coroutine `test_asymmetric_coroutine` forwarding constructor | For an out-of-class member-template definition of an explicit class specialization, the collector treated the sole member-template head as though it described the already-specialized owner primary. Owner binding then paired the owner's explicit `void` argument with member parameter `Fn`, overwriting the correctly deduced function-reference type and producing `void&&`. An explicit-specialization owner now contributes zero typed owner template parameters, leaving the definition's head exclusively attached to the member template. No source text, cache, spelling rule, ABI path, or Boost-specific behavior is added. | `pa21/tests/general/300-explicit-specialization-out-of-class-member-template-owner.t` covers an ordinary member template; `pa22/tests/general/300-explicit-specialization-out-of-class-constructor-template-owner.t` covers forwarding-constructor deduction | `/tmp/boost-frontier-v2-suite-029-asymmetric-template-trace.log` records correct deduction of `void (&)(pull_coroutine<void>&)` followed by the erroneous owner rebinding `Fn=void`; the initial exact graph fails `test_asymmetric_coroutine` at `push_coroutine<void> coro(f12)`. Clang 22 and GCC 15 accept and run both reducers warning-clean. | Focused regressions and the PA21/PA22 `513/513` report pass; all configured strict suites pass; placement/hygiene, all 23 zero-reparse categories, all 14 audit tests, twelve cache configurations with byte-identical output, Clang/GCC warning checks, and the PA9-excluded `3965/3965` report pass. The focused and exact full forced Coroutine graphs pass compile, link, and runtime. | +0.77% instructions, -1.27% RSS, and -2.30% footprint against frozen compiler-and-51-header epoch `9764b3835`; candidate-only report `/tmp/cppgm-boost-frontier-v2-coroutine-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Core constexpr object address and `this` base references | Constant lookup conflated an object's stored scalar value with the independently valid constant address of its storage, so mutable static objects were either read illegally or rejected when only their address was needed. The constant domain now has an explicit addressable value, direct constexpr calls preserve its lvalue category, and `this` plus `*this` use the same typed object identity. Failed built-in scalar operations on address-only operands no longer speculatively enter overloaded-operator resolution unless a class or enum operand can actually select one. | `pa20/tests/general/400-constexpr-mutable-static-object-address.t`, `400-constexpr-mutable-static-object-read-bad.t`, `400-constexpr-derived-this-base-reference.t`, and `400-constexpr-derived-this-runtime-base-reference.t`; the corrected existing `400-constexpr-local-default-object-member-call.t` oracle also passes | Core rejected valid address-only mutable-object and derived-`this` cases, while speculative `p != 0` fallback could mutate semantic state and runtime base-reference materialization could recurse. The negative mutable-object read remains rejected. | PA20 and the combined owner report pass; all twelve cache modes emit byte-identical output for the positive reducers and reject the negative reducer; strict, reparse, placement, GCC 15, broad, focused, and exact full-Core gates pass. | measured with the combined Core closure: +0.80% instructions, -1.21% RSS, -2.35% footprint against frozen compiler-and-51-header epoch `9764b3835` | `(this commit)` |
| fixed | Core deleted-constructor overload selection | Constructor candidate collection discarded deleted constructors before ranking, allowing a worse viable constructor to win. Deleted constructors now participate in normal typed ranking and are rejected only when the selected best candidate is finalized. | `pa16/tests/general/200-deleted-constructor-selected-over-viable-bad.t` | The pre-fix compiler accepted a construction whose best overload was deleted; Clang and GCC reject it. | PA16 and the combined owner report pass; the negative reducer is rejected in all twelve cache modes; strict, reparse, placement, GCC 15, broad, focused, and exact full-Core gates pass. | measured with the combined Core closure | `(this commit)` |
| fixed | Core private-base `static_cast` access | Typed inheritance conversion validated that the source and target were related but did not enforce base accessibility for explicit `static_cast`. The cast path now applies the semantic class model's access result, while member context retains access to its own private base. | `pa15/tests/general/200-private-base-static-cast-bad.t` and `200-private-base-static-cast-member.t` | The pre-fix compiler accepted an external cast through a private base; host compilers reject it while accepting the same conversion in member context. | PA15 and the combined owner report pass; positive output is byte-identical and the negative reducer is rejected in all twelve cache modes; strict, reparse, placement, GCC 15, broad, focused, and exact full-Core gates pass. | measured with the combined Core closure | `(this commit)` |
| fixed | Core lazy class-scope `static_assert` | Class-scope assertions were evaluated only on witness-oriented collection paths, so ordinary lazy class completion could omit a failing assertion. Reference collection now evaluates the assertion in the class semantic scope regardless of witness emission. | `pa19/tests/general/100-class-scope-static-assert-expression-bad.t` | Core instantiated the class without rejecting its false class-scope assertion; host compilers reject it. | PA19 and the combined owner report pass; the negative reducer is rejected with caches enabled and disabled; strict, reparse, placement, GCC 15, broad, focused, and exact full-Core gates pass. | measured with the combined Core closure | `(this commit)` |
| fixed | Contract functional-cast lookup | An unqualified functional cast inside a nested lexical scope could bind an outer value declaration before considering the nearer type declaration. The initial parser's structured type candidate now performs typed lexical hiding before value fallback. | `pa18/tests/general/100-functional-cast-argument-nested-type-hides-outer-value.t` | The Contract reducer selected the outer value and rejected the cast; host compilers select the nested type. | Included in the full Contract and repository validation recorded in suite row 25; normal and all cache-disabled outputs are byte-identical. | measured with the combined Contract closure: +0.52% instructions, -1.38% RSS, -2.43% footprint against frozen epoch `9764b3835` | `(this commit)` |
| fixed | Contract chained member-template lambda identity | Captureless lambdas in separate nested member-template instantiations could share one synthesized closure anchor. Closure identity now includes the exact semantic instantiation context. | `pa21/tests/general/300-nonprimary-chained-member-template-lambda-anchor-collision.t` | The second Contract instantiation collided with the first closure's generated identity. | Included in suite row 25 validation; the host-linked reducer passes. | measured with the combined Contract closure | `(this commit)` |
| fixed | Contract destructor body and unwind cleanup | Destructor lowering omitted destruction of body locals before member/base teardown and did not progressively expose remaining subobject cleanup while a generated destructor call could throw. Destructor body scopes are explicit, generated member/base actions use stable snapshots, and EH suffix cleanup advances after each completed action. | `pa15/tests/general/200-destructor-body-local-before-base-destruction.t`, `pa25/tests/general/200-destructor-body-unwind-runs-base-destruction.t`, and `pa25/tests/general/200-destructor-subobject-unwind-runs-later-base-destruction.t` | Contract's throwing destructor cases skipped required later base cleanup; focused traces also exposed cleanup-vector invalidation as emitted destructor statements registered temporaries. | Included in suite row 25 validation; focused throwing-body and virtual throwing-body targets pass. | measured with the combined Contract closure | `(this commit)` |
| fixed | Contract C++11 elision access | The C++11 elided copy/move path skipped semantic accessibility validation once code generation omitted the constructor call. Elision still avoids emission, but now validates the selected constructor and destructor. | `pa16/tests/spec/100-elided-private-copy-constructor-still-required-bad.t` | The deliberate private-copy Contract case compiled when both host compilers reject it for access. | Included in suite row 25 validation; the negative owner test and host controls reject for the expected reason. | measured with the combined Contract closure | `(this commit)` |
| fixed | Contract member-pointer access SFINAE | Access checking for substituted pointer-to-member expressions lacked the typed naming class needed for protected-member rules. Lookup now carries the naming class and applies public/private/protected access during substitution, including the derived-object restriction. | `pa26/tests/general/300-member-pointer-access-substitution-failure.t` | Contract's inaccessible member-pointer probes remained viable instead of failing substitution. | Included in suite row 25 validation; `virtual_access_multi` passes. | measured with the combined Contract closure | `(this commit)` |
| fixed | Contract literal branch lowering | LowIR short-circuit folding treated any expression carrying integer metadata as a literal; field offsets use the same metadata slot, so nonliteral field conditions were folded. Folding is now restricted to direct literal AST nodes while true literal `&&`/`||` branches still omit unreachable calls. | `pa14/tests/general/200-literal-logical-short-circuit-omits-unreachable-call.t` and `pa15/tests/general/200-nonliteral-field-condition-not-folded.t` | A Contract virtual test lost its runtime branch because a field offset was read as a constant condition. | Included in suite row 25 validation; `virtual_sparse` passes. | measured with the combined Contract closure | `(this commit)` |
| fixed | Contract inherited virtual receiver adjustment | Virtual dispatch reapplied the selected base adjustment from the original object even though the implicit-object conversion had already produced the call subobject. The call now computes its receiver relative to that converted subobject. | `pa27/tests/general/100-inherited-virtual-base-dispatch-uses-converted-receiver.t` | Contract's inherited virtual-base call dispatched through the wrong receiver address. | Included in suite row 25 validation; focused virtual targets pass. | measured with the combined Contract closure | `(this commit)` |
| fixed | Contract i128 literal call ABI | Machine call lowering emitted one literal operand for an i128 argument instead of the ABI's two register-sized chunks. Literal i128 arguments now follow the existing typed call-chunk layout. | `pa28/tests/behavior/800-call-i128-literal-abi-chunks.t` | A Contract link/runtime path received a malformed wide literal argument. | Included in suite row 25 validation; native owner test passes. | measured with the combined Contract closure | `(this commit)` |
| fixed | Contract using-imported base overload filtering | Member lookup retained a using-imported base function after finding a direct derived declaration with the same signature. The typed candidate set now removes that hidden base declaration while preserving genuine overloads. | `pa15/tests/general/300-using-base-static-same-signature-derived-preferred.t` | Contract selected or diagnosed the hidden base static function instead of the direct derived member. | Included in suite row 25 validation; host and compiler reducers return the same result. | measured with the combined Contract closure | `(this commit)` |
| fixed | Contract local-lambda RTTI name | Hosted RTTI emission lacked a stable fallback name for a lambda local to an instantiated function template. The existing typed closure identity now supplies that local RTTI name. | `pa33/tests/general/200-host-template-local-lambda-rtti-name.t` | Contract's host-linked RTTI target missed the local lambda type name. | Included in suite row 25 validation; hosted compile/link/runtime owner test passes. | measured with the combined Contract closure | `(this commit)` |
| fixed | Container Hash sequence-container equality | Structured callable-object invocation stopped at the first viable `operator()` binding. Once an earlier `char` specialization existed, it could therefore supply the result for a later `int` invocation despite the exact `int` conversion. The structured path now ranks every viable binding with the shared typed standard-conversion, reference-binding, qualification, and implicit-object cv rules, preserving the prior order only for ties. No text, cache, ABI, or Boost-specific path is added. | `pa22/tests/spec/300-using-member-decltype-invoke-cross-specialization.t`, at PA22's function-template result/substitution owner | The header-free reducer first instantiates the `char` member alias, then observes the pre-fix compiler choosing that result for `int`. In Boost, `hash_list_test` and `hash_deque_test` each report 39 equality errors and `hash_set_test` reports 87; all three focused targets pass after typed ranking. | PA22 passes `275/275`; all configured strict suites pass; PA22 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. Clang 22 accepts the builtin reducer warning-clean, both changed production units compile warning-clean under GCC 15, and the exact final Container Hash graph passes. | measured together with the adjacent dependent-result fix: instructions +0.10%, max RSS -3.07%, peak footprint -2.49% against immutable compiler-and-51-header baseline `9764b3835`; candidate-only report `/tmp/cppgm-boost-frontier-v2-container-hash-candidate.json`; no parent or live-header command was run | `(this commit)` |

| fixed | Container Hash `is_range_test4` return SFINAE | Function-result substitution considered a materialized outer class specialization sufficient to skip source-pattern rebinding, even when a non-type argument still came from a dependent expression. Its unqualified wrapper trait could then be resolved through the wrong surrounding specialization metadata instead of the function template's lexical scope, making an intentionally missing member type appear valid. The compiler now detects such dependent non-type arguments in the retained AST and rebinds the copied typed source pattern in the declaration/instantiation scope. No semantic text, cache, spelling heuristic, or Boost-specific rule is added. | `pa22/tests/spec/300-dependent-nontype-result-lexical-template-lookup-sfinae.t`, a header-free declaration-scope return-SFINAE reducer at PA22's function-template substitution owner | The exact pre-fix trace substitutes the intended `void*` argument but resolves the unqualified `iterator_traits<void*>` to the outer namespace's primary instead of the local wrapper's empty explicit specialization, producing a true trait. Boost consequently reports both `X1` and `X2` as ranges. The fixed trace resolves the wrapper specialization and drops the candidate; Clang 22 and GCC 15 accept the reducer warning-clean. | PA22 passes `275/275`; all configured strict suites pass; PA22 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. Both changed production units compile warning-clean under GCC 15, and the exact final Container Hash graph passes. | measured together with the adjacent callable-ranking fix: instructions +0.10%, max RSS -3.07%, peak footprint -2.49% against immutable compiler-and-51-header baseline `9764b3835`; candidate-only report `/tmp/cppgm-boost-frontier-v2-container-hash-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | `libs/accumulators/test//count` dependency build | The initial parser treated an unknown qualified template suffix before `;` as expression-ambiguous even after an elaborated class-key. `parse_class_specifier` now selects the existing typed qualified-name mode that permits the final template-id, preserving `QualifiedName`, `TemplateIdSyntax`, qualifier template syntax, and qualifier type syntax on the AST node. | `pa21/tests/general/300-qualified-friend-class-template-id.t`, placed at the `template.friend` owner `pa21:300` | Non-STL reducer and Boost `friend class detail::interface_iarchive<Archive>;` both failed during initial parse; parser trace rejected the class member at `KW_FRIEND`. Focused pre-fix log: `/tmp/boost-v2-acc-count-parser-frontier.log`. | Warning-clean build; reducer accepted by Clang and `cppgm++`; PA10/PA21 direct LowIR report `352/352`; PA21 strict `216/216`, compared `161`, failures `0`; placement audit marks all reducer features `ok`; all 23 text-reparse categories remain zero and 14 audit tests pass; forced Boost rerun clears the parser diagnostic and builds Serialization. | instructions +0.17%; max RSS +1.66%; peak footprint +0.00%; pass | `(this commit)` |
| fixed | `libs/accumulators/test//count` | Substitution had already attached the concrete `support::empty_list` type to `Next` through `qualifier_type_syntax`, but both qualified function lookup paths discarded that semantic type and tried to look up its fully qualified rendering as one name component. `lookup_function_templates_node` and `lookup_functions_node` now consume `cppast_qualifier_type_syntax(...)->semantic_type` first and retain the existing structured template-id/type lookup only as a fallback. This corrects the uncached algorithm; no cache, text parser, or spelling special case is added. | `pa21/tests/general/300-qualified-owner-static-template-nttp.t`, placed at the `template.member_template` owner `pa21:300` | Exact Boost-header reducer `/tmp/cppgm-boost-v2-parameter-arg-list.cpp` and non-STL reducer `/tmp/cppgm-boost-v2-qualified-dependent-static-template.cpp` failed resolving `mp_bool`/`bool_constant`. Semantic trace showed the substituted qualifier type was present while call lookup reported `template_count=0` only for the namespace-qualified concrete owner. | Warning-clean compiler build; exact Boost and non-STL reducers accepted by `cppgm++`, and the non-STL reducer accepted by Clang; focused PA21 check passes; direct PA21 report `217/217`; PA21 strict `217/217`, compared `161`, failures `0`; placement audit reports zero findings and marks all reducer features `ok`; all 23 text-reparse categories remain zero and 14 audit tests pass; forced single-job `count` rebuild compiles `count.o` and advances to Boost.Test `format_report`. | instructions -0.08%; max RSS +1.15%; peak footprint -0.01%; pass, below hotspot threshold | `(this commit)` |
| fixed | `libs/accumulators/test//count` Boost.Test dependency | Lookup found `format_report`, deduction succeeded, and the specialization signature was concrete, but typed symbol construction could not represent the semantic `__builtin_va_list` type and discarded the only candidate. The symbol adapter now maps the builtin through `abi_mangle::Type`: on x86_64 the underlying type is typed as `__va_list_tag[1]`, with the required function-parameter adjustment to `__va_list_tag *`; supported non-x86 host forms are also selected structurally. A separate function-parameter adapter prevents the incorrect shortcut of treating every `va_list` use as a pointer. The PA30 model already provides the necessary typed array, pointer, named-type, and standard-namespace primitives, so no scaffold extension or raw mangled fragment is added. | `pa34/tests/compile/500-builtin-va-list-function-template-symbol.t`, placed in the PA34 host-builtin compile cluster | Non-STL reducer `/tmp/cppgm-v2-format-report-min.cpp` failed after successful template deduction with `failed to build ABI IR function symbol for weak function tools::format_report`; symbol trace identified the `__builtin_va_list` parameter. Clang emits `P13__va_list_tag` for a parameter and `A1_13__va_list_tag` for a template type argument on x86_64. | Warning-clean build; non-STL parameter and type-argument symbols match Clang byte-for-byte; exact Boost.Test `test_tools.cpp` compiles; focused PA34 check passes; direct PA34 report `309/309`; all configured strict suites pass; placement audit reports zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass; forced single-job `count` rebuild compiles the complete Boost.Test archive and leaves only the two Regex objects failing. | instructions -0.11%; max RSS +0.22%; peak footprint -0.03%; pass, below hotspot threshold | `(this commit)` |
| fixed | `libs/accumulators/test//count` Boost.Regex dependency | The parsed `&matcher<A,B,C>::first` node retained its structured qualifier syntax, but two normal semantic consumers discarded the node: the eager initializer constant probe and target-aware overloaded member-pointer resolution both called legacy `lookup_qualified_functions` with the rendered `QualifiedName`. Both paths now use `lookup_functions_node` or `lookup_function_template_id_node`, preserving typed qualifier/template syntax. This corrects the uncached algorithms; no cache, text parser, or spelling special case is added. | `pa26/tests/general/300-current-specialization-qualified-member-pointer.t`, placed at the `class.member_pointer` owner `pa26:300` | Non-STL reducer `/tmp/cppgm-v2-regex-member-pointer.cpp` failed first in the opportunistic constant initializer probe and then, after that path was fixed, in actual target-aware member-pointer resolution. Debug stacks showed both calls entering `lookup_qualified_functions` and decomposing `matcher<A,B,C>` without an AST node. Exact Boost.Regex failures were `posix_api.o` and `wide_posix_api.o` in `perl_matcher::find_imp`. | Warning-clean build; reducer accepted by Clang and `cppgm++`; focused PA26 check passes; PA26 direct report `67/67`; all configured strict suites pass; placement audit reports zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass; forced single-job `count` passes and updates 72 targets; full direct-LowIR report passes `3823/3823`; forced full-suite survey advances to independent Accumulators frontiers. | instructions -0.10%; max RSS -0.81%; peak footprint +0.01%; pass, below hotspot threshold | `(this commit)` |
| fixed | `libs/accumulators/test//covariance` libc++ parameter collection | The initial parser recognized GNU complex types but retained only their spaced display text. When a class-template specialization later collected a member template, the structured declaration parser correctly refused to recover a multi-token type from that string and rejected the parameter clause. The parser now records `_Complex` as a typed builtin transform over the initially parsed component `type_id`; ordinary and template declaration paths consume that retained AST through the existing structured transform resolver. No semantic text lookup, reparse, cache, or libc++ spelling special case is added. | Strengthened `pa34/tests/compile/600-gnu-complex-template-constructor.t` to make the enclosing holder a class template and force specialization population | Non-STL reducer `/tmp/cppgm-v2-gnu-complex-class-template.cpp` and focused Boost `covariance.o` failed with `unsupported function template parameter-clause`; the old PA34 test did not instantiate its non-template holder and therefore never populated the member path. Clang accepts the reducer. | Warning-clean build; reducer and strengthened PA34 test pass; direct PA34 report `309/309`; all configured strict suites pass; PA34 placement audit reports zero findings and zero hygiene findings; all 23 text-reparse categories remain zero and 14 audit tests pass; forced focused `covariance` clears the parameter-clause diagnostic and advances to a typed GNU complex ABI gap. | instructions -0.04%; max RSS +0.66%; peak footprint -0.03%; pass, below hotspot threshold | `(this commit)` |
| fixed | `libs/accumulators/test//covariance` libc++ GNU complex symbols | Semantic GNU complex types were already structured named `TypePtr`s, but the typed symbol adapter did not map them into the ABI model, so weak member symbol construction failed. The adapter now maps complex float, double, and long double to `abi_mangle::Type::builtin` with Itanium codes `Cf`, `Cd`, and `Ce`. PA30's normalized fact grammar, README, and test surface expose the same typed forms, and symbol tracing captures the resulting `AbiMangleTarget`; no raw mangled suffix or rendered-symbol recovery is added. | New `pa30/tests/abi/100-gnu-complex-types.t`; strengthened `pa34/tests/compile/600-gnu-complex-template-constructor.t` with a non-template complex-parameter member | Focused Boost failed building the weak symbol for `std::__1::complex<float>::__builtin(_Complex float)`. Clang's object symbols confirm `Cf`, `Cd`, and `Ce`; the non-STL PA34 trace failed before the map and now records `holder<float>::set_builtin` as `_ZN6holderIfE11set_builtinECf`. | Warning-clean `abimangle` and `cppgm++` builds; PA30 fact output is exactly `Cf`, `Cd`, `Ce`; PA30/PA34 report passes `390/390`; both placement audits report zero findings and zero hygiene findings; all configured strict suites pass; all 23 text-reparse categories remain zero and 14 audit tests pass; focused `covariance` clears the ABI failure and advances to Boost.Parameter result type lookup. | instructions +0.08%; max RSS +1.17%; peak footprint +0.04%; pass, below hotspot threshold | `(this commit)` |
| fixed | `libs/accumulators/test//covariance` Boost.Parameter result type | A bound template-template parameter already retained the selected member alias declaration and its concrete owner type, but substitution replaced the semantic template-id head with the alias's rendered qualified name. Generic lookup then decomposed and re-resolved `arg_list<...>` instead of applying the bound member alias. Substitution now preserves the parameter head when a typed owner is present, bound-template resolution runs before generic template-id lookup, direct fallback lookup retains the AST node, concrete alias owners are materialized from typed specialization metadata, and already concrete declared owners are not rebound as current instantiations. No cache, text lookup, or spelling-specific recovery is added. | `pa22/tests/general/400-member-alias-template-template-call-result-owner.t`, placed at the PA22 full deduction/result-substitution owner | Exact non-STL reducer `/tmp/cppgm-v2-nested-member-alias-call.cpp` reproduced the `B<T...>` member-alias application and extractor call. Focused Boost failed resolving `arg_list<...>::binding::fn<...>` after the typed binding had already been selected; debugger stacks showed generic qualified lookup re-entering the rendered owner. Removing the concrete-owner guard also makes the reducer fail independently. | Warning-clean full compiler rebuild; reducer passes in 0.07s at 12.3 MB RSS and 301,955,485 instructions; focused PA22 neighborhood passes `3/3`; PA22 direct report passes `251/251`; all configured strict suites pass; PA22 placement audit reports zero findings and zero hygiene findings; all 23 text-reparse categories remain zero and 14 audit tests pass; forced focused `covariance` and forced full-suite `covariance`/`weighted_covariance` pass. | instructions -0.13%; max RSS -0.09%; peak footprint +0.02%; pass, below hotspot threshold | `(this commit)` |
| fixed | `libs/accumulators/test//extended_p_square` Boost.Random dependency | The class-model method parser rejected every declarator with a direct nested declarator from its normal typed filtered path. That guard is necessary for complex returned-array/function shapes, but it also rejected a structurally simple parenthesized member name such as `int (value)() noexcept`, leaving the outer function qualifier in the declaration input. The guard now distinguishes the exact `nested-declarator -> declarator -> identifier` name shape from complex nested declarators, so the established typed filter handles its outer parameter clause and `noexcept`. No text parse, fallback lookup, or cache is added. | `pa15/tests/general/200-parenthesized-noexcept-member-definition.t`, placed with ordinary class method and `noexcept` metadata ownership | Exact Boost-shaped reducer `/tmp/cppgm-v2-static-constexpr-parenthesized-member.cpp` reproduced `static constexpr unsigned long long (max)() noexcept`; minimal reducer `/tmp/cppgm-v2-parenthesized-noexcept-member-definition.cpp` reproduced the essential form. The same parenthesized forms without `noexcept` passed before the fix, isolating the incorrectly bypassed function-qualifier filter. Clang accepts both failing reducers. | Warning-clean full compiler build; minimal and exact reducers pass; focused PA15 neighborhood passes `2/2`; PA15 direct report passes `201/201`; all configured strict suites pass; PA15 placement audit reports zero findings and zero hygiene findings; all 23 text-reparse categories remain zero; forced focused and completed full-suite graph clear all 22 `splitmix64` member-definition failures. | instructions -0.13%; max RSS -0.31%; peak footprint +0.02%; pass, below hotspot threshold | `(this commit)` |
| fixed | `libs/accumulators/test//extended_p_square` Boost.Iterator dependency | Structural alias expansion recursively materialized every concrete class-template type argument before resolving the selected outer template. Boost.Iterator's `eval_if_default_t<Value, iterator_value<Base>>` therefore instantiated the unused `iterator_value<unsigned long>` default even though `Value` was explicit; the later Boost.Test `check_frwd` diagnostic was only fallout. Alias expansion now carries typed arguments without recursively completing them. Substitution first preserves source-scope dependent non-type expression metadata, then refreshes stale specialization metadata only for a fully concrete all-type argument list, which retains correct pack expansion without reviving eager completion. No cache, text parse, or spelling special case is added. | `pa22/tests/general/300-alias-explicit-argument-skips-unused-default-instantiation.t`, placed at the PA22 alias-instantiation owner | Exact Boost reducer `/tmp/cppgm-v2-boost-iterator-traversal-alias.cpp` and non-STL reducer `/tmp/cppgm-v2-inherited-deferred-alias.cpp` both failed by materializing an invalid unused default. Restoring the removed eager loop makes the checked-in regression fail. An initial unconditional metadata refresh regressed existing PA22 dependent non-type cases and was discarded. | Warning-clean build; Clang and `cppgm++` accept the new regression and both reducers; new and existing PA22 regressions plus both reducers pass with `CPPGM_DISABLE_CLASS_INFO_FOR_TYPE_CACHE=1`; PA22 direct report passes `252/252`; all configured strict direct-LowIR suites pass; PA22 placement audit reports zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass; PA35 reverse-iterator control passes; forced focused Boost compile/link/run passes and the completed suite graph clears all three alias-related failures. | instructions -0.27%; max RSS -0.26%; peak footprint -0.06%; pass, all cumulative metrics improve | `(this commit)` |
| fixed | `libs/accumulators/test//error_of` | Partial-specialization matching cached a typed placeholder pattern for a non-deduced later argument, then reused it after an earlier argument had deduced the partial's parameters. The ordinary dependent-type resolver handled most cached patterns, but a still-dependent nested class/alias pattern retained the placeholder binding and selected the fallback specialization. Matching now builds the complete typed template argument list from the deduction state and applies the existing typed `TypePtr` substitution only when the resolver fast path leaves a non-deducible pattern dependent. The substituted pattern is then resolved in the current match scope. No source parse, text decomposition, result cache, or spelling-specific recovery is added. | `pa22/tests/spec/300-nondeduced-partial-pattern-recursive-completion.t`, a header-free reducer at the earliest dependent deduction/recursive completion owner | Exact Boost reducer `/tmp/cppgm-v2-boost-count-construction.cpp` failed with `failed non-type template argument evaluation: ::boost::mpl::aux::nested_type_wknd<T1>::value`; the reduced test failed by selecting `value_of_impl<non_iterator_tag>` after the stale `enable_if<has_fusion_tag<Sequence>>::type` pattern missed. Clang accepts both. | Warning-clean build; exact and checked-in reducers pass normally and with `CPPGM_DISABLE_CLASS_INFO_FOR_TYPE_CACHE=1` and `CPPGM_DISABLE_DEPENDENT_TYPE_RESOLUTION_CACHE=1`; focused PA22 direct check passes; PA22 direct report passes `253/253`; strict direct-LowIR PA18/19/21/22/23 passes with zero failures; PA22 placement and hygiene audits report zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass; forced single-job Boost target passes compile, link, and runtime with 72 updated targets. Hotspot counters are in `/tmp/cppgm-v2-nondeduced-hotspot.log`; temporary trace accounting on the stable benchmark saw 3,491 eligible patterns, 2,071 still dependent after the fast resolver, and 861 successful typed substitutions. The 1 ms sample `/tmp/cppgm-v2-nondeduced.sample.txt` contains 7,104 samples; the fallback substitution has fewer than five top-of-stack samples and the dependent resolver has 12, while allocation and existing ABI/type work dominate. | instructions +0.75%; max RSS -0.56%; peak footprint +0.04%; pass; optimized from the eager +0.94% version and below all hard gates | `(this commit)` |
| fixed | `libs/accumulators/test//rolling_mean`, `//rolling_variance` compile | Hierarchy lookup established that every path found the same declaration, then unconditionally required a unique base subobject and conflated entity lookup with object adjustment. The lookup result now states whether it needs a unique subobject: fields and callable sets containing nonstatic members retain the ambiguity check, while static data, all-static callable sets, and nested template declarations keep the shared declaration without an object offset. The same traversal and access metadata remain; no cache, source parse, text decomposition, or spelling-specific recovery is added. | `pa26/tests/general/100-duplicate-base-static-member-lookup.t`, paired with the existing `100-bad-ambiguous-member.t` negative control at the PA26 multiple-inheritance owner | Non-STL reducers `/tmp/cppgm-v2-static-object-duplicate-base.cpp` and `/tmp/cppgm-v2-static-function-duplicate-base.cpp` failed with `ambiguous base class path [current D] [target A]`, while Clang accepts both. The corresponding nonstatic-field reducer is rejected by both compilers. Boost failed looking up `tag::lazy_rolling_{mean,variance}::window_size` because `rolling_window_size_<0>` is reached through two dependency bases. | Warning-clean compiler rebuild; static data/function reducers and the expanded nested class/alias/function-template regression pass with Clang and `cppgm++`; nonstatic-field control remains rejected; PA26 direct report passes `68/68`; all configured strict direct-LowIR suites pass; PA26 placement reports zero findings and zero hygiene findings; all 23 text-reparse categories remain zero and 14 audit tests pass. Forced focused Boost rebuild compiles and links both targets, updates 72 targets, and exposes only runtime failures; log `/tmp/boost-v2-acc-duplicate-base-static-fix.log`. | instructions +0.74%; max RSS +1.05%; peak footprint +0.06%; pass | `(this commit)` |
| fixed | `libs/accumulators/test//rolling_mean` lazy result | The transitive `__is_base_of` evaluator used the unspecialized class metadata name as its traversal key. Distinct specializations such as `depends_on<sum,count>` and `depends_on<window>` therefore appeared to be the same visited node, so MPL omitted the rolling-window dependency and materialized the accumulator update order incorrectly. The traversal now tracks typed `TypePtr` identities with `type_equals`. This corrects the traversal algorithm itself; it adds no cache, text parse, rendered identity, or symbol-path change. | `pa34/tests/compile/700-builtin-base-of-specialization-cycle-key.t`, a header-free reducer at the PA34 host builtin-trait owner | Semantic tracing showed `depends_on<window>` rejected as already visited only because a different `depends_on<sum,count>` specialization shared its metadata name. The pre-fix compiler rejects the reducer's transitive base assertion and orders `rolling_sum` before `rolling_window_plus1`, producing `4.2`/`5.4` instead of `4.0`/`5.0`. | Warning-clean build; Clang and the fixed compiler accept the regression; the exact reduced runtime returns the correct rolling results; PA34 focused and direct report pass `310/310`; all configured strict direct-LowIR suites pass; PA34 placement reports zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. The forced focused Boost target rebuilds 72 targets and leaves only the independent persistence decoder failure; log `/tmp/boost-v2-acc-base-of-specialization-cycle-key-fix.log`. The forced full survey reaches 36 passes with only the exact five persistence failures and zero compile failures before its bound; evidence `/tmp/boost-frontier-v2-suite-001-base-of-specialization-cycle-key`. Hotspot counters are in `/tmp/cppgm-v2-base-of-hotspot.log`; the 1 ms sample `/tmp/cppgm-v2-base-of-hotspot.sample.txt` contains no frames from the changed evaluator. | instructions +0.73%; max RSS -0.06%; peak footprint +0.10%; pass | `(this commit)` |
| fixed | `libs/accumulators/test//rolling_count`, `//rolling_sum`, `//rolling_moment`, `//rolling_variance`, `//rolling_mean` persistence | LowIR virtual-base adjustment required a class to have locally observed virtual functions or a local vtable before using its imported runtime layout. In the exact Boost.Serialization translation unit, `std::basic_ostream` had a typed external vtable candidate and precise virtual-base layout but no locally observed virtual function, so `basic_ostream *` to `basic_ios *` incorrectly fell back to a fixed `+8` adjustment. Dynamic adjustment now accepts the typed imported external-layout fact directly. Pointer conversion on this external-only fallback uses a null-preserving branch before loading the vptr; existing local-vtable paths are unchanged. This corrects the uncached lowering algorithm and adds no cache, text parse, rendered identity, or symbol-path change. | Strengthened `pa33/tests/general/200-host-virtual-base-indirect-base-access.t` by removing `B`'s explicit virtual destructor, leaving virtual inheritance as its sole reason to require the host-provided vtable. This is a header-free host ABI/runtime reducer at the earliest owning PA. | Debugger evidence in `/tmp/cppgm-v2-persistence-lldb-value.txt` and `/tmp/cppgm-v2-persistence-save-counts.txt` showed the archive wrote the correct byte counts but `ostream_iterator<char>::put_val` corrupted the stream state. Pre-fix LowIR in `/tmp/cppgm-v2-basic-text-oprimitive.lowir` used fixed `+8`; exact semantic state contained `class_virtual_base_layouts_` and an external vtable candidate but no local virtual-function mark. The fixed LowIR loads the runtime offset from the vtable `-24` slot. | Exact normal and `CPPGM_DISABLE_CLASS_INFO_FOR_TYPE_CACHE=1` LowIR are byte-identical in `/tmp/cppgm-v2-boost-basic-text-oprimitive.final.lowir` and `/tmp/cppgm-v2-boost-basic-text-oprimitive.final-nocache.lowir`; corrected Boost persistence roundtrip passes. Focused PA32 null regression, strengthened PA33 owner regression, PA27 direct report `34/34`, PA33 direct report `2256/2256`, all configured strict direct-LowIR suites, PA33 placement/hygiene audit, warning-clean build, and all 23 zero-reparse categories plus 14 audit tests pass. Exact forced Accumulators survey and continuation close the suite with zero failures; final full direct-LowIR report passes `3830/3830`. | instructions +0.74%; max RSS +0.50%; peak footprint +0.10%; pass; +0.01 instruction percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/algorithm/test//apply_permutation_test` | Explicit class-instantiation suppression had a special empty-member bypass that treated every weak template member as though it were inline. An empty user-defined out-of-class destructor therefore bypassed `extern template` suppression and was emitted weakly in ordinary client translation units. The bypass now uses typed `FunctionBinding` inline/constexpr facts plus the parsed declaration/definition node identity; weak linkage alone cannot establish ownership. No cache, text parse, spelling test, or symbol-text path is added. | Strengthened `pa32/tests/general/200-host-extern-template-ooc-member-reference.t` with a header-free out-of-class destructor and explicit destructor call. Its inspect oracle requires the consumer object to leave `Box<int>::~Box()` undefined while the host provider owns the explicit instantiation; the existing in-class member remains a weak control. | Header-free reducer `/tmp/cppgm-boost-algorithm-extern-template-destructor.cpp` was accepted by Clang but cppgm emitted two weak destructor variants. In the Boost.Test archive, cppgm's weak `std::__1::basic_ios<char>::~basic_ios()` definition caused `execution_monitor.o` to be extracted to satisfy `apply_permutation_test`, producing 27 duplicate framework symbols. The test object links and runs when the redundant archive is omitted, and a host-compiled test object reproduces the duplicate-symbol behavior against the polluted archive. | Warning-clean build; reducer objects from normal and `CPPGM_DISABLE_CLASS_INFO_FOR_TYPE_CACHE=1` builds are byte-identical and match Clang's undefined destructor symbol; focused PA32 regression passes; PA32 direct report passes `108/108`; all configured strict direct-LowIR suites pass; the strengthened regression has no placement or hygiene finding, while the PA32 audit still reports the separately queued pre-existing attribute-placement violation; all 23 text-reparse categories remain zero and 14 audit tests pass. The rebuilt Boost.Test archive leaves `basic_ios<char>::~basic_ios()` undefined. Forced focused and full Algorithm runs pass, and the final full direct-LowIR report passes `3830/3830`. | instructions +0.88%; max RSS -0.05%; peak footprint +0.06%; pass after a no-code-change repeat; +0.14 instruction percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/align/test//alignment_of_test` | The retained `alignof(T)` AST was already structurally substituted to a concrete typed operand, but the template-argument leaf constexpr hook returned that `TypePtr` without completing its class layout. The shared evaluator therefore reported `named type alignment unavailable`, after which the dependency check misclassified the concrete expression as still dependent. The hook now completes layout through the existing semantic class-completion service before the evaluator asks for alignment, matching the normal semantic constexpr path. No cache, text parse, spelling test, or symbol path is added. | `pa19/tests/general/100-dependent-alignof-nontype-template-argument.t`, a header-free reducer at the earliest integral NTTP owner `pa19:100` | Exact reducer `/tmp/cppgm-align-dependent-nttp.cpp` and the smaller `Payload` regression both failed before the fix on `constant<size_type, alignof(T)>`; Clang accepts both. The pre-fix failure is unchanged with `CPPGM_DISABLE_CLASS_INFO_FOR_TYPE_CACHE=1`, and trace evidence `/tmp/cppgm-align-dependent-nttp-eval-trace.log` shows the post-substitution operand is the concrete `struct Struct<bool>` before layout evaluation fails. | Warning-clean build; exact and owner reducers compile and run with Clang, normal cppgm, and cache-disabled cppgm; focused owner check passes; PA19 direct report passes `135/135`; all configured strict direct-LowIR suites pass; PA19 placement and hygiene audits report zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. Forced focused and complete Align runs pass, and the final full direct-LowIR report passes `3831/3831`. | instructions +0.68%; max RSS +1.59%; peak footprint +0.06%; pass; -0.20 instruction percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/any/test` no-RTTI compile targets | `resolve_qualified_owner_type_node` correctly retained the parsed `QualifiedName` and qualifier template-id syntax for an out-of-class declarator, but represented the owner nested-name-specifier as an ordinary synthetic `type_name`. That caused the dependent-type resolver to demand `typename` for `outer<T>::inner<D>` even though the declarator-id grammar already establishes each nested-name-specifier component as a type. The synthetic typed node now carries an explicit implicit-`typename` context fact, and the existing diagnostic predicate consumes it. No cache, text parse, source-spelling check, or broad diagnostic suppression is added. | `pa21/tests/general/300-out-of-class-nested-member-template-static-definition.t`, a header-free reducer at the earliest member-template declaration/collection owner `pa21:300` | `/tmp/cppgm-any-nested-member-template-static.cpp` failed with `dependent qualified type requires typename: outer<T,Mode>::inner<D>` both normally and with class-info and dependent-type-resolution caches disabled; Clang accepts it. Boost.Describe's `update_modifiers<T,Bm>::fn<D>::pointer` produced the same diagnostic in eight Any no-RTTI targets. | Warning-clean build; reducer and owner test pass with Clang, normal cppgm, and both relevant caches disabled; focused owner check passes; PA21 direct report passes `218/218`; all configured strict direct-LowIR suites pass; PA21 placement and hygiene audits report zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. Forced focused Boost passes; the full forced suite clears all eight shared failures and leaves only the two independent `unique_move` variants. | instructions +0.80%; max RSS +0.25%; peak footprint +0.15%; pass; +0.12 instruction percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/any/test/unique_any//unique_move`, `//no_rtti_unique_move` | Initial template-argument parsing already tokenized `&&` as `OP_LAND`, but the fallback fragment classifier treated only trailing `*` and `&` as possible abstract declarators. It therefore attempted `library::box<int>&&` only as an expression, failed that parse, and left a text-only `TemplateArgumentSyntax`; explicit function-template argument resolution correctly refused to reparse it. Classifying `OP_LAND` as type-then-expression preserves the initial structured qualified template-id plus rvalue-reference declarator. No semantic reparse, cache, rendered identity, or symbol-path change is added. | `pa18/tests/general/100-qualified-template-id-rvalue-reference-argument.t`, a header-free reducer at the first type-template-argument owner `pa18:100` | `/tmp/cppgm-qualified-template-id-rvalue-reference-explicit-arg.cpp` failed with `unknown function api::take<library::box<int>&&>` both normally and with class-info plus dependent-type-resolution caches disabled; Clang accepts and runs it. Trace showed the semantic boundary received `syntax=yes` but no type-id, template-id, or expression, proving loss during the initial parse rather than semantic reparsing. | Warning-clean build; the exact reducer passes with Clang, normal cppgm, and both caches disabled; the PA18 regression emits the expected LowIR; PA18 direct report passes `223/223`; all configured strict direct-LowIR suites pass; PA18 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. Both focused Boost targets and the complete forced Any suite pass. Final direct-LowIR report passes `3833/3833`. | instructions +0.81%; max RSS +0.99%; peak footprint +0.10%; pass; +0.01 instruction percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/array/test` constexpr initialization and access targets | Constexpr class initialization consumed flattened brace arguments as if each argument directly initialized one field, so a nested built-in array aggregate failed before member access. The evaluator now uses the shared structured aggregate-initializer plan when brace elision requires it. It also evaluates array and overloaded `operator[]` access structurally, handles the selected branch of discarded conditionals, and records `__builtin_expect` with a typed `FunctionBinding` enum consumed by constant evaluation. No name spelling is reparsed in the evaluator. | `pa20/tests/general/300-constexpr-template-aggregate-subscript-member.t`, placed at the earliest constexpr class-template evaluation owner `pa20:300` | The initial Array survey failed `array_init_test_cx`, `array_copy_test_cx`, and `array_get_test_cx` while evaluating aggregate initialization or member subscript access. The reduced template aggregate case failed with the same constant-expression diagnostic before the fix. | Warning-clean build; owner regression and PA20 direct report pass; all configured strict direct-LowIR suites, placement checks, and the strict 23-category zero-reparse audit pass. The focused `array_init_test_cx` rebuild passes, and the broader fix clears all aggregate-access failures. | instructions +0.86%; max RSS +0.50%; peak footprint +0.10%; pass after removing duplicate normal-path semantic work | `7c7e7cfc9` |
| fixed | `libs/array/test` constexpr pointer targets | Constexpr aggregate and array values did not propagate storage provenance through member and element access, reference initialization, reference returns, or copied call arguments. Address-of therefore could not produce a stable subobject pointer, and comparisons could not distinguish copied object storage from the source. Structured constant values now carry storage identity and offsets through those operations; by-value aggregate arguments receive distinct storage, while reference operations preserve their referent. Null pointer conversions and zero offset arithmetic are handled in the same typed constant-value model. | `pa20/tests/general/400-constexpr-subobject-pointer-identity.t`, covering array/member addresses, pointer offsets, reference preservation, and by-value copy identity | After the aggregate-access fix, `array_data_test_cx`, `array_iterator_test_cx`, and `array_access_test_cx` failed constexpr pointer assertions. The reduced non-STL cases failed identically with the relevant semantic caches disabled. | Normal and cache-disabled LowIR are byte-identical; warning-clean build, focused PA20 check, PA20 direct report `73/73`, all configured strict suites, placement, and the strict zero-reparse audit pass. All three focused Boost pointer targets compile, link, and run. | instructions +0.65%; max RSS +0.23%; peak footprint +0.10%; pass; cumulative instructions improve 0.21 percentage points | `43bd0cc6b` |
| fixed | `libs/array/test//to_array_test` | Reference binding first compares referent types while ignoring top-level cv. For arrays, that cv belongs to the element type in the semantic representation, so the scalar-only comparison incorrectly treated `int[3]` and `const int[3]` as unrelated. Conversion ranking then fell through to a non-reference qualification conversion, fabricated an array prvalue, and made the `const T (&&)[N]` overload viable for an lvalue. The typed referent comparison now recursively compares identical array shapes and ignores cv at the effective array top level. No template special case, cache, allocation, or text parse is added. | `pa14/tests/general/100-array-cv-rvalue-reference-overload.t`, placed at the earliest audit-approved reference conversion owner `pa14:100` | A non-template reducer reproduced the ambiguity for mutable and const array lvalues before the fix, proving this was a general conversion algorithm defect rather than template deduction. Clang selects the lvalue-reference overload. The pre-fix trace showed correct deduction and rejection of the mutable rvalue-reference candidate, followed by incorrect acceptance of the const rvalue-reference candidate. | The reducer passes normally and with all four relevant template/type caches disabled; the two modes emit byte-identical LowIR. PA14 owner report passes `200/200`; the existing converted-temporary rvalue-reference control passes; strict direct reports, placement, audit unit tests, and all 23 zero-reparse categories pass. Focused `to_array_test`, the complete forced Array survey, and the final direct report `3836/3836` pass. | instructions +0.73%; max RSS +0.26%; peak footprint +0.08%; pass; hotspot counters are stable and the changed helper is absent from the 1 ms sample | `(this commit)` |
| fixed | `libs/asio/test//any_completion_executor` `prefer` substitution | Direct function-type template arguments such as `void(P0,P1,PN...)` retained their initial structured `type_id`, but pack expansion only descended through nested template-ids. One occurrence of the Asio `call_traits` signature therefore retained `PN` while the parallel result-type occurrence expanded it, and SFINAE dropped the four-property overload. The fix expands the retained direct function parameter clause from bound `TypePtr` values, rebuilds parameter declarations with typed declarators, and canonicalizes only a direct function `type_id`; it never invokes a parser. The direct-shape restriction preserves packed pointer-to-member types. | `pa23/tests/general/500-forwarding-pack-function-type-enable-if.t`, a header-free forwarding-reference and alias-SFINAE reducer at the PA23 integration owner. The pre-existing `pa26/tests/general/300-member-pointer-pack-decltype-memfn.t` is the non-direct function-type guard that caught and prevented over-broad ancestor annotation. | `/tmp/cppgm-asio-prefer-nprops-const-lvalue-decay.cpp` and the PA23 reducer failed with the expected alias-SFINAE/callee-not-callable diagnostic both normally and with all five template/type caches disabled; Clang accepts them. The initial forced Asio survey timed out with 193 compile diagnostics. After the fix, forced `any_completion_executor` advances past `prefer` to the separate allocator constructor failure. | Warning-clean build; PA23 reducer and exact Asio-shaped reducer pass normally and with all five caches disabled; PA23 and PA26 direct reports pass `397/397` and `68/68`; all configured strict suites pass; PA23 placement and hygiene report zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. The final full direct report excluding the user-exempt PA9 suite passes `3826/3826`; PA3's earlier load timeout passes `19/19` in isolation. | instructions +0.79%; max RSS -0.49%; peak footprint +0.07%; pass; only +0.06 instruction percentage points from Array, with stable hotspot counts and one sample in the changed annotator | `(this commit)` |
| fixed | `libs/asio/test//any_completion_executor` allocator rebind construction | Class-template member alias substitution correctly rebuilt the qualified nested `Alloc<T, Args...>::rebind<U>::other` syntax, but copied `CppAstNode::semantic_type` annotations still described the dependent source form. Structured qualified lookup then localized the qualifier template-id for current-class lookup and incorrectly reused that localized form for general lookup. Substituted class aliases with qualified template-id syntax now clear only stale node type annotations, preserving resolved template arguments, and take the existing structured template resolver; general lookup consumes the original source qualifier while current-class lookup keeps the localized form. No text parse or cache repair is involved. | `pa22/tests/spec/300-qualified-nested-allocator-rebind-detector-alias.t`, a non-STL detector/SFINAE and template-template partial-specialization reducer at the earliest class-template specialization owner `pa22:300` | A controlled pre-fix compiler rebuild rejects the checked-in reducer with `unsupported alias template instantiation for allocator_traits_rebind_t`; `/tmp/cppgm-asio-nested-allocator-rebind.cpp` fails the same way, and Clang accepts both. The failure persists with each of `CPPGM_DISABLE_CLASS_INFO_FOR_TYPE_CACHE`, `CPPGM_DISABLE_DEPENDENT_TYPE_RESOLUTION_CACHE`, `CPPGM_DISABLE_TEMPLATE_ARGUMENTS_CACHE`, `CPPGM_DISABLE_TEMPLATE_ARGUMENTS_FAST_CACHE`, `CPPGM_DISABLE_BOUND_MEMBER_FAILURE_CACHE`, and `CPPGM_DISABLE_QUALIFIED_TYPE_LOOKUP_CACHE`, proving the uncached algorithm was wrong. | Warning-clean build; reducer passes normally and in all six cache-disabled modes; PA22 direct report passes `254/254`; all configured strict direct-LowIR suites pass; PA22 placement and hygiene report zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. Focused `any_completion_executor` compiles, links, runs, and passes. The exact full survey advances to the independent `Bits` layout frontier, and the final direct report excluding PA9 passes `3827/3827`. | instructions +0.85%; max RSS -0.53%; peak footprint +0.06%; pass; +0.06 instruction percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/asio/test//append`, `//append_select` | Member-template substitution rebuilt the typed return specialization `executor<OtherAllocator, Bits>` using only the member template's parameter list. The direct `Bits` argument retained its parsed `id_expression`, but the concrete enclosing class binding was not materialized into the specialization key, leaving display/key metadata symbolic while typed ABI metadata already encoded `4`. Substitution now consumes only the retained direct identifier AST, excludes names shadowed by the current parameter list, resolves only typed `template_bound_value_names` from enclosing scopes, and copies the concrete `TemplateArgument` through the existing structured non-type path. The shared lookup was renamed from its obsolete mangle-only name. No string parse, source scan, cache, or symbol-text recovery is added. | `pa21/tests/spec/300-nested-class-member-template-nontype-return-key.t`, a header-free nested member-class/member-function-template reducer at the earliest PA21 member-template owner | The exported main reference binary and the pre-fix compiler both fail the reducer with `named type size unavailable: context::executor<allocator<void>, Bits>`. LLDB logs `/tmp/cppgm-asio-append-bits-debug-fatal-type-size-lldb.log` and `/tmp/cppgm-asio-append-bits-debug-lowir-node-lldb.log` show semantic analysis completes and the fatal request occurs during LowIR layout. The concrete specialization metadata and ABI path already carry value `4`, proving a typed identity mismatch rather than initial parse loss or text reparse. | Warning-clean build; Clang accepts the reducer; normal and all six relevant cache-disabled cppgm runs emit byte-identical LowIR with concrete key/value `4` and typed `Lj4E` ABI symbols. PA21 focused check and direct report pass `219/219`; all configured strict suites pass with zero comparison failures; PA21 placement/hygiene report zero findings; all 23 text-reparse categories and audit tests pass. Forced focused targets pass, the exact survey clears the complete `Bits` family, and the final direct report excluding PA9 passes `3828/3828`. | instructions +0.90%; max RSS +1.27%; peak footprint +0.10%; pass on the required three-run median, +0.05 instruction percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/asio/test//bind_allocator`, `//bind_allocator_select` | A detector partial specialization used `void_t<decltype(__builtin_invoke(...))>`, but its concreteness scan traversed only ordinary AST children. The `decltype` and call were retained under `TemplateArgumentSyntax` and `TemplateIdSyntax` sidecars, so matching reused the declaration-time placeholder `resolved_type` after deducing concrete arguments and selected the primary template. A sidecar-aware structural predicate now identifies only this dependent `decltype`-call pattern and sends its retained type/template syntax through the existing typed resolver while bypassing the stale top-level annotation. No source text is parsed or reconstructed. | `pa22/tests/spec/300-void-t-decltype-call-sidecar-partial-specialization.t`, a header-free `void_t`, call-result, and handler-requirements reducer at the earliest PA22 class partial-specialization owner | The pre-fix compiler at the merged `main` baseline rejects both the checked-in reducer and the exact Boost-shaped reducer; Clang accepts them, and the exported PA22 reference compiler generated the direct LowIR reference. With the fix, a normal owner run and `CPPGM_TRACE=template.resolve` plus witness capture emit byte-identical LowIR. Trace mode disables class-specialization selection caching and witness capture disables stable alias expansion caching, proving the uncached matching algorithm. | Warning-clean build; focused owner check and PA22 direct report pass `255/255`; all configured strict direct-LowIR suites pass; PA22 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. Forced focused `bind_allocator` targets compile, link, run, and pass; the exact survey clears both immediate-executor variants, records 192 passes and six later failures, and advances to the independent buffer-literal family. The final full direct report excluding PA9 passes `3829/3829`. | instructions +0.96%; max RSS +0.78%; peak footprint +0.11%; pass on the required three-run median, only +0.06 instruction percentage points from the preceding frontier after removing broad structured scans and cache-key dependency walks | `(this commit)` |
| fixed | `libs/asio/test//buffer`, `//buffer_select` | Numeric user-defined literal handling treated the presence of any ordinary same-suffix function as proof that the cooked numeric form existed. Asio also declares the string form `operator""_buf(const char *, size_t)`, so `0x00_buf` was incorrectly constructed as a one-argument cooked call and never reached its raw `template<char...> operator""_buf()` overload. Selection now inspects the typed `FunctionBinding` parameter list and chooses the cooked integer or floating path only for an exact one-parameter `unsigned long long` or `long double` operator; otherwise it uses the existing structured character-template-argument path. This consumes typed declarations and the already-tokenized literal token spelling required for the raw character pack; it does not parse saved semantic source text. | `pa19/tests/general/200-user-defined-integer-literal-overload-set.t`, a header-free integer-and-floating raw literal reducer at the existing PA19 numeric UDL owner | The current pre-fix compiler and the merged-main compiler both call the unrelated two-parameter string operator with one argument; Clang accepts and runs the reducer. The exported PA19 binary predates UDL support and cannot generate the new golden, so the checked LowIR reference was constructed from and byte-checked against the fixed structured lowering under PA19's documented checked-golden policy. | Warning-clean build; Clang, normal cppgm, and cppgm with all nine semantic/template caches disabled pass, with byte-identical normal/cache-disabled LowIR. PA19 direct report passes `136/136`; all configured strict direct-LowIR suites pass; PA19 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. Forced focused buffer targets pass, the exact survey records 190 passes plus four later failures before timeout, and the final full direct report passes `3830/3830` with PA9 explicitly omitted. | instructions +0.92%; max RSS -0.54%; peak footprint +0.10%; pass on a no-code-change repeat after the first +1.01% near-threshold result; -0.04 instruction percentage points from the preceding frontier, unchanged query counts, and no changed-helper sample | `(this commit)` |
| fixed | `libs/asio/test//ip_tcp`, `//ip_tcp_select` | During the functional-cast probe for an explicit function template-id, type lookup first tried to match the normalized display spelling against the retained `TemplateIdSyntax`. Normalization omitted the dependent `template` keyword inside one argument, so the exact full-spelling match failed and the probe rejected the call before ordinary function-template lookup. When that exact match fails, type lookup now verifies the direct template identifier against the active exact anchor and consumes the anchor's original typed template arguments and syntax. It does not parse or reconstruct text, add a cache, or change symbol construction. | `pa22/tests/general/100-dependent-trailing-return-explicit-template-id-keyword.t`, a header-free explicit function-template call with a dependent nested member-template type in a trailing return, at the earliest PA22 class-template integration owner | Both `/tmp/cppgm-ip-tcp-min.cpp` and the fuller `/tmp/cppgm-ip-tcp-explicit-signature-pack.cpp` failed before the fix while Clang accepted them. Trace instrumentation showed that initial parsing retained `::template rebind<long>::other`; rejection occurred only because the normalized type-probe spelling omitted `template`. The instrumentation was removed. | Warning-clean build; the owner regression, both reducers, and normal plus all nine relevant cache-disabled modes pass, with byte-identical LowIR. PA22 direct report passes `256/256`; all configured strict suites pass; PA22 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and 14 audit tests pass. Focused and broad Asio runs pass both `ip_tcp` targets. The final full direct report excluding PA9 passes `3831/3831`. | instructions +0.89%; max RSS +0.29%; peak footprint +0.15%; pass; cumulative instructions improve 0.03 percentage points and the changed cold branch does not warrant hotspot escalation | `(this commit)` |
| fixed | `libs/asio/test//posix_stream_descriptor`, `//posix_stream_descriptor_select` | Member-template result recovery used the ordinary instantiation scope, which has no active function or `this` binding. The nested `initiation(this)` functional cast in `decltype(async_initiate<...>(...))` therefore rejected every constructor and left `async_write_some` dependent. A sidecar-aware structural predicate now detects the retained `KW_THIS` node and performs only that still-dependent non-static member-result recovery in a typed synthetic method scope with the concrete owner and cv/ref facts. No text is parsed, reconstructed, or used for lookup. | `pa22/tests/general/100-member-trailing-return-this-explicit-template-id.t`, a header-free mixed explicit/deduced call reducer at the audit-approved PA22 full-deduction owner | The fresh exported-main reference binary and pre-fix compiler both reject the reducer with unknown member `start`; `/tmp/cppgm-trailing-explicit-call-this-cast.live-trace.log` shows member collection succeeds and result recovery rejects `this` outside a member function. The initial broad member-result scope hook made the self-compile benchmark fail in unrelated `make_pair`/`push_back` resolution, proving that applicability had to be structural and narrow. | Warning-clean build; Clang and cppgm compile/run the reducer; normal, all nine individual cache-disabled modes, and the all-disabled mode emit byte-identical LowIR; PA22 passes `257/257`; all configured strict suites pass; placement exits cleanly; all 23 text-reparse categories remain zero and 14 audit tests pass. The self-compile benchmark and both forced Boost targets pass on the narrowed implementation. Final direct-LowIR report excluding PA9 passes `3832/3832`. The exported-main oracle predates the fix, so the checked maintainer LowIR golden was byte-verified against all uncached modes rather than preserving its failure fixture. | instructions +0.79%; max RSS +0.36%; peak footprint +0.14%; pass; -0.10 instruction percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/asio/test//strand`, `//strand_select` | Full-expression temporary output support classified class prvalues by `TypePtr::named_has_layout`. The nested member-class-template specialization retained a pre-layout named `TypePtr`, even though typed `ClassInfo` was already complete, so its destructor was never required for emission. The existing layout-bearing completion path is preserved; only layout-free named prvalues now use typed `class_info_for_type` lookup and require the destructor when that class is already complete. No parser, spelling match, cache, ABI construction, or symbol-trace path changes. | `pa21/tests/general/300-nested-member-template-prvalue-dtor-output.t`, a header-free nested class-template/member-template prvalue reducer at the earliest PA21 member-template owner | The controlled pre-fix compiler at `b22b02029` emits only a declaration for `context::executor<long>::~executor` and fails host link with that exact undefined symbol; Clang accepts and runs the reducer. Typed output tracing on Boost showed the concrete `basic_executor_type<recycling_allocator<void, default_tag>, 4>` class and correct typed ABI symbol, but no definition requirement. Object inspection proved both `on_invoker_exit` destructors were defined and referenced that missing concrete executor destructor. | Warning-clean build; focused owner test and PA21 direct report pass `220/220`; normal, class-info-cache-disabled, and all-nine-caches-disabled LowIR are byte-identical; PA21 placement/hygiene has zero findings; all configured strict direct-LowIR suites pass; all 23 text-reparse categories remain zero and 14 audit tests pass. Exact forced `strand` targets compile, link, run, and pass. The final full direct report excluding PA9 passes `3833/3833`. | instructions +0.78%; max RSS +0.22%; peak footprint +0.03%; pass; -0.007% instructions, -0.14% RSS, and -0.11% footprint from the preceding checkpoint | `(this commit)` |
| fixed | `libs/assign/test//array`, `//list_of` | Function-template finalization substituted a defaulted type argument into retained AST, then resolved the original dependent spelling first. For the array conversion candidate this preserved a dependent `enable_if` placeholder instead of resolving `void`; for the reference-target probe it also allowed the invalid pointer-to-reference SFINAE expression to survive. Finalization now resolves the substituted `CppAstNode` directly once its retained syntax is structurally concrete, rejects failed or still-dependent results, and commits only its retained value dependencies after a speculative witness pause. Separately, typed ABI construction no longer encodes a result type for conversion-function templates. Symbol diagnostics report typed `TemplateArgument` state, and successful trace capture remains `abi_mangle::AbiMangleTarget`; no text parse or rendered-symbol fallback is added. | `pa22/tests/general/400-defaulted-sfinae-conversion-function-template-symbol.t` at the PA22 full deduction/SFINAE owner; `pa30/tests/abi/300-conversion-function-template.t` covers the existing typed ABI fact combination, so no scaffold extension was needed | The initial forced survey rejected both targets while building the weak `generic_list<int>::operatorContainer` symbol. Focused trace `/tmp/boost-frontier-v2-assign-array-symbol-trace-typed-args.log` showed defaulted dependent arguments reaching typed symbol construction. Direct trace `/tmp/boost-assign-array-direct-template-trace.stderr` showed the object target correctly finalized `<array, void, void>` only after concrete AST resolution, while the `const array&` probe substituted an invalid pointer-to-reference and had to fail. Clang emits all three template arguments but no separate conversion result in the symbol. | Warning-clean build; PA22/PA30 direct report passes `340/340`; all configured strict suites pass; PA22/PA23/PA30 placement and hygiene audits have zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, nine individually cache-disabled, and all-disabled reducer LowIR are byte-identical. Exact Boost translation units compile, and forced focused `array`/`list_of` compile, link, run, and pass. The full direct report excluding PA9 passes `3835/3835`. | instructions +0.77%; max RSS +0.78%; peak footprint +0.20%; pass, instructions improve 0.01 percentage points from the preceding frontier | `(this commit)` |
| fixed | `libs/assign/test//multi_index_container` | Initial parsing and template substitution had already retained the concrete nested `mp_identity<name>` as `TemplateIdSyntax` inside the outer `mp_to_bool<std::is_base_of<...>>` argument. Concrete type lookup normalized the requested name to `boost::mp11::mp_identity<name>`, but its local retained-syntax search walked only qualifier template-ids and required the source spelling to include the same namespace qualification. The shared search now descends through typed template-argument and AST sidecars and accepts exact or top-level-unqualified structured renderings after prefiltering by the typed template identifier. It consumes retained syntax only; no tokenization, parser, source scan, semantic cache, or text-derived ABI path is added. | `pa21/tests/general/200-nested-template-id-argument-pack-base-lookup.t`, a header-free alias/pack/base integration reducer at the placement-audited `pa21:200` owner | The exact Boost MP11 reducer `/tmp/cppgm-boost-mp11-nested-template-id-anchor.cpp` and the forced `multi_index_container` target fail with `failed template-id parse in type lookup: boost::mp11::mp_identity<name>` while Clang accepts the reducer. Failure-only instrumentation confirmed that the outer and nested substituted `TemplateIdSyntax` sidecars were present; only qualification-sensitive matching and missing argument traversal rejected them. The instrumentation was removed. | Warning-clean build; the PA21 reducer passes normal, witness, all nine individual cache-disabled modes, and the all-disabled mode with byte-identical LowIR; PA21 direct report passes `221/221`; all configured strict suites pass; placement/hygiene is clean; all 23 text-reparse categories remain zero and all 14 audit tests pass. The exact minimal Boost reducer and focused target pass, the complete forced Assign survey rebuilds 84 targets and passes all 14 tests, and the final direct report excluding PA9 passes `3836/3836`. | instructions +0.82%; max RSS +1.15%; peak footprint +0.14%; pass, only +0.05 instruction percentage points from the preceding checkpoint | `(this commit)` |

| fixed | `libs/atomic/test` | Boost.Atomic exposed shared typed-lowering gaps plus its required x86 asm surface. Complete named classes now evaluate `__is_trivially_copyable` from typed special-member, base, field, polymorphism, and destructor facts. Scoped-enum globals use scalar constant initialization. Logical-not compares in the operand LowIR domain, signed narrow-to-i128 conversion sign-extends the low half before deriving the high mask, and dead f80 call results explicitly pop the x87 stack. GNU asm output/input expressions are parsed from the original token stream during the initial parse, analyzed into typed `asm_operand` nodes, and lowered for the exact Boost x86 locked update, exchange, compare-exchange, CAS-loop, and bit-update templates. No operand text is parsed, no cache is added, and no symbol path changes. | `pa14/tests/general/200-scoped-enum-global-constant-init.t`; strengthened header-free PA29 int128 lowering coverage; `pa29/tests/general/300-runtime-discarded-float80-call-result.t`; `pa34/tests/compile/500-builtin-trivially-copyable-class-special-members.t`; `pa34/tests/run/800-gnu-asm-atomic-update-run.t` | The initial forced suite produced false class-trait results, zero-valued scoped-enum globals, unsupported GNU asm diagnostics, twelve high-half U128 truth failures, two signed i128 widening failures, and one long-double comparison failure. LLDB showed the long-double values were initially exact but repeated dead f80 call returns overflowed the x87 stack; the i128 mismatch was a zero-extended low register paired with a sign high half. | Warning-clean build; all focused reducers and owner tests pass; normal and all-six-cache-disabled trait/asm LowIR are byte-identical; PA14/PA29/PA34 direct reports pass; all configured strict suites pass; placement audits report zero findings; all 23 reparse categories remain zero and all 14 audit tests pass; final forced Atomic suite passes all 90 rebuilt targets; final full direct report excluding PA9 passes `3840/3840`. | instructions +0.91%; max RSS +0.19%; peak footprint +0.32%; pass; +0.09 instruction percentage points from the preceding checkpoint | `(this commit)` |

| fixed | `libs/beast/test` shared `buffers_cat.hpp:362` dependency | Class partial-specialization selection treated the unresolved structured argument pack `T...` as one argument and eagerly selected the fixed-arity partial `outer<T>`. Selection now defers to the primary for a dependent `TemplateArgumentSyntax::pack_expansion`, after preserving the existing exact-dependent-partial match used by partial definitions. Completing the nested class from its owner definition now runs the standard nested finalizer before returning, and stored member definitions use the owner template's retained out-of-class nested-class AST anchor instead of a transient forward-declaration node. This fixes the uncached algorithm; no cache, semantic text parse, or symbol fallback is added. | `pa21/tests/general/100-dependent-pack-owner-skips-fixed-partial-nested-definition.t`, a header-free reducer at the placement-audited PA21 partial-specialization owner | The current pre-fix compiler rejected `/tmp/cppgm-beast-nested-iterator-operator.cpp` with `unsupported template declarator`; Clang accepted it. Typed trace showed `outer<T...>` selecting the one-argument partial with `deferred=no`; after correcting selection, replay found the stored definition but skipped it first because nested completion returned before finalization and then because reference/concrete owners used different AST nodes for the same retained nested definition. Initial Beast log `/tmp/boost-frontier-v2-suite-010-beast-initial-forced.log` repeated the same declarator failure at line 362. | Warning-clean build; Clang and `cppgm++` compile and run the reducer; normal, all six individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR. Focused PA21 check and direct report pass `222/222`; all configured strict suites pass; PA21 placement audit has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. The exact forced Beast rerun rebuilds 104 targets with zero `unsupported template declarator` diagnostics and advances to the independent typed ABI failure at line 335; log `/tmp/boost-frontier-v2-suite-010-beast-after-pack-owner.log`. | instructions +0.86%; max RSS +1.16%; peak footprint +0.33%; pass; instructions improve 0.05 percentage points from the Atomic checkpoint; report `/tmp/cppgm-boost-frontier-v2-beast-pack-owner-final-perf.json` | `(this commit)` |
| fixed | `libs/beast/test` shared `buffers_cat.hpp:335` dependency | Typed ABI construction recognized only the primary owner's declared type-parameter identity. In an out-of-class definition the retained owner argument instead carries the renamed dependent `Bound` pack as a typed `TemplateArgument::type`, so the reference parameter could not become `pack(ref(const(template-param 0)))`. The adapter now maps that retained typed owner argument to its corresponding parameter index and marks the same typed node as the owner pack before emitting the existing `abi_mangle::Type` facts. No source or rendered symbol is parsed, no string is converted back into an ABI target, and no cache is added. | `pa19/tests/general/200-out-of-class-owner-parameter-pack-rename.t`, a header-free source reducer at the earliest pack owner; `pa30/tests/abi/600-template-parameter-pack-reference-constructor.t` directly covers the existing typed fact combination, so the assignment/scaffold needed no extension | The exact typed trace reported a failed lvalue-reference parameter with `owner-pack=no`; retained owner-argument inspection showed the current dependent `Bn` type and `TemplateArgument::type` were the same `TypePtr`. The pre-fix header-free reducer failed identically while Clang accepted it. | Warning-clean build; Clang and fixed cppgm accept the reducer; normal and all six relevant cache-disabled modes emit byte-identical LowIR. PA19 and PA30 focused checks and direct report pass `220/220`; both placement audits have zero findings; all configured strict direct-LowIR suites pass; all 23 text-reparse categories remain zero and all 14 audit tests pass. Direct replay advances to the independent concrete-pack assertion. The exact forced Beast survey updates 106 targets with zero remaining `buffers_cat_view` ABI failures and records the next frontier in `/tmp/boost-frontier-v2-suite-010-beast-after-dependent-owner-pack.log`. | instructions +0.86%; max RSS -0.55%; peak footprint +0.33%; pass; instructions and footprint are unchanged from the preceding frontier; report `/tmp/cppgm-boost-frontier-v2-beast-dependent-owner-pack-perf.json` | `(this commit)` |
| fixed | `libs/beast/test` shared `buffers_cat.hpp:110` dependency | Completion replaced a nested forward declaration with its retained out-of-class definition AST, but populated that AST in a scope containing only the primary owner's `Buffers` binding. The stored definition's renamed `Bn` parameter pack therefore had no structured binding even though the owner already retained all three concrete typed arguments. Before population, completion now binds the stored definition parameters directly to the owner's retained `instantiation_binding_arguments` through the existing template binder. `sizeof...(Bn)` is then evaluated from `Scope::named_type_packs`; no expression text is parsed or rewritten and no cache is added. | `pa19/tests/general/200-out-of-class-nested-owner-pack-static-assert.t`, a header-free reducer at the earliest pack and `sizeof...` owner | Clang accepts the reducer, while the pre-fix compiler reports `static_assert unevaluated: sizeof...(Bound) == 2`; its scope dump contains `Primary=[int, long]` but no `Bound` binding. Exact Boost diagnostics likewise record three concrete owner arguments while failing the renamed nested-definition pack. | Warning-clean build; Clang and cppgm accept the reducer; normal and all six relevant cache-disabled modes emit byte-identical LowIR. The focused check passes, PA19 direct report passes `360/360`, placement has zero findings, all configured strict direct-LowIR suites pass, and all 23 text-reparse categories plus 14 audit tests pass. Direct Beast replay advances to the independent trailing-return definition. The exact forced survey updates 107 targets with zero old ABI or concrete-pack assertion failures and records the next frontier in `/tmp/boost-frontier-v2-suite-010-beast-after-nested-owner-pack-binding.log`. | instructions +0.80%; max RSS -0.30%; peak footprint +0.28%; pass; instructions improve 0.06 percentage points from the preceding frontier; report `/tmp/cppgm-boost-frontier-v2-beast-nested-owner-pack-binding-perf.json` | `(this commit)` |
| fixed | `libs/beast/test` shared `chunk_encode.hpp:304` dependency | Initial parsing already retained the complete qualified declarator and trailing-return AST. Template declaration collection nevertheless tried to resolve the dependent alias `reference` before the out-of-class nested owner was concrete, then treated `UnsupportedDeclSpecifiers` as fatal even though other dependent member-signature failures use retained declaration storage. Deferral now admits that status only when the initial declarator structurally contains a trailing return and the existing storage helper proves a qualified out-of-class member definition. The storage path uses the nested owner from the already resolved qualified parse scope, matching the eager path, and later resolves the original AST nodes in the concrete binding scope. No text is parsed or rebuilt, and no cache is added. | `pa18/tests/general/300-out-of-class-nested-class-trailing-return-operator.t`, a header-free reducer at the placement-audited PA18 out-of-class nested-class and ordinary trailing-return owner | The pre-fix compiler rejects the reducer and Beast line 304 with `unsupported function template decl-specifier-seq`; Clang accepts the reducer. The existing PA18 trailing-return regression kept its nested type inline and therefore resolved its alias eagerly. Failure-only tracing showed that retained owner storage declined `outer<T>::inner` because it retried lookup from the pattern scope even though qualified function parse-scope resolution had already found the nested owner; the probe was removed. | Warning-clean build; reducer and focused check pass; normal and all six cache-disabled modes emit byte-identical LowIR; PA18 direct report passes `224/224`; all configured strict direct-LowIR suites pass; placement has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Exact direct Beast replay advances to `type_traits.hpp:94`. The forced suite updates 115 targets with zero line-304 failures, skips 288, and fails 44 at later frontiers; log `/tmp/boost-frontier-v2-suite-010-beast-after-nested-trailing-return-defer.log`. | instructions +0.83%; max RSS +1.04%; peak footprint +0.32%; pass; only +0.03 instruction percentage points from the preceding frontier; report `/tmp/cppgm-boost-frontier-v2-beast-nested-trailing-return-defer-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//any_completion_handler` trait dependency | The initial parser already retained the defaulted member-template parameter and trailing-return `decltype` AST, but class member collection treated static `auto` declarations as variables, dependent decltype analysis tried to materialize output and bodies, class completion invalidated reference-only member candidates during overload resolution, and the built-in comma probe converted away the right-hand class result. Class member parsing now follows the existing structured trailing-return declarator path; unevaluated analysis suppresses output/body materialization; overload selection refreshes invalidated candidates from typed owner, declaration, signature, qualifier, and instantiation identity; assignment probing uses typed no-body conversions; and built-in comma preserves both operand types. Dependent `declval` member access is deferred from the original AST until substitution. No source text, rendered template-id, or ABI spelling is reparsed, and no cache is added. | `pa22/tests/spec/300-defaulted-member-template-declval-trailing-return-sfinae.t`, a header-free positive/negative SFINAE reducer at the PA22 full substitution owner | The exact Beast trait and reducer failed while collecting `is_fields_helper::test`, then exposed stale-candidate lifetime, no-body assignment, and comma-result defects as each earlier layer was corrected. Clang accepts the reducer. The pre-fix B2 graph repeated the line-94 trait failure across HTTP targets. | Warning-clean full build; Clang strict C++11 and cppgm accept the reducer; exact Beast trait compiles normally and under six individual cache-disabled modes plus all-disabled mode with byte-identical objects. PA22/PA23 direct report passes `656/656`; all configured strict suites pass with direct LowIR comparison; the full direct report excluding PA9 passes `3846/3846`; PA22/PA23 placement audits have zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Witness declval calls use `FunctionCallSourceDecision` with the typed `DeclvalCall` origin, and witness-mode LowIR is isolated from normal refs. The exact B2 target advances to `CancellationSlot::operator=` in `/tmp/boost-beast-any-completion-handler-after-trait-fix.log`. | instructions +0.78%; max RSS +0.06%; peak footprint +0.35%; pass; instructions improve 0.05 percentage points from the preceding frontier; report `/tmp/cppgm-boost-frontier-v2-beast-declval-trailing-return-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//any_completion_handler` `filtering_cancellation_slot<CancellationSlot>` dependency | A dependent-base `using CancellationSlot::operator=` resolved the concrete base class but collected only already-declared reference members. `boost::asio::cancellation_slot` has only implicit copy/move assignment operators, so the valid using target appeared empty. The lookup path now materializes the concrete base's implicit special members through the existing typed class-model APIs before visible member lookup. The imported bindings remain in the real overload set; witness output merely coalesces a synthesized assignment rejection when an inherited selected function-template drop already represents the same typed owner and reason. No cache, source parse, name parse, or symbol path is involved. | `pa18/tests/general/300-dependent-base-using-implicit-copy-assignment.t`, a header-free runtime reducer at the earliest placement-audited dependent-base using owner | Clang accepts and runs the reducer; the pre-fix compiler reports `unknown using-declaration target: Slot::operator=`. The fixed LowIR adjusts the derived object to its base subobject and calls the synthesized `_ZN4slotaSERKS_`. Overload trace for the PA21 inherited-operator witness confirms that base copy/move bindings remain in the semantic candidate set; only their duplicate source-event projection is coalesced. | Warning-clean build; PA18 direct and strict checks pass; all configured strict direct-LowIR suites pass. Normal, six individually cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `81224258ddb8223b7ab9ccc6be45d9cfa4e6a68058a3df562aaa46fbd71abbb2`. PA18 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. The full direct report excluding PA9 passes `3847/3847`. Exact forced replay clears `CancellationSlot::operator=` and advances to `prefer.hpp:482` in `/tmp/boost-beast-any-completion-handler-after-implicit-assignment.log`. | instructions +0.73%; max RSS +1.16%; peak footprint +0.36%; pass; instructions improve 0.05 percentage points from the preceding frontier; report `/tmp/cppgm-boost-frontier-v2-beast-implicit-assignment-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//any_completion_handler` Asio `prefer_free<T, Property>` dependency | The structured dependent-expression `decltype` fast path accepted an ordinary value binding without applying point-of-definition visibility. That let an Asio `prefer` CPO object declared after the trait definition satisfy the earlier dependent call probe, recursively instantiate `call_traits` while `prefer_free` reference-member collection was in progress, and leave the fallback partial unavailable. The leaf path now applies the existing declaration-order rule before accepting a value binding. Stable initial-parse source locations drive the comparison; retained AST token offsets are only a fallback. No cache, text parse, source scan, or symbol path is added. | `pa22/tests/general/300-dependent-call-later-cpo-value-not-visible.t`, a header-free trait-before-call-traits reducer at the PA22 expression-SFINAE owner | The pre-fix compiler rejects the recursive reducer after accepting the later callable object as visible, while Clang selects the false trait and fallback call path. The exact Boost-header reducer fails only when `prefer_free` is explicitly instantiated before `call_traits`, exposing the reentrant completion order. Normal call analysis already rejected the later object; only the structured leaf fast path omitted that rule. | Warning-clean build; Clang and cppgm accept the header-free and exact Boost-header reducers. A PA19 local-callable regression initially exposed incomparable retained token offsets and passes after source-location ordering became primary. Normal, six individually cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `92a625fc3988150e0f6d79820b738eb54043d2f5c0cb3c01456828a417d23a18`. PA22 direct report passes `260/260`; all configured strict direct-LowIR suites pass; PA22 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. The full direct report excluding PA9 passes `3848/3848`. Final forced replay clears `prefer.hpp:482` and advances the target-local translation unit to missing `variant::emplace`; the two known shared archive frontiers remain unchanged. Log `/tmp/boost-beast-any-completion-handler-after-late-cpo-visibility-final.log`. | instructions +0.68%; max RSS -0.88%; peak footprint +0.26%; pass; instructions improve 0.05 percentage points from the preceding frontier; report `/tmp/cppgm-boost-frontier-v2-late-cpo-visibility-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//any_completion_handler` `variant::emplace<I>` dependency | The in-class declaration of `serializer::do_visit` intentionally leaves its non-type member-template parameter unnamed, while the out-of-class definition names it `I`. Resolution attached the definition body to the declaration but retained only the declaration's unnamed parameter metadata, so the first semantic analysis of `v_.template get<I>()` could not bind `I` and the enclosing `emplace<I>` call appeared to have no member. Every out-of-class function-template resolution path now merges the definition head into the resolved declaration with the existing structured redeclaration helper, and stored-definition replay merges those typed aliases into concrete member candidates before attaching the original body AST. No cache, source parse, template-id text parse, or symbol path is involved. | `pa21/tests/general/300-out-of-class-member-template-unnamed-declaration-param.t`, a header-free reducer at the placement-audited PA21 member-template owner | Clang accepts both the minimal and Beast-shaped reducers. The pre-fix compiler's concrete `variant` class already contained the correct `get` and `emplace` templates, and `do_visit` already held its concrete non-type argument; targeted tracing showed only the definition spelling `I` was absent from the declaration scope. The fixed compiler compiles and runs both reducers, and exact replay clears `variant::emplace` before advancing to the independent `buffers_suffix` trait assertion at `boost/beast/core/impl/buffers_suffix.hpp:146`. Log `/tmp/boost-beast-any-completion-handler-after-unnamed-param-fix.log`. | Warning-clean full build; focused check and PA21 direct report pass `223/223`; all configured strict direct-LowIR suites pass; PA21 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, six individually cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `76b048e517f37c41b04c950c60ec1f2b9e904e22b92152c0f29d552aeb089fab`. The full direct report excluding PA9 passes `3849/3849`. | instructions +0.59%; max RSS -0.87%; peak footprint +0.24%; pass; versus the preceding frontier instructions improve 0.09%, RSS is +0.01%, and footprint improves 0.03%; report `/tmp/cppgm-boost-frontier-v2-unnamed-member-template-param-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//any_completion_handler` `buffers_suffix<Buffers>` dependency | The initial parser and declaration collector retained the out-of-class definition's renamed owner parameter `Buffers` in `OutOfClassMemberFunctionDecl::parameters`. Stored-definition replay created a durable template scope and bound those structured parameters to the concrete owner arguments for signature resolution, but discarded that scope when it attached the body to the resolved `FunctionBinding`. First body analysis therefore fell back to the instantiated class member scope, which exposed only the declaration spelling `BufferSequence`. Replay now assigns the already-bound definition scope as the function's declaration scope when it installs the retained body AST, matching the selected-specialization replay path. No text is parsed, no alias is added globally, and no cache or symbol path changes. | `pa18/tests/spec/300-out-of-class-member-owner-param-rename.t`, a header-free `sizeof(Renamed)` reducer at the PA18 class-template and out-of-class member owner | The pre-fix compiler rejects the reducer with `invalid sizeof type-id`; its scope dump contains `Original=int` but no `Renamed`, while Clang accepts and runs it. The exact Beast diagnostic likewise contains only `BufferSequence=boost::beast::buffers_triple` while evaluating `is_const_buffer_sequence<Buffers>`. | Warning-clean build; fixed cppgm and Clang compile and run the reducer; focused PA18 check and direct report pass `226/226`; all configured strict direct-LowIR suites pass; PA18 placement and hygiene report zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, six individually cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `285220e150535554e78f2077162a80aad3b8de1072288e994747981e05fd7f62`. The exact Beast `buffers_suffix.cpp` and `any_completion_handler.cpp` commands compile successfully. The full direct report excluding PA9 passes `3850/3850`. | instructions +0.67%; max RSS +0.31%; peak footprint +0.24%; pass; versus the preceding frontier instructions are +0.07%, RSS is +1.19%, and footprint is flat; report `/tmp/cppgm-boost-frontier-v2-ooc-owner-param-scope-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//any_completion_handler` shared Asio library | Namespace ADL treated every directly registered namespace function as visible, including hidden friends declared by unrelated classes. While analyzing `signal_set_base::flags` comparisons, this admitted `operator!=(executor, executor)`, probed two converting `executor(flags)` constructors, and eventually emitted `executor::impl<flags>` even though `executor` was not an associated class. Non-template function bindings now retain whether their only declaration is a hidden friend. Real ordinary, qualified, and namespace ADL-direct lookup exclude that binding; the existing associated-class friend collection admits it only for the correct operand class, and synthetic ADL candidate scopes preserve friends already admitted there. A later ordinary namespace declaration clears the hidden-only state. No cache, text parse, symbol trace, or ABI path changes. | `pa15/tests/spec/300-hidden-friend-not-visible-to-qualified-lookup.t` and `300-hidden-friend-not-visible-to-unrelated-adl.t`, Clang-confirmed negative reducers at the earliest friend/ADL/operator owner; existing PA15 hidden-friend calls guard associated-class admission | The exact trace shows `f != flags::dont_care` selecting the unrelated `boost::asio::operator!=(executor, executor)`, then materializing `executor(Executor)` with `Executor=signal_set_base::flags` and tracking `_ZN5boost4asio8executorC1INS0_15signal_set_base5flagsEEET_`. The pre-fix frontier compiler incorrectly accepts both the qualified hidden-friend call and the shared-namespace ADL call through unrelated argument types, while Clang rejects both. | Warning-clean build; focused PA15 direct report passes `203/203`; strict PA18/19/21/22/23 direct suites pass; placement exits clean; all 23 text-reparse categories and 14 audit tests pass. A positive associated hidden-friend reducer is byte-identical normally, with six individual caches disabled, and with all disabled (SHA-256 `e500c0daaabae393a5040d54e9d39f6c315d74b02f27de781b8a91b24d63763a`); both negatives remain rejected with all caches disabled. The exact `lib_asio.cpp` command succeeds, focused B2 archives `lib-asio.a`, and advances only to the independent Beast `chacha::state_size` constexpr-member initializer. Full direct report excluding PA9 passes `3851/3851`; the additional unrelated-ADL reducer then passes in the focused PA15 report. | instructions -0.79%; max RSS -1.27%; peak footprint +0.22%; pass; versus the preceding frontier instructions improve 1.44%, RSS improves 1.58%, and footprint improves 0.02%; report `/tmp/cppgm-boost-frontier-v2-hidden-friend-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//any_completion_handler` shared Beast library `chacha::state_size` dependency | The initial parser correctly retained the standard `sizeof(type-id)` ambiguity as a structured type-id for `sizeof(chacha::keysetup_)`. Ordinary expression semantics already recovered a failed single qualified type-name as an id-expression, but constexpr evaluation stopped after type parsing failed. The recovery builder now lives in the shared AST layer and the constexpr evaluator applies it directly to the retained node. Completing the referenced class during this evaluation also exposed two stale-reference bugs: class finalization iterated a map while recursive full completion cleared it, and leaf constant materialization retained the destroyed `ValueBinding`. Both paths now snapshot semantic inputs and reacquire the current typed binding after recursive work. The final `sizeof` uses the field binding's array type. No source text, tokenization, cache, symbol trace, or ABI path is involved. | `pa20/tests/general/300-static-constexpr-sizeof-qualified-nonstatic-member.t`, a Clang-confirmed non-STL reducer at the full constexpr owner | Clang accepts the reducer. The pre-fix compiler segfaults while reference-only finalization recursively completes `chacha<20>`; LLDB shows the invalidated iterator in `finalize_class_constant_members`. With iterator reacquisition alone it reports the same unsupported initializer as Boost, proving the remaining issue is constexpr's missing structured type-id recovery rather than the cache or parser. | Warning-clean full rebuild; focused PA20 check and direct report pass `74/74`; placement and hygiene report zero findings; all configured strict direct-LowIR suites pass after correcting the existing PA23 qualified-`sizeof` oracle to constant-initialize `observed=3`; all 23 text-reparse categories and 14 audit tests pass. Normal, six individually cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. Replaying the exact `lib_beast.cpp` command with `CPPGM_B2_CXX` explicitly bound to this worktree proves `chacha.hpp:97` is fixed and advances to the independent `generate_block` initial-parser failure. The earlier `stream_state.hpp:62` result was produced by the wrapper's stale default compiler checkout and is superseded. Full direct report excluding PA9 passes `3853/3853`, including PA37 object roundtrip `7/7`. | instructions -0.83%; max RSS -0.97%; peak footprint +0.07%; pass; versus the preceding frontier instructions improve 0.05%, RSS is +0.30%, and footprint improves 0.15%; report `/tmp/cppgm-boost-frontier-v2-chacha-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//any_completion_handler` shared Beast library `chacha::generate_block` dependency | The grammar parser accepts decl-specifiers after a named qualified type, but its declaration-start prefilter only admitted an identifier, cv-qualifier, pointer, or reference after the qualified name. Lazy first parsing of `std::uint32_t constexpr constants[4]` therefore rejected the block item before `parse_decl_specifier_seq` could consume the valid sequence. The prefilter now admits the same decl-specifier keyword set as the parser. This changes initial parsing only: no body or semantic text is reparsed, no cache is added, and the parser memo table merely stores the corrected predicate result. | `pa19/tests/general/100-qualified-type-before-constexpr-local.t`, a header-free reducer at the placement-audited earliest constexpr declaration owner | The pre-fix current-worktree compiler rejects the reducer at the qualified type and fails Beast body materialization near `std`; Clang accepts and runs the reducer. The Boost wrapper defaulted to `/Users/vishvananda/cppgm-boost-b2-frontier/dev/cppgm++`, so all exact replays now set `CPPGM_B2_CXX=/Users/vishvananda/cppgm-extended/dev/cppgm++` explicitly. | Warning-clean build; cppgm and Clang compile and run the reducer; PA19 passes `139/139`; placement and hygiene report zero findings; all configured strict direct-LowIR suites pass; all 23 text-reparse categories and 14 audit tests pass. Normal, six individually cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `fea8fccffbb8363aa47ada03e29256b428bc7c9d978f496889ec4ce996741170`. The exact current-worktree `lib_beast.cpp` command exits successfully. Full direct report with PA9 explicitly excluded passes `3854/3854`, including PA37 object roundtrip `7/7`. | instructions -0.82%; max RSS -0.42%; peak footprint +0.10%; pass; versus the preceding measurement instructions are +0.01%, RSS is +0.56%, and footprint is +0.03%; report `/tmp/cppgm-boost-frontier-v2-qualified-constexpr-parser-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//_detail_is_invocable` | The dependent `decltype(callable(arg), true_type())` leaf evaluator correctly retained `C` as `const callable` and validated both comma operands, but callable-object lookup removed reference and top-level cv to find the class and then reused that unqualified lookup type to decide implicit-object constness. The non-const `operator()` therefore remained viable. Each leaf callable-object route now derives constness from the typed referent before stripping cv for class lookup. No cache, source parse, rendered type, or symbol path is involved. | `pa22/tests/general/300-dependent-call-operator-implicit-object-const-sfinae.t`, a header-free positive/negative expression-SFINAE reducer at the placement-audited PA22 owner | The pre-fix reducer and exact Beast test fail the negative assertion while Clang accepts it. Template trace shows the parameter instantiated as `const callable&&`; the wrong result is created during first specialization resolution, before any later cache hit. | Warning-clean full build; cppgm and Clang accept the reducer; focused check and PA22 direct report pass `261/261`; all configured strict direct-LowIR suites pass; PA22 placement and hygiene report zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all nine individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The exact focused Beast target compiles, links, runs, and passes after rebuilding 46 targets. Full direct report excluding PA9 passes `3855/3855`, including PA37 object roundtrip `7/7`. | instructions -0.80%; max RSS -1.12%; peak footprint +0.06%; pass; versus the preceding measurement instructions are +0.02%, RSS improves 0.71%, and footprint improves 0.04%; report `/tmp/cppgm-boost-frontier-v2-invocable-implicit-object-cv-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//buffers_adaptor` original explicit-instantiation failure | The initial AST already retained every qualified template-id sidecar. Collection nevertheless associated `buffers_adaptor<T>::subrange<B>::iterator` constructor and `subrange<B>::begin` definitions with the outer template head, then concrete member class-template clones sharing the retained class-definition AST lost those deferred definitions. Qualified owner lookup now consumes the retained node sidecars, selects the template head matching the deepest owner, and indexes deferred definitions by AST identity for member class templates only. Concrete clones inherit the retained nested class, ordinary member, and member-function-template definitions without a global class-template scan. No source text, rendered template-id, fallback resolver, symbol path, or semantic result cache is involved. | `pa21/tests/spec/300-explicit-instantiation-out-of-class-nested-member.t`, a header-free runtime reducer at the PA21 explicit-instantiation/member-template owner; it forces both the nested iterator constructor and `begin()` body through `template struct adaptor<int>` | The pre-fix compiler rejects the reducer with `missing templated special-member binding` and Beast with `failed template-id parse in type lookup: buffers_adaptor<MutableBufferSequence>`. Trace showed `begin` stored on `range` and the constructor on `iterator`, but the concrete member-template clone had neither definition. An attempted unresolved-owner output-node assignment cleared Beast but caused `basic_ostream::sentry::~sentry` output-audit failures in a four-line `<sstream>` reducer and the stable perf benchmark; it was removed. | Warning-clean build; focused PA21 check and PA21 direct report pass `224/224`; strict direct-LowIR suites pass; PA21 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all nine individual cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `3d13810f29b9522a244db8ff26888b20851e6ef04f11c5da8b60238ba78b2546`. The exact target clears the original failure and reaches the independent convertibility assertion. Full direct report excluding PA9 passes `3856/3856`, including PA37 object roundtrip `7/7`. | instructions -0.95%; max RSS -0.99%; peak footprint +0.07%; pass; versus the preceding measurement instructions improve 0.15%, RSS is +0.13%, and footprint is +0.01%; report `/tmp/cppgm-boost-frontier-v2-nested-owner-definitions-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//buffers_adaptor` mutable-to-const subrange conversion | The nested class-template forward declaration is intentionally unnamed (`template<bool> class subrange;`), while its out-of-class definition names that parameter `isMutable`. The retained definition replaced the forward declaration's AST but its structured parameter metadata was discarded, so the concrete `subrange<false>` scope exposed only its positional binding and `isMutable_ = isMutable` became a type-like unresolved value. The collector now merges the retained definition head into the forward declaration with the existing structured redeclaration helper before creating the concrete class-template declaration. No parser, source text, trait special case, cache, symbol trace, or ABI path changes. | `pa21/tests/spec/300-nested-member-template-definition-parameter-alias-default.t`, a header-free implicit-conversion reducer using a defaulted type SFINAE parameter at the placement-audited PA21 member-template owner | The pre-fix compiler rejects the checked-in reducer's implicit conversion and the fuller non-STL reducer fails the same `is_convertible` assertion as Beast. Trace shows constructor deduction succeeds but binds `Other=bool` because the definition spelling is missing. The fixed reducer passes Clang and cppgm native execution. | Warning-clean build; focused PA21 check and direct report pass `225/225`; all configured strict direct-LowIR suites pass; PA21 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all nine individual cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `74028885999dbfdf1405b692582f0a9e481a3d5b9611a60f9471c0af7147a086`. The exact Beast target compiles, links, runs, and passes. Full direct report excluding PA9 passes `3857/3857`, including PA37 object roundtrip `7/7`. | instructions -0.84%; max RSS -0.70%; peak footprint +0.07%; pass; versus the preceding measurement instructions are +0.113%, RSS is +0.293%, and footprint is -0.004%; report `/tmp/cppgm-boost-frontier-v2-member-definition-param-alias-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//bind_handler` | Associated-namespace collection visited the typed `ClassInfo` for a concrete class-template specialization before its reference shape had been collected, so its base classes contributed no associated namespaces during the first deduction. Later semantic work collected the same class and changed the uncached ADL result: the outer `testHooks` specialization retained Beast's wrapper while reanalysis selected the more specialized Asio `detail::bind_handler`. ADL now demand-collects reference members for a concrete, idle associated class before walking its bases. This uses retained AST and typed class metadata, does not force full layout or bodies, and adds no parse, text lookup, symbol, ABI, or result-cache path. | `pa18/tests/spec/100-adl-associated-private-base-before-template-deduction.t`, a header-free private-base ADL reducer at the placement-audited PA18 class-template/ADL owner | A controlled pre-fix rebuild rejects the non-STL reducer because deduction first omits the private base's namespace. The exact Beast trace shows first deduction selecting `beast::detail::bind_wrapper<executor_binder<...>>`, then later conversion seeing `asio::detail::binder0<...>`. Clang selects the Asio helper when the complete class shape is available. The old algorithm marks an incomplete, uncollected class uncacheable, proving the wrong result occurs on the initial uncached computation rather than being introduced or hidden by a cache. | Warning-clean build; focused PA18 direct report passes `227/227`; all configured strict direct-LowIR suites pass; PA18 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all nine individual cache-disabled modes, and all-disabled mode emit byte-identical reducer LowIR with SHA-256 `6307416fd777900aea9a45504cd9b3e0509538b488cb5f52a3613598156cde2a`. The exact Beast target compiles, links, runs, and passes after rebuilding 46 targets. Full direct report excluding PA9 passes `3858/3858`, including PA37 object roundtrip `7/7`. The subsequent full forced Beast survey updates 345 targets and moves the ordered frontier to `buffers_cat.cpp:40`. | instructions -0.69%; max RSS -0.40%; peak footprint +0.13%; pass; versus the preceding frontier instructions are +0.149%, RSS is +0.302%, and footprint is +0.066%; report `/tmp/cppgm-boost-frontier-v2-adl-reference-shape-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//buffers_cat` | Initial declaration parsing consumed `decltype(bs)` as a complete decl-specifier before the existing named-type path could retain the following `::const_iterator`. When a balanced `decltype` or GNU `typeof` is followed by `::`, both decl-specifier and type-id-specifier parsing now keep the construct in the existing initial qualified-type path. That path retains the `QualifiedName` and qualifier-type AST sidecars used by semantics. There is no later source or semantic text parse, cache, symbol trace, or ABI-path change. | `pa15/tests/spec/100-decltype-qualified-nested-type-local.t`, a header-free reducer at the placement-audited PA15 nested class type owner; it covers both a local declaration and a `static_cast` type-id | Clang accepts the reducer. A controlled pre-fix cppgm compile rejects the declaration with `expected block-item near KW_DECLTYPE:decltype`, matching Beast's `decltype(bs)::const_iterator it;` failure at `buffers_cat.cpp:40`. | Warning-clean build; focused PA15 check and direct report pass `204/204`; all configured strict direct-LowIR suites pass; PA15 placement exits clean; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all nine individual cache-disabled modes, and all-disabled mode emit byte-identical reducer LowIR with SHA-256 `9202675bf9f274c99953ba7689d5d3a61b72b10221b1fde778fae8b763f05209`. The exact Beast target compiles, links, runs, and passes after rebuilding 46 targets. Full direct report excluding PA9 passes `3859/3859`, including PA37 object roundtrip `7/7`. The full forced survey improves to 349 updated / 27 failed / 71 skipped and moves the ordered frontier to `basic_stream.cpp:1241`. | instructions -0.76%; max RSS -0.49%; peak footprint +0.10%; pass; versus the preceding frontier instructions improve 0.073%, RSS improves 0.092%, and footprint improves 0.033%; report `/tmp/cppgm-boost-frontier-v2-decltype-qualified-name-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//basic_stream` qualified executor convertibility assertion | Initial template-argument construction tried the qualified-id expression shortcut before consulting the fragment classifier, so `decltype(sock)::executor_type` retained only an expression interpretation. Qualified `decltype` and GNU `typeof` arguments are now classified as type/value ambiguous and parsed through the existing structured fragment parser first. The assertion was then evaluated during a legacy member-body validator that recursively visited only `static_assert` nodes in a synthetic scope, bypassing preceding local declarations. Early validation now uses the normal structured statement analyzer in lexical order, retains its body output for later emission, and keeps witness capture hooks out of the normal path. No source or semantic text is reparsed. | `pa18/tests/spec/100-decltype-qualified-template-argument-type.t` covers the qualified type argument at the PA18 type-template-argument owner. `pa19/tests/spec/100-member-function-static-assert-local-scope.t` covers the PA19-owned qualified value argument, a preceding local declaration used by `static_assert`, and later emission of the validated member. | Header-free reducers proved direct and typedef-mediated trait operands worked while `holder<decltype(sock)::executor_type>` failed unless prefixed with `typename`. A member-scope reducer then showed direct `__is_convertible` had empty operands and only `this` in scope. The legacy validator introduced by `76591e656` bound parameters but skipped local declarations and nested statement scopes. Clang accepts both final reducers. | Warning-clean full build; focused PA18/PA19 checks and direct report pass `368/368`; the existing unused-member false assertion remains diagnosed; all configured strict direct-LowIR suites pass; PA18/PA19 placement and hygiene report zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `8a1f60ec6e2ddc82eb61eb033a45d600c4c2cde11d537f1079d408dc89a16228` and `2ede7f42ec357d79e5586e0137bb23a07ecdb52346485aeba4af293729a3eab3`. The exact Beast target clears the assertion and reaches the later `suite::expect` failure. Full direct report with PA9 explicitly excluded passes `3861/3861`. The full forced survey improves to 353 updated / 26 failed / 68 skipped. | instructions -0.50%; max RSS +0.10%; peak footprint +0.20%; pass; versus the preceding frontier instructions are +0.267%, RSS is +0.600%, and footprint is +0.093%; report `/tmp/cppgm-boost-frontier-v2-qualified-decltype-static-assert-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//basic_stream` `suite::expect` call | `static_cast<MemberPointer>(&Stream::template read_some<Buffer>)` analyzed its operand as a generic expression before applying the explicit target type, so the overloaded member-template id threw `overloaded id-expression unsupported` and all outer `expect` candidates were discarded. Named `static_cast` now enters the existing typed target-aware selector first only when its target is a member-function pointer and its operand is an address expression. The selector consumes the initial AST and concrete target type; no source text, semantic spelling, cache, symbol trace, or ABI path is involved. | `pa26/tests/general/300-static-cast-overloaded-member-template-pointer-call-argument.t`, a header-free reducer at the placement-audited PA26 overloaded member-pointer owner; it selects both one- and two-parameter member-template overloads while passing the casts directly to another function template | Before the fix, `/tmp/cppgm-boost-frontier-v2-expect-reducer.cpp` is accepted by Clang but cppgm traces `candidate name=expect ... arg-analysis-failed=overloaded id-expression unsupported` and then reports the misleading outer `unknown member function`. Adding the second `read_some` overload is sufficient to reproduce the Beast failure. | Warning-clean build; Clang executes the reducer; focused PA26 check and direct report pass `69/69`; all configured strict direct-LowIR suites pass; PA26 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `86b64a3c65aa6dc5b29e523a09e0ddb2367ba53e667b9fda9033f1f8c6ca05d7`. Full direct report with PA9 explicitly excluded passes `3862/3862`, including PA37 object roundtrip `7/7`. The exact target reaches the later `__mem_fn::operator()` result failure, and the forced survey improves to 357 updated / 25 failed / 65 skipped. | instructions -0.53%; max RSS +0.84%; peak footprint +0.18%; pass; versus the preceding frontier instructions improve 0.030%, RSS is +0.740%, and footprint improves 0.016%; report `/tmp/cppgm-boost-frontier-v2-member-template-cast-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//basic_stream` libc++ `__mem_fn` result and call | Structured member-pointer invocation recognized only raw pointers and direct owner objects, so a concrete pointer-like class with `operator*` left `decltype(__builtin_invoke(...))` dependent after deduction. Actual builtin lowering also selected `object.*member` for every non-pointer instead of the standard `(*object).*member` fallback. The shared typed result algorithm now resolves a concrete zero-argument `operator*` and retries owner compatibility, while builtin lowering synthesizes the retained-AST unary dereference only for the pointer-like case. No source text, semantic spelling, cache, symbol trace, or ABI path is involved. | `pa34/tests/run/800-builtin-invoke-pointer-like-member-run.t`, a no-header runtime reducer at PA34's existing hosted/vendor `__builtin_invoke` owner; it covers a generic SFINAE result, a libc++-shaped `__mem_fn`, and member-function and member-data calls through a pointer wrapper | Before the fix, `/tmp/cppgm-mem-fn-pointer-like-result-reducer.cpp` retains the concrete invoke-result alias as dependent and drops `__mem_fn::operator()`, while `/tmp/cppgm-builtin-invoke-pointer-like-direct.cpp` reports `invalid pointer-to-member call object`; Clang accepts both. The failure occurs on the initial structured computation. | Warning-clean build; scratch and checked-in reducers pass Clang and cppgm; PA34 direct report passes `313/313`; all configured strict direct-LowIR suites pass; PA34 placement exits clean; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `c08997b35c5025200a77397bfb0006070c6ca7a15ad35e26ea8443db1e65952d`. Full direct report with PA9 explicitly excluded passes `3863/3863`, including PA37 object roundtrip `7/7`. The exact Beast target compiles, links, executes all 243 checks, and exposes four later runtime mismatches; the host-Clang control passes all checks. | instructions -0.57%; max RSS +0.21%; peak footprint +0.17%; pass; versus the preceding frontier instructions improve 0.043%, RSS improves 0.628%, and footprint improves 0.008%; report `/tmp/cppgm-boost-frontier-v2-pointer-like-invoke-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//basic_stream` successful async writes | `T object = T()` was recognized as an empty same-type functional constructor call, but the reconstructed typed constructor action did not retain that it value-initializes the result. LowIR's whole-variable trivial-constructor fast path then treated the zero-argument action as a no-op. Lifetime analysis now propagates the typed value-init fact and lowering zero-fills the target storage. Global collection drops only the exact trivial zero-argument action that targets the whole static object because its emitted backing data is already zero, avoiding redundant dynamic initialization. No source text, cache, symbol trace, ABI path, or witness-only logic is involved. | `pa15/tests/general/300-value-init-empty-functional-cast-aggregate.t`, a header-free aggregate reducer at the earliest value-semantics owner | LLDB showed the handlers received `ENAMETOOLONG`/`EINVAL` with zero bytes. The call into `socket_ops::send` had the correct socket, buffer, length, and flags, while host `sendmsg` saw garbage `msg_name`, `msg_namelen`, `msg_control`, and `msg_controllen` from `msghdr msg = msghdr()`. A dirty-stack non-STL reducer exits 1 and emits no zero stores before the fix; Clang exits 0. This occurs on the initial typed lifetime/lowering path. | Warning-clean build; PA15/PA20/PA21 direct reports pass `504/504`; the strict PA18/19/21/22/23 direct suites pass; placement/hygiene audits for all three touched PAs exit clean; all 23 text-reparse categories remain zero and all 14 audit tests pass. PA21's local aggregate reference now includes the required member zero store; PA20's constant global removes a redundant empty `__cppgm_init`. Normal, all ten individual cache-disabled modes, and all-disabled reducer LowIR are byte-identical with SHA-256 `fe0c1711e8be81559c618807e409715adfca3d4a395fc86b546bfceb292e67b8`. Full direct report with PA9 explicitly excluded passes `3864/3864`, including PA37 object roundtrip `7/7`. The exact final Beast target force-rebuilds 46 targets and passes compile, link, and runtime. | instructions -0.50%; max RSS -0.02%; peak footprint +0.17%; pass; versus the preceding frontier instructions are +0.068%, RSS improves 0.228%, and footprint is +0.002%; report `/tmp/cppgm-boost-frontier-v2-value-init-zero-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//async_base` typed host RTTI for a named local class | The enclosing function-template signature contains the dependent qualified result `result<T0>::type`. Typed ABI construction returned a nominal class-template owner IR for `result<T0>`, but that IR had no template name and could not be emitted; treating construction alone as success suppressed the retained typed owner `TemplateIdSyntax`. Dependent qualified member construction now accepts the first owner IR only when the existing typed `abi_mangle` emitter can encode it, allowing the retained typed template-id owner to supply the valid IR. There is no source or semantic text parse, rendered-symbol fallback, symbol-string deduction, or cache. | `pa33/tests/general/200-host-local-class-dependent-result-rtti-mangling.t`, a header-free local polymorphic class inside a function template with a dependent result and named local template argument, at PA33's host RTTI/local-class mangling owner | Before the fix the exact target reports `failed to derive host typeinfo-name symbol` for `op`. The reducer's ordinary function symbol is already correct, while its `_ZTI` and `_ZTS` symbols fail because the result type cannot be emitted in the typed enclosing context. Clang emits `_ZTIZN4test4readI6streamZNS_3runEvE7handlerEEN6resultIT0_E4typeERT_OS4_E2op` and the corresponding `_ZTS`; cppgm now matches both exactly. Existing PA30 typed `local-type` and dependent-template facts cover the ABI shape, so no assignment or scaffold extension is needed. | Warning-clean build; focused PA33 regression and PA33 direct report pass `81/81`; strict PA18/19/21/22/23 direct suites pass; PA33 placement/hygiene report zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical Mach-O objects with SHA-256 `f395fcc868826d4f31a4a451b4896d2e6324ed4ecbc6dec11cb676455100cafb`, proving the initial typed algorithm independently of caches. Full direct report with PA9 explicitly excluded passes `3865/3865`, including PA37 object roundtrip `7/7`. The exact final Beast target force-rebuilds 46 targets and passes compile, link, and runtime. | instructions -0.47%; max RSS -0.61%; peak footprint +0.12%; pass; versus the preceding frontier instructions move only +0.03 percentage points while both memory measures improve; report `/tmp/cppgm-boost-frontier-v2-local-rtti-owner-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//detect_ssl` Boost.Tribool enum equality | Nonmember operator analysis had already collected ordinary and associated candidates with the correct source visibility, then built a synthetic call scope containing that complete set. Generic call analysis performed ADL again on the synthetic call, bypassing the earlier point-of-definition filtering and admitting three ordinary `operator==` declarations that occur after the inline body. A typed call hint now states that ADL candidates are precollected for unary, binary, compound-assignment, and the existing ADL-only range call path, so generic analysis consumes the supplied set without repeating lookup. Built-in enum equality remains the exact match. This uses the initial AST and typed operands; there is no reparse, source spelling recovery, cache, symbol trace, ABI path, or witness-only behavior. | `pa18/tests/general/100-enum-operator-synthetic-adl-point-of-definition.t`, a header-free reducer at PA18's first function-template/overload-resolution owner; one unrelated earlier operator template keeps synthetic operator resolution active while three later ordinary overloads must remain invisible to the inline body | Before the fix the reducer and Boost `boost/logic/tribool.hpp:141` report the same ambiguity among `operator==(tribool, tribool)`, `operator==(tribool, bool)`, and `operator==(bool, tribool)` even though the operands are the same nested enum type. Clang accepts and runs the reducer. Trace showed the initial candidate set had zero ordinary functions and nine templates in Beast, followed by the generic call path adding the later functions a second time. | Warning-clean build; Clang and cppgm execute the reducer with exit 0; focused PA18 check and direct report pass `229/229`; strict PA18/19/21/22/23 direct suites pass; PA18 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, every one of the ten cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `f369e0baa75087f1f891690d652a6531d7ca793918d5fa07011cd253d7eca0cc`, proving the algorithm independently of caches. The full direct report with PA9 explicitly excluded passes `3866/3866`, including PA37 object roundtrip `7/7`. Exact forced Beast rebuild updates all 46 requested targets and passes compile, link, and runtime. Stable self-compile counters show the removed lookup accounted for 1970 ADL collections, 3940 associated-type visits, and 36307 template-deduction cache checks; the pre-fix sample has 46 redundant-append samples and the fixed sample has zero. | instructions -4.22%; max RSS -1.80%; peak footprint -1.02%; pass; report `/tmp/cppgm-boost-frontier-v2-detect-ssl-adl-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//file_posix` and `//file_stdio` defaulted lambda helper | The initial parser already stores each lambda default argument as a typed AST node in `PreparedLambdaExpression`. The captureless-lambda shortcut represented the lambda as a free function and `auto const` consequently decayed it to a function pointer, whose type cannot carry default arguments. Lambda analysis now reserves that shortcut for call operators without default arguments; the existing synthesized closure/operator path registers the original typed default-argument nodes and handles the call normally. Ordinary captureless lambdas retain the optimized representation. No source or semantic text parse, spelling recovery, cache, symbol trace, ABI path, or witness-only behavior is involved. | `pa24/tests/general/200-captureless-lambda-default-parameter.t`, a header-free reducer in the PA24 lambda cluster | Clang executes `auto const add = [](int lhs, int rhs = 2) { return lhs + rhs; }; add(3)` successfully. Before the fix cppgm bound `add` as `const pointer to function of (int, int)` and rejected the one-argument call, matching Beast `file_test.hpp:164` where `create(path)` calls a captureless lambda whose second parameter defaults to an empty string. | Warning-clean build; Clang and cppgm execute the reducer with exit 0; focused check and PA24 direct report pass `105/105`; all configured strict direct-LowIR suites pass; PA24 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `a90ed23dd84f217c17df08f2f214fc446cd28dfeec95c21d60c02fe2fc4cb961`, proving the initial structured algorithm independently of caches. The full direct report with PA9 explicitly excluded passes `3867/3867`, including PA37 object roundtrip `7/7`. An exact forced Beast rebuild updates 50 targets; both file backends compile, link, run, and pass. | instructions -4.22%; max RSS -1.37%; peak footprint -1.05%; pass; report `/tmp/cppgm-boost-frontier-v2-file-lambda-default-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//flat_buffer` and `//multi_buffer` inherited `suite::fail` calls | Non-template member-body `static_assert` validation uses the normal typed statement analyzer and caches that body output. It ran while the enclosing class was still collecting members, so the inherited call's selected derived-to-base implicit-object adjustment could not be rematerialized through ordinary class completion. The tempting bypass of completion was incorrect: base declarations existed, but non-primary offsets were still zero until layout and the cached output could miscompile. Member-body assertion validation now runs in declaration order immediately after class layout is finalized, so ordinary typed conversion materialization sees the final offsets and the cached output is valid. No cache, source parse, semantic spelling, symbol trace, ABI path, or witness-only behavior is added. | `pa21/tests/spec/300-inherited-member-template-call-during-static-assert-validation.t`, a 20-line header-free reducer at the earliest PA21 member-template owner; `pa26/tests/general/100-static-assert-validation-inherited-nonprimary-member-template.t`, a header-free non-primary-base runtime reducer at the earliest PA26 multiple-inheritance owner | The preserved pre-fix control fails the PA21 reducer with `failed to rematerialize selected call conversions`. Combined tracing on Beast shows `flat_buffer_test` has its direct `suite` base recorded while `complete=no`, `full=yes`. A preserved provisional completion-bypass compiler accepts the PA26 reducer but caches offset `0`, differs from its cache-disabled `+4` LowIR, and exits `1`; Clang and the final compiler exit `0`. This proves why the bypass was rejected and that the final phase-ordering fix is not masked by the body-output cache. | Warning-clean build; controlled pre-fix and rejected-bypass proofs; focused PA21/PA26 checks and direct report pass `296/296`; the existing unused-member false-assertion negative still passes; all configured strict direct-LowIR suites pass; both placement/hygiene audits report zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. For both reducers, normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `52f9e5b5007e9080406c04051c9825009f31d64f93d16b06ff9d252950d57678` and `b80bdd5754d90a8f76d247d0a9d24fa13d4ecb28bd8b8b69a0167dbd7474b07a`. Both exact Beast objects compile cleanly from the captured survey commands. The full direct report with PA9 explicitly excluded passes `3869/3869`, including PA37 object roundtrip `7/7`. | instructions -4.05%; max RSS +0.26%; peak footprint -0.96%; pass; report `/tmp/cppgm-boost-frontier-v2-postlayout-static-assert-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//flat_buffer` and `//multi_buffer` same-type copy assignment | Out-of-class member-definition collection partitions the surrounding template heads into the class-template owner prefix and an optional member-template suffix. The typed prefix counter recognized the owner only when the incoming head was larger than the owner head. With equal arity it offered the owner parameter to member-template lookup, so `operator=(basic_flat_buffer const&)` was attached to the adjacent `template<class OtherAlloc> operator=(basic_flat_buffer<OtherAlloc> const&)` declaration after matching `OtherAlloc = Allocator`; the real copy-assignment binding remained declaration-only. Equal arity now means the complete head is the owner prefix, leaving an empty member-template suffix. Nested member-template definitions retain their larger combined head and continue through the same structured matcher. No source text, rendered signature, cache, symbol, ABI, or witness-only path is added. | `pa21/tests/spec/300-out-of-class-copy-assignment-before-member-template-overload.t`, a header-free runtime reducer at the earliest PA21 member-template owner; it places a same-type copy assignment before a cross-type assignment member template, defines both out of class, and executes both selected overloads | The preserved pre-fix compiler emits only `declare function @box_int___operator_` and `lowir2native` rejects the unresolved symbol; trace classifies both retained definitions as `method-template=yes`. The fixed trace classifies the same-type definition as `method-template=no`, records it in the ordinary out-of-class definition map, and attaches a body to the selected copy-assignment binding. Clang and the fixed compiler execute the reducer with exit 0. Exact Beast objects previously contained move and cross-allocator assignment definitions but left the same-type symbol undefined. | Warning-clean build; focused PA21 check and direct report pass `227/227`; all configured strict direct-LowIR suites pass; PA21 placement/hygiene exits clean; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical reducer LowIR with SHA-256 `8e9cdf995696d2e239e4c3d300e21b94f91d965b2a3dbb8258635d5acf7fefb2`. Exact forced `flat_buffer` and `multi_buffer` targets compile, link, run, and pass. The full direct report with PA9 explicitly excluded passes `3870/3870`, including PA37 object roundtrip `7/7`. | instructions -4.03%; max RSS +0.34%; peak footprint -0.95%; pass; +0.026% instructions, +0.083% RSS, and +0.011% footprint from the preceding checkpoint; report `/tmp/cppgm-boost-frontier-v2-ooc-copyassign-template-head-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//span` dependent constructor symbol | The constructor ABI adapter retained the parsed template argument and expression AST, but did not substitute a concrete enclosing class NTTP into the dependent function-template expression type and could not resolve an unqualified namespace constant as a typed variable entity. The same exact-symbol comparison exposed related structured cases: concrete owner type packs in function types, source-written default omission, dependent member-expression owner suppression, enumerator values, unsigned modulo-width values, internal-linkage namespace variables, and standard/inline namespace variable names. These paths now consume `TemplateParameterInfo`, `TemplateArgument`, `ValueBinding`, retained declarator/type-id AST, and `abi_mangle::Type`/`DependentExpression`; scoped variables use the typed `emit_variable_symbol_from_name_components` API. No source or semantic text is reparsed, no rendered symbol is decomposed, and no cache or witness-only path is added. | `pa22/tests/general/500-constructor-sfinae-namespace-constant-symbol.t`, a header-free constructor-SFINAE reducer at the earliest full-deduction/SFINAE owner; PA30 adds direct typed facts for `uint`/`ulong` modulo values, internal variables, `std` variables, and inline namespaces. Existing PA22/PA23 dependent ABI reducers were strengthened through their exact LowIR references. | The exported assignment reference compiler fails the new PA22 reducer with the same `failed to build ABI IR function symbol for weak function span<char const, -1>::span` diagnostic, so its new LowIR reference was generated from the fixed compiler only after byte-for-byte Clang symbol verification; no witness reference came from cppgm. Clang and cppgm now agree on the exact Boost C1/C2 symbol, including `Lm18446744073709551615E`, `L_ZN5boostL14dynamic_extentEE`, and the dependent `enable_if` expression. Clang controls also establish `_ZL7extract`, `_ZN3libL17default_alignmentE`, `_ZSt7nothrow`, `_ZNSt3__14cerrE`, and unchanged anonymous-namespace encoding. Fifty-four existing LowIR references were corrected from the old missing internal-linkage marker after these controls. | Warning-clean Clang build and isolated GCC build; focused affected report passes `1859/1859`, final PA30 passes `88/88`, all configured strict suites pass, placement/hygiene has zero findings, all 23 text-reparse categories remain zero, and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical reducer LowIR with SHA-256 `47ad2284407d9fb51998d9a2b0bef6be441b151890e38a7eadd4538fd24c7434`, proving the initial typed algorithm independently of caches. The exact `span` target force-rebuilds 46 targets, links, executes, and passes. The complete direct report with PA9 explicitly excluded passes `3876/3876`, including PA37 object roundtrip `7/7`; PA9 was not rerun. | instructions -3.95%; max RSS -0.11%; peak footprint -0.93%; pass; rolling deltas are +0.080% instructions, -0.445% RSS, and +0.020% footprint, below the hotspot threshold; report `/tmp/cppgm-boost-frontier-v2-span-dependent-abi-final-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//static_string` | Three typed defects surfaced in sequence. Class-template instantiation replay accepted any dependent specialization as a canonical source owner, so a contextual `pair` specialization could supply the wrong member set; ownership now requires the exact primary parameter placeholders for type, value, and template-template parameters. Dependent expression comparison ignored operator payload whenever a node had children, making `condition` and `!condition` equivalent; non-wrapper nodes now compare their retained operator/value metadata. Overload ranking now applies the standard initializer-list preference for braced-list arguments. Finally, out-of-class member-template definitions are matched by typed parameter identity, use typed result identity only to distinguish ambiguous return-SFINAE overloads, and accept a unique parameter match without forcing alias normalization. No source text, rendered signature, cache, symbol, ABI, or witness-only path is added. | `pa22/tests/spec/300-out-of-class-return-sfinae-overload-definitions.t`, a header-free complementary return-SFINAE/body-identity reducer at the PA22 member-template owner; `pa25/tests/general/200-braced-list-prefers-initializer-list-exact-rank.t`, a header-free list-initialization overload reducer at PA25 | The original exact target first failed converting contextual `pair` instantiations; all caches disabled reproduced it, proving the source-owner algorithm itself was wrong. After that fix, the PA25 reducer and Boost `assign({'1','2','3'})` were ambiguous. The PA22 reducer selected the wrong body before typed result matching. Once those were fixed, `/tmp/cppgm-set-emplace.cpp` linked with an undefined libc++ `__tree::__find_equal`; trace showed the unique declaration could not be attached when its unqualified member-alias result and qualified dependent definition result were not normalized identically. Clang accepts all reducers. | Warning-clean Clang build and isolated GCC 15 build; exact Boost target passes compile, link, and runtime; PA22/PA25 direct report passes `333/333`; all configured strict direct-LowIR suites pass; placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical reducer LowIR with SHA-256 `e5a3c04a5d08833e3a050bcd9fb70d8229f79761d8c1efe86c90b5328e88ed2e` and `f2e31e665b2cdd6c7325abf3c8365e4dc33f482d6bc7e7e1bcb0f11430d5ed50`. Full direct report with PA9 explicitly excluded passes `3878/3878`, including PA37 object roundtrip `7/7`; PA9 was not rerun. Hotspot counters record 113146 node visits and 475073 semantic queries; a 1 ms sample puts the new source-owner predicate at 11 top-of-stack samples, with no top samples in the member resolver or dependent-expression comparator. | instructions +0.95%; max RSS -1.07%; peak footprint +0.10%; pass; result-type comparison was moved off the unique-match path; report `/tmp/cppgm-boost-frontier-v2-static-string-final-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//basic_parser` default-capturing lambda | The implicit-capture scan handled an unqualified id-expression only through `CppAstNode::value`. An explicit template-id keeps its identifier in the initially parsed `TemplateIdSyntax`, so `parse<int>(...)` did not record the enclosing `this` capture and later tried to call `parser_test::parse` on the lambda closure object. Capture discovery now reads the retained structured template identifier and uses that name for value, member, and member-template lookup. Explicit `this->` calls and ordinary unqualified member calls retain their existing paths. No tokenization, source scan, semantic text parse, rendered name recovery, cache, symbol trace, ABI path, or witness-only behavior is added. | `pa24/tests/general/200-default-capture-unqualified-member-template-call.t`, a header-free runtime reducer in the PA24 lambda cluster; it combines default `&` capture, an unqualified explicit member-template call, a nested lambda argument, and a defaulted trailing function parameter | The current head and the all-cache-disabled mode reject the reducer with `implicit object conversion failed`, while Clang executes it successfully. An explicit `this->parse<int>` control and an implicit non-template member-call control both pass, isolating capture discovery for the structured template-id. Serial forced Beast replay reproduces the same `parsegrind` diagnostic before the fix. | Warning-clean Clang build and isolated GCC 15 build; Clang and cppgm execute the reducer; PA24 focused check and direct report pass `106/106`; all configured strict direct-LowIR suites pass; PA24 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `ef07873fe2af6d7c17487591172c4efbbd018c21f09df6a75fdda86469df81f6`. The final full direct report with PA9 explicitly excluded passes `3879/3879`, including PA37 object roundtrip `7/7`; PA9 was not rerun. A first two-assignment report had one PA33 compiler process exit `139` under load; the exact PA33 case and the complete one-assignment rerun pass. Final forced exact Beast replay has no `parsegrind`/implicit-object diagnostic and advances to the independent libc++ `nextafter` ambiguity. | instructions -3.01%; max RSS -0.61%; peak footprint -0.82% versus the fixed baseline; rolling instructions +0.029%, below hotspot threshold; report `/tmp/cppgm-boost-frontier-v2-basic-parser-lambda-capture-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//basic_parser` libc++ `nextafter(double, float)` selection | The initial parser and alias-template substitution retained the variadic `decltype((Types() + ...))` operand as structured AST, and function-template deduction correctly bound `double`, `float`, and the defaulted SFINAE value. Both declaration-time `decltype` analysis and concrete alias instantiation nevertheless sent the bound fold node to ordinary PA12 expression analysis, which does not evaluate fold expressions; the arithmetic-template result stayed dependent and the viable exact-match `nextafter` candidate was discarded. Both typed paths now use the existing constexpr fold reducer before normal expression analysis. Recursive replacement copies an operand only after a bound fold actually reduces. No tokenization, semantic text parse, source scan, cache, symbol, ABI, or witness-only path is added. | `pa34/tests/compile/600-fold-expression-alias-decltype-result.t`, a header-free variadic alias/function-result reducer in the existing PA34 fold-expression hosted-compatibility cluster | `/tmp/cppgm-alias-template-fold-min.cpp` fails before the fix by omitting the only `choose(double, float)` specialization while Clang accepts it. Exact libc++ trace shows deduction succeeds with `double|float|0`, then `std::__1::__promote_t<double,float>` remains dependent and the candidate is dropped. The system-header reduction `/tmp/cppgm-nextafter-system.cpp` reproduces the same ambiguity. | Warning-clean Clang build and isolated GCC 15 build; header-free and system-header reducers pass; focused PA34 test and direct report pass `314/314`; all configured strict direct-LowIR suites pass; PA34 placement exits cleanly; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `90bc9b6f0c8771bbc6b44556dfa72c09ceb9a2ccee93780f28b2e4247af601b9`, proving the uncached algorithm. The full direct report with PA9 explicitly excluded passes `3880/3880`, including PA37 object roundtrip `7/7`; PA9 was not rerun. The final forced exact Boost graph updates all 46 targets and passes compile, link, and runtime. | instructions -3.03%; max RSS -0.86%; peak footprint -0.82% versus the fixed baseline; rolling instructions -0.025%; report `/tmp/cppgm-boost-frontier-v2-fold-alias-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//file_body` constructor-template recursion and missing reader/writer definitions | Implicit-conversion constructor selection carried `allow_explicit=false`, but function-template deduction and default substitution ran before the later instantiated-binding filter rejected explicit constructors. Beast's explicit forwarding constructor default asks `!is_convertible<Arg, header>`, so `__is_convertible` recursively evaluated the same default on the normal path. Both constructor-template collectors now reject an ineligible explicit declaration before deduction. After compilation terminated, the link exposed a separate typed replay defect: the concrete nested `reader`/`writer` classes had to be prepared from the outer template's retained out-of-class member-class definition, and their replayed constructor-template declarations shared the exact class/function AST nodes with the stored definition but had no nested `source_template` pointer. Reference-only preparation now invalidates stale forward members, binds the typed outer arguments, and exact shared class-declaration identity attaches the stored body. No cache, guard, text parse, source scan, rendered signature, symbol recovery, ABI fallback, or witness-only path is added. | `pa21/tests/spec/300-out-of-class-nested-class-member-template-constructor.t`, a header-free two-parameter nested constructor definition/replay reducer at the PA21 member-template owner; `pa34/tests/compile/700-nonstl-reentrant-constructor-default-convertible.t`, a header-free `__is_convertible` versus direct `__is_constructible` reducer at the PA34 builtin-trait owner | The pre-fix compiler segfaults on the compact recursive-conversion assertion while Clang accepts it. The strengthened PA21 reducer compiled but left its typed C1 constructor undefined; the fixed object defines matching C1/C2 symbols. The compact Boost reducer likewise changed from undefined reader/writer C1 references to the same four C1/C2 definitions emitted by Clang, without changing their typed ABI spelling. The full translation unit now completes instead of crashing, and the forced exact target links and runs. | Warning-clean Clang build and isolated GCC 15 build; PA21 and PA34 direct reports pass `228/228` and `315/315`; all configured strict direct-LowIR suites pass; both placement/hygiene audits exit cleanly; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical PA34 reducer LowIR with SHA-256 `829527de4efad69a104bbdaf01367fedbdfdff6a733020ee0795192296139aff`, proving the terminating algorithm independently of caches. Direct comparison justified two existing PA22/PA23 LowIR ordinal updates because the invalid explicit candidate is no longer instantiated; their object symbol and bodies are unchanged and no witness references changed. The full report with PA9 explicitly excluded passes `3882/3882`, including PA37 object roundtrip `7/7`; PA9 was not rerun. The exact forced target rebuilds 46 targets and passes compile, link, and runtime. | instructions -3.19%; max RSS -0.62%; peak footprint -1.04% versus the fixed baseline; rolling deltas are -0.166% instructions, +0.247% RSS, and -0.218% footprint; report `/tmp/cppgm-boost-frontier-v2-file-body-nested-member-template-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//message` default construction trait | Explicitly defaulted default constructors were registered as definitions, but implicit-deletion refresh covered only synthesized default constructors and defaulted copy/move assignments. The `message<one_arg_body>` base chain therefore retained a live default constructor even though `one_arg_body::value_type` cannot be default-constructed. The single-type `__is_constructible` evaluator also treated any selected constructor as success without checking `is_deleted`. Completed classes now refresh only their typed constructor bucket through the existing implicit-default-constructor deletion algorithm, and the builtin trait rejects a deleted selection. No cache, text parse, source scan, signature rendering, symbol path, ABI path, or witness-only behavior is added. | `pa16/tests/spec/100-defaulted-default-constructor-deleted-member-bad.t`, a header-free compile-fail reducer at the earliest defaulted-special-member owner; `pa34/tests/compile/700-nonstl-defaulted-constructor-deleted-trait.t`, a header-free class-template/builtin-trait reducer matching the Beast shape | The immutable `37d54feea` survey fails Beast's `!std::is_constructible<request<one_arg_body>>::value` assertion while Clang accepts it. The PA16 reducer confirms that direct default construction must select an implicitly deleted constructor; the PA34 reducer confirms the same state through a concrete class-template specialization and `__is_constructible`. | Warning-clean Clang build and isolated GCC 15 build with repository warning flags; focused PA16/PA34 direct report passes `481/481`; all configured strict direct-LowIR suites pass; both placement/hygiene audits report zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The PA9-excluded broad report passes `3883/3884`; its only miss is the previously observed load-sensitive PA33 local-class RTTI runtime exit `139`, whose exact check and complete isolated PA33 report pass `81/81`. PA37 object roundtrip passes `7/7`; PA9 was not run. The exact forced message target rebuilds 46 targets and passes compile, link, and runtime. | instructions -3.25%; max RSS -0.68%; peak footprint -0.99% versus the fixed baseline; rolling deltas are -0.06, -0.06, and +0.05 percentage points; report `/tmp/cppgm-boost-frontier-v2-message-defaulted-ctor-perf-final.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//type_traits` namespace-alias-qualified `decltype` assertion | The initial parser retained the complete `decltype(*net::buffer_sequence_begin(...))` AST and its typed `QualifiedName`, and the template argument retained its original source-token start. Fragment parsing necessarily gave the nested AST fragment-relative token offsets, but only non-type argument evaluation activated the existing source-anchor rebasing path. Type-template argument resolution therefore resolved `net` to the correct namespace and found both direct function templates, then declaration-order filtering compared their real declaration positions with the fragment-relative call position and removed them. Structured type-id resolution now threads `TemplateArgumentSyntax::source_token_start` into the existing clone-and-rebase path for `decltype`; no text, qualified name, template-id, source line, or symbol is parsed or reconstructed, and no cache is added. | `pa21/tests/general/100-namespace-alias-qualified-function-decltype-type-argument.t`, a header-free qualified-call/partial-specialization reducer at the earliest PA21 class partial-specialization owner | Clang accepts the reducer while the pre-fix compiler reports `failed class template instantiation: is_same<decltype(*alias::sequence_begin(...)), int const&>`. Typed trace proves the qualifier resolves to namespace `source` and its direct bucket contains both `sequence_begin` templates before source-order filtering returns zero candidates. The fixed compiler accepts the same retained AST, and exact Beast replay clears the line-40 assertion before reaching the independent line-97 nested iterator type failure. | Warning-clean Clang build and isolated full GCC 15 build; PA21 direct report passes `229/229`; all configured strict direct-LowIR suites pass; PA21 placement/hygiene exits clean; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode compile successfully and emit byte-identical LowIR with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`, proving the uncached algorithm. The full direct report with PA9 explicitly excluded passes `3885/3885`, including PA37 object roundtrip `7/7`; PA9 was not run. The exact target rebuilds 46 targets and advances to the next independent assertion. | instructions -3.21%; max RSS -0.40%; peak footprint -0.99% versus the fixed baseline; rolling deltas are +0.04, +0.28, and 0.00 percentage points; report `/tmp/cppgm-boost-frontier-v2-type-argument-token-anchor-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/http//type_traits` direct nested iterator member type | The structured qualifier walker prepared the concrete outer specialization and nested `const_iterator` reference, but its final concrete member lookup did not request class completion on a miss. That left an out-of-class nested definition unpopulated unless unrelated earlier use had completed it. The final lookup now uses the same `complete_class_on_miss` behavior already used by the parallel typed bound-owner path. It consumes the retained qualifier AST and concrete class scope; no retry, cache, text parse, spelling recovery, or symbol path is added. | `pa18/tests/general/300-direct-member-type-completes-out-of-class-nested-definition.t`, a header-free explicit type-argument reducer at the earliest PA18 class-template instantiation owner | The isolated parent compiler rejects the reduced `holder<adaptor<int>::iterator::value_type>` alias while Clang accepts it. Typed Beast trace shows the three-part `buffers_suffix` outer aliases resolving correctly, then the fresh nested iterator's final `value_type` lookup returning no match; an unrelated two-part specialization worked only because an earlier `begin()` expression had populated its iterator. | Warning-clean Clang build and isolated GCC 15 build; PA18 direct report passes `230/230`; all configured strict direct-LowIR suites pass; PA18 placement/hygiene exits clean; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `415531a392849ab88a2bdd853f5d3708915e2d825cef9639330f966787f493f8`, proving the uncached algorithm. The full direct report with PA9 explicitly excluded passes `3886/3886`, including PA37 object roundtrip `7/7`; PA9 was not run. The exact forced target rebuilds all 46 targets and passes compile, link, and runtime. | instructions -3.27%; max RSS -0.63%; peak footprint -0.96% versus the fixed baseline; rolling deltas are -0.06, -0.23, and +0.03 percentage points; report `/tmp/cppgm-boost-frontier-v2-direct-nested-member-completion-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/zlib//deflate_stream`, `//inflate_stream` | Out-of-class member-template definition matching treated every excess incoming template parameter as a possible owning-class template prefix. On the non-template `suite` owner, the concrete `<E, F, String>` definition therefore suffix-matched the neighboring `<F, String>` overload as well as its real declaration, leaving the specialization body unattached. The ordinary and special-member resolvers now allow suffix matching only when typed owner metadata establishes an actual class-template prefix. No cache, parser, text recovery, symbol construction, ABI path, or witness-only behavior changes. | `pa22/tests/general/100-out-of-class-member-template-overload-head-definition.t`, a header-free reducer at the placement-audited PA22 full function-template deduction owner | The isolated parent compiler at `dab6c27ae` compiles the reducer but fails host link with an undefined `suite::except<problem, callable&, char[1]>`; Clang and the fixed compiler link and run it. The exact Beast object had four undefined concrete `suite::except` bodies before the fix. Typed `nm` comparison showed cppgm's undefined mangled names already matched Clang's definitions byte-for-byte, isolating declaration attachment rather than ABI construction. | Warning-clean fresh Clang and isolated GCC 15 builds; PA22 direct report passes `264/264`; all configured strict direct-LowIR suites pass; PA22 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `9c6c37c2e6f213e8a4c620dc82931f0e600b04f450f274760deb502a09ca00b2`. The full direct report with PA9 explicitly excluded passes `3887/3887`, including PA37 object roundtrip `7/7`; PA9 was not run. Both forced exact Beast targets rebuild 59 targets and pass compile, link, and runtime. | instructions -3.31%; max RSS -1.53%; peak footprint -0.96%; pass; rolling deltas are -0.045% instructions, -0.913% RSS, and +0.004% footprint; report `/tmp/cppgm-boost-frontier-v2-member-template-overload-head-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/bench/parser//nodejs_parser` | Namespace-scope embedded-type collection required the adjacent declarator to be a bare identifier, rejecting the standard `static struct { ... } table[]` form even though the initial parser retained the complete anonymous `class-specifier` and array declarator. The full semantic path now derives the object name through the structured declarator while rejecting function declarators, collects the class, and attaches its typed result to the retained specifier. PA11's independent type-model analyzer performs the same initial-AST operation. No text parse, source scan, cache, symbol, ABI, or witness-only path is added. | `pa11/tests/spec/200-namespace-anonymous-class-array-object.t`, a header-free anonymous-class array declaration at the explicit PA11 namespace-scope anonymous-type and array type-model owner | The controlled parent compiler rejects the reducer both in `--emit-types` with `unsupported decl-specifier-seq` and in the normal compiler with `unsupported namespace-scope embedded type-specifier`; Clang accepts it. The exact Beast failure is the same shape with a 33-element initialized table. The pre-fix forced log is `/tmp/boost-frontier-v2-suite-010-nodejs-parser-focused-prefix.log`. | Warning-clean Clang and GCC 15 recompiles of both changed modules; PA11 direct report passes `51/51`; all configured strict direct-LowIR suites pass; all 23 text-reparse categories remain zero and all 14 audit tests pass. The PA11 Required Features explicitly own this scaffold behavior; the generic later-feature placement scanner is not applicable to PA11 and reports 115 pre-existing findings across that assignment. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical type output with SHA-256 `23bf2a8e4b49fd24de934d5bc901d26f3566472aea7267cce5dbff692f40a434`. The full direct report with PA9 explicitly excluded passes `3888/3888`, including PA37 object roundtrip `7/7`; PA9 was not run. The forced exact Beast target updates two targets and passes. | instructions -3.36%; max RSS -0.24%; peak footprint -0.99%; pass; rolling deltas are -0.052% instructions, +1.31% RSS, and -0.030% footprint; report `/tmp/cppgm-boost-frontier-v2-anonymous-class-array-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/bench/parser//bench_parser` | The initial lazy-body parser knew the namespace `fields` type but its class-member snapshots omitted function-definition value names and member function-template value-template names. It therefore parsed `fields(db);` as a declaration of `db` with the outer `fields` type, shadowing the actual buffer parameter. Class snapshots now retain both typed name sets, and a same-scope template-name tie prefers the value-template name during the original parse. No semantic fallback, AST synthesis, source scan, text parse, cache, symbol, ABI, or witness-only path is added. | `pa15/tests/general/200-member-call-hides-outer-type-declaration.t`, a header-free non-template reducer in the existing PA15 ordinary member lookup/call cluster | The controlled parent compiler and exact Boost target both fail because the following `db.prepare(...)` is looked up on `fields`; Clang accepts the reducer. Final `--emit-ast` output shows `fields(db);` directly as an expression-statement containing a call-expression, proving that the original parse selects the correct grammar. | Warning-clean Clang build and isolated GCC 15 compile; PA10/PA15 direct report passes `342/342`; all configured strict direct-LowIR suites pass; PA15 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `427300794763be04f2206bdc3d62817ed6ec9166d7deef599e6065d37db2e3f0`. The full direct report with PA9 explicitly excluded passes `3889/3889`, including PA37 object roundtrip `7/7`; PA9 was not run. The forced exact Beast target updates two targets and passes compile, link, and runtime. | instructions -3.27%; max RSS -0.82%; peak footprint -0.91%; pass; rolling deltas are +0.099% instructions, -0.582% RSS, and +0.084% footprint; report `/tmp/cppgm-boost-frontier-v2-member-call-initial-parser-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/bench/wsload//wsload-compile` decorator member | The initial parser retained the complete nested pointer declarator, parameter clause, and `function-qualifier` for `void (*move)(storage&, storage&) noexcept`. The shared typed declarator builder rejected every function qualifier, relying on method-specific filtering that correctly does not run for a pointer-to-function object. In C++11 the exception specification is not part of the function type, so the builder now consumes the structured qualifier only when the immediately preceding suffix is a function. No tokenization, parsing, text recovery, cache, symbol, ABI, or witness-only path is added. | `pa11/tests/general/300-noexcept-function-pointer-declarator.t`, a header-free zero-parameter pointer-to-function object at the placement-audited PA11 exception-specification/type-model owner | The controlled parent rejects the PA11 reducer with `unsupported declarator`, while Clang accepts it. Removing only `noexcept` from the exact class-member reducer makes the pre-fix compiler pass, and final `--emit-ast` output shows the qualifier already retained after the function parameter clause. | Warning-clean Clang build and isolated GCC 15 compile; PA11 direct report passes `52/52`; all configured strict direct-LowIR suites pass; the focused PA11 placement audit reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical type output with SHA-256 `4262355654d9faeb98f7acb7b35fb1e3973965e2f58de81f3f98b4af2acfa648`. The full direct report with PA9 explicitly excluded passes `3890/3890`, including PA37 object roundtrip `7/7`; PA9 was not run. Forced exact `wsload-compile` clears the decorator failure and reaches an independent alias-template argument-resolution failure. | instructions -3.25%; max RSS -0.93%; peak footprint -0.94%; pass; rolling deltas are +0.021% instructions, -0.107% RSS, and -0.031% footprint; report `/tmp/cppgm-boost-frontier-v2-noexcept-function-pointer-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/bench/wsload//wsload-compile` nested `impl_type::build_response` definition | Initial parsing and semantic declaration collection retained the complete nested owner, member-template headers, alias-template arguments, and typed function signature. Ordinary out-of-class method-template resolution then looked up direct templates on a forward nested-class reference without first applying the outer class template's retained out-of-class member-class definition. The direct template bucket was empty, so collection misclassified the definition as an ordinary member and later analyzed its `Body` and `Allocator` parameters outside their member-template scope. The ordinary resolver now uses the same typed nested-owner preparation already used by the special-member resolver and aligns the effective template headers with the retained owner-header count. No parsing, source scan, text recovery, cache, symbol, ABI, or witness-only path is added. | `pa21/tests/spec/300-nested-owner-out-of-class-member-template-alias-parameter.t`, a header-free explicit-template-argument reducer at the placement-audited PA21 member-template declaration/ownership owner | The controlled parent rejects the reducer with the exact `failed alias template argument resolution for request` diagnostic while Clang and the fixed compiler compile and execute it. Typed tracing before the fix shows the correct initial `request<Body, fields<Allocator>>` signature followed by `method-template=no`; the fixed resolver finds the retained member-template declaration. The exact Boost target clears the alias failure and reaches the independent `detail::frame_header fh;` local declaration at `stream_impl.hpp:948`. | Clang and GCC 15 compile the changed module successfully with no warnings attributable to the change; PA21 direct report passes `230/230`; all configured strict direct-LowIR suites pass; PA21 placement/hygiene reports zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `9e66ee08dd00992c5a8dce4a27e7bf7deba33a20e4bc8bba2fe8b46f0fc23d8f`, proving the corrected uncached algorithm. The full direct report with PA9 explicitly excluded passes `3891/3891`, including PA37 object roundtrip `7/7`; PA9 was not run. | instructions -3.36%; max RSS -0.95%; peak footprint -0.94%; pass; rolling deltas are -0.114% instructions, -0.022% RSS, and -0.006% footprint; report `/tmp/cppgm-boost-frontier-v2-nested-owner-member-template-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/bench/wsload//wsload-compile` local `detail::frame_header` declaration | The original lazy-body parse retained the local using-directive and the structured `detail::frame_header` qualified name. Unqualified qualifier lookup nevertheless searched a block-scope using-directive as though the nominated namespace's members were declared in that block, so `boost::endian::detail` hid the nearer `boost::beast::websocket::detail`. Using-directive targets now participate only at their nearest common ancestor namespace injection scope. Direct declarations are checked at each closer scope, and imported namespace/type candidates are merged with ambiguity detection only at the proper injection level. The common no-directive path performs only its parent walk; recursive visited-state allocation occurs only after a real using-directive is encountered. No tokenization, reparse, source scan, cache, symbol, ABI, or witness-only path is added. | `pa12/tests/general/200-local-using-directive-preserves-nearer-namespace-type.t`, a header-free reducer at the placement-audited PA12 using-directive lookup owner | The controlled parent rejects the local declaration with `unsupported local declaration decl-specifier`, and disabling qualified-type lookup caching reproduces the same failure, proving the uncached algorithm is wrong. Clang and the fixed compiler resolve `detail::frame_header` to the nearer namespace typedef. A nested class/member-template control preserves the exact Boost scope shape. The forced exact Beast target rebuilds two targets and passes compile, link, and runtime. | Warning-clean build; isolated Clang and GCC 15 compilation report no warnings attributable to the changed module. PA12 direct and all configured strict direct-LowIR suites pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical semantic output with SHA-256 `12ee21c6814d14be02c4bbf219f7512065e5210c8c7b833ee8aaba282ccf011c`. The isolated reducer placement report has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. The full direct report with PA9 explicitly excluded passes `3892/3892`, including PA37 object roundtrip `7/7`; PA9 was not run. | instructions -2.84%; max RSS -1.18%; peak footprint -0.89%; pass; rolling deltas are +0.532% instructions, -0.225% RSS, and +0.052% footprint; report `/tmp/cppgm-boost-frontier-v2-using-directive-injection-scope-perf.json` | `(this commit)` |
| fixed | `libs/beast/test/beast/core//tcp_stream`, `//basic_stream` | Out-of-class member-template definition matching received the flattened outer and member headers `{T, U}`, but the directly nested non-template `impl_type` has no `source_template` of its own. The typed owner-prefix helper therefore reported zero and could not compare the declaration's `{U}` suffix, leaving constructor and ordinary member-template bodies unattached. When the direct class has no source template, the helper now recognizes its enclosing dependent class-template instantiation as the prefix owner and applies the existing partial-specialization-aware parameter count. No parser, source scan, text recovery, cache, symbol construction, ABI, or witness-only path is added. | `pa21/tests/spec/300-inline-nested-owner-out-of-class-member-template-definitions.t`, a header-free constructor plus ordinary member-template runtime reducer at the placement-audited PA21 member-template ownership cluster | The exact parent compiler at `8c7cef0e8` emits only declarations and fails host link for both concrete bodies, while Clang accepts and runs the reducer. Both Beast objects likewise contain undefined concrete `impl_type` member-template symbols whose mangled names exactly match the definitions expected by Clang, isolating declaration attachment rather than ABI construction. Typed trace reports the declaration's `{U}` against incoming `{T, U}` before the fix and shows that the direct nested class has no source template while its enclosing typed owner does. | Warning-clean Clang and GCC 15 compiles; PA21 direct report passes `231/231`; all configured strict direct-LowIR suites pass; PA21 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `b9a8841536907348b5b7224af36f12aa001dd628e218e6a70d9543c220027c61`. The full direct report with PA9 excluded passes `3893/3893`, including PA37 object roundtrip `7/7`. Forced exact `tcp_stream` and `basic_stream` replays pass, and the final full Beast survey updates all 447 targets and exits successfully. | instructions -2.81%; max RSS -0.43%; peak footprint -0.86%; pass; rolling deltas are +0.040% instructions, +0.754% RSS, and +0.030% footprint; report `/tmp/cppgm-boost-frontier-v2-inline-nested-owner-template-prefix-perf.json` | `(this commit)` |
| fixed | `libs/bimap/test//test_bimap_unconstrained` and the common MP11 failure cluster | Recursive partial-specialization expansion had an exact `TypePtr` for each bound pack element, but when the expression expansion itself was unchanged it reconciled the original `U...` syntax against the already substituted type spelling and emitted a null syntax. Later class-template materialization then lacked a typed source syntax and incorrectly retried lexical lookup at the template definition point. `expand_template_argument_inputs` now creates an owned expanded type-pack argument syntax from the retained source node and exact semantic type. This fixes the uncached structured algorithm; no parsing, source scan, rendered-name lookup, or cache is added. | `pa21/tests/general/400-recursive-pack-alias-carries-expanded-type-syntax.t`, a header-free recursive conditional-alias/partial-specialization reducer at the earliest complete integration owner | The exact parent compiler at `749b6b0d2` rejects the checked-in reducer while Clang and the fixed compiler compile and run it. The failure resolves `tags::first` from the later recursive specialization at the template definition point even though the exact type was already bound, isolating loss of typed syntax rather than lookup or caching. | Warning-clean Clang and GCC 15 compiles; PA21 direct report passes `232/232`; all configured strict direct-LowIR suites pass; PA21 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The full direct report with PA9 excluded passes `3894/3894`, including PA37 object roundtrip `7/7`; PA9 was not run. Forced focused `test_bimap_unconstrained` passes, and the exact full Bimap rerun removes all 22 original positive compile failures, leaving two independent positive failures. | instructions -2.81%; max RSS -0.66%; peak footprint -0.97%; pass; rolling deltas are -0.006% instructions, -0.230% RSS, and -0.112% footprint; report `/tmp/cppgm-boost-frontier-v2-typed-pack-syntax-perf.json` | `(this commit)` |
| fixed | `libs/bimap/test//test_bimap_extra` | The partial-specialization matcher persistently cached one placeholder `TypePtr` per argument position. A pattern containing `typename member_with_tag<Tag, Relation>::type` is match-scope sensitive: reusing the same dependent qualified-member graph across candidate scopes left the later concrete `member_with_tag<left, bimap_core<int>>::type` unresolved, so `is_tag_of_member_at_left` and then `core_iterator_type_by` selected their primaries. Placeholder patterns that structurally contain a dependent qualified member are now excluded only from that persistent cross-match cache. Each match semantically resolves the retained initial `TemplateArgumentSyntax`; no source text is tokenized, parsed, reconstructed, or looked up by spelling, and ordinary placeholder patterns remain cached. | `pa22/tests/spec/300-dependent-qualified-member-partial-cache.t`, a header-free, single-base `enable_if`/partial-specialization reducer at PA22's substitution/SFINAE owner | Clang and the fixed compiler accept the reducer, while the controlled pre-fix compiler rejects it selecting primary `core_iterator_type_by`. Disabling the placeholder cache globally makes the exact Boost target pass, isolating an invalid cache assumption rather than an enumerator algorithm. Normal, witness mode (which disables the placeholder cache), and trace mode (which disables class-specialization selection caching) emit byte-identical LowIR with SHA-256 `3379649e9dc6be90c522617d640e43e970e063a9508c1e7ec7822c2b0313c3d5`. | Warning-clean Clang and GCC compiles; focused PA22 check and direct report pass `265/265`; all configured strict PA18/19/21/22/23 direct-LowIR suites pass; PA22 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. The PA9-excluded broad direct report completes with all assignment statuses zero, including PA37 object roundtrip; PA9 was not run. Forced `test_bimap_extra` compiles, links, runs, and passes. | instructions -2.66%; max RSS -1.32%; peak footprint -0.99%; pass; rolling deltas are +0.06% instructions, -0.80% RSS, and +0.06% footprint; logs `/tmp/cppgm-boost-frontier-v2-qualified-member-cache-fixed-perf.log` and `/tmp/cppgm-boost-frontier-v2-qualified-member-cache-rolling-perf.log` | `(this commit)` |
| fixed | `libs/bimap/test//xpressive` source-character deduction | Nested partial matching parsed `box<const T*>` to a typed pointer/cv graph, but `type_pattern_template_parameter` looked up only the graph's semantic payload (`T#0`). It therefore failed to deduce the nested parameter even though the existing typed lookup could recover the source identity `T`; the less-specialized `box<T*>` pattern alone remained viable. The helper now passes the retained `TypePtr` to the shared typed parameter-identity lookup, which checks canonical key, semantic payload, and source identity without rendering or parsing text. | `pa21/tests/spec/100-nested-cv-pointer-partial-specialization-ordering.t`, a header-free nested template-id ordering reducer at the PA21 class partial-specialization owner | Clang selects `box<const T*>`; the controlled pre-fix compiler selects `box<T*>` and emits a comparison against `1`. The Boost header reducer selected `stream_char_common<boost::iterator_range<Char*>>` with `Char=const char`, producing `src_char_t=void`; the fix selects the strictly more specialized const-pointer pattern and produces `src_char_t=char`. The remaining target-side `void` is independent. | Warning-clean Clang and GCC 15 compiles; focused PA21 check and direct report pass `233/233`; all configured strict PA18/19/21/22/23 direct-LowIR suites pass; PA21 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all-cache-disabled, witness/cache-off, and trace/selection-cache-off LowIR are byte-identical with SHA-256 `4cf5916d88601180f714f2a34b0cde9226a369a703b5d4acf47d29050d578470`. The full direct report with PA9 excluded passes `3896/3896`, including PA37 object roundtrip `7/7`; PA9 was not run. The exact forced Xpressive replay reaches only the independent `widest_char<void, char>` target probe; log `/tmp/boost-frontier-v2-suite-011-xpressive-nested-cv-fix.log`. | instructions -3.46%; max RSS -1.46%; peak footprint -1.83%; pass; aggregate rolling deltas from the preserved pre-Bimap checkpoint are -0.39% instructions, -2.28% RSS, and -1.00% footprint; logs `/tmp/cppgm-boost-frontier-v2-nested-cv-fixed-perf.log` and `/tmp/cppgm-boost-frontier-v2-nested-cv-rolling-perf.log` | `(this commit)` |
| fixed | `libs/bimap/test//xpressive` target-character deduction | Initial parsing retained both qualified `local::declval<...>` template-ids and the dependent `U&` type argument in the expression AST. When ordinary operator analysis correctly found the definition-time overload set inconclusive, the final `decltype` fallback consulted only unqualified placeholder/name flags; those flags cannot see a dependent type stored inside a qualified `TemplateIdSyntax`, so both trailing-return candidates were discarded before substitution. The final fallback now also consumes the already computed structured AST dependency result. After substitution that result becomes concrete, so the positive `int` probe resolves to `char` and the invalid control still fails by SFINAE. No source or semantic text is parsed, no rendered name or ABI path is involved, and no cache is added. | `pa22/tests/spec/300-qualified-declval-dependent-operator-probe.t`, a header-free positive/negative operator-probe reducer at the PA22 expression-SFINAE substitution owner | The controlled parent rejects the positive assertion by producing `void`; Clang 22 and GCC 15 accept both assertions. Targeted temporary tracing showed `structured=yes` with every legacy spelling/name dependency flag false at definition time, then `structured=no` after concrete substitution. The exact Boost reducer reaches the same qualified `declval` operator shape. | Warning-clean Clang and GCC 15 compiles; PA22 direct report passes `266/266`; all configured strict PA18/19/21/22/23 direct-LowIR suites pass; PA22 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all-cache-disabled, witness/cache-off, and trace/selection-cache-off LowIR are byte-identical with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The full direct report with PA9 excluded passes `3897/3897`, including PA37 object roundtrip `7/7`; PA9 was not run. The full forced Bimap graph passes all other 39 test actions and six expected compile failures; single-job forced Xpressive passes compile, link, and runtime, and the final full continuation exits successfully. | instructions -3.52%; max RSS -1.09%; peak footprint -1.83%; pass; aggregate rolling deltas from the preserved pre-Bimap checkpoint are -0.53% instructions, -1.54% RSS, and -0.87% footprint; reports `/tmp/cppgm-boost-frontier-v2-qualified-declval-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-qualified-declval-rolling-perf.json` | `(this commit)` |
| fixed | `libs/bimap/test//xpressive` overload-candidate lifetime under class completion | Member lookup collected raw `FunctionBinding *` candidates from an incomplete `lexical_stream` specialization whose reference members had been collected. The implicit-object inheritance conversion for an early candidate completed the class, reset its instantiated metadata, and discarded every reference-only member binding. The old loop then dereferenced the stale later candidate before its current-candidate-only refresh could run. At-risk candidates now snapshot typed owner, canonical member name, function type, ref qualifier, source-template declaration identity, and template-instantiation key; every later dereference first checks the live-binding set and reacquires only an exactly matching binding through the existing typed lookup path. Candidate indices remain stable through viability filtering, deduplication, diagnostics, selection, and conversion rematerialization. No parse, source scan, text, cache, symbol, ABI, or witness path is involved. | `pa22/tests/spec/300-member-overload-set-survives-class-completion.t`, a header-free two-overload trailing-return SFINAE reducer at the PA22 complete overload/substitution owner | LLDB allocation history traced invalidation through `try_apply_inheritance_conversion_impl` -> `complete_class_type` -> `reset_instantiated_class_info` -> `discard_class_function_bindings` -> `discard_function_binding`. The release compiler's authoritative live-binding check deterministically observes candidate index 1 as stale and reacquires it. Normal and all-ten-cache-disabled runs each report `overload-candidate-refresh-attempts=1` and `overload-candidate-refresh-successes=1`, and their LowIR is byte-identical with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. | Warning-clean repository build and changed translation units under Clang and GCC 15; focused regression and PA22 direct report pass `267/267`; all configured strict direct-LowIR suites pass; PA22 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. The PA9-excluded broad direct report passes `3898/3898`, including PA37 object roundtrip `7/7`. Forced focused Xpressive and the final full eight-job Bimap graph both pass compile, link, and runtime; the full graph updates 205 targets with no unexpected failure or skip. | instructions -3.06%; max RSS -1.09%; peak footprint -1.86%; pass; aggregate rolling deltas from the preserved pre-Bimap checkpoint are -0.25% instructions, -0.89% RSS, and -0.87% footprint; the +0.28 instruction percentage-point movement from the preceding aggregate is documented and remains below the hotspot trigger; reports `/tmp/cppgm-boost-frontier-v2-bimap-candidate-refresh-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-bimap-candidate-refresh-rolling-perf.json` | `(this commit)` |
| fixed | `libs/bind/test//bind_function_test` and `//bind_and_or_test` free-function target identity | Target-aware function-pointer resolution treated an unqualified id-expression as a namespace function overload set even when ordinary lookup had a visible value binding for a same-named function parameter. In Boost.Bind's `bind(F f, A1)` body, every dependent use of parameter `f` was therefore lowered as `addr @f`; passing namespace function `g` still constructed a binder for `f`. `resolve_function_id_for_target` now first uses the existing structured, visibility-aware value lookup and declines overload-set resolution when a value shadows the function name. Generic expression analysis then consumes the retained parameter binding. No source parsing, text lookup, cache, ABI, or symbol policy is added. | `pa18/tests/spec/100-function-parameter-shadows-function-pointer-target.t`, a header-free runtime reducer at the earliest class-template/function-pointer lowering owner | Clang accepts the reducer. The immutable `db9879223` V2 baseline and the pre-fix frontier both reject it at runtime and emit `addr @f` for `preserve_target(g)`; the fixed compiler loads the `%f` parameter and returns `g`. The same baseline reproduces all three direct `bind_function_test` runtime assertions, proving a latent baseline gap rather than a V2 regression. | Warning-clean compiler build; focused PA18 LowIR and witness checks pass; PA18 direct report passes `231/231`; all configured strict PA18/19/21/22/23 suites pass; placement/hygiene has zero findings; all 23 text-reparse categories remain zero. Direct exact `bind_function_test` and `bind_and_or_test` pass, as does their focused forced B2 replay. The exact full eight-job Bind graph records 75 passing test actions and the expected compile failure, leaving only the two independent `apply_rv` compile failures. | instructions -3.33%; max RSS -0.96%; peak footprint -1.75%; pass; rolling deltas from the preceding candidate-refresh checkpoint are -0.282% instructions, +0.127% RSS, and +0.119% footprint; report `/tmp/cppgm-boost-frontier-v2-bind-function-shadow-fixed-perf.json` | `(this commit)` |
| fixed | `libs/bind/test//apply_rv_test` and `//apply_rv_test2` concrete callable result type | `canonicalize_member_typedef_type` applied its legacy named-type spelling fallback to every `TK_NAMED` alias result. In `result_traits<R, F>` instantiated with concrete arguments `::F` and `::F (*)()`, `typedef R type` already denoted the exact nondependent class `::F`, but the fallback looked up its display spelling in the specialization member scope and rebound it to the same-spelled second template parameter `F`. The fallback now runs only when the typed alias result actually depends on a template parameter; concrete identities remain untouched. No cache, text parse, source scan, symbol, ABI, or witness-only path is added. | `pa18/tests/spec/300-concrete-class-argument-not-rebound-by-template-parameter.t`, a header-free concrete member-alias reducer at the PA18 class-template instantiation owner | The immutable V2 baseline rejects both exact Boost sources and the checked-in reducer while Clang accepts them. Template trace shows the specialization bindings were already correct (`R=::F`, `F=::F (*)()`), and the typedef was parsed as `::F`; the incorrect conversion occurs only in member-alias canonicalization. In Boost, that wrong result makes `static_cast<F&&>(f)(static_cast<A&&>(a)...)` try to call a function pointer returning `F` as though it accepted the forwarded `int&`. | Warning-clean compiler build and isolated GCC 15 compile; focused PA18 LowIR and witness checks pass; PA18 direct report passes `232/232`; all configured strict PA18/19/21/22/23 suites pass; placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `7fedb2be1a2f77c4560d005ca3db32f623ea3320b2d9e0b507063c7b3e5728d3`. The PA9-excluded broad direct report passes `3900/3900`, including PA37 object roundtrip `7/7`. Both focused and final full forced Bind graphs pass compile, link, and runtime; the full graph updates all 303 targets with no unexpected failure or skip. | instructions -3.39%; max RSS -1.22%; peak footprint -1.85%; pass; rolling deltas are -0.03% instructions, +0.81% RSS, and +0.18% footprint; reports `/tmp/cppgm-boost-frontier-v2-apply-rv-typed-identity-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-apply-rv-typed-identity-rolling-perf-valid.json` | `(this commit)` |
| fixed | Bloom's nine initial `filter_core::new_array` compile failures | Anonymous function-local classes received synthetic semantic names such as `__local_type1`, but only named function-local classes retained their enclosing-function Itanium entity metadata. The symbol adapter therefore tried to mangle the anonymous class as an ordinary nested type; simple cases produced the wrong `_ZN...__local_type1...` symbol and Bloom's structured owner could not build a symbol at all. Anonymous local classes now retain their typed enclosing-function owner plus a per-function discriminator. The ABI model emits Itanium unnamed local type entities `Ut_`, `Ut0_`, ... for that sentinel metadata, and template/class resets preserve it. Metadata probes are restricted to actual named or unnamed function-local owners. No source parse, rendered-name lookup, cache, or raw mangled fragment is added. | `pa33/tests/general/200-host-unnamed-local-class-template-constructor-mangling.t`, with exact object-symbol expectations for two unnamed local classes in one class-template member | The parent compiler emits `_ZN6holderIiE13__local_type1C1Ev` and `_ZN6holderIiE13__local_type2C1Ev`; Clang emits `_ZZN6holderIiE4readEvENUt_C1Ev` and `_ZZN6holderIiE4readEvENUt0_C1Ev` plus their `C2` variants. The fixed compiler matches all four Clang symbols byte-for-byte, links, and runs the reducer. Bloom's initial log reports the same missing implicit-constructor ABI symbol in nine objects at `core.hpp:711`. | Warning-clean build and isolated GCC 15 compiles of all five changed translation units; focused PA33 inspection passes and PA33 direct report passes `82/82`; all configured strict suites pass; placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 20 audit tests pass. Normal, every individual cache-disabled mode, and all-disabled mode emit byte-identical LowIR with SHA-256 `ee1b75c69b93c4ed059acbece9b9b7628d6f84178f5cbb6f4d9a11135049e4b8`. The PA9-excluded broad direct report passes `3901/3901`, including PA37 object roundtrip `7/7`. Focused `test_capacity` passes; the full forced Bloom replay clears all nine shared failures and reaches one independent `std::get` lookup frontier. | instructions -3.09%; max RSS -0.27%; peak footprint -1.83%; pass; rolling deltas are +0.321% instructions, +1.092% RSS, and +0.011% footprint; reports `/tmp/cppgm-boost-frontier-v2-bloom-unnamed-local-fixed-perf-optimized.json` and `/tmp/cppgm-boost-frontier-v2-bloom-unnamed-local-rolling-perf-optimized.json` | `466dd3c77` |
| fixed | `libs/bloom/test//test_construction` variadic local-lambda result | General syntax-pack expansion substituted the spelling of `T&&...` once per deduced element but left every retained semantic argument type as the source `T&&`. The two-element `tuple<T&&...>` result therefore remained dependent even after `forward_as_tuple` deduction succeeded. The expansion now substitutes each genuine type argument against a scalar view of the already selected pack element and stores that exact `TypePtr` plus resolved type syntax. Non-type packs retain their expression/value path. No source text is parsed, looked up, or used as semantic identity, and no cache is added. | `pa24/tests/general/200-function-template-pack-result-two-local-lambdas.t`, a header-free two-lambda reducer at the first complete captureless-lambda/template-pack integration owner | The controlled parent compiler at `466dd3c77` rejects the checked-in shape as `unknown function make_pack`; the exact libc++ reducer rejects `std::forward_as_tuple` and Bloom reports downstream `unknown function std::get<B?0:1>`. Trace shows deduction binds both local-lambda types, while the source result remains dependent. Clang 22 and GCC 15 accept and run the reducer. | Warning-clean Clang build and isolated GCC 15 production-TU compile; focused regression and PA24 direct report pass `107/107`; all configured strict PA18/19/21/22/23 suites pass; PA24 placement/hygiene has zero findings; all 23 reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `988038ea57f63cb561769d78b74112e161ce243e4dae7e7b9f39f7df2e054c2b`. The PA9-excluded broad report passes `3902/3902`, including PA37 `7/7`. Forced focused `test_construction` and the final full Bloom graph compile, link, run, and pass; the full graph updates all 38 requested targets. | instructions -3.24%; max RSS -1.01%; peak footprint -1.84%; pass; broader rolling deltas from the pre-Bloom Bind checkpoint are +0.30% instructions, +0.85% RSS, and +0.01% footprint; reports `/tmp/cppgm-boost-frontier-v2-bloom-pack-result-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-bloom-pack-result-rolling-from-bind-perf.json` | `(this commit)` |
| fixed | CallableTraits packed local member-pointer alias materialization | Structured pack substitution updated the spelling of `Owner::*` but did not attach the selected element's exact `TypePtr` to the retained `ptr_operator`. Member-pointer declarator analysis then bypassed the shared semantic-node lookup helper and re-looked up the synthetic local-class spelling. That spelling resolved as a dependent member alias, leaving the selected class alias dependent and causing `set_member_function_qualifiers` materialization to fail. Pack substitution now annotates the structured member-pointer owner with the exact replacement type, and declarator analysis consumes that annotation through the existing `lookup_type_from_ast_node` path. The replacement spelling remains presentation-only. No source or semantic text is parsed, no cache or ABI path is added, and `ignore_semantic_type` keeps its existing behavior. | `pa26/tests/general/300-local-class-member-pointer-alias-pack.t`, a header-free runtime reducer at the placement-audited PA26 member-pointer owner | Clang 22 and GCC 15 accept and run the reducer. The controlled older compiler rejects the exact nested alias `Return (Owner::*)(Args...)`; a namespace-scope owner passes, as do direct and non-pack nested aliases. Temporary structured tracing showed partial-specialization selection was already correct and exposed the first bad decision: the expanded `Owner::*` node retained a dependent alias key for the synthetic local spelling instead of the concrete local-class `TypePtr`. | Warning-clean compiler build and isolated GCC 15 compiles of both changed production translation units; focused PA26 check and direct report pass `71/71`; all configured strict PA18/19/21/22/23 suites pass; PA26 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `a7ba1475a929b304d249d3205e5cb0bedf2dac7fff0cb8f03d9db9f7978dc4fc`. The PA9-excluded broad report passes `3903/3903`, including PA37 `7/7`. Focused `remove_transaction_safe` passes compile, link, and runtime; the exact full forced replay removes every alias-materialization diagnostic and clears five eager/lazy pairs. | instructions -3.23%; max RSS -1.87%; peak footprint -1.83%; pass; broader rolling deltas from the pre-Bloom Bind checkpoint are +0.11% instructions, -0.08% RSS, and +0.02% footprint; reports `/tmp/cppgm-boost-frontier-v2-callable-local-member-owner-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-callable-local-member-owner-rolling-from-bind-perf.json` | `0a04194b6` |
| fixed | CallableTraits function-object cv/ref qualifier detection | In the structured leaf evaluator for `decltype(&T::operator())`, owner recovery tried only template-specific and exact-current-scope helpers. An ordinary namespace class owner therefore missed even though the semantic context could resolve the retained owner node, and address-of fell back to an ordinary function pointer with an explicit object parameter. Once the owner was found, reference-only class bindings could also retain a declared function type with no ref qualifier; the helper repaired cv from `FunctionBinding` but reused that stale field. Owner recovery now consumes any retained qualifier `TypePtr`, then the standard semantic node lookup, before existing template fallbacks. Member-function reconstruction maps the binding's typed `RefQualifier` into the function type alongside cv. No text is parsed, no cache, source scan, symbol, or ABI path is added. | `pa26/tests/general/300-qualified-member-address-decltype-ref.t`, a header-free literal/global/local reducer at the placement-audited PA26 member-pointer owner | Clang 22 and GCC 15 accept the reducer. The controlled older compiler accepts the literal `int (C::*)() volatile &` control but rejects `qualifier_flags<decltype(&global_functor::operator())>`; template trace shows the literal is a member pointer while the bad leaf path first produced `int (*)(volatile global_functor *)`, then after owner lookup produced a member pointer whose function type was only `volatile`. The final path retains both member-pointer identity and `volatile &`. | Warning-clean build and isolated GCC 15 production-TU compile; focused PA26 check and direct report pass `72/72`; all configured strict suites pass; PA26 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The PA9-excluded broad report passes `3904/3904`, including PA37 `7/7`. Focused `is_volatile_member` passes compile, link, and runtime; the exact full replay clears all seven function-object qualifier pairs and leaves five independent pairs. | instructions -3.26%; max RSS -1.06%; peak footprint -1.76%; pass; immediate rolling deltas from the typed local member-pointer-owner checkpoint are -0.00% instructions, +0.87% RSS, and -0.01% footprint; reports `/tmp/cppgm-boost-frontier-v2-callable-member-address-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-callable-member-address-rolling-perf.json` | `6ac60d65e` |
| fixed | CallableTraits nested dependent member-pointer template argument | The direct type-id substitution fast path accepted every one-node `ptr_operator`, but its helper supports only token `*`, `&`, and `&&`. For `K C::*`, substituting `K` therefore failed and the caller replaced the entire type-id with bare `K`, dropping the structured member-pointer declarator. The fast path is now restricted to the token operators it implements; `C::*` stays on recursive typed AST substitution, which replaces the data type and owner while preserving the declarator. No source or semantic text is parsed, no cache, source scan, symbol, ABI, or witness-only path is added. | `pa26/tests/general/300-nested-alias-dependent-member-pointer-argument.t`, covering both `int &` and a reference to a const function pointer inside a member data pointer at the placement-audited PA26 owner | Clang 22 and GCC 15 accept the reducer; the controlled older compiler rejects its first assertion. A header-free reducer showed direct `remove_reference<T>::type C::*` succeeds and isolated the loss to a nested alias that passes the dependent member-pointer through `conditional`. Structured trace showed `K C::*` initially resolved as a dependent member pointer but became bare `int` immediately after the simple type-id fast path. | Warning-clean build and isolated GCC 15 production-TU compile; focused PA26 check and direct report pass `73/73`; all configured strict suites pass; PA26 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The PA9-excluded broad report passes `3905/3905`, including PA37 `7/7`. Focused and eager/lazy full-graph `apply_member_pointer_pmd` compile, link, and run; the exact forced replay leaves four independent eager/lazy pairs. | instructions -3.28%; max RSS -1.65%; peak footprint -1.83%; pass; immediate rolling deltas are -0.02% instructions, +0.55% RSS, and +0.00% footprint; reports `/tmp/cppgm-boost-frontier-v2-callable-member-pointer-argument-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-callable-member-pointer-argument-rolling-perf.json` | `f70bdfadd` |
| fixed | CallableTraits captureless-lambda closure identity | Direct lambda operands in `auto` deduction used the generic captureless-lambda shortcut, which represents the expression as a synthetic free function and therefore deduced a function pointer instead of the closure type. `auto` deduction now requests the existing typed closure analyzer for direct lambda AST nodes. Preserving that identity exposed the previously masked standard conversion from a captureless closure to a matching function pointer, so conversion analysis now lazily materializes the already typed call operator as a synthetic free target only when that user-defined conversion is requested; built-in unary `+` uses the same typed pointer conversion. No source or semantic text is parsed, no cache, spelling recovery, ABI special case, or witness-only path is added. | `pa24/tests/general/200-captureless-lambda-auto-decltype-closure.t`, a header-free runtime reducer at the PA24 captureless-lambda owner, covering `decltype(auto-variable)` identity, implicit function-pointer conversion, and unary-plus conversion | Clang 22 and GCC 15 accept and run the reducer. The controlled older compiler rejects the exact closure-identity assertion, while `/tmp/cppgm-captureless-lambda-decltype-reducer.cpp` additionally proves that `decltype(&G::operator())` is a member pointer. Focused Boost evidence first showed `G` bound as `void (*)(int, char)`; preserving the closure fixed `function_type`, and the PA36 hosted exception regression isolated the missing lazy pointer conversion used by libc++'s cleanup lambda. | Warning-clean compiler build and isolated GCC 15 compiles of all three changed production translation units; focused PA24 check and direct report pass `108/108`; the exact PA36 hosted RTTI case passes; all configured strict PA18/19/21/22/23 suites pass; PA24 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `1a863f89640209e1199fccdc6cda600c6f88680ea8f614e96dcc154fd5c986c2`. The PA9-excluded broad report passes `3906/3906`, including PA37 `7/7`. The exact forced CallableTraits replay clears eager/lazy `function_type` and `add_varargs_constraints`, leaving only the four eager/lazy `is_invocable` compile actions and 12 skips. | instructions -3.79%; max RSS -1.13%; peak footprint -1.86%; pass; immediate rolling deltas are -0.48% instructions, -0.50% RSS, and +0.02% footprint; reports `/tmp/cppgm-boost-frontier-v2-captureless-lambda-identity-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-captureless-lambda-identity-rolling-perf.json` | `(this commit)` |
| fixed | CallableTraits inherited constexpr conversion in a built-in comparison | The constexpr evaluator first tried the built-in `==` directly on a scalar and a class value. When that failed, its fallback considered only overloaded operator calls, so it never applied the class operand's inherited constexpr conversion function and retried the original built-in operator. The fallback now reuses the existing typed target-conversion evaluator when exactly one operand is a class and the other is not, preserving operand order when it retries the built-in operation. No source or semantic text is parsed, no cache, spelling recovery, trait special case, or witness-only path is added. | Existing `pa20/tests/general/400-constexpr-class-bool-conversion.t`, strengthened with `true == __and_<int>{}` at the constant-evaluation owner | Clang 22 and GCC 15 accept the strengthened reducer. The detached `9290640fa` compiler rejects the minimal `/tmp/cppgm-dependent-base-conversion-pack-reducer.cpp` as an unevaluated static assertion, while the fixed compiler accepts it. The header-free exact `is_invocable` wrapper and Boost-header intermediate probe also advance past the original positive member-function-pointer assertion. | Warning-clean compiler build and isolated GCC 15 production-TU compile; focused PA20 check and direct report pass `74/74`; all configured strict suites pass; PA20 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `4483c226839f4cf2cf1e9fb4a1774ea8a0d62f21211220dd56ce54db8af1764e`. The PA9-excluded broad report passes `3906/3906`, including PA37 `7/7`. Focused eager `is_invocable` and `is_invocable_r` both advance to the independent lvalue-ref-qualified member-function-pointer/prvalue-object case. | instructions -3.72%; max RSS -0.98%; peak footprint -1.81%; pass; immediate rolling deltas from exact `9290640fa` are +0.09% instructions, -0.60% RSS, and +0.00% footprint; reports `/tmp/cppgm-boost-frontier-v2-invocable-conversion-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-invocable-conversion-rolling-perf.json` | `(this commit)` |
| fixed | CallableTraits ref-qualified member-function-pointer invocation | The normal `.*`/`->*` call path converted the implicit object and arguments but never compared the pointed-to function's retained ref-qualifier with the object expression's value category. The structured invocation helper did check a qualifier, but reused ordinary implicit-object binding rules; those allow a const lvalue-reference method on an rvalue, whereas the C++11 pointer-to-member operator rule rejects any `&`-qualified member pointer on an rvalue and any `&&`-qualified member pointer on an lvalue or through `->*`. Both paths now call one dedicated typed rule before accepting the call. No source or semantic text is parsed, no cache, spelling recovery, trait special case, or witness-only path is added. | `pa26/tests/general/300-member-function-pointer-ref-qualified-invocation-sfinae.t`, a header-free expression-SFINAE reducer covering unqualified, `&`, `const &`, and `&&` member pointers on lvalue and rvalue objects | Clang 22 and GCC 15 accept the C++11 reducer. The detached `3433e1bcc` compiler makes `is_invocable<void (object::*)() &, object>` true and fails the first negative assertion; a direct invalid-call reducer is also accepted before the fix and rejected after it. The exact header-free enclosing-pack reducer matches both Boost invocation traits. | Warning-clean compiler build and isolated GCC 15 compiles of all three changed production translation units; focused PA26 check and direct report pass `74/74`; all configured strict suites pass; PA26 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The PA9-excluded broad report passes `3907/3907`, including PA37 `7/7`. Focused eager targets and the exact full eager/lazy CallableTraits graph compile, link, run, and pass; after the shared-rule refactor the final graph updates all 376 requested targets in `/tmp/boost-frontier-v2-suite-014-invocable-final-shared-helper-full-forced.log`. | instructions -3.63%; max RSS -0.51%; peak footprint -1.85%; pass; immediate rolling deltas from exact `3433e1bcc` are -0.06% instructions, +1.38% RSS, and +0.04% footprint; reports `/tmp/cppgm-boost-frontier-v2-pmf-ref-qualifier-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-pmf-ref-qualifier-rolling-perf.json` | `(this commit)` |
| fixed | Charconv namespace-qualified direct type-pack bases | Direct base-pack expansion substituted each bound `Policies...` element into a rendered namespace-qualified spelling, then looked that spelling up from the instantiated `policy_holder` scope. The lookup lost the exact bound type and class completion never installed the bases, so inherited constexpr policy members were unavailable as non-type template arguments. Direct identifier-pack expansion now carries the existing bound `TypePtr` vector beside the expanded presentation names, and both ordinary and reference base-clause paths consume those exact types before their existing structured fallback. Patterned pack bases keep the existing path. No source or semantic text is parsed, no lookup retry or cache is added, and spelling remains presentation-only. | `pa19/tests/general/200-namespace-qualified-type-pack-base-inherited-nttp.t`, a header-free reducer at the placement-audited PA19 variadic class-template owner | The pre-fix `e816d6560` worktree compiler rejects the exact temporary reducer at `policy_holder::return_has_sign`; a controlled older compiler also rejects the checked-in shape. LLDB records `lookup_base_type_name("policy_impl::sign_policy")` returning null during direct base-pack expansion. Clang 22 and GCC 15 accept the reducer. | Warning-clean compiler build and isolated GCC 15 production-TU compile; both reduced forms pass; focused PA19 check and direct report pass `141/141`; all configured strict suites pass; PA19 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. All twelve cache configurations emit byte-identical LowIR with SHA-256 `d4e3d0f5b86d9ba730a92d8025366175c204fe894b7271804a90a9b7437731e6`. Direct Charconv `to_chars.cpp` compiles and the exact forced replay builds the library, advancing to the independent static-constexpr link cluster. The PA9-excluded broad report passes `3908/3908`, including PA37 `7/7`. | instructions -3.62%; max RSS -0.39%; peak footprint -1.77%; pass; exact-parent deltas from `e816d6560` are -0.00% instructions, -0.08% RSS, and +0.01% footprint; reports `/tmp/cppgm-boost-frontier-v2-charconv-base-pack-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-charconv-base-pack-rolling-perf.json` | `(this commit)` |
| fixed | Charconv concrete structured-qualifier static constexpr value use | Structured qualified lookup suppressed constant folding whenever qualifier syntax was present, even when retained `decltype` syntax had already resolved to a concrete type. The value binding therefore carried its exact constexpr value but the expression was emitted as a storage load, producing undefined C++11 static constexpr member symbols at host link. Qualified lookup now returns the already resolved qualifier `TypePtr`; the expression path permits the existing constant-fold rule only when that exact type is non-dependent. Dependent or unresolved qualifiers retain the old behavior. No source or semantic text is parsed, no lookup retry, cache, symbol special case, or ABI rule is added. | `pa20/tests/general/300-concrete-decltype-qualified-static-constexpr-fold.t`, a header-free reducer at the placement-audited PA20 constant-evaluation owner; existing PA20 and PA22 LowIR references are corrected to reflect non-ODR-use folding | Clang 22 and GCC 15 accept and run the reducer. The exact controlled `149cff9a3` parent emits undefined `policy::tag` and `unsigned_constant<1u>::value` references. LLDB shows that the failing `tag` binding has a concrete owner, initializer, and constexpr value; the qualifier guard alone disables folding. | Warning-clean compiler build and isolated GCC 15 compiles of both changed production translation units; focused PA20 check and direct report pass `75/75`; all configured strict suites pass; PA20 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `66f9a18a64be689b1ec2ffb4020ab0328729d3272e907f17d279887e8eac4322`. The PA9-excluded broad report passes `3909/3909`, including PA37 `7/7`. Focused `to_chars_float` passes compile, link, and runtime; the exact final Charconv graph updates all 150 requested targets successfully. | instructions -3.78%; max RSS -1.61%; peak footprint -1.85%; pass; exact-parent deltas from `149cff9a3` are -0.03% instructions, +0.50% RSS, and +0.05% footprint; reports `/tmp/cppgm-boost-frontier-v2-charconv-static-constexpr-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-charconv-static-constexpr-rolling-perf.json` | `(this commit)` |
| fixed | Chrono scalar-duration result substitution | Resolving a dependent `typename duration_divide_result<Duration, Scalar>::type` rebuilt the class template-id with every carried argument, including the source-defaulted trailing boolean. That turned the default into an explicit expression evaluated from the outer operator instantiation scope, where the nested `detail::is_duration` name was not visible, and SFINAE dropped the valid overload. The typed resolver now trims only the trailing run of structurally source-defaulted arguments and lets the existing default-argument path regenerate them through the class template's declaring scope. A source-defaulted argument that is explicitly referenced inside a later class argument is preserved, as guarded by PA34 assignability. No source or semantic text is parsed, no lookup retry, recovery path, or cache is added. | `pa22/tests/spec/300-dependent-result-defaulted-nontype-declaring-scope.t`, a header-free full-result-substitution reducer; existing `pa34/tests/compile/700-is-assignable-member-assignment-template.t` guards a non-trailing carried default marker | The controlled parent rejects the header-free reducer. LLDB on the Boost reducer shows the carried default `is_duration<double>::value` re-entering lookup from `boost::chrono` instead of `boost::chrono::detail`. The initial forced Chrono survey fails the scalar-duration overload family, including `saturating_h`. | Warning-clean compiler build and isolated GCC 15 production-TU compile; Clang 22 and cppgm accept and run the reducers; focused PA22 and PA34 checks pass; PA22 direct report passes `268/268` and placement/hygiene has zero findings; all configured strict suites pass; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `71e55b7ce0921cb7bb61b097a520a2293dfd04dc64a50239fe83d66be01dda7e`. The PA9-excluded broad report passes `3910/3910`, including PA37 `7/7`. Focused `saturating_h` compiles, links, runs, and passes; the exact full replay improves from 32 to 30 failures and from 84 to 78 skips. | instructions -3.80%; max RSS -0.99%; peak footprint -1.83%; pass; exact-parent deltas from `01425122b` are -0.11% instructions, +0.19% RSS, and +0.01% footprint; reports `/tmp/cppgm-boost-frontier-v2-chrono-default-scope-fixed-perf.json` and `/tmp/cppgm-boost-frontier-v2-chrono-default-scope-rolling-perf.json` | `(this commit)` |
| fixed | Chrono overloaded unary duration constant evaluation | Unary constant evaluation returned failure immediately when the built-in operation did not accept a class value, unlike binary evaluation, so it never reached the existing typed special-expression hook that resolves overloaded operators. Allowing that hook after built-in unary failure exposed a second structural omission: the synthetic `m.operator-()` member id had no retained `QualifiedName`, so member-call resolution could not identify the operator. The evaluator now follows the typed built-in-then-overload ordering, and the synthetic member id retains the exact overloaded operator name. No source or semantic text is parsed, no cache, spelling recovery, operator special case, or witness-only path is added. | `pa20/tests/general/400-constexpr-overloaded-unary-member-operator.t`, a header-free duration-like namespace-constexpr reducer at the PA20 full constant-evaluation owner | The exact Boost-header and header-free reducers reject `constexpr duration m2 = -m` while accepting the explicit control `m.operator-()`; Clang 22 accepts and runs both forms. Focused `arithmetic_pass_h` reports `static_assert unevaluated: m2.count() == -m.count()` before the fix in `/tmp/boost-frontier-v2-suite-016-arithmetic-focused-initial.log`. | Warning-clean compiler build and isolated GCC 15 compiles of both changed production translation units; Clang 22 and cppgm accept and run the reducer; PA20 direct report passes `76/76` and placement/hygiene has zero findings; all configured strict suites pass with 338 comparisons and zero failures; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `2e06bb2e6f7e7fb9afe1ac83b3a48f590ee44d46b74c3ed899a5e95eae74797d`. The PA9-excluded broad report passes `3911/3911`, including PA37 `7/7`. Focused `arithmetic_pass_h` passes; the exact full replay improves from 30 to 25 failures and from 78 to 63 skips. | instructions -3.75%; max RSS -1.78%; peak footprint -1.80%; pass; incremental deltas from the already recorded `069eb2dc8` measurements are +0.052% instructions, -0.795% RSS, and +0.026% footprint; report `/tmp/cppgm-boost-frontier-v2-chrono-unary-constexpr-fixed-perf.json` | `(this commit)` |
| fixed | Chrono nested catch miss through an active catch cleanup | An inner typed-handler landing pad exposed only its own catch clauses, while its miss and cleanup edges jumped directly to the enclosing handler and bypassed the still-active outer catch cleanup. The host unwinder therefore could not select the enclosing typed handler during phase one, and direct transfer retained an inconsistent region stack. Inner landing pads now append a cleanup boundary and enclosing typed handler metadata when propagation crosses an active catch; miss and cleanup paths execute intervening typed scopes and the active catch cleanup, including `__cxa_end_catch`, before transferring to the already selected outer handler. No source or semantic text is parsed, no cache, backend-validator relaxation, or spelling recovery is added. | `pa25/tests/general/100-source-nested-catch-miss-cleans-active-handler.t`, a header-free runtime reducer at the PA25 source-to-LowIR exception-lowering owner | Focused `rounding_pass_h` and the header-free reducer both fail before the fix with `inconsistent host EH region stack at ...catch_entry_2`; Clang 22 accepts and runs the reducer. The reducer includes a local destructor so successful propagation proves both the intermediate scope cleanup and active catch cleanup occur before the enclosing `catch(long)`. | Warning-clean compiler build and isolated GCC 15 production-TU compile; Clang 22 and cppgm accept, link, and run the reducer; focused and full PA25 reports pass, including `71/71`; PA25 placement/hygiene and LowIR EH review report zero findings; all configured strict suites pass; all 23 text-reparse categories remain zero and all 14 audit tests pass. All twelve cache configurations emit byte-identical LowIR with SHA-256 `bb64973b148d42cadf653a676835227c8482375d8b9d6c0cb7cf9832a2760266`. The PA9-excluded broad report passes `3912/3912`, including PA37 `7/7`. Focused `rounding_pass_h` passes compile, link, and runtime; the exact full replay improves from 25 to 6 failures and from 63 to 12 skips, closing every EH compile failure. | instructions -3.90%; max RSS -1.73%; peak footprint -1.79%; pass; incremental deltas from the already recorded `3c2623d84` measurements are -0.156% instructions, +0.049% RSS, and +0.008% footprint; candidate-only report `/tmp/cppgm-boost-frontier-v2-chrono-active-catch-candidate.json` | `(this commit)` |
| fixed | Chrono out-of-class member result carrying a direct owner type parameter | The mangle-metadata resolver evaluated every carried class-template argument from the result class template's declaring scope. For `ratio_string<Ratio, Char>::prefix()`, that discarded the active out-of-class owner bindings, left `basic_string<Char, ...>` dependent, and prevented the definition from matching the instantiated `basic_string<char, ...>` declaration. Metadata containing a direct carried type parameter now resolves from the active typed instantiation scope; purely compound dependent metadata retains the conservative declaration-scope path so alias and partial-specialization fallback remains authoritative. Source-defaulted arguments still refresh through the class template's explicit declaring scope. No source or semantic text is parsed, and no lookup retry, cache, symbol exception, or ABI rule is added. | `pa18/tests/general/300-out-of-class-member-dependent-class-return.t`, a header-free runtime LowIR reducer at the PA18 class-template instantiation owner; existing PA23 dependent-alias partial-specialization and PA22/PA34 default-scope tests guard the narrowed boundary | The pre-fix compiler leaves `holder<tag, char>::value()` undefined in the header-free reducer, and the exact Boost target leaves `ratio_string<ratio<1, 5000000000>, char>::prefix()` undefined at host link. Definition tracing shows the stored result remains `result<Char>` despite concrete active `{Char=char, Tag=tag}` bindings. Clang 22 accepts and runs the reducer. An initially broad active-scope switch made the PA23 alias control fail, establishing the need for the final direct-parameter restriction. | Warning-clean compiler build and isolated GCC 15 production-TU compile; focused PA18, PA22, PA23, and PA34 controls pass; PA18 direct report passes `233/233`; all configured strict suites pass; PA18 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `6f8ffffa8856362f80fbcccb1c19dd15d0229c4ff7c343bacf4197e0411e2969`. The PA9-excluded broad report passes `3913/3913`, including PA37 `7/7`. Focused `v1_io_ex1_s` passes, and the exact final Chrono graph updates all 420 requested targets with no failures or skips. | instructions -3.31%; max RSS -0.69%; peak footprint -1.73%; pass; arithmetic-only deltas from the already recorded `33bebba1e` parent medians are +0.615% instructions, +1.064% RSS, and +0.069% footprint; candidate-only report `/tmp/cppgm-boost-frontier-v2-chrono-dependent-result-scope-candidate.json` | `(this commit)` |
| fixed | Compat pointer-to-data-member cv and xvalue results | Reference-return lowering treated only lvalues or reference-typed nodes as glvalues. A scalar `object.*member` on an rvalue object is instead a non-reference-typed xvalue, so the old path copied its value into a hidden `retref` local and returned a dangling address. Reference returns now preserve xvalue storage through the existing address lowering. Separately, same-class/base owner adjustment asked inheritance conversion for unqualified `X` or `X*`; successful adjustment replaced a const source type with that unqualified target before the member result inherited object cv. The adjustment target now carries the source object's typed top-level cv onto the member-pointer owner. No text, parse, cache, symbol, ABI, or Boost-specific path is added. | `pa26/tests/general/300-data-member-pointer-xvalue-reference-return.t` checks direct member-storage identity in LowIR and `pa26/tests/general/300-data-member-pointer-object-cv.t` checks const base/derived rvalue, lvalue, and pointer result types at the PA26 member-pointer owner | Before the fix, the xvalue reducer emits `slot $retref__1`, copies the member into it, and returns its address. The cv reducer fails its first assertion with `same<int&&, int const&&>`; Clang 22 and GCC 15 accept all six cv forms. The fixed LowIR returns the computed field address directly and the semantic reducer emits only `main` returning zero. | Warning-clean compiler build and changed production translation units under GCC 15; focused PA26 checks and the full PA26 direct report pass `76/76`; all configured strict suites pass; PA26 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. All twelve cache configurations emit byte-identical LowIR with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The PA9-excluded broad report passes `3915/3915`, including PA37 `7/7`. All five focused Compat member-data targets pass, and the exact forced graph improves from 20 failures/30 skips to 15 failures/25 skips. | instructions -3.36%; max RSS -0.47%; peak footprint -1.74%; pass; arithmetic-only deltas from the already recorded `33bebba1e` parent medians are +0.564% instructions, +1.286% RSS, and +0.059% footprint. The required hotspot log and sample contain no changed-helper frame; reports `/tmp/cppgm-boost-frontier-v2-compat-member-data-candidate.json`, `/tmp/cppgm-boost-frontier-v2-compat-member-data-hotspot.log`, and `/tmp/cppgm-boost-frontier-v2-compat-member-data.sample.txt` | `(this commit)` |
| fixed | Compat forwarded function designators in constexpr invocation | The constant-value domain had no function-designator value, so passing a function name either failed evaluation or decayed it early and lost the reference-binding category. Indirect constant-call evaluation then required a resolved direct `callee`, and pack expansion rewrote constant-valued named parameters as literals, turning required lvalues into prvalues. The full libc++/Boost graph also exposed a retained still-dependent direct candidate that could bypass concrete call analysis. Function designators now carry typed symbol identity as their own constant kind, bind to function references, and decay only through conversion. Constant calls accept analyzed indirect callees and resolve the stored function identity; direct candidates whose function types remain template-dependent are ignored. Pack expansion preserves parameter id-expressions and their semantic type instead of literalizing them. No source or semantic text is parsed, and no cache, symbol-string recovery, witness-only rule, or Boost-specific path is added. | `pa20/tests/general/100-constexpr-function-pointer-reference-call.t` covers indirect calls through function pointer and function reference at the PA20 constant-call owner; `pa22/tests/general/300-constexpr-forwarded-function-designator-pack-call.t` covers forwarding-reference binding, a named argument pack, dependent invoke-result viability, and concrete invocation at the PA22 full-deduction/SFINAE owner | Before the fix the header-free exact forms fail their static assertions, and template trace rejects `forward<int>(argument)` after the named parameter was materialized as a prvalue. The full Boost header graph also selects a stale `invoke` binding whose function type still contains `typename F` and `typename A`; filtering that non-concrete candidate lets ordinary structured call analysis select the concrete specialization. The exact forced graph reports all three function/object constexpr targets as failures before the fix. | Warning-clean compiler build and all six changed production translation units compile warning-free with GCC 15; Clang 22 controls pass; combined PA20/PA22 direct report passes `346/346`; both placement audits have zero findings; all configured strict suites pass; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR for both regressions with SHA-256 `485fc8e3251fe7b25d56cc0db4e5cc73da7486c3984948de64f662839846898f`. The PA9-excluded broad report passes `3917/3917`, including PA37 `7/7`. Focused `invoke_fn_constexpr_test`, `invoke_obj_constexpr_test`, and `invoke_r_fn_constexpr_test` pass; the exact forced graph improves from 15 failures/25 skips to 12 failures/22 skips. | instructions -2.88%; max RSS -0.31%; peak footprint -1.62%; pass; arithmetic-only deltas from the already recorded `e1e9f0094` parent medians are +0.492% instructions, +0.164% RSS, and +0.112% footprint, below the hotspot trigger; candidate-only report `/tmp/cppgm-boost-frontier-v2-compat-function-designator-perf.json` | `(this commit)` |
| fixed | Compat forwarded member-pointer constant invocation | Constant evaluation had no member-pointer value kind, so `&X::member` and `&X::function` could not survive wrapper storage and forwarding into `.*`/`->*` or a member-function call. The unary address path also did not retain a value snapshot for a dereferenced constant object. Adding the typed member-pointer value exposed two existing resolution-order defects: a cached function-template specialization could be reused across incompatible base and derived pointer argument types, and a shared internal symbol could identify the wrong concrete specialization before the object symbol was considered. Member-pointer values now carry the exact semantic type and member identity; access and invocation use typed owner conversion and the implicit object; address values preserve their existing aggregate snapshot; direct-call reuse rejects pointer/member-pointer parameter mismatches; resolved calls use the indexed internal symbol first and fall back to object-symbol lookup only when validation fails. The unary semantic retry is restricted to `&`, the only new required case. No source or semantic text is parsed, and no cache, ABI spelling recovery, witness rule, or Boost-specific path is added. | `pa26/tests/general/300-constexpr-forwarded-member-pointer-derived-object.t` covers data and const member-function pointers stored in wrappers, forwarded first through a base pointer and then through a derived pointer to exercise specialization ordering | Before the fix the four exact Compat member-pointer constexpr targets fail constant evaluation. The header-free reducer also fails after the base-pointer call seeds the stale specialization used by the derived-pointer call; Clang 22 accepts it. The exact Boost-header reducer and direct upstream tests establish both data and member-function forms. | Warning-clean compiler build and all four changed production translation units compile warning-free with GCC 15; the Clang 22 control passes; combined PA20/PA22/PA26 direct report passes `423/423`; all configured strict suites pass; PA26 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled modes, and all-disabled mode emit byte-identical LowIR with SHA-256 `498eef6c24f0c7e45889cc0989d67a4919b53d20a76e39757e5b213116e20c30`. The PA9-excluded broad report passes `3918/3918`, including PA37 `7/7`. All four focused member-pointer constexpr targets pass, and the exact optimized forced graph improves from 12 failures/22 skips to 8 failures/18 skips. | instructions -2.82%; max RSS -1.00%; peak footprint -1.66%; pass; arithmetic-only deltas from the immutable recorded `b7efbce95` medians are +0.068% instructions, -0.697% RSS, and -0.031% footprint; candidate-only report `/tmp/cppgm-boost-frontier-v2-compat-member-pointer-constexpr-final-optimized-candidate.json` | `(this commit)` |
| fixed | Compat const-rvalue implicit-object ref-qualifier ranking | The compiler represents a non-static member's implicit object as an ABI pointer parameter. Consequently, instantiated `operator() const &` and `operator() const &&` candidates had identical pointer types and exact conversion ranks for a const rvalue. The existing implicit-object cv preference also tied because both methods add the same const qualification, leaving an ambiguity where C++ prefers the rvalue-reference binding. After explicit arguments, object cv, and the ordinary implicit-object conversion are otherwise tied, overload comparison now prefers `RQ_RVALUE` over `RQ_LVALUE`; all other qualifier combinations remain unchanged. No parse, source text, cache, symbol, ABI, witness, or Boost-specific path is added. | `pa16/tests/spec/200-ref-qualified-member-call-const-rvalue-rank.t`, at the earliest ref-qualified-member owner, directly overloads `pick() const &` and `pick() const &&` and calls through a const xvalue | The controlled parent rejects the header-free reducer as ambiguous while Clang 22 and GCC 15 select the `const &&` member. Template trace on `bind_front_obj_test` shows two otherwise identical `bind_front_<F2, int>::operator()` matches from lines 53 and 58; this ambiguity causes the enclosing `test_with_impl` template to be dropped and produces the misleading unknown-function diagnostic. | Warning-clean compiler build and isolated GCC 15 production-TU compile; Clang 22 and GCC 15 controls pass; PA16 direct report passes `166/166`; all configured strict suites pass; PA16 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. All twelve cache configurations emit byte-identical LowIR with SHA-256 `914ac92e4ef20cea440067256b13b8c0168389e3d6dc7a489992e4e49402b196`. The PA9-excluded broad report passes `3919/3919`, including PA37 `7/7`. Both focused bind object targets pass, and the exact forced graph improves from 8 failures/18 skips to 6 failures/12 skips. | instructions -2.87%; max RSS -1.33%; peak footprint -1.60%; pass; arithmetic-only deltas from the immutable recorded `4c3c19f3a` medians are -0.052% instructions, -0.335% RSS, and +0.056% footprint; candidate-only report `/tmp/cppgm-boost-frontier-v2-compat-implicit-object-refq-candidate.json` | `(this commit)` |
| fixed | Compat contained-object virtual-base forwarding | Hidden virtual-base argument lowering treated a path rooted in reference storage as a hosted polymorphic reference before considering that the path had crossed into a by-value class member. In libc++ tuple storage this made `std::get<0>(tuple<Y> &&)` read a host vtable adjustment at offset `-24` from the contained `Y`, although that complete contained object has cppgm's fixed structured `virtual X` layout and no host vptr. Lowering now walks the typed `CallSemNode` path, detects an ordinary non-reference class member with matching `CallSemVirtualBaseLayout`, and emits its fixed contained-subobject offset before the existing dynamic hosted-reference fallback. Reference members are excluded, so genuine hosted polymorphic reference storage retains vtable adjustment. No source or semantic text is parsed, and no cache, spelling, ABI-symbol, libc++, tuple, or Boost-specific rule is added. | `pa36/tests/link/700-hosted-tuple-contained-vbase-get-runtime-smoke.t`, using real hosted `<tuple>`, rvalue `std::get`, and a member-function pointer through a virtual base at the PA36 hosted link/runtime owner | The controlled parent executable exits 139. Its LowIR loads a pointer from the tuple leaf, indexes it by `-24`, and loads a runtime vbase offset. Clang 22 exits zero. A blanket removal of hosted dynamic adjustment makes the tuple reducer pass but crashes the existing cross-TU `std::ostream &` virtual-base control, proving that the boundary is contained by-value storage rather than all structured virtual bases. The fixed LowIR projects the tuple field and base subobject, then applies the retained fixed offset `1`. | Warning-clean compiler build and isolated GCC 15 production-TU compile; Clang and cppgm controls pass; the new regression and existing hosted `std::ostream &` dynamic-layout control pass; PA36 direct report passes `2300/2300`; all configured strict suites pass; PA36 placement/hygiene has zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. All twelve cache configurations emit byte-identical LowIR with SHA-256 `810b1fa87f6dcaa6ae19d7f4f27924685d1b0ffc47cfaa6bd2d39211ca858dc9`. The PA9-excluded broad report passes `3920/3920`, including PA37 `7/7`. Both focused bind member-function targets pass, and the exact forced graph improves from 6 failures/12 skips to 4 failures/10 skips. | instructions -2.86%; max RSS -0.40%; peak footprint -1.67%; pass; arithmetic-only deltas from the saved `7c2bad236` parent medians are +0.002% instructions, +0.942% RSS, and -0.073% footprint, below the hotspot trigger; only the candidate was measured in `/tmp/cppgm-boost-frontier-v2-compat-contained-vbase-candidate.json` | `(this commit)` |
| fixed | Compat `function_ref` member-function non-type template construction | Member-pointer non-type template arguments retained only their scalar placeholder during function-template deduction, so the converting constructor lost the selected member identity. Three structured consumers then exposed independent gaps: repeated leaf evaluation formed `&X::q` as `int *` instead of `int X::*`; the reference-binding source fast path classified a named non-type template parameter as an lvalue instead of the standard prvalue; and class partial-specialization matching accepted template-template arguments by arity without checking parameter kind or non-type parameter type. Deduction now carries the exact structured `TemplateArgument`; qualified member-address analysis uses typed field ownership; reference-source analysis consumes the existing typed non-type-template value hooks; and template-template parameters retain recursively parsed parameter descriptors whose kinds and typed non-type declarations are checked on every structured deduction path. No source or semantic text is parsed, and no cache, spelling, ABI-symbol, library, or Boost-specific rule is added. | `pa21/tests/general/200-template-template-nontype-parameter-type-match.t` covers kind/type compatibility at the PA21 template-template matching owner; `pa26/tests/general/300-function-template-constructor-member-pointer-nontype.t` covers data/member-function pointer constructor deduction and forwarding-reference value category at the PA26 member-pointer composition owner | The preceding exact graph rejected both `function_ref_mfn_test` objects. Stepwise typed traces first exposed constructor-template loss, then a prvalue/lvalue mismatch in `invoke_r`, and finally false selection of Boost.Core's `tn_holder<L<T,N>>` array partial for `nttp_holder<F, f>`. The false partial bound `T` to the member-pointer type and invented `N=0` even though `nttp_holder` declares its second parameter as dependent `F`, not `size_t`. Clang 22 accepts both header-free regressions. | Warning-clean full compiler rebuild; both focused regressions pass; combined PA21/PA26 direct report passes `312/312`; all configured strict suites pass; both placement/hygiene audits report zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. The PA9-excluded broad report passes `3922/3922`, including PA37 `7/7`. Focused forced `function_ref_mfn_test` and `function_ref_mfn_noexcept_test` compile, link, run, and pass. The exact full graph finds 941 targets, updates 214, and improves from four failures/ten skips to two failures/four skips; both function-ref targets pass in that graph. | instructions -2.84%; max RSS -0.17%; peak footprint -1.42%; pass; candidate-only medians are `263941437744` instructions, `1291649024` RSS, and `1009545216` footprint. Arithmetic against saved `9c12bf798` parent medians is +0.027% instructions, +0.229% RSS, and +0.254% footprint; the parent was not rerun. Report `/tmp/cppgm-boost-frontier-v2-function-ref-nttp-candidate-perf.json` | `(this commit)` |
| fixed | Compat `move_only_function_test` explicit initializer-list construction | A functional cast whose target was `std::initializer_list<T>` entered ordinary class-constructor selection, although the parser and semantic model already retained the direct braced initializer and the compiler's initializer-list object representation. Functional-cast analysis now routes this exact typed target through the existing initializer-list expression builder. No source text, library spelling recovery, or fallback parse is used. | `pa25/tests/general/200-explicit-initializer-list-functional-cast.t`, at PA25's initializer-list source-to-LowIR owner | The reduced explicit `std::initializer_list<int>{1,2,3}` call did not materialize the three-element list under the pre-fix compiler; the exact Compat compile advanced to its placement-new expression after this repair. | Focused PA25 and full affected-owner reports pass; placement, strict, twelve cache modes, zero-reparse, warning, PA9-excluded `3928/3928`, and exact Compat gates pass. | instructions -3.32%; max RSS -0.64%; peak footprint -1.49%; pass | `(this commit)` |
| fixed | Compat `move_only_function_test` globally qualified placement new | New-expression analysis always preferred a matching class-scope allocation-function bucket when the allocated type declared one, even when the retained AST contained a leading global-scope qualifier. The analyzer now bypasses class allocation lookup for `::new` and resolves the ordinary global placement overload. | `pa16/tests/general/300-global-qualified-placement-new-bypasses-class-member.t`, at PA16's new-expression owner | The exact target failed at `::new (&s_) VT(...)` by trying `callable::operator new(size_t)` with the placement address; log `/tmp/boost-frontier-v2-suite-018-explicit-initlist-full-forced.log`. Clang and the reduced runtime test select global placement new. | Focused PA16 and full affected-owner reports pass; placement, strict, twelve cache modes, zero-reparse, warning, PA9-excluded `3928/3928`, and exact Compat gates pass. | instructions -3.32%; max RSS -0.64%; peak footprint -1.49%; pass | `(this commit)` |
| fixed | Compat same-function named local-class symbols | Named local classes reused only their source name inside an enclosing function's local-name ABI context. Two disjoint `struct Local` declarations in one function therefore produced colliding constructor/member symbols. Collection now assigns a per-function, per-source-name ordinal, and the typed Itanium encoder emits the standard named-local discriminator (`_0`, `_1`, ... after the first entity). | `pa32/tests/general/200-host-same-function-named-local-class-symbols.t`; updated the PA30 raw local-context ABI oracle | Clang's second local class uses `_0`; the pre-fix compiler emitted the first local name for both entities. The two-TU PA32 runtime test and PA30 raw ABI check isolate encoding and host link behavior. | Focused PA30/PA32 and full affected-owner reports pass; placement, strict, warning, PA9-excluded `3928/3928`, and exact Compat gates pass. | instructions -3.32%; max RSS -0.64%; peak footprint -1.49%; pass | `(this commit)` |
| fixed | Compat defaulted alias under a shadowing owner parameter | Substituting a concrete default alias argument updated its display spelling but later dependent-flag clearing discarded the already resolved `TypePtr`; lookup could then bind the generated spelling to a same-named enclosing template parameter. Substitution now retains the original source spelling as provenance and preserves a concrete carried resolved type whenever source and substituted spelling differ. Structured consumers use that type directly. | `pa22/tests/general/500-defaulted-alias-preserves-shadowed-type-identity.t`, at PA22's alias/SFINAE integration owner | The header-free reducer's default `Value = decay_t<F>` resolves to global `::R` while the enclosing class parameter is also named `R`; the pre-fix path rebound the concrete default to the enclosing parameter and dropped the constructor. | Focused PA22 and full affected-owner reports pass; placement, strict, twelve cache modes, all 23 zero-reparse categories, warning, PA9-excluded `3928/3928`, and exact Compat gates pass. | instructions -3.32%; max RSS -0.64%; peak footprint -1.49%; pass | `(this commit)` |
| fixed | Compat constructibility traits with function-type arguments | Builtin trait argument synthesis classified an unreferenced function type as a prvalue. A function designator expression is an lvalue, so constructibility checks against a function lvalue-reference constructor incorrectly failed. The typed trait adapter now assigns `VC_LVALUE` to `TK_FUNCTION`, retaining the exact function type. | `pa34/tests/compile/700-builtin-constructible-function-type-lvalue-ref.t`, at PA34's hosted builtin-trait owner | Removing only this branch makes the new static assertion fail. The exact `move_only_function_test` previously compiled but failed four trait predicates, including the in-place function-type case at line 747. | Focused PA34 and full affected-owner reports pass; controlled pre-fix replay fails; placement, strict, twelve cache modes, zero-reparse, warning, PA9-excluded `3928/3928`, and exact Compat gates pass. | instructions -3.32%; max RSS -0.64%; peak footprint -1.49%; pass | `(this commit)` |
| fixed | Compat `to_array_rvalue_test` const-xvalue element transfer | LowIR and output-dependency selection treated every class xvalue as a request for the non-const move constructor. A `const T&&` cannot bind `T&&` and must use `T const&`, but the old lowering moved from it and mutated the source array. A shared typed transfer rule now preserves source object/referent cv and chooses non-const move only for a non-lvalue compatible with the target; return, conditional, throw, local, aggregate, and hidden-transfer paths all use the same rule. | `pa16/tests/general/300-const-xvalue-return-uses-copy-constructor.t`, alongside the existing non-const xvalue move control at PA16's copy/move owner | Controlled pre-fix LowIR selects `item(item&&)` and the source is modified; fixed LowIR selects `item(item const&)`. The exact rvalue `to_array` runtime target exited 1 before the repair and passes afterward. | Focused PA16 and full affected-owner reports pass; placement, strict, twelve cache modes, zero-reparse, warning, PA9-excluded `3928/3928`, and exact final Compat `220/220` gates pass. | instructions -3.32%; max RSS -0.64%; peak footprint -1.49%; pass | `(this commit)` |
| fixed | Concept Check function-address non-type template arguments | Binding a concrete function as a non-type template argument recorded the selected function identity but did not require semantic validation of its definition. Boost's concept wrapper takes the address of `requirement_<Model>::failed`, whose body calls `Model`'s destructor; skipping both bodies made invalid models compile successfully. Selected function-valued arguments now enter a deferred semantic-only validation queue after template collection. The queue validates explicit special-member bodies and recursively follows called functions without requesting output closure, materialization, class output, or emitted-function ordering. No source text, rendered symbol, cache recovery, or Boost-specific path is used. | `pa23/tests/general/300-function-address-nttp-instantiates-member-body-bad.t`, at PA23's integrated non-type-template instantiation owner, mirrors the standard wrapper/destructor shape and requires a compile failure when the model lacks its requested member | Before the fix seven Concept Check negative targets were unexpected successes. Immediate validation was insufficient because a speculative reference pass could consume the diagnostic and a later cached class selection could bypass revalidation; the deferred post-collection queue makes validation deterministic. Clang 22 and GCC 15 reject the owner reducer. | Warning-clean production build and isolated Clang/GCC translation-unit checks; PA21/PA23/PA26 report `710/710`; all configured strict suites pass; PA23 placement has zero findings; all 23 text-reparse categories and all 23 audit tests pass. Normal, each of nine cache-disable modes, raw class-cache-off, and all-off modes produce byte-identical valid output and identical negative diagnostics. The PA9-excluded broad report accounts for `3929/3929` after one load timeout passes in isolation, including PA37 `7/7`. The final exact forced graph finds 428 targets, updates 44, and exits 0 with every negative failed-as-expected. | instructions -3.05%; max RSS -1.13%; peak footprint -1.45%; pass; candidate-only medians are `263372489936` instructions, `1279246336` RSS, and `1009299456` footprint. Arithmetic against saved `768ee5f44` parent medians is -0.216% instructions, -0.960% RSS, and -0.024% footprint; the parent was not rerun. Report `/tmp/cppgm-boost-frontier-v2-concept-check-semantic-validation-candidate.json` | `(this commit)` |
| fixed | Config constexpr reference parameter address identity | Named scalar constant lookup materialized the value of a real variable without attaching its storage identity. The exact-underlying-type reference cast then treated that value as an ordinary scalar cast and discarded identity again, so taking the address of the reference parameter could not form a constant pointer. Constant lookup now attaches typed storage identity only for real variable definitions, and an exact reference cast preserves the value and identity when it does not drop top-level cv. Temporary values remain identity-free and are rejected. No source text, symbol spelling recovery, cache, ABI, or Boost-specific path is added. | `pa20/tests/general/400-constexpr-reference-parameter-address-identity.t` covers a named static object passed through `constexpr const int &`; `400-constexpr-reference-parameter-temporary-address-bad.t` proves that a temporary still cannot escape through the same address-taking function | The initial Config graph rejects `constexpr const int* xp = addr(x)` in four targets as an unsupported initializer. The header-free positive fails before the fix while Clang 22 and GCC 15 accept it; both host compilers reject the negative temporary case. | Warning-clean changed production units; PA20 direct report passes `79/79`; all configured strict suites pass; PA20 placement/hygiene has zero findings; all 23 text-reparse categories and all 23 audit tests pass. Thirteen positive and thirteen negative cache configurations are respectively byte-identical. The PA9-excluded broad report passes `3931/3931`, including PA37 `7/7`. The final exact Config graph finds 1391 targets, updates all 128 requested targets, and exits successfully. | instructions -2.89%; max RSS -2.31%; peak footprint -1.40%; pass; candidate-only medians are `263793932710` instructions, `1263951872` RSS, and `1009807360` footprint. Arithmetic against saved `56afe87c1` parent medians is +0.160% instructions, -1.196% RSS, and +0.050% footprint; the parent was not rerun. Report `/tmp/cppgm-boost-frontier-v2-config-constexpr-reference-candidate.json` | `(this commit)` |
| fixed | Container `string_test` libc++ `string_view` comparison | Function-template collection canonicalized `type_identity_t<view<T>>` to `view<T>` and discarded the alias-template syntax before call deduction. The later string-literal argument was therefore used for deduction instead of being converted after `T` and the traits were fixed by the first argument. Function deduction now resolves the direct alias template from the retained parameter-declaration AST and inspects the alias target's structured qualified-name and qualifier-template-id syntax. A target such as `typename type_identity<T>::type` is a non-deduced context; a direct `using direct_t = T` remains deducible. No source or semantic text is parsed, no cache is added, and ordinary direct aliases retain their existing path. | `pa23/tests/spec/500-alias-template-nondeduced-context-conversion.t` covers earlier-argument binding, conversion, and an unbound identity-only fallback; `500-direct-alias-remains-deduced.t` guards the alias-target classification boundary at PA23's library-shaped template integration cluster | The pre-fix header-free reducer reports `unknown function equal`, while Clang 22 and GCC 15 accept and run it. The initial exact Container graph reports no viable `operator==` for `std::string_view` and a string literal even though candidate tracing shows the libc++ alias-based overload in the set and drops it only during deduction. | Warning-clean compiler build and isolated GCC 15 production-TU compile; both reducers pass Clang 22, GCC 15, and cppgm; PA23 direct report passes `400/400`; placement has zero findings; all configured strict suites pass; all 23 text-reparse categories and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled reducer LowIR are byte-identical. The PA9-excluded broad report passes `3933/3933`, including PA37 `7/7`. Focused `string_test` passes compile, link, and runtime; the exact forced Container replay removes it and leaves seven independent compile failures. | instructions -0.12%; max RSS -2.11%; peak footprint -0.16%; pass against immutable frozen-header baseline `9764b3835`; candidate-only report `/tmp/cppgm-boost-frontier-v2-container-alias-perf.log`, no parent rerun | `(this commit)` |

| fixed | Container recursive `slist`, `list`, and `set` node layout | A concrete class-template specialization can finish structural collection while a recursively referenced concrete value type still has no layout. The old dependent-shape finalization marked that specialization complete, so a later explicit layout demand returned it unchanged and `new Node` failed despite the recursive value type having completed in the meantime. `ClassInfo` now records typed provenance only for this concrete deferred-layout state. A later safe, explicit layout demand resets and rematerializes that same specialization; real layout finalization, dependent instantiations, and every reset path clear the provenance. No source or semantic text is parsed, no cache or Boost-specific rule is added, and already-required function bindings prevent an unsafe reset. | `pa23/tests/general/400-concrete-recursive-node-layout-retry.t`, at PA23's library-shaped template integration owner | The header-free reducer is accepted warning-clean by Clang 22 and GCC 15, passes the fixed compiler, and the saved parent fails with the exact `named type size unavailable: boost::container::base_node<recursive_slist, ...>` diagnostic. Real Boost traces show the node specialization finishing structurally during the enclosing recursive class's implicit-special-member work, before the recursive class receives layout. | The focused PA23 check and full PA23 report pass `401/401`; all configured strict suites pass; PA23 placement and hygiene report zero findings; all 23 text-reparse categories and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `7aadc4ff1aaf4d6c715ec696c5f8e80478fb1740110ef13fbc35f70b4c19bb43`. Changed units compile warning-clean under GCC 15. The PA9-excluded broad report passes `3934/3934`, including PA37 `7/7`. Focused and exact forced Boost replays pass `slist_test`, `list_test`, and `set_test`; the full graph updates 309 targets and advances from seven failures/21 skips to four failures/12 skips. | instructions -0.09%; max RSS -1.54%; peak footprint -0.03%; pass against immutable frozen-header baseline `9764b3835`; candidate-only medians are `263323410825` instructions, `1272758272` RSS, and `1009561600` footprint; report `/tmp/cppgm-boost-frontier-v2-container-recursive-layout-perf.json`; no parent command was run | `(this commit)` |

| fixed | Container `scoped_allocator_usage_test` dispatch return SFINAE | Concrete namespace function-template instantiation copied every template-bound type from its caller's use scope. When the caller was a member template of an all-pack primary's partial specialization, the concrete scope therefore retained unrelated primary placeholders. That made invalid concrete return types appear dependent and kept mutually exclusive dispatch overloads viable. Concrete namespace function templates now bind their own arguments in the declaration scope and overlay only function-local named types carried by those typed arguments; member templates and dependent namespace arguments retain the existing full instantiation context. No source text, cache, spelling test, ABI path, or Boost-specific rule is added. | `pa22/tests/general/300-concrete-namespace-function-sfinae-excludes-caller-placeholders.t`, a header-free return-SFINAE reducer at PA22's substitution/candidate-dropping owner | The saved exact `e4a961924` parent rejects the reducer with the same ambiguous dispatch diagnostic as Boost, while the fixed compiler, Clang 22, and GCC 15 accept it warning-clean. The parent scope trace contains the concrete partial-specialization bindings followed by unrelated `InnerAllocators=typename InnerAllocators` and `OuterAllocator=typename OuterAllocator` placeholders; the fixed concrete namespace instantiation excludes them. | Focused PA22 check and direct report pass `271/271`; all configured strict suites pass; PA22 placement and hygiene have zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `8deabfb1330360472df48dcc0e35ddef64bb38c17dc4bb302dc81ae164c35ca0`. The changed production unit compiles warning-clean under GCC 15. The PA9-excluded broad report passes `3935/3935`, including PA37 `7/7`. Focused Boost compile/link/runtime passes; the exact full graph passes 76 tests and advances from four failures/12 skips to three failures/nine skips. | instructions -0.66%; max RSS -3.53%; peak footprint -2.52%; pass against immutable frozen-header baseline `9764b3835`; candidate-only medians are `261810261946` instructions, `1246990336` RSS, and `984469504` footprint; report `/tmp/cppgm-boost-frontier-v2-container-scoped-allocator-usage-perf.json`; no parent command was run | `(this commit)` |

| fixed | Container `scoped_allocator_adaptor_test` cross-specialization converting constructor | A member constructor template instantiated from an all-pack primary's partial specialization carried concrete inner-allocator pack arguments, but its retained source syntax still named the ancestor owner pack. Structured function-template deduction initialized the parameter from the exact typed argument, then unconditionally replaced it with that older dependent syntax. The concrete `inner` element therefore became `typename InnerAllocators`, deduction failed, and the converting constructor was dropped. Structured deduction now keeps an already concrete carried type when resolving retained syntax would downgrade it to a dependent placeholder; genuinely concrete syntax still replaces the carried type and top-level cv remains applied afterward. No source text, cache, symbol spelling, ABI path, or Boost-specific rule is added. | `pa22/tests/general/300-converting-constructor-retains-concrete-owner-pack.t`, a header-free unevaluated constructor-SFINAE reducer at PA22's function-template deduction owner | The saved exact `4463de0cc` parent accepts the reducer but selects its fallback, while the fixed compiler selects the converting-constructor probe and Clang 22 and GCC 15 accept it warning-clean. Template deduction tracing shows the parent resolving canonical `allocator_adaptor<OtherOuter, inner>` back to `InnerAllocators...`; the fix retains concrete `inner`. The unevaluated regression isolates viability without triggering the separate retained-owner-pack ABI-symbol issue. | Focused PA22 check and direct report pass `272/272`; all configured strict suites pass; PA22 placement and hygiene have zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `a40acab6ef7e1692f1a015336711c995034f93dfbd14775439bad468c183f5bd`. The changed production unit compiles warning-clean under GCC 15. The PA9-excluded broad report passes `3936/3936`, including PA37 `7/7`. Focused Boost compile/link/runtime passes; the exact full graph passes 77 tests and advances from three failures/nine skips to two failures/six skips. | instructions -0.56%; max RSS -2.22%; peak footprint -2.52%; pass against immutable frozen-header baseline `9764b3835`; candidate-only medians are `262077817150` instructions, `1263992832` RSS, and `984477696` footprint; report `/tmp/cppgm-boost-frontier-v2-container-scoped-allocator-adaptor-perf.json`; no parent command was run | `(this commit)` |

| fixed | Container `flat_set_adaptor_test` allocator/container rebind ordering | Relaxed template-template matching made all three `container_rebind` partials viable for `vector<T, A=void, O=void>`: `Cont<V,A,An...>`, `Cont<V,A>`, and `Cont<V>`. Matching already proved that each omitted canonical trailing argument equaled its typed source default, but ordering then compared only the declaration patterns and produced no unique winner. Class partial ordering now inspects the matched actual specialization's structured `ClassTemplateSpecializationMangleInfo` or typed named metadata and first prefers a direct template-template head that omits fewer canonical arguments. Equal coverage retains the existing bidirectional and fixed-over-pack rules. No text parse, cache, spelling test, symbol path, or Boost-specific rule is added. | `pa21/tests/general/400-defaulted-template-template-partial-coverage-order.t`, beside PA21's existing template-template partial-ordering controls | The saved pre-fix compiler reports the exact three-way `ambiguous partial class specialization`; Clang 22 and GCC 15 select the full-coverage variadic partial warning-clean, as does the fixed compiler. Pairwise host controls establish the order `Cont<V,A,An...>` over `Cont<V,A>` over `Cont<V>` for the three-parameter defaulted template, while the existing genuinely unary fixed-over-pack PA21 control remains unchanged. Exact Boost tracing shows the same three matched `container_rebind` patterns at equal base score. | Focused PA21 check and direct report pass `235/235`; all configured strict suites pass; PA21 placement has zero placement and hygiene findings, with its one pre-existing EH review lead unchanged; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `fc19432b2355de05d62190d267aa98add078ef7d34f10daaeab4049741acaf17`. Both changed production units compile warning-clean under GCC 15. The PA9-excluded broad report passes `3937/3937`, including PA37 `7/7`. Focused Boost compile/link/runtime passes; the exact full graph passes 78 tests and advances from two failures/six skips to one failure/three skips. | instructions -0.59%; max RSS -3.45%; peak footprint -2.51%; pass against immutable frozen-header baseline `9764b3835`; candidate-only medians are `262000581751` instructions, `1248104448` RSS, and `984526848` footprint; report `/tmp/cppgm-boost-frontier-v2-container-flat-set-adaptor-perf.json`; no parent command was run | `(this commit)` |

| fixed | Container `devector_test` reference-qualified initializer-list constructor | Phase-one list-constructor selection passed the first constructor parameter directly to the typed `std::initializer_list` classifier. That classifier intentionally accepts class types, not reference wrappers, so a constructor taking `const std::initializer_list<T>&` was rejected while the equivalent by-value form participated. All ordinary and constructor-template phase-one filters now remove the parameter reference and top-level cv before the existing typed classifier. No source text, template spelling, cache, ABI path, or Boost-specific rule is added. | `pa25/tests/general/200-copy-list-const-reference-initializer-list-ctor.t`, beside PA25's existing by-value direct-braced initializer-list control | The saved pre-fix compiler accepts the header-free reducer but emits a call to the fallback `Pick(int,int)` constructor; Clang 22 and GCC 15 select the reference-qualified initializer-list constructor and return zero. The pre-fix focused Boost target reports the same constructor in its typed set but rejects it as `not initializer_list ctor`; the fixed target compiles and links. | Focused PA25 check and direct report pass `73/73`; all configured strict suites pass; PA25 placement and hygiene report zero findings; all 23 text-reparse categories remain zero and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `325bb4d049a4a633424a730d06682a27613476870265f5f6b106e9b745169477`. The changed production unit and reducer compile warning-clean under GCC 15. The PA9-excluded broad report passes `3938/3938`, including PA37 `7/7`. Focused Boost compilation and linking pass, exposing the independent runtime invalid-free frontier. | instructions -0.49%; max RSS -2.94%; peak footprint -2.56%; pass against immutable frozen compiler-and-header baseline `9764b3835`; candidate-only medians are `262258932547` instructions, `1254641664` RSS, and `984076288` footprint; report `/tmp/cppgm-boost-frontier-v2-container-devector-initlist-perf.json`; no parent command was run | `(this commit)` |

| fixed | Container `devector_test` input-range dispatch | A defaulted non-type class-template argument retained its declaring template's `T`, but enclosing function-template mangle substitution rebound that spelling through the use scope after substituting only `ForwardIterator`. An unrelated enclosing `devector<T>` binding captured the default and changed `has_iterator_category<input_iterator<...>>::value` to `has_iterator_category<regular_elem>::value`, selecting the forward-range overload for an input iterator. Defaulted non-type arguments are now rematerialized from their declaring template's already-substituted argument list, so owner parameters shadow unrelated use-scope bindings. Type defaults retain their established substitution path. No source text, cache, spelling rule, ABI fallback, or Boost-specific path is added. | `pa22/tests/general/100-defaulted-nontype-owner-scope-shadow.t`, at PA22's full function-template deduction owner; its witness reference comes from the patched Clang witness emitter | The exact typed trace showed parameters `[ForwardIterator]`, replacement `ForwardIterator=input_iterator<devector<regular_elem>>`, the unrelated replacement `T=regular_elem`, and the wrong result `has_iterator_category<regular_elem>::value`. The fixed full TU selects the input-range overload and no longer reaches the invalid free. The header-free reducer produces impossible `has_iterator_category<first_type>` and `<second_type>` instantiations before the fix, while patched Clang and the fixed compiler do not. | PA22 direct report passes `273/273`; all configured strict suites pass; PA22 placement/hygiene is clean; all 23 text-reparse categories and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `ca7abb148a8884993b15bd5e119342f933e9e62ae76888d6bb07cb7bfa1ea595`. Clang 22 and GCC 15 accept the reducer warning-clean, and both changed production units compile warning-clean with GCC 15. The focused Boost target advances from invalid free to the separate backing-array lifetime failure. | measured together with the adjacent lifetime fix: instructions +0.02%, max RSS -3.63%, peak footprint -2.57% against the immutable compiler-and-51-header epoch; candidate-only report `/tmp/cppgm-boost-frontier-v2-container-default-owner-initlist-candidate.json`; no parent or live-header command was run | `(this commit)` |

| fixed | Container `devector_test` initializer-list `regular_elem` lifetime | LowIR constructed nontrivial `std::initializer_list<T>` backing-array elements in a hidden slot but never registered their destructors. All 41 `regular_elem` initializer-list elements in `test_insert_init_list` therefore remained live and made nine later checkpoints fail. Each successfully constructed backing element now enters the existing full-expression cleanup stack immediately, providing reverse destruction and constructor-unwind safety. When the list initializes a named automatic `initializer_list` object, those exact cleanup actions transfer to the enclosing lexical scope so the backing array remains alive with the list. No semantic text, parser, cache, type spelling, or Boost-specific path is added. | `pa25/tests/general/200-initializer-list-backing-array-lifetime.t`, at PA25's initializer-list and EH cleanup owner | The exact runtime count was zero before `test_insert_init_list` and 41 afterward, exactly matching the 41 nontrivial list elements in that function. The reducer checks three call-argument elements are destroyed at the full-expression boundary, two named-list elements stay alive until lexical scope exit, and successful earlier elements are represented on later-constructor unwind paths. Clang 22 and the fixed compiler both return zero. | PA25 direct report passes `74/74`; all configured strict suites pass; PA25 placement/hygiene is clean; all 23 text-reparse categories and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `fad25179310d67817521e50e03eaadb60d8e02eb6a503af55e8024b2ddfe31da`. The PA9-excluded broad report passes `3940/3940`, including PA37 `7/7`. Focused `devector_test` passes, and the exact final Container graph updates all 325 requested targets and exits successfully. | instructions +0.02%; max RSS -3.63%; peak footprint -2.57%; pass against immutable compiler-and-51-header baseline `9764b3835`; candidate-only report `/tmp/cppgm-boost-frontier-v2-container-default-owner-initlist-candidate.json`; no parent or live-header command was run | `(this commit)` |

| fixed | Fusion `fold` qualified static-value pack expansion | Initial template-argument parsing recognized `Conditions::value...` as a type-id but failed to retain the parallel qualified-id expression because the expression parser received the trailing ellipsis. The parser now strips the ellipsis only when the remaining original token range is a complete qualified name with at least one qualifier, builds the structured expression, and marks that template argument as a pack expansion. Direct type packs such as `T...` retain the established type path. No semantic reparse, source scan, cache, spelling rule, ABI path, or Boost-specific branch is added. | `pa22/tests/general/500-inherited-static-value-pack-base.t`, at PA22's integrated variadic substitution owner | The initial exact Fusion graph reaches `and_impl1<Cond::value...>`, evaluates `and_<is_convertible<int,int>>::value` false, omits the forwarding constructor, and rejects `boost::fusion::vector<int>(1)`. The header-free reducer checks the same inherited static member-value expansion, while Clang 22 and GCC 15 accept it warning-clean. | PA22 passes `286/286`; configured strict suites pass; PA22 placement/hygiene has zero findings; all 23 reparse categories remain zero and all 22 audit/perf tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `b0c078f4a398ac0f1053d6ffed463b987556a95e0e807229598f8eb895b70bcd`. Both production units are warning-clean under Clang 22 and GCC 15. The PA9-excluded broad report passes `3999/3999`, including PA37 `7/7`; the exact final Fusion graph passes. | measured with the complete Fusion closure: -1.49% instructions, +0.84% RSS, +2.23% footprint against immutable frozen compiler-and-51-header epoch `9764b3835`; report `/tmp/cppgm-boost-frontier-v2-fusion-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Fusion `is_sequence` and `is_view` qualified result-SFINAE | A substituted function-template result already carried a concrete semantic type, but the dependency fallback still scanned its display text and treated an ordinary `incomplete` identifier as dependent merely because a surrounding namespace scope contained template names. That retained the invalid `ok_tag<sizeof(incomplete)>` result instead of dropping the candidate by SFINAE. Dependency classification now suppresses only the textual unresolved-identifier fallback when the relevant type-id or expression AST node already has a concrete nondependent semantic type; structured placeholder, bound-name, pack, and dependent-type checks remain active. No text is parsed, no candidate retry or cache is added, and dependent nodes retain the established path. | `pa22/tests/general/500-qualified-dependent-result-sizeof-sfinae.t`, beside the existing unqualified `500-dependent-result-sizeof-sfinae-base.t` control at PA22's integrated result-SFINAE owner | The reduced qualified `detail::check_complete<T>(0)` probe retained `ok_tag<sizeof(incomplete)>` before the fix instead of selecting the ellipsis fallback. The exact Boost traits use the same incomplete-type probe in `is_sequence` and `is_view`; both final focused targets and the uninterrupted full graph pass. Clang 22 and GCC 15 accept the reducer warning-clean. | The qualified reducer and existing unqualified control pass. PA22, strict, placement, reparse, audit/perf, host-warning, PA9-excluded broad, and exact Fusion gates pass. Normal, all ten individual cache-disabled, and all-disabled reducer LowIR are byte-identical with SHA-256 `41be34202d3c92843e6c4a85111139a9ae83cfa84c9e4a1bd1afe16bd2a78565`. An experimental lightweight ellipsis shortcut was rejected before acceptance because `tuple_traits__maybe_variadic` exposed eleven false constructibility results; the final typed dependency fix leaves both variadic and nonvariadic tuple-trait paths passing. | measured with the complete Fusion closure; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-fusion-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | LEAF dependent enum-valued non-type partial pack | Non-type partial-specialization matching could validate and expand a dependent value pack before resolving its parameter's dependent enum type from retained syntax. The matcher now resolves the parameter type structurally after earlier type deductions, defers the compatibility check only while that expected type remains dependent, and preserves the exact annotated enum type on expanded literal values. | `pa22/tests/spec/300-dependent-nontype-partial-value-pack.t` | LEAF's error matching path lost the enum type carried by `condition<Enum>` while expanding its value pack, so the intended partial specialization did not materialize. The reducer checks both selection and the exact enum type of every expanded value. | Focused regression, PA22 owner checks, configured strict suites, placement/hygiene, all cache modes, zero-reparse/audit gates, GCC 15 warning checks, the PA9-excluded `4008/4008` report, and final LEAF replay pass. | measured with the complete LEAF closure: -2.22% instructions, +1.27% RSS, +2.44% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-leaf-candidate.json`; no parent or live header was measured | `(this commit)` |
| fixed | LEAF qualified `sizeof`/`decltype` result SFINAE | Substituting a qualified concrete type into a retained type-id did not carry its structured qualified-name syntax into the nested `sizeof`/`decltype` result probe, and the expression description path omitted the typed `sizeof` node. The existing structured sidecars now retain that identity through result-SFINAE evaluation without parsing display text. | `pa22/tests/spec/300-dependent-sizeof-decltype-qualified-member.t` | LEAF's Boost.JSON encoder path failed a return-type probe involving the size of a qualified concrete wrapper and its qualified member. The reducer forms the same `sizeof(T) == sizeof(decltype(declval<T const &>().value))` constraint. | Focused regression, PA22 owner checks, strict, placement, cache, zero-reparse/audit, warning, broad, and final LEAF gates pass. | measured with the complete LEAF closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | LEAF conversion-function template top-cv deduction | Conversion-function-template deduction used the cv-qualified target class itself as the deduction target. Top-level cv belongs to the resulting standard conversion sequence, not the conversion-type-id deduction target, so otherwise valid `operator Type()` candidates disappeared for `const result<T>`. The typed deduction target now uses the unqualified base type while preserving the original destination for ranking and conversion. | `pa23/tests/spec/400-conversion-function-template-top-cv-sequence.t` | The reducer converts the same source through `operator Type()` to both `result<int>` and `const result<int>`; the latter failed before the fix. | Focused regression, PA23 owner checks, configured strict suites, placement/hygiene, all cache modes, zero-reparse/audit gates, warning checks, broad report, and final LEAF replay pass. | measured with the complete LEAF closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | LEAF same-type nonpolymorphic `dynamic_cast` | The semantic checker required a polymorphic source class before classifying every pointer `dynamic_cast`, including identity and unambiguous upcast forms that require no runtime check. It now accepts same-class and valid base-class conversions first and requires polymorphism only for `void*`, cross-cast, and downcast runtime forms. | `pa25/tests/general/100-dynamic-cast-same-nonpolymorphic.t` | LEAF's typed exception/error path used an identity `dynamic_cast` on a nonpolymorphic class and was rejected before lowering. The reducer verifies pointer identity. | Focused regression, PA25 owner checks, placement, cache, strict, zero-reparse/audit, warning, broad, and final LEAF gates pass. | measured with the complete LEAF closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | LEAF inline-namespace `__PRETTY_FUNCTION__` type spelling | Predefined-function rendering retained inline-namespace components from the canonical class metadata and could drop namespace qualification from enum types. The typed display path now collapses inline namespaces while preserving the enclosing namespace for both classes and enums. | `pa34/tests/run/800-pretty-function-inline-namespace-run.t` | LEAF's diagnostic/encoder output expected `api::value`-style public type spelling rather than its internal inline ABI namespace. The runtime reducer checks the class spelling and a neighboring qualified enum control. | Focused runtime regression, PA34 owner checks, placement, cache, strict, zero-reparse/audit, warning, broad, and final LEAF gates pass. | measured with the complete LEAF closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | LEAF constructor versus conversion-template initial sequence | User-defined conversion candidates retained only their second standard conversion sequence, so a forwarding converting constructor and a conversion-function template could tie even though their initial source bindings differed. Candidate records now retain the typed initial parameter, argument, rank, reference binding, and qualification preference, compare that initial sequence first, and instantiate only according to the caller's existing body policy. | `pa22/tests/spec/300-constructor-template-initial-sequence-beats-const-conversion-template.t` | `error_code_test` and the system-result example could select or retain the wrong `error_id -> result<T>` path. The reducer requires the direct forwarding constructor to beat the const conversion-function template. | Focused regression and exact LEAF error/result targets pass. Normal plus ten individual cache-off modes plus all-off are byte-identical with SHA-256 `297a34cbf47a5dd117852d2d5f1b5c981480f70c87fb501c0a1f8667c5a03d8f`; PA22, strict, placement, zero-reparse/audit, warning, broad, and final LEAF gates pass. | measured with the complete LEAF closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | LEAF concrete alias target with nested conversion template | Direct alias expansion could leave a concrete class alias represented by its dependent source pattern, hiding nested conversion templates of the selected specialization. Blindly materializing every alias changed unrelated witness lifecycles, so the final path materializes only a structurally direct class-template target whose dependent leaves are the alias's own type parameters, and regenerates stale concrete argument display from typed structure. | `pa23/tests/spec/400-concrete-alias-target-nested-conversion-template.t` | `print_file_leaf_result` could not convert a nested `error_result` through `operator result<U>()` when `result` was reached through the outer alias. Blind materialization fixed LEAF but changed two existing PA21 witnesses; the narrowed typed predicate preserves both. | Focused regression and exact example pass. Normal plus ten individual cache-off modes plus all-off are byte-identical with SHA-256 `d3c2b595ed9d983148f8696192056bc4a7d3bd5cb0a88d6497a910ac80cadeec`; PA23, configured strict witnesses, placement, zero-reparse/audit, warning, broad, and final LEAF gates pass. | measured with the complete LEAF closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | LEAF injected class template hides later outer alias | Deferred unqualified template-name lookup allowed a later outer alias template to replace the nearer injected class-template name inside an already declared class member template. Both context-aware and direct typed lookup now reject that alias when a nearer class template or source-template class supplies the injected name. | `pa23/tests/spec/400-injected-class-template-hides-later-outer-alias.t` | LEAF's nested `error_result::operator result<U>()` and `move_from(result<U>&&)` rebound `result` to the later global alias instead of the enclosing injected class template. | Focused regression and exact LEAF nested-result targets pass. Normal plus ten individual cache-off modes plus all-off are byte-identical with SHA-256 `9cfe76dd776677d5bf5204fb78ba75d7d7ae23a12dc6ea16da8c8ccc67de6edb`; PA23, strict, placement, zero-reparse/audit, warning, broad, and final LEAF gates pass. | measured with the complete LEAF closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | LEAF base-subobject virtual receiver view | Virtual-call lowering first searched only the receiver's static vtable view, which fixes ordinary base-subobject dispatch but can discard construction-time or owner-specific VTT tables; using only the emitted declaration has the opposite failure. The final typed selection first preserves an exact emitted-function slot from the active table and falls back to the receiver's static view only when no exact slot exists. | `pa27/tests/general/100-base-subobject-cast-virtual-dispatch.t` | The reducer casts a derived object to `base const&` and requires the derived override. Intermediate static-view logic broke PA29 separate-object VTT retention and two PA32 virtual-base construction/dispatch controls; the ordered exact-slot fallback passes all four cases. | Focused PA27 runtime, PA29/PA32 VTT controls, owner checks, placement, cache, strict, zero-reparse/audit, warning, the PA9-excluded `4008/4008` report, and final LEAF replay pass. | measured with the complete LEAF closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Lexical Cast parenthesized qualified call before multiplication | The unary C-style-cast probe asked the shared parenthesized type-id/expression disambiguator not to attempt an expression parse. A qualified zero-argument call such as `(limits::epsilon()) * 2` can also parse syntactically as a type-id with a function-style abstract declarator, leaving `* 2` to look like the cast operand. That malformed cast survived until semantics rejected its function target. The unary probe now enables the disambiguator's existing structured expression preference; genuine type-ids retain the cast path, while the complete qualified call becomes the parenthesized expression before postfix/binary parsing. No text reparse, semantic retry, cache, name spelling rule, or Boost-specific path is added. | `pa10/tests/general/100-parenthesized-qualified-call-before-multiply.t`, a pure AST oracle at the initial parser owner | The initial `lexical_cast_test` and `float_types_test` builds both report `unsupported cast target type` whose AST is `type-name std::numeric_limits<T>::epsilon` plus an abstract `parameter-clause`. A header-free executable reducer fails identically before the change and returns zero under Clang 22, GCC 15, and the fixed compiler. | The focused AST check and complete PA10 report pass `137/137`; configured strict suites pass; all 23 text-reparse categories and all 14 audit tests pass. Normal, all ten individual cache-disabled, and all-disabled LowIR are byte-identical with SHA-256 `24ef12525c6eed99673044a5dc99c45bd0f1640c94358c5cf30b5063009fc8b8`. The new test has no placement finding despite PA10's pre-existing conservative audit backlog. `cppast_parser.cpp` compiles warning-clean under GCC 15. The PA9-excluded broad report passes `4009/4009`, focused Boost targets pass, and the exact final Lexical Cast graph passes. | -2.00% instructions, +1.20% RSS, +2.35% footprint against immutable frozen compiler-and-51-header epoch `9764b3835`; report `/tmp/cppgm-boost-frontier-v2-lexical-cast-candidate.json`; no parent or live-header command was run | `(this commit)` |
| fixed | Local Function macro-retained nested deduced calls | Namespace function-template visibility compared retained macro replacement token ordinals directly with declaration ordinals. A token retained from a macro definition can predate the ordinary function whose body contains the expansion, so later-declared helpers such as `wrap` were incorrectly hidden inside the expansion and downstream `remove_cv_ref_t` aliases failed. Visibility now anchors such uses to the enclosing ordinary function definition and otherwise uses retained physical same-file locations when token streams are incomparable. No source scan, text parse, cache, spelling rule, or Boost-specific path is added. | `pa22/tests/general/100-local-decltype-alias-nested-deduced-calls.t` plus two auxiliary include fixtures, at the full function-template deduction owner | The controlled pre-fix compiler rejects the header-split macro reducer and the exact `add_seq_nova.cpp` path after hiding a namespace template whose declaration precedes `main`; Clang 22 and the fixed compiler accept both. | PA22 and the exact Boost target pass. Normal plus ten individual cache-off modes plus all-off are byte-identical with SHA-256 `09cc953d6b5fa1792fbe68896d216efc9e6faeb891fa5acf2112bc80e2d3ad61`; owner, strict, placement, zero-reparse/audit, GCC 15, broad, frozen performance, and final Local Function gates pass. | measured with the complete Local Function closure: -1.84% instructions, +2.23% RSS, +2.50% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-local-function-candidate.json`; no parent or live header was measured | `(this commit)` |
| fixed | Local Function repeated declaration/expression probe | Boost.LocalFunction uses the documented repeated-declaration trick in which the first `declared<resolve<sizeof(args)>::cmp1<0>::cmp2> args;` declares a sentinel and the second token-identical statement is an expression parsed through `<` and `>`. The initial AST retains the second form as a simple declaration, so semantic analysis tried to evaluate the entire qualified expression as one non-type argument. The statement layer now recognizes only the exact structured template-id/declarator shape, resolves the retained owner and member constants, verifies the prior sentinel specialization, and analyzes the corresponding structured `operator>` call. No source text, token reconstruction, cache, or name spelling special case is used. | `pa24/tests/general/400-repeated-local-declaration-template-probe.t`, at the post-template source-to-LowIR language-closure owner; no witness reference was fabricated without the patched Clang emitter | The controlled pre-fix compiler reports `failed non-type template argument evaluation` on the second statement in `all_decl.cpp`, while Clang 22 and the fixed compiler accept the header-free reducer and exact Boost source. | PA24 and the exact Boost target pass. Normal plus ten individual cache-off modes plus all-off are byte-identical with SHA-256 `ad9f5153e3b09ff6292903f8c578a4688ca4f6a34672cb383deff586862b78c4`; placement/hygiene is zero, and the configured strict, zero-reparse/audit, GCC 15, broad, frozen performance, and final Local Function gates pass. | measured with the complete Local Function closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Local Function qualified-return function-pointer declaration | The initial parser preserves `ns::result (*function_pointer)();` through a legal expression-shaped ambiguity: an empty outer call around a functional cast whose argument is unary `*function_pointer`. Statement semantics previously sent that shape directly to expression analysis and rejected the class/type call. The statement layer now consumes the retained qualified type node and reconstructs the corresponding structured nested pointer/function declarator before using the ordinary declaration analyzer. No source text, token reconstruction, cache, spelling rule, or Boost-specific path is added. | `pa12/tests/general/100-qualified-return-function-pointer-declaration.t`, reduced to a namespace typedef and ordinary function at the first statement-semantics owner | The controlled pre-fix compiler rejects the reducer and exact `overload.cpp` declaration as a functional cast; Clang 22 and the fixed compiler classify the local as a pointer to a function returning the qualified type. | PA12 and the exact Boost target pass. Normal plus ten individual cache-off modes plus all-off are byte-identical with SHA-256 `2dbac162670ac6ed39a4256b932fe8d523144d3e720f79e096a372bb1ce16755`; the new reducer adds no finding to PA12's pre-existing conservative placement backlog, and strict, zero-reparse/audit, GCC 15, broad, frozen performance, and final Local Function gates pass. | measured with the complete Local Function closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Locale omitted-template-id explicit function specialization | Explicit function specializations without a written template-id were deduced from synthetic call arguments, which cannot deduce a template parameter appearing only in the return type. The collector now gives the existing typed deduction service the complete declared function type as its target and records the returned arguments and pack sizes. No text reconstruction, speculative call, cache, or Boost-specific path is added. | `pa22/tests/spec/100-explicit-specialization-return-only-deduction.t`, at the full function-template deduction owner | The initial Locale graph rejects 25 sources at the shared specializations in `boostLocale/test/tools.hpp` with `unknown function template explicit specialization`. The header-free reducer specializes `result<T> make_result(int)` as `result<long> make_result(int)` with the template-id omitted and fails before the fix. | The reducer, PA22, all twelve cache modes (SHA-256 `472cb5f6ed3001f51205a47b1b51fab0c9966d8b11649022b25eb99185d5bc53`), strict, placement, zero-reparse/audit, Clang/GCC controls, warning, broad, and final Locale gates pass. | measured with the complete Locale closure: -1.78% instructions, +2.51% RSS, +2.54% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-locale-candidate.json`; no parent or live header was measured | `(this commit)` |
| fixed | Locale maximum Unicode scalar UTF-8 encoding | The four-byte encoder used a strict comparison against `0x10FFFF`, rejecting the maximum valid Unicode scalar while accepting every smaller scalar. The inclusive bound now matches the Unicode scalar range. | `pa1/tests/100-universal-character-name-max.t`, at the preprocessing-token Unicode owner | `test_utf.cpp` reports `Invalid 32 bit value for utf8` for U+10FFFF before the one-character boundary fix. The PA1 token reducer isolates the same maximum universal-character-name value. | The focused PA1 check, six-owner `978/978` report, host control, warning, broad, and final Locale gates pass. The PA1 placement audit retains only its ten documented pre-existing findings; the new reducer adds none. Semantic cache proof is not applicable to token encoding. | measured with the complete Locale closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Locale late static-member definition explicit-instantiation replay | An already-complete class-template specialization could cache output/dependency state before its out-of-class static data definition was recorded. A later explicit-instantiation definition then reused the stale completion and omitted storage. Recording the definition now invalidates the existing out-of-class definition caches for that typed owner template, allowing ordinary replay to observe it. | `pa21/tests/spec/300-explicit-instantiation-prior-reference-static-data.t`, at explicit class instantiation and static-member ownership | Locale's `facet_id<info>::id` and `facet_id<calendar_facet>::id` were absent at link when their specializations had been completed before the definitions were seen. The reducer forms the specialization before the out-of-class definition, then explicitly instantiates the class and fails to emit the static object before the fix. | The reducer, PA21, all twelve cache modes (SHA-256 `7b1654ba2fa17e4ef8b507f1077c19cf156c48a4fb212a0e97bffe7edfd2cbb9`), strict, placement, zero-reparse/audit, warning, broad, focused Locale links, and final graph pass. | measured with the complete Locale closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Locale range-for element user-defined conversion | Range-for lowering synthesized the dereferenced element and attached it directly to the loop variable, bypassing the normal initialization conversion path. Both iterator and bounded-array/initializer-list paths now pass the typed element through the shared argument-conversion service before binding the loop variable, so conversion operators and reference binding are represented in LowIR. | `pa24/tests/general/100-conversion-operator-reference-initialization.t`, at PA24's range-for and conversion closure | `show_config` bound `const Backend&` directly to `std::reference_wrapper<const Backend>` storage instead of invoking its reference conversion, producing a null virtual receiver at runtime. The reducer checks both ordinary reference initialization and range-for over wrapper elements. | The reducer, PA24, all twelve cache modes (SHA-256 `ec4100996e8972ad7e170915be384c65e50553d7028e9b0bcfd3fd946dd4492a`), placement, strict, zero-reparse/audit, host controls, warning, broad, focused `show_config`, and final Locale gates pass. | measured with the complete Locale closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Locale implicit-object qualification exact-match rank | The member-candidate implicit-object conversion classified a qualification-only pointer adjustment as a conversion rank. C++ ranks qualification adjustment on the implicit object parameter as an exact match, so the member operator could incorrectly tie with or lose to a nonmember candidate. The typed rank now returns exact for compatible pointee types after qualification conversion. | `pa15/tests/general/300-member-vs-nonmember-operator-implicit-object-cv-rank.t`, at the first class/member overload owner | `test_date_time` reports an ambiguity between `date_time::operator-(date_time_period_set const&) const` and the nonmember `operator-(date_time const&, date_time const&)`. The header-free reducer requires the member candidate to win because its implicit-object and explicit parameter sequences are better. | The reducer, PA15, all twelve cache modes (SHA-256 `90bdbf62b4a0414113a746ab1e321217268fb3ba34015b6f15e4f99b5648954d`), placement, strict, zero-reparse/audit, Clang/GCC controls, warning, broad, focused date-time, and final Locale gates pass. | measured with the complete Locale closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Locale pack-expanded unknown-bound array expression | Array braced-init expression analysis required a source type with a known bound before expanding the retained initializer elements. For `using expander = int[]; expander{0, expr...}`, the pack expansion itself supplies the bound. The typed path now expands first, rejects an empty unknown-bound list, deduces the effective bound from the resulting elements, and carries that bounded array type through lowering. | `pa24/tests/general/100-variadic-pack-unknown-bound-array-list.t`, beside the existing bounded pack-array controls at PA24's direct braced-expression owner | `test_formatting` fails with `invalid array braced-init-list` in its standard pack-expansion sink. The reducer instantiates an unknown-bound `int[]` alias with three pack elements and fails before the fix. | The reducer, PA24, all twelve cache modes (SHA-256 `4784323d7706faa120d24849f0cc18edf0f8b57f155ab960bd3b0f1eccf92547`), placement, strict, zero-reparse/audit, host controls, warning, broad, focused formatting, and final Locale gates pass. | measured with the complete Locale closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | Locale qualified base `num_put::do_put` dispatch | The hosted `num_put` runtime bridge matched both public `put` and protected virtual `do_put`. That is correct for the public dispatching entry point but wrong for an explicitly qualified base call, which must invoke the base implementation nonvirtually; bridging it redispatched to the derived facet and recursed. The bridge now applies only to `put`, leaving qualified `do_put` on the ordinary typed direct-call path. | `pa36/tests/link/700-hosted-num-put-qualified-base-runtime-smoke.t`, at hosted library link/runtime behavior | `test_generator`, `test_posix_formatting`, and `test_std_formatting` overflow the stack after a derived facet calls `std::num_put<char>::do_put`. The hosted reducer overrides `do_put`, calls the qualified base implementation, and must print `60` once. | The focused PA36 runtime reducer prints `60`, existing public-`put` controls remain green, PA36 and the six-owner report pass, placement/hygiene is zero, production units are warning-clean under Clang 22 and GCC 15, and broad plus final Locale gates pass. | measured with the complete Locale closure; candidate-only frozen report; no parent or live header was measured | `(this commit)` |
| fixed | MP11 `mp_fold_q` concrete class-template reference after a dependent textual collision | A prior dependent `quote<F>` reference and a later concrete global `quote<F>` shared the same presentation key. The class-reference cache reused the dependent instance and its member-alias scope, so function-template output retained unresolved `Q::fn`. A textual hit that transitions from dependent to concrete now bypasses the fast return, resets the existing reference instance, and rebinds its already typed concrete arguments. No text parse, spelling-specific rule, ABI fallback, or Boost branch is added. | `pa23/tests/general/400-dependent-class-template-reference-cache-rebind.t`, a 16-line header-free C++11 integration reducer with checked-in LowIR and exit-status references; PA23:400 placement has no later-feature finding | The clean current-commit compiler emits an unresolved `check<Q::fn<int,int>>`; exact Boost `mp_fold_q.cpp` leaves eleven trait specializations undefined. Clang 22 accepts the reducer warning-clean. | The header-free reducer, reduced Boost-header case, and exact Boost source compile, link, and run; focused B2 passes. The final PA22/PA23 direct report passes `712/712`; configured strict comparisons pass, including PA23 `338/338`; all 23 reparse categories and the audit/performance tests pass; normal and two relevant cache-disabled reducer outputs are byte-identical; Clang 22 and GCC 15 production-unit builds are warning-clean. | -11.30% instructions, -10.65% RSS, and -11.45% footprint; candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-mp-fold-q-cache-rebind-perf.json`; no parent or live header was measured | `(this commit)` |
| fixed | MP11 `mp_reverse_fold` both-pack fixed-prefix partial ordering | The existing structured comparator handled a trailing pack against a fixed pattern but returned a tie when both template-id patterns had trailing packs. When both fixed prefixes consist only of distinct unconstrained type parameters, the pattern with more fixed parameters accepts a strict subset and is more specialized. The comparator now recognizes only that structured case; concrete or repeated constraints remain on their existing ordering paths. No source text, Boost rule, cache, or language-version extension is added. | `pa21/tests/general/400-template-id-fixed-prefix-pack-partial-ordering.t`, 8 nonblank header-free C++11 lines (11 with spacing, 444 bytes) at PA21's template-template/class-partial-ordering owner; its 106-byte empty-main reference is reused from an existing PA21 fixture rather than generated by the candidate compiler | The saved pre-fix compiler reports the exact ambiguous partial-class-specialization error. Clang 22 selects the longer prefix, and exact Boost's one-element and ten-element `mp_reverse_fold_impl` partials have the same structured relation. | The reducer passes cppgm++ and Clang warning-clean; PA21 passes `243/243`; strict PA18/19/21/22 comparisons have zero failures; all 23 reparse categories and 23 audit tests pass; the production unit is warning-clean under Clang 22 and GCC 15. Exact `mp_reverse_fold` and `_q` compile, link, run, and pass under a two-job forced B2 replay. | final candidate-only frozen gate passes twice; confirmation is -11.27% instructions, -10.64% RSS, and -11.36% footprint in `/tmp/cppgm-boost-frontier-v2-mp-reverse-fold-pack-order-perf-repeat.json`; no parent or live header was measured | `(this commit)` |
| fixed | MP11 `mp_compose` template-template pack element identity | A bound template-template parameter pack recorded its size but not a distinct structured binding for every concrete template element. Expanding `X...` could therefore reuse the first bound head for every slot, turning `L<A, B>` into `L<A, A>`. Binding now records each exact template argument under the existing pack-element naming convention, and structured type/expression syntax substitution consumes the corresponding element without reparsing source text. | `pa23/tests/general/400-template-template-pack-alias-expansion-order.t`, 6 lines / 243 bytes with no headers and a 106-byte LowIR reference | The saved pre-fix compiler makes both expanded elements `A`; the conditional type check therefore rejects `C<A, B>::R` against `L<A, B>`. Clang 22 and GCC 15 accept the reducer as C++11. | The reducer passes normally and with the relevant caches disabled with byte-identical LowIR. The final PA22/PA23 direct report passes `712/712`; configured strict comparisons pass, including PA23 `338/338`; all 23 reparse categories and 31 audit/performance unit tests pass. The final PA9-excluded broad report passes `4068/4068`, including PA37 object roundtrip `7/7`. Exact `mp_compose` compiles, links, runs, and passes under a forced two-job B2 replay at 234,467,328 B RSS with zero swaps. | -11.08% instructions, -10.45% RSS, and -11.43% footprint; three-run candidate-only frozen report `/tmp/cppgm-boost-frontier-v2-mp-compose-cache-lifetime-perf.json`; no parent or live header was measured | `(this commit)` |
| fixed | Dependent-type resolution cache input lifetime | The nested dependent-type cache keyed entries by a raw `Type *` while retaining only the resolved output. Recursive resolution could release an ephemeral input and allocate an unrelated type at the same address before the outermost query cleared the cache, producing a stale hit such as `const wrap_iter<size_t*>& -> int`. Each key now owns its input `TypePtr` for the cache entry's bounded lifetime; lookup ordering remains pointer-based and the cache is still cleared after the outermost query. | `pa22/tests/general/300-dependent-type-resolution-cache-input-lifetime.t`, reduced to 21 lines / 851 bytes of header-free C++11 with a local three-line `enable` stand-in | The saved pre-fix compiler fails 43 of 256 concurrent compilations of the reduced declaration graph. Trace evidence showed the same address first stored for a resolved `enable_t<..., int>` input and then reused by an unrelated `const W&` input. The hosted `<algorithm>` form reproduced 9 of 16 times; disabling the cache passed 16 of 16. | Clang 22 and GCC 15 accept the reducer warning-clean. The focused PA22 check, cache-on/off byte parity, and 48 concurrent fixed-compiler repetitions pass with zero swaps and at most 10,137,600 B RSS per process. The frozen self-compile gate completes three fresh compiler processes, each releases fully between runs, and records zero process swaps. | included in the same final three-run result: -11.08% instructions, -10.45% RSS, and -11.43% footprint; the cache ownership fix retains both the instruction win and recovered memory | `(this commit)` |

## Decision Log

- `2026-07-26`: Closed MSM and advanced the cursor to MultiArray. The exact
  C++11-only `msm-unit-tests` target passes all 37 actions and updates all 213
  requested targets. The C++17 and C++20 sub-suites remain outside CPPGM's
  supported language lane. The old approximately 9 GiB no-output pathology is
  gone: even the heaviest completed TU is bounded at 4,380,020,736 B in the
  two-job graph and releases after compilation. A serialized replay of the
  three serialization targets passes at 2,839,158,784 B maximum RSS with zero
  process swaps. Ordinary targets remain parallel; known multi-GiB targets are
  serialized to avoid aggregate system pressure.

  The final semantic failure was an exact-value loss after lazy materialization,
  not another cache or retention problem. A structured boolean fallback
  completed an inherited static member's qualifier and reduced integer `3` to
  `true`; equality then observed `1` instead of retrying typed lookup. Commit
  `2c6db27c0` retries that typed lookup and adds a 21-line header-free C++11
  PA19 reducer with a patched-Clang witness. The reducer has byte-identical
  LowIR in normal, ten individual cache-off, and all-off modes.

  Final validation passes the PA9-excluded direct report `4094/4094`, PA37
  object roundtrip `7/7`, all 960 strict comparisons, all 23 zero-reparse
  categories, all 31 audit/performance unit tests, PA19 placement/hygiene, and
  Clang 22 C++11 warning controls. The committed immutable source-and-51-header
  gate passes at -26.56% instructions, -28.56% RSS, and -35.61% footprint;
  report `/private/tmp/cppgm-boost-frontier-v2-msm-lazy-integral-final-perf.json`.
  No parent compiler or live project header was measured.

- `2026-07-25`: Packaged the final MSM algorithmic pass and returned to the
  forced frontier. Name-directed reference-only member materialization replaces
  eager whole-class work while witness generation retains its required eager
  path. Structured nested/local specialization identity, completed host-ABI
  fact reuse, bounded internal names, and removal of redundant rendered-symbol
  storage close the remaining measured algorithmic and retention defects. The
  25-nonblank-line PA21 reducer is header-free C++11, uses no `<type_traits>`,
  passes Clang and GCC with warnings as errors, and produces byte-identical
  LowIR with each of ten caches and all caches disabled.

  The guarded default-backend `CompositeEuml` scale input completes in 113.47s
  at 1,954,742,272 B RSS, 1,872,072,704 B footprint, and 528,081,721,146
  instructions with zero swaps. Its LowIR SHA-256 is
  `864522e6abe8a9f1f40fdd48c5525610dd0069a36473a91bd638512606bbb77c`.
  It substitutes a single default `test_machines` backend and omits the forced
  `Playing_type` process-event instantiation, so it is not the exact source.
  The exact one-job B2 compile clears the old memory-bound phase and reaches a
  later `boost::mpl::for_each` qualified-call diagnostic at 3,453,186,048 B
  maximum RSS with zero process swaps. This is still a substantial recovery
  from the former 8.98 GiB no-output footprint, but the exact target remains
  open and 1.95 GB is not its peak.

  The clean committed-HEAD immutable source-and-51-header gate passes at
  -26.39% instructions, -28.56% RSS, and -35.36% footprint; versus the prior
  concrete-syntax checkpoint it improves another 3.32%, 7.15%, and 8.35%.
  The final PA9-excluded direct-LowIR report passes `4092/4092`, including PA37 `7/7`;
  all 959 strict comparisons, all 23 zero-reparse categories, and all 14 audit
  tests pass. The five normal LowIR reference changes have narrow causes:
  lazy first-use ordering in PA18, PA22, PA23, and PA26, and removal of an
  internal overload ordinal in PA21. Witness references are unchanged. The
  next action is to reduce and fix the `for_each_impl<...>::execute`
  call-candidate loss exposed by the exact source.

- `2026-07-25`: Ended the owner-specific representation pass and returned to
  the MSM frontier. At 8,203 reference collections, the primary semantic census
  accounts for 243.2 MB and the pointer-deduplicated omitted AST census adds
  73.6 MB. Type-mangle argument ownership contributes 43.4 MB; 40.8 MB of that
  is source syntax attached to concrete type and value arguments. At 32,768
  collections the compiler owns 41,927 classes, 37,393 functions, 12,397
  function templates, and 73,500 template scopes. Those counts grow
  at less than four times the first-boundary counts, while retained class-member
  AST children grow from 34,635 to 114,866. We found no hidden second
  specialization wave or large repeated member traversal in the measured
  interval.

  The packaged representation change releases concrete type and ordinary value
  source syntax after typed mangle metadata is complete. It keeps dependent
  syntax, template entities, witness-session syntax, and concrete member-pointer
  values. An initial broad run found the two member-pointer dependencies in
  PA26; preserving that value category restores PA26 `82/82`. The same broad
  run passed the other `4089/4089` direct-LowIR tests, and all 959 configured
  strict comparisons pass. The immutable source-and-51-header gate records
  -23.87% instructions, -23.06% RSS, and -29.47% footprint with median RSS
  994,582,528 B. On `CompositeEuml`, the change reduces RSS by about 2%; that
  matches the census and cannot close the Clang gap. Further progress requires
  a compact semantic/AST representation rather than another cache, alias
  retry, or eager-instantiation change.

  The packaged exact replay confirms the boundary. At 204 seconds the compiler
  reports 6.88 GiB RSS. macOS then compresses resident pages while physical
  footprint keeps rising; at 261 seconds the process owns 8.98 GiB of physical
  footprint and system swap reaches 1.55 of 2.00 GiB. The compiler remains
  CPU-active and produces no diagnostic. We stopped the compiler child, then
  its replacement child and B2. The system recovered without an orphaned
  process. Log:
  `/private/tmp/boost-frontier-v2-suite-071-concrete-syntax-composite-euml.log`.

- `2026-07-24`: Kept suite 71 open for one owner-specific representation pass.
  The packaged semantic fixes are clean, but the exact `CompositeEuml` replay
  remains active beyond 328 seconds and reaches a 10.5 GB footprint. It was
  stopped at 32% system memory free with 3.713 of 4.096 GB swap used. A
  reference-collection probe records 86,334 requests, 32,768 actual
  collections, 32,768 pointer-unique `ClassInfo` objects, and zero repeated
  collections, so the multiplier is not repeated collection of one
  specialization. Allocator stack logging attributes the largest retained
  family to substituted AST vectors. A census at 8,192 collected references
  accounts for 93.1 MB of `Type`, 45.5 MB of `Scope`, 33.7 MB of `ClassInfo`,
  31.6 MB of `FunctionBinding`, and smaller owners, but omits several embedded
  AST graphs. Exact string redundancy is only 12.4 MB. Clone-once and
  copy-on-change experiments both pass the focused direct report `862/862`,
  yet their exact RSS trajectories match the baseline; both are reverted.
  Broad base-object splitting, string interning, and another cache are
  therefore unsupported by the measurements. The next pass will count the
  omitted AST owners, then change only the dominant retained representation.
  The late linear source-owner scan is a possible time improvement after
  memory is controlled, but it cannot explain the pre-output footprint.

- `2026-07-24`: Packaged the reference-field `decltype` handoff and returned to
  the MSM frontier. Exact monitored `CompositeMachine` and `Entries` replays
  compile, link, run, and pass. A five-line PA21 C++11 reducer captures the next
  `CompositeEuml` defect: reference-only class collection omitted ordinary
  fields, and class-scope `decltype` analysis required an evaluated implicit
  object. The typed fix records the field and analyzes it under a scoped
  unevaluated operand. The first broad run caught a layout interaction in PA26
  and PA32 because a reference-only field has no final offset. Member-pointer
  template argument encoding now completes the owner and refreshes the binding
  before reading that offset; both owners pass without LowIR reference changes.
  Clang's `-O0` statistics record 192,290 class-template-specialization
  declarations and 534,800 template-specialization types for the heavy TU.
  cppgm's allocator sample spreads 1.46 GB across seven million small
  allocations with 2--4% fragmentation. Those measurements found no extra
  specialization branch or narrow retained cache in the sampled interval.
  Closing the remaining multiplier needs a semantic-object representation
  project. The direct report passes `4091/4091`; strict, cache parity,
  placement, all 23 zero-reparse categories, 31 audit/performance tests, host
  controls, and Clang/GCC warning builds pass. The frozen compiler-and-51-header
  gate records -24.06% instructions, -21.32% RSS, and -30.28% footprint, with
  zero swaps and no parent or live-header measurement. The next action is an
  exact monitored `CompositeEuml` replay.

- `2026-07-24`: Closed the last planned MSM algorithm audit and returned to the
  frontier. The exact `CompositeMachine` failure was a typed current-injected
  specialization identity error, not `reverse_fold` runaway: unpatched HEAD
  rejects the TU in 12.19s at 449,990,656 B RSS, while the isolated structured
  fix passes that point. Clang's filtered AST contains 483
  `reverse_fold_impl` specialization declarations versus cppgm's 427 creations
  at a comparable point, and both compilers spend their breadth in the same
  dominant MPL/Fusion template families. The remaining time/RSS multiple is
  therefore representation and per-node cost unless a later frontier failure
  supplies contrary evidence; no cache or speculative lookup change was kept.
  The PA9-excluded direct report passes `4090/4090`, configured strict LowIR
  comparisons and zero-reparse/audit controls pass, and Clang/GCC warning
  builds are clean. The three-run immutable frozen-source/51-header gate records
  -24.06% instructions, -21.29% RSS, and -28.57% footprint. The next action is
  a complete monitored `CompositeMachine` replay.

- `2026-07-24`: Packaged the follow-on structured-template fixes after a fresh
  correctness, performance, and frontier audit. Two minimal PA21 C++11
  reducers are accepted by Clang 22 and rejected by both controlled pre-fix
  compilers. Exact `CompositeEuml` advances past its former guarded
  `previous_song[True_()]` result into the second transition table, where
  `Playing` lookup is the next independent failure. A fresh `CompositeMachine`
  replay still fails the older concrete hierarchical owner lookup, so that
  frontier remains open and the smaller alias reducer is not presented as its
  complete reduction. An owner-first member-pointer experiment was rejected
  after the full report exposed two PA26 SFINAE regressions; reverting it
  restored PA26 `82/82` and the complete direct-text report to `4101/4101`.
  All five strict suites, placement, all 23 zero-reparse categories, 31
  audit/performance unit tests, `git diff --check`, and Clang/GCC warning builds
  pass. The three-run immutable frozen-source/51-header gate records -24.30%
  instructions, -21.11% RSS, and -30.09% footprint. Its +1.42% rolling
  instruction movement triggered the required hotspot/sample audit: the new
  structural deduction routine has no five-sample self entry, the visited-set
  walk appears in only two samples, and syntax reconstruction in 11 of 7,787;
  the dominant frames remain existing alias expansion and AST substitution.
  No speculative cache or further normal-path optimization is justified before
  returning to the semantic frontier.

- `2026-07-24`: Packaged the `AnonymousEuml` algorithmic and memory work after
  one final instrumented pass. With exact B2 `-O0` inputs, Clang 22 compiles the
  translation unit in 6.94s at 399,683,584 B RSS and 23,330,508,818
  instructions. The final cppgm++ compile takes 88.19s at 2,003,734,528 B RSS
  and 354,594,916,944 instructions, down from the earlier 6.2 GiB diagnostic;
  its object is byte-identical before and after the last no-witness
  optimization. The remaining gap is not excess template-expansion breadth:
  Clang's AST records 51,003 class-template-specialization declarations, while
  cppgm++ creates 36,809 class specializations and receives only 10,029
  class-template reference requests. cppgm++ instead performs 6,092,656
  type-to-class-info queries, including 4,517,980 while rescanning 160,627
  emitted nodes over eight instantiated-output fixpoint iterations. A stack
  sample exposed one avoidable part of that work: disabled witness lifecycle
  hooks still rendered nested class identities, recursively walking defaulted
  arguments and mangle structure before discarding the event. Gating those
  hooks on an active witness session removes 2.02% of exact-TU instructions
  without changing the object. The remaining repeated class-info traffic is
  now explicitly identified as fixpoint/model lookup overhead rather than a
  pathological specialization explosion; replacing it safely requires a
  separate worklist/generation design, not a cache keyed by rendered aliases.
  The final strict direct-LowIR suites pass 959/959 and the complete direct-text
  report passes 4099/4099. The 16 updated LowIR references were audited rather
  than accepted as arbitrary churn: they reflect typed CV/member-pointer name
  normalization, removal of unused eager-instantiation ordinal suffixes, two
  Clang-confirmed ABI corrections (covarying owner-pack mangling and direct
  two-pointer aggregate return), and byte-identical lazy first-use ordering.
  The PA27 virtual-base reducer is intentionally reduced from 30 source lines
  and 248 reference lines to 10 and 138; the larger remaining PA27 refs come
  from sources of only 4--22 lines and are intrinsic vtable/RTTI/lifecycle
  output for distinct object-model cases. The frozen performance gate passes at
  -25.36% instructions, -22.41% RSS, and -28.72% footprint with the exact
  51-header closure verified before every run.

- `2026-07-23`: Reopened `AnonymousEuml` as a compile-memory frontier. Its
  successful compile/link/run is functionally useful but a full-TU
  retained-state diagnostic reaches 6,205,792,256 B RSS and 6,833,315,840 B
  footprint, far outside the other MSM units. The retained-state census
  separates breadth from representation: cppgm creates about 35,595 class
  specializations against Clang's 46,526, while a controlled stop at exactly
  28,000 cppgm classes uses 3,330,023,424 B RSS. Replacing only semantic
  identity names at least 4096 bytes reduces that same-count result to
  2,277,285,888 B, a 1,052,737,536 B delta. Whole-name interning barely helps
  because most recursively composed names are unique. Replacing the spellings
  with compact hashes is diagnostic evidence, not a fix: it reduces the
  same-count memory again but a full replay remains active in late
  class-reference lookup, dependency analysis, and specialization reset churn.
  Relaxing the exact named-key guard during that probe caused five focused
  PA19/PA21/PA23 ABI/materialization regressions; restoring the guard returns
  the focused report to 1585/1585. A direct `Type`-to-`ClassInfo` backlink is
  also insufficient because identical spelling can refer to contextually
  rematerialized owners. Both `Type *` and `ClassInfo *` therefore require a
  binding-environment snapshot before they can participate in durable
  identity. The production direction is a collision-safe immutable identity
  containing the declaration, typed arguments, and binding environment or
  generation, with textual names rendered only at ABI/output boundaries. No
  identity cache or hash substitution is retained from this investigation.

- `2026-07-22`: Closed the apparent repeated-alias MSM frontier without a
  cache. The repeated work was not thousands of legitimate resolutions of one
  hot alias: leaf-only class-template keys collapsed MPL's `end` and Fusion's
  `end`, reused the MPL helper for Fusion, and attempted index 4 of a
  four-element vector. Nested `insert_range`/`as_vector` failure propagation
  then doubled the same invalid retry chain; one exact
  `value_at_impl<..., mpl_::int_<4>>` accounts for 3,494 of 3,771 reset traces.
  Collision-local structured namespace identity separates those entities while
  preserving established non-colliding keys and output. A second independent
  eager-completion defect instantiated an incomplete class reached only through
  a cast pointer and reference ABI parameter; reference-only cast parsing,
  identical-pointer short-circuiting, and indirect-parameter metadata deferral
  remove that work. The 9-line and 5-line PA21 reducers are header-free C++11
  and use no `<type_traits>`. The exact MSM source now advances through Fusion
  indices 0--3 to `for_each_impl<true>`. A 107.66-second bounded normal replay
  peaks at 979,886,080 B RSS with zero process swaps and remains active in a
  later deep reference-member expansion; that is the next distinct frontier,
  not the repaired end-iterator retry. Configured strict suites pass; the
  PA9-excluded broad report passes 4,084/4,085 before its sole PA24 order-only
  LowIR reference is refreshed, after which PA24 passes 115/115 direct. The
  frozen candidate-only gate records -10.70% instructions, -10.65% RSS, and
  -11.58% footprint and verifies all 51 frozen headers before every run.

- `2026-07-22`: Advanced the initial MSM intake through four independent C++11
  semantic fronts without adding a cache. Context-aware lookup now finds an
  inherited enclosing member template and retains a dependent qualified member
  template-template argument; deleting-destructor overload analysis supplies
  its already known `void *` argument type instead of bringing the complete
  destroyed class into ADL; the parser recognizes a qualified return followed
  by an actual parenthesized member-pointer declarator; and target-aware
  member-pointer initialization now deduces member-function-template
  specializations. Minimal header-free owners live at PA10, PA21, and PA26. The
  first parser predicate was too broad and broke the frozen self-compile at
  libc++'s `std::__destroy_at(...)`; it was replaced with a structural scan for
  the `qualified-owner::*identifier` declarator itself. A proposed non-type
  template-argument terminal-failure change was also discarded after it broke
  the existing PA23 dependent-bool strict case. Focused owners and all
  configured PA18/PA19/PA21/PA23 strict comparisons pass. The PA9-excluded
  broad run passes every test except the intentionally red next-frontier PA22
  reducer and one pre-existing nondeterministic PA33 RTTI-mangling SIGSEGV. The
  PA33 test passes fresh and in a complete replay; repeated invocations
  reproduce the same dangling-scope crash with both the current compiler and
  the saved pre-fix debug compiler, so it is not attributed to this patch set.
  The three-run frozen candidate gate records -11.08% instructions, -10.42%
  RSS, and -11.38% footprint. A bounded exact MSM replay reaches the next
  distinct cause: MPL's `end` and Fusion's `end` template-template arguments
  collapse to the same leaf-only specialization/reference key. The wrong MPL
  helper is reused for Fusion, producing 3,494 invalid end-iterator resets
  among 3,771 resolutions. This is an entity-identity collision, not evidence
  for a failure cache; no cache change has been made. The suite remains open at
  a collision-driven qualified identity fix.

- `2026-07-22`: Classified suite `#70 libs/mqtt5/test` as
  `skipped-language`. Boost 1.91 declares C++17 in
  `libs/mqtt5/meta/libraries.json`, beyond CPPGM's current C++11 source lane.
  No B2 graph was run and no compiler behavior is inferred from the historical
  configuration-only result. The cursor advances to C++03-declared
  `#71 libs/msm/test`.

- `2026-07-22`: Closed suite `#69 libs/mpl/test`. Full-source compilation now
  enables ordinary narrow multicharacter integer literals while the direct PA2
  post-token contract stays strict, and qualified static arrays now decay to a
  pointer non-type template argument while preserving the static symbol for
  instantiated-body lowering. The owners are 3 lines / 94 bytes at PA19 and
  5 lines / 273 bytes at PA22, both header-free C++11 and neither using
  `<type_traits>`. Exact `string` and the final forced two-job MPL graph pass;
  the latter updates all 220 requested targets, records 99 passing test
  actions, peaks at 447,705,088 B RSS, and records zero swaps. PA19/PA22,
  strict, PA9-excluded broad `4079/4079`, placement, cache parity, all 23
  zero-reparse categories, all 31 audit/performance tests, host controls, and
  Clang/GCC warning builds pass. The earlier nested-alias reducer is also
  minimized from 27 lines / 898 bytes to 16 lines / 706 bytes without losing
  its ten-level trigger. A fresh hotspot run explains its repeated queries:
  with the long-standing template-argument cache disabled, structured lookup
  and class-reference/member-alias collection each replay the inner prefix,
  yielding exact 2048, 1024, 512, ... counts. With the cache enabled, the same
  reducer needs only 44 resolver calls; therefore no new cache change is made.
  The final candidate-only immutable compiler/source/51-header gate passes at
  -11.16% instructions, -10.72% RSS, and -11.37% footprint. All three fresh
  processes release between runs, record zero swaps, and system swap remains
  unchanged at 551.75 MiB.

- `2026-07-22`: Classified suite `#68 libs/mpi/test` as
  `blocked-external`. Its exact forced two-job graph finds one bookkeeping
  target and exits successfully in 3.00s at 39,317,504 B maximum RSS with zero
  swaps, but performs no compile, link, or runtime action. Boost.MPI is in the
  supported C++03 lane; however, its Jamfile creates tests only when
  `mpi.configured` is true, and this host has neither `mpic++` nor `mpirun`.
  The empty graph therefore cannot be credited as a compiler pass. System swap
  remains unchanged at 551.75 MB. No compiler or test change is required, and
  the cursor advances to `#69 libs/mpl/test`.

- `2026-07-22`: Closed MP11 `mp_partial_sum` and credited suite `#67` as a
  pass. Defaulted member-alias arguments now preserve every template binding
  owned by the alias declaration while overlaying non-conflicting caller
  bindings at all four structured default-argument paths. This prevents a
  caller's same-named template-template parameter from rebinding the alias's
  declaration-owned `F` and recursively expanding the alias. The owner is a
  10-line / 427-byte header-free C++11 PA21:300 reducer with no `<type_traits>`;
  a saved pre-fix compiler exceeds an 8-second bound, while Clang 22 and GCC 15
  accept it warning-clean. Exact `mp_partial_sum` passes compile/link/runtime in
  14.28s at 272,891,904 B RSS with zero swaps. The final forced two-job MP11
  graph updates all 922 requested targets with no failure or skip. Its
  1,634,521,088 B peak is the finite `mp_fold` size-0-through-37 stress unit;
  live samples show active structured expansion, memory releases afterward,
  and swap remains unchanged. Strict, broad `4077/4077`, cache, placement,
  warning, zero-reparse, and audit gates pass. The frozen compiler/source/51-
  header gate records -10.91% instructions, -10.47% RSS, and -11.42% footprint
  without measuring a parent compiler or live header. The cursor advances to
  `#68 libs/mpi/test`, whose exact configuration intake is pending on a host
  without `mpic++` or `mpirun`.

- `2026-07-22`: Closed MP11 `tuple_transform` and `tuple_transform_2` with
  two 5-/4-line PA19 pack-expansion owners and one 4-line PA22 function-template
  deduction owner. All are header-free C++11 and use no `<type_traits>`. Pack
  discovery now observes only the current expansion layer, nested expansion AST
  survives until its own layer, and defaulted non-type arguments fill only after
  deduction. Saved pre-fix compilers prove all three failures. The exact pair
  passes compile/link/runtime in 28.24s at 349,736,960 B RSS with zero swaps;
  cache parity, placement, Clang/GCC controls, warning, strict, zero-reparse, and
  audit gates pass, as does the PA9-excluded `4076/4076` broad report. The frozen
  compiler/source/51-header gate records -11.12% instructions, -10.49% RSS, and
  -11.41% footprint without measuring a parent or live header. All ordinary MP11
  failures are closed; `mp_partial_sum` is the sole remaining suite frontier.

- `2026-07-22`: Closed MP11 `mp_set_difference` and `mp_set_intersection`
  with 10-line and 8-line header-free C++11 PA21 owners. An empty structured
  class specialization is now materialized before stale class-info cache state
  can win, and retained member-alias source arguments expand in their original
  use scope rather than a same-named callee pack's scope. Saved intermediate
  compilers prove the two causes independently. The exact two-target forced
  replay passes in 12.19s at 247,087,104 B maximum RSS with zero swaps. PA21
  passes `247/247`; configured strict suites, all cache modes, all 23
  zero-reparse categories, 14 audit tests, Clang/GCC controls, placement, and
  the PA9-excluded `4073/4073` broad report pass. The frozen 51-header gate
  records -11.12% instructions, -10.61% RSS, and -11.45% footprint without
  measuring a parent compiler or live header. The two tuple-transform failures
  reproduce independently after these fixes and remain the ordinary frontier.

- `2026-07-22`: Closed MP11 `construct_from_tuple`, `tuple_apply`, and
  `tuple_for_each` with one 6-line / 239-byte, header-free C++11 PA21 owner.
  Class-partial deduction now follows a retained identity-alias entity to its
  direct result argument, then resolves the original structured pattern in the
  concrete deduced scope so ignored SFINAE arguments are still validated. The
  existing PA22 detected-or fallback control confirms invalid ignored
  arguments still reject the partial. The forced three-target replay passes in
  17.28s at 242,778,112 B maximum RSS with zero swaps. PA21/PA22 pass
  `549/549`; configured strict suites and the PA9-excluded `4071/4071` report
  pass; cache parity, Clang/GCC controls, all 23 zero-reparse categories, 23
  audit tests, and production warning builds pass. The immutable
  compiler/source/51-header gate records -11.19% instructions, -10.56% RSS,
  and -11.37% footprint with full release between all three processes and
  unchanged system swap. Four ordinary MP11 targets remain before the separate
  `mp_partial_sum` pathology.

- `2026-07-22`: Closed exact MP11 `mp_compose_sf` with two independently
  causal, header-free C++11 reducers: 11 lines at PA21's partial-specialization
  owner and 12 lines at PA23's member-alias/template-template integration
  owner. The typed fix binds the selected partial specialization's parameters
  and arguments into its member-alias scope, and preserves an already-bound
  concrete member-template head while substituting display syntax through an
  alias with a trailing pack. The exact forced target compiles, links, runs,
  and passes in 5.63s at 76,201,984 B maximum RSS with zero swaps. PA21/PA23
  direct-text tests pass `653/653`; both strict suites, all ten cache-disabled
  modes, Clang/GCC C++11 controls, all 23 zero-reparse categories, and 23 audit
  tests pass. The frozen compiler/source/header epoch is unchanged; its
  candidate performance replay remains part of the next MP11 closure gate.
  Seven ordinary targets remain to replay, while `mp_partial_sum` stays the
  separately bounded pathological expansion.

- `2026-07-22`: Closed exact MP11 `mp_compose` with a 6-line / 243-byte
  header-free C++11 PA23 reducer. Each bound template-template pack element now
  retains its own structured identity during alias expansion, so `X...` no
  longer repeats its first template. The forced two-job Boost target compiles,
  links, runs, and passes at 234,467,328 B RSS with zero swaps. A transient
  frozen-workload failure exposed an independent raw-pointer lifetime defect in
  the nested dependent-type cache; its input key now retains a `TypePtr` until
  the outermost query clears the cache. The final 21-line PA22 reducer uses no
  hosted headers, fails 43/256 times with the saved pre-fix compiler, and passes
  48/48 times after the fix with byte-identical cache-on/off output. PA22 passes
  `304/304`; configured strict comparisons, all 23 zero-reparse categories, and
  31 audit/performance unit tests pass; the final PA22/PA23 direct report is
  `712/712`, PA23 strict comparisons pass `338/338`, and the final PA9-excluded
  broad report passes `4068/4068`, including PA37 object roundtrip `7/7`. The
  immutable frozen-source-and-51-header gate
  completes three fresh processes and improves instructions by 11.08%, RSS by
  10.45%, and footprint by 11.43%, with zero process swaps and no parent or live
  header measurement. `mp_compose_sf` is the next independent ordinary
  frontier; `mp_partial_sum` remains separately bounded.

- `2026-07-22`: Closed both MP11 reverse-fold targets with a narrowly structured
  class-partial-ordering rule. The regression is 8 nonblank header-free C++11
  lines in PA21 and uses no `<type_traits>`. The saved pre-fix compiler fails it
  with the original ambiguity, while cppgm++, Clang 22, PA21 `243/243`, strict
  PA18/19/21/22, zero-reparse/audit checks, production warning controls, and
  focused two-job B2 replays pass. The final frozen candidate gate passes twice,
  retaining -11.27% instructions, -10.64% RSS, and -11.36% footprint; neither
  parent nor live headers were measured. A transient benchmark build exposed a
  pre-existing missing-libc++-function flake while diagnostic instrumentation
  confirmed the new comparator branch was never entered by that workload; the
  final uninstrumented binary then completed two consecutive gates. The ordered
  compiler frontier advances to `mp_compose`; `mp_partial_sum` remains the
  separately bounded pathological expansion.

- `2026-07-22`: Closed `mp_fold_q` with a 16-line header-free C++11
  reducer. The failure was a dependency-state collision in the class-template
  reference cache, not a need for `<type_traits>` or newer language syntax.
  Placement review puts the essential alias/member-alias/template-template
  composition in PA23:400, leaving PA22 header-free. The exact Boost target,
  PA22 `303/303`, configured strict comparisons, zero-reparse/audit checks,
  warning controls, and the frozen candidate-only gate pass. The gate retains
  the instruction win and recovered memory at -11.30% instructions, -10.65%
  RSS, and -11.45% footprint. The two-job MP11 inventory now advances to the
  independent `mp_reverse_fold` ambiguity; `mp_partial_sum` remains a bounded
  pathological-expansion lead rather than a leak.

- `2026-07-21`: Closed the exact MP11 `mp_fold` frontier while improving both
  sides of the requested performance tradeoff. Exact direct template-id
  ambiguities are compacted during substitution, restored only at a real pack
  expansion, and identical dependent-type work is memoized for one outer query.
  The 27-line / 898-byte PA22 owner, PA22/PA23 `703/703`, all configured strict
  comparisons, all 23 zero-reparse categories, Clang/GCC controls, and exact
  `mp_fold` compile/link/runtime pass. The immutable frozen-source-and-51-header
  gate improves to -11.15% instructions, -10.33% RSS, and -11.38% footprint;
  no parent compiler or live header was measured. The exact serialized MP11
  intake records 185 passes from 234 declared tests. Its earliest ordinary
  frontier is the `mp_list_c` non-type pack-element reuse cluster. The separate
  `mp_partial_sum` compile was sampled twice and stopped after 15m13s: its stack
  remains in recursive structured alias-template expansion, CPU remains active,
  RSS plateaus near 345 MB, and swap does not grow. This is pathological
  expansion rather than a RAM leak or deadlock, so the suite remains open.

- `2026-07-21`: Closed Boost.Move and advanced the ordered cursor to suite 67,
  `libs/mp11/test`, which declares C++11 and remains in the supported language
  lane. The initial exact serialized Move graph found 882 targets, updated 109
  of 121 requested targets, failed four, and skipped eight. Three compact
  C++11 owners repair deleted-copy triviality, non-class member-pointer SFINAE,
  and the retained `type_name(&value)` statement ambiguity that had turned
  `mach_timebase_info(&info)` into a declaration and crashed both benchmarks.
  The final exact graph updates all 121 targets and passes in 215.65s at
  515,858,432 B maximum RSS with zero swaps. PA26/PA34 pass `408/408`; all
  configured strict suites and the PA9-excluded `4054/4054` broad report pass.
  Twelve cache configurations for each reducer are byte-identical; placement,
  hygiene, all 23 zero-reparse categories, 28 audit/perf unit tests, Clang/GCC
  warning builds, and diff hygiene are clean. The long broad and Boost runs
  retain only a tiny batch parent, release every compiler child's working set,
  and leave system swap unchanged at 208.75 MiB. The immutable frozen-source
  and 51-header gate preserves the main performance result: -4.23%
  instructions, -8.30% peak RSS, and -9.04% peak footprint. No parent compiler
  or live project header was measured.

- `2026-07-21`: Closed Boost.Metaparse and advanced the ordered cursor to
  suite 66, `libs/move/test`. The final exact serialized graph finds 3623
  targets, updates all 241 requested targets, passes every positive action,
  handles `too_long_string` as failed-as-expected, and exits successfully in
  910.13s. Its three C++11 owners are deliberately small: a seven-core-line
  PA20 dependent `T()` constexpr test, a six-core-line PA21 nested member class
  specialization, and a 20-line / 534-byte PA18 three-alias progression that
  isolates the compile-time failure. The last case was CPU explosion rather
  than a leak: a 13-alias controlled chain exceeded 60 seconds while holding
  about 6.5 MiB RSS because reference collection eagerly repeated predecessor
  work. Structural AST deferral reduces it to 0.02--0.03s and lets the exact
  MPL string header compile in 3.20s at about 94 MiB. The full suite's isolated
  1.16 GiB peak is consistent with the deliberately extreme 128-argument
  expected-failure unit;
  ordinary children release their working sets and swap remains unchanged at
  240.75 MiB. PA18 passes `238/238`; configured strict suites, twelve cache
  modes, all 23 zero-reparse categories, 14 audit tests, Clang/GCC warning
  checks, the post-refactor `all_headers`/`string` replay, and the PA9-excluded
  `4051/4051` report pass. The final immutable frozen-source-and-51-header gate
  improves on Logic: -4.34% instructions, -8.42% RSS, and -9.08% footprint.

- `2026-07-21`: Closed Boost.Logic, classified Boost.Math as a language-policy
  skip, and advanced the ordered cursor to suite 65, `libs/metaparse/test`.
  Logic's exact final single-job graph updates all 30 requested targets, passes
  its three positive compile/link/runtime actions, handles all six deliberate
  failures as expected, and exits without a skip. Its C++11 PA20 reducer is 17
  lines / 779 bytes and covers overloaded logical operators plus contextual
  explicit-bool conversion. The final implementation avoids the rejected eager
  whole-expression semantic probe, keeps scalar constexpr paths fast, and
  uses a raw read-only type-wrapper walk for overload eligibility. PA20 passes
  `86/86`; configured strict suites, twelve cache modes, placement/hygiene, all
  23 zero-reparse categories, 14 audit tests, Clang/GCC warning controls, and
  the PA9-excluded `4048/4048` report pass. The final frozen-source-and-51-header
  gate retains a substantial -3.93% instruction result while recovering memory
  to -8.34% RSS and -8.94% footprint. Logic's sampled compiler child is about
  203 MiB RSS, build steps record zero swaps, and system swap remains unchanged
  at 272.75 MiB. Math declares C++14 and explicitly requires C++14 constexpr,
  `decltype(auto)`, generic lambdas, return-type deduction, and variable
  templates, so it is recorded as `skipped-language` without running a C++11
  graph. Metaparse declares C++03 and remains in the supported C++11 lane.

- `2026-07-21`: Closed Boost.Log and advanced the ordered cursor to suite 63,
  `libs/logic/test`. The exact final forced C++11 graph was deliberately
  serialized after the prior host ran out of RAM: it found 26689 targets,
  updated all 549 requested targets, performed 281 C++ compiles, one C compile,
  14 archives, 125 links, and 54 captured runtime actions, recorded 59 passing
  test actions plus one deliberate compile failure as failed-as-expected, and
  exited with no failure or skip. All former runtime families, duplicate-Asio
  links, `tutorial_filtering`, `tutorial_attributes`, and
  `extension_app_launcher` pass in the same authoritative graph. Heavy Spirit,
  Phoenix, formatter, and example translation units sampled up to about
  1.74 GiB RSS, but every compiler child released its working set at exit and
  system memory stayed 71--72% free. Docker has no active VM workload; only its
  tiny privileged helper and sandbox daemon remain. PA22 passes `294/294`, all
  configured strict suites pass, the PA9-excluded broad report passes
  `4047/4047`, cache parity is byte-identical, all 23 text-reparse categories
  and 14 audit tests pass, and Clang 22/GCC 15 warning controls are clean. The
  final candidate-only frozen source-and-51-header gate records -4.41%
  instructions, -8.47% RSS, and -9.48% footprint against immutable epoch
  `9764b3835`; no parent or live project header was measured. The PA22
  function-pointer inner-pack reducer is 16 lines / 392 bytes, and the PA27
  large-reference audit leaves only the irreducible recursive-VTT and diamond
  virtual-destructor lifecycle owners above 10 KiB. Logic declares C++03, so it
  remains in the supported C++11 lane and will run rather than be skipped.

- `2026-07-20`: Closed the Phoenix callable-result frontier while retaining the
  Lockfree performance win. Concrete class-template owners are no longer
  classified as dependent merely because nested member templates leave
  lexical placeholder maps in scope, and bound packs are expanded structurally
  before class member function types and non-trailing parameter packs are
  parsed. Exact `filter_parser.cpp` now compiles at 1.083 GB RSS with zero swap.
  A two-job incremental Log survey peaks at 3.21 GB aggregate compiler RSS,
  turns memory over between translation units, leaves swap fixed at 98 MB, and
  advances to the independent `attribute_set.cpp` procedural-LowIR frontier.
  Strict suites, the PA9-excluded `4033/4033` report, placement, all 23 zero-
  reparse categories, 14 audit tests, cache-disabled parity, and Clang/GCC 15
  controls pass. The final three-run immutable 51-header gate records -4.58%
  instructions, -0.47% RSS, and +2.31% footprint. The requested PA27 reducer
  audit also cuts total `.ref` bytes from 205686 to 163997; PA27 passes `36/36`
  and all five edited reducers execute under Clang and GCC in C++11 mode. The
  remaining two refs above 10 KB are the minimal recursive-VTT lifecycle and
  explicit diamond virtual-destructor slot-merging owners.

- `2026-07-20`: Completed the first Log closure without giving back the
  Lockfree instruction win. The initial exact four-job graph failed 79 targets
  and reported a 2.47 GB maximum child RSS with zero swaps. Three minimal fixes
  close the shared sized-delete, qualified-template-id parser, and inherited
  allocation-call failures. In particular, synthetic class-specific
  `operator new` calls now retain the resolved nested class-template qualifier
  chain instead of rebuilding it from display text. A broad first version was
  rejected when strict PA22 comparison exposed unnecessary constexpr-member
  storage; the final resolver trusts only explicitly marked synthetic
  qualifiers. Exact repaired Log compiler children use about 396--510 MB RSS,
  while the next independent Phoenix `filter_parser.cpp` frontier uses 962 MB,
  all with zero swaps. Configured strict suites pass, the PA9-excluded report
  passes `4033/4033`, all 23 reparse categories and 14 audit tests remain clean,
  and Clang/GCC 15 warning checks pass. The new PA18 reducer has no placement or
  hygiene finding; PA18 retains one unrelated pre-existing finding. The final
  candidate-only immutable 51-header gate passes at -5.51% instructions,
  -0.50% RSS, and +2.36% footprint. No parent compiler or live project header
  was measured. The ordered cursor remains on Log at the Phoenix callable-result
  frontier.

- `2026-07-20`: Accepted the general memory closure found during Lockfree and
  cleared the repository and frozen-header gates before starting Log. Large
  class arrays now use counted construction/destruction loops, LowIR/object
  transformations avoid repeated whole-program copies, and host-EH successor
  lookup builds its label index once. The Lockfree stress source falls from
  about 7.3 GiB to about 635 MiB RSS with zero swap. The initial immutable
  candidate improved instructions by 5.62% and passed RSS, but failed peak
  footprint at +4.01%. The retained-memory census showed that the unbounded
  template-argument backing map held 17,212 entries while its bounded 512-entry
  fast ring supplied 17,062 of 17,378 hits. Bounding the backing map at 4096
  entries preserves the fast ring and yields final candidate-only medians of
  -5.35% instructions, -0.39% RSS, and +2.34% footprint. Focused owners pass
  `654/654`, PA22/PA26 pass `372/372`, configured strict suites pass, and the
  PA9-excluded broad report passes `4028/4028`, including PA37 `7/7`.
  Placement, cache-parity, all 23 zero-reparse categories, audit tests, Clang
  and GCC 15 warning controls, and `git diff --check` pass. No parent compiler
  or live project header was measured.

- `2026-07-20`: Classified Lockfree as `skipped-language` and advanced the
  ordered cursor to suite 62, `libs/log/test`. Boost 1.91 explicitly declares
  C++14 as Lockfree's minimum language version. Its Jamfile does not gate the
  C++11 graph, so the exact run attempts C++14-only `std::conditional_t` and
  `std::make_unique` sources and fails 14 compile updates. CPPGM remains a
  C++11 source-language compiler; hosted compatibility for modern libc++
  internals does not make arbitrary C++14 library sources part of the lane.
  The diagnostic C++14 replay therefore earns no frontier credit. Its memory
  investigation remains useful independently: compact class-array lifecycle
  lowering reduces the pathological stress source from about 7.3 GiB to about
  635 MiB RSS with zero swap. The repository and frozen-header candidate gates
  for that general compiler fix remain pending.

- `2026-07-20`: Closed Locale after seven typed semantic, encoding, and hosted-
  facet fixes and advanced the ordered cursor to suite 61,
  `libs/lockfree/test`. The initial exact graph failed 26 compile updates; the
  second graph exposed the remaining overload, array, and runtime frontiers.
  The final exact four-job graph finds 4618 targets, updates all 173 requested
  targets, records 28 passing test actions, and exits with no failure or skip.
  The six affected owners pass `978/978`, all configured strict suites pass,
  and the explicit PA9-excluded broad report passes `4019/4019`, including
  PA37 `7/7`. Placement/hygiene, twelve cache configurations for all five
  semantic reducers, all 23 zero-reparse categories, 14 audit tests, host
  controls, and Clang/GCC 15 warning checks are clean. The candidate-only
  immutable 51-header performance gate passes at -1.78% instructions, +2.51%
  RSS, and +2.54% footprint. No parent compiler or live project header was
  measured.

- `2026-07-20`: Closed Local Function after three structured semantic fixes and
  advanced the ordered cursor to suite 60, `libs/locale/test`. The initial exact
  graph failed 32 updates across macro-retained function-template visibility,
  the repeated local declaration/expression probe, and a qualified-return local
  function-pointer declaration. The final exact four-job forced graph finds
  1875 targets, updates all 314 requested targets, records 80 passing test
  actions, handles all six deliberate failures as expected, and exits with no
  unexpected failure or skip. PA12/PA22/PA24 pass `532/532`, all configured
  strict suites pass, and the PA9-excluded broad report passes `4012/4012`,
  including PA37 `7/7`. Placement/hygiene, twelve cache configurations per
  reducer, all 23 zero-reparse categories, 14 audit tests, Clang controls, and
  GCC 15 production-unit checks are clean. The candidate-only immutable
  51-header performance gate passes at -1.84% instructions, +2.23% RSS, and
  +2.50% footprint. No parent compiler or live project header was measured.

- `2026-07-20`: Closed Lexical Cast after one initial-parser fix and advanced
  the ordered cursor to suite 59, `libs/local_function/test`. The initial exact
  graph misparsed `(std::numeric_limits<T>::epsilon()) * 2` in both
  `lexical_cast_test` and `float_types_test`; the final exact four-job forced
  graph finds 4096 targets, updates all 158 requested targets, records 30
  passing test actions, handles both deliberate failures as expected, and
  exits successfully with no skip or failed update. PA10 passes `137/137`, the
  configured strict suites and PA9-excluded `4009/4009` report pass, and all
  cache, text-reparse/unit-audit, new-test placement, host-control, and warning
  checks are clean. The
  candidate-only immutable 51-header performance gate passes at -2.00%
  instructions, +1.20% RSS, and +2.35% footprint. No parent compiler or live
  project header was measured.

- `2026-07-20`: Classified LEAF as `blocked-external` after closing all nine
  compiler-owned frontiers and advanced the ordered cursor to suite 58,
  `libs/lexical_cast/test`. The final exact four-job forced graph finds 3229
  targets, requests 482 updates, records 122 passes and 22 expected failures,
  and leaves only the positive `nlohmann_json_encoder_test.o` unbuilt because
  this environment does not provide `nlohmann/json.hpp`; its three downstream
  targets are the only skips. Configured strict suites, the full PA9-excluded
  direct-LowIR report (`4008/4008`), placement/hygiene, all 23 zero-reparse
  categories, audit tests, cache-disabled proofs, and GCC 15 warning checks
  pass. The candidate-only immutable 51-header performance gate passes at
  -2.22% instructions, +1.27% RSS, and +2.44% footprint. No parent compiler or
  live project header was measured.

- `2026-07-20`: Closed Lambda without a compiler change and advanced the
  ordered cursor to suite 57, `libs/leaf/test`. The exact four-job forced graph
  finds 1437 targets, updates all 79 requested targets, records 20 passing
  compile/link/runtime actions, and exits successfully with no failure, skip,
  or error marker. No regression, broad repository rerun, or performance
  measurement was required.

- `2026-07-20`: Closed Iterator without a compiler change and advanced the
  ordered cursor to suite 56, `libs/lambda/test`. The exact four-job forced
  graph finds 4492 targets, updates all 175 requested targets, records 46
  passing actions, handles all eight deliberate compile failures as
  failed-as-expected, and exits successfully with no unexpected skip or failed
  update. No regression, broad repository rerun, or performance measurement
  was required.

- `2026-07-20`: Closed Heap without a compiler change and advanced the ordered
  cursor to suite 55, `libs/iterator/test`. The exact four-job forced graph
  finds 3066 targets, updates all 69 requested targets, passes all eight
  compile/link/runtime actions, and exits successfully with no failure, skip,
  or error marker. No regression, broad repository rerun, or performance
  measurement was required.

- `2026-07-20`: Closed Hash2 without a compiler change and advanced the
  ordered cursor to suite 54, `libs/heap/test`. The exact four-job forced graph
  finds 2144 targets, updates all 506 requested targets, records 130 passing
  actions, handles `append_tag_invoke_4` as a deliberate failed-as-expected
  compile test, and exits successfully. No regression, broad repository rerun,
  or performance measurement was required.

- `2026-07-20`: Closed Hana without a compiler change and advanced the ordered
  cursor to suite 53, `libs/hash2/test`. The exact four-job forced graph checks
  C++14 constexpr support, finds the single bookkeeping target, and exits
  successfully. Both C++14 configurations are unavailable in the C++11 lane,
  so no regression, broad repository rerun, or performance measurement was
  required.

- `2026-07-20`: Classified Graph Parallel as `blocked-external` and advanced
  the ordered cursor to suite 52, `libs/hana/test`. B2 stops during target
  selection before invoking cppgm: the Graph Parallel and MPI libraries have
  no selectable alternatives, and the examples cannot resolve
  `/python//python`. The checkout has no `mpic++` or `mpirun`, while the suite
  Jamfiles require configured MPI and Python targets. This matches the V1
  external-setup result and provides no compiler evidence. No compiler change,
  regression, broad repository rerun, or performance measurement was required.

- `2026-07-20`: Closed Graph without a compiler change and advanced the
  ordered cursor to suite 51, `libs/graph_parallel/test`. The initial exact
  forced graph reached its 30-minute wrapper deadline after 163 explicit
  passes with no failure, skip, or error marker. After terminating the four
  compiler processes orphaned by that external deadline, the exact
  continuation updated the remaining 18 targets, recorded six further passes,
  and exited successfully. A final exact rerun found the complete 12547-target
  closure with nothing left to update and exited successfully. No regression,
  broad repository rerun, or performance measurement was required.

- `2026-07-20`: Closed GIL after correcting an upstream Boost 1.91 Jamfile
  collision and advanced the ordered cursor to suite 50, `libs/graph/test`.
  `core/io/Jamfile` declared compile-only and run actions for the same
  `path_spec.cpp` implicit target, producing `No best alternative`; the exact
  duplication is also present in the Boost 1.91 tag and current GIL `develop`.
  Removing the redundant compile-only action retains the stronger run action.
  The corrected C++11 graph completes every configuration check, finds only its
  bookkeeping target because C++14 constexpr is unavailable, and exits
  successfully. No compiler change, regression update, broad rerun, or
  performance measurement was required.

- `2026-07-20`: Closed Geometry without a compiler change and advanced the
  ordered cursor to suite 49, `libs/gil/test`. The exact four-job graph
  completes its configuration checks, finds the single bookkeeping target,
  and exits successfully. Both C++14 constexpr configurations are unavailable
  in the C++11 lane, so no regression, broad repository rerun, or performance
  measurement was required.

- `2026-07-20`: Closed Fusion and advanced the ordered cursor to suite 48,
  `libs/geometry/test`. The initial exact graph exposed an obsolete duplicated
  `make_pair_r-value` target in the external Boost Jamfile and the first causal
  compiler failure in `fold`. The external metadata now matches the guarded
  positive upstream target, and its locally malformed source fixture was also
  synchronized to current upstream after host Clang independently rejected it.
  Two typed fixes retain qualified static-value expression packs during the
  initial parse and stop concrete semantic result nodes from being relabeled
  dependent by a textual unresolved-identifier fallback. No semantic text is
  reparsed and no cache or ABI path is added. Both PA22 reducers are identical
  in all twelve cache configurations; PA22 `286/286`, strict, placement,
  reparse, 22 audit/perf unit tests, Clang/GCC, warning, and PA9-excluded
  `3999/3999` gates pass. The final exact graph updates all 844 requested
  targets, records 220 passing test actions, and exits successfully. The
  candidate-only frozen performance gate passes at -1.49% instructions,
  +0.84% RSS, and +2.23% footprint after verifying the exact 51-header closure;
  no parent or live-header measurement was run.

- `2026-07-19`: Closed Functional without a compiler change and advanced the
  ordered cursor to suite 47, `libs/fusion/test`. The exact four-job forced
  graph finds 174 targets, updates all four requested targets, compiles, links,
  and runs `function_test`, and exits successfully. No regression, broad
  repository rerun, or performance measurement was required; the immutable
  Function compiler-and-51-header checkpoint remains current.

- `2026-07-19`: Closed Function Types without a compiler change and advanced
  the ordered cursor to suite 46, `libs/functional/test`. The exact four-job
  forced graph finds 2580 targets, updates all 89 requested targets, records 43
  passing test actions, handles all three negative decomposition tests as
  failed-as-expected, and exits successfully. No regression, broad repository
  rerun, or performance measurement was required; the immutable Function
  compiler-and-51-header checkpoint remains current.

- `2026-07-19`: Closed Function and advanced the ordered cursor to suite 45,
  `libs/function_types/test`. The initial exact four-job graph finds 1350
  targets and exposes one causal compile failure: class-scope placement
  `operator new` is stored as an ordinary method when its redundant `static`
  specifier is omitted. The shared class-function classifier now applies the
  language-mandated static fact to ordinary, reference, dependent, definition,
  template, and output paths. The existing header-free PA16 reducer now omits
  `static` and retains byte-identical LowIR in all twelve cache modes. PA16,
  strict, placement, reparse, audit/perf, Clang/GCC, warning, and PA9-excluded
  `3997/3997` gates pass. The final exact graph updates all 214 requested
  targets and exits successfully. Candidate-only frozen performance passes at
  -1.26% instructions, +1.71% RSS, and +2.23% footprint. The gate verifies
  epoch `9764b3835`, the exact frozen source, all 51 frozen headers, and closure
  hash `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`;
  no parent or live-header measurement was run.

- `2026-07-19`: Closed Format without a compiler change and advanced the
  ordered cursor to suite 44, `libs/function/test`. The exact four-job forced
  graph reports ICU unavailable for this lane, finds 1360 targets, updates all
  43 requested targets, records nine passing compile/link/runtime actions, and
  exits successfully in 46s. No regression, broad repository rerun, or
  performance measurement was required.

- `2026-07-19`: Closed Foreach without a compiler change and advanced the
  ordered cursor to suite 43, `libs/format/test`. The exact four-job forced
  graph finds 875 targets, updates all 98 requested targets, records 25 passing
  compile/link/runtime actions, and exits successfully in 55s. No regression,
  broad repository rerun, or performance measurement was required; the
  immutable Flyweight compiler-and-51-header checkpoint remains current.

- `2026-07-19`: Closed Flyweight and advanced the ordered cursor to suite 42,
  `libs/foreach/test`. The initial exact four-job graph finds 3307 targets and
  three causal compile failures: `test_concurrent_factory` exhausts the stack
  in typed initializer constant evaluation, `test_intermod_holder` loses a
  qualified recursive `pointer_traits::to_raw_pointer` call during that
  semantic closure, and `test_multictor` reports an ambiguous initializer-list
  assignment. A `(node, normalized target type)` no-progress guard terminates
  the exact constant-evaluation cycle, and typed per-element conversion ranks
  order competing initializer-list overloads. The two header-free PA20/PA25
  reducers pass all twelve cache configurations byte-identically; owner,
  strict, placement, reparse, audit/perf, Clang/GCC, and warning gates pass.
  The PA9-excluded broad report passes `3997/3997`, including PA37 `7/7`.
  The final exact graph updates all 111 requested targets, records 11 passing
  actions, and exits successfully in 191s. Candidate-only frozen performance
  passes at -1.46% instructions, +1.39% RSS, and +2.18% footprint. The gate
  verifies epoch `9764b3835`, the exact frozen source, all 51 frozen headers,
  and closure hash
  `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`;
  no parent or live-header measurement was run.

- `2026-07-19`: Closed Filesystem and advanced the ordered cursor to suite 41,
  `libs/flyweight/test`. The initial exact forced graph finds 3521 targets,
  requests 420 updates, updates 384, fails nine compile actions, and skips 27
  downstream targets. Five causal classes cover qualified overload-set
  arguments, source-point-aware ADL imports, ADL-only range lookup, semantic
  return-SFINAE identity, and range-for iterator copy deduction. Six reducers
  across PA12, PA15, PA22, and PA24 pass Clang 22, GCC 15, twelve cache modes
  each, owner reports, configured strict suites, placement/hygiene, all 23
  zero-reparse categories, all 14 audit tests, and warning-clean production
  builds. Three PA26 LowIR refs record corrected overload ordinals only. The
  PA9-excluded broad report passes `3995/3995`, including PA37 `7/7`. The final
  exact graph updates all 168 requested targets, handles the deliberate
  `cf_path_nullptr_test` failure as failed-as-expected, and exits successfully.
  The final candidate-only performance result is -1.30% instructions, +1.50%
  RSS, and +2.24% footprint against immutable epoch `9764b3835`; the gate
  verifies the exact frozen source, all 51 frozen headers, and closure hash
  `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`.
  No parent or live-header measurement was run.

- `2026-07-19`: Closed Fiber without a compiler change and advanced the ordered
  cursor to suite 40, `libs/filesystem/test`. The exact forced graph completes
  its architecture, language, threading, ICU, atomic, and host capability
  checks, finds the single bookkeeping target, and exits successfully in
  22.91s. No regression or performance gate was required; the immutable
  compiler-and-51-header checkpoint remains Exception.

- `2026-07-19`: Closed Exception and advanced the ordered cursor to suite 39,
  `libs/fiber/test`. The initial exact graph had three causal compile failures
  in Boost.JSON, `boost_json_test`, and `nlohmann_json_test`, for six failed
  targets and eleven skips. Six coherent typed repairs cover concrete-owner
  result SFINAE, dependent alias detection, unsigned enum LowIR, derived/base
  pointer equality, elaborated template-argument display, and selected-only
  conversion-template body and weak-symbol materialization. Seven owner
  reducers, twelve cache modes each, the `760/760` owner report, configured
  strict suites, all 23 zero-reparse categories, all 22 audit/perf tests, four
  placement audits, an isolated GCC 15 production build, and the PA9-excluded
  `3989/3989` report pass. The focused nlohmann target compiles, links, and runs
  in 678.91s. The final exact graph finds 4583 targets, updates all 217,
  records 54 passing actions, handles all four deliberate compile failures as
  failed-as-expected, and exits successfully in 782.16s. The final three-run
  performance result is -1.44% instructions, +1.76% RSS, and +2.19% footprint
  against immutable epoch `9764b3835`; the gate verifies the exact frozen
  source, all 51 frozen headers, and closure hash
  `7c8a5445f33f04b314de98e6a099de4d75124b4bb032fc97ee5055e56d4827c8`.
  No parent or live-header measurement was run.

- `2026-07-19`: Closed Endian without a compiler change and advanced the
  ordered cursor to suite 38, `libs/exception/test`. The exact forced graph
  finds 793 targets, updates all 184 requested targets, passes all 47
  compile/link/runtime actions across both intrinsic and non-intrinsic lanes,
  and exits successfully. No production code, regression, broad repository
  gate, or performance measurement was needed; the immutable compiler-and-51-
  header checkpoint remains Dynamic Bitset.

- `2026-07-19`: Closed Dynamic Bitset and advanced the ordered cursor to suite
  37, `libs/endian/test`. The initial exact graph compiled but left five test
  executables with undefined nested conversion, defaulted-copy, and repeated
  injected-owner member definitions. Typed collection and instantiation replay
  now retain all three forms. The exact final graph finds 3137 targets, updates
  all 90 requested targets, passes nine positive actions, and handles the one
  deliberate compile failure as failed-as-expected. PA18 passes `235/235`, the
  PA9-excluded report passes `3982/3982`, and strict, audit, reparse, cache,
  host-control, and GCC warning gates pass. The frozen candidate-only gate
  passes at +0.96% instructions, +0.11% RSS, and +0.45% footprint. It rejected
  the first broad fallback at +12.86% instructions; a typed final-qualifier
  equality guard removes that hot-path work. No parent or live-header
  measurement was run.

- `2026-07-19`: Closed DLL and advanced the ordered cursor to suite 36,
  `libs/dynamic_bitset/test`. Fifteen independent typed semantic, ABI, object,
  and mangling causes reduce the initial 41 failures and 87 skips to a clean
  exact graph: 2925 targets found, all 192 requested updates completed, 31
  test actions passed, and both compile-fail tests failed as expected. The
  final validation passes strict, all 23 zero-reparse categories, audit/perf
  unit tests, isolated GCC 15, and the PA9-excluded `3981/3981` report.
  Candidate-only performance passes at +0.96% instructions, +0.96% RSS, and
  +0.39% footprint against the immutable `9764b3835` compiler-and-51-header
  epoch. The gate now enforces the checked-in epoch manifest and rejects
  source, header membership, digest, symlink, closure, or command drift; no
  parent or live-header measurement was run.

- `2026-07-19`: Closed Detail without a compiler change and advanced the
  ordered cursor to suite 35, `libs/dll/test`. The exact forced graph finds 544
  targets, updates all 48 requested targets, passes all 12 actions, and exits
  successfully. No production code, regression, broad repository gate, or
  performance measurement was needed.

- `2026-07-19`: Closed Describe without a compiler change and advanced the
  ordered cursor to suite 34, `libs/detail/test`. The exact forced graph finds
  856 targets, updates all 182 requested targets, passes 49 positive actions,
  reports all four deliberate C++14-only failures as failed-as-expected, and
  exits successfully. No production code, regression, broad repository gate,
  or performance measurement was needed.

- `2026-07-19`: Closed Decimal without a compiler change and advanced the
  ordered cursor to suite 33, `libs/describe/test`. Both C++14
  `decltype(auto)` configurations are unavailable in the C++11 lane; the exact
  forced graph finds the single bookkeeping target and exits successfully. No
  production code, regression, broad repository gate, or performance
  measurement was needed.

- `2026-07-19`: Closed DateTime without a compiler change and advanced the
  ordered cursor to suite 32, `libs/decimal/test`. The exact forced graph finds
  2323 targets, updates all 272 requested targets, passes 59 positive actions,
  reports all four deliberate special-value compile failures as
  failed-as-expected, and exits successfully. No production code, regression,
  broad repository gate, or performance measurement was needed; the immutable
  compiler-and-51-header performance checkpoint remains CRC.

- `2026-07-19`: Closed CRC and advanced the ordered cursor to suite 31,
  `libs/date_time/test`. Resolving a dependent non-type parameter type in a
  scope that already bound the target parameter substituted earlier parameters
  correctly but also rewrote the target declarator's declared identifier to
  its bound value. The final typed fix masks only that declared-identifier AST
  node, performs the existing structured substitution, and restores the node
  before declarator parsing. The exact final forced graph finds 752 targets,
  updates all 26 requested targets, passes all six compile/link/runtime
  actions, and exits successfully. PA19 passes `143/143`; strict, placement,
  all 23 zero-reparse categories, all 14 audit tests, twelve cache modes,
  Clang/GCC warning gates, and the PA9-excluded `3966/3966` report pass.
  Candidate-only performance against the immutable compiler-and-51-header
  epoch is +0.82% instructions, -1.53% RSS, and -2.31% footprint, only 0.05
  instruction percentage points above Coroutine. The existing substitution
  helper falls from 25 to 22 top-stack samples. The measurement uses only
  `benchmarks/self_compile/stable/include`; no parent compiler or live project
  header was measured.

- `2026-07-19`: Closed Coroutine and advanced the ordered cursor to suite 30,
  `libs/crc/test`. An out-of-class member-template definition of an explicit
  class specialization incorrectly exposed its sole template head as owner
  parameters, so owner binding overwrote the correctly deduced function
  reference with the owner's explicit `void` argument. Explicit-specialization
  owners now contribute zero typed owner template parameters. The exact final
  forced graph finds 3227 targets, updates all 70 requested targets, passes both
  compile/link/runtime actions, and exits successfully. The PA21/PA22 report
  passes `513/513`; strict, placement, all 23 zero-reparse categories, all 14
  audit tests, twelve cache modes, Clang/GCC warning gates, and the PA9-excluded
  `3965/3965` report pass. Candidate-only performance against the immutable
  compiler-and-51-header epoch is +0.77% instructions, -1.27% RSS, and -2.30%
  footprint, a 0.03 percentage-point instruction improvement from Core. The
  threshold profile shows the collector on the established semantic collection
  path, while the new branch adds no loop, lookup, allocation, or cache. The
  measurement uses only `benchmarks/self_compile/stable/include`; no parent
  compiler or live project header was measured.

- `2026-07-19`: Closed Core and advanced the ordered cursor to suite 29,
  `libs/coroutine/test`. Four typed semantic causes covered address-only
  constant values and derived-`this` references, selected deleted
  constructors, private-base `static_cast` access, and class-scope
  `static_assert` during lazy completion. The exact final forced graph finds
  6233 targets, updates all 1137 requested targets, and exits successfully.
  The affected direct report passes `606/606`; strict, placement, all 23
  zero-reparse categories, all 14 audit tests, twelve cache modes, Clang/GCC
  warning gates, and the PA9-excluded `3963/3963` report pass. Candidate-only
  performance against the immutable compiler-and-51-header epoch is +0.80%
  instructions, -1.21% RSS, and -2.35% footprint. The threshold diagnostic
  finds no changed helper in a hotspot. The measurement uses only
  `benchmarks/self_compile/stable/include`; no parent compiler or live project
  header was measured.

- `2026-07-19`: Closed Convert without a compiler change and advanced the
  ordered cursor to suite 28, `libs/core/test`. The exact four-job forced graph
  completes its configuration checks, finds one bookkeeping target, and exits
  successfully. No regression or performance measurement was required.

- `2026-07-19`: Closed Conversion without a compiler change and advanced the
  ordered cursor to suite 27, `libs/convert/test`. The exact four-job forced
  graph finds 364 targets, updates all 18 requested targets, reports both
  negative tests as failed-as-expected, passes all three positive tests, and
  exits successfully. No regression or performance measurement was required.

- `2026-07-19`: Closed Contract and advanced the ordered cursor to suite 26,
  `libs/conversion/test`. The suite exposed ten independent typed causes across
  functional-cast lookup, lambda closure identity, destructor body and unwind
  cleanup, C++11 elision access, member-pointer access SFINAE, literal branch
  folding, inherited virtual receiver adjustment, i128 call ABI chunks,
  using-imported base overload filtering, and hosted local-lambda RTTI naming.
  Each cause has an owner regression at the earliest audited PA. The exact
  final forced graph runs 214 tests, updates all 877 requested targets, and
  exits successfully; the final five-target cleanup replay also passes. The
  affected report passes `1365/1365`; strict, placement, all 23 zero-reparse
  categories, all 23 audit tests, ten individual plus all-disabled cache modes
  across six reducers, host controls, GCC 15 production builds, and the PA9-
  excluded `3955/3955` report pass. Candidate-only performance against the
  immutable compiler-and-51-header epoch is +0.52% instructions, -1.38% RSS,
  and -2.43% footprint. The threshold diagnostic finds no changed helper in a
  hotspot; no parent compiler or live project header was measured.

- `2026-07-19`: Closed Context without a compiler change. The exact four-job
  forced graph completes its configuration checks, finds one bookkeeping
  target, and exits successfully. No regression or performance measurement was
  required.

- `2026-07-18`: Closed Container Hash and advanced the ordered cursor to suite
  24, `libs/context/test`. Three sequence-container failures came from the
  structured invocation helper keeping the first viable callable overload
  across specializations; typed conversion ranking now selects the exact
  `operator()`. The separate `is_range_test4` failure came from treating a
  materialized outer result specialization as fully settled while its non-type
  argument still held a dependent expression; retained typed-AST rebinding now
  performs unqualified wrapper lookup in the function template's lexical
  scope. PA22 `275/275`, strict, placement, ten individual plus all-disabled
  cache modes for both reducers, all 23 zero-reparse categories, all 14 audit
  tests, host controls, GCC 15 production builds, and the PA9-excluded
  `3942/3942` report pass. The exact final Container Hash graph finds 3199
  targets, updates all 341 outdated targets, and exits successfully.
  Candidate-only frozen compiler-and-51-header performance is +0.10%
  instructions, -3.07% RSS, and -2.49% footprint; no parent compiler or live
  project header was measured.

- `2026-07-18`: Closed Container and advanced the ordered cursor to suite 23,
  `libs/container_hash/test`. The apparent move-assignment storage corruption
  was first an input/forward dispatch error: enclosing substitution captured a
  same-named use-scope type inside a defaulted non-type argument owned by
  another class template. Declaring-template rematerialization removes the
  invalid free and exposes the independent 41-element initializer-list leak.
  Backing-array elements now use the existing full-expression cleanup stack,
  with exact cleanup transfer for named automatic initializer-list lifetime
  extension. PA22 `273/273`, PA25 `74/74`, strict, placement, ten individual
  plus all-disabled cache modes, all 23 zero-reparse categories, all 14 audit
  tests, Clang/GCC warnings, and the PA9-excluded `3940/3940` report pass. The
  exact forced Container graph updates all 325 requested targets and exits
  successfully. Candidate-only frozen compiler-and-51-header performance is
  +0.02% instructions, -3.63% RSS, and -2.57% footprint; no parent or live-
  header workload was run.

- `2026-07-18`: Closed Container's final compile frontier without closing the
  suite. Phase-one list-constructor filtering now unwraps reference and cv from
  the typed first parameter before asking whether it is `std::initializer_list`.
  The PA25 reducer preserves the saved-parent wrong-constructor selection and
  is host warning-clean. PA25 `73/73`, strict, placement, ten individual plus
  all-disabled cache modes, all 23 zero-reparse categories, all 14 audit tests,
  GCC 15 warnings, and the PA9-excluded `3938/3938` report pass. Focused Boost
  now compiles and links, then exposes an independent runtime invalid free in
  `test_elem_base::operator=(test_elem_base&&)` during `test_assign_input_range`.
  Only the candidate was measured against the immutable frozen compiler-and-
  header baseline; instructions, RSS, and footprint all improve, and no parent
  command was run.

- `2026-07-18`: Closed Container's flat-set adaptor rebind ambiguity without
  closing the suite. Three direct template-template-head partials matched the
  same three-argument `vector` specialization by omitting zero, one, or two
  canonical arguments that the matcher had already proved equal to source
  defaults. Partial ordering now prefers fewer typed default omissions before
  applying the existing equal-coverage rules. The PA21 reducer preserves the
  exact saved-parent ambiguity and is host warning-clean. PA21 `235/235`,
  strict, placement, ten individual plus all-disabled cache modes, all 23
  zero-reparse categories, all 14 audit tests, GCC 15 warnings, and the PA9-
  excluded `3937/3937` report pass. Focused Boost compile/link/runtime passes.
  The exact forced Container graph updates 321 targets and passes 78 tests,
  leaving only `devector_test` and its three downstream skips. Only the
  candidate was measured against the immutable frozen-header baseline;
  instructions, RSS, and footprint all improve, and no parent command was run.

- `2026-07-18`: Closed Container's scoped-allocator converting-constructor
  failure without closing the suite. A partial-specialization member carried a
  concrete inner-allocator pack, but function-template deduction replaced that
  exact typed argument with retained source syntax for the ancestor owner pack.
  Deduction now refuses only that concrete-to-dependent downgrade and otherwise
  preserves the existing structured-syntax and cv path. The PA22 unevaluated
  constructor-SFINAE reducer preserves an exact saved-parent/fixed split without
  expanding this checkpoint into a separate ABI-symbol issue. PA22 `272/272`,
  strict, placement, ten individual plus all-disabled cache modes, all 23
  zero-reparse categories, all 14 audit tests, GCC 15 warnings, and the PA9-
  excluded `3936/3936` report pass. Focused Boost compile/link/runtime passes.
  The exact forced Container graph updates 317 targets and passes 77 tests,
  leaving two failures and six skips. The ordered frontier is now
  `flat_set_adaptor_test`. Only the candidate was measured against the
  immutable frozen-header baseline; instructions, RSS, and footprint all
  improve, and no parent command was run.

- `2026-07-18`: Closed Container's scoped-allocator dispatch ambiguity without
  closing the suite. Concrete namespace function templates had imported the
  complete caller use scope, including unrelated primary-template placeholders
  retained behind a concrete partial specialization. Those placeholders made
  invalid concrete return types appear dependent and prevented SFINAE candidate
  removal. Concrete namespace instantiation now overlays only typed local names
  carried by its own arguments; dependent namespace calls and member templates
  retain the full context. The PA22 reducer preserves the exact saved-parent
  ambiguity and is host warning-clean. PA22 `271/271`, strict, placement, ten
  individual plus all-disabled cache modes, all 23 zero-reparse categories, all
  14 audit tests, GCC 15 warnings, and the PA9-excluded `3935/3935` report pass.
  Focused Boost compile/link/runtime passes. The exact forced Container graph
  updates 313 targets and passes 76 tests, leaving three failures and nine skips.
  The ordered frontier is now `scoped_allocator_adaptor_test`. Only the
  candidate was measured against the immutable frozen-header baseline; all
  instruction and memory metrics improve, and no parent command was run.

- `2026-07-18`: Closed Container's three recursive layout failures without
  closing the suite. A concrete node specialization could be marked complete
  with no layout while its recursive value type was still incomplete; later
  completion short-circuited instead of rematerializing it. Typed
  `concrete_layout_deferred` provenance now permits only that safe retry and is
  cleared by real layout finalization and reset paths. The PA23 reducer is
  host-clean and preserves an exact saved-parent/fixed split. PA23 `401/401`,
  strict, placement, ten individual plus all-disabled cache modes, all 23
  zero-reparse categories, all 14 audit tests, GCC 15 warnings, and the PA9-
  excluded `3934/3934` report pass. The exact forced Container graph updates
  309 targets: `slist_test`, `list_test`, and `set_test` all pass, leaving four
  failures and 12 skips. The ordered frontier is now
  `scoped_allocator_usage_test`. Only the candidate was measured against the
  immutable frozen-header baseline; instructions, RSS, and footprint all
  improve, and no parent command was run.

- `2026-07-18`: Began Container and closed its first ordered failure without
  closing the suite. The initial exact forced graph failed eight compile
  actions. libc++'s alias-based `string_view` equality overload was present in
  lookup but lost during deduction because canonical parameter types no longer
  retained that the second parameter came from a dependent qualified-member
  alias target. Deduction now uses the retained parameter AST and alias
  declaration AST to classify that non-deduced context; direct aliases remain
  deducible. The two PA23 reducers, `400/400` direct report, strict, ten
  individual and all-disabled cache modes, Clang/GCC warnings, placement, all
  23 zero-reparse categories, and all 14 audit tests pass. The PA9-excluded
  report passes `3933/3933`. Focused `string_test` passes, and the exact
  post-fix graph updates 297 targets while leaving seven compile failures. The
  ordered frontier is now `slist_test` and its recursive `base_node` layout.
  Only the candidate was measured against the immutable frozen-header baseline;
  all fixed gates pass and no parent command was run.
- `2026-07-18`: Corrected the primary performance workload after closing
  Config. The old default froze `semantic_overload.cpp` but still selected live
  `dev/src` headers. The gate now selects a checked-in, verified 51-file project
  header closure, documents its `56afe87c1` provenance, and rejects baseline
  and candidate commands that differ anywhere except the output path. Unit
  checks cover the default include path, closure completeness, output-path
  normalization, and rejection of a changed header path; an actual compile
  using only the frozen project include directory passes. The old live-header
  results remain historical within their epoch. A clean candidate-only baseline
  at exact commit `9764b3835` records medians of `263559800479` instructions,
  `1292648448` RSS, and `1009885184` footprint. It is the immutable baseline for
  future frozen-header comparisons. No parent command was run.
- `2026-07-18`: Closed Config and advanced the ordered cursor to suite 22,
  `libs/container/test`. Constant lookup now gives named variable values their
  typed storage identity, and exact cv-compatible reference casts preserve it;
  temporaries remain identity-free. The PA20 positive and negative reducers,
  `79/79` focused report, strict suites, thirteen positive and negative cache
  configurations, Clang/GCC warnings, placement, all 23 zero-reparse
  categories, and all 23 audit tests pass. The PA9-excluded broad report passes
  `3931/3931`. The final exact graph finds 1391 targets, updates all 128
  requested targets, and exits 0. Only the candidate was measured; every fixed
  and saved-parent performance gate passes, and no parent command was run.
- `2026-07-18`: Closed Concept Check and advanced the ordered cursor to suite
  21, `libs/config/test`. Function-valued non-type template arguments now queue
  semantic-only body validation after collection and recursively validate
  called constraint bodies without affecting output ownership or emission
  order. The PA23 destructor-shaped reducer, `710/710` affected report, strict,
  twelve cache configurations, Clang/GCC warnings, placement, all 23 zero-
  reparse categories, and all 23 audit tests pass. The PA9-excluded broad report
  accounts for `3929/3929` after its sole load timeout passes in isolation. The
  final exact forced graph finds 428 targets, updates all 44 requested targets,
  and exits 0. Only the candidate was measured; all fixed and saved-parent
  performance metrics improve, and no parent command was run.
- `2026-07-18`: Closed Compute and advanced the ordered cursor to suite 20,
  `libs/concept_check/test`. The exact forced graph exits 0 after finding its
  two bookkeeping targets; both OpenCL configurations are unavailable on this
  host, matching the inventory and the established frontier convention. No
  compiler or repository change was required, so no regression or performance
  gate was triggered.
- `2026-07-18`: Closed Compat and advanced the ordered cursor to suite 19,
  `libs/compute/test`. The final `move_only_function_test` path required five
  typed fixes: explicit initializer-list functional casts use the retained
  initializer-list expression, globally qualified placement new bypasses class
  allocation lookup, repeated named local classes receive standard Itanium
  discriminators, substituted default aliases preserve their resolved type
  across a shadowing spelling, and builtin function-type trait operands are
  lvalues. `to_array_rvalue_test` required one shared cv-aware class-transfer
  rule so `const T&&` copies while ordinary `T&&` still moves. A proposed
  consteval guard had no owner witness, passed the complete graph when removed,
  and was discarded. The exact minimal forced graph rebuilds all 220 requested
  targets and passes. Clang/GCC warning checks, affected-owner reports,
  placement, strict comparisons, all 23 zero-reparse categories, all 14 audit
  tests, and twelve byte-identical cache modes pass. The final PA9-excluded
  report is `3928/3928`. Only the candidate was measured: its medians are
  `262633785323` instructions, `1285660672` RSS, and `1008902144` footprint;
  arithmetic against saved parent medians is -0.495%, -0.464%, and -0.064%.
- `2026-07-18`: Closed both Compat `function_ref` member-function targets. The
  constructor failure was structured member-pointer NTTP identity loss during
  function-template deduction, followed by typed gaps in repeated member-address
  analysis, named-NTTP value category, and template-template parameter matching.
  The implementation now retains and compares the existing AST/type/template
  entities throughout; no semantic text or cache path was added. PA21/PA26,
  strict, placement, all 23 reparse categories, and the PA9-excluded `3922/3922`
  report pass. Only the candidate was measured; arithmetic reuses saved parent
  medians. The forced Compat graph now has two failures/four skips, with
  `move_only_function_test` as the next frontier.

- `2026-07-18`: Closed Compat's two bind member-function runtime crashes. A
  reference-rooted expression had crossed into libc++'s by-value tuple leaf,
  but hidden virtual-base forwarding still used the hosted-reference vtable
  fallback and read an offset at `-24` from a contained object with no host
  vptr. The typed call-semantic path now recognizes a complete contained class
  member and uses its retained structured virtual-base layout first; reference
  members and genuine hosted polymorphic references keep the dynamic path. The
  real `<tuple>` PA36 reducer, existing `std::ostream &` dynamic-layout control,
  PA36 `2300/2300`, strict/audit/cache gates, and PA9-excluded `3920/3920`
  report all pass. Only the candidate was measured; arithmetic against the
  saved `7c2bad236` parent medians is +0.002% instructions, +0.942% RSS, and
  -0.073% footprint. Both focused targets pass, and the exact graph improves
  from 6 failures/12 skips to 4 failures/10 skips. The suite remains open at
  `function_ref_mfn_test`, where the converting constructor template is absent
  from the viable constructor set for a member-function NTTP holder.
- `2026-07-18`: Closed Compat's two bind-object compile failures. The apparent
  rooted-namespace lookup miss was downstream: both `operator() const &` and
  `operator() const &&` survived a const-rvalue call with identical implicit
  object pointer ranks, so deduction of the surrounding test helper failed on
  the ambiguity. Otherwise tied implicit-object sequences now prefer the
  rvalue ref-qualified member. The earliest-owner PA16 reducer, twelve cache
  modes, strict/audit/GCC gates, and PA9-excluded `3919/3919` report all pass.
  Only the current candidate was measured; rolling deltas use the immutable
  saved `4c3c19f3a` medians. Both focused targets pass, and the exact graph
  improves from 8 failures/18 skips to 6 failures/12 skips. The suite remains
  open at the `bind_front_mfn_test` and `bind_back_mfn_test` runtime crashes.
- `2026-07-18`: Closed Compat's four-target forwarded member-pointer constexpr
  cluster. Constant values now retain typed data/member-function pointer
  identity through wrapper storage, forwarding, `.*`/`->*`, typed owner
  adjustment, and implicit-object calls. The direct constant-call fast path
  rejects stale pointer/member-pointer specializations, while resolved symbol
  lookup remains indexed-first and validates the concrete object symbol. The
  PA26 reducer, twelve cache modes, strict/audit/GCC gates, and PA9-excluded
  `3918/3918` report all pass. Only the current candidate was measured; rolling
  deltas use the immutable saved `b7efbce95` medians and remain below the
  hotspot trigger. The exact optimized graph improves from 12 failures/22
  skips to 8 failures/18 skips. The suite remains open at
  `bind_front_obj_test`, where rooted `::boost::detail::test_with_impl` is
  incorrectly resolved through `boost::detail::detail`.
- `2026-07-18`: Closed Compat's three-target forwarded function-designator
  constexpr cluster. Constant evaluation now preserves a function name as a
  typed designator through reference binding and explicit decay, resolves
  indirect calls from its stored symbol identity, preserves named parameter
  lvalues through pack expansion, and rejects stale still-dependent direct-call
  candidates. The earliest-owner PA20 and PA22 regressions, cache-independent
  controls, strict/audit/GCC gates, and PA9-excluded `3917/3917` report all
  pass. The exact forced graph improves from 15 failures/25 skips to 12
  failures/22 skips. Only the current candidate was measured; the rolling
  comparison reuses `e1e9f0094`'s recorded medians and remains below the
  hotspot trigger. The suite remains open at `invoke_mfn_constexpr_test` and
  the four-target member-pointer constexpr cluster.
- `2026-07-18`: Opened Compat and closed its five-target pointer-to-data-member
  cluster. The initial exact forced graph finds 941 targets, requests 664
  updates, updates 614, fails 20 actions, and skips 30. Scalar member-access
  xvalues returned by reference were copied into a dangling local, while typed
  owner adjustment discarded const qualification before computing member
  result types. The PA26 reducers preserve storage identity and all base,
  derived, lvalue, rvalue, and pointer cv forms. All compiler gates pass,
  including PA9-excluded `3915/3915`; only the current candidate was measured,
  and the rolling comparison reuses `33bebba1e`'s recorded medians. The next
  exact graph has 15 failures and 25 skips. The suite remains open at
  `invoke_fn_constexpr_test` and its independent unevaluated static assertion.
- `2026-07-18`: The exact forced Circular Buffer graph updates all 34 requested
  targets and passes all seven tests on `7836d6a18`. No repository change or
  performance measurement is required. Suite 17 is closed and the cursor
  advances to `libs/compat/test`.
- `2026-07-18`: Closed Chrono's fourth and final cause. The result type of an
  out-of-class member definition lost its concrete owner binding because the
  mangle-metadata resolver always parented its argument scope at the result
  class template's declaration. The final rule uses the active typed scope only
  when metadata carries a direct type parameter; compound-only alias and
  partial-specialization metadata stays on the conservative path, as guarded by
  the PA23 regression caught during validation. Focused `v1_io_ex1_s` and the
  exact 420-target graph pass with no failures or skips. All compiler gates pass,
  including PA9-excluded `3913/3913`. Only the current candidate was measured;
  rolling deltas reuse `33bebba1e`'s recorded medians and are +0.615%
  instructions, +1.064% RSS, and +0.069% footprint. Suite 16 is closed and the
  cursor advances to `libs/circular_buffer/test`.
- `2026-07-18`: Closed Chrono's third build-order cause. A nested landing pad
  could not advertise an enclosing typed handler across an active catch cleanup,
  while direct miss transfer bypassed that cleanup and retained an inconsistent
  host EH region stack. The typed lowering now exposes the enclosing clauses
  after a cleanup boundary and runs intermediate plus active-catch cleanups
  before transfer. Focused `rounding_pass_h` and the header-free PA25 runtime
  reducer pass; the exact replay improves from 25 to 6 failures and from 63 to
  12 skips, closing the entire EH cluster. All compiler gates pass, including
  PA9-excluded `3912/3912`. Only the current candidate was measured: the rolling
  comparison reuses `3c2623d84`'s recorded medians and yields -0.156%
  instructions. The suite remains open on the independent
  `ratio_string<...>::prefix()` host-link family at `v1_io_ex1_s`.
- `2026-07-17`: Closed Chrono's second build-order cause. Unary constexpr
  evaluation stopped after a failed built-in operation and never tried the
  existing typed overloaded-expression hook; its synthetic member call also
  lacked a retained structured operator name. The corrected built-in-then-
  overload path clears `arithmetic_pass_h` and the related duration
  construction, comparison, cast, and value constexpr family. The exact full
  replay improves from 30 to 25 failures and from 78 to 63 skips. All compiler
  gates pass, including PA9-excluded `3911/3911`; the new candidate is measured
  once and compared with the prior commit's recorded measurements, yielding a
  +0.052% rolling instruction delta. The suite remains open at
  `rounding_pass_h` and its independent host EH region-stack failure.
- `2026-07-17`: Opened Chrono and closed its first build-order cause. The
  initial exact forced survey finds 2997 targets, requests 420 updates, updates
  304, fails 32 actions, and skips 84 targets. Scalar-duration result
  substitution had flattened a trailing source-defaulted class-template
  argument into an explicit expression and evaluated it outside the class
  template's declaring scope. Trimming only the trailing defaulted run lets the
  typed default resolver restore that scope while preserving non-trailing
  carried defaults. Focused `saturating_h` passes; the full replay improves to
  30 failures and 78 skips. All compiler gates pass, including PA9-excluded
  `3910/3910` and exact-parent `-0.11%` instructions. The suite remains open at
  `arithmetic_pass_h` and its unevaluated duration-negation assertion.
- `2026-07-17`: Closed Charconv and advanced the V2 cursor to
  `#16 libs/chrono/test`. The 15 remaining link failures were one typed
  constant-expression cause: structured qualified lookup suppressed folding
  solely because qualifier syntax existed, even when retained `decltype`
  syntax resolved to an exact concrete type. Returning that existing `TypePtr`
  from lookup and applying the normal constexpr binding rule only for a
  non-dependent qualifier removes the undefined static-member symbols without
  changing dependent lookup. Focused `to_chars_float` passes, and the exact
  final graph updates all 150 requested targets successfully. The PA20
  controlled-parent reducer, twelve cache modes, direct/strict reports,
  placement, zero-reparse audit, GCC 15, fixed/rolling performance, and
  PA9-excluded `3909/3909` gates pass.
- `2026-07-17`: Opened Charconv at `e816d6560`. The initial exact forced
  survey finds 730 targets, requests 150 updates, updates 39, and stops at one
  compile failure plus 110 skips: `to_chars.o` cannot evaluate the inherited
  `policy_holder::return_has_sign` non-type argument. Direct base-pack
  expansion had discarded each namespace-qualified bound policy's exact type
  before completing the instantiated holder. Carrying the bound `TypePtr`
  through both base-clause paths closes that cause. The exact replay compiles
  and archives Charconv, updates 105 targets, and advances to 15 link failures
  plus 30 skips, all exposing missing C++11 static constexpr member symbols.
  The PA19 reducer, twelve cache modes, direct/strict reports, placement,
  zero-reparse audit, GCC 15, fixed/rolling performance, and PA9-excluded
  `3908/3908` gates pass. The cursor remains on suite 15 at the independent
  static-constexpr definition/emission cluster.
- `2026-07-17`: Closed CallableTraits and advanced the V2 cursor to
  `#15 libs/charconv/test`. The last eager/lazy pair exposed two invocation
  paths with inconsistent and incomplete member-pointer ref-qualifier checks.
  The normal call analyzer skipped the check entirely; the structured helper
  reused ordinary member-call binding, which is too permissive for C++11
  `.*`/`->*`. Both now enforce the pointed-to function's typed `&`/`&&`
  qualifier against the object value category. Focused eager `is_invocable`
  and `is_invocable_r` pass, and the exact final graph updates all 376 targets
  without failure. The PA26 controlled-parent reducer, twelve cache modes,
  direct/strict reports, placement, zero-reparse audit, GCC 15, fixed/rolling
  performance, and PA9-excluded `3907/3907` gates pass.
- `2026-07-17`: Split the first remaining `is_invocable` failure into a generic
  constexpr conversion defect and the next member-pointer viability rule. The
  trait's inherited conversion to `bool` was valid and its `value` member was
  already true; only `true == trait<...>()` failed because constexpr fallback
  skipped typed class-to-scalar conversion before retrying the built-in
  comparison. Reusing the existing conversion evaluator clears the first
  positive assertion in focused eager `is_invocable` and `is_invocable_r`.
  Both targets now reach the independent `void (foo::*)() &` negative case on a
  `foo` prvalue. The strengthened PA20 reducer, controlled parent, twelve cache
  modes, direct/strict reports, placement, zero-reparse audit, GCC 15,
  fixed/rolling performance, and PA9-excluded `3906/3906` gates pass.
- `2026-07-17`: Closed CallableTraits' fourth shared cause without advancing the
  suite cursor. Direct captureless-lambda operands in `auto` deduction took the
  synthetic free-function shortcut and erased the closure identity observed by
  `decltype`; switching that boundary to the existing typed closure analyzer
  exposed a missing standard conversion that the shortcut had masked. The
  conversion layer now lazily materializes a typed free target from the retained
  call operator only when implicit function-pointer conversion or unary `+` asks
  for it. Eager/lazy `function_type` and `add_varargs_constraints` now pass. The
  exact forced suite updates 360 targets and leaves only 4 failed compile actions
  plus 12 skips: eager/lazy `is_invocable` and `is_invocable_r`. The PA24
  reducer, twelve cache modes, direct/strict reports, exact PA36 hosted RTTI,
  placement, zero-reparse audit, GCC 15, fixed/rolling performance, and
  PA9-excluded `3906/3906` gates pass. `is_invocable` is next at the first
  member-function-pointer/object case.
- `2026-07-17`: Closed CallableTraits' third shared cause without advancing the
  suite cursor. The simple type-id substitution fast path accepted structured
  `C::*` declarators even though its helper implements only token pointer and
  reference operators; failure replaced the whole type-id with the substituted
  base type. Restricting that fast path preserves the member-pointer declarator
  on recursive typed AST substitution and clears `apply_member_pointer_pmd` in
  eager and lazy modes. The exact suite now has 8 failed compile actions and 24
  skips across four pairs. The PA26 reducer, twelve cache modes, direct/strict
  reports, placement, zero-reparse audit, GCC 15, fixed/rolling performance,
  and PA9-excluded `3905/3905` gates pass. `function_type` is next because it
  and `add_varargs_constraints` both expose a captureless lambda's `decltype`
  as a function pointer instead of the retained lambda class.
- `2026-07-17`: Closed CallableTraits' second shared cause without advancing
  the suite cursor. `decltype(&T::operator())` used a structured leaf resolver
  that skipped ordinary semantic owner lookup, then reconstructed cv but not
  the binding's ref qualifier. The literal member-pointer control was already
  correct; the bad path produced an ordinary function pointer or a member
  pointer missing `&`/`&&`. Typed owner lookup plus ref-qualifier reconstruction
  clears seven eager/lazy pairs: `has_member_qualifiers`, `has_varargs`, and all
  five const/cv/lvalue/rvalue/volatile member predicates. The exact suite now
  has 10 failed compile actions and 30 skips across five pairs. The PA26
  reducer, twelve cache modes, direct/strict reports, placement, zero-reparse,
  GCC 15, fixed/rolling performance, and PA9-excluded `3904/3904` gates pass.
  The next target is `apply_member_pointer_pmd`.
- `2026-07-17`: Closed CallableTraits' first shared cause without advancing the
  suite cursor. A function-local member-pointer owner selected through a packed
  class alias was exact during substitution, but the retained `ptr_operator`
  dropped that semantic type and the member-pointer declarator bypassed the
  standard semantic-node lookup. Carrying the exact owner `TypePtr` through
  both stages clears `has_member_qualifiers_simple`, `is_noexcept`,
  `is_transaction_safe`, `remove_noexcept`, and `remove_transaction_safe` in
  eager and lazy modes. The forced suite improves from 34 failed compile
  actions and 102 skips to 24 failures and 72 skips, with no remaining
  alias-materialization diagnostic. The PA26 reducer, all twelve cache modes,
  direct/strict reports, placement, zero-reparse audit, GCC 15, performance,
  and PA9-excluded `3903/3903` gates pass. The next focused target is
  `is_volatile_member`, representing the remaining local function-object
  qualifier cluster.
- `2026-07-17`: Opened CallableTraits from the exact forced V2 survey at commit
  `733e3f182`. B2 finds 1348 targets, updates 994, fails 34 compile actions in
  17 eager/lazy pairs, and skips 102 downstream targets. The dominant explicit
  diagnostic is failure to instantiate the typed
  `set_member_function_qualifiers` alias used by member-function traits. The
  survey also exposes later assertion-only frontiers in varargs constraints,
  member-data invocation, function-object function types, and invocation
  traits; those remain classified separately until the shared qualifier-alias
  cluster is fixed. The first focused target is `remove_transaction_safe`.
- `2026-07-17`: Closed Bloom and advanced the V2 cursor to CallableTraits.
  Bloom's `std::get<B ? 0 : 1>` diagnostic was downstream, not an explicit
  non-type template-argument lookup defect: all `get` candidates were present,
  but their `std::forward_as_tuple(f, g)` argument was rejected because the
  deduced `tuple<T&&...>` result retained two stale `T&&` semantic arguments.
  General pack expansion now carries each selected element's exact `TypePtr`
  through dependent class materialization. A witness-only non-type-pack control
  caught and prevented an overbroad first version. The PA24 two-local-lambda
  reducer, direct/strict reports, placement/hygiene, all 23 zero-reparse
  categories, 14 audit tests, twelve cache configurations, warning/GCC gates,
  fixed and rolling performance checks, and PA9-excluded `3902/3902` report all
  pass. The final forced Bloom graph updates all 38 requested targets and exits
  successfully. PA9 was not run.

- `2026-07-17`: Opened Bloom and cleared its shared nine-object anonymous
  local-class ABI failure. Anonymous local classes retained synthetic semantic
  names but not the enclosing-function local entity used by Itanium mangling;
  the parent compiler consequently emitted ordinary nested-class symbols in a
  simple reducer and could not construct Bloom's more complex constructor
  symbol. The typed class metadata now retains the enclosing function and an
  unnamed-local discriminator, producing Clang-matching `Ut_` and `Ut0_`
  constructor symbols. The PA33 object inspection covers two anonymous types,
  every cache-disabled mode is byte-identical, and warning, GCC 15, strict,
  placement, reparse, broad PA9-excluded, fixed-performance, and rolling gates
  pass. The full forced Bloom replay clears eight of the nine failed objects and
  advances the ordered frontier to the one independent `test_construction`
  failure: `std::get<B ? 0 : 1>` in `if_constexpr<false>`. PA9 was not run.

- `2026-07-17`: Closed Bind and advanced the V2 cursor to Bloom. The two
  `apply_rv` compile failures were not failures to invoke a prvalue callable:
  the result-traits member typedef had already resolved to concrete class
  `::F`, then a legacy spelling fallback rebound that nondependent identity to
  the neighboring template parameter `F` and changed it to `::F (*)()`. Gating
  that fallback on actual typed template dependence preserves the concrete
  identity without adding a cache or recovery path. The PA18 reducer fails on
  the immutable V2 baseline and passes with the fix; Clang agrees, and every
  cache-disabled mode emits byte-identical LowIR. PA18 direct, strict, witness,
  placement, hygiene, reparse, broad PA9-excluded, fixed-performance, and
  rolling-performance gates all pass. The final forced Bind graph updates all
  303 requested targets, records 77 passing test actions including the expected
  compile failure, and exits successfully. PA9 was not run.

- `2026-07-17`: Cleared Bind's two runtime failures and advanced the ordered
  cursor to `apply_rv_test`. Both the current frontier and immutable V2 baseline
  lowered the parameter reference in `template<class F> bind(F f, ...)` as the
  same-named namespace function `@f`, so `bind(g, 1)` retained `f`. The
  target-aware function-id resolver now defers when the existing structured
  lookup finds a visible value binding, allowing ordinary typed expression
  analysis to load the function parameter. The PA18 reducer fails on the
  baseline and passes with the fix; Clang agrees. PA18 direct, strict, witness,
  placement, and all 23 zero-reparse gates pass. Fixed performance is -3.33%
  instructions, -0.96% RSS, and -1.75% footprint; every rolling increase is
  below the early-warning threshold. Both runtime targets pass directly and in
  focused B2, and the full forced graph now records 75 passing actions, one
  expected compile failure, two independent compile failures, and six skips.
  PA9 was not run.

- `2026-07-17`: Opened Bind from the exact forced V2 survey at commit
  `9e8630d5a`. The graph updates 291 targets and records 73 passing test actions,
  one expected compile failure, four unexpected failures, and eight downstream
  skips. The ordered frontier is `bind_function_test`: a wrapper constructed
  from `bind(f, 1)` compares equal to `bind(g, 1)`, and after assignment from
  `bind(g, 1)` it still calls `f`. All eight `bind_and_or_test` misses likewise
  require `g` to differ from `f`. Two later `apply_rv` targets independently
  fail overload resolution in `boost/bind/apply.hpp:23`. A direct templated
  function-pointer holder passes and is retained as a control, so reduction and
  first-bad-commit isolation precede any compiler change. PA9 was not run.

- `2026-07-17`: Closed Bimap and advanced the V2 cursor to Bind after correcting
  the last Xpressive failure classification. The earlier eight-job child miss
  was not external memory pressure: an implicit-object conversion completed an
  incomplete class and discarded the reference-only `FunctionBinding` objects
  already stored in the overload candidate vector. The loop refreshed only its
  current entry, so a later candidate and the diagnostic path could dereference
  freed storage. At-risk candidates now carry a structured typed refresh key,
  check the compiler's live-binding set before every use, and reacquire only an
  exact owner/name/type/qualifier/template-identity match through typed lookup.
  The PA22 reducer deterministically records one attempt and one successful
  reacquisition normally and with all ten semantic caches disabled; both emit
  byte-identical LowIR. Strict, placement, reparse, and the PA9-excluded
  `3898/3898` direct report pass. Fixed performance is -3.06% instructions,
  -1.09% RSS, and -1.86% footprint; the pre-Bimap rolling aggregate is -0.25%,
  -0.89%, and -0.87%. Focused Xpressive passes, and the final exact eight-job
  Bimap graph updates all 205 targets, including Xpressive and six expected
  compile failures, with no unexpected failure or skip. PA9 was not run.

- `2026-07-17`: Cleared Xpressive's target-character deduction failure. Initial
  parsing retained the qualified `declval<U&>` template-id, but the last
  `decltype` fallback checked only unqualified spelling/name dependency flags
  and dropped the operator candidates before substitution. Reusing the already
  computed structured AST dependency signal fixes the uncached algorithm
  without text parsing or a cache; the invalid concrete control still fails by
  SFINAE. The PA22 reducer fails on the controlled parent, every cache-disabled
  result is byte-identical, strict, placement, reparse, the PA9-excluded
  `3897/3897` report, and fixed/rolling performance gates pass. Its first full
  concurrent Bimap replay exposed the independent stale-candidate lifetime
  defect described above rather than an external resource failure. PA9 was not
  run.

- `2026-07-17`: Split Xpressive's `widest_char<void, void>` diagnostic at the
  first incorrect typed deduction. A nested `iterator_range<const Char*>`
  partial was rejected because parameter lookup used only the typed node's
  semantic payload instead of the existing complete typed identity lookup; the
  generic `iterator_range<Char*>` then bound `Char=const char`. The PA21 reducer
  fails on the controlled parent and passes with every relevant cache disabled.
  The fixed Boost header reducer and exact forced target advance to the
  independent target-side `widest_char<void, char>` probe. Clang, GCC 15, PA21
  direct and strict suites, placement, reparse, the PA9-excluded `3896/3896`
  broad report with PA37 object roundtrip `7/7`, and fixed/rolling performance
  gates pass. PA9 was not run.

- `2026-07-17`: Cleared Bimap's `test_bimap_extra` frontier and advanced to the
  independent Xpressive `widest_char<void, void>` alias failure. The reported
  enumerator error was fallout from `core_iterator_type_by` selecting its
  primary after a context-sensitive dependent qualified-member placeholder was
  reused across partial-specialization match scopes. Globally disabling that
  cache makes the exact target pass; the accepted fix instead excludes only
  structurally unsafe patterns while retaining the cache for ordinary types.
  Each match consumes the initial retained AST/type syntax, with no source-text
  reparse. The controlled pre-fix compiler fails the single-base PA22 reducer;
  normal, witness/cache-off, and trace/selection-cache-off LowIR are identical.
  Clang, GCC, placement, strict, reparse, the PA9-excluded broad report, focused
  Boost, and fixed/rolling performance gates pass. PA9 was not run.

- `2026-07-17`: Cleared Bimap's common MP11 frontier and advanced within suite
  11 to `test_bimap_extra`. Recursive pack expansion already held each exact
  semantic type but dropped its syntax when the source pack expression itself
  was unchanged, causing later materialization to retry lexical lookup at the
  wrong declaration point. The fix carries an owned typed expansion node; the
  exact parent fails the non-STL reducer and every cache-disabled result matches
  the normal result. Clang, GCC 15, placement, strict, reparse, the PA9-excluded
  `3894 / 3894` direct report, fixed and rolling performance, the focused target,
  and the complete forced Bimap rerun pass this frontier. The rerun leaves two
  independent positive compile failures; the first is `test_bimap_extra`'s
  unsupported enumerator value. PA9 was not run and will not be rerun for its
  load-sensitive timeout.

- `2026-07-17`: Closed Beast and advanced the V2 cursor to
  `#11 libs/bimap/test`. The two remaining links already requested correctly
  mangled concrete `impl_type` member-template symbols; the resolver had not
  attached their retained definitions because it ignored the direct nested
  class's typed enclosing class-template owner when counting the flattened
  owner header. The controlled parent fails both constructor and ordinary
  member-template bodies, while every cache-disabled output is byte-identical
  after the algorithm fix. No reparse, source scan, cache, symbol, ABI, or
  witness-only path is added. Clang, GCC 15, placement, strict, reparse, the
  PA9-excluded `3893 / 3893` direct report, fixed and rolling performance, both
  exact targets, and the complete 447-target forced Beast suite pass. PA9 was
  not run.

- `2026-07-17`: Closed the final observed Beast `bench/wsload//wsload-compile`
  failure by applying using-directives at their typed namespace injection scope.
  The initial lazy-body parse already retained both the directive and qualified
  type name; semantic lookup incorrectly treated `boost::endian::detail` as a
  block member and hid the nearer `boost::beast::websocket::detail`. The
  controlled parent fails with the qualified-type cache disabled, while every
  normal, individual cache-disabled, and all-disabled output is byte-identical
  after the algorithm fix. There is no text reparse, source scan, cache, symbol,
  or ABI change. Clang, GCC 15, placement, strict, reparse, the PA9-excluded
  `3892 / 3892` direct report, exact Boost, and fixed/rolling performance gates
  pass. PA9 was not run.

- `2026-07-17`: Closed the second Beast `bench/wsload//wsload-compile`
  sub-frontier by preparing a nested class from its retained outer-template
  definition before ordinary out-of-class member-template matching. The
  original parse and typed signature were already correct; the resolver had an
  empty direct member-template bucket and later treated the definition as an
  ordinary member outside the `Body`/`Allocator` scope. The ordinary path now
  shares the existing typed preparation used for special members. There is no
  reparse, source scan, cache, symbol, or ABI change. The controlled parent,
  Clang, cache-disabled, placement, strict, reparse, PA9-excluded direct, exact
  Boost, and performance gates pass. PA9 was not run. The same exact compile
  now reaches the independent local `detail::frame_header` declaration in
  `write_close`.

- `2026-07-17`: Closed the first Beast `bench/wsload//wsload-compile`
  sub-frontier in the typed declarator builder. The original parse already
  retained the pointer-to-function shape and `noexcept`; PA11 type construction
  now consumes that qualifier after the structured function suffix because it
  is not part of a C++11 function type. There is no reparse, source scan,
  fallback AST, cache, or symbol path. Controlled-parent, placement, strict,
  reparse, Clang, GCC 15, PA9-excluded direct, cache-disabled, exact Boost, and
  performance gates pass. PA9 was not run. The same exact compile now fails on
  independent typed `http::request` alias argument resolution.

- `2026-07-17`: Closed Beast `bench/parser//bench_parser` in the original
  lazy-body parse. The parser's class snapshots now carry typed member function
  and member function-template value names, so the nearer `fields` function
  hides the outer namespace type before `fields(db);` is parsed. There is no
  reparse, semantic retry, synthesized AST, source scan, or cache dependency.
  The controlled-parent reducer and final AST establish the grammar change;
  strict, reparse, placement, Clang, GCC 15, PA9-excluded direct, cache-disabled,
  exact Boost, and performance gates pass. PA9 was not run. The next frontier is
  `bench/wsload//wsload-compile`.

- `2026-07-17`: Closed Beast `bench/parser//nodejs_parser` on the initial
  structured declaration path. Both semantic frontends had the anonymous class
  and adjacent array declarator AST, but only accepted a bare object name; the
  full frontend also failed to attach the class `TypePtr` to the anonymous
  source specifier. The PA11 type-model reducer fails on the controlled parent,
  all cache-disabled outputs are identical, and the exact initialized Boost
  table compiles. Strict, reparse, Clang, GCC 15, PA9-excluded direct, and
  performance gates pass. PA9 was not run. The next frontier is
  `bench/parser//bench_parser`.

- `2026-07-17`: Closed both Beast Zlib links by correcting typed out-of-class
  member-template definition matching. A suffix fallback intended for an
  owning class-template head also ran for the non-template `suite` owner, so
  the three-parameter definition matched both its real declaration and a
  neighboring two-parameter overload. Both resolver paths now require a real
  typed owner prefix before dropping parameters. The isolated parent fails the
  PA22 reducer at link; all cache-disabled modes are byte-identical, both exact
  Beast targets pass, and the PA9-excluded direct report passes `3887 / 3887`.
  Placement, reparse, strict, Clang, GCC 15, and performance gates pass. PA9
  was not run. The next frontier is `bench/parser//nodejs_parser`.

- `2026-07-16`: The complete forced Beast survey at `31270cc14` confirms
  `http//type_traits` and all other independently closed targets pass. It finds
  7384 targets, attempts all 447 updates, updates 435, and passes 98 tests. Five
  actions fail with seven downstream skips: `deflate_stream` and
  `inflate_stream` compile but miss concrete forwarding `suite::except`
  specializations at link, while `nodejs_parser`, `bench_parser`, and
  `wsload-compile` have three separate semantic diagnostics. The first observed
  failure, Zlib `deflate_stream`, is the next focused frontier; the other
  actions remain independent leads.

- `2026-07-16`: Closed `http//type_traits` without a cache or text retry. The
  structured qualifier walk already prepared the concrete outer and nested
  class scopes, but its final alias lookup alone omitted class completion on a
  miss. Aligning it with the existing typed bound-owner lookup populates the
  retained out-of-class nested definition before resolving `value_type`. The
  parent compiler fails the PA18-only reducer, while the fixed compiler and all
  cache-disabled modes emit byte-identical LowIR. Placement and reparse audits,
  strict suites, the isolated GCC build, and the PA9-excluded `3886 / 3886`
  direct report pass. PA9 was not run. Fixed performance is -3.27%
  instructions and rolling movement improves by 0.06 percentage points. The
  exact target rebuilds all 46 targets and passes compile, link, and runtime.

- `2026-07-16`: Closed the first `http//type_traits` assertion without a text
  fallback or cache. The parser had retained both the typed qualified call and
  the template argument's original token anchor; structured type-argument
  resolution simply failed to pass that anchor to the existing `decltype`
  rebasing path. Source-order filtering consequently compared real declaration
  positions with fragment-relative use positions and removed both valid
  overloads. The PA21 reducer, all cache-disabled modes, placement and reparse
  audits, strict suites, isolated GCC build, and PA9-excluded `3885 / 3885`
  direct report pass. PA9 was not run. Fixed performance is -3.21%
  instructions and rolling movement is +0.04%. The exact target rebuilds 46
  targets and advances to its independent nested iterator `value_type` failure.

- `2026-07-16`: Closed `http//message` by applying the existing typed implicit
  deletion rules to explicitly defaulted default constructors. The constructor
  state had never been refreshed after class completion, and the single-type
  builtin constructibility path independently ignored `is_deleted`. The final
  implementation scans only the class constructor bucket, adds no cache or text
  recovery, and is identical in every cache-disabled mode. The exact target,
  focused reports, strict suites, placement and reparse audits, GCC build, and
  performance gate pass. The immutable survey's next ordered failure is
  `http//type_traits` at its retained `decltype` call/dereference assertion.

- `2026-07-16`: Closed `http//file_body` without a recursion guard or cache.
  `__is_convertible` was correctly requesting implicit conversion, but explicit
  constructor templates were still deduced before being rejected, so Beast's
  explicit forwarding-constructor default recursively asked the same trait.
  Filtering the typed declaration before deduction makes the algorithm
  terminate. The later link failure was independent: concrete nested
  reader/writer declarations and their stored definitions shared exact AST
  identity but lacked a nested class-template pointer, so typed definition
  replay now accepts the shared class declaration and emits C1/C2. Both
  header-free reducers, every cache-disabled mode, placement and reparse
  audits, strict suites, and the PA9-excluded `3882 / 3882` direct report pass.
  PA9 was not rerun. Fixed performance is -3.19% instructions, and rolling
  movement is -0.166%. The exact target rebuilds 46 targets and passes; a
  complete forced Beast survey will establish the next frontier.

- `2026-07-16`: Closed the libc++ `nextafter(double, float)` ambiguity in the
  exact Beast `basic_parser` target. Deduction already selected the generic
  arithmetic template, but its retained variadic fold alias result was sent to
  ordinary expression analysis and stayed dependent. Both typed `decltype`
  paths now reduce only bound fold AST nodes through the existing constexpr
  evaluator, without reparsing or copying unchanged operands. The PA34 reducer,
  all cache-disabled modes, placement and reparse audits, strict suites, and the
  PA9-excluded `3880 / 3880` direct report pass. PA9 was not rerun. Fixed
  performance is -3.03% instructions and rolling movement is -0.025%. The exact
  forced parser graph updates all 46 targets and passes; a complete forced Beast
  survey will establish the next frontier.
- `2026-07-16`: The exact full Beast survey at `a9f116ad6` completed without a
  timeout after 1059 seconds, passing 89 tests and leaving 14 failed actions.
  Its first observed failure was an unqualified explicit member-template call
  inside a default-capturing lambda. A header-free PA24 reducer proved that the
  uncached algorithm missed `this` only for a retained `TemplateIdSyntax`;
  explicit `this->` and non-template controls already passed. Capture discovery
  now consumes that initial structured identifier. Every cache-disabled LowIR
  is byte-identical, the reparse and placement audits stay at zero, strict
  passes, and the PA9-excluded direct report passes `3879 / 3879`. PA9 was not
  rerun. Fixed perf is -3.01% instructions and rolling movement is +0.029%.
  The forced exact target clears the lambda failure and advances to a separate
  libc++ `nextafter(double, float)` overload ambiguity.
- `2026-07-16`: Closed `core//static_string` through typed primary-placeholder
  source ownership, retained operator identity, initializer-list ranking, and
  declaration/result identity for out-of-class member-template definitions.
  The first failure reproduces with all caches disabled, and both checked-in
  reducers are byte-identical in normal, every individual cache-disabled, and
  all-disabled modes. The exact target passes compile, link, and runtime; the
  reparse audit remains at zero, strict passes, and the final direct report
  excluding PA9 passes `3878 / 3878`. PA9 was not rerun. The rolling perf gate
  passes at +0.95% instructions with lower RSS; sampling confirms the new
  matching paths are not dominant. The next step is the next ordered Beast
  survey failure.
- `2026-07-16`: Closed `core//span` through retained AST and typed
  `abi_mangle` facts. The enclosing class's concrete extent, the namespace
  constant in `enable_if`, owner packs, source-written defaults, and entity
  linkage now remain structured through symbol emission; no text is reparsed.
  The exported reference compiler independently reproduces the PA22 reducer's
  pre-fix ABI failure, while Clang validates the corrected exact symbols and
  internal-linkage reference updates. Every cache-disabled mode is
  byte-identical, the reparse audit stays at zero, strict passes, and the full
  direct report excluding PA9 passes `3876 / 3876`; PA9 was not rerun. The
  exact forced target rebuilds 46 targets and passes compile, link, and runtime.
  Performance is -3.95% instructions versus the fixed baseline and +0.080%
  versus the preceding checkpoint, below the sampling threshold. The next
  independent lead is an exact `core//static_string` replay.
- `2026-07-16`: The layered Beast buffer link failure was not an output-policy
  omission. Typed collection trace showed the equal-sized class-template owner
  head being treated as a member-template head and binding the ordinary copy
  assignment definition to the adjacent cross-allocator overload. Correcting
  the owner-prefix boundary leaves no member-template suffix at equal arity;
  nested member-template definitions remain unchanged. The PA21 reducer proves
  the pre-fix unresolved symbol and fixed runtime behavior, every cache-disabled
  mode is byte-identical, and both exact Beast targets pass. PA9 was explicitly
  omitted from the `3870 / 3870` broad report and was not rerun.
- `2026-07-16`: Reduced the Beast buffer failure to an inherited member-template
  call in a method containing `static_assert`. Early assertion validation ran
  while the derived class was still collecting members, before the selected
  implicit-object adjustment could be materialized. A first completion bypass
  compiled Beast but a PA26 non-primary-base reducer proved it cached offset `0`
  before layout, differed from cache-disabled LowIR, and miscompiled at runtime.
  That bypass was rejected. Validation now runs after finalized class layout;
  both PA21 and PA26 reducers are byte-identical with every cache disabled, the
  full report excluding PA9 passes, and both exact Beast objects compile. The
  next layered `flat_buffer` failure is missing copy-assignment emission at link.
- `2026-07-16`: Closed `core//file_posix` and `core//file_stdio`. Their shared
  lambda helper has a defaulted second parameter, but the captureless-lambda
  free-function shortcut decayed the lambda to a function pointer and lost the
  typed default-argument contract. Only lambdas with parsed default-argument
  nodes now use the existing closure/operator path. The PA24 reducer, all cache
  controls, strict suites, placement/reparse audits, and the full report
  excluding PA9 pass; both exact Beast targets pass a 50-target forced rebuild.
  The cumulative performance gate remains -4.22% instructions, -1.37% RSS, and
  -1.05% footprint. A fresh complete forced survey will establish the next
  ordered Beast frontier.

- `2026-07-16`: The complete forced Beast survey at immutable commit
  `55e666c62` completed in `766.4s`: 7384 targets found, 447 attempted, 373
  updated, 53 skipped, 21 failed, and 81 tests passed. It independently
  confirmed `detect_ssl`, `async_base`, and `basic_stream`; compared with the
  preceding survey it added four updated targets, removed three skips, and
  removed the `detect_ssl` failure. The checked-in Jamfile order establishes
  `file_posix` as the next independent frontier, with `file_stdio` sharing its
  failing helper. Evidence is in
  `/tmp/boost-frontier-v2-suite-018-detect-ssl-adl-final`.

- `2026-07-16`: Closed `core//detect_ssl` by removing a second ADL pass from
  synthetic operator calls whose candidate scope was already fully collected.
  The PA18 reducer preserves the exact point-of-definition visibility failure,
  all cache-disabled LowIR is byte-identical, the reparse and placement audits
  remain clean, and the full direct report excluding PA9 passes `3866/3866`.
  The exact target force-rebuilds 46 targets and passes. Against the fixed
  baseline, instructions improve to -4.22%, RSS to -1.80%, and footprint to
  -1.02%. Old/new hotspot counters and 1 ms samples attribute the large gain to
  the eliminated duplicate ADL and downstream template-deduction work. A fresh
  complete forced Beast survey will establish the next frontier.

- `2026-07-16`: The full forced Beast survey at immutable commit `0a9a22d6d`
  completed in `737.2s`: 7384 targets found, 447 attempted, 369 updated, 56
  skipped, 22 failed, and 81 tests passed. Both `async_base` and `basic_stream`
  pass independently in this full run. Eight-worker completion order exposed
  `span` first in the live tail, but the checked-in Beast core Jamfile places
  `detect_ssl` first among the remaining failures. Its header-level expression
  compares two values of the same enum type; cppgm omits the exact built-in
  equality candidate and reports only three worse user-defined conversions.
  The next step is a header-free reducer and earliest-owner placement. Evidence
  is in `/tmp/boost-frontier-v2-suite-017-local-rtti-owner-final`.

- `2026-07-16`: Closed `core//async_base` through the typed `abi_mangle` path.
  The malformed but nominally constructed semantic owner for
  `result<T0>::type` had suppressed the retained typed template-id owner; the
  fix requires constructed owner IR to be emittable before accepting it. The
  PA33 no-header reducer matches Clang's exact `_ZTI` and `_ZTS` names, all
  cache-disabled outputs are identical, text-reparse and placement audits stay
  clean, the full direct report excluding PA9 passes `3865/3865`, and the
  forced target passes compile, link, and runtime. The performance gate is
  -0.47% instructions, -0.61% RSS, and +0.12% footprint versus the fixed
  baseline. A fresh forced Beast survey will establish the next frontier.

- `2026-07-16`: Re-ran the complete forced Beast suite at immutable commit
  `509b7c962`. The survey completed in `782.9s` with 23 failed targets and
  independently reconfirmed that `basic_stream` passes in the full suite. The
  first ordered independent failure is `core//async_base`: LowIR cannot derive
  the host typeinfo-name symbol for the named function-local class `op` inside
  a function-template instantiation. The diagnostic is already reached through
  `symbol_linkage::typeinfo_name_symbol_for_type`; the next step is to repair
  missing typed ABI-mangle context, without rendered-name or source-text
  fallback. Evidence is in
  `/tmp/boost-frontier-v2-suite-016-value-init-zero-final/libs__beast__test.log`.

- `2026-07-16`: Traced the four `basic_stream` runtime failures through the
  handler ABI and socket layer to an uninitialized host `msghdr`. The typed
  lifetime action for an empty same-type functional cast had lost its
  value-initialization fact, and the whole-variable trivial-constructor
  lowering consequently emitted no zero stores. The PA15 non-STL reducer
  covers the initial semantic path; PA20 and PA21 direct-reference fallout was
  audited rather than masked. Redundant whole-object static zero actions are
  now omitted. All cache-disabled outputs, reparse, placement, direct, strict,
  Boost, and performance gates pass; a fresh full forced Beast survey will set
  the next ordered frontier.

- `2026-07-16`: Completed pointer-like member-pointer invocation on the typed
  path used by libc++ `__mem_fn`. Both dependent result substitution and
  actual `__builtin_invoke` lowering now apply `operator*` when the object is
  neither the member owner nor a raw pointer. The PA34 no-header reducer covers
  generic SFINAE, the libc++ wrapper shape, member functions, member data, and
  runtime execution. All cache-disabled outputs are byte-identical, the text
  reparse audit remains zero, and the direct, strict, placement, and
  performance gates pass. Focused Beast now compiles, links, and executes all
  243 checks, exposing four async-read runtime mismatches; host Clang passes the
  same target, so those checks are the next compiler frontier.

- `2026-07-16`: Applied an explicit member-function-pointer `static_cast`
  target before generic operand analysis, reusing the existing structured
  overloaded-member selector. The PA26 no-header reducer proves the outer
  function-template candidate was dropped only because its cast operand was
  analyzed without the target. Normal and every cache-disabled result are
  byte-identical, the text-reparse audit remains zero, and all direct, strict,
  placement, and performance gates pass. The exact forced Beast survey
  improves from 353 updated / 26 failed / 68 skipped to 357 / 25 / 65 and
  advances `basic_stream` to libc++ `__mem_fn::operator()` result substitution.

- `2026-07-16`: Kept qualified `decltype` types in the initial structured
  parser. A followed `::` now routes both declaration and type-id forms through
  the existing qualified-type path, retaining typed AST sidecars without any
  later source or semantic text replay. The PA15 non-STL reducer covers both
  grammar contexts, every cache-disabled mode is byte-identical, and the strict
  reparse audit remains zero. Focused Beast, strict and full PA9-excluded direct
  reports, placement, and performance gates pass. The forced survey improves
  from 345 updated / 28 failed / 74 skipped to 349 / 27 / 71 and establishes
  `basic_stream.cpp:1241` as the next ordered frontier.
- `2026-07-16`: Made ADL's associated-base traversal stable on its first
  uncached computation. A concrete class-template specialization now has its
  retained reference shape collected before ADL walks its bases, so deduction
  no longer changes after unrelated later semantic work. The PA18 non-STL
  reducer fails on a controlled pre-fix rebuild, all nine cache-disabled modes
  remain byte-identical, and the text-reparse audit stays at zero. The focused
  `bind_handler` target and all repository gates pass. The full forced Beast
  survey improves from 341 updated / 29 failed / 77 skipped to 345 / 28 / 74
  and establishes `buffers_cat.cpp:40` as the next ordered frontier.
- `2026-07-16`: Cleared the `buffers_adaptor` mutable-to-const conversion
  assertion by preserving the out-of-class nested definition's structured
  parameter name. The forward declaration owns the default metadata and the
  definition supplies `isMutable`; the existing typed redeclaration merge now
  combines both before concrete member-template instantiation. The PA21
  reducer fails without the merge, passes Clang and cppgm native execution,
  and stays byte-identical with all caches disabled. The focused Beast target,
  strict and full PA9-excluded direct reports, placement audit, zero-reparse
  audit, and three-run performance gate all pass. The next full forced Beast
  survey will establish the next ordered frontier.
- `2026-07-16`: Fixed the original `buffers_adaptor` explicit-instantiation
  failure from retained AST and typed owner scopes. Deferred out-of-class
  definitions now follow concrete nested member class-template clones through a
  member-only AST identity index, and the deepest matching template head owns
  the definition. A broader unresolved-owner output-node assignment was proven
  wrong by libc++ `sentry` and removed. The PA21 runtime reducer, all
  cache-disabled modes, strict and full PA9-excluded direct reports,
  zero-reparse audit, and fixed performance gate pass. The next frontier is the
  independent mutable-to-const buffer sequence convertibility assertion.
- `2026-07-16`: Fixed dependent call-expression SFINAE for const callable
  objects. The original AST and typed `const callable&&` binding were already
  correct; only the leaf call resolver discarded cv before implicit-object
  viability. The PA22 reducer, all cache-disabled modes, focused Beast runtime,
  zero-reparse audit, full report without PA9, and fixed performance gate pass.
  The next ordered Beast frontier is `buffers_adaptor.cpp:28`.
- `2026-07-16`: Corrected the named-type declaration-start prefilter so valid
  sequences such as `std::uint32_t constexpr constants[4]` reach the initial
  grammar parser. A PA19 non-STL reducer catches the syntax at its earliest
  owner. This investigation also found that `cppgm-b2-toolchain.sh` defaulted
  to an older checkout; the reported `stream_state.hpp:62` frontier is
  superseded, and exact commands now bind `CPPGM_B2_CXX` to this worktree.
  The current compiler builds the exact `lib_beast.cpp` command successfully,
  the full direct report passes `3854/3854` with PA9 omitted, and performance
  remains within every fixed and rolling gate.
- `2026-07-15`: Shared the existing AST-only `sizeof(type-id)` ambiguity
  recovery with constexpr evaluation and made class constant evaluation safe
  across reference-to-full completion. Beast's `chacha::state_size` now
  evaluates from the retained `chacha::keysetup_` node and typed array binding;
  no text is parsed or reconstructed. The exact shared Beast compile advances
  to `stream_state.hpp:62`, all cache-off outputs match, the full direct report
  passes `3853/3853` with PA9 omitted, and rolling instructions improve 0.05%.
- `2026-07-15`: Corrected hidden-friend visibility in the uncached lookup
  algorithm. A hidden friend remains a namespace entity for linkage, but is no
  longer exposed by real ordinary, qualified, or namespace-wide ADL lookup.
  The existing typed associated-class friend collection is now its only ADL
  entry point, and synthetic ADL scopes retain only friends already selected
  there. This removes Asio's unrelated `executor(flags)` conversion probes and
  the pathological `executor::impl<flags>` output. Exact `lib_asio.cpp` now
  compiles, the full direct report passes `3851/3851` with PA9 omitted, all
  cache-off and reparse checks remain clean, and rolling instructions improve
  by 1.44%.
- `2026-07-15`: Attached the durable, already-bound out-of-class definition
  scope when replay installs an ordinary class-template member body. This makes
  a renamed owner parameter available on the first semantic analysis of the
  retained AST, matching the existing selected-specialization path. The fix is
  cache-independent and introduces no text or symbol recovery. Exact Beast
  replay clears `buffers_suffix.hpp:146` and compiles the complete focused
  translation unit; the full direct report passes `3850/3850` with PA9 omitted.
- `2026-07-15`: Fixed out-of-class member-function-template definition replay
  when an in-class non-type parameter is unnamed and receives its usable name
  only on the definition. The compiler now merges the already parsed definition
  parameter metadata into the resolved declaration and concrete replay
  candidates before analyzing the retained body AST. This is a first-analysis
  scope repair with cache-disabled byte identity, not a cache or text reparse.
  Exact Beast replay advances from `variant::emplace<I>` to the independent
  `buffers_suffix` trait assertion.
- `2026-07-15`: Fixed Asio's `prefer_free`/`call_traits` order dependency by
  enforcing point-of-definition visibility in the structured dependent-call
  leaf path. The bug was in the uncached algorithm: a later ordinary CPO object
  was admitted into an earlier dependent expression and reentered the trait
  during reference collection. Stable source locations preserve valid local
  declarations while excluding the later namespace value. Normal and all
  cache-disabled outputs are identical, the full direct report passes
  `3848/3848` with PA9 omitted, cumulative instructions improve to +0.68%, and
  the exact target advances to `variant::emplace` in `serializer.hpp:51`.

- `2026-07-15`: Fixed Beast's dependent-base assignment using-declaration by
  materializing the concrete base's implicit special members before the normal
  typed visible-member lookup. A PA18 runtime reducer proves the imported copy
  assignment is semantically callable, every relevant cache-disabled mode is
  byte-identical, and overload tracing proves the witness coalescing does not
  remove candidates from resolution. The strict suites and full direct report
  pass, the text-reparse audit remains at zero, cumulative instructions improve
  to +0.73%, and the exact target advances to qualified `prefer(t, p)` lookup at
  `boost/asio/prefer.hpp:482`.

- `2026-07-15`: Fixed Beast's in-class defaulted member-template trailing-return
  trait entirely from the initial AST and typed semantic model. Unevaluated
  probing no longer materializes bodies or output, member candidates survive
  class-completion invalidation through typed identity refresh, and built-in
  comma preserves the probe result type. Six cache-disabled modes plus the
  all-disabled run are byte-identical. The full direct report passes `3846/3846`
  with PA9 omitted, the three-run performance median improves to +0.78%, and
  the exact target advances to the independent `CancellationSlot::operator=`
  using-declaration frontier.

- `2026-07-15`: Deferred Beast's out-of-class nested trailing return from the
  original parsed AST until the nested owner is concrete. The storage path now
  consumes the qualified owner already established by the initial semantic
  walk, matching the eager path, and all cache-disabled outputs are identical.
  The exact forced survey has no remaining `chunk_encode.hpp:304` failures and
  advances the focused HTTP frontier to `type_traits.hpp:94`.

- `2026-07-15`: Bound the retained parameter list of Beast's out-of-class
  nested definition to the concrete owner's typed instantiation arguments
  before class population. The original `sizeof...` AST now reads the ordinary
  named type-pack binding; no text replacement or cache is involved. The exact
  forced survey has zero old pack-assertion failures and advances to the
  independent trailing-return `operator*` definition at `chunk_encode.hpp:304`.

- `2026-07-15`: Fixed the Beast nested-constructor symbol through the typed ABI
  path. The retained renamed owner argument is mapped by its `TypePtr` to the
  primary parameter index, then represented with the existing typed template
  parameter and pack-expansion facts. No ABI fact, scaffold extension, text
  parse, rendered-symbol fallback, or cache was needed. The exact forced survey
  contains no remaining `buffers_cat_view` symbol failures and moves the ordered
  frontier to concrete `sizeof...(Bn)` evaluation at `buffers_cat.hpp:110`.

- `2026-07-15`: Corrected Beast's first shared failure without a cache. A
  dependent argument pack no longer masquerades as a fixed one-argument class
  specialization, and the concrete nested owner now receives its retained
  out-of-class member definition through the ordinary finalization path. The
  regression is byte-identical with every relevant cache disabled. The exact
  forced Beast rerun has no remaining `unsupported template declarator`
  diagnostics and exposes the next typed ABI frontier at
  `buffers_cat.hpp:335`.

- `2026-07-15`: Closed Atomic after fixing the shared typed behaviors exposed by
  its API matrix and adding structured initial-parse GNU asm operands. The asm
  lowering consumes typed operand expressions and exact instruction templates;
  it does not recover operands from clause text. Complete-class trait results
  and asm LowIR are byte-identical with all six relevant caches disabled. The
  exact forced suite passes all 90 rebuilt targets, the final direct report
  passes `3840/3840` with PA9 omitted, and the cursor advances to
  `libs/beast/test`.

- `2026-07-15`: Closed Assign after fixing the independent
  `multi_index_container` lookup. Initial parsing and substitution had retained
  the nested template-id; semantic lookup rejected it because the active typed
  anchor search ignored template arguments and compared an unqualified source
  spelling with a fully qualified lookup spelling. The sidecar-aware search
  fixes the uncached algorithm without parsing text. The exact forced suite
  rebuilt 84 targets and all 14 tests passed. The final direct report passes
  `3836/3836` with PA9 omitted, and the cursor advances to
  `libs/atomic/test`.

- `2026-07-15`: The Assign `array` and `list_of` failures shared a defaulted
  conversion-function-template defect, not an initial-parse loss, text reparse,
  or cache dependency. Retained substituted AST now controls concrete default
  viability, including pointer-to-reference SFINAE rejection, and the typed ABI
  path omits conversion-template result encoding. The PA30 model already had
  every required fact; a combined assignment regression closes the coverage
  gap. Both focused Boost targets pass, while the independent
  `multi_index_container` structured lookup remains the active frontier.

- `2026-07-15`: Closed Assert without a compiler change. The exact forced run
  updated 72 targets and all 18 tests passed. The cursor advances to
  `libs/assign/test`.
- `2026-07-15`: Closed Asio after the `strand` fix. The uninterrupted exact
  forced prefix recorded 210 passes and no failures before its external
  timeout. Every in-flight timeout target was separately force-rebuilt and
  passed, then the full continuation updated 680 targets, ran 285 passing
  tests, and exited successfully with no skips or failures. Sampling the
  unusually large `execution/mapping` tail showed LowIR hidden virtual-base
  argument emission as the dominant path; it is retained as a separate
  performance lead because the fixed aggregate benchmark is flat and the
  accepted `strand` change is not on that sampled path.
- `2026-07-15`: The Asio `strand` link failure was an output-closure defect,
  not an initial-parse loss, text reparse, ABI spelling error, or cache-masked
  algorithm. The nested executor specialization retained a layout-free named
  `TypePtr`, while its typed `ClassInfo` was already complete. Full-expression
  temporary collection incorrectly used the stale layout bit as class
  identity and omitted the concrete executor destructor definition. The
  accepted fix preserves normal layout completion and adds only a typed class
  lookup for layout-free named prvalues. A controlled pre-fix compiler fails
  the PA21 reducer at host link, all cache-off modes emit identical fixed
  LowIR, both Boost targets pass, the strict text-reparse audit remains zero,
  and performance is flat to slightly improved. The suite remains open for a
  forced continuation survey.
- `2026-07-15`: The POSIX descriptor declaration was collected correctly; its
  dependent trailing result was recovered in a scope with no function or
  `this`, so the nested functional cast failed before the member became
  callable. The accepted fix consumes the initially parsed `KW_THIS` node and
  constructs a typed method scope only for that still-dependent member result.
  A broad first attempt broke the stable self-compile benchmark and was
  discarded before validation. The narrowed implementation passes cache-off,
  Boost, strict, zero-reparse, full-report, and performance gates; the Asio
  cursor advances to the independently observed `strand` link pair.
- `2026-07-15`: The `ip_tcp` failure was an uncached structured-identity bug in
  a pre-overload type probe, not initial parse loss, semantic text reparsing, or
  missing function-template deduction. The parser retained the explicit call
  and dependent `::template rebind` type. Display normalization later omitted
  that keyword, causing an exact spelling comparison to reject the call before
  ordinary overload lookup. Identifier-checking the active exact anchor lets
  the existing typed arguments flow through unchanged. All nine relevant
  cache-disabled modes emit byte-identical LowIR, the strict reparse audit stays
  at zero, and cumulative instructions improve to +0.89%. Both focused Boost
  targets pass. The broad survey recorded 286 passes, the next POSIX compile
  pair, and a later `strand` link pair before it was stopped after exceeding the
  documented timeout. The final direct report passes `3831 / 3831` with PA9
  explicitly omitted as requested.

- `2026-07-15`: Asio's buffer failure was a typed overload-set
  classification bug, not initial parse loss, semantic text reparsing, or a
  cache repair. The numeric literal token and raw character-pack path already
  existed; the selector confused an unrelated two-parameter string literal
  operator with the one-parameter cooked numeric form. Exact typed parameter
  inspection fixes both integer and floating raw literals. Normal and
  all-cache-disabled LowIR are byte-identical, the strict reparse audit remains
  zero, and the repeated cumulative median improves to +0.92% instructions.
  The exact forced survey clears both buffer variants, records 190 passes and
  four later failures, and advances to the `ip_tcp` pair. The final direct
  report passes `3830 / 3830` with PA9 explicitly omitted as requested.

- `2026-07-15`: The bound-handler failure was an uncached partial-specialization
  matching defect, not initial parse loss, text reparse, or a cache repair. The
  parser had retained the dependent `decltype` call below template-id argument
  sidecars, but the concreteness scan never visited those structured nodes and
  reused a stale declaration-time type. The narrow sidecar-aware check now
  reevaluates that retained typed syntax after deduction. Normal and cache-off
  diagnostic runs emit byte-identical LowIR, all focused gates pass, and the
  optimized three-run median is +0.96% instructions. The forced survey clears
  the allocator and immediate-executor handler families, records 192 passes
  plus six later failures, and advances to raw integer buffer-literal overload
  selection. The final direct report passes `3829 / 3829` with PA9 omitted as
  requested.

- `2026-07-15`: The `append` fatal was not an initial parse, reparse, cache, or
  LowIR algorithm defect. Semantic analysis had a concrete enclosing `Bits=4`
  binding, but nested member-template substitution left the direct typed
  `id_expression` symbolic in the returned specialization key. Materializing
  that existing typed binding makes the semantic identity, layout, and typed
  ABI path agree. The reducer and all six cache-disabled modes emit identical
  LowIR. The three-run fixed-baseline median is +0.90% instructions, and
  sampling shows no pathological new path. Focused `append` targets pass; the
  exact survey clears all prior `Bits` failures and records 175 passes plus 12
  later failures before timeout. The suite advances to `bind_allocator`, and
  the final direct report passes `3828 / 3828` with PA9 omitted as requested.

- `2026-07-15`: The allocator converting-constructor diagnostic was a
  downstream symptom. A detector-selected `allocator_traits_rebind` partial
  specialization chose the right member alias, but stale semantic annotations
  and reuse of a current-class-localized qualifier prevented the substituted
  qualified nested template-id from resolving generally. The typed fix passes
  with all six relevant caches disabled, preserves resolved template argument
  facts, and keeps the text-reparse audit at zero. Focused
  `any_completion_executor` now passes. The exact forced full survey records
  160 passes and 28 compile failures before its 1800-second timeout; the first
  remaining family is 14 concrete-layout requests that retain symbolic `Bits`.
  The final direct report passes `3827 / 3827` with PA9 omitted under the user's
  existing load-timeout exemption.

- `2026-07-15`: The initial forced Asio survey timed out after 1800 seconds and
  exposed several independent semantic families. The first ordered focused
  target showed that initial parsing had already retained both
  `void(P0,P1,PN...)` type arguments; semantic pack substitution expanded only
  one occurrence. Direct typed function-parameter expansion fixes that defect
  with all caches disabled and no text parse. The first broad report caught an
  over-broad pointer-to-member annotation in PA26, so the accepted algorithm is
  restricted to direct function declarators. The focused Boost target now
  reaches the independent allocator converting-constructor frontier. The final
  report passes every assignment except PA9, which was not rerun per the
  load-timeout exemption.

- `2026-07-15`: Closed Array after three independent typed fixes. Structured
  constexpr aggregate access cleared the initialization/copy/get group;
  subobject storage identity cleared the pointer/data/iterator group; and a
  non-template reducer proved `to_array` was a general array cv-reference
  ranking defect rather than template deduction or a cache-masked algorithm.
  Every reducer passes with the relevant caches disabled, all 23 text-reparse
  categories remain zero, the complete forced suite rebuilds all 146 discovered
  targets successfully, and the final direct report passes `3836 / 3836`.
  Cumulative instructions are +0.73% after optimization and sampling shows no
  new hotspot. Suite 5 is closed; the cursor advances to `libs/asio/test`.

- `2026-07-14`: The remaining Any failure was not semantic text reparsing.
  Initial parsing classified the already-tokenized `&&` in a qualified type
  template argument as expression-only, then carried only its display text to
  semantics. Adding `OP_LAND` to the existing type-then-expression classifier
  retains the structured type-id at the first parse. The non-STL PA18 reducer
  passes with both semantic caches disabled, all validation and performance
  gates pass, both focused move targets pass, the complete forced Any survey
  exits successfully, and the final direct report passes `3833 / 3833`. Suite
  4 is closed; the cursor advances to `libs/array/test`.
- `2026-07-14`: The initial forced Any survey found eight no-RTTI failures in
  Boost.Describe out-of-class definitions of nested member-template static data
  plus a later move-cast failure. The owner prefix was already structured at
  initial parse, but a synthetic `type_name` incorrectly activated the ordinary
  missing-`typename` rule. Marking that typed nested-name-specifier context
  clears all eight failures normally and with both semantic caches disabled.
  PA21, strict, placement, and zero-reparse gates pass; cumulative instructions
  are +0.80%. The forced full suite now leaves only `unique_move` and its
  no-RTTI variant, so suite 4 remains open at that independent frontier.

- `2026-07-13`: Started V2 at `db9879223` because removal of semantic text
  reparsing and the following template-resolution fixes may have changed suites
  previously marked passing.
- `2026-07-13`: Reset suite credit to zero. The V1 tracker remains historical
  evidence and is not a V2 cursor.
- `2026-07-13`: Chose `libs/accumulators/test`, inventory row 1, as the first
  suite.
- `2026-07-13`: Made the start-commit performance baseline immutable. Rolling
  baselines may characterize a fix but cannot replace the fixed cumulative
  gate.
- `2026-07-13`: Completed the baseline bootstrap. The fixed three-run baseline
  was measured in a detached worktree at exact commit `db9879223`; the active
  and main branch tips at `2acedcec7` contain only V2 process documents and
  refreshed assignment references beyond that production compiler baseline.
- `2026-07-13`: Accepted one PA9 generated-program timeout in the full report
  as the known load-sensitive test behavior after its isolated direct-LowIR
  report passed `11 / 11` immediately.
- `2026-07-13`: The first forced V2 Accumulators run completed `mixed` in
  491.7s. Classified its earliest causal failure as an initial-parser frontier
  in a Boost.Serialization dependency rather than the later parallel target
  diagnostics.
- `2026-07-13`: Reduced the first frontier to an initial-parser failure on
  `friend class detail::interface_archive<T>;`. The typed class-specifier parse
  now accepts the unambiguous qualified template-id without reparsing text. The
  regression lives at the `pa21:300` template-friend owner, not PA10, under the
  current placement rules.
- `2026-07-13`: The forced `count` rerun cleared the parser failure and advanced
  `count.o` to `mp_bool` alias-template resolution. The same parallel run also
  observed independent Boost.Test `format_report` lookup and Boost.Regex
  template-id lookup failures; these are later dependency frontiers, not the
  earliest causal failure for the focused target.
- `2026-07-13`: Reduced `mp_bool` to a non-STL qualified static member-template
  call whose substituted owner type lives in `qualifier_type_syntax`. Both call
  lookup paths now use that typed owner directly. The forced single-job rebuild
  compiled `count.o` and established Boost.Test `format_report` as the next
  ordered target frontier; Boost.Regex remains a separate later dependency
  frontier.
- `2026-07-13`: Traced the apparent `format_report` lookup failure through
  successful lookup and deduction to typed ABI symbol construction for
  `__builtin_va_list`. Kept parameter adjustment distinct from the underlying
  array type, matched both Clang symbols, and verified that the existing PA30
  typed model needs no new fact or scaffold primitive. The forced rebuild now
  identifies Boost.Regex current-specialization lookup as the sole ordered
  blocker.
- `2026-07-13`: Debug stacks proved that the Regex failure was not parsing or
  cache behavior. Two normal semantic consumers discarded the already parsed
  member-pointer operand and used `QualifiedName`-only function lookup. Both now
  retain the AST node through typed lookup. The forced `count` rebuild passes,
  both Regex POSIX objects compile, and the full survey advances to independent
  Accumulators target failures.
- `2026-07-13`: The post-fix forced full-suite survey updated 114 targets and
  failed 37 compile targets. The first reported target-local failure was
  `covariance`, in libc++ `complex<float>` member-template constructor
  collection. Treat that as the next candidate only after a focused one-job
  rerun confirms it outside parallel scheduling.
- `2026-07-14`: Traced the final `covariance` failure past successful typed
  template-template binding. Substitution had overwritten the bound semantic
  head with its qualified display name, causing generic lookup to re-resolve
  the rendered owner. Preserving the typed binding fixes the uncached algorithm;
  the non-STL reducer, focused target, and full-suite covariance targets pass,
  with cumulative instructions down 0.13% from the immutable baseline.
- `2026-07-14`: The forced post-fix full survey updated 154 targets and failed
  28. The next ordered target is `extended_p_square`; 22 compile failures share
  its Boost.Random `splitmix64` member-definition diagnostic. Later independent
  classes are one MPL constant-evaluation failure, two ambiguous-base failures,
  and three serialization runtime failures.
- `2026-07-14`: Reduced the Boost.Random failure to a parenthesized member name
  followed by an outer `noexcept`. The class-model guard incorrectly classified
  the simple nested identifier as a complex returned declarator and bypassed
  the existing typed qualifier filter. Refining that structural predicate
  clears all 22 shared failures with instructions down 0.13% from the immutable
  baseline. The forced survey timed out while targets were still completing;
  a non-forced continuation completed the graph and established 13 remaining
  targets in six independent failure classes. `extended_p_square` remains the
  ordered target, now at visible `check_frwd` template candidate loss.
- `2026-07-14`: Traced the apparent `check_frwd` loss back through
  `iterator_traversal_t` to eager materialization of every alias argument. The
  compiler was instantiating an unused default that the selected explicit
  argument made irrelevant. Removing that eager normal-path work exposed stale
  all-type pack mangle metadata; the final substitution rule refreshes only
  fully concrete type-only argument lists while preserving dependent non-type
  expressions. Cache-disabled controls pass, and cumulative instructions fall
  0.27%. The focused target and all three affected suite tests pass. The forced
  survey reached its 1800s bound with four active targets; its continuation
  completed with ten independent failures. `error_of` is next.
- `2026-07-14`: The `error_of` diagnostic was downstream of a stale typed
  partial-specialization pattern, not failed MPL constant evaluation. The first
  argument had deduced `Sequence`, but the reused non-deduced `enable_if`
  pattern still carried its placeholder binding and selected the wrong
  iterator tag. The final algorithm runs the existing dependent resolver first
  and performs complete typed substitution only when the pattern remains
  dependent. Both semantic caches can be disabled without changing the result;
  no correctness cache or text reparse was introduced. The focused target
  passes compile, link, and runtime. Cumulative instructions are +0.75% after
  improving the eager implementation's +0.94%; hotspot counters and sampling
  show no pathological recursion or witness-only work on the normal path.
- `2026-07-14`: The post-`error_of` forced survey reached its 1800s bound after
  exercising every failure class. All five prior MPL/non-type compile failures
  pass. The remaining compile failures were `rolling_mean` and
  `rolling_variance`, both from the same declaration being reached through two
  nonvirtual `rolling_window_size_<0>` base subobjects; three rolling
  persistence tests separately failed base64 decoding at runtime.
- `2026-07-14`: Reduced the duplicate-base diagnostic to static data and static
  function lookups in a header-free PA26 multiple-inheritance case. Hierarchy
  lookup had incorrectly required a unique object subobject for every inherited
  entity. Classifying the typed lookup result preserves unique-path enforcement
  for fields and nonstatic callables while allowing one static or nested entity
  reached by multiple paths. The focused targets now compile and link. The
  forced full survey reached its bound with 42 passes, the exact five runtime
  failures, zero compile failures, and only previously passing
  `weighted_tail_variate_means` still active. Cumulative instructions are
  +0.74%, and the next ordered frontier is the `rolling_mean` lazy-window
  result behavior.
- `2026-07-14`: Traced the lazy rolling result to the uncached transitive
  `__is_base_of` algorithm. Its visited key used an unspecialized metadata name,
  so distinct `depends_on<...>` specializations collapsed and MPL omitted the
  rolling-window dependency. Typed `TypePtr` identity restores the correct
  materialization order without text recovery or a cache. The focused target
  clears every rolling-result assertion, and the forced full survey reaches 36
  passes with zero compile failures and only the exact five shared persistence
  failures before its 1800s bound. Cumulative instructions are +0.73%; the
  changed evaluator is absent from sampling and text-resolution counters stay
  at zero.
- `2026-07-14`: Traced the apparent base64 failure back to an incorrect imported
  virtual-base pointer adjustment in Boost.Serialization's output iterator.
  The exact translation unit retained a typed external vtable candidate and
  precise `basic_ostream` virtual-base layout, but lowering rejected that fact
  because it had not locally observed a virtual function and emitted fixed
  `+8` instead of the vtable `-24` slot lookup. The final fix admits the typed
  external layout directly and null-preserves pointer adjustment only on that
  fallback. Cache-disabled LowIR is byte-identical, the strengthened PA33
  header-free host ABI reducer passes, and no text or symbol recovery is added.
  The exact forced Accumulators survey has zero failures, its continuation exits
  successfully, and the final direct-LowIR report passes `3830 / 3830`. Suite
  1 is closed; the cursor advances to `libs/algorithm/test`.
- `2026-07-14`: The initial forced Algorithm survey had one real failure:
  `apply_permutation_test` reported 27 duplicate Boost.Test symbols at link.
  The test includes the header-only framework while its Jamfile also names the
  static framework archive, which is valid when archive extraction is correct.
  cppgm had polluted `execution_monitor.o` with a weak libc++ `basic_ios`
  destructor definition, so that member was extracted to satisfy the test
  object's destructor reference and its unrelated strong framework symbols
  collided. A non-STL reducer proved that the explicit-instantiation
  declaration was incorrectly bypassed for every empty weak member, including
  out-of-class definitions. The bypass now requires typed inline/constexpr or
  in-class definition facts. Normal and cache-disabled objects are identical,
  the rebuilt archive leaves the destructor undefined, and both the focused
  and complete forced suite pass. The repeated fixed-baseline performance gate
  is +0.88% instructions, -0.05% RSS, and +0.06% footprint; the final direct
  report passes `3830 / 3830`. Suite 2 is closed; the cursor advances to
  `libs/align/test`.
- `2026-07-14`: Cleared the pre-existing placement-audit finding in a separate
  test-only follow-on. The `__exclude_from_explicit_instantiation__` attribute
  is essential to the system-include aggregate-copy oracle, so removing it
  would weaken the test. The complete basename-matched sidecar family moved
  from `pa32:200` to PA36's `600` hosted header-emission/link-runtime cluster.
  The focused test passes, the combined PA32/PA36 direct report passes
  `176 / 176`, both placement audits report zero findings and zero hygiene
  findings, and all 23 text-reparse categories remain zero. No production code
  changed, so no performance measurement was required.
- `2026-07-14`: The initial forced Align survey had one causal compile failure:
  libc++ `alignment_of<Struct<bool>>` could not form its `alignof(_Tp)` integral
  constant. A header-free PA19 reducer proved that initial parsing and typed
  substitution were already correct and that disabling the class-info cache did
  not change the failure. The template-argument constexpr hook alone omitted
  concrete class-layout completion before the shared evaluator asked for
  alignment. Reusing the existing typed completion service fixes the uncached
  algorithm without reparsing or adding a cache. The focused and full forced
  Align runs pass, the final direct-LowIR report passes `3831 / 3831`, and the
  fixed-baseline performance result improves to +0.68% instructions. Suite 3 is
  closed; the cursor advances to `libs/any/test`.

## Next Commands

```sh
cd /Users/vishvananda/boost_1_91_0
/usr/local/bin/timeout 14400 env JOBS=1 \
  CPPGM_BOOST_B2_FRONTIER=1 \
  CPPGM_B2_CXX=/Users/vishvananda/cppgm-extended/dev/cppgm++ \
  CPPGM_B2_HOST_CC=/usr/local/opt/llvm/bin/clang \
  CPPGM_B2_HOST_CXX=/usr/local/opt/llvm/bin/clang++ \
  ./run-cppgm-b2.sh -a libs/msm/test//CompositeEuml

# After the monitored target passes and releases:
/usr/local/bin/timeout 14400 env JOBS=1 \
  CPPGM_BOOST_B2_FRONTIER=1 \
  CPPGM_B2_CXX=/Users/vishvananda/cppgm-extended/dev/cppgm++ \
  CPPGM_B2_HOST_CC=/usr/local/opt/llvm/bin/clang \
  CPPGM_B2_HOST_CXX=/usr/local/opt/llvm/bin/clang++ \
  ./run-cppgm-b2.sh -a libs/msm/test
```
