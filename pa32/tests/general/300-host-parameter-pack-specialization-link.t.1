#include "300-host-parameter-pack-specialization-link.helper.h"

template<class... Types>
int pack_sum(Types&&... values)
{
  return sizeof...(values);
}

int main()
{
  int value = 0;
  if(pack_sum(value) != 1)
    return 1;
  return host_pack_sum() == 1 ? 0 : 2;
}
