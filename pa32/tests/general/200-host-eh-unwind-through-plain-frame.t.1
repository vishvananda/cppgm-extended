#include "200-host-eh-unwind-through-plain-frame.helper.h"

int main()
{
  int expected = 17;
  int guard0 = keep_live(1, 2, 3, 4);
  int guard1 = keep_live(5, 6, 7, 8);
  try
  {
    plain_frame(expected);
  }
  catch(const Marker & marker)
  {
    return marker.value == expected && guard0 == 78 && guard1 == 182 ? 0 : 2;
  }
  return 1;
}
