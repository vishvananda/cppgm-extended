#include <deque>

int main()
{
  const char * text = "abc";
  std::deque<char> values;
  values.assign(text, text + 3);
  return values.size() == 3 &&
                 values[0] == 'a' &&
                 values[1] == 'b' &&
                 values[2] == 'c' ?
             0 :
             1;
}
