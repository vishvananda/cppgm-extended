#include <algorithm>
#include <string>

using namespace std;

enum class HeaderState
{
  Hash,
  Other
};

bool is_include(HeaderState state, const string & text)
{
  return state == HeaderState::Hash && text == "include";
}

int main()
{
  return is_include(HeaderState::Hash, "include") ? 0 : 1;
}
