name: Inception

on:
  workflow_run:
    workflows:
      - Tests
    types:
      - completed
  workflow_dispatch:
    inputs:
      selfhost_run_id:
        description: GitHub Actions run id from a successful Tests or PA38 Selfhost run
        required: true
      flavor:
        description: PA38 selfhost artifact flavor
        required: true
        default: ubuntu-24.04-gcc
        type: choice
        options:
          - ubuntu-24.04-gcc
          - ubuntu-24.04-clang-libcxx
          - ubuntu-26.04-gcc
          - ubuntu-26.04-clang-libcxx

permissions:
  actions: read
  contents: read

jobs:
  compare-cppgm-inception:
    name: compare cppgm++ inception (non-required on PRs)
    runs-on: ubuntu-24.04
    container:
      image: ${{ contains(github.event.inputs.flavor || 'ubuntu-24.04-gcc', '26.04') && 'ubuntu:26.04' || 'ubuntu:24.04' }}
    timeout-minutes: 300

    defaults:
      run:
        shell: bash

    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       (github.event.workflow_run.event == 'pull_request' ||
        (github.event.workflow_run.event == 'push' &&
         github.event.workflow_run.head_branch == 'main')))

    env:
      SELFHOST_FLAVOR: ${{ github.event.inputs.flavor || 'ubuntu-24.04-gcc' }}
      CXX: ${{ contains(github.event.inputs.flavor || 'ubuntu-24.04-gcc', 'clang-libcxx') && 'clang++' || 'g++' }}
      CPPGM_HOST_CXX: ${{ contains(github.event.inputs.flavor || 'ubuntu-24.04-gcc', 'clang-libcxx') && 'clang++' || 'g++' }}
      CPPGM_STDLIB_FLAGS: ${{ contains(github.event.inputs.flavor || 'ubuntu-24.04-gcc', 'clang-libcxx') && '-stdlib=libc++' || '' }}
      SELFHOST_RUN_ID: ${{ github.event.workflow_run.id || github.event.inputs.selfhost_run_id }}
      ARTIFACT_NAME: pa38-cppgm-self-${{ github.event.inputs.flavor || 'ubuntu-24.04-gcc' }}

    steps:
      - name: Install host compiler
        run: |
          set -euo pipefail
          packages="bash ca-certificates git make perl tar build-essential"
          if [[ "$SELFHOST_FLAVOR" == *clang-libcxx ]]; then
            packages="$packages clang libc++-dev libc++abi-dev"
          fi
          apt-get update
          apt-get install -y --no-install-recommends $packages

      - name: Download cppgm++ self compiler artifact
        uses: actions/download-artifact@v7
        with:
          name: ${{ env.ARTIFACT_NAME }}
          run-id: ${{ env.SELFHOST_RUN_ID }}
          github-token: ${{ github.token }}
          path: ${{ runner.temp }}/pa38-self-artifact

      - name: Read artifact metadata
        id: metadata
        run: |
          set -euo pipefail
          metadata="$RUNNER_TEMP/pa38-self-artifact/metadata.env"
          if [ ! -f "$metadata" ]; then
            echo "missing metadata.env in artifact" >&2
            exit 2
          fi
          source_sha=$(sed -n 's/^source_sha=//p' "$metadata" | head -n 1)
          source_repository=$(sed -n 's/^source_repository=//p' "$metadata" | head -n 1)
          if [ -z "$source_sha" ]; then
            echo "artifact metadata did not contain source_sha" >&2
            exit 2
          fi
          if [ -n "$source_repository" ] && [ "$source_repository" != "$GITHUB_REPOSITORY" ]; then
            echo "artifact came from $source_repository, expected $GITHUB_REPOSITORY" >&2
            exit 2
          fi
          printf 'source_sha=%s\n' "$source_sha" >> "$GITHUB_OUTPUT"
          cat "$metadata"

      - name: Check out matching source
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.metadata.outputs.source_sha }}

      - name: Restore cppgm++ self compiler
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/pa38-restored-self"
          tar -C "$RUNNER_TEMP/pa38-restored-self" -xzf \
            "$RUNNER_TEMP/pa38-self-artifact/pa38-cppgm-self.tar.gz"
          (cd "$RUNNER_TEMP/pa38-restored-self" && sha256sum -c SHA256SUMS)
          test -x "$RUNNER_TEMP/pa38-restored-self/bin/cppgm++-self"
          "$RUNNER_TEMP/pa38-restored-self/bin/cppgm++-self" --version || true

      - name: Show compiler configuration
        run: |
          "$CXX" --version
          printf 'CXX=%s\n' "$CXX"
          printf 'CPPGM_HOST_CXX=%s\n' "$CPPGM_HOST_CXX"
          printf 'CPPGM_STDLIB_FLAGS=%s\n' "$CPPGM_STDLIB_FLAGS"
          printf 'PA38_RESTORED_CPPGM_SELF=%s\n' "$RUNNER_TEMP/pa38-restored-self/bin/cppgm++-self"

      - name: Compare cppgm++ inception
        id: compare
        run: |
          CPPGM_OBJECT_ROOT="$GITHUB_WORKSPACE/obj/pa39/restored-self-driver" \
            make -C pa39 compare-cppgm++-inception-from-restored-self \
            PA38_RESTORED_CPPGM_SELF="$RUNNER_TEMP/pa38-restored-self/bin/cppgm++-self" \
            CPPGM_HOST_CXX="$CPPGM_HOST_CXX" \
            CPPGM_STDLIB_FLAGS="$CPPGM_STDLIB_FLAGS"

      - name: Summarize result
        if: always()
        run: |
          {
            echo "### cppgm++ inception"
            echo
            echo "- Trigger: \`${{ github.event_name }}\`"
            echo "- Selfhost run id: \`$SELFHOST_RUN_ID\`"
            echo "- Flavor: \`$SELFHOST_FLAVOR\`"
            echo "- Artifact: \`$ARTIFACT_NAME\`"
            echo "- Source SHA: \`${{ steps.metadata.outputs.source_sha }}\`"
            echo "- Compare outcome: \`${{ steps.compare.outcome }}\`"
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "${{ steps.compare.outcome }}" != "success" ] &&
             [ "${{ github.event_name }}" = "workflow_run" ] &&
             [ "${{ github.event.workflow_run.event }}" = "pull_request" ]; then
            echo "::warning::cppgm++ inception advisory failed for this PR."
          fi
